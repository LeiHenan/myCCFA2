# 预登记 — <方向名>（<一句话：本方向在赌什么>）

**登记**：<YYYY-MM-DD> ｜ **探针**：`probes/<pNN-slug>/` ｜ **对应候选**：`candidates/<slug>/`
**登记时点**：**数据采集前**（本行必须保留；事后修订只能追加到 §修订记录，并标注"数据采集后"）

> 预登记的效力来自**时点**，不是来自措辞。写完立刻提交并推送，commit 时间就是证据。

## 主假设（Headline claim）

> **<一句可被否证的话>**

若该假设不成立，本工作退化为 <什么> ⇒ 见 §杀判据。

| 项 | 内容 |
|---|---|
| **观测量** | <端到端指标 + 其**恒等式**形式，例：`Δln(吞吐) = Δln(接受长度) − Δln(每步代价)`> |
| **区间（网格）** | <每个轴的取值，**必须含极值**；并写下逐轴论证：翻转会不会发生在极值处> |
| **引擎/版本** | <引擎 + 版本 + 驱动/CUDA 前提> |
| **基线** | ① 关闭态 ② **最佳固定配置** ③ **引擎已 ship 的控制器**（不得省略）④ <先行工作> |
| **oracle 上界** | **<X>%**（S4 算出，含算法说明）；门槛 8% |
| **噪声预算** | <同配置重复极差/置信区间>（**采数前**估计） |
| **可采纳前置条件** | <什么情况下本测量无效，必须显式排除，例：`preemption==0` / 无排队 / 同 KV dtype> |
| **截止** | <日期> |

## Go 判据（**全部**满足才 Go）

1. <判据 1：效应量 + **可分辨性**（> k×噪声，k 写明）>
2. <判据 2>
3. 相对**最佳固定配置** ≥8%，且相对**已 ship 控制器**有增量
4. 必答项：<审稿人会问的那一问> 的**结构性**回答

## 三级判定（决策级 ≠ 论文级）

| 级 | 时间 | 网格 | 判据 |
|---|---|---|---|
| **T0 旋钮验证** | <半天> | 无网格 | <不动就当天杀> |
| **T1 信号探针** | <半天–1 天> | <最小网格> | <有信号 ⇒ 存活；无 ⇒ 杀> |
| **T2 增量对照** | <1–2 天> | <反转点/关键点> | **<8% ⇒ 杀**；≥8% ⇒ Go |

## 杀判据

| 条件 | 动作 |
|---|---|
| <条件 1> | 杀 |
| oracle 上界 < 8% | 杀（S4 闸门） |
| <条件 3> | 杀或降级为现象记录 |

## 扩展条款（预登记，**改动即事后调整**）

1. <触发条件 ⇒ 允许扩什么，且必须在同一预登记下>
2. <不允许自动扩什么>

## 已知的反对证据 / 拥挤邻居（必须写入结论，不得省略）

- **引擎侧（最强基线）**：<引擎里已 ship 的相关旋钮/调度器>
- **学术侧**：<≥3 个最近邻，标注读过的页/节>

## 修订记录

| 日期 | 变更 | 时点 |
|---|---|---|
| <YYYY-MM-DD> | 初版 | 数据采集前 |
