# 服务器工作交接单（`ssh -p 38300`，RTX 4080 SUPER 32GB）

**日期**：2026-09-15 ｜ **状态**：**已按用户指示停机**（GPU 1 MiB / 0% util，无我方进程）

---

## 1. 机器上的资产（全部在 `/root/autodl-tmp`，数据盘，重启保留）

| 路径 | 内容 | 大小 |
|---|---|---|
| `models/Qwen3-4B` | **dense** 参考模型 | 8.9 G |
| `models/Qwen3.5-4B` | **hybrid** 目标（24 linear + 8 full，`Qwen3_5ForConditionalGeneration`） | 13 G |
| `models/Falcon-H1-3B` | **第二 hybrid 家族**（`FalconH1ForCausalLM`） | 8.5 G |
| `models/eagle3-qwen3-4b` | EAGLE3 draft head（`Eagle3LlamaForCausalLM`，hidden 2560） | 417 M |
| `models/dflash2` | DFlash2 drafter（**注意：vLLM 方法名是 `dflash`，不是 `dflash2`**） | 2.1 G |
| `*.py` 探针 | 本轮全部脚本（c1…c1i、e4 系列、e2e_grammar…） | — |
| `*.json` | **33 份**原始结果 | — |
| `pipcache/` `tmp/` | 已配置的镜像与临时目录 | — |

**所有结论与原始 JSON 已同步到 `github.com/LeiHenan/myCCFA2`**（HEAD `0b015c39`）；
机器上的 33 份 JSON 是**冗余副本**，即使实例释放也不丢结论。

## 2. 环境配方（重建时需要，均踩过坑）

```bash
# 复用系统自带 torch 2.12.1+cu130，避开 ~4GB 下载；只装 vLLM 本体
/root/miniconda3/bin/pip install --no-deps -i http://mirrors.aliyun.com/pypi/simple vllm
# 缺的依赖（逐个补，共 10 余个）：cbor2, openai, cachetools, pyarrow,
#   prometheus_fastapi_instrumentator, model_hosting_container_standards, ...
# 必需环境变量
export VLLM_USE_FLASHINFER_SAMPLER=0      # 本机无 flashinfer 包
export VLLM_ATTENTION_BACKEND=FLASH_ATTN
# 内存预算：单进程最多 2 个 LLM 实例 → --gpu-util 0.42；三实例必挂
# max_model_len：4096 以上 + hybrid 会 KV 不足 → 用 1024~4096
```

## 3. **不要重犯的坑**（本轮全部踩过，逐条有据）

| # | 坑 | 后果 |
|---|---|---|
| 1 | 用 **OpenAI server + 48 次串行 HTTP** 测吞吐 | **放弃 continuous batching**，吞吐虚低 3–20×（61 vs 1200 tok/s）。**必须用离线 `llm.generate(list)` 批量提交** |
| 2 | `pkill -f vllm` 写在**同一条命令**里 | 模式匹配到该命令自己的 shell ⇒ **自杀**，静默零输出（踩了两次） |
| 3 | `setsid nohup` / `screen` 做持久化 | 该容器上**均不持久化**（`/run` 是临时的）；**用 harness 的 background job**（保持 SSH 连接）才可靠 |
| 4 | 单次命令窗口 | 约 10 分钟；n=140 会超时，**n=40 是上限** |
| 5 | `method="dflash2"` | pydantic 拒绝；合法值是 `dflash`/`dspark` |
| 6 | 让 drafter 用 `scale=0.01` 造 dummy LoRA | ΔW≈0.047 ⇒ **不改变输出**，所有"无效应"结论都是假的 |
| 7 | 判定式写成 `A == B` | 在 `interpretable=false`（各臂输出全同）时会**假阳性**；必须 `interpretable AND (A==B)` |
| 8 | 用 token id **0** 做碰撞测试 | 0 正是 vLLM 的掩码哨兵 ⇒ 两臂本就等价 |
| 9 | 用**合成 token id** 当语料 | 伪造出真实文本上不存在的效应（见 `quantile-speedup` 的撤回） |
| 10 | Falcon-H1 + prefix caching + 投机 | 崩在 `page_size_padded >= page_size`；**属上游 OPEN PR #47635**，非我方问题 |

## 4. 未完成的工作（按优先级）

| 优先 | 事项 | 为什么值得做 | 卡在哪 |
|---|---|---|---|
| **1** | **拆步计时**：把 hybrid 投机的那项"与接受率无关的成本"定位到 file:line | C-1i 里 hybrid 的 `A_actual/A* = 2.35` 按模型应赢却输 ⇒ 必然有第三项成本 | 未开始 |
| **2** | **第二个 hybrid 家族** | 我自设闸门要求 ≥2 家族；Falcon-H1 被上游 bug 封死，需找不触发断言者（Zamba2-2.7B / Nemotron-H / GraniteMoeHybrid 可能同样中招，**先试**） | 未开始 |
| 3 | C-3 用**更好的检测器**重做 | 上一轮枚举器只找到 1 个键路径站点（已证明良性） | **本轮没做**（时间投在 C-1） |
| 4 | C-2 结构化输出并发代价 | 微基准 15.6×，端到端 conc≤8 无代价 | **依赖封死**：`lmformatenforcer` 在 PyPI 上 404，且 `backend=xgrammar` 仍强行 import 它 |

## 5. 本轮唯一有分量的结果（可直接引用）

> **hybrid 目标上，投机解码的接受率更高（0.687 vs dense 0.573）、平均接受长度更长
> （3.06 vs 2.72），但每步成本乘数相当（1.34 vs 1.29）而吞吐更低（0.794× vs 1.043×）。**
> **⇒ "接受率高 ⇒ 加速多"在 hybrid 上不成立。**

支撑：C-1b/d/h/i；反例衬底：C-1e（其归一化口径后被判错，原始数据仍有效）；
**上游对照**：#54691（DFlash + 185k，drafter 扫全长 KV）——**机制不同**，我用 ngram（无 drafter KV）。

**距离立项还差**：① 上述未完成事项 1 与 2；② 未检索是否已有人报过 "ngram + hybrid"。
