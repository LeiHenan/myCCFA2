FETCHED: https://github.com/flashinfer-ai/flashinfer/issues/3626  (is_pr=True, 775955 bytes)
LAYOUT NODE (PR):
  number = 3626
  title = feat: add FP8 per-token-head KV cache with inline float32 scales
  state = CLOSED
  closedTime = 2026-09-11T15:50:40Z
  createdTime = 2026-06-13T04:12:08Z
  mergedTime = None
  mergedBy = None
  commitsCount = 1
  baseBranch = main
  headBranch = fp8pth
  author = ir1ka

########## RENDERED TIMELINE EVENTS ##########
EV: js-timeline-progressive-focus-container" data-gid="RRE_lADOJ-UyCM8AAAABFWsVMc8AAAAGN55MpQ"> | ir1ka | requested review from | aleozlx, | bkryu, | cyx-6, | dhiraj113, | jiahanc, | jimmyzho, | kahyunnam, | nv-yunzheq, | qsang-nv, | saltyminty, | samuellees, | sricketts, | yongwww, | yyihuang and | yzh119 | as code owners | June 13, 2026 04:12 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="IC_kwDOJ-UyCM8AAAABF_1-Rg"> | coderabbitai | Bot | commented | Jun 13, 2026 | • | edited | Loading | Uh oh! | There was an error while loading. Please reload this page. | Copy link | <clipboard-copy role="menuitem" value="<!-- This is an auto-generated comment: summarize by coderabbit.ai --> | <!-- review_stack_entry_start --> | [![Review Change Stack](https://storage.googleapis.com/coderabbit_public_assets/review-stack-in-coderabbit-ui.svg)](https://app.coderabbit.ai/change-stack/flashinfer-ai/flashinfer/pull/3626?utm_source=github_walkthroug
EV: js-timeline-progressive-focus-container" data-gid="LE_lADOJ-UyCM8AAAABFWsVMc8AAAAGN55cIA"> | flashinfer-bot | added | the | op: attention | label | Jun 13, 2026 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="PRR_kwDOJ-UyCM8AAAABC63D5w"> | gemini-code-assist | Bot | reviewed | Jun 13, 2026 | View reviewed changes | gemini-code-assist | Bot | left a comment | Copy link | Copy Markdown | Contributor | There was a problem hiding this comment. | <path d="M3.72 3.72a.75.75 0 0 1 1.06 0L8 6.94l3.22-3.22a.749.749 0 0
EV: js-timeline-progressive-focus-container" data-gid="HRFPE_lADOJ-UyCM8AAAABFWsVMc8AAAAGN5_G9A"> | ir1ka | force-pushed | the | fp8pth | branch | from | 6c411f4 to | 184045f | Compare | June 13, 2026 04:18 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="PRR_kwDOJ-UyCM8AAAABC631Bg"> | coderabbitai | Bot | reviewed | Jun 13, 2026 | View reviewed changes | coderabbitai | Bot | left a comment | Copy link | <clipboard-copy role="menuitem" value="**Actionable comments posted: 11** | > [!CAUTION] | > Some comments are outside the diff and can’t be posted inline due to platform limitations. | > | > | > | > <details> | > <summary>⚠️ Outside diff range comments (3)</summary><blockquote> | > | > <details> | > <summary>flashinfer/decode.py (2)</summary><blockquote> | > | > `109-124`: _⚠️ Potential issue_
EV: js-timeline-progressive-focus-container" data-gid="PRR_kwDOJ-UyCM8AAAABC64TKQ"> | coderabbitai | Bot | reviewed | Jun 13, 2026 | View reviewed changes | coderabbitai | Bot | left a comment | Copy link | <clipboard-copy role="menuitem" value="**Actionable comments posted: 2** | <details> | <summary>🤖 Prompt for all review comments with AI agents</summary> | ``` | Verify each finding against current code. Fix only still-valid issues, skip the | rest with a brief reason, keep changes minimal, and validate. | Inline comments: | In `@flashinfer/decode.py`: | - Line 524: Remove the spurious trailing
EV: js-timeline-progressive-focus-container" data-gid="HRFPE_lADOJ-UyCM8AAAABFWsVMc8AAAAGN8JbzQ"> | ir1ka | force-pushed | the | fp8pth | branch | 3 times, most recently | from | 1c9e5e8 to | eab2d5b | Compare | June 14, 2026 07:57 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="PRR_kwDOJ-UyCM8AAAABC8fi5A"> | coderabbitai | Bot | reviewed | Jun 14, 2026 | View reviewed changes | coderabbitai | Bot | left a comment | Copy link | <clipboard-copy role="menuitem" value=" | <details> | <summary>🧹 Nitpick comments (1)</summary><blockquote> | <details> | <summary>include/flashinfer/attention/decode.cuh (1)</summary><blockquote> | `208-221`: _💤 Low value_ | **Consider gating scale loads on a single thread per warp.** | All `bdx` threads redundantly load the same scale values since the destination `scale_smem` offset depends o
EV: js-timeline-progressive-focus-container" data-gid="HRFPE_lADOJ-UyCM8AAAABFWsVMc8AAAAGOMZbMw"> | ir1ka | force-pushed | the | fp8pth | branch | 2 times, most recently | from | 9d62b7d to | c43f73c | Compare | June 14, 2026 14:17 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="C_kwDOPxbGXdoAKDE5MmExOTBhMGRmYmJhMzhkN2UwNzBiYjcxNTExZjcyNzA2MTZkNWY"> | feat: add FP8 per-token-head KV cache with inline float32 scales | … | 192a190 | Implement FP8 per-token-head KV cache where a float32 scale is stored inline | immediately after each head's FP8 data, with stride padded to head_dim + 16 | (16B aligned). Covers prefill and decode for single/batch, paged/ragged, | MHA/GQA. | Kernel changes: | - Add AttentionVariant::use_per_token_head flag for compile-time scale-aware | dispatch | - Prefill: apply per-row scales in compute_
EV: js-timeline-progressive-focus-container" data-gid="HRFPE_lADOJ-UyCM8AAAABFWsVMc8AAAAGOQ4d4g"> | ir1ka | force-pushed | the | fp8pth | branch | from | c43f73c to | 192a190 | Compare | June 14, 2026 14:36 | ir1ka | marked this pull request as draft | June 15, 2026 16:24 | <div class="
EV: js-timeline-progressive-focus-container" data-gid="CE_lADOJ-UyCM8AAAABFWsVMc8AAAAHNrxkhg"> | ir1ka | closed this | Sep 11, 2026 | --> | Sign up for free | to join this conversation on GitHub. | Already have an account? | Sign in to comment | --><form id="pull-request-reviewers-form-3858972721" class="js-issue-sidebar-form" aria-label="Select reviewers" data-reviewers-team-size-check-url="/flashinfer-ai/flashinfer/pull/3626/review-requests/team-size-check" data-turbo="false" action="/flashinfer-ai/flashinfer/pull/3626/review-requests" accept-charset="U

########## RENDERED TIMELINE (markdown blocks) ##########

[RENDERED BLOCK author=ir1ka date=2026-06-13T04:12:08Z]
Implement FP8 per-token-head KV cache where a float32 scale is stored inline immediately after each head's FP8 data, with stride padded to head_dim + 16 (16B aligned). Covers prefill and decode for single/batch, paged/ragged, MHA/GQA.

Kernel changes:

- Add AttentionVariant::use_per_token_head flag for compile-time scale-aware dispatch

- Decode: multiply float32 scales after cast_load in compute_qk and update_local_state, with async scale load pipelined alongside KV

- page_produce_kv_sf_pth / produce_kv_sf_pth: load inline scales from global memory into shared memory (requires sizeof(DTypeKV) == 1)

- Conditional scale smem via empty-base-class inheritance to avoid SM75 head_dim=256 overflow (49,152B standard vs 45,148B FP8-PTH)

Launcher changes:

- batch_attention plan: resolve kernel pointer at plan time via get_persistent_kernel to pass smem_size to TwoStageHolisticPlan

Python API:

- Add use_per_token_head param to single_prefill/decode, batch prefill/decode, and BatchAttention wrappers; forces backend="fa2" when enabled

- Update JIT URI and AOT generator to emit per-token-head variants for FP8 KV

Other:

- scheduler.cuh: fix int64→uint32 narrowing with static_cast for CUDA 13

- GCC 14 (PartitionPagedKVCacheBinarySearchMinNumPagePerBatch)

Tests: all pass across 10 files (include 4 new files) covering jit/non-jit, paged/ragged, fp16/bf16, fp8_e4m3, MHA/GQA, head_dim 64/128/256, NHD/HND, ROPE_LLAMA, causal and large-batch.

📌 Description

Add fp8 per-token-head inline scale kv cache quantization.

🔍 Related Issues

#3617

🚀 Pull Request Checklist

Thank you for contributing to FlashInfer! Before we review your pull request, please make sure the following items are complete.

✅ Pre-commit Checks

- I have installed pre-commit by running pip install pre-commit (or used your preferred method).

- I have installed the hooks with pre-commit install.

- I have run the hooks manually with pre-commit run --all-files and fixed any reported issues.

If you are unsure about how to set up pre-commit, see the pre-commit documentation.

🧪 Tests

- Tests have been added or updated as needed.

- All tests are passing (unittest, etc.).

Reviewer Notes

Summary by CodeRabbit

Summary by CodeRabbit

- 
New Features

- Added an FP8 per‑token‑head (PTH) scaling option across single/batch prefill, single/batch decode, and BatchAttention via a boolean flag.

- 
Improvements

- JIT now generates and dispatches dedicated PTH kernel variants and adjusts shared-memory/launch parameters accordingly.

- Public APIs accept scalar float K/V FP8 dequantization scales.

- When PTH is enabled, backend selection/validation is enforced (including rejecting unsupported backends).

- 
Tests

- Added GPU-focused correctness/accuracy test coverage for PTH, plus SM capability guards and backend assertion checks.

[RENDERED BLOCK author=? date=?]
Note

Reviews paused

It looks like this branch is under active development. To avoid overwhelming you with review comments due to an influx of new commits, CodeRabbit has automatically paused this review. You can configure this behavior by changing the reviews.auto_review.auto_pause_after_reviewed_commits setting.

Use the following commands to manage reviews:

- @coderabbitai resume to resume automatic reviews.

- @coderabbitai review to trigger a single review.

Use the checkboxes below for quick actions:

- ▶️ Resume reviews

- 🔍 Trigger review

📝 Walkthrough
Walkthrough

Adds a compile-time/runtime use_per_token_head dimension and threads it through dispatch macros, CUDA kernels (prefill/decode/persistent), planner/occupancy, JIT kernel generation, C++/Python APIs, and tests to enable FP8 per-token-head KV-cache scaling with inline per-token scales.

Changes

FP8 Per-Token-Head KV-Cache Scaling

Layer / File(s)
Summary

Dispatch macros and type infrastructure 
 include/flashinfer/utils.cuh, csrc/batch_attention_customize_config.jinja, csrc/batch_decode_customize_config.jinja, csrc/single_decode_customize_config.jinja, csrc/batch_prefill_customize_config.jinja, csrc/single_prefill_customize_config.jinja

Adds DISPATCH_USE_PER_TOKEN_HEAD macro for FP8-conditional dispatch, introduces is_fp8_type<T> type trait specializations for __nv_fp8_e4m3 and __nv_fp8_e5m2, and updates all DISPATCH_context macros to route AttentionVariant alias and compute invocations through per-token-head dispatch wrapper.

API signature updates 
 csrc/batch_attention.cu, csrc/batch_attention_jit_binding.cu, csrc/batch_decode.cu, csrc/batch_decode_jit_binding.cu, csrc/batch_prefill.cu, csrc/batch_prefill_jit_binding.cu, csrc/single_decode.cu, csrc/single_decode_jit_binding.cu, csrc/single_prefill.cu, csrc/single_prefill_jit_binding.cu, flashinfer/attention/_core.py, flashinfer/decode.py, flashinfer/prefill.py

Extends function signatures with use_per_token_head parameter across batch/single decode, batch/single prefill, batch attention, and JIT FFI bindings; narrows k_scale/v_scale type annotations from Optional[torch.Tensor] to Optional[float] in Python APIs.

Persistent kernel selection refactor 
 csrc/batch_attention.cu, include/flashinfer/attention/persistent.cuh, include/flashinfer/attention/scheduler.cuh

Replaces fixed persistent-kernel instantiation with dynamic selection via GetBatchPagedAttentionPersistentKernel<CTA_TILE_Q_1, ...>(), introduces DISPATCH_KV_REPACK macro for compute-capacity-based FORCE_DISABLE_KV_REPACK selection, and refactors TwoStageHolisticPlan to accept explicit kernel pointer + launch parameters instead of recomputing locally.

KV-repack control 
 csrc/batch_prefill.cu, csrc/single_prefill.cu, include/flashinfer/attention/prefill.cuh

Adds FORCE_DISABLE_KV_REPACK template parameter to prefill dispatchers, introduces kUseKvRepack trait for compute-capacity/head-dimension-aware repack enablement, threads repack control through ragged/paged batch prefill and single prefill dispatch paths.

Decode kernels with per-token scaling 
 include/flashinfer/attention/decode.cuh

Adds per-token K and V scaling to decode device kernels: allocates conditional PTH scale shared-memory buffers, implements load_kv_sf_pth async loader with predicate guards, extends compute_qk/update_local_state to accept PTH scale pointers and apply per-row scaling, updates single/paged decode dispatchers' dynamic shared-memory sizing.

Prefill kernels with per-token scaling 
 include/flashinfer/attention/prefill.cuh

Adds PTH scaling infrastructure: pth_scale_frag_inplace scaling helper, PthScaleSmemStorage shared-memory layout, Fp4ScaleSmemStorage/KvSmemRepackStorage refactored accessors, new PTH scale loaders (page_produce_kv_sf_pth, produce_kv_sf_pth) for both paged/ragged layouts, extends compute_qk/compute_sfm_v/repack_fp8_tile signatures with PTH parameters, updates single/ragged/paged prefill pipelines to load PTH scales and pass pointers into compute stages.

Persistent kernel PTH implementation 
 include/flashinfer/attention/persistent.cuh

Implements per-token-head path in persistent batch-attention: conditionally builds per-work-item pth_indptr from KV indirection and block size, rewires paged K/V shared-fragment production to use page_produce_kv_sf_pth instead of page_produce_kv_sf, updates compute_qk/compute_sfm_v invocations to read K/V shared-fragments from k_sf_pth_ptr()/v_sf_pth_ptr() accessors across initial, mask-loop, and post-loop stages.

Batch POD and variant infrastructure 
 include/flashinfer/attention/batch_pod.cuh, include/flashinfer/attention/variants.cuh, include/flashinfer/attention/variant_helper.cuh

Updates BatchPODWithKVCacheTensorDispatched prefill/decode KernelTraits instantiations with new template argument after POS_ENCODING_MODE; extends DefaultAttention with UsePerTokenHead template parameter and corresponding use_per_token_head static flag; adds use_per_token_head = false constant to AttentionVariantBase.

JIT kernel generation 
 flashinfer/jit/attention/modules.py

Updates JIT code generators to emit per-use_per_token_head-value kernel files: introduces _use_pth_literal() selector ([False, True] for FP8 dtypes, [False] otherwise), modifies gen_customize_* functions to render/write pth-indexed files (e.g., *_kernel_pth_{0|1}.cu), threads use_per_token_head into kernel instantiation templates for single/batch decode, single/batch prefill (fa2 paths), and batch attention.

Python wrapper APIs 
 flashinfer/attention/_core.py, flashinfer/decode.py, flashinfer/prefill.py

Extends BatchAttention.__init__, single_decode_with_kv_cache, BatchDecodeWithPagedKVCacheWrapper.__init__, single_prefill_with_kv_cache, BatchPrefillWithPagedKVCacheWrapper.__init__, and BatchPrefillWithRaggedKVCacheWrapper.__init__ with use_per_token_head parameter; stores flag and validates fa2-only backend requirement when enabled; threads parameter into underlying module/kernel invocations; updates docstrings 

[RENDERED BLOCK author=? date=2026-06-13T04:15:55Z]
Code Review

This pull request introduces support for FP8 per-token-head (PTH) inline scales across FlashInfer's attention kernels, including batch attention, single/batch decode, and single/batch prefill. The changes span CUDA kernel implementations, JIT bindings, Python API wrappers, and comprehensive unit tests. The review feedback highlights opportunities to improve safety and performance: specifically, guarding the else branches in page_produce_kv_sf_pth and produce_kv_sf_pth with if (smem_in_bounds) to avoid passing out-of-bounds shared memory pointers, and vectorizing the slow Python loops used to copy scale bytes in the test suites.

 Important
The consumer version of Gemini Code Assist on GitHub is being sunset. Starting June 18, 2026, new organization installations will be blocked, and all code review activity will officially cease on July 17, 2026.

For more details on the timeline and next steps, please review the Help Documentation.

[RENDERED BLOCK author=? date=?]
Actionable comments posted: 11

 Caution
Some comments are outside the diff and can’t be posted inline due to platform limitations.

⚠️ Outside diff range comments (3)

flashinfer/decode.py (2)
109-124: ⚠️ Potential issue | 🟠 Major | ⚡ Quick win

Mirror the new flag in single_decode_with_kv_cache_with_jit_module().

This raw JIT run ABI now takes use_per_token_head before the existing extra arguments, but single_decode_with_kv_cache_with_jit_module() still forwards *args immediately after window_left. Existing users of that helper will have every optional argument shifted by one slot. Add a named use_per_token_head: bool = False parameter there and pass it explicitly before *args.

🤖 Prompt for AI Agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@flashinfer/decode.py` around lines 109 - 124, The helper
single_decode_with_kv_cache_with_jit_module() needs to add a new parameter
use_per_token_head: bool = False and ensure it is passed explicitly into the raw
JIT run ABI before the existing extra-args; update the function signature to
include use_per_token_head: bool = False and change the call site that invokes
run_func(...) to pass use_per_token_head immediately after window_left (i.e.
before forwarding *args) so subsequent optional arguments keep their original
positions.

1628-1705: ⚠️ Potential issue | 🔴 Critical | ⚡ Quick win

Do not append use_per_token_head to the TRT-LLM decode ABI.

self._cached_module.paged_run for the trtllm-gen path still expects maybe_custom_mask immediately after enable_pdl. Inserting self._use_per_token_head here shifts every following positional argument (logits_soft_cap, sm_scale, workspace_size, paged_kv_cache, ...) by one slot, so the backend breaks even when the flag is False. Gate this bool to the FA2 paged-run ABI only, or reject the feature before entering the TRT-LLM path.

Minimal direction for the fix
 run_args = [
 self._float_workspace_buffer,
 self._int_workspace_buffer,
 self._plan_info,
 q,
 k_cache,
 v_cache,
 self._qo_indptr_buf,
 self._paged_kv_indptr_buf,
 self._paged_kv_indices_buf,
 self._paged_kv_last_page_len_buf,
 out,
 lse,
 MaskMode.NON_CAUSAL.value,
 TensorLayout[self._kv_layout].value,
 window_left,
 enable_pdl,
- self._use_per_token_head,
 ]
+ if self._backend == "fa2":
+ run_args.append(self._use_per_token_head)

🤖 Prompt for AI Agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@flashinfer/decode.py` around lines 1628 - 1705, The bug is that
self._use_per_token_head is appended to run_args unconditionally, shifting
positional params for the trtllm-gen paged_run ABI; fix by only adding
self._use_per_token_head for the FA2/prefill ABI (i.e., when self._backend !=
"trtllm-gen") or by explicitly gating it inside the non-TRT branch: move the
inclusion of self._use_per_token_head out of the shared run_args tail and into
the branch that builds args for the non-TRT paged_run (or alternatively
raise/reject when self._use_per_token_head is set and self._backend ==
"trtllm-gen"); touch the run_args construction around the else block that
currently appends the long list (referencing run_args, self._use_per_token_head,
and the self._backend == "trtllm-gen" conditional) so the TRT path retains the
original positional layout expected by paged_run.

include/flashinfer/attention/decode.cuh (1)
856-867: ⚠️ Potential issue | 🟠 Major

Guard zero-occupancy before computing kv_chunk_size in SingleDecodeWithKVCacheDispatched

cudaOccupancyMaxActiveBlocksPerMultiprocessor can return 0 for num_blocks_per_sm, making max_num_kv_chunks == 0 and then ceil_div(seq_len, max_num_kv_chunks) divides by zero (ceil_div is (x + y - 1) / y). The later nblks.x == 0 check doesn’t prevent this.

- File: include/flashinfer/attention/decode.cuh (around the kv_chunk_size computation)

Suggested fix
 FLASHINFER_CUDA_CALL(cudaOccupancyMaxActiveBlocksPerMultiprocessor(
 &num_blocks_per_sm, kernel, num_threads, smem_size));
+ if (num_blocks_per_sm < 1) {
+ FLASHINFER_ERROR(
+ "The decode kernel requires too much shared memory or registers for this launch "
+ "configuration.");
+ }
 uint32_t max_grid_size = uint32_t(num_blocks_per_sm) * uint32_t(num_sm);

🤖 Prompt for AI Agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@include/flashinfer/attention/decode.cuh` around lines 856 - 867, The code in
SingleDecodeWithKVCacheDispatched uses
cudaOccupancyMaxActiveBlocksPerMultiprocessor which can return
num_blocks_per_sm==0 and leads to a divide-by-zero when computing kv_chunk_size;
fix by guarding the occupancy result before the division: after calling
cudaOccupancyMaxActiveBlocksPerMultiprocessor, if num_blocks_per_sm==0 set
num_blocks_per_sm=1 (or compute max_num_kv_chunks = max(1u,
uint32_t(num_blocks_per_sm) * uint32_t(num_sm) / num_kv_heads)) so
max_num_kv_chunks is never zero, then compute kv_chunk_size =
max(ceil_div(seq_len, max_num_kv_chunks), 256) and proceed. Update
SingleDecodeWithKVCacheDispatched's occupancy->max_num_kv_chunks/kv_chunk_size
logic accordingly.

🧹 Nitpick comments (5)

tests/attention/test_batch_attention_per_tensor.py (1)
45-62: ⚡ Quick win

Use the centralized threshold function instead of hardcoding the threshold.

The mode parameter is passed but unused, and the threshold is hardcoded at line 57. The get_cos_sim_threshold function was added to tests/utils_fp8.py specifically to centralize threshold management. Import and use it for consistency and maintainability.

♻️ Proposed refactor
Add the import at the top of the file:

 import pytest
 import torch
 import flashinfer
-from tests.utils_fp8 import to_float8
+from tests.utils_fp8 import to_float8, get_cos_sim_threshold

Then update the check_accuracy function:

 def check_accuracy(
 o_ref: t

[RENDERED BLOCK author=? date=?]
Actionable comments posted: 2

🤖 Prompt for all review comments with AI agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

Inline comments:
In `@flashinfer/decode.py`:
- Line 524: Remove the spurious trailing comma from the docstring type
annotation for the parameter use_per_token_head (change "use_per_token_head :
bool," to "use_per_token_head : bool") in both docstring occurrences so the RST
param type is correct; keep the actual function signature comma in code
unchanged.
- Around line 1224-1227: Replace the fragile assert in the user-facing
validation with an explicit check that raises ValueError: instead of the assert
in the method containing the symbols self._use_per_token_head and self._backend
(the plan API), add an if that tests if self._use_per_token_head is True and
self._backend != "fa2" and then raise ValueError with the same message (e.g.,
f"The per token head is incompatible with {self._backend}").

🪄 Autofix (Beta)
Fix all unresolved CodeRabbit comments on this PR:

- Push a commit to this branch (recommended)

- Create a new PR with the fixes

ℹ️ Review info

⚙️ Run configuration
Configuration used: defaults

Review profile: CHILL

Plan: Pro

Run ID: 86669cff-b92b-4f02-8d9c-b4ce1f5559a3

📥 Commits
Reviewing files that changed from the base of the PR and between 6c411f4 and 184045ffc29e3dd9fc7e57b37ec6f3157a16407b.

📒 Files selected for processing (43)

- csrc/batch_attention.cu

- csrc/batch_attention_customize_config.jinja

- csrc/batch_attention_jit_binding.cu

- csrc/batch_attention_paged_kernel_inst.jinja

- csrc/batch_decode.cu

- csrc/batch_decode_customize_config.jinja

- csrc/batch_decode_jit_binding.cu

- csrc/batch_decode_kernel_inst.jinja

- csrc/batch_prefill.cu

- csrc/batch_prefill_customize_config.jinja

- csrc/batch_prefill_jit_binding.cu

- csrc/batch_prefill_paged_kernel_inst.jinja

- csrc/batch_prefill_ragged_kernel_inst.jinja

- csrc/single_decode.cu

- csrc/single_decode_customize_config.jinja

- csrc/single_decode_jit_binding.cu

- csrc/single_decode_kernel_inst.jinja

- csrc/single_prefill.cu

- csrc/single_prefill_customize_config.jinja

- csrc/single_prefill_jit_binding.cu

- csrc/single_prefill_kernel_inst.jinja

- flashinfer/attention/_core.py

- flashinfer/decode.py

- flashinfer/jit/attention/modules.py

- flashinfer/prefill.py

- include/flashinfer/attention/batch_pod.cuh

- include/flashinfer/attention/decode.cuh

- include/flashinfer/attention/persistent.cuh

- include/flashinfer/attention/pod.cuh

- include/flashinfer/attention/prefill.cuh

- include/flashinfer/attention/scheduler.cuh

- include/flashinfer/attention/variant_helper.cuh

- include/flashinfer/attention/variants.cuh

- include/flashinfer/utils.cuh

- tests/attention/test_batch_attention.py

- tests/attention/test_batch_attention_per_tensor.py

- tests/attention/test_batch_attention_per_token_head.py

- tests/attention/test_batch_per_token_head.py

- tests/attention/test_batch_prefill.py

- tests/attention/test_batch_prefill_kernels.py

- tests/attention/test_single_per_token_head.py

- tests/attention/test_single_prefill.py

- tests/utils_fp8.py

💤 Files with no reviewable changes (17)

- include/flashinfer/attention/variants.cuh

- include/flashinfer/attention/variant_helper.cuh

- include/flashinfer/attention/pod.cuh

- tests/utils_fp8.py

- tests/attention/test_batch_prefill.py

- tests/attention/test_batch_prefill_kernels.py

- tests/attention/test_single_prefill.py

- tests/attention/test_batch_attention_per_token_head.py

- include/flashinfer/utils.cuh

- tests/attention/test_batch_attention.py

- tests/attention/test_batch_attention_per_tensor.py

- include/flashinfer/attention/persistent.cuh

- tests/attention/test_single_per_token_head.py

- include/flashinfer/attention/scheduler.cuh

- tests/attention/test_batch_per_token_head.py

- include/flashinfer/attention/decode.cuh

- include/flashinfer/attention/prefill.cuh

🚧 Files skipped from review as they are similar to previous changes (25)

- csrc/single_decode_customize_config.jinja

- csrc/single_decode_kernel_inst.jinja

- csrc/batch_decode_kernel_inst.jinja

- csrc/batch_decode_customize_config.jinja

- csrc/single_prefill_customize_config.jinja

- csrc/single_decode_jit_binding.cu

- csrc/batch_prefill_jit_binding.cu

- include/flashinfer/attention/batch_pod.cuh

- csrc/batch_prefill_customize_config.jinja

- csrc/batch_attention_jit_binding.cu

- csrc/batch_decode_jit_binding.cu

- csrc/batch_decode.cu

- csrc/batch_prefill_paged_kernel_inst.jinja

- csrc/single_prefill_jit_binding.cu

- csrc/single_prefill.cu

- csrc/batch_prefill_ragged_kernel_inst.jinja

- flashinfer/attention/_core.py

- csrc/single_prefill_kernel_inst.jinja

- csrc/batch_attention_paged_kernel_inst.jinja

- csrc/single_decode.cu

- csrc/batch_attention_customize_config.jinja

- csrc/batch_prefill.cu

- csrc/batch_attention.cu

- flashinfer/jit/attention/modules.py

- flashinfer/prefill.py

[RENDERED BLOCK author=? date=?]
🧹 Nitpick comments (1)

include/flashinfer/attention/decode.cuh (1)
208-221: 💤 Low value

Consider gating scale loads on a single thread per warp.

All bdx threads redundantly load the same scale values since the destination scale_smem offset depends only on (ty, tz), not tx. While this is correct (all threads write the same value), it issues bdx redundant global memory loads per scale.

Gating on tx == 0 would reduce memory traffic:

♻️ Suggested optimization
 template <uint32_t tile_size_per_bdx, typename AttentionVariant, typename IsValidFn,
 typename GetScalePtrFn>
 __device__ __forceinline__ void load_kv_sf_pth(float* scale_smem, IsValidFn is_valid,
- GetScalePtrFn get_scale_ptr) {
+ GetScalePtrFn get_scale_ptr, uint32_t tx) {
 if constexpr (AttentionVariant::use_per_token_head) {
+ if (tx == 0) {
 `#pragma` unroll
- for (uint32_t j = 0; j < tile_size_per_bdx; ++j) {
- cp_async::pred_load_32b<cp_async::SharedMemFillMode::kFillZero>(
- reinterpret_cast<uint32_t*>(scale_smem),
- reinterpret_cast<const uint32_t*>(get_scale_ptr(j)), is_valid(j));
- ++scale_smem;
+ for (uint32_t j = 0; j < tile_size_per_bdx; ++j) {
+ cp_async::pred_load_32b<cp_async::SharedMemFillMode::kFillZero>(
+ reinterpret_cast<uint32_t*>(scale_smem),
+ reinterpret_cast<const uint32_t*>(get_scale_ptr(j)), is_valid(j));
+ ++scale_smem;
+ }
 }
 }
 }

🤖 Prompt for AI Agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@include/flashinfer/attention/decode.cuh` around lines 208 - 221, In the
load_kv_sf_pth function, all bdx threads are redundantly issuing the same global
memory loads to the same destination address since the scale_smem offset depends
only on (ty, tz) coordinates and not on tx. To optimize this, add a thread-level
gate to the loop that performs the cp_async::pred_load_32b call, checking that
tx equals 0 before executing the load operation. This ensures only one thread
per warp loads each scale value, eliminating redundant memory traffic while
preserving correctness since all threads would write identical data anyway.

🤖 Prompt for all review comments with AI agents
Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

Nitpick comments:
In `@include/flashinfer/attention/decode.cuh`:
- Around line 208-221: In the load_kv_sf_pth function, all bdx threads are
redundantly issuing the same global memory loads to the same destination address
since the scale_smem offset depends only on (ty, tz) coordinates and not on tx.
To optimize this, add a thread-level gate to the loop that performs the
cp_async::pred_load_32b call, checking that tx equals 0 before executing the
load operation. This ensures only one thread per warp loads each scale value,
eliminating redundant memory traffic while preserving correctness since all
threads would write identical data anyway.

ℹ️ Review info

⚙️ Run configuration
Configuration used: defaults

Review profile: CHILL

Plan: Pro

Run ID: 6c83aa2c-b3c2-426f-bfff-40cb9c28a52b

📥 Commits
Reviewing files that changed from the base of the PR and between 1c9e5e83de6d8f3f951e964b7f8a722b08cffd76 and eab2d5b4d8ec2ff7dfe5f3e80f301ca6c334a628.

📒 Files selected for processing (43)

- csrc/batch_attention.cu

- csrc/batch_attention_customize_config.jinja

- csrc/batch_attention_jit_binding.cu

- csrc/batch_attention_paged_kernel_inst.jinja

- csrc/batch_decode.cu

- csrc/batch_decode_customize_config.jinja

- csrc/batch_decode_jit_binding.cu

- csrc/batch_decode_kernel_inst.jinja

- csrc/batch_prefill.cu

- csrc/batch_prefill_customize_config.jinja

- csrc/batch_prefill_jit_binding.cu

- csrc/batch_prefill_paged_kernel_inst.jinja

- csrc/batch_prefill_ragged_kernel_inst.jinja

- csrc/single_decode.cu

- csrc/single_decode_customize_config.jinja

- csrc/single_decode_jit_binding.cu

- csrc/single_decode_kernel_inst.jinja

- csrc/single_prefill.cu

- csrc/single_prefill_customize_config.jinja

- csrc/single_prefill_jit_binding.cu

- csrc/single_prefill_kernel_inst.jinja

- flashinfer/attention/_core.py

- flashinfer/decode.py

- flashinfer/jit/attention/modules.py

- flashinfer/prefill.py

- include/flashinfer/attention/batch_pod.cuh

- include/flashinfer/attention/decode.cuh

- include/flashinfer/attention/persistent.cuh

- include/flashinfer/attention/pod.cuh

- include/flashinfer/attention/prefill.cuh

- include/flashinfer/attention/scheduler.cuh

- include/flashinfer/attention/variant_helper.cuh

- include/flashinfer/attention/variants.cuh

- include/flashinfer/utils.cuh

- tests/attention/test_batch_attention.py

- tests/attention/test_batch_attention_per_tensor.py

- tests/attention/test_batch_attention_per_token_head.py

- tests/attention/test_batch_per_token_head.py

- tests/attention/test_batch_prefill.py

- tests/attention/test_batch_prefill_kernels.py

- tests/attention/test_single_per_token_head.py

- tests/attention/test_single_prefill.py

- tests/utils_fp8.py

🚧 Files skipped from review as they are similar to previous changes (38)

- csrc/single_decode_kernel_inst.jinja

- csrc/single_decode_customize_config.jinja

- include/flashinfer/attention/batch_pod.cuh

- csrc/batch_decode_kernel_inst.jinja

- tests/attention/test_batch_prefill.py

- include/flashinfer/attention/pod.cuh

- csrc/single_decode_jit_binding.cu

- include/flashinfer/attention/variant_helper.cuh

- csrc/single_prefill_kernel_inst.jinja

- csrc/batch_decode_customize_config.jinja

- include/flashinfer/utils.cuh

- flashinfer/attention/_core.py

- csrc/batch_attention_jit_binding.cu

- csrc/single_prefill_jit_binding.cu

- csrc/batch_decode.cu

- csrc/batch_prefill_customize_config.jinja

- tests/attention/test_batch_attention.py

- csrc/batch_decode_jit_binding.cu

- include/flashinfer/attention/variants.cuh

- tests/attention/test_batch_attention_per_tensor.py

- csrc/single_decode.cu

- csrc/batch_prefill_paged_kernel_inst.j
