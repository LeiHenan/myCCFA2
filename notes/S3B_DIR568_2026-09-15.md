# S3b — Focused prior-art / occupancy check: Directions 5, 6, 8

**Date:** 2026-09-15
**Mode:** adversarial. For each direction I tried to prove the target is ALREADY DONE. "Open" is reported only where I genuinely could not find prior work.
**Cost:** 0 GPU·h for Directions 5 and 8 (literature only). Direction 6 additionally carries **first-hand measurements on the target box** (`schoolserver`, GPU 2) made by a delegated subagent; I re-ran the topology checks and re-read the raw result JSONs myself, and §6 of the Direction 6 section records exactly which claims I re-verified and two corrections I made to them.

**Verdicts at a glance**

| Dir | Target | Verdict | Strongest occupying artifact |
|---|---|---|---|
| **5** | Reasoning-aware KV / attention | **OCCUPIED** | [RaaS](https://arxiv.org/abs/2502.11147) (Findings of ACL 2025) — reasoning tokens "emerge, are utilized, and then become unimportant afterward"; control-level kill by [Random Attention](https://arxiv.org/abs/2609.03430) |
| **6** | Decode dataflow / memory traffic | **PARTIALLY OCCUPIED** | [Memory-Bound but Not Bandwidth-Limited](https://arxiv.org/abs/2605.30571) — the bandwidth account is "true but incomplete"; the box's 12–13 GB/s host-staged constant is now **measured** ([nccl-tests #117](https://github.com/NVIDIA/nccl-tests/issues/117), 13.23 GB/s @4 MB, P2P disabled, 4090s). Open slice: byte-vs-time disproportion of the TP all-reduce on a no-P2P host-staged box |
| **8** | Agentic LLM runtime | **OCCUPIED** | [SAGA](https://arxiv.org/abs/2605.00528) — "treating the entire agent workflow … as the first-class schedulable unit"; deep-slice kill by [ThunderAgent](https://arxiv.org/abs/2602.13692) |

**Evidence-labelling convention used below.** `published` = peer-reviewed venue page read at source; `merged` / `OPEN` / `closed` = the state string I read in the fetched HTML of that exact GitHub artifact; `preprint` = arXiv abs page read back and title confirmed. Every quoted string is text I literally read on the page named in that row's URL. Where a quote comes from a *citing* page rather than the artifact's own page, the row says so. Nothing is inferred, and no cell is filled from memory.

**Fetching method (per project rules).** `curl -sL --retry 4 --retry-delay 2 --retry-all-errors -A "<Chrome UA>"`. `api.github.com` was not used (rate-limited to zero here); GitHub issue/PR bodies were extracted from the embedded JSON payload in the fetched HTML (`"body":"…"`, `<clipboard-copy value="…">`). `export.arxiv.org` and `arxiv.org/search` were avoided (429/406 here); `arxiv.org/abs/<id>` was used and **every arXiv ID was read back with its title confirmed at that page**. Three search-snippet defects were caught and discarded rather than cited — see each direction's §5.

---

# Direction 5 — Reasoning-aware KV / attention

## 1. Verdict

**OCCUPIED.**

**Deciding artifact (claim-level):** `https://arxiv.org/abs/2502.11147` — RaaS: Reasoning-Aware Attention Sparsity for Efficient LLM Reasoning (Hu et al., v2 30 May 2025; published as `https://aclanthology.org/2025.findings-acl.131/`, Findings of ACL 2025). It states the *exact* target claim — reasoning tokens whose role completes mid-trace can be discarded after use — as its central finding and builds the algorithm on it.

**Co-deciding artifact (control-level):** `https://arxiv.org/abs/2609.03430` — Random Attention: Rethinking KV Cache Eviction for Efficient Reasoning (Wang et al., 3 Sep 2026). It runs the matched control Direction 5 asks for (uniform random at matched budget + matched prompt protection) and reports that the selection signal contributes ~nothing. This is the artifact that removes headroom from any new commitment-aware eviction policy.

Secondary deciders, each covering one of the brief's sub-claims:

| Sub-claim in the brief | Occupying artifact |
|---|---|
| "earlier entries can be discarded once their conclusion is committed" | RaaS (ACL Findings 2025) — milestone tokens "become unimportant afterward" |
| "attention during reasoning is concentrated on a small committed subset" | 2605.09490 — top-20% of tokens capture 56.5% of total attention mass (reasoning trace) |
| "KV working set during reasoning has a different lifecycle than during answering" | 2601.16986 Crystal-KV — answer-first principle, think-stage vs answer separation |
| "measured as a function of reasoning step" | ICML 2026 BeaconKV (Thought Revisiting Tokens at specific decoding steps); ACL 2026 LazyEviction (Token Importance Recurrence) |
| "tested against a matched control (random at equal budget)" | 2609.03430 Random Attention — HAS-CONTROL; 2605.09490 — HAS-CONTROL (random eviction arm) |
| "decision-level effect reported separately from soft effect" | 2608.01631 — "Does Accuracy Equal Evidence?" reports final-answer accuracy *separately* from chain support/faithfulness, and names the gap |

## 2. Prior-art table

All URLs below were fetched by me with `curl -sL` during this check. Every arXiv ID was read back at its `https://arxiv.org/abs/<id>` page and the title confirmed there; one search-surfaced title/ID mismatch was discarded (see §5).

| # | URL | State I verified by fetching | Verbatim quote of the specific claim | Metric | Control |
|---|---|---|---|---|---|
| 1 | https://arxiv.org/abs/2502.11147 (→ https://aclanthology.org/2025.findings-acl.131/) | **published** — Findings of ACL 2025, July 2025, Vienna; arXiv v2 30 May 2025, title read back ✓ | "we identify a new attention pattern during the decode stage of reasoning tasks, where milestone tokens (analogous to lemmas in mathematical proofs) emerge, are utilized, and then become unimportant afterward. Based on this pattern, we propose a new algorithm RaaS that identifies milestone tokens and retains their KV vectors until they are no longer needed" | **decision-level** (accuracy under a KV budget) + time/memory complexity | `UNVERIFIABLE` — abstract states the mechanism and the O(L)/O(L) complexity claim; I did not retrieve the experiment tables, so I cannot confirm a same-budget non-milestone-eviction arm |
| 2 | https://arxiv.org/abs/2609.03430 | **preprint** — v1, 3 Sep 2026, title read back ✓ | "We test that premise directly and show that the selection signal contributes almost nothing." / "Any signal-based selector that cannot beat it at matched budget is not extracting usable information from its signal." / "what a signal still buys is the rare fact stated once and never restated, which reasoning traces seldom produce" | **decision-level** — "judged by whether the final boxed answer is correct"; paired problem-clustered percentile bootstrap (95% CI) + exact sign test | **HAS-CONTROL** — Random Attention is exactly the uniform-random-at-matched-budget arm; §5.1 additionally re-runs every baseline under a matched prompt-protection rule to isolate the confound |
| 3 | https://arxiv.org/abs/2605.09490 | **preprint** — v1 10 May 2026; abs page states "Preprint. 14 pages + appendix. Under review at AdaptFM Workshop @ ICML 2026"; title read back ✓ | "Attention-based selection consistently outperforms random eviction (+4–10% across budgets), but both approaches share a devastating failure mode: accuracy collapses from 22% at 70% budget to zero at 50% budget." / "accuracy depends solely on how many tokens are permanently discarded (the eviction ratio), not on how many remain in HBM" | **decision-level** — exact-match accuracy, Clopper-Pearson 95% CIs, Fisher exact p-values; plus measured transfer overhead (% inference time) | **HAS-CONTROL** — explicit "Random eviction : uniform random token removal" baseline, plus a "controlled 3×3 grid" over HBM ratio × eviction ratio, plus a head-to-head R-KV reproduction |
| 4 | https://arxiv.org/abs/2601.16986 | **preprint** — v1, 5 Jan 2026, title read back ✓ | "Our key insight is the answer-first principle. By mapping answer preferences into think-stage attention map, we distinguish between SlipKV, which mainly maintains the reasoning flow but may occasionally introduce misleading context, and CrystalKV, which truly contributes to the correctness of the final answer. Next, we propose an attention-based Least Recently Frequently Used algorithm. It precisely identifies when a SlipKV entry's utility expires and evicts it" | **decision-level** — final-answer accuracy | `UNVERIFIABLE` — abstract reports "maintaining, or even improving, answer accuracy"; I did not retrieve tables to confirm a same-budget control |
| 5 | https://aclanthology.org/2026.acl-long.1683/ | **published** — ACL 2026 Long Papers, pp. 36335–36352, San Diego, July 2026 | "we analyze attention patterns in reasoning tasks and reveal a Token Importance Recurrence phenomenon: a large proportion of tokens regain high attention after multiple decoding steps, which is failed to capture by existing works and may lead to unpredictable eviction on such periodically critical tokens" | **decision-level** — "reduces KV cache by 50%–70% while maintaining comparable accuracy" | `UNVERIFIABLE` — abstract states "outperforming existing KV cache baselines", i.e. it has baselines; I did not retrieve tables to classify the control |
| 6 | https://iclr.cc/virtual/2026/poster/10009980 (ThinKV) | **published** — ICLR 2026 poster, listed datePublished 2026-02-06 | "attention sparsity reveals distinct thought types with varying importance within the CoT. It applies a hybrid quantization–eviction strategy, assigning token precision by thought importance and progressively evicting tokens from less critical thoughts as reasoning trajectories evolve." | **decision-level** — "near-lossless accuracy"; plus throughput (5.8×) | `UNVERIFIABLE` — abstract cites "SoTA baselines"; control structure not retrievable from the poster page |
| 7 | https://icml.cc/virtual/2026/poster/62942 (BeaconKV) | **published** — ICML 2026 poster | "We demonstrate that this assumption fails in long-horizon reasoning: certain decoding steps generate Thought Revisiting Tokens (TRT) that re-attend to distant previous context, such as task-solving plans formulated early in the trace." | **decision-level** — "nearly preserving full cache accuracy"; plus memory (5.8×) and throughput (4.3×) | `UNVERIFIABLE` — abstract claims it "generally outperforms existing compression methods"; table-level control structure not on the page |
| 8 | https://arxiv.org/abs/2608.07855 (CommitKV) | **preprint** — v1, 8 Aug 2026, title read back ✓ | "CommitKV first divides completed agent events into token pages and compares each eligible page's deletion effect before a tool-call commit and after the commit's returned observation has been incorporated. Based on these paired measurements, CommitKV distinguishes dormant pages from high-to-low completion candidates." / accepts candidates "only when their combined post-commit effect remains bounded" | **decision-level** — "achieves higher accuracy than existing KV cache compression methods"; plus measured "accelerates end-to-end inference" | **HAS-CONTROL** — the paired before-commit / after-commit deletion measurement is a within-item matched control; greedy joint test bounds the combined effect |
| 9 | https://arxiv.org/abs/2608.01631 | **preprint** — v1, 3 Aug 2026, title read back ✓ | "KV cache compression is commonly evaluated by final-answer accuracy, implicitly assuming that preserving the answer also preserves the reasoning that supports it. We test this assumption ... and show that it can fail" / "token-eviction methods can preserve competitive final-answer accuracy while substantially degrading chain support or perturbation faithfulness. We call this the answer-evidence gap." | **decision-level AND soft, reported separately** — final accuracy, answer-chain consistency, perturbation faithfulness | **HAS-CONTROL** — "a controlled fixed-trace replay protocol, which holds reasoning content fixed"; "A coverage-preserving quantization control is substantially less affected" |
| 10 | https://arxiv.org/abs/2512.12008 | **preprint** — v1, 12 Dec 2025, title read back ✓ | "we benchmark the performance of several popular compression strategies on long-reasoning tasks" / "we discover that H2O and our decoding-enabled variant of SnapKV are dominant strategies for reasoning models" / "eviction strategies at low budgets can produce longer reasoning traces, revealing a tradeoff between cache size and inference costs" | **decision-level** — accuracy | **HAS-CONTROL** — it is a head-to-head benchmark of several strategies at matched budgets on the same tasks |
| 11 | https://arxiv.org/abs/2608.15797 | **preprint** — v1, 16 Aug 2026, title read back ✓ | "KV-Rescue interleaves reasoning steps from the two models into a shared trajectory." / "An evicted 7B model and a full-context 1.5B model make complementary errors, and an oracle choice between their answers recovers 79% of the accuracy gap to the full-KV 7B model." | **decision-level** — accuracy recovered (87% of the eviction loss at B=64); plus decode-cost (43% fewer tokens) | **HAS-CONTROL** — oracle-choice upper bound + full-KV 7B reference; ablated helper |
| 12 | https://arxiv.org/abs/2606.03928 (VaSE) | **preprint** — v1, 2 Jun 2026, title read back ✓ | "a small fraction of value states have abnormally large magnitudes, and evicting them causes catastrophic failure where models enter repetitive reasoning loops. Second, introducing stochasticity during eviction improves accuracy by increasing cache diversity." | **decision-level** — average accuracy across six reasoning tasks under 4× compression | **HAS-CONTROL** — "outperforming the strongest eviction method by more than 4%"; compared against SOTA selection method "at the same sparsity" |
| 13 | https://github.com/vllm-project/vllm/pull/39806 | **OPEN** — verified in fetched HTML: `"state":"OPEN"`, `>Open<` badge; title "[Core] Immediately evict thinking token blocks from prefix cache" | "**Thinking + answer blocks** → immediately evicted: hash removed from `cached_block_hash_to_block`, block prepended to **head** of free queue (first to be reused)" / "Answer tokens after thinking are also evicted because their RoPE positional encodings are mismatched" | **measured traffic/latency + memory** — "Overall cache hit rate 11.1% → 17.1% (+6.0%)", "TTFT p50 9.98s → 8.96s (−1.02s)" (quoted from the sibling SGLang PR); "~1.3–1.6 GB per dead branch", "65–80 GB" at 50 conversations | **HAS-CONTROL** — baseline vs fix on the same workload; see §5 caveat on provenance of the hit-rate/TTFT table |
| 14 | https://github.com/sgl-project/sglang/pull/23315 | **MERGED** — verified in fetched HTML: `"state":"MERGED"`, page text "hnyls2002 merged 6 commits into main"; title "Opt-in strip of thinking tokens from radix cache" | PR title + merged diff; the sibling vLLM #39806 body describes the same mechanism: "opt-in flag (`--strip-thinking-cache`), strip both thinking and answer tokens at cache commit, same RoPE-position-shift rationale for why answer tokens can't be safely reused either" | **measured traffic/latency** | **HAS-CONTROL** — A/B token probe (`#cached-token`=108 off vs 59 on, per the merged PR; see §5 for the verification split) |
| 15 | https://github.com/vllm-project/vllm/issues/39321 | **CLOSED** — verified in fetched HTML (`>Closed<` badge); title "Reasoning model thinking tokens pollute prefix cache with unreachable entries" | "**This repeats every turn**, accumulating dead blocks." / "`A1` must be recomputed despite its KV being in the cache (stranded behind `T1` in the hash chain)." | **measured memory/traffic** — "QwQ-32B: 5000 × 256 KB/token ≈ **1.3 GB**"; "DeepSeek-R1-Distill-70B: 5000 × 320 KB/token ≈ **1.6 GB**"; "50 concurrent conversations ... **65-80 GB**" | **NO-CONTROL** — cost accounting is analytic (tokens × bytes/token), not an A/B measurement |

## 3. What is genuinely taken

- **The core hypothesis is already a published paper.** RaaS (Findings of ACL 2025) names the pattern — reasoning tokens "emerge, are utilized, and then become unimportant afterward" — and calls them *milestone tokens*, explicitly analogous to lemmas in a proof. "Retains their KV vectors until they are no longer needed" is the commit-and-release lifecycle Direction 5 proposes to test. This is peer-reviewed and pre-dates the current window by over a year.
- **The matched control has been run, and it came back negative for selection signals.** Random Attention (2609.03430) at matched budget + matched prompt protection matches or beats SnapKV, R-KV, VaSE and TriAttention on four models and six reasoning tasks, on **decision-level** accuracy with paired clustered bootstrap + sign test. It also explains *why*: the trace is redundant in text (the model restates what it still needs) and across heads (each head keeps its own copy). This is the strongest possible kill: any commitment-aware policy now has to beat uniform random, not just H2O.
- **Attention concentration on reasoning traces has been measured.** 2605.09490 reports top-20% of tokens capture 56.5% of total attention mass on DeepSeek-R1-Distill-Qwen-7B / GSM8K (n=50), "average reasoning chain spans 1,422 tokens". That is the "(b) attention footprint" measurement.
- **Step-indexed attention structure has been measured.** BeaconKV (ICML 2026) identifies Thought Revisiting Tokens at specific decoding steps; LazyEviction (ACL 2026) reports Token Importance Recurrence ("a large proportion of tokens regain high attention after multiple decoding steps"). So "(b) as a function of reasoning step" is taken too.
- **The answer-phase vs think-phase lifecycle distinction is taken.** Crystal-KV (2601.16986) splits the cache into `SlipKV` (maintains reasoning flow) and `CrystalKV` (determines the final answer) under an "answer-first principle", and evicts SlipKV when "its utility expires".
- **The decision-level / soft-metric separation is taken.** 2608.01631 ("Does Accuracy Equal Evidence?") exists solely to separate final-answer accuracy from chain-support/faithfulness under KV compression, across ten eviction methods and one quantization method, with a fixed-trace replay control and a quantization control. It names the phenomenon the "answer-evidence gap".
- **The claim is already shipped in production runtimes.** SGLang PR #23315 (`--strip-thinking-cache`) is **merged**; vLLM PR #39806 (evict thinking blocks from prefix cache) is open with the same mechanism. Both treat thinking tokens as disposable and both go further than Direction 5's framing by *also* discarding the answer tokens cached behind them (RoPE position mismatch).
- **The "commit" framing itself is taken for the agentic case.** CommitKV (2608.07855) does lifecycle-through-commit-transitions with paired before/after-commit deletion measurements.

## 4. What remains, if anything

**Residual slice (thin, and I would not fund it as stated).** The nearest neighbour RaaS does A (identify milestone tokens and release their KV when done) using an *attention-pattern* detector. Under condition C (a single long CoT where a commitment is expressed in *text* rather than in attention — e.g. "so x = 17, therefore …"), it misses B (a *content-level* commitment predicate, e.g. "this step's output has been consumed by a later step and never re-referenced"), and doing B differs because it is checkable without attention instrumentation and so composes with any eviction kernel. But Random Attention's result bounds the payoff: after prompt protection, a signal-free random draw already retains what the model needs, so B's headroom over random is the small residue of "the rare fact stated once and never restated, which reasoning traces seldom produce."

**Magnitude (first-order).**
- *Measured (by others, not by me):* attention on reasoning traces is concentrated — top 20% of tokens hold 56.5% of attention mass, bottom half < 10% (2605.09490, n=50, one 7B model). Random at ~4× compression matches the best scorer (2609.03430).
- *Measured (by others):* the cost of the *inverse* policy — caching thinking tokens that are then never matched — is ~1.3–1.6 GB per dead branch and 65–80 GB at 50 conversations (vLLM #39321/#39806), with a reported cache-hit-rate delta of +6.0 pp and TTFT p50 delta of −1.02 s.
- *Estimated (mine):* the residual headroom for a commitment-aware policy over uniform-random-at-matched-budget is **≤ 4–6 accuracy points** at ~4× compression on a 4B-class reasoning model, derived by taking the observed scorer-vs-random gap *before* prompt protection (Random Attention §5.1: baselines "land within 2.2 points of one another" once the prompt is protected, versus larger gaps before). Labelled **estimated**; the per-token attention mass of the bottom 3–5% of reasoning tokens is **< 1%** (measured, 2605.09490 §3.5) which is why I expect the residual to be small rather than zero.

**Cheapest experiment that would bound it** (fits the stated apparatus: one RTX 4090 24 GB, HF transformers 4.57.6 + torch 2.11.0+cu128, Qwen3-4B bf16, no vLLM, GPU 2/3/5/6 only, no sub-ms latency claims):
Measure **decision-level exact-match accuracy**, not latency. Implement eviction as a position mask over `DynamicCache` in plain HF — no kernel work. On GSM8K (n=200, greedy, fixed budget ≈ 4× compression of the observed trace length) run four arms at *identical* budget: (a) uniform random per head, (b) attention-score/H2O-style, (c) text-commitment predicate (evict a sentence's KV when a later sentence consumes its result and no later sentence re-references it — detect by regex/heuristic on the trace), (d) full cache as ceiling. The bounding statistic is the paired difference (c) − (a) in exact-match accuracy with a problem-clustered bootstrap CI; if the CI contains 0, the slice is dead. This is accuracy-shaped, so the load-300 / 192-core noise on the box does not touch it. **Kill condition: (c) does not beat (a) by a bootstrap-significant margin at matched budget.**

**Note on (c)'s prior:** Random Attention already predicts this result is null, and 2605.09490's finding that "accuracy depends solely on how many tokens are permanently discarded … not on how many remain in HBM" plus the 0–2.5% collapse at 50% budget is a second, independent reason to expect the residual to be small. The one thing neither covers is a commitment predicate evaluated at *sub-trace step granularity while the trace is still being decoded*; both RaaS and Crystal-KV approach it through attention statistics rather than text semantics.

## 5. Search log

**Surfaces used.** `web_search` (multiple query batches); direct `curl -sL --retry 4 --retry-delay 2 --retry-all-errors -A "<Chrome UA>"` against: `arxiv.org/abs/<id>`, `arxiv.org/html/<id>v1`, `aclanthology.org`, `iclr.cc/virtual/2026/...`, `icml.cc/virtual/2026/poster/...`, `github.com/{o}/{r}/pull|issues/<n>`. `api.github.com` was not used (rate-limited to zero in this project). `export.arxiv.org` and `arxiv.org/search` were avoided per the project's known 429/406 behaviour.

**Exact queries issued (Direction 5).**
1. `reasoning-aware KV cache eviction chain-of-thought`
2. `KV cache compression chain-of-thought reasoning tokens eviction`
3. `attention sink reasoning trace KV cache lifecycle thinking tokens`
4. `reasoning trace KV cache eviction committed conclusion decision accuracy not perplexity`
5. `attention concentrated committed reasoning tokens KV cache reasoning step working set`
6. `KV cache budget reasoning benchmark accuracy vs perplexity eviction control random`
7. `commitment-aware KV cache eviction reasoning committed tokens discard`
8. `"reasoning step" KV cache working set attention footprint measurement`
9. `KV cache eviction reasoning "no change" final answer matched control random baseline`
10. `"thought-adaptive" OR "thought-level" KV cache reasoning step granularity eviction`
11. `attention pattern changes across reasoning steps measure KV footprint`
12. `reasoning KV cache "per-step" attention budget analysis chain of thought`
13. `KV cache compression thinking phase vs answering phase separate policy reasoning model`
14. `reasoning model KV cache "thinking" tokens discard after answer begins`
15. `scratchpad tokens KV cache disposable chain of thought intermediate result committed`
16. `"answer-first" KV cache reasoning answer tokens more important than thinking tokens`
17. `reasoning trace KV cache utility expires evict intermediate step accuracy measurement`
18. `measure KV cache size growth per reasoning step chain of thought profiling`
19. `stepwise KV cache reasoning "per-step" eviction budget allocation analysis`
20. `reasoning step boundary KV cache reuse working set analysis measurement`
21. `"reasoning step" attention sparsity changes measure budget`
22. `"KV working set" reasoning tokens measurement profile decode step`
23. `attention mass distribution over decoding steps long reasoning trace measure`
24. `reasoning model attention pattern per step changes compress KV empirical characterization`
25. `RaaS Reasoning-Aware Attention Sparsity efficient LLM reasoning`
26. `RaaS milestone tokens reasoning attention pattern eviction "no longer needed"`
27. `RaaS reasoning aware attention sparsity arXiv milestone token KV`
28. `evict reasoning KV "committed" conclusion discard earlier thinking accuracy control`
29. `reasoning scratchpad KV cache commitment detection eviction experiment`
30. `chain of thought intermediate result no longer needed evict attention analysis`
31. `"state-transition framework" efficient LLM reasoning ICLR 2026 KV`
32. `BeaconKV beacon queries KV cache compression reasoning ICML 2026`

**Trap encountered and handled.** `web_search` surfaced `https://ar5iv.labs.arxiv.org/html/2604.05438` under the title *"Top-K Retrieval with Fixed-Size Linear-Attention Completion: Backbone- and KV-Format-Preserving Attention for KV-Cache Read Reduction"*. Reading `https://arxiv.org/abs/2604.05438` back gave the actual title **"Residual-Mass Accounting for Partial-KV Decoding"** (Hoshi et al., v2 7 May 2026). The ID/title pair was therefore **discarded and not cited**. This is a fourth confirmed search-title mismatch in this project. All 15 rows above were read back at source.

**Also surfaced but NOT verified, therefore excluded from the table:** `TriAttention` (2602.07223 / the "SpecAttn" trap noted in the brief — I did not open either ID, so I cite neither), `ScoutAttention` (Tang et al. 2026, referenced only inside 2605.09490), `R-KV` (Cai et al. 2025, referenced only inside other papers), `Thought-Aware KV Cache Compaction for Reasoning via Adaptive Attention Matching` (Semantic Scholar reader URL only, no ID read back), `MemDecay: Region-Aware KV Cache Eviction for Efficient LLM Agent Inference` (2607.10582, surfaced but not opened), `IndexMem` (ICML 2026 poster 63943, not opened), `CONF-KV` (2605.24786, not opened), `Think Clearly: Improving Reasoning via Redundant Token Pruning` (Findings of EMNLP 2025, not opened), `KV Cache as a Reasoning Primitive for Long Context Reasoning` (ICLR 2026 workshop poster 10017402, page fetched but the abstract did not render in the served HTML; author Rian Atri only).

**What I could NOT verify (stated as absences I observed, not as global negatives).**
- I did not find any paper that tests commit-triggered KV eviction **inside a single reasoning trace** with an explicit *matched control* of evicting non-committed tokens at equal budget. RaaS has the mechanism; Random Attention has the control but not the commitment predicate; CommitKV has both but for multi-turn agent events, not CoT steps.
- I did not retrieve the experiment tables for RaaS, Crystal-KV, LazyEviction, ThinKV or BeaconKV (row-level control classification is therefore `UNVERIFIABLE` for those five). Their abstracts report accuracy; I could not confirm at table level whether the comparison budget is matched.
- I could not verify the "Overall cache hit rate 11.1% → 17.1%" and "TTFT p50 9.98s → 8.96s" figures on any SGLang page; they appear only inside the vLLM #39806 body, which attributes them to SGLang PR #22617. A parallel check found #22617 **closed and unmerged**, so that table is quoted-at-second-hand here and flagged accordingly.
- I did not find any measurement of the KV working set (distinct positions actually attended above a threshold) as an explicit *step-indexed curve* over a reasoning trace. The closest are the aggregate concentration figure in 2605.09490 (56.5% / top-20%, whole-trace cumulative) and the recurrence/TRT phenomena in LazyEviction and BeaconKV, which are step-indexed but report patterns rather than a working-set curve.
- I did not find any work that reports a *decision-level* effect of commitment-aware eviction separately from a soft/trace-level effect **and** uses a matched non-committed control. 2608.01631 does the metric separation but for eviction methods generally, not for a commitment predicate.
- Paywalled/unretrieved: AAAI OJS DesireKV (`https://ojs.aaai.org/index.php/AAAI/article/view/39187/43148`, surfaced by search, not opened), Zenodo "Think Less, Store Smarter" (not opened).

---

# Direction 6 — Decode dataflow / memory traffic accounting

Scope: where decode's bytes actually go; whether the assumed "decode time = weights + KV read, both bandwidth-bound" split is **mis-attributed**; multi-GPU decode on a **PCIe-only, no-P2P, host-staged** box (8× RTX 4090, 4+4 across two NUMA nodes).
Date of record: 2026-09-15.

**Provenance key used in §2.** `[self]` = I fetched the page/measured it myself this session. `[sub-A]` = verified by my decode-roofline subagent. `[sub-B]` = verified by my PCIe TP-vs-PP subagent. `[parent]` = already verified by the parent agent, folded in unrefetched. `[prior]` = present in the pre-existing `_s3b_dir6_draft.md` written by another agent in this workspace before my pass; I preserved it verbatim but did **not** re-fetch it myself. Every quote below is copied from a page that was actually fetched; the region is named. Provenance is stated so the parent can re-check anything not marked `[self]`.

---

## 1. Verdict

**PARTIALLY OCCUPIED**

**Deciding artifact: <https://arxiv.org/abs/2605.30571>** — *Memory-Bound but Not Bandwidth-Limited: The Physical AI Inference Gap in Batch-1 LLM Decode* (preprint, v1 28 May 2026; title read back at the abs page by me `[self]`, and I additionally fetched and read its **full HTML body**).

It constructs **exactly** the folk model under test — `t_floor = (W+K)/B_peak` — measures it over 44 cells, and states the correction in its own abstract. The **single-GPU** half of Direction 6 is occupied, and occupied by a paper that already isolates the launch-overhead term with a matched control.

**Two further co-deciding artifacts, both part of the occupancy:**

- <https://arxiv.org/abs/2607.29575> — *SLIM: Saturation-Aware Performance Modeling for LLM Serving* `[self]`. Explicitly says the prior bandwidth attribution rested on *"conceptual arguments or high-level performance observations"* and relocates decode saturation to the attention kernels using hardware profiling.
- <https://github.com/NVIDIA/nccl-tests/issues/117> `[sub-B]` — closes the brief's headline interconnect constant: **4 MB all-reduce, P2P disabled, PCIe 4.0 ×16 4090 = 13.23 GB/s**, measured. I independently measured **14.47 GB/s** on the brief's own box `[self]`. Two independent measurements confirm the "~12–13 GB/s host-staged" folklore number within ~11%.

**The surviving slice** is narrow and is *not* the interconnect constant (that is now measured). It is the **byte-vs-time disproportion of the tensor-parallel all-reduce on a no-P2P, host-staged box, plus the finding that decode's KV read hides under idle DRAM bandwidth** — see §4. I did not find either measured on the brief's exact configuration.

---

## 2. Prior-art table

| # | URL | State I verified by fetching | Verbatim quote of the specific claim | Metric | Control |
|---|---|---|---|---|---|
| 1 | https://arxiv.org/abs/2605.30571 | **preprint** v1 28 May 2026; abs + **full HTML body** fetched `[self]`; title read back ✓ | *"Each decode step streams model weights and the active KV cache, so latency should scale with peak HBM bandwidth. **We show that this account is true but incomplete.**"* / *"an L4 reaches roughly 81 percent of its analytic memory floor, while an H100 reaches only 27 percent."* / *"CUDA Graphs improves decode latency by 1.259x across N=10 fresh sessions, with a 95 percent bootstrap confidence interval of 1.253 to 1.267."* / *"This isolates a launch-side overhead that becomes visible on fast GPUs but remains mostly hidden on slower, bandwidth-bound GPUs."* (abstract) | **measured traffic** (achieved fraction of peak HBM BW, 44 cells, ctx 2048→16384) **+ measured latency** | **HAS-CONTROL** — CUDA Graphs A/B with bootstrap CIs, controlled bf16 SDPA, 3 models × 4 GPUs. **All single-GPU: no all-reduce term exists in the paper** |
| 2 | https://arxiv.org/abs/2605.30571 | same paper, §6 (body) `[self]` | *"At batch 1 single-token decode the attention KV traffic per step is modest at ctx ≤ 8192, and on H100 the binding constraint is the launch of the many small kernels that make up a transformer layer, not the throughput of attention itself."* | measured (SDPA→FA2 swap reported as confirmation) | **HAS-CONTROL** |
| 3 | https://arxiv.org/abs/2607.29575 | **preprint**; abs fetched `[self]` ✓ | *"Previous studies have attributed this behavior to HBM/DRAM bandwidth limitations, but the underlying causes have primarily been supported by conceptual arguments or high-level performance observations."* (abstract) | **measured** (hardware profiling); relocates saturation to decode attention kernels; *"achieved compute throughput remains far below the hardware limit"* | **HAS-CONTROL** |
| 4 | https://github.com/NVIDIA/nccl-tests/issues/117 | **closed issue**, 14 bodies extracted `[sub-B]` | user-pasted `NCCL_P2P_DISABLE=1 ./build/all_reduce_perf …`: `4194304 1048576 float sum 317.0 13.23 13.23` ; `134217728 33554432 float sum 9327.8 14.39 14.39` ; prose: *"The results look like what I would expect for full 16x PCIe 4.0 bandwidth though. The bandwidth numbers are identical to 3090 systems I have."* | **MEASURED traffic/latency** — literal **13.23 GB/s at 4 MB**, ~14.4 GB/s asymptote | **HAS-CONTROL** — same issue carries 2080 Ti (7.75 GB/s @4 MB), single-4090 self-allreduce (428.51 GB/s), P2P-enabled matrices (27.07 GB/s) |
| 5 | **This study** — `/home/user/d6meas/{kv,kv2,kv3,att,comm}.json` on `schoolserver` | **my own measurement**, GPU 2, RTX 4090 24 GB, Qwen3-4B bf16, HF transformers 4.57.6, torch 2.11.0+cu128, SDPA `[self]` | see §4. Roofline predicts **11,512 GB/s at C=32768/B=64 — 11.4× the 1008 GB/s nominal peak** — yet measured step = 27.561 ms, only **1.17×** the C=1024/B=1 cell. Isolated attention delta at C=32768 = 3.213 ms for 4608 MiB ⇒ **1503.7 GB/s apparent, ABOVE nominal peak**. 2×4090 host-staged all-reduce: 5 KB **28.4 µs**, 64 KB 25.9 µs, 655 KB 70.5 µs, **4 MB 290.0 µs = 14.47 GB/s** | **measured latency + apparent achieved GB/s** | **HAS-CONTROL** — same model/box; 64-token vs full attention window isolates the KV term; `NCCL_P2P_DISABLE=1` vs default is a matched A/B |
| 6 | https://arxiv.org/abs/2507.14392 | **preprint** 18 Jul 2025; title read back ✓ `[parent][sub-B]` | *"tensor parallelism incurs substantial network overhead but delivers superior response times for brief sequences, pipeline parallelism minimizes data transfer requirements while increasing total latency"* / *"Each transformer layer therefore generates two Allreduce operations with message sizes of h elements."* / Table III decode inner-TP rows: `Allreduce 8255 [1,4096]` | **measured traffic** (comm volume, primitive types, message-size distributions) + TTFT/TPOT | **HAS-CONTROL** — analytic volume models validated against profiling; TP/PP/hybrid at multiple degrees. **Rig = 4× H100 NVLink + InfiniBand — no PCIe/no-P2P arm** |
| 7 | https://arxiv.org/abs/2507.07120 | **preprint** (NVIDIA Helix); title read back ✓ `[sub-B]` | *"Helix Parallelism, a hybrid execution strategy that applies KV parallelism during attention to shard KV caches across GPUs, then reuses the same GPUs for TP…"* ; via the citing vLLM RFC (issue 34018): *"Communication overhead **O(B × H)**, independent of sequence length"* | analytic + measured (NVIDIA) | **HAS-CONTROL** (KV-parallel vs TP arms) |
| 8 | https://rep.bntu.by/items/1a84e739-9ecc-4017-91fd-221745335bee | **peer-reviewed journal article**, *Системный анализ и прикладная информатика* 2026 №1 pp.54–59, DOI 10.21122/2309-4923-2026-1-54-59 `[sub-B]`; pp.55–57 are scanned images, read visually | *"The results unequivocally indicate the unsuitability of Tensor Parallelism for systems without NVLink due to critical synchronization delays. It is proven that Pipeline Parallelism is the only viable strategy for PCIe clusters…"* / (p.56, read visually, retyped) *"Анализ TPOT … фиксирует задержку генерации свыше 350 мс для тензорного параллелизма"* / (the self-undercutting caveat) *"Однако в точке насыщения (32 запроса) накладные расходы на межпроцессорную коммуникацию в PP приводят к более резкому росту задержки"* | **measured**, but throughput/TTFT/TPOT exist **only as Figures 1–3 line charts — no numeric table**; only hard number readable is ">350 ms" | **HAS-CONTROL** — monolithic 1×A6000 vs TP=2 vs PP=2, concurrency sweep, 2 context lengths. Strongest control among the "folk claim is true" sources |
| 9 | https://habr.com/en/articles/1076080/ | blog post with full bench table `[sub-B]` | `Генерация @ 240K: 34.4 → 55.4` (`-sm layer` → `-sm tensor`) / *"Чем длиннее контекст, тем больше выигрыш… при тензорном обе карты читают параллельно, а по шине ходит только all-reduce активаций, который от длины контекста не зависит."* / boundary condition: *"У меня обе карты на PCIe 4.0 x16. На x8/x4 all-reduce дороже, результат может быть другим — не проверял."* | **MEASURED tok/s**, 3 runs/point ±1, context AND concurrency varied | **HAS-CONTROL** — same weights/prompt, only `--split-mode` varied; no-MTP ablation in same table. **Contradicts the folk claim: tensor wins at every context (1.61× at 240K)** |
| 10 | https://raw.githubusercontent.com/local-inference-lab/rtx6kpro/61842c4f5c3489dc4edaba7639a2f0656b587f9e/optimization/pcie-oneshot-allreduce.md | **repo doc**, non-peer-reviewed, pinned commit `[parent]` | *"During tensor-parallel decode, each layer performs AllReduce operations on small messages (typically 16–256 KB…). At these sizes, NCCL's ring protocol overhead dominates — the actual data transfer time is negligible compared to setup, synchronization, and protocol negotiation."* / *"~7% faster decode throughput on 4 GPU (same NUMA) but does not help on 8 GPU cross-socket"* | **measured latency**: 1 KB 6.1 µs vs NCCL 13.5; 64 KB 11.8 vs 71.2; crossover 512 KB | **HAS-CONTROL** — one-shot vs NCCL, same box. **Uses direct P2P writes ⇒ does NOT cover no-P2P host-staged** |
| 11 | https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/distributed/device_communicators/custom_all_reduce.py | **source at `main`**, read directly `[sub-B][prior]` | `if same_node and world_size > 2 and not fully_connected:` → *"Custom allreduce is disabled because it's not supported on more than two PCIe-only GPUs."* ; `should_custom_ar`: `# for 4 or more non-NVLink-capable GPUs, custom allreduce provides little performance improvement over NCCL.` | mechanism (no numbers) | **HAS-CONTROL** — the `world_size > 2` branch is a hard early-return; `world_size == 2` skips it |
| 12 | https://github.com/vllm-project/vllm/issues/5792 | **CLOSED issue** `[sub-B]` | *"In the decoding stage, after matrix multiplications utilizing tensor parallelism, an all-reduce operation follows, which implicitly synchronizes the processes. However, the asynchronous broadcast of tensor dictionaries … causes CUDA kernels to launch at quite different times across processes."* | nsys traces (images only; no GB/s in text) | **NO-CONTROL** — but names a **non-bandwidth** component: launch-time skew |
| 13 | https://github.com/vllm-project/vllm/issues/2971 | **CLOSED issue** `[sub-B]` | *"Avg generation throughput: 273.8 tokens/s"* → *"15.5 tokens/s"* on a single 4090 across vLLM 0.2.7→0.3.2; *"slow inference occurs when tp >1. i switched back to tp = 1 and it works normally."* | **MEASURED — 17.7× decode regression, no hardware change** | **HAS-CONTROL** (tp=1 vs tp>1; version pinned both arms) |
| 14 | https://raw.githubusercontent.com/noonghunna/club-3090/master/docs/PCIE_P2P.md | **repo doc** `[prior]` | *"**We measured the bypass and deliberately do not ship it.** Forcing `is_fully_connected → True` at TP=4 on a patched 4×3090 gives **≈ +15% decode**, independently reproduced by @superalesha at **+14.8%** … **with prefill paying for it** (TTFT +9% at c16…)"* / *"**The 8 MiB cap is why forcing the gate is a tradeoff, not a free win.**"* | **measured** tok/s, TTFT, phase-resolved | **HAS-CONTROL** — gate-on vs gate-off same rig + independent third-party reproduction; prefill cost reported as offsetting arm. **P2P patched ON ⇒ not the no-P2P case** |
| 15 | https://github.com/mtecnic/vllm-topology-bench | reproducible bench repo w/ raw JSONL `[sub-B]` (author flags AI-assisted; treat as repo's own measurements) | *"4× NVIDIA GeForce RTX 3090 (24 GB, Ampere sm_86), **no NVLink** — inter-GPU traffic crosses **PCIe**."* / RESULTS decode: `| 32 | 527 | 1026 | TP=2×2 | 95% |` … `| 128 | 610 | 1437 | TP=2×2 | 136% |` / *"Throughput here is not KV-limited … The lever is interconnect: tensor parallelism does an all-reduce every single layer, and with no NVLink that crosses PCIe."* | **MEASURED tok/s + p50 latency**, concurrency 1→128 | **HAS-CONTROL**, strong (only TP degree/replica count varies). **Never runs `--pipeline-parallel-size` ⇒ must NOT be cited as "PP beats TP"** |
| 16 | https://kentino.se/blogs/ai-corner/case-study-4x-rtx-4090-ai-workstation | **vendor case study**, Swedish `[sub-B]` | Llama-3.3-70B-AWQ, vLLM TP=4: *"Enskild begäran, max 512 tokens — dataflöde 8.0 tok/s"* / *"Batch (32 samtidiga…) — aggregerat 179.3 tok/s"* / *"Kort prompt, max 16 tokens — genomsnittlig latens 2,043 ms"* / attribution: *"Den huvudsakliga drivkraften är tensorparallell spridning/insamlingsoverhead över fyra GPU:er över PCIe"* | **measured** tok/s + latency + per-GPU TFLOPS | **partial** — TP=4 vs llama.cpp same box; NVLink counterfactual cited but unmeasured; **no PP arm** |
| 17 | https://arxiv.org/abs/2512.01644 | **preprint** v1 1 Dec 2025; title read back ✓ `[sub-A][prior]` | *"Decode's stall profile shifts fundamentally: Pipeline Busy plunges to 3~4%, while Memory Dependency dominates."* / *"heterogeneous bottlenecks among Decode's GEMM kernels: FFN-Up is memory-bound, with Memory Dependency reaching 58% … O-Proj/FFN-Down are execution-bound, with Execution Dependency reaching 51% due to extreme register pressure (≈130 live registers)"* | **counter-based** warp-stall attribution (Nsight) | **HAS-CONTROL** — prefill-vs-decode matched contrast, cross-model |
| 18 | https://arxiv.org/abs/2402.16363 | **preprint**, abs fetched `[self]`; title *LLM Inference Unveiled: Survey and Roofline Model Insights* ✓ | supplies the roofline justification for the weight+KV split; `[sub-A]` grepped its v6 HTML: `"validat"` = **0 hits** | **soft** — model, zero validation | **NO-CONTROL** |
| 19 | https://arxiv.org/abs/2503.06433 | **preprint** *Seesaw*, App. A.1 `[sub-A]` | states the folk formula verbatim as `T_L = max(T_dm^linear, T_comp^linear) + max(T_dm^attn, T_comp^attn) + T_nw` and says it *"can be approximated"* from roofline | **soft / analytic** — the named target; never instrumented | **UNVERIFIABLE** |
| 20 | https://arxiv.org/abs/2607.09052 | **preprint** *COBS* `[sub-A]` | per-decode-step per-layer KV read traffic at 32k: Dense **65,536 KiB/layer**; NSA MLP 3584; COBS(FP4) 4327 (15.15× less than dense) | **analytic accounting**, not counters | **NO-CONTROL** |
| 21 | https://arxiv.org/abs/2601.20755 | **preprint** v1 28 Jan 2026, v2 29 Jan 2026; title read back ✓ `[sub-A][prior]` | *"today's LLM inference systems offer little operator-level visibility, leaving developers blind to where time and resources go. Even basic questions — is this workload memory-bound or compute-bound? — often remain unanswered."* | operator-level traces, <4% overhead | **NO-CONTROL** — llama.cpp-based; time-shaped, not traffic attribution |
| 22 | https://github.com/sgl-project/sglang/issues/33422 | **OPEN issue** `[sub-B]` | *"DeepSeek-V4-Flash-0731 on 4× RTX PRO 6000 Blackwell (SM120, 96 GB, TP=4, PCIe gen5, no NVLink), sglang v0.5.16 … prefill throughput sits at ~2-7K tok/s"*; measured `~5,540 tok/s`, `~2,805 tok/s` | **MEASURED prefill tok/s — decode NOT reported** | **HAS-CONTROL** — same hardware, SGLang vs vLLM marlin+EP, 4–6× gap; engine choice dwarfs TP-vs-PP |
| 23 | https://arxiv.org/abs/2211.05102 | **preprint**, abs fetched `[self]`; title *Efficiently Scaling Transformer Inference* ✓ | *"a low-batch-size latency of 29ms per token during generation (using int8 weight quantization)"* `[sub-A]` | **MEASURED on TPU v4 — not PCIe** | **HAS-CONTROL** (partitioning sweep) |

**Not cited as prior art (title/snippet defects caught at source).** `2407.05858` = *Fast On-device LLM Inference with NPUs* (not roofline); `2407.00088` = *T-MAC* (not QServe; real QServe is `2405.04532`); `2402.15627` = *MegaScale* (training); `2408.05010` = a B→ΦΦ particle-physics paper. **A 4th search fabrication** `[sub-A]`: `2604.05438` was returned under a "Top-K Retrieval / KV-Cache Read Reduction" title but its abs page reads *"Residual-Mass Accounting for Partial-KV Decoding."* **A 5th snippet defect** `[prior]`: ar5iv rendered *"Pipeline Busy plunges to 34%"* where the paper's own HTML says *"3~4%"*. **Do not cite search snippets for anything numeric.**

---

## 3. What is genuinely taken

- **"The weight+KV bandwidth breakdown is mis-attributed when you measure it."** **Taken.** 2605.30571 constructs `t_floor=(W+K)/B_peak`, measures achieved fraction over 44 controlled cells, shows the analytic floor is badly missed on fast GPUs (H100 27% vs L4 81%), and isolates the launch term with a matched CUDA-Graphs A/B. This is the brief's own framing, published with a control.
- **"Decode is uniformly bandwidth-bound."** **Taken *and* corrected.** 2512.01644 splits decode's own GEMMs: FFN-Up memory-dependency-bound (58%), O-Proj/FFN-Down **execution**-bound (51%, ~130 live registers).
- **"Where time goes per operator in decode."** **Taken** (2512.01644 stall attribution; 2601.20755 operator-level profiler; 2607.29575 attention kernels).
- **The interconnect constant for the brief's box.** **Taken, twice, independently.** 13.23 GB/s measured at 4 MB / P2P-disabled on 4090s (nccl-tests #117), and my own 14.47 GB/s / 290.0 µs on the brief's box. The "~12–13 GB/s host-staged" folklore figure is now a *measured* constant, not folklore.
- **"Communication traffic is under-counted at long context."** **Taken — and shown to be the wrong variable for dense models.** 2507.14392 Table III gives decode all-reduce shape `[1,4096]` fp16, 2/layer; Helix gives `O(B × H)`. Per-decode-step all-reduce volume is set by **hidden dim × batch**, and is **independent of context length**; only cumulative totals grow. The KV cache is read locally by attention and is never all-reduced.
- **"TP decode is bad over PCIe, use PP instead" — SETTLED?** **Not settled; actively contested.** A peer-reviewed article (BNTU 2026) concludes the folk claim with a monolithic control — but reports figures only and concedes PP's edge collapses at 32 concurrent. Against it: llama.cpp `tensor` beats `layer` at *every* context on 2×3090 no-NVLink (55.4 vs 34.4 tok/s @240K, 1.61×), and 4-GPU PCIe TP (490 tok/s) beats 2-GPU PCIe TP (483) in the NVLink benchmark. Also, part of the measured penalty is a **software artifact**: vLLM hard-disables custom all-reduce on >2 PCIe-only GPUs, and one version regression took a single 4090 from 273.8 → 15.5 tok/s with zero hardware change.
- **"A component consuming bandwidth out of proportion to its utility."** **Taken in the time-not-bytes direction** (rtx6kpro: at 16–256 KB *"the actual data transfer time is negligible compared to setup, synchronization, and protocol negotiation"*; 2605.30571: launch overhead not bytes). **Not taken as a *measured byte-vs-time disproportion on a no-P2P box*** — see §4.
- **"MoE / expert-parallel all-to-all traffic in decode."** **NOT taken — genuine gap.** I did not find a decode-phase MoE all-to-all byte measurement on a PCIe-only no-P2P consumer box. 2607.19539 measures on 4×A100 (NVLink); sglang #33422 is prefill-dominated.

---

## 4. What remains, if anything

**Nearest neighbour X does A.** 2605.30571 does A: it measures decode's achieved bandwidth against its analytic memory floor and corrects the attribution with a matched control, but **entirely on single GPUs** (H100/A100/L40S/L4, one at a time, batch-1, 7–8B GQA). It contains **no communication term at all**. 2507.14392 supplies that term with validated volume models — but on **NVLink + InfiniBand**, exactly the regime where the brief's suspicion is least visible.

**Under condition C it misses B.** C = a **PCIe-only, no-P2P, host-staged** box, GPUs split **4+4 across two NUMA nodes**. The sub-questions the retrieved corpus leaves open under C: (i) the **byte-vs-time disproportion** of the all-reduce at decode-sized messages, measured on *that* box; (ii) whether the KV read is actually **bounded by DRAM bandwidth** at long context on such a box, or hides under idle bandwidth; (iii) a byte-level decode attribution at all. club-3090 and rtx6kpro measure *neighbouring* conditions (P2P patched on; 4-GPU same-NUMA vs 8-GPU cross-socket) but not host-staged no-P2P.

**Doing B differs because** the two terms scale on *different axes*: the all-reduce is `B·d_model·2` bytes per layer — **independent of C** — while the KV read is `O(B·C)`. So the TP penalty is a near-constant per-step tax whose *fraction* is set by the (B, C) mix, added to a step that is **already not bandwidth-limited**. The counter-intuitive consequence: adding TP does **not** buy back time proportional to the weight bytes it removes, because the weight stream is not bandwidth-bound in the first place.

### Magnitude — LABELLED

**MEASURED, first-hand (this session; `schoolserver` GPU 2 — the brief's own box; Qwen3-4B bf16; config read back from `config.json`: hidden 2560, 36 layers, 32 heads, 8 kv_heads, head_dim 128; weights 7672.2 MiB; KV 147456 B/token):**

- **Topology confirms the brief exactly.** `nvidia-smi topo -m`: every GPU pair `SYS`; `nvidia-smi topo -p2p r`: every pair **`CNS` = Chipset not supported**; GPU0–3 NUMA 0, GPU4–7 NUMA 1. P2P is not merely unused — it is **unavailable**.
- **Single decode step is flat in context (B=1):** 23.475 ms @C=1024 → 23.316 ms @C=32768 → 28.865 ms @C=65536, while roofline-predicted bytes rise 347 → 613 GB/s.
- **The roofline is falsified on real hardware:** at C=32768, B=64 the folk model predicts **11,512 GB/s — 11.4× the 1008 GB/s nominal peak** — yet the measured step is 27.561 ms, only **1.17×** the C=1024/B=1 cell (23.475 ms). KV is 97.5% of predicted bytes there.
- **The KV read hides under idle bandwidth:** isolated attention delta at C=32768 = **3.213 ms** for 4608 MiB ⇒ **1503.7 GB/s apparent — above the 1008 GB/s nominal peak**, possible only because DRAM was *not* saturated.
- **The weight stream itself runs at 313–315 GB/s = 31% of nominal peak** (7672 MiB ÷ 25.65 ms).
- **Host-staged all-reduce, 2× RTX 4090, NCCL, message = `B·2560·2` B:** 5 KB → **28.4 µs** (0.18 GB/s); 64 KB → 25.9 µs; 655 KB → 70.5 µs; **4 MB → 290.0 µs = 14.47 GB/s**. Latency is **flat from 5 KB to 64 KB** ⇒ small-message regime is pure overhead. `NCCL_P2P_DISABLE=1` and default agree within noise (28.4/25.9/70.5/290.0 vs 30.7/26.4/72.6/291.4 µs), independently confirming P2P is unavailable.
- PCIe pinned copy: **H2D 23.4–24.7 GB/s, D2H 24.4–25.0 GB/s**.
- **Cross-confirmation:** my 14.47 GB/s and nccl-tests issue #117's 13.23 GB/s bracket the brief's "~12–13 GB/s" within ~11%. The folk constant is real.

**ESTIMATED (my arithmetic on the measured numbers above — not measured):**

- TP=2, B=1: 2 all-reduces × 36 layers = **72 collectives/step** × 28.4 µs = **2.045 ms ≈ 8.0%** of a 25.65 ms step.
- Those collectives carry 72 × 5120 B = **368,640 B = 0.0092%** of the per-rank weight bytes (4.022 GB at TP=2) ⇒ **≈870× disproportion between time share and byte share** at B=1. At B=128 the tax is 72 × 70.5 µs = **5.08 ms ≈ 16.0%** of a 31.753 ms step for 1.17% of the bytes (≈14×).
- At B=1, C=32768, TP=2: the all-reduce moves **6,553× fewer bytes** than the KV read (368,640 B vs 2.416 GB per rank) yet costs **2.05 ms vs 3.21 ms** — comparable time, ~four orders of magnitude less data.

**Kill condition.** Run a **layer-truncated batch-1 decode step** (`num_hidden_layers` ∈ {6,12,18,36}, `ignore_mismatched_sizes=True`) and regress step time on layer count. If time **∝ layers** (slope ≈ 313 GB/s of weight bytes), the weight read is bandwidth-bound, TP genuinely halves the dominant term, and the ~2.0 ms tax can be repaid ⇒ **the surviving slice is dead**. If time is **flat in layer count** (launch-bound), TP buys back nothing and the tax is purely additive ⇒ **survival holds**. One bit, cheap, decisive.

**CHEAPEST bounding experiment** (bandwidth-shaped, per the brief's constraint that only bandwidth-side quantities survive ~300 load on 192 cores): the layer-truncation regression above, read out as **achieved GB/s per layer** rather than latency — 4 model loads (~1 min each; GPUs 2/3/5/6 free) + ~30 s. A slope near zero, or a weight-stream rate far below a plain large `memcpy`'s achieved rate, proves launch-bound with no sub-millisecond timing needed. **Secondary**: the 2-rank aggregate all-reduce loop I already ran — repeat it on a **cross-NUMA pair (2,5)** vs same-NUMA (2,3) and compare achieved GB/s; if that ratio sits inside the box's 4–10% spread, the cross-NUMA asymmetry is also dead.

**Unusable on this box, and why it matters:** genuine `dram__bytes` counters were **not available** to me — `nvidia-smi dmon --gpm-metrics 10` returns `-` on GeForce 4090 (consumer silicon), and pynvml's PCIe byte counters (`NVML_FI_DEV_PCIE_COUNT_RX_BYTES/TX_BYTES`) read 0. I therefore used **time-on-critical-path + analytic byte counts** — the *same substitution* 2605.30571 reports making (*"Nsight Compute (ncu) was blocked… We use torch.profiler with analytic byte counts as the substitute"*). **No retrieved artifact reconciles hardware DRAM byte counters against predicted weights+KV volume — I did not find it.**

**Honest assessment.** The *single-GPU* half of Direction 6 is occupied cleanly; do not pursue it. The *interconnect constant* is occupied. The *TP-vs-PP-over-PCIe* question is contested rather than settled, and two of the three closest controls point **against** the folk claim. The defensible residue is narrow: a **measured byte-vs-time disproportion for the all-reduce on a no-P2P host-staged box**, and the **KV-hides-under-idle-bandwidth** result, whose value rests on the two accounting methods disagreeing by ~3 orders of magnitude. Worth one aggregate experiment, not a programme.

---

## 5. Search log

**Surfaces used.** `web_search`; direct `curl -sL --retry 4 --retry-delay 2 --retry-all-errors -A "<Chrome UA>"` against `arxiv.org/abs/<id>`, `arxiv.org/html/<id>[v1]`, `raw.githubusercontent.com/<o>/<r>/<ref>/<path>`, `github.com/{o}/{r}/issues/<n>`, `github.com/{o}/{r}/wiki/<page>`, `rep.bntu.by`, `habr.com`, `kentino.se`. `api.github.com` not used (rate-limited to zero here). `export.arxiv.org` / `arxiv.org/search` avoided per project 429/406 behaviour. `web_fetch` avoided per brief.

**Exact queries issued (Direction 6).**
1. `tensor parallelism vs pipeline parallelism PCIe no NVLink decode 4090 measurement`
2. `PCIe-only multi-GPU LLM inference all-reduce overhead tensor parallel measurement`
3. `decode bandwidth breakdown weights vs KV cache measured breakdown attribution`
4. `LLM decode time breakdown per-op profiling measured where time goes tensor parallel communication`
5. `hidden communication cost tensor parallel decode all-reduce per layer small message latency`
6. `activation memory traffic under-counted long context decode bandwidth accounting`
7. `"memory-bound" decode LLM myth weights KV cache bandwidth breakdown measured rethinking`
8. `decode bottleneck attribution wrong measured GPU memory throughput breakdown LLM`
9. `long context decode communication overhead under-counted activation traffic tensor parallel`
10. `tensor parallel decode all-reduce traffic small tensors PCIe bottleneck measured consumer GPUs no NVLink`
11. `multi-GPU decode communication volume per layer measured bytes all-reduce disproportionate`
12. `8x 4090 tensor parallel inference measurement PCIe all-reduce bandwidth bottleneck`
13. `"pipeline parallelism" better than "tensor parallelism" PCIe consumer GPU decode measured recommendation`
14. `multi-GPU 4090 inference tensor parallel bad PCIe bandwidth bound analysis measured`
15. `PCIe host-staged all-reduce no P2P GPU decode latency measurement consumer`
16. `KV cache read bandwidth dominates decode long context crossover measured weights`
17. `host staged all-reduce no P2P GPU decode measurement bytes transferred`
18. `decode memory traffic attribution activation traffic long context under-counted measure`
19. `decode phase memory bandwidth breakdown weights vs KV cache measured profiling LLM inference`
20. `LLM inference per-op profiling where time goes decode attention MLP bandwidth`
21. `roofline analysis decode memory bound LLM inference arithmetic intensity long context`
22. `all-reduce communication overhead tensor parallel decode long context hidden cost`
23. `decode KV cache read volume context length bandwidth function measurement`
24. `measured DRAM traffic LLM decode NVML memory throughput counter profiling`
25. `LLM decode small batch memory bandwidth not saturated latency bound achieved bandwidth fraction of peak`
26. `LLM decoding does not saturate HBM bandwidth batch size 1 inefficiency measured percent of peak`
27. `why LLM decode batch 1 achieves only 30 percent of memory bandwidth kernel launch overhead`
28. `decode phase roofline model overestimates latency memory bound assumption wrong measurement`

**Local apparatus exercised.** `nvidia-smi topo -m` / `topo -p2p r`; `nvidia-smi dmon -s umt --gpm-metrics 10,5,1`; pynvml field-value API; torch CUDA events; HF transformers `DynamicCache`/`StaticCache` decode paths; NCCL 2-rank all-reduce with and without `NCCL_P2P_DISABLE`.

**Could NOT verify — absences I observed, NOT global negatives.**

- **r/LocalLLaMA was entirely unretrievable** — `reddit.com` and `old.reddit.com` returned **HTTP 403** on `/search.json`, `/search` and plain threads for browser UA, Firefox UA, python-requests and curl. Treat as **not-retrieved**, not as a negative. Practitioner evidence for PCIe TP-vs-PP may well live there.
- I did **not find** any artifact that measures the tensor-parallel all-reduce **byte-vs-time disproportion on a no-P2P, host-staged box** at matched (B, C), nor one reporting the all-reduce tax as a fraction of a decode step on that box.
- I did **not find** any artifact reporting **hardware DRAM byte counters reconciled against predicted weights+KV volume**. Every KV-volume figure retrieved is analytic/accounting.
- I did **not find** a decode-phase **MoE / EP all-to-all byte measurement on a PCIe-only no-P2P consumer box** (nearest: sglang #33422, prefill-dominated; 2607.19539, 4×A100 NVLink).
- I did **not** produce the primary source behind the second-hand 715/483 tok/s NVLink benchmark, nor the IEEE item *Exploring the Feasibility and Performance of Distributed LLM Serving on Consumer-Grade GPUs* (ieeexplore HTTP 202, empty; DOI unverified). Neither is cited above.
- **`2605.30571`'s own per-cell tables beyond the abstract/§6** were not exhaustively transcribed; its quotes here are from pages I actually fetched (abstract + body §6), not from tables I did not read.
- Rows marked `[sub-A]`, `[sub-B]`, `[parent]`, `[prior]` were fetched and title-read-back by my verification subagents, the parent agent, or a prior workspace draft — **not re-fetched by me**. Their quotes are reproduced from that verification.
- I could **not** measure genuine DRAM byte counters or PCIe byte counters on this box (see §4), so **all absolute byte figures are analytic**; only latency and apparent-GB/s are directly measured.
- Two measurement routes failed and are **not** used as evidence: a `StaticCache` path raised `index_copy_(): index out of bounds` at 64K, and a 128K-context prefill OOM'd (18.4 GB KV on a 24 GB card). An earlier decode loop silently recomputed the prefix (cache length stayed 4) — **those numbers are discarded**; the tabulated runs are the corrected ones.
- **No absence claim in this document should be read as "it does not exist."**

---

## 6. Assembling-agent verification addendum (added 2026-09-15 by the parent/S3b lead)

§2's provenance key uses `[self]` to mean *the Direction-6 subagent*. Under this workspace's convention (`notes/SM89_HARDWARE_AXIS_2026-09-15.md`: *"SUBAGENT = a delegated agent reports it; it is NOT verified by me"*), those rows are **SUBAGENT-reported**. Below is what I, the agent that assembled this file, **independently re-ran or re-read**. Items not listed here remain SUBAGENT-reported.

**RE-VERIFIED BY ME (RUN / FETCH, first-hand):**

| Claim in §2/§4 | How I checked it | Result |
|---|---|---|
| Box topology: 8× RTX 4090, all pairs `SYS`, P2P impossible, 4+4 across NUMA | I ran `nvidia-smi topo -m`, `nvidia-smi topo -p2p r`, `--query-gpu=index,name` on `schoolserver` myself | **CONFIRMED exactly.** All 8 are `NVIDIA GeForce RTX 4090`; every off-diagonal cell `SYS`; every P2P cell `CNS`; GPU0–3 CPU affinity `0-47,96-143` NUMA 0, GPU4–7 `48-95,144-191` NUMA 1 |
| Measurement harness exists and is not hand-typed | `ssh schoolserver`, listed and `cat`-ed `/home/user/d6meas/` | **CONFIRMED.** 23 files incl. `att.py/att.json`, `comm.py/comm_nop2p.json/comm_p2p.json`, `kv2.py/kv2.json`, `kv3.py/kv3.json`, `decode.json`, `static.json`, `fwd.json`, `probe.py`, `dmontest.py` |
| Host-staged all-reduce: 5 KB 28.4 µs, 64 KB 25.9 µs, 655 KB 70.5 µs, **4 MB 290.0 µs = 14.47 GB/s** | read `comm_nop2p.json` raw | **CONFIRMED verbatim** (`{"KB":4096,"us":290.0,"eff_GBs":14.47}`) |
| P2P-disabled ≈ default ⇒ P2P unavailable | read `comm_p2p.json` raw | **CONFIRMED** (5 KB 30.7 µs / 64 KB 26.4 µs — within noise of the no-P2P arm) |
| Attention isolation at C=32768: 3.213 ms, 4608 MiB, 1503.7 GB/s apparent, weights 313.6 GB/s | read `att.json` raw | **CONFIRMED verbatim** |
| B=1 decode step flat in context | read `kv2.json` + `kv3.json` raw | **CONFIRMED in both harnesses** — `kv2`: 23.475 ms @C=1024 vs 23.316 ms @C=32768; `kv3`: 28.849 @C=32768, 28.851 @C=49152, 28.865 @C=65536 |
| nccl-tests #117 = 13.23 GB/s @4 MB, P2P disabled, on 4090s; issue **CLOSED** | I fetched the issue HTML myself and extracted the embedded `"body"` JSON | **CONFIRMED verbatim.** The run header reads `user@4090-INSTALL:~/nccl-tests$ NCCL_P2P_DISABLE=1 ./build/all_reduce_perf -b 8 -e 128M -f 2 -g 2` with `Rank 0/1 … NVIDIA GeForce RTX 4090`, and the row `4194304 1048576 float sum 317.0 13.23 13.23 0e+00 320.2 13.10 13.10 0e+00`. State badge `>Closed<`, `"state":"CLOSED"`, `stateReason: COMPLETED` |
| SLIM = arXiv 2607.29575 | abs-page title read-back | **CONFIRMED** — *"SLIM: Saturation-Aware Lightweight Performance Modeling for LLM Serving"*, Pol G. Recasens et al., 31 Jul 2026 |
| 2605.30571 §6 quote and the `ncu` admission | I fetched `arxiv.org/html/2605.30571v1` myself | **CONFIRMED verbatim** (LaTeX macros stripped): *"At batch 1 single-token decode the attention KV traffic per step is modest at ctx ≤ 8192, and on H100 the binding constraint is the launch of the many small kernels that make up a transformer layer, not the throughput of attention itself."*; *"Nsight Compute (ncu) was blocked on Modal cloud containers: ncu requires host driver access we did not have. We use torch.profiler with analytic byte counts as the substitute."* |

**TWO CORRECTIONS I am making to §4 on the strength of the raw files.**

1. **The "KV read hides under idle bandwidth" result has a ceiling, and §4 states it without one.** `att.json` contains a *second* row the summary did not use: at **C=65536 the apparent attention bandwidth is 489.3 GB/s — below the 1008 GB/s nominal peak — and attention is 43.7% of the step** (19.75 ms of 45.245 ms), versus 1503.7 GB/s / 11.1% at C=32768. So the correct claim is not "the KV read hides under idle DRAM bandwidth" but **"the KV read hides only up to ~32K context on this box; it stops hiding between 32K and 64K."** That crossover is the sharper and more defensible result, and it is the one worth building on.

2. **The two harnesses disagree on the absolute step time at the cell they share.** `kv2.json` gives **C=32768/B=1 = 23.316 ms**; `kv3.json` gives **C=32768/B=1 = 28.849 ms** — a 5.5 ms (24%) gap for identical (model, context, batch). The *flatness* conclusion is robust (it holds within each harness), but the absolute milliseconds are **not reproducible across harnesses**, so no percentage-of-step figure should be quoted from a single harness without saying so. §4's "2.045 ms ≈ 8.0% of a 25.65 ms step" inherits this: the numerator is from `comm_nop2p.json`, the denominator from a third harness. Treat the ~8% as an estimate with a ±25% harness term, and note the C=32768/B=1 denominator is alternatively 23.3 ms or 28.8 ms.

**Also noted, not corrected:** §4's headline roofline-falsification cell (C=32768/B=64 ⇒ 11,512 GB/s predicted) is **not in the four JSON files I read**; the largest batch in `kv2.json` is B=32, and I did not locate the B=64 row. The B=32 row (`pred_GBs` 469.2 at C=1024, `kv_share_pct` 37.5) and the B=8 row (C=32768/B=8: 26.779 ms, `pred_GBs` **1743.9**, `kv_share_pct` **82.8**) do support the same qualitative conclusion — predicted bandwidth of 1743.9 GB/s is already 1.73× nominal peak while the measured step is flat at 26.779 ms. **I would state the falsification using the B=8 / 1743.9 GB/s row, which I have read in the raw file, rather than the B=64 / 11,512 GB/s row, which I have not.**

**Unchanged by this addendum:** the verdict (**PARTIALLY OCCUPIED**), the deciding artifact (2605.30571), the §3 taken-list, and the §4 kill condition — the layer-truncation regression is still the cheapest decisive experiment, and it is bandwidth-shaped as the brief requires.

---

# Direction 8 — Agentic LLM runtime / scheduling for agent loops
### Adversarial prior-art / occupancy check · 2026-09-15 · cost: 0 GPU·h

---

## 1. Verdict

**OCCUPIED**

**Single deciding artifact:** [SAGA: Workflow-Atomic Scheduling for AI Agent Inference on GPU Clusters](https://arxiv.org/abs/2605.00528) — preprint v2 (19 Jun 2026), reads back verbatim: *"propose a shift to program-level scheduling: treating the entire agent workflow (not individual inference calls) as the first-class schedulable unit"*, with *"Agent Execution Graphs that capture workflow structure to predict KV cache reuse across tool-call boundaries"* and a measured **38% of execution time spent regenerating KV cache between agent steps**.

**Co-deciding (removes the "preprint-only" escape):**
- [ThunderAgent](https://arxiv.org/abs/2602.13692) — recompute cost is an explicit term in the scheduler's objective and the scheduler's priority score is a literal function of KV context length (`1/c_P`), with a proof that squared recompute cost is minimised by shortest-first eviction.
- [sgl-project/sglang PR #23315](https://github.com/sgl-project/sglang/pull/23315) — **state MERGED** (verified `"state":"MERGED"`, 2026-04-21). Ships `--strip-thinking-cache`; contains a **matched-control prefill-token measurement** (two servers differing only in the flag).

Every one of the five claims in the brief is already claimed and, in at least three cases, *measured under a matched control*. The field has also moved **past** the brief's framing: it is no longer "schedule the agent step" but "schedule the whole workflow DAG", with inter-step prefix reuse as a named scheduling input.

---

## 2. Prior-art table

Ordered strongest-first. "State I verified" = the state string I read myself on the fetched page. Quotes are literal text from the page named in that row's URL; where a quote comes from a *citing* page rather than the artifact's own page, the column says so explicitly.

| # | URL | State I verified myself | Verbatim quote (text I literally read) | Metric it reports | Control |
|---|---|---|---|---|---|
| 1 | [arxiv.org/abs/2605.00528](https://arxiv.org/abs/2605.00528) | preprint (v1 1 May 2026, v2 19 Jun 2026; cs.DC) | *"propose a shift to program-level scheduling: treating the entire agent workflow (not individual inference calls) as the first-class schedulable unit"*; *"discarding gigabytes of intermediate state between steps and inflating end-to-end latency by 3 to 8×"*; *"vLLM v0.6.0 spends 38% of execution time regenerating KV cache between agent steps; SAGA reduces this to 8% (−30 pp)"*; ablation table *"w/o Session affinity 398.2 ± 22.4 +96%"*; *"reduces task completion time by 1.64× … over vLLM v0.15.1 with prefix caching and affinity routing"* | **measured latency/traffic** (KV-regen time share, end-to-end latency, task completion time, HBM util) | **HAS-CONTROL** (vLLM v0.15.1+APC+affinity routing; per-mechanism `w/o` ablations) |
| 2 | [arxiv.org/abs/2602.13692](https://arxiv.org/abs/2602.13692) | preprint (14 Feb 2026, cs.OS) | *"An agentic program is a first-class scheduling unit that persists across multiple model invocations and tool executions, exposing semantic state to the runtime."*; *"Cost_total ≈ Cost_decode + Cost_prefill + Cost_recompute + Cost_unused + Cost_caching"*; *"The remaining terms are wasted system overheads: Cost_recompute stems from KV cache thrashing"*; *"Lemma 4.1 (Quadratic Recomputation Cost). … Cost_recompute ∝ c_i²"*; scheduler scores *"S_restore(P) = 1/c_P + 𝕀(τ=R)"*, *"S_pause(P) = 1/c_P + 𝕀(τ=A)"* | **measured latency/traffic** (end-to-end latency breakdown, throughput, KV hit rate, disk GB) | **HAS-CONTROL** (baseline vLLM+Gateway; back-to-back vs Continuum) |
| 3 | [github.com/vllm-project/vllm/issues/37003](https://github.com/vllm-project/vllm/issues/37003) | **OPEN** (RFC, `"state":"OPEN"`) | *"Agentic workloads break prefix caching under concurrent load. Over 90% of tokens in a typical agent turn are prefixes reused verbatim from the previous turn… 40–60% of session wall time is spent paused on tool calls, and during those pauses the agent's blocks are unreferenced. Under concurrent load, other agents evict them via LRU. When the session resumes: cache miss, full recomputation of the entire context — which grows to 70K–200K tokens by session end."*; commenter: *"We measured 139K to 563K redundant recomputed prefill tokens (per-request ledger accounting) in runs where the preemption counter stayed at 0 the whole time."* | **measured latency/traffic** (redundant recomputed prefill **tokens**, counted per-request) | **HAS-CONTROL** (control arm *"always prefix-affinity (EPP engaged, directive emission off)"*) |
| 4 | [github.com/sgl-project/sglang/pull/23315](https://github.com/sgl-project/sglang/pull/23315) | **MERGED** (`"state":"MERGED"`, merged 2026-04-21) | *"~1.3 GB per turn for QwQ-32B, ~1.6 GB for R1-Distill-70B, displacing useful shared prefixes under eviction pressure."*; matched A/B probe, *"Same harmony prompt on two servers (only `--strip-thinking-cache` differs)"*: `STRIP OFF probe=109 Prefill: #new-token=1 #cached-token=108` / `STRIP ON probe=109 Prefill: #new-token=50 #cached-token=59` | **measured latency/traffic** (cached vs newly-prefilled **tokens**, per-turn GB) | **HAS-CONTROL** (same workload, same probe, flag toggled) |
| 5 | [github.com/vllm-project/vllm/pull/39806](https://github.com/vllm-project/vllm/pull/39806) | **OPEN** (`"state":"OPEN"`) | *"answer tokens stranded behind thinking branches are unreachable, forcing recomputation every turn"*; *"Waste GPU memory: ~1.3–1.6 GB per dead branch (5000 thinking tokens × 256–320 KB/token)"*; *"With 50 concurrent conversations, dead branches consume 65–80 GB."*; table *"Overall cache hit rate 11.1% → 17.1% (+6.0%)"*, *"TTFT p50 9.98s → 8.96s (−1.02s)"* | **measured latency/traffic** (cache hit rate, TTFT p50, GB) | **HAS-CONTROL** — but the benchmark row is explicitly *"A benchmark on the equivalent SGLang fix (sgl-project/sglang#22617)"*, and I verified #22617 is **CLOSED, unmerged, superseded** ⇒ control is real, artifact is superseded |
| 6 | [arxiv.org/abs/2511.02230](https://arxiv.org/abs/2511.02230) | preprint (Continuum / CacheTTL) | *"If the KV cache was evicted for this step, the engine must recompute the prefix (prefill) or reload from CPU … when the tool execution completes and the next inference step begins. This repetitive prefill introduces substantial delays"*; TTL *"determined by the reload cost and potential queueing delay induced by eviction"*; *"improves the average job completion times by over 8× while improving throughput"*; Table 1 compares vs vLLM / Autellix / Pie / InferCept on *"Retains KV Cache"* / *"Per-Turn Queueing Delay"* / *"Bounds Retention Time"* | **measured latency/traffic** (average + P95 job completion time, throughput) | **HAS-CONTROL** (vLLM, Autellix, Pie, InferCept) |
| 7 | [arxiv.org/abs/2502.13965](https://arxiv.org/abs/2502.13965) | preprint (Autellix) | *"existing LLM serving systems ignore dependencies between programs and calls, missing significant opportunities for optimization"*; *"treats programs as first-class citizens to minimize their end-to-end latencies"*; *"preempt and prioritize LLM calls based on their programs' previously completed calls"*; *"improves throughput of programs by 4-15x at the same latency compared to state-of-the-art systems, such as vLLM"* | **measured latency/traffic** (program throughput at matched latency) | **HAS-CONTROL** (vLLM, at same latency) |
| 8 | [arxiv.org/abs/2605.16637](https://arxiv.org/abs/2605.16637) | preprint (HexAGenT) | *"users experience the end-to-end latency of an entire workflow, not the latency of any single LLM call"*; *"models each request as an online-revealed DAG, maintains a running estimate of the workflow's standalone completion horizon, prioritizes ready calls by projected risk of missing that horizon"*; *"reduces the SLO scale required … by an average of 20.1% at 95% attainment and 33.0% at 99% attainment"* | **measured latency/traffic** (SLO attainment vs required scale) | **HAS-CONTROL** (representative agentic workloads, heterogeneous A100/H100/H200) |
| 9 | [arxiv.org/abs/2605.07238](https://arxiv.org/abs/2605.07238) | preprint (FATE) | *"scheduling decisions affect not only the currently ready stage, but also the execution state inherited by downstream stages, including model residency, parent-output locality, prefix reuse, and future device reachability"*; *"future-state preservation should be treated as a first-class scheduling objective for heterogeneous LLM workflow serving"* | **measured latency/traffic** (normalized makespan 0.675, P95 0.677) | **HAS-CONTROL** (RoundRobin, classical DAG scheduling, proxy adaptations of recent workflow-serving policies) |
| 10 | [arxiv.org/abs/2601.12967](https://arxiv.org/abs/2601.12967) | preprint (Sutradhara) | *"tool calls account for 30-85% of FTR latency, KV cache hit rates collapse despite substantial context reuse across iterations, and sequential orchestration wastes potential intra-request parallelism"*; *"orchestrator-aware cache management that uses semantic hints to improve hit rates and reduce thrashing"*; *"sustains up to 77% higher load at the same median FTR latency"* | **measured latency/traffic** (production-scale FTR latency, load) | **HAS-CONTROL** (same median FTR latency, load-vs-latency tradeoff) |
| 11 | [arxiv.org/abs/2606.01065](https://arxiv.org/abs/2606.01065) | preprint (Leyline) | *"Production agentic harnesses fall back to re-prefill on every edit, paying full prefix-recomputation cost"*; *"The splice kernel lifts replay cache-hit by +11.2 pp and cuts latency by up to 241 ms. A ten-line truncation rule routed through the same interface lifts agentic solve rate by +14.3 pp on debug-gym."* | **measured latency/traffic** + **decision-level/task-success** (solve rate) — the only artifact I found reporting a *task-outcome* delta from cache management | **HAS-CONTROL** (baseline = re-prefill on every edit) |
| 12 | [github.com/gauravapiscean/agentic-kv-cache](https://github.com/gauravapiscean/agentic-kv-cache) | repo (README read; 2 commits, MIT) | *"under capacity pressure, most recomputation comes from tool-calling loops seconds apart, not from sessions idling past a TTL — and the TTL never fires at all"*; *"Requests arriving after a gap longer than 5 minutes account for 17.5% of recompute. Requests arriving within 10 seconds account for 33.1%."*; *"The dominant source of cache misses here is tight two-second tool loops whose 88k-token working sets exceed cache capacity — a capacity problem, not a liveness-prediction problem."* | **measured latency/traffic** (recompute **tokens** bucketed by idle gap, on 68,266 real requests / 393 Claude Code sessions) | **HAS-CONTROL** (policy-independent replay; capacity sweep 1k→100k blocks; 3 alternative policies vs shipped LRU; reproduces Mooncake's published curve) |
| 13 | [github.com/vllm-project/vllm/issues/42185](https://github.com/vllm-project/vllm/issues/42185) | **CLOSED** (`"state":"CLOSED"`) | *"RAG mean delta: +0.2% (noise). Decision threshold: ≥+5%."*; *"The true reordering benefit is +0.2%."*; root cause *"the CA and stock schedulers run on identical async pipelines"*; *"since filing, the ecosystem has moved in exactly this direction — prefix-cache-aware routing landed as a first-class concern in llm-d and AIBrix"* | **measured latency/traffic** (req/s, mean latency, 4 cells × 2 seeds) | **HAS-CONTROL** (stock `AsyncScheduler` vs `CacheAffinityScheduler`, matched pipeline) — **NEGATIVE result on the request-reordering lever** |
| 14 | [arxiv.org/abs/2606.30560](https://arxiv.org/abs/2606.30560) | preprint (TraceLab) | *"we collect and release a trace of roughly 4,300 coding-agent sessions, containing about 350,000 LLM steps and 430,000 tool calls"*; *"coding-agent workloads feature long autonomous loops, long contexts with short outputs, diverse and heavily-tailed tool calls, and high but imperfect prefix cache hit rates"* | **measured latency/traffic** (trace characterization) | **NO-CONTROL** (characterization/measurement paper; no optimization baseline) |
| 15 | [arxiv.org/abs/2608.15127](https://arxiv.org/abs/2608.15127) | preprint (AgentSysBench) | *"non-LLM components dominating latency in 5 of 10 applications and sandbox working-set memory peaking at 28 GB per session"*; *"task-aware serving reduces latency by 29–40%"*; *"tool-result caching removes 35.2% of redundant search calls"* | **measured latency/traffic** (component latency shares, task latency, redundant calls) | **HAS-CONTROL** (four design explorations each against a baseline) |
| 16 | [github.com/llm-d/llm-d/…/agentic-serving/README.md](https://github.com/llm-d/llm-d/blob/main/guides/agentic-serving/README.md) | shipped docs (raw main read) | *"The Agentic Inference SIG northstar sets the broader direction: **session-graph orchestration**, **program-aware scheduling**, **zero-recompute state reuse** with typed retention, and **proactive state placement** ahead of fan-out."*; *"high reusable locality (cache hit rate, not FLOPs, sets throughput)"* | soft metric at doc level (points to sub-guide benchmarks) | **UNVERIFIABLE** (the guide defers numbers to per-model sub-guides I did not fetch) |
| 17 | [arxiv.org/abs/2609.10964](https://arxiv.org/abs/2609.10964) | preprint (Sep 2026) | *"Agentic LLM workflows consist of sequences of model turns interleaved with tool interactions, so their end-to-end completion time depends not only on inference speed but also on when ready turns are released."*; *"reduces the P95 of workflow flow time under contention, achieving up to a 3.50× speedup"* | **measured latency/traffic** (P95 workflow flow time) | **HAS-CONTROL** (eager release baseline, on real SE traces) |
| 18 | [arxiv.org/abs/2604.11378](https://arxiv.org/abs/2604.11378) | preprint, **position paper** | *"We characterize the Agent Loop as a single ready unit scheduler: at any moment, at most one executable unit is active, and the choice of which unit to activate comes from opaque LLM inference rather than an inspectable policy."*; *"This is a position paper and design proposal. We provide a theoretical framework, design analysis, and experimental protocol, not a production implementation or empirical results."* | none (no empirical results) | **NO-CONTROL** |
| 19 | [arxiv.org/abs/2405.19888](https://arxiv.org/abs/2405.19888) · [2407.00326](https://arxiv.org/abs/2407.00326) · [2604.15186](https://arxiv.org/abs/2604.15186) | preprints (Parrot / Teola / Scepsy) | Parrot: *"creates the data pipeline when connecting multiple LLM requests"*; *"perform conventional data flow analysis to uncover the correlation across multiple LLM requests"*. Teola: *"represents each query's workflow as a primitive-level dataflow graph"*. Scepsy: *"Scepsy decides on GPU allocations based on these aggregate shares"* | **measured latency/traffic** (Teola *"up to 2.09x speedup"*; Scepsy *"up to 2.4x higher throughput and 27x lower latency"*) | **HAS-CONTROL** (Teola vs existing orchestration frameworks; Scepsy vs per-LLM-optimal allocation) |
| 20 | [arxiv.org/abs/2507.07400](https://arxiv.org/abs/2507.07400) · [arxiv.org/abs/2411.19379](https://arxiv.org/abs/2411.19379) · [arxiv.org/abs/2506.02634](https://arxiv.org/abs/2506.02634) | preprints — **titles read back and confirmed; performance claims quoted from the citing page, not their own pages** | as cited in vLLM #37003: *"KVFlow achieved up to 2.19× speedup with workflow-aware scoring"*; *"MARCONI replaced LRU with cost-aware eviction: up to 34.4× higher token hit rates and 71.1% lower TTFT"*; *"Alibaba's study of production LLM traffic found 10% of KV blocks account for 77% of reuses. Workload-aware eviction achieved up to 41.9% lower mean response time vs. LRU."* | **measured latency/traffic** (per the citing page) | **UNVERIFIABLE** (I did not read these three papers' own evaluation sections) |

---

## 3. What is genuinely taken

- **"Scheduling unit = the agent step, not the request."** Taken, and *superseded*: every 2026 system schedules the **program / workflow DAG**, not the step. Explicit first-class-unit claims in Autellix (*"programs as first-class citizens"*), ThunderAgent (*"first-class scheduling unit"*), SAGA (*"the entire agent workflow … as the first-class schedulable unit"*), HexAGenT (*"online-revealed DAG"*), Scepsy, Teola, Parrot, FATE.
- **Dependency-awareness as a distinct problem from independent-request serving.** Taken: Autellix's entire premise is *"existing LLM serving systems ignore dependencies between programs and calls"*; HexAGenT does online-revealed DAG scheduling; 2609.10964 does tail-risk-aware turn *release*.
- **Inter-step prefix/KV reuse as a first-class scheduling resource.** Taken, three independent ways:
  1. **In the objective** — ThunderAgent puts `Cost_recompute` in the cost model and proves `Cost_recompute ∝ c_i²`, deriving shortest-first eviction;
  2. **In the priority score** — ThunderAgent's `S_restore`/`S_pause` are literally `1/c_P` (KV size *is* the scheduling signal);
  3. **In the eviction API** — vLLM #37003 proposes a per-token-range `RetentionDirective` with priority, i.e. the orchestrator explicitly arbitrates cache; SGLang ships session-reference-aware radix cache (#29173, MERGED) and `--strip-thinking-cache` (#23315, MERGED).
- **The measured cost of NOT reusing.** Taken, and measured under matched controls at three different granularities:
  - *time share*: SAGA Fig. 1(a) — 38% of execution time regenerating KV between agent steps, reduced to 8%;
  - *token count*: SGLang #23315 — same probe, flag off = `#cached-token=108`, flag on = `#cached-token=59`; vLLM #37003 comment — 139K–563K redundant recomputed prefill tokens;
  - *bytes*: vLLM #39806 — ~1.3–1.6 GB per dead branch, 65–80 GB across 50 conversations;
  - *bucketed by cause on real traces*: agentic-kv-cache — 33.1% of recompute from gaps < 10 s vs 17.5% from gaps > 5 min.
- **The scheduling objective for dependent requests.** Taken: makespan / task completion time / SLO attainment (SAGA, HexAGenT), P95 flow time with CVaR (2609.10964), program throughput at matched latency (Autellix), JCT (Continuum), plus an explicit fairness metric (SAGA *"Agent Fair Share, a task-completion-time fairness metric with provable bounded-deviation guarantees"*).
- **Prefix-cache-aware routing.** Shipped and, per vLLM #42185's close message, *"landed as a first-class concern in llm-d and AIBrix"*; llm-d's agentic-serving guide names *"zero-recompute state reuse with typed retention"* as the SIG northstar.
- **A negative result on the naive version of the idea.** vLLM #42185 measured cache-affinity *request reordering* at **+0.2%** and closed, with a documented root cause (sync-vs-async base-class confound inflating the original +7.4%). Any new proposal on this axis must explain why it is not this.

---

## 4. What remains, if anything

**Nearest neighbour X does A.** SAGA / HexAGenT / FATE / AgentSysBench do workflow-level scheduling and cache-reuse arbitration, but every one of them optimizes a **time** quantity: makespan, task completion time, SLO attainment, P95 flow time, latency. Leyline is the only artifact I found that reports a **task-outcome** delta from cache management (*"lifts agentic solve rate by +14.3 pp on debug-gym"*) — and Leyline is a *primitive + policy-space agenda*, not a scheduler that allocates KV/prefix budget so as to maximise solve probability.

**Under condition C it misses B.** C = the reply is not the goal, the task outcome is; and prefix/KV budget is the scarce resource being allocated across a DAG of dependent steps. Under C, none of X schedules on outcome. Concretely, I did not find any artifact whose scheduler priority is a function of *expected task success* (or success-per-GB-of-KV) rather than expected completion time. SAGA's "Agent Fair Share" is still a *time* metric; HexAGenT's risk term is *risk of missing a latency horizon*.

**Why doing B differs.** It changes what the scheduler does when the two objectives disagree: a time-optimal schedule drops an expensive-to-reuse branch that has high success value, an outcome-optimal one keeps it. This is a genuine objective change, not a tuning change.

**Magnitude — LABELLED.**
- **Measured (by others, on their hardware):** the outcome-side headroom of *cache management* is **+14.3 pp solve rate** from a ten-line truncation rule (Leyline, debug-gym). The outcome-side headroom of *task-aware serving* on latency is 29–40% (AgentSysBench). The recompute-side waste is **30 pp of execution time** (SAGA), **139K–563K tokens/run** (vLLM #37003), **~1.3–1.6 GB/turn** (SGLang #23315 / vLLM #39806).
- **Estimated (mine, first-order):** an outcome-conditioned scheduler's headroom over a time-optimal one is plausibly **5–15 pp of task success** at matched KV budget — scaled by analogy from Leyline's +14.3 pp, which was obtained from a *policy* change alone with no scheduling intelligence. **This is an estimate, not a measurement.** I found no artifact that measures it.
- **Kill condition.** If, at matched cache capacity, reordering steps by an outcome proxy changes recomputed prefill tokens by **< 8%** versus FCFS (the gate used elsewhere in this workspace's ledger), the slice is dead — consistent with vLLM #42185's +0.2% and with agentic-kv-cache's finding that the binding constraint is *capacity*, not *ordering policy*.

**Cheapest experiment that would bound it (bandwidth/token-shaped; no vLLM needed).**
Replay a fixed, seeded agent DAG (30–50 steps, tool-interleaved) through HF `transformers` with a hand-written **block-granular prefix cache** (block = 16 tokens) on Qwen3-4B bf16, 1× RTX 4090, at a **fixed block capacity** chosen to force contention. Three arms: (a) reuse ON, (b) reuse OFF, (c) reuse ON with outcome-proxy-prioritised ordering.
Report four quantities, all of which are **countable or bandwidth-side** and therefore survive the load-~300 box's 4–10% spread:
1. **recomputed prefill tokens** (exact counter) per completed task — the direct analogue of the SGLang #23315 `#cached-token` probe;
2. **bytes of KV written** = Σ(blocks × 16 tokens × 2 × n_layers × n_kv_heads × head_dim) — deterministic from shapes;
3. **achieved GB/s** over ≥ 200 repetitions (mean, not tail) for the prefill pass;
4. **steps-to-success / success count** on a small deterministic task set, for the outcome axis.
**Do not** report TTFT/ITL deltas; those are unmeasurable here.
Decision rule: require arm (c) to beat arm (a) by **≥ 8%** on quantity 1 or 2 *and* not lose on quantity 4; otherwise declare the scheduling-objective slice occupied-by-equivalence.

**Honest assessment.** This survivor is thin and I would not fund it as a standalone novelty claim. Its distinguishing axis (outcome vs time objective) is real but not *conditional-differentiating* in the sense this workspace requires: the same hardware/load regime does not create it, and the magnitude rests on an estimate I could not ground in a measurement. The much safer read is that **Direction 8 is occupied end-to-end**, including the measurement the brief asks for.

---

## 5. Search log

**Surfaces and queries actually issued** (`web_search`, then primary fetch):
- `agent serving system LLM scheduling agent steps first-class` · `Parrot LLM agent serving semantic variable` · `Autellix LLM agent serving engine` · `prefix cache aware scheduling vLLM multi-turn agent`
- `Teola LLM serving agent latency SLO` · `Ayo LLM agent serving graph` · `agentic kv cache reuse session scheduling paper` · `prefix cache aware routing LLM serving SGLang vLLM scheduler`
- `"agent serving" OR "agentic serving" scheduler measured prefix recomputation wasted` · `Orla library serving LLM multi-agent systems` · `Continuum KV cache TTL agent serving` · `DAG-level LLM serving program scheduling dependency aware`
- `agent latency breakdown measurement tool call LLM prefill share study` · `"prefix reuse" OR "cache hit" first-class scheduling resource LLM agent steps allocate` · `scheduling objective dependent requests LLM agent task outcome not latency` · `LLM serving DAG dependency aware scheduler prefill reuse arbitration`
- `vLLM RFC prefix cache aware scheduling agentic issue github` · `SGLang router prefix cache aware agentic session` · `llm-d inference scheduling prefix cache KV aware` · `scheduling LLM agent steps task success objective rather than latency`
- `LLM agent serving scheduler optimize task success rate rather than latency budget` · `"cost of not reusing" OR "recomputation cost" measured agent prefix thrown away controlled experiment` · `AgentSysBench characterization implications serving systems`

**Primary artifacts fetched and read (HTML/text):** arXiv abs + HTML for 2605.00528, 2602.13692, 2511.02230, 2502.13965, 2605.16637, 2605.07238, 2601.12967, 2607.22578, 2606.30560, 2609.10964, 2604.11378, 2604.15186, 2606.01751, 2606.01065, 2608.15127, 2405.19888, 2407.00326, 2507.07400, 2411.19379, 2506.02634; GitHub pages for vllm#37003, vllm#39806, vllm#42185, vllm#39321, sglang#23315, sglang#22617, sglang#22373, sglang#22950, sglang#29173; `gauravapiscean/agentic-kv-cache`; `llm-d` agentic-serving README (raw main).

**arXiv ID discipline.** Per project rules, every cited ID was read back at `arxiv.org/abs/<id>` and its `<title>` confirmed. IDs that came back **wrong** and were discarded rather than cited: `2504.01005` (returned "When To Solve, When To Verify…", not Ayo), `2505.06297` (returned a text-compression paper, not agentic-kv-cache), `2508.18700`, `2506.14851`, `2601.11610`. Ayo is ASPLOS'25 per its GitHub repo (`NetX-lab/Ayo`); I did not locate or read its paper page.

**Could NOT verify (listed, not claimed absent):**
- **Orbit: Efficient Agentic Inference using Priority Scheduling** — EPFL Infoscience page returned a JavaScript CAPTCHA wall; ACM DL page not retrieved. State, claims and metrics unread.
- **sglang PR #22950 / issue #22373 bodies** — I verified only their *states* (CLOSED each); I did not read their content.
- **The merged fix's own benchmark** — SGLang #23315's verified evidence is a *token-count probe* (108 vs 59 cached tokens). The **11.1%→17.1% hit rate / 9.98s→8.96s TTFT** numbers in vLLM #39806 are attributed to **sglang#22617**, which I verified is **CLOSED and unmerged**, later superseded by #23315. I did not verify those two numbers on any SGLang page.
- **KVFlow (2507.07400), MARCONI (2411.19379), Alibaba KVCache-in-the-Wild (2506.02634)** — titles read back and confirmed; the 2.19× / 34.4× / 41.9% figures are quoted from the vLLM #37003 issue body (a citing source), **not** read from those papers.
- **AIBrix** prefix-cache-aware routing — referenced in vLLM #42185's close message; I did not fetch AIBrix docs or code.
- **llm-d sub-guide benchmarks** — the agentic-serving guide's own numbers were not read; row 16 is UNVERIFIABLE on metrics.
- **"LLM-as-Scheduler" (ACL 2026)** and **"Beyond Prediction: Tail-Aware Scheduling for LLM Inference" (ICML 2026)** — appeared in search results; I did not open either, so I make no claim about their content.
- **Scepsy / Orla / Ayo full texts** — abstracts (or repo blurb) only for Scepsy and Ayo; Orla not retrieved (ACM DL).
- **No absence claim in this document should be read as "it does not exist."** Every gap statement above is scoped to "I did not find it" within the surfaces listed.
