# 预登记 — Frontier（drafter 算力分配前沿，**主线决胜**）

**登记时间**：2026-09-11 ｜ **探针**：`probes/p06-frontier/` ｜ **对应方案**：**#6 主线**

| 项 | 内容 |
|---|---|
| **量** | 端到端 `tok/s`、per-step draft 成本 (ms)、平均接受长度、**相对最佳固定配置的端到端比** |
| **区间** | depth {1..6} × width {窄链式, 默认树, 宽树} × batch {1, 8, 32} × ctx {4k, 32k, 128k, 185k}，每格 ≥3 次重复 |
| **基线** | ① spec off（AR）；② **最佳固定配置**；③ **DSpark 生产调度器**（置信度头 + STS + 离线 SPS 成本表 + ragged-verify） |
| **Go 判据** | 存在**内点最优** **且** (bs, ctx) 平面出现**配置反转** **且** 相对最佳固定配置 **≥8%**，**且**能一句话说清与已 ship 调度器的差异 |
| **杀判据** | 收益只在 185k 角落；或无内点最优；或无配置反转；或 <8% |
| **家族前提** | 必须至少 1 个**多层并行 drafter**（DFlash 5 层 / DSpark 3 层 MoE）。**EAGLE-3 只有 1 层 ⇒ 无深度旋钮，只能作基线** |
| **附带交付物** | **DEX 替换测试**（`probes/p06-frontier/README.md` §6）：若机制可被读成"`2606.29223` 换成一个独立 drafter"，则改措辞或改方向 |
| **截止** | W2 结束（≤2 周） |

**改判据的规则**：同 `notes/prereg/p01-e1-uncommitted-kv.md`。**本探针的判据在 W2 结束前不得放宽**——它是主线生死线。
