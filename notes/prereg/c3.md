# 预登记 — C3（约束解码三交集扫描）

**登记时间**：2026-09-11 ｜ **探针**：`probes/c3_constrained_scan/` ｜ **对应方案**：#3

| 项 | 内容 |
|---|---|
| **量** | 相对**无约束**吞吐比（每格），附 mask 耗时、CPU 占用、TTFT/TPOT P50/P95 |
| **区间** | batch {1, 8, 32, 128, 256} × 语法类 {JSON, 嵌套 JSON+regex, C++ 子集, **Python 变体/Bython 类**} × spec {on, off} |
| **基线** | 同引擎无约束解码；跨 ≥2 个后端交叉验证 |
| **Go 判据** | **class-4 @batch≥32 ≤0.85×** **且** 瓶颈结构性（随 schema 复杂度超线性、与 batch 耦合、无法在请求前摊销） |
| **归档判据** | class-4 @batch≥32 >0.85×；或差异可被预编译/mask 缓存/schema 复用抹平 |
| **先验（必须写入结论）** | 字段级端到端余量 **≤6%**；三个候选 gap 上限 1–2% / 1–6% / ≤2.5%（`docs/evidence/constrained_decoding.md` T3/T5） |
| **截止** | W1（1 周） |
| **定位** | 低概率彩票；**不得**因本探针结果把 #3 提升为主线之外的投入 |

**改判据的规则**：同 `notes/prereg/e1.md`。
