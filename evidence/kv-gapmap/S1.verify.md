# Verification of S1

Verifier: adversarial re-fetch, 2026-09-13. Method: every cited URL re-fetched through the same
local proxy helper the author used (`curl -sL --retry 4 --retry-all-errors -m 40`), HTTP status and
effective URL recorded; GitHub pages re-parsed from their embedded JSON payloads (issue body,
timeline comments, PR metadata); arXiv abstracts re-extracted from the `abs/` pages and full texts
from the `html/<id>v1` renderings; in-tree claims re-checked against `/tmp/kvsrc/vllm-main` and the
live `raw.githubusercontent.com` copies. Quotes were matched case-insensitively after normalising
whitespace, markdown emphasis/backticks, smart quotes and XML math markup; code-block line breaks in
the original are reflowed in my normalisation, which is noted where it matters.

---

## URL check

All 36 URLs cited in S1.md were fetched. **36/36 returned HTTP 200. No 404s, no error pages, no
unrelated redirects.** The single non-identity effective URL is a GitHub-native issue→PR redirect.

| URL | HTTP | Effective URL | Title seen | Verdict |
|---|---|---|---|---|
| https://arxiv.org/abs/2309.06180 | 200 | same | Efficient Memory Management for Large Language Model Serving with PagedAttention | OK |
| https://arxiv.org/abs/2312.07104 | 200 | same | SGLang: Efficient Execution of Structured Language Model Programs | OK |
| https://arxiv.org/abs/2405.04437 | 200 | same | vAttention: Dynamic Memory Management for Serving LLMs without PagedAttention | OK |
| https://arxiv.org/abs/2506.15155 | 200 | same | eLLM: Elastic Memory Management Framework for Efficient LLM Serving | OK |
| https://arxiv.org/abs/2605.22416 | 200 | same | Asymmetric Virtual Memory Paging for Hybrid Mamba-Transformer Inference (submitted 21 May 2026 — matches the row) | OK |
| https://arxiv.org/abs/2606.06256 | 200 | same | RedKnot: Efficient Long-Context LLM Serving with Head-Aware KV Reuse and SegPagedAttention | OK |
| https://arxiv.org/abs/2606.26666 | 200 | same | PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs | OK |
| https://arxiv.org/abs/2608.23834 | 200 | same | Minima-KV: Retention-Preserving KV Cache Compression with Mixed-Format Paged Attention | OK |
| https://raw.githubusercontent.com/.../hybrid_kv_cache_manager.md | 200 | same | Hybrid KV Cache Manager (15575 bytes, 246 lines — exactly as claimed) | OK |
| https://raw.githubusercontent.com/.../vllm/v1/core/kv_cache_utils.py | 200 | same | vLLM source (113728 bytes, 2856 lines — exactly as claimed) | OK |
| https://github.com/vllm-project/vllm/pull/10730 | 200 | same | [V1] Do not allocate beyond the max_model_len (MERGED 2024-11-28) | OK |
| https://github.com/vllm-project/vllm/pull/24486 | 200 | same | [Hybrid]: Decouple Kernel Block Size from KV Page Size (MERGED 2025-10-09) | OK |
| https://github.com/vllm-project/vllm/pull/37688 | 200 | same | [HMA] [KVEvent] Enable GPU-side KV events for HMA (MERGED 2026-04-12) | OK |
| https://github.com/vllm-project/vllm/pull/37898 | 200 | same | [Hybrid] Marconi-style admission policy for hybrid cache (MERGED 2026-06-10) | OK |
| https://github.com/vllm-project/vllm/pull/39658 | 200 | same | Revert "[HMA] [KVEvent] Enable GPU-side KV events for HMA" (#37688) (CLOSED, mergedTime null) | OK |
| https://github.com/vllm-project/vllm/pull/43258 | 200 | same | v1/engine: emit prefix-cache KV-events at hash_block_size granularity for hybrid Mamba+Attention models (OPEN) | OK |
| https://github.com/vllm-project/vllm/pull/51482 | 200 | same | [BugFix][Core] free_blocks: restore prepend (LIFO) reuse order when prefix caching is off (MERGED 2026-08-11) | OK |
| https://github.com/vllm-project/vllm/pull/56156 | 200 | same | [Core] Pack heterogeneous KV-cache pages for hybrid models (OPEN) | OK |
| https://github.com/vllm-project/vllm/pull/56445 | 200 | same | [Bugfix][Core] Use GCD of layer counts to eliminate KV cache dummy padding (#46462) (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/24280 | 200 | same | [Feature]: decouple attention backend block size from KVCacheManager block size (CLOSED/COMPLETED) | OK |
| https://github.com/vllm-project/vllm/issues/35703 | 200 | **https://github.com/vllm-project/vllm/pull/35703** | [Hybrid] Map multiple FullAttn layers to a single page (OPEN) | **MISMATCH** (benign) |
| https://github.com/vllm-project/vllm/issues/37121 | 200 | same | [Performance]: KV cache ~7x memory overestimation for hybrid Mamba/attention models (Qwen3.5) (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/40696 | 200 | same | [Feature]: Prefix caching completely ineffective for Mamba-hybrid models (Qwen3.5) when prompt < block_size (528 tokens) (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/42510 | 200 | same | [Bug]: vllm serve crashes at KV cache initialization when using --block-size 1 or 8 … (CLOSED/NOT_PLANNED, closer github-actions) | OK |
| https://github.com/vllm-project/vllm/issues/45702 | 200 | same | [RFC]: Partial Cache Hits for Hybrid Models (CLOSED/COMPLETED) | OK |
| https://github.com/vllm-project/vllm/issues/46462 | 200 | same | [Bug]: DFlash+Qwen3.6-35B-A3B, invalid gpu kv cache usage, low decode speed (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/53595 | 200 | same | [Bug]: align-mode prefix caching silently retains only semantic checkpoints … (CLOSED/NOT_PLANNED by author) | OK |
| https://github.com/vllm-project/vllm/issues/54199 | 200 | same | [Bug]: RETRACTED — duplicate of #53142 … (CLOSED/NOT_PLANNED by author) | OK |
| https://github.com/vllm-project/vllm/issues/54607 | 200 | same | [Doc]: --prefix-match-unit and the default match granularity are undocumented (CLOSED/NOT_PLANNED by author) | OK |
| https://github.com/vllm-project/vllm/issues/55196 | 200 | same | [RFC]: fp8 KV cache gives little to no memory benefit on Mamba/GDN hybrid models (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/55280 | 200 | same | [Bug][ROCm] GLM-5.3-Flash kpool 32-page split (hybrid block_size=1152) GPU Memory access fault on MTP decode (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/55697 | 200 | same | [RFC]: Application-Directed Prefix Checkpoints for Mamba / Hybrid Prefix Caching (OPEN) | OK |
| https://github.com/vllm-project/vllm/issues/56396 | 200 | same | [Bug]: DeepSeek-V4.1-Flash: SimpleCPUOffloadConnector crashes with `assert block_size % hash_block_size == 0` (block_size=8, hash_block_size=32) (OPEN) | OK |
| https://github.com/sgl-project/sglang/issues/33004 | 200 | same | [Feature] Honor explicit max_running_requests when sizing hybrid Mamba state cache (CLOSED/NOT_PLANNED) | OK |
| https://github.com/sgl-project/sglang/issues/38840 | 200 | same | [Bug] Encoder-decoder KV cache: shared boundary page is double-freed when page_size > 1 (OPEN) | OK |
| https://github.com/ggml-org/llama.cpp/pull/13988 | 200 | same | kv-cache : refactor the update/defrag mechanism (MERGED 2025-06-04) | OK |

No dead URLs. No response returned a GitHub 404 page. No duplicate response sizes across the 36
fetches (largest size 705006 bytes appears once) — there is no sign of a cached/blank payload being
served twice.

**Secondary IDs referenced in the file but not tabulated as URLs** were also checked and all exist:
#53142, #53178, #42571, #46841, #4762, #54949, #54735, #54608, #56155 (OPEN), #56404 (OPEN), #45238
(OPEN). #56155, #56404 and #46841/#4762/#54949/#54735/#54608 are pull requests that the `issues/<n>`
form redirects to.

---

## Verbatim quote check (ABANDONED rows)

Re-fetched each ABANDONED URL and searched the retrieved payload (issue body, timeline comment
bodies, PR patch) for the claimed string. "FOUND EXACTLY" = the string occurs character-for-character
after whitespace/markdown normalisation; elisions that the author did not mark with an ellipsis are
called out.

| Row | Status | Notes |
|---|---|---|
| ABANDONED-1 (#54607) | **FOUND EXACTLY** (3 of 3 quotes), with one unmarked elision | `"Closing this. The premise was wrong and the remaining justification did not survive checking either."` matches the author's closing comment (2026-09-01T00:55:07Z) — **but** the author then continues `" --prefix-match-unit exists, it does what I said could not be done, and it is documented …"`. The S1 quote stops mid-sentence without an ellipsis, which drops the one sentence that contradicts the row's framing. The 240-token / 784-token note is verbatim from the issue body; `"I am withdrawing the diagnosis…"` is verbatim from the 00:40 comment. Dates (created 2026-08-31, closed 2026-09-01) and closer (author, `NOT_PLANNED`) confirmed. |
| ABANDONED-2 (#54199) | **FOUND EXACTLY** (2 of 2) | `"Retracting this report — the central premise is wrong, and #53142's mechanism explains our crash after all."` is the opening line of the 3399-char retraction comment. `"So \`mamba_block_size\` stays 1600 while \`cache_config.block_size\` becomes 8/16"` occurs inside the fenced Python block; the S1 text reflows the code and backticks it — faithful, not a paraphrase. Title, `NOT_PLANNED`, author-closure and the 2026-08-28/29 dates all confirmed. |
| ABANDONED-3 (#53595) | **FOUND EXACTLY** (2 of 2) | `"Closing: this was redundant with the PR that fixes it and with the PR that already describes the same invisibility."` verbatim (2026-08-24T16:35:33Z, author jschmied). The long `prefix_cache_retention_interval` quote is verbatim from the issue body (line breaks reflowed). Closer is the author, `NOT_PLANNED`, created and closed 2026-08-24 — confirmed; no stale-bot or maintainer action on the page. |
| ABANDONED-4 (SGLang #33004) | **FOUND** (3 of 3), two with a case change | `"I am disappointed that this issue was marked as 'not planned'."` verbatim from bash99's comment. The two mechanism quotes are verbatim **except** that S1 capitalises the sentence-initial word: original `"today, \`--mamba-full-memory-ratio\` determines a static startup partition."` → S1 `"Today, …"`; original `"a ratio selected for a long context…"` → S1 `"A ratio selected…"`. Content identical. `NOT_PLANNED` confirmed, closed by `huangzhilin-hzl` on the day of filing (2026-07-31, 03:21→03:22). The row's own hedge ("the maintainer's own stated reason was not present") is accurate for the retrieved payload — the only explanation text on the page is the reporter's reaction. |
| ABANDONED-5 (#42510) | **FOUND EXACTLY** (2 of 2) | Both bot strings match character-for-character. Confirmed by timeline: stale marker from `github-actions` 2026-08-12T02:13:48Z, closure comment + `ClosedEvent` from `github-actions` 2026-09-12T01:39:15Z, `stateReason=NOT_PLANNED`, created 2026-05-13. There is no human closure rationale on the page, so the row's "must NOT be read as a maintainer decision" is correct. |
| ABANDONED-6 (hybrid design doc) | **FOUND EXACTLY** (6 of 6) | The three-sentence round-robin passage, the two Gemma-3/padding sentences, and the "Please report any cases where padding overhead becomes unacceptable" sentence all occur verbatim in the retrieved 246-line doc. |
| ABANDONED-7 (arXiv 2605.22416) | **FOUND EXACTLY** (quotes), **wrong section label** | `"We frame the production impact of AVMP as a hypothesis to be tested, not a measured outcome."` and the Jamba-1.5-Mini VRAM sentence are verbatim, but they live in **§6.3 "Production hypothesis"**, not in a "limitations/future-work section" as S1 states (the paper's limitations are §4.6). `"Implementation is pure Python; Triton integration is future work."` and `"Out-of-Memory events drop 7.6%"` are verbatim from v1 abstract/§6.2. RTX 3060 12GB confirmed (§4.1). Submitted 21 May 2026 — confirmed. |
| ABANDONED-8 (#39658) | **FOUND EXACTLY** (patch facts); **stated-reason claim unverifiable** | `"This reverts commit cc07dad789e9ae3f02ab6a4794800b8c1cc240c6."` appears in the author's `.patch` `Subject:` block and diffstat line `vllm/v1/core/block_pool.py \| 7 +-`, and `vllm/distributed/kv_events.py \| 12 +-` are the exact diffstat entries. The claim that the patch removes `group_idx` from `BlockStored`/`BlockRemoved` is confirmed in the diff hunks. The claim that "no human closure comment was retrievable" cannot be verified either way: GitHub lazy-loads PR conversation bodies and no comment bodies are present in the fetched HTML, so I can neither confirm nor refute the absence. |

**ABANDONED rows whose stated reason is a self-declared absence:** ABANDONED-2 (the quoted "stated
reason" is a retraction comment, not a closure rationale), ABANDONED-8 (no reason reported). Both
are honestly flagged by the author, and neither misrepresents anything I could disprove.

---

## READ BODY downgrades

The READ BODY label is largely justified: every quote-bearing row I re-fetched really does contain
its quoted material. Two rows cannot be confirmed from the artifact they cite, and are downgraded:

| Row | Old label | New label | Reason |
|---|---|---|---|
| CLOSED-5 (PR #10730) | READ BODY | **TITLE + STATE ONLY** | Fetched page contains the title and `"state":"MERGED"` (2024-11-28) exactly as claimed, but no PR conversation or diff text is present in the payload, so no body was read. S1 already discloses this ("GitHub lazy-loads PR conversation bodies, so the diff text was not in the retrieved payload"), so the label overstates the evidence rather than the claim. |
| CLOSED-15 (llama.cpp PR #13988) | READ BODY | **TITLE + STATE ONLY** | Title and `"state":"MERGED"` (2025-06-04) confirmed, but the claim "PR conversation body was not retrievable (lazy-loaded), so the row rests on title + merged state" is accurate about what S1 used — and therefore the READ BODY label is wrong. The PR *does* have a human-readable description in the page (diffstat/Observatory metadata plus the author's `value=` copy block: "Remove `virtual llama_kv_cache::update()`… We also start to avoid the explicit \"defrag\" term…"). S1 could have read it; it did not. The row's substantive claim (title + merged state) is correct. |

Rows I spot-checked and **kept at READ BODY** (body detail confirmed):

| Row | Detail checked | Result |
|---|---|---|
| CLOSED-2 (#2312.07104) | RadixAttention abstract sentence | verbatim in abstract |
| CLOSED-3 (design doc) | both quotes; "three coordinators" | both verbatim at doc lines 47 and 221; the doc enumerates exactly three concrete coordinators (`KVCacheCoordinatorNoPrefixCache`, `UnitaryKVCacheCoordinator`, `HybridKVCacheCoordinator`) plus the abstract base — the count is defensible |
| CLOSED-4 (#24280 / PR #24486) | issue-body quote, closing comment, `kernel_block_size` assertion | issue quote verbatim; closing comment is verbatim modulo a markdown link rendered as plain text ("https://github.com/vllm-project/vllm/pull/24486" → "#24486"); `kernel_block_size` and the assertion text confirmed in tree |
| CLOSED-6 (#51482) | title + merged state | confirmed (MERGED 2026-08-11) |
| CLOSED-7 (#45702) | both RFC quotes, `hash_block_size` in tree | both verbatim; `hash_block_size` present in `kv_cache_coordinator.py` |
| CLOSED-8 (#37688) | title + merged state | confirmed (MERGED 2026-04-12) |
| CLOSED-9 (in-tree flags) | `mamba_block_size`, CLI flag, validator, neighbour docstring | all confirmed at the exact cited locations (`vllm/config/cache.py:177`, `vllm/engine/arg_utils.py:1293`, `vllm/config/vllm.py:2963`); the neighbour docstring is verbatim from the `mamba_page_size_padded` field directly above |
| CLOSED-10 (raw source) | line numbers, docstring, `page_size_padded` count | the docstring and line numbers match the **live remote copy** (1365 / 1533 / 11); the local `/tmp/kvsrc/vllm-main` copy has since drifted (1338 / 1506 / 12), i.e. the quoted numbers are a snapshot, not fabrication |
| CLOSED-11 (#37898) | title + merged | confirmed (MERGED 2026-06-10) — but see the misattribution below |
| CLOSED-12 (#2405.04437) | both abstract quotes | verbatim |
| CLOSED-13 (#2608.23834) | both abstract quotes; target GPU | verbatim, including "single 96-GB NVIDIA RTX PRO 6000 Blackwell GPU" |
| CLOSED-14 (#2606.26666) | both abstract quotes | verbatim |
| OPEN-1 (#55196) | all four quotes | verbatim (three in the RFC body, `get_uniform_page_size` assertion sentence in a comment) |
| OPEN-2 (#56156) | all four quotes | verbatim from the PR description (retrieved only as the page's `<clipboard-copy>`/`og:description` payload — the react JSON body field is empty) |
| OPEN-3 (#40696) | both quotes | verbatim |
| OPEN-4 (#43258) | title + `"state":"OPEN"` | confirmed |
| OPEN-5 (#56396) | both quotes | verbatim |
| OPEN-6 (#46462 / PR #56445) | in-tree FIXME, PR cost sentence, runtime warning | FIXME present verbatim (remote line 1533, local 1506); PR sentence verbatim; the `"Add 6 padding layers, may waste at most 60.00% KV cache memory"` log line is verbatim from #46462 |
| OPEN-7 (#55196 / 2605.22416) | both quotes | verbatim (abstract) |
| OPEN-8 (#55697) | RFC title + state | confirmed; the page carries exactly 5 timeline items, matching the claimed "5 embedded comment blocks" |
| OPEN-10 (SGLang #38840) | title + state | confirmed (OPEN, created 2026-09-10) |
| OPEN-11 (design doc) | all four quotes | verbatim, including the `"…until"` sentence that S1 marks with an ellipsis |
| HW-1 … HW-5 | hardware strings | all verbatim in the cited sources (HW-2's `∼200 Gbps` is written `∼200 Gbps` with a math tilde; **HW-5's `"- 8× MI325X"` is written `"- 8× MI325X (gfx942), ROCm 7.2.3"`** — the quote is a faithful prefix) |

No row's *headline fact* was found to be fabricated. The only READ BODY rows that must be downgraded
are CLOSED-5 and CLOSED-15.

---

## Suspect or unverifiable claims

* **Misattribution (material, OPEN-9 and CLOSED-11).** Both rows quote a comment that is on **issue #45238**, not on the artifact they cite:
  * OPEN-9 attributes `"The block-granularity floor is untouched: …"` to "A later comment on issue #45238" — correct, and the quote is verbatim. But the same comment says `"So the worst case (total silent loss) is fixed on current main by #37898"`, i.e. it is a *correction that partially retracts* the issue's premise. S1 uses it purely as corroboration for an open gap and never records the retraction.
  * CLOSED-11 says `"Its effect is corroborated by a later retraction comment on vLLM #54199, verbatim: …"`. That is **wrong**: `"the remaining value of option (a) is smaller than the issue suggested (~+12% hit rate / ~3% TTFT on this small-model rig...)"` and `"current main (16282a9c4, with #37898): 40/64 (hits at block idx 1 = floor(1500/528)*528)"` come from a comment by **Sahil170595 on issue #45238** (`#issuecomment-4678754070`); issue #54199 (eak7824) contains no such text. The quotes themselves are real — the URL and the framing are wrong.
* **Compressed quote with an unmarked elision (ABANDONED-1).** The closure rationale is quoted as one sentence; the original continues with the author's admission that `--prefix-match-unit` **exists, works, and is documented**. The omitted clause is the exact opposite of the "block-granularity floor" argument the row's Note builds on. Elision is not marked.
* **Case-modified "verbatim" text (ABANDONED-4).** Two quotes silently capitalise sentence-initial words (`today,` → `Today,`; `a ratio selected…` → `A ratio selected…`). Cosmetic, but the row labels them "verbatim".
* **Wrong section attribution (ABANDONED-7).** The "hypothesis to be tested" and Jamba-VRAM quotes are attributed to "the paper's own limitations/future-work section"; they are in §6.3 "Production hypothesis". Minor, but the claim about *where* the paper hedges is checkable and wrong.
* **Unverifiable absence claims.** ABANDONED-8 asserts no human closure comment exists on #39658; the fetched page contains no comment bodies (GitHub lazy-loading), so the absence can be neither confirmed nor refuted from the cited artifact. Same structural limit applies to the REST-API-403 narrative in the Method note — plausible and consistent with what I observed (`api.github.com` returns HTTP 403 "API rate limit exceeded for 50.7.158.236"), but the 403 itself is infrastructure state, not evidence about the content.
* **Secondary sources used to support findings are themselves uncited.** #45238 (the source of the OPEN-9 and CLOSED-11 quotes), #53142 (ABANDONED-2's proposed explanation — exists, OPEN), #53178, #42571 and #56404 are all real, but none is a tabulated row and none is in the URL list. A reader cannot re-derive the two most load-bearing "gap" quotes from the file's own citations.
* **Version drift in in-tree line citations.** CLOSED-10's "line 1365 / line 1533 / 11 × `page_size_padded`" match the live remote copy I fetched today but **not** the `/tmp/kvsrc/vllm-main` copy that S1 says it grepped (which has them at 1338 / 1506 / 12). Likewise OPEN-6 quotes "`kv_cache_utils.py:1506`" (local) while the remote has 1533. The file mixes two snapshots without saying so; the strings are real in both.
* **"Confirmed in the 0.29.0 tree" is unfalsifiable as written.** The local `/tmp/kvsrc/vllm-main` has no `.git`, no `_version.py` and no version string in `setup.py`, so "vLLM 0.29.0" cannot be confirmed from the artifact. Every underlying code claim I checked is true of the local copy, so this is a labelling weakness, not a factual error.
* **"three coordinators" (CLOSED-3).** The design doc's own wording is "The `KVCacheManager` is organized into 3 layers" and then lists three concrete coordinators under the abstract `KVCacheCoordinator`; a strict reading lists four class names. Not a fabrication, but the summary is looser than the doc.
* **ABANDONED-8's "revert PR closed unmerged" is correct, and the target #37688 is indeed still MERGED** — checked both ways. No issue here; noting it because it is the kind of claim that is often wrong.
* **No suspicious duplicate patterns found.** No two of the 36 responses share a byte count; no copy-pasted description blocks across rows; no quote reads like a paraphrase in the rows I could verify. The file's own numeric self-reports (15575 bytes / 246 lines for the doc; 113728 bytes / 2856 lines for `kv_cache_utils.py`) reproduce **exactly** on re-fetch, which is a strong positive signal that the fetches happened.
* **Provider-side note (not the author's fault, but relevant to trust).** Four of the retrieved GitHub artifacts carry explicit AI-authorship disclosures (`#56156`: "This contribution was developed with AI assistance."; `#54199`: "investigated and written with AI assistance (Anthropic Claude) under my direction"; `#56396` and `#45238` likewise). Several of S1's highest-value "gap" rows rest on single-reporter issues of this kind, which is a source-quality caveat the file does not record.

---

## Summary

| Metric | Count |
|---|---|
| URLs checked | 36 |
| OK (200, identity or benign redirect, title matches claim) | 35 |
| MISMATCH | 1 (`issues/35703` → `pull/35703`; the target is the artifact claimed, so benign) |
| DEAD (404 / error page / unrelated redirect) | 0 |
| Verbatim quotes checked (ABANDONED + OPEN + CLOSED spot-checks) | 71 |
| Quotes FOUND exactly | 68 |
| Quotes FOUND with a defect (unmarked elision, case change, or wrong attribution) | 3 |
| Quotes NOT FOUND | 0 |
| Rows re-labelled to TITLE + STATE ONLY | 2 (CLOSED-5, CLOSED-15) |
| Rows whose claimed detail could not be verified | 1 (ABANDONED-8's "no human closure comment" absence claim) + 1 unconfirmable label (CLOSED-15's lazy-load claim) |

**Overall reliability verdict: HIGH, with two specific accuracy defects.**

Every URL resolves, every numeric self-report reproduces, every quoted string I could reach is a real
string in a real artifact, and the in-tree line-number citations I could check are exact. There is no
evidence of fabricated sources, invented quotes, or hallucinated issue numbers — the file's failure
modes are those of a careful but over-compressing summariser. The defects that must be fixed before
this file is used as evidence are: (1) **CLOSED-11 cites the wrong issue** for its corroborating
quotes (#54199 should be #45238), and (2) **ABANDONED-1 truncates a closure rationale mid-sentence**
in a way that removes the author's admission that the mechanism the row relies on already exists —
the same admission that OPEN-9's quoted comment says fixes the "worst case". A secondary defect is
ABANDONED-7's wrong section attribution and ABANDONED-4's unmarked case edits. Read the ABANDONED
section as *directionally* correct but re-verify any quote it uses to argue that a gap is unclosed.
