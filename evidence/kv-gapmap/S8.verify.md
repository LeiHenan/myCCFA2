# Verification of S8

Adversarial re-verification performed by re-fetching every cited URL through the local proxy helper
(`curl -sL --retry 4 --retry-delay 2 --retry-all-errors -m 40` with a desktop UA), extracting HTML→text
(including the React/JSON payload that holds GitHub comment bodies), and matching every quoted passage
against the retrieved text after normalising whitespace, HTML entities and markdown emphasis.

Local-tree cross-checks were run against `/tmp/kvsrc/vllm-main` (present) and `/tmp/kvsrc/sglang-main`
(present, `python/` genuinely absent, 607 files), which the agent cites as its cross-check source.

---

## URL check

All 34 URLs cited in the task list plus 8 further URLs/fragments that appear in the file were fetched.
Every one returned HTTP 200 and resolved to a page whose title matches what S8 claims it is.

| URL | HTTP | Effective URL | Title seen | Verdict |
|---|---|---|---|---|
| https://docs.vllm.ai/en/latest/configuration/optimization/ | 200 | same | Optimization and Tuning - vLLM | OK |
| https://github.com/vllm-project/vllm/blob/main/vllm/config/scheduler.py | 200 | same | vllm/vllm/config/scheduler.py at main · vllm-project/vllm | OK |
| https://docs.vllm.ai/en/latest/features/automatic_prefix_caching/ | 200 | same | Automatic Prefix Caching - vLLM | OK |
| https://github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md | 200 | same | vllm/docs/serving/context_parallel_deployment.md at main | OK |
| https://docs.sglang.ai/advanced_features/server_arguments.html | 200 | https://docs.sglang.io/docs/advanced_features/server_arguments | Server Arguments - SGLang Documentation | OK (domain redirect .ai → .io, same page) |
| https://github.com/vllm-project/vllm/pull/54404 | 200 | same | [ROCm][CI] Add attention-sink support to ROCm AITER sparse MLA (state MERGED) | OK |
| https://github.com/vllm-project/vllm/pull/56252 | 200 | same | [CPU] Adds support for fp32 attention sinks (state OPEN) | OK |
| https://arxiv.org/abs/2404.06654 | 200 | same | [2404.06654] RULER: What's the Real Context Size of Your Long-Context Language Models? | OK |
| https://arxiv.org/abs/2606.04557 | 200 | same | [2606.04557] Cartridges at Scale: Training Modular KV Caches over Large Document Collections | OK |
| https://arxiv.org/abs/2608.26523 | 200 | same | [2608.26523] VPP: Virtual Pipeline Parallelism for Efficient Chunked Prefill in Long-Context LLM Inference | OK |
| https://arxiv.org/abs/2607.21535 | 200 | same | [2607.21535] Windowed-MTP: Removing the Full-Context Draft-KV Tax at Million-Token Context | OK |
| https://arxiv.org/abs/2310.01889 | 200 | same | [2310.01889] Ring Attention with Blockwise Transformers for Near-Infinite Context | OK |
| https://arxiv.org/abs/2309.17453 | 200 | same | [2309.17453] Efficient Streaming Language Models with Attention Sinks | OK |
| https://github.com/vllm-project/vllm/issues/50497 | 200 | same | [RFC]: Per-request scheduling for MLA chunked context (prefill) — state CLOSED, stateReason COMPLETED | OK |
| https://github.com/vllm-project/vllm/issues/53684 | 200 | same | [RFC]: Allow the DSA Indexer to Use Context Parallelism Independently — OPEN | OK |
| https://github.com/vllm-project/vllm/issues/51948 | 200 | same | [RFC]: Bounded-memory video sessions — KV retention for long-running streaming-input requests — OPEN | OK |
| https://github.com/vllm-project/vllm/issues/47351 | 200 | same | [RFC]: Sparse-attention-based self-speculative decoding (StreamingLLM, then Vegas) — OPEN | OK |
| https://github.com/sgl-project/sglang/issues/36208 | 200 | same | [RFC] Per-Token-Range KV-Cache Retention Priority — OPEN | OK |
| https://github.com/vllm-project/vllm/issues/49630 | 200 | same | [Feature]: Integration of HieraSparse N:M Semi-Structured Sparse KV Cache Attention — OPEN | OK |
| https://github.com/vllm-project/vllm/issues/46715 | 200 | same | [Bug]: … GLM-5.1-NVFP4 … garbled characters … — OPEN, stateReason null | OK |
| https://github.com/vllm-project/vllm/issues/42419 | 200 | same | [RFC]: [V1][Attention] Add an experimental training-free sparse prefill attention backend … — CLOSED, NOT_PLANNED | OK |
| https://github.com/vllm-project/vllm/pull/47181 | 200 | same | Integrate Decode Context Parallel with FlashInfer MLA and other features — CLOSED | OK |
| https://github.com/vllm-project/vllm/pull/47541 | 200 | same | SWA Hybrid attention integrated with FlashInfer MLA + DCP — OPEN | OK |
| https://github.com/vllm-project/vllm/pull/47542 | 200 | same | DCP + split-q for speculative decoding support — OPEN | OK |
| https://github.com/vllm-project/vllm/pull/50736 | 200 | same | [Feature] Uneven decode context parallelism for heterogeneous TP ranks — CLOSED | OK |
| https://arxiv.org/abs/2607.23693 | 200 | same | [2607.23693] Compute Globally, Materialize Locally: The Memory Contract of Sparse Event-KV | OK |
| https://arxiv.org/abs/2608.01631 | 200 | same | [2608.01631] Does Accuracy Equal Evidence? Reasoning Faithfulness under KV Cache Compression | OK |
| https://github.com/vllm-project/vllm/issues/46767 | 200 | same | TurboQuant KV cache: _continuation_prefill workspace under-sized (~1.43x) … — CLOSED, NOT_PLANNED | OK |
| https://docs.sglang.ai/advanced_features/dcp.html | 200 | https://docs.sglang.io/docs/advanced_features/dcp | Decode Context Parallelism - SGLang Documentation | OK (domain redirect .ai → .io, same page) |
| https://github.com/vllm-project/vllm/issues/56700 | 200 | same | [SM120] Field report: running DeepSeek-V4.1-Flash end-to-end on 8x RTX PRO 6000 … (1M context verified) — OPEN | OK |
| https://github.com/vllm-project/vllm/issues/55757 | 200 | same | [Bug]: sparse-MLA (DSA) models unservable on SM120 in v0.28.0 … — OPEN | OK |
| https://github.com/vllm-project/vllm/pull/54929 | 200 | same | [Attention] Portable Triton sparse-MLA fallback for SM12x (consumer Blackwell) — OPEN | OK (1st fetch timed out; retry 200) |
| https://github.com/vllm-project/vllm/blob/main/docs/design/attention_backends.md | 200 | same | vllm/docs/design/attention_backends.md at main | OK |
| https://arxiv.org/abs/2507.07120 | 200 | same | [2507.07120] Helix Parallelism: Rethinking Sharding Strategies for Interactive Multi-Million-Token LLM Decoding | OK |
| http://arxiv.org/abs/2310.01889 (as printed inside the CLOSED-11 quote) | 200 | https://arxiv.org/abs/2310.01889 | Ring Attention … | OK (http→https) |
| http://arxiv.org/abs/2507.07120 (as printed inside the CLOSED-4 quote) | 200 | https://arxiv.org/abs/2507.07120 | Helix Parallelism … | OK (http→https) |
| https://arxiv.org/html/2607.23693v1 | 200 | same | full text, Compute Globally, Materialize Locally | OK |
| https://docs.sglang.ai/advanced_features/hicache_design.html (QUERIES RUN) | 200 | https://docs.sglang.io/docs/advanced_features/hicache_design | HiCache System Design and Optimization | OK |
| https://docs.sglang.ai/advanced_features/pipeline_parallelism.html (QUERIES RUN) | 200 | https://docs.sglang.io/docs/advanced_features/pipeline_parallelism | Pipeline Parallelism for Long Context | OK |
| https://github.com/youzhedian (CLOSED-4 attribution) | 200 | same | youzhedian (hc) · GitHub | OK |
| github.com/Alicewithrabbit/BFLA (ABANDONED-1, scheme-less in file) | 200 | https://github.com/Alicewithrabbit/BFLA | BFLA: Block-Filtered Long-Context Attention Mechanism (DuSAv2) | OK |
| https://api.github.com/search/issues?q=repo:vllm-project/vllm+ (QUERIES RUN) | 200 | same | truncated fragment as printed; its distinct queries were re-run and matched (see below) | OK (truncated display only) |

**Secondary artifacts referenced by number but not given as URLs** — all exist and match the stated role:
`#41565` ([Bug] TurboQuant _continuation_prefill … v0.20.0 regression, OPEN), `#43357` ([Bug] TurboQuant
workspace locked at 3.06 MB …, OPEN), sglang `#38413` ([Docs] Drop ghost prefill-CP and custom-sigquit rows
from server arguments, CLOSED), vLLM `#54160` ([Hy4] support Hy4-preview model), `#54405` ([ROCm][CI] Enable
HY-V4 model initialization on ROCm). The extra PR/issue numbers listed in QUERIES RUN as "retrieved in full"
(#54568, #52094, #55789, #42185, #53749, #55394, #53595, #54300, #55733, sglang #36522) all resolve to real
pages with plausible titles. Every arXiv ID in the "actually retrieved" list that was spot-checked exists
(2306.14048 H2O, 2406.10774 Quest, 2608.07458 CoinRAG, 2608.04074, 2602.07223 Vegas, 2407.12128 TTA paper,
2402.09353 DoRA), consistent with the agent's non-match notes.

---

## Verbatim quote check (ABANDONED rows)

Every ABANDONED row was re-fetched and every verbatim passage searched in the retrieved text. All 8 rows
passed. FOUND EXACTLY counts a match after whitespace/entity/markdown-emphasis normalisation (the source
pages carry HTML artefacts such as `4.4 × 4.4\times`, `100 × 100\times`, `16 16 of 20 20`, `GPU ↔ \leftrightarrow`,
and the agent faithfully reproduced several of them — evidence of genuine HTML extraction, not paraphrase).

| Row | Status | Notes |
|---|---|---|
| ABANDONED-1 (BFLA, vLLM #42419) | FOUND EXACTLY | Both stale-bot comments match word-for-word (they live in the page JSON payload, invisible to a naive text dump — the agent clearly extracted them). Label "RFC stale Over 90 days of inactivity" present. All three BFLA passages and the upstreaming ask present. `Closed as not planned` + `stateReason: NOT_PLANNED` present; created May 12, 2026 ✓. No human close event on the page, so "closed ~2026-09" is an inference (correctly hedged) — the stale-bot text itself only promises closure after 30 days of inactivity. |
| ABANDONED-2 (DCPP, arXiv 2608.26523) | FOUND EXACTLY | All 5 quoted passages found in the full text (`/html/2608.26523v1`), including the four numbers used as the negative result: 22 vs 97 invocations, +10.5%/+26.48 s compute, +36.1%/+6.01 s comms, 20.47 s residual bubble, 14.32 s / 4.6% TTFT regression, 4.4% throughput loss, 1.6–3.6% mid-range gains. `[Submitted on 27 Aug 2026]` ✓ |
| ABANDONED-3 (vLLM PR #47181) | FOUND EXACTLY | "Breaking down to smaller PRs" (author), "xuanyu-mistral closed this Jul 3, 2026", the maintainer evals request, the reviewer split request, and all 4 technical passages (split-q kernel / SWA hybrid / TRTLLM GQA / NaN×0 guard) match verbatim. Sub-PRs #47541 and #47542 exist and are OPEN ✓; state CLOSED (unmerged) ✓; created Jun 30, 2026 ✓ |
| ABANDONED-4 (vLLM PR #50736) | FOUND EXACTLY | hmellor's scope rejection matches verbatim including `VLLM_PP_LAYER_PARTITION`; the author's rebuttal (30.5% / 134%) matches verbatim; "moved this to Done" and `github-project-automation` present; closed Aug 3, 2026, close event `2026-08-03T14:03:55Z` ✓ |
| ABANDONED-5 (CAS, arXiv 2606.04557) | FOUND EXACTLY | Both "stated reason" passages, the mid-conversation insertion limitation, the compression-tolerance sentence (100× TechQA / ≤2× FinQA, 3–4× fewer tokens, FinQA edge for text RAG) and the retrieval-quality bound all match in the HTML full text. The "inverted negative" framing is the agent's reading, but the quoted text is genuine. `[Submitted on 3 Jun 2026]` ✓ |
| ABANDONED-6 (arXiv 2607.23693) | FOUND EXACTLY | Premise passage, both eviction corollaries, and the YaRN/LoCoMo statistics (both arm estimates and the paired −.074, p=.02) match in the HTML full text. `[Submitted on 26 Jul 2026]` ✓ (matches the claimed 26 Jul 2026; the file's "arXiv:2607.23693v1 [cs.AI]" stamp is the page's own rendering) |
| ABANDONED-7 (arXiv 2608.01631) | FOUND EXACTLY | All 5 quoted passages match in the HTML full text, including the task-dependence paragraph naming AIME, GPQA-Diamond, MedCalc and "RULER QA". `[Submitted on 3 Aug 2026]` ✓ |
| ABANDONED-8 (vLLM #46767) | FOUND EXACTLY | The duplicate-closure note, the ~1.43× need/cap scaling claim, the `--gpu-memory-utilization` invariance claim and the fp8-vs-TurboQuant NIAH comparison all match verbatim. Created Jun 25, 2026 ✓; `Closed as not planned` ✓; live siblings #41565 and #43357 both exist and are OPEN ✓ |

Quotation hygiene across the rest of the file (CLOSED / OPEN / HW rows) was also spot-checked row by row —
see the summary for the single defect found there.

---

## READ BODY downgrades

| Row | Old label | New label | Reason |
|---|---|---|---|
| — | — | — | No downgrades. Every one of the 35 rows labelled READ BODY carries in-body detail that I was able to confirm against the retrieved page or the local source tree. |

Specific READ BODY confirmations worth recording (all verified, not merely plausible):

- **CLOSED-1**: all three doc quotes verbatim; `vllm/config/scheduler.py:116` really is
  `enable_chunked_prefill: bool = True`; `_set_default_chunked_prefill_and_prefix_caching_args` exists
  (arg_utils.py:2773) and assigns `self.enable_chunked_prefill = default_chunked_prefill`.
- **CLOSED-2**: docstring at scheduler.py:70‑72, `--long-prefill-token-threshold` at arg_utils.py:1596,
  consumer at `v1/core/sched/scheduler.py:663`, secondary read at :476, in-tree tests at
  `tests/v1/core/test_scheduler.py:608` (`long_prefill_token_threshold=400`) and
  `tests/v1/core/prefix_cache/test_partial_prefix_cache_hits.py:271` — all exact. Only drift: the startup
  validation is at scheduler.py:326‑331, not 324‑329 (2-line drift, message text exact).
- **CLOSED-3**: the four required conditions, `--mamba-cache-mode align`, `--prefix-match-unit 64` and
  `--enable-mamba-fine-grained-prefix-cache` all present in the doc; the `vllm/config/cache.py:100‑109`
  docstring matches.
- **CLOSED-4**: all five doc quotes verbatim; DCP flags in arg_utils.py are at 1089‑1114 (agent said
  1090‑1115, same block, 1-line drift); the Moonshot/Helix attribution sentence is verbatim and
  arXiv:2507.07120 really is the Helix Parallelism paper.
- **CLOSED-5**: `--enable-prefill-cp` (default False, "bool flag (set to enable)") and `--cp-strategy`
  (choices `zigzag, interleave`, default None) confirmed in the SGLang reference; the six "ghost" spellings
  return 0 hits in the live page; sglang PR #38413 states "Live prefill-CP CLI is only
  --enable-prefill-cp and --cp-strategy {zigzag,interleave}" and names all six ghosts.
- **CLOSED-6**: `AndreasKaratzas merged 7 commits … Sep 7, 2026` and all three PR-body passages verbatim;
  `docs/design/attention_backends.md:113` carries the Sink row, and lines 131‑135 contain the Blackwell
  FA4 `head_size=256` exclusion sentence verbatim; `supports_sink` is real in-tree
  (`vllm/v1/attention/backend.py:165` and 6 backends); #56252 is OPEN.
- **CLOSED-7…12**: titles, datelines, submission/ revision dates verified on the abs pages; the RULER and
  StreamingLLM abstract openings match; the Windowed-MTP setup details (single B200, TP1, bf16 KV in SGLang,
  W=4032, n_sink=64) and its RULER task list match the HTML full text; the vLLM ring-attention quote is
  verbatim in the CP design doc.
- **OPEN-1…7**: every quoted passage matches; states are exactly as claimed (50497 CLOSED/COMPLETED,
  53684/51948/47351/49630/46715/36208 OPEN; 46715 has no stateReason); creation timestamps match to the
  second except #53684, where the page shows `2026-08-25T06:11:51Z` and the file says `…:50Z` (1-second drift).
- **HW-1…8**: all quoted requirement evidence matches; `cp_common.py` really probes
  `_SymmetricMemory.has_multicast_support(DeviceType.CUDA, device.index)`, gates on
  `handle.multicast_ptr != 0` and logs "Direct CP symmetric memory across %d ranks: %s"; the SGLang DCP
  topology/NVML quotes and both documented multi-GPU examples match; the VPP 16×Ascend 910C setup, the CAS
  H200/B200 cluster and 20-cartridge H100 note, and the SM120 #56700 / #55757 passages all match.

---

## Suspect or unverifiable claims

- **One altered quotation (highest-severity finding, still minor).** OPEN-5 renders the RFC's own Non-Goals
  text inside quotation marks as *"Deferred — not because it is unwanted, but because it can't be added
  cleanly on top of the current tree without its own design pass."* The page says *"Deferred — not because
  **it's** unwanted, but because it can't …"*. Expansion of a contraction inside a quote — a
  near-verbatim, not a fabrication, but the only place where quoted wording differs from source.
- **Cosmetic reformatting inside quotes (not factual errors).** HW-2 prints the two documented commands as
  `--tp-size 8 --dcp-size 8`; the source shows them on separate lines of one `sglang serve` block. HW-7
  drops the LaTeXML artefact in "GPU ↔ \leftrightarrow persistent storage"; CLOSED-7 writes "submitted
  9 Apr 2024" for "[Submitted on 9 Apr 2024 (v1) …]". The agent's reproduction elsewhere of artefacts such
  as `4.4 × 4.4\times`, `100 × 100\times`, `16 16 of 20 20` and `p = .02 p{=}.02` is strong evidence these
  quotes were genuinely scraped rather than composed.
- **Two line-number drifts and one 1-second timestamp drift** (scheduler.py validation 324‑329 vs actual
  326‑331; arg_utils DCP flags 1090‑1115 vs actual 1089‑1114; #53684 `06:11:50Z` vs `06:11:51Z`). Immaterial.
- **ABANDONED-1's "closed ~2026-09" is an inference, not a page fact.** The page exposes no close
  timestamp and no human close event; the agent says so explicitly, and the 90-day-stale + 30-day-close
  arithmetic from the May 12 creation makes the inference reasonable.
- **GitHub-API process claims are mostly reproducible, with one exception.** Re-running the queries gives
  `"chunked prefill" in:title` → total_count **233** ✓, `label:wontfix` → **0** ✓, `is:pr is:closed
  is:unmerged "long context"` → **190** ✓. However the accompanying claim that the `is:unmerged` qualifier
  "did not filter — results included merged PRs" did **not** reproduce: I re-ran it and 0 of the 30 returned
  items had a `merged_at`. The agent discarded that result set anyway, so nothing downstream depends on it.
  The claim that the "not planned + long context" issue search returned HTTP 403 could not be reproduced
  either (it now returns 200, total_count 167) — transient rate limiting is plausible and this is a
  self-reported session detail, not a citable artifact.
- **arXiv API claim confirmed**: `export.arxiv.org/api/query` still answers **HTTP 429 "Rate exceeded."**,
  supporting the file's explanation for why paper-level findings come from `/abs/` and `/html/` pages.
- **Unverifiable by construction (no artifact to fetch):** the Web-search query lists, the "local greps"
  narrative, and the claim that arXiv `/list/…` URLs 404'd. These are process notes; nothing in the
  CLOSED/OPEN/ABANDONED/HW tables depends on them.
- **No copy-paste or duplicate-description patterns found.** The 35 row descriptions are distinct; the
  fetched pages have distinct byte sizes; no two rows quote the same passage for different claims.
  The same paper legitimately appears in two rows (2608.26523 as CLOSED-9 and ABANDONED-2; 2606.04557 as
  CLOSED-8, ABANDONED-5 and HW-7), but each row draws on a different part of the text and the file states
  the positive/negative split explicitly.
- **Coverage honesty checks out.** The EMPTY SUBSECTIONS notes are accurate: no ring-attention abandonment
  row exists (the CP doc says "under active development", which is an open status), the Blackwell
  `head_size=256` sink caveat is real, and the "thin: exact merge PR for
  `--enable-mamba-fine-grained-prefix-cache`" self-caveat is fair.

---

## Summary

- **URLs checked: 42** (34 primary + 8 additional URLs/fragments that appear in the file).
  **OK = 42, MISMATCH = 0, DEAD = 0.** Two SGLang URLs redirect from `docs.sglang.ai` to the canonical
  `docs.sglang.io` (same pages), and two `http://arxiv.org` links upgrade to `https`. One printed GitHub
  API URL is a truncated fragment (it is a query-list entry, and its queries were re-run successfully).
- **Verbatim quotes (ABANDONED rows): 8/8 rows FOUND EXACTLY** — 38 distinct quoted passages, all present
  in the re-fetched pages (GitHub comment bodies required reading the page JSON payload, since a plain
  text dump hides them). **FOUND EXACTLY = 38, FOUND APPROXIMATELY = 0, NOT FOUND = 0, COULD NOT FETCH = 0.**
- **Whole-file quote sweep: ~142 quoted/claim spans checked. 141 verified against source; 1 altered**
  (OPEN-5 "it is" for "it's"); 3 further spans are faithful but cosmetically reformatted (HW-2 command
  layout, HW-7 LaTeX artefact, CLOSED-7 "submitted on").
- **READ BODY downgrades: 0.** All 35 rows' in-body details were confirmable; the label is justified
  throughout. Two line numbers drift by 1–2 lines and one creation timestamp by 1 second.
- **arXiv/PR/issue existence and title checks: all pass.** No cited identifier resolves to a different paper
  or unrelated artifact; every secondary artifact cross-referenced by number (#41565, #43357, #38413,
  #54160, #54405, the QUERIES-RUN page list) exists.
- **Rows I could not verify: none.** Rows carrying residual caveats, none of which undermine the row's
  claim: **OPEN-5** (one contraction altered inside quotation marks), **ABANDONED-1** (closure date is a
  stated inference), **HW-2** and **HW-7** (cosmetic quote reformatting), **CLOSED-2** and **CLOSED-4**
  (1–2-line source-line drift), **OPEN-2** (1-second timestamp drift).
- **Overall reliability verdict: HIGH.** This is not a fabricated file. Every URL is live, every paper
  title/dateline matches, every issue/PR number exists with the claimed state (OPEN/CLOSED/NOT_PLANNED/
  COMPLETED/MERGED all verified from page state fields), and the negative-result quotes that carry the
  deliverable's highest-value claims are verbatim-correct in all eight ABANDONED rows. The defects are
  hygiene-level (one expanded contraction, cosmetic quote reformatting, minor line/timestamp drift) plus
  one non-reproduced, non-load-bearing claim about a GitHub API result set. No repair of `S8.md` is needed
  on evidentiary grounds; at most, OPEN-5's quotation marks should be tightened.
