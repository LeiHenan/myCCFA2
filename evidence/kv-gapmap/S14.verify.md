# Verification of S14

Adversarial verification of `evidence/kv-gapmap/S14.md`. Method: every URL in the file was re-fetched with the
prescribed helper (`curl -sL --retry 4 ... `), the page title was read from the payload, and all prose quotes were
checked against a normalized (tag-stripped, JSON-unescaped, whitespace-collapsed) copy of the retrieved bodies —
i.e. the same text the agent claims to have read. 65 URLs were fetched (all 54 cited URLs, plus every other URL or
cross-referenced artifact number that appears in the file), plus the arXiv HTML of 2606.14779 because CLOSED-17's
quotes are not on the abstract page it cites. Local source-tree claims were checked against `/tmp/kvsrc/vllm-main`.

Nothing in the agent's file was modified.

## URL check

| URL | HTTP | Effective URL | Title seen | Verdict (OK / MISMATCH / DEAD) |
|---|---|---|---|---|
| `https://docs.vllm.ai/en/latest/design/prefix_caching/` | 200 | (no redirect) | Automatic Prefix Caching - vLLM | OK |
| `https://docs.vllm.ai/en/latest/usage/reproducibility/` | 200 | (no redirect) | Reproducibility - vLLM | OK |
| `https://docs.vllm.ai/en/latest/features/batch_invariance/` | 200 | (no redirect) | Batch Invariance - vLLM | OK |
| `https://docs.vllm.ai/en/latest/design/metrics/` | 200 | (no redirect) | Metrics - vLLM | OK |
| `https://docs.vllm.ai/en/latest/features/per_request_metrics/` | 200 | (no redirect) | Per-Request Metrics - vLLM | OK |
| `https://vllm.ai/blog/2026-09-10-tiered-kv-offloading` | 200 | (no redirect) | Tiered KV Cache Offloading in vLLM \| vLLM Blog | OK |
| `https://docs.sglang.io/docs/advanced_features/deterministic_inference` | 200 | (no redirect) | Deterministic Inference - SGLang Documentation | OK |
| `https://docs.sglang.io/docs/advanced_features/hicache_design` | 200 | (no redirect) | HiCache System Design and Optimization - SGLang Documentation | OK |
| `https://docs.sglang.io/docs/advanced_features/observability` | 200 | (no redirect) | Observability - SGLang Documentation | OK |
| `https://github.com/vllm-project/vllm/pull/26245` | 200 | (no redirect) | [Metrics] [KVConnector] Add connector prefix cache hit rate stats by ptovam · Pull Request #262 | OK |
| `https://github.com/vllm-project/vllm/pull/33524` | 200 | (no redirect) | [Fix] prefix cache hit rate == 0 bug with gpt-oss style models by ivanium · Pull Request #33524 | OK |
| `https://github.com/vllm-project/vllm/pull/43447` | 200 | (no redirect) | [Prefix Caching] DeepSeekv4 - Support selective prefix-cache retention for sliding-window KV ca | OK |
| `https://github.com/vllm-project/vllm/pull/46592` | 200 | (no redirect) | Feat/invariant with prefix cache by littlecircle0730 · Pull Request #46592 · vllm-project/vllm  | OK |
| `https://github.com/vllm-project/vllm/pull/42985` | 200 | (no redirect) | [PoC] Soft-pin recently-hit prefix-cache entries in get_new_blocks by manueldomke · Pull Reques | OK |
| `https://github.com/vllm-project/vllm/pull/43191` | 200 | (no redirect) | Extend prefix-cache soft-pin with a popular-insert signal (follow-up to #42985) by manueldomke  | OK |
| `https://github.com/vllm-project/vllm/pull/43302` | 200 | (no redirect) | [KVConnector] Multi-storage replication for shared-prefix KV blocks (fixes #42948 cascade colla | OK |
| `https://github.com/vllm-project/vllm/issues/27433` | 200 | (no redirect) | [Feature]: Batch Invariant Feature and Performance Optimization · Issue #27433 · vllm-project/v | OK |
| `https://github.com/vllm-project/vllm/issues/44701` | 200 | (no redirect) | [Bug]: V1 prefix-cache extra-key domain collision between LoRA name and cache_salt · Issue #447 | OK |
| `https://github.com/vllm-project/vllm/issues/54490` | 200 | (no redirect) | [Bug]: enabling prefix caching changes deterministic repeated output · Issue #54490 · vllm-proj | OK |
| `https://github.com/vllm-project/vllm/issues/54487` | 200 | (no redirect) | [Bug]: prefix-caching hash configuration changes deterministic repeated output · Issue #54487 · | OK |
| `https://github.com/vllm-project/vllm/issues/39146` | 200 | (no redirect) | [Bug]: KV block corruption in base scheduler, Non-deterministic output at temperature=0 without | OK |
| `https://github.com/vllm-project/vllm/issues/39389` | 200 | (no redirect) | [Bug]: V1 engine prefix caching was causing non-deterministic outputs during greedy decoding T= | OK |
| `https://github.com/vllm-project/vllm/issues/40179` | 200 | https://github.com/vllm-project/vllm/pull/40179 | [Core] Add --deterministic-prefix-caching for reproducible prefill on ROCm by gigamonkeyx · Pul | OK |
| `https://github.com/vllm-project/vllm/issues/56370` | 200 | (no redirect) | [Bug]: Batch invariance is broken when sequence parallelism / async TP is enabled (`VLLM_BATCH_ | OK |
| `https://github.com/vllm-project/vllm/issues/42948` | 200 | (no redirect) | [Bug]: Prefix-cache 0% hit on re-sent request — DeepSeek-V4-Flash hybrid groups lose all first- | OK |
| `https://github.com/vllm-project/vllm/issues/42082` | 200 | (no redirect) | [RFC]: Standardize KV-cache Layouts · Issue #42082 · vllm-project/vllm · GitHub | OK |
| `https://github.com/vllm-project/vllm/issues/37076` | 200 | (no redirect) | [Bug]: Potential use-after-free in KV block allocator under eviction pressure · Issue #37076 ·  | OK |
| `https://github.com/vllm-project/vllm/issues/10611` | 200 | (no redirect) | [Feature]: load and save kv cache from disk · Issue #10611 · vllm-project/vllm · GitHub | OK |
| `https://github.com/vllm-project/vllm/issues/26850` | 200 | (no redirect) | [Feature]: Add new stats metrics for available_kv_cache_memory · Issue #26850 · vllm-project/vl | OK |
| `https://github.com/sgl-project/sglang/issues/22819` | 200 | (no redirect) | [Bug] KV cache corruption at radix cache block boundary with `--enable-deterministic-inference` | OK |
| `https://github.com/sgl-project/sglang/issues/10278` | 200 | (no redirect) | [Feature] Support deterministic inference with Batch Invariant Ops · Issue #10278 · sgl-project | OK |
| `https://github.com/sgl-project/sglang/issues/22607` | 200 | (no redirect) | [PP + HiCache] HiCache Consistency Fix Plan · Issue #22607 · sgl-project/sglang · GitHub | OK |
| `https://github.com/sgl-project/sglang/issues/38319` | 200 | (no redirect) | [Bug] Chunked Prefill + Radix Insert race corrupts KV pages (QSA, Qwen3.8-Flash-Next) · Issue # | OK |
| `https://github.com/sgl-project/sglang/pull/22841` | 200 | (no redirect) | fix: wire disable_finished_insert for deterministic inference (#22819) by ZhichaoWang970201 · P | OK |
| `https://github.com/sgl-project/sglang/pull/30850` | 200 | (no redirect) | [HiCache] fix: hybrid-mamba prefix ending at chunk boundary is never backed up by decajoin · Pu | OK |
| `https://github.com/ggml-org/llama.cpp/pull/24004` | 200 | (no redirect) | server : opt-in automatic disk prompt/KV cache (--slot-save-auto) (2/2) by F3zz1k · Pull Reques | OK |
| `https://github.com/sgl-project/sglang/pull/11147` | 200 | (no redirect) | Deterministic Mode: Add 1-stage triton kernel for prefill by hebiao064 · Pull Request #11147 ·  | OK |
| `https://arxiv.org/abs/2609.04748` | 200 | (no redirect) | [2609.04748] Same Request, Different Answer: Quantization Amplifies Cache-Induced Divergence in | OK |
| `https://arxiv.org/html/2609.04748v1` | 200 | (no redirect) | Same Request, Different Answer: Quantization Amplifies Cache-Induced Divergence in LLM Serving | OK |
| `https://arxiv.org/abs/2607.21475` | 200 | (no redirect) | [2607.21475] Error Certificates for KV-Cache Eviction via Randomized Design | OK |
| `https://arxiv.org/abs/2607.11942` | 200 | (no redirect) | [2607.11942] How Query Visibility Changes KV-Cache Compression Rankings: A Matched-Budget Audit | OK |
| `https://arxiv.org/abs/2608.05863` | 200 | (no redirect) | [2608.05863] Runtime Observability for Heterogeneous Attention Memory | OK |
| `https://arxiv.org/html/2608.05863v2` | 200 | (no redirect) | Runtime Observability for Heterogeneous Attention Memory | OK |
| `https://arxiv.org/abs/2608.01631` | 200 | (no redirect) | [2608.01631] Does Accuracy Equal Evidence? Reasoning Faithfulness under KV Cache Compression | OK |
| `https://arxiv.org/abs/2606.14779` | 200 | (no redirect) | [2606.14779] Unified KV Pooling to Accelerate Long-Context LLM Serving | OK |
| `https://arxiv.org/abs/2605.03375` | 200 | (no redirect) | [2605.03375] Tutti: Making SSD-Backed KV Cache Practical for Long-Context LLM Serving | OK |
| `https://arxiv.org/html/2605.03375v1` | 200 | (no redirect) | Tutti: Making SSD-Backed KV Cache Practical for Long-Context LLM Serving | OK |
| `https://arxiv.org/html/2603.17803v1` | 200 | (no redirect) | Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs | OK |
| `https://arxiv.org/html/2606.16135v1` | 200 | (no redirect) | SwiftCache: Efficient LLM Serving for Multi-turn Conversations with Heterogeneous KV Cache Shar | OK |
| `https://arxiv.org/abs/2606.16135` | 200 | (no redirect) | [2606.16135] SwiftCache: Efficient LLM Serving for Multi-turn Conversations with Heterogeneous  | OK |
| `https://arxiv.org/abs/2603.17803` | 200 | (no redirect) | [2603.17803] Swarm: Co-Activation Aware KVCache Offloading Across Multiple SSDs | OK |
| `https://arxiv.org/abs/2605.18071` | 200 | (no redirect) | [2605.18071] KVDrive: A Holistic Multi-Tier KV Cache Management System for Long-Context LLM Inf | OK |
| `https://arxiv.org/abs/2606.00279` | 200 | (no redirect) | [2606.00279] Bit-Exact AI Inference Verification Without Performance Tradeoffs | OK |
| `https://arxiv.org/abs/2606.01065` | 200 | (no redirect) | [2606.01065] Leyline: KV Cache Directives for Agentic Inference | OK |
| `https://arxiv.org/abs/2605.27494` | 200 | (no redirect) | [2605.27494] Grounded Cache Routing for Retrieval-Augmented Generation: When Is It Safe to Reus | OK |
| `https://docs.vllm.ai/en/latest/features/kv_events/` | 404 | (no redirect) | vLLM | DEAD |
| `https://docs.sglang.ai/backend/deterministic_inference.html` | 404 | https://docs.sglang.io/backend/deterministic_inference.html |  | DEAD |
| `https://thinkingmachines.ai/blog/defeating-nondeterminism-in-llm-inference/` | 200 | (no redirect) | Defeating Nondeterminism in LLM Inference - Thinking Machines Lab | OK |
| `https://github.com/vllm-project/vllm/issues/32802` | 200 | (no redirect) | [Bug]: GPT-OSS 0% prefix cache hits with hybrid attention + EAGLE · Issue #32802 · vllm-project | OK |
| `https://github.com/vllm-project/vllm/issues/42374` | 200 | https://github.com/vllm-project/vllm/pull/42374 | [Core][WIP][1/N] Standardize kv layout by LucasWilkinson · Pull Request #42374 · vllm-project/v | OK |
| `https://github.com/vllm-project/vllm/pull/37164` | 200 | (no redirect) | [Bugfix] Fix TOCTOU race in KV block allocator causing prefix-cache block theft by AbhiOnGithub | OK |
| `https://github.com/vllm-project/vllm/issues/56377` | 200 | https://github.com/vllm-project/vllm/pull/56377 | [Bugfix] Disable sequence parallelism / async TP under batch invariance and add a TP regression | OK |
| `https://github.com/vllm-project/vllm/pull/29125` | 200 | (no redirect) | [Feature] Batch invariant: Enable `TRITON_MLA` without prefix-caching by yewentao256 · Pull Req | OK |
| `https://github.com/vllm-project/vllm/pull/47715` | 200 | (no redirect) | [Bugfix] Fix KV cache metrics event handling by zhujx001 · Pull Request #47715 · vllm-project/v | OK |
| `https://arxiv.org/html/2606.14779v1` | 200 | (no redirect) | Unified KV Pooling to Accelerate Long-Context LLM Serving | OK |

Verdict notes:

- Every one of the 54 URLs the row table cites returned **HTTP 200** and the page title matched the artifact the row
  claims (docs page names, PR/issue numbers and titles, arXiv IDs and arXiv titles).
- The only two non-200 results are the two URLs the file itself already flags as 404 in its own `QUERIES RUN` section
  (`docs.vllm.ai/en/latest/features/kv_events/` and `docs.sglang.ai/backend/deterministic_inference.html`). They are
  not cited as evidence for any row; the agent disclosed them honestly. They are counted DEAD here because they are
  dead, not because the file misrepresents them.
- No URL redirects somewhere unrelated. Two GitHub `issues/<n>` URLs for pull requests redirect/resolve to the pull
  request page (`/pull/42374`, `/pull/56377`); `.../issues/40179` likewise serves PR #40179 (see suspect claims).

## Verbatim quote check (ABANDONED rows)

Every ABANDONED row was re-fetched and searched for the exact words the agent put in quotation marks (quotes are of
GitHub rendered text / raw-markdown payloads and of arXiv abstract or HTML text).

| Row | Status | Notes |
|---|---|---|
| ABANDONED-1 (SGLang PR #22841 + issue #22819) | FOUND EXACTLY | All four quotes are verbatim: "Trade-off: disables prefix sharing for in-flight requests in deterministic mode (equivalent to the write-path half of --disable-radix-cache). Acceptable because the flag already prioritizes reproducibility over throughput." and "Reproduction before: 7/11 requests diverge across 10 runs on H100 with Qwen2.5-0.5B-Instruct + --enable-deterministic-inference. Reproduction after: 0/11 divergence across 10 runs." are the body of the commit `fix: also gate cache_unfinished_req in deterministic mode (#22819)`, which is on both the PR page and the issue page; the two #22819 quotes ("unsafe combination ... deterministic FA3 + radix prefix-cache reuse" and "This avoids silent correctness issues ...") are verbatim in that issue. Page facts also check out: "ZhichaoWang970201 closed this / Apr 22, 2026", PR `CLOSED` with no merge, issue #22819 `CLOSED`/`COMPLETED`. The row's careful statement that the close action carries no comment text is correct — the only comment-shaped payloads on #22841 are the PR description, a bot quota notice, and a `/tag-and-rerun-ci` command. |
| ABANDONED-2 (vLLM issue #10611) | FOUND EXACTLY | Stale-bot text and the original ask are verbatim; state `CLOSED` / `stateReason=NOT_PLANNED` with labels `feature request` + `stale`. The row's self-imposed caveat is also correct: the string "automatically closed due to inactivity" does **not** occur in the retrieved payload, and the agent explicitly declined to quote it. The remaining timeline (created 2024-11-25; stale 2025-03-30; unstale 2025-04-10; stale 2025-07-09; unstale 2025-07-23) matches the retrieved event timestamps exactly. |
| ABANDONED-3 (llama.cpp PR #24004) | FOUND EXACTLY | All three `ggml-gh-bot` policy bullets, "am17an ... closed this ... Jun 2, 2026", "F3zz1k wants to merge 3 commits into ggml-org:master", the anti-collision design sentence ("auto-restore: on a cold prompt, restore the longest snapshot that is a byte-verified token prefix of the request (collision-safe: never restore on hash alone) ..."), and the downstream-fork sentence "Ports ggml-org#24004 (closed unmerged upstream) with BeeLlama adaptations" are all verbatim on the page. |
| ABANDONED-4 (vLLM PR #42985 close comment) | FOUND EXACTLY (minor elision) | The quoted close comment is verbatim from the page's raw-markdown payload, including "131% pool overflow", "46% at BS=4, 21% at BS=8" and "#42082 (RFC) + #42374 (WIP)". Differences are formatting only: markdown bold/italic markers stripped, link syntax flattened to link text, and the final two paragraphs ("Thanks to everyone who looked at the earlier iterations. Refs: #42948, #42082, #42374.") dropped without an ellipsis. The "Known limitations" quotes also verify verbatim. |
| ABANDONED-5 (vLLM PR #43191) | FOUND APPROXIMATELY | The close comment ("Superseded by #43302 . The protection-based approach ... hitting 97%+ at the same workload. Closing.") is verbatim. The quoted result table is a **re-rendering, not a verbatim quote**: the page's table has four data columns `vanilla v0.21.0 | PR #42985 (v1) | this PR (v1 + popular)` and renders `A.3 ~44s, 0.0% hit | A.3 ~1.4s, 99.95% hit ✅ | A.3 ~1.4s, 99.95% hit ✅`; the file drops the middle column and the `~`/✅ marks, producing "| A.3 ~44s, 0.0% hit | A.3 1.4s, 99.95% hit |". Numbers are right; the quote is condensed without saying so. |
| ABANDONED-6 (vLLM PR #43302) | FOUND EXACTLY | The close comment is verbatim (including "Pre-reserving 4 GB of pristine blocks ... Hence the 0% hit rate." and "Thanks to @stecasta for the careful re-test that prevented this from being merged as a regression."). The independent-test table quoted as stecasta's is verbatim from issue #42948's raw markdown (`| 4 | 8 | 1.0% | **0.0%** | +10% slower (348 s vs 317 s) |`, `| 8 |16 | 0.3% | **0.0%** | +13% slower (581 s vs 514 s) |`) as is "Both prefix_cache_hit_rate and external_prefix_cache_hit_rate reported 0% throughout." |
| ABANDONED-7 (vLLM issue #37076) | FOUND EXACTLY | Both bot texts are verbatim, `stateReason=NOT_PLANNED`, the `ClosedEvent` actor in the payload is `github-actions` at 2026-07-15T02:18:56Z, and flutist's 2026-03-16 `cache_full_blocks` comment is verbatim. |
| ABANDONED-8 (vLLM issue #26850) | FOUND EXACTLY | Stale-bot text verbatim, `stateReason=NOT_PLANNED`, `ClosedEvent` actor `github-actions`, and the timeline really does contain a commit titled "[Core] Add token-level KV cache metrics to V1 engine". |
| ABANDONED-9 (deferral note on PR #42985) | FOUND EXACTLY | The "Background: #32802 ... does not fully address the issue ..." paragraph is verbatim in the #42985 PR body, i.e. on exactly the page the row names. |
| ABANDONED-10 (arXiv 2607.11942) | FOUND EXACTLY | Both abstract sentences are verbatim on the abstract page (`KeyDiff ... 31 of 36 cells`, `SnapKV ... (-0.066)`, `Delta=+0.198 for SnapKV` ... `Delta=+0.011 for KeyDiff`). |
| ABANDONED-11 (arXiv 2608.01631) | FOUND EXACTLY | Both abstract sentences, including "We call this the answer-evidence gap.", are verbatim on the abstract page. |
| ABANDONED-12 (arXiv 2605.03375 HTML v1) | FOUND EXACTLY | All three quotes are verbatim in the HTML full text: "restoring KV cache from SSDs is no longer beneficial (vLLM v0.12.0 vs. v0.17.0)", the 98.3% crossover sentence, and the SSD-LW "GPU bubble time to around 80%" sentence. |

Result: **12 of 12 ABANDONED rows verified; 11 quotes FOUND EXACTLY, 1 FOUND APPROXIMATELY (ABANDONED-5 table
reformatting), 0 NOT FOUND, 0 COULD NOT FETCH.**

## READ BODY downgrades

Every row in the file is labelled READ BODY. I spot-checked the body of each cited artifact for the specific detail
claimed. **No row's core detail is missing from its cited artifact, so no row is downgraded all the way to TITLE
ONLY.** Two rows need their labels/attribution qualified:

| Row | Old label | New label | Reason |
|---|---|---|---|
| OPEN-7 (vLLM issue #39389) | READ BODY | READ BODY (detail confirmed, surrounding claim contradicted) | The literal claim is true — the "🐛 Describe the bug" section does read "NA". But the row's account ("essentially everything — no repro, no triage, no fix, no closure") is contradicted by the same page: the body's environment section contains a repro sketch (`llm = LLM(model="google/gemma-3-4b-it", enable_prefix_caching=False)` vs `...True`, "Then do llm.generate(), the results can be very much different"), and the timeline carries substantive triage ("This discrepancy is likely not a bug, but a manifestation of numerical drift inherent in floating-point arithmetic ... floating-point addition is non-associative ...") plus a second user's independent reproduction. |
| CLOSED-17 (arXiv 2606.14779) | READ BODY | READ BODY (URL attribution corrected) | The testbed sentence and the SPDK-polling limitation sentence are verbatim, but they exist only in `arxiv.org/html/2606.14779v1`, not on the cited abstract page `arxiv.org/abs/2606.14779` (which carries only the abstract). The agent lists the HTML retrieval in `QUERIES RUN`, so this is a provenance slip in the row's URL column, not fabrication. |

## Suspect or unverifiable claims

- **OPEN-7 is the one materially misleading row.** Its quoted "NA" is real, but the row uses it to argue the issue
  has no reproduction and no triage. The retrieved page shows both. It is also worth noting the triage comment
  attributes the reported behaviour to expected floating-point non-associativity rather than a cache bug, which
  weakens the row's implicit "recurring real defect" reading. This is the only row I would not accept as written.
- **ABANDONED-5's quoted table is presented as verbatim but is a condensed re-rendering** (a data column and the
  `~`/✅ marks removed). Content is not wrong; the "VERBATIM" framing is.
- **Unmarked quote truncations/formatting edits.** ABANDONED-4 silently drops the last two paragraphs of the close
  comment; CLOSED-3 joins a bullet list into one line with ";" and "/"; CLOSED-5 joins three bullets with ";";
  HW-5 compresses the blog's five benchmark bullets into one semicolon-joined sentence. In every case the words are
  the page's words, but they are not presented in the page's form, and only the LaTeX-artifact cleanup is disclosed
  in the file's provenance note.
- **CLOSED-17 attributes HTML-only quotes to an abstract URL** (see downgrade table).
- **OPEN-8 cites the wrong URL path for a PR**: `https://github.com/vllm-project/vllm/issues/40179` for PR #40179.
  GitHub serves the PR page at that path (title and `wants to merge 1 commit into vllm-project:main` are correct), so
  the claim verifies, but the canonical URL is `/pull/40179`. Same pattern, self-inflicted, for #42374/#56377.
- **Claims I could not reproduce from artifacts alone (not contradicted, just unverifiable here):** (a) ABANDONED-2's
  "closed by the stale automation" — the payload shows `CLOSED`/`NOT_PLANNED`, `github-actions` stale/unstale events
  and no maintainer decision, but contains no `ClosedEvent` node, so the closing actor itself is not directly visible;
  (b) CLOSED-2's "page footer dated April 28, 2026"/"September 7, 2026" (the dates are present on the two doc pages,
  but as generated build timestamps, not a semantic claim); (c) the `QUERIES RUN` notes about arXiv API HTTP 429 and
  GitHub search API 403 rate limits — historical conditions that cannot be re-tested after the fact.
- **Claims that did check out and deserve credit:** the local source-tree citations are exact —
  `vllm/config/cache.py:145` ("default, as SHA256 is the most secure choice..."), `tests/v1/engine/test_engine_args.py:34`
  (`prefix_caching_hash_algo == "sha256"`), `vllm/config/vllm.py:1800` ("Disabling cascade attention when
  VLLM_BATCH_INVARIANT is enabled."), and `vllm/v1/metrics/loggers.py:584-601` / `608-629`
  (`vllm:prefix_cache_queries`, `vllm:prefix_cache_hits`, `vllm:external_prefix_cache_queries`,
  `vllm:external_prefix_cache_hits`). OPEN-8's negative claim also checks out: `--deterministic-prefix-caching`
  appears in the local vLLM tree only inside commented-out lines of a test file, i.e. it is not an implemented flag.
- **No copy-paste or byte-count anomalies.** The file quotes no byte counts, so identical-byte-count patterns cannot
  arise; my own fetches produced distinct payload sizes for every distinct URL, and no two rows describe the same
  artifact in suspiciously identical language. Quoted strings are not paraphrases: all 12 ABANDONED quotes matched
  the retrieved pages word-for-word (modulo the formatting notes above).

## Summary

- URLs checked: **65** — **63 OK, 0 MISMATCH, 2 DEAD**. Both DEAD URLs are 404s the file itself already disclosed as
  404 and did not cite as evidence. All 54 URLs the row table relies on are live, on-topic, and titled as claimed.
- Verbatim quotes (ABANDONED rows): **12/12 rows verified — 11 FOUND EXACTLY, 1 FOUND APPROXIMATELY, 0 NOT FOUND,
  0 COULD NOT FETCH.**
- arXiv IDs: all 13 distinct IDs (across 19 abs/HTML URLs) resolve to papers whose arXiv titles match the titles the
  file claims, with matching submission/revision dates (2609.04748, 2607.21475 v3, 2607.11942, 2608.05863 v2,
  2608.01631, 2606.14779, 2605.03375, 2603.17803, 2606.16135, 2605.18071, 2606.00279, 2606.01065, 2605.27494).
- GitHub numbers: PR #26245 (MERGED), #33524 (MERGED), #43447 (MERGED), #46592 (OPEN), #42985/#43191/#43302 (CLOSED
  unmerged), #40179 (OPEN, 1 commit), #37164 (OPEN), #42374 (OPEN, 93 commits), #56377 (OPEN), #29125 (MERGED),
  #47715 (OPEN); issues #27433, #44701, #54490, #54487, #39146, #39389, #56370, #42948, #42082 all OPEN; #37076,
  #10611, #26850 CLOSED/NOT_PLANNED; #32802 CLOSED/COMPLETED; SGLang #22819, #10278, #22607 CLOSED/COMPLETED,
  #38319 OPEN, PRs #22841 CLOSED unmerged, #30850 OPEN, #11147 MERGED; llama.cpp #24004 CLOSED unmerged. Every state,
  date, author and commit-count claim attached to these numbers matched the retrieved payload.
- Rows I could not fully verify: **OPEN-7** (its one-line "NA" detail is real, but its "no repro, no triage" framing
  is contradicted by the page — the row should be rewritten or dropped); **ABANDONED-2** (closing actor not directly
  visible in the payload, though nothing contradicts the stale-bot account). No other row is unverifiable.
- **Overall reliability verdict: HIGH.** This is an unusually honest artifact. Every cited URL resolves to the
  claimed artifact, every quoted string I could test exists verbatim somewhere in the retrieved page or paper, the
  file's own caveats (the two 404s, the arXiv-API 429s, the missing stale-close comment, the LaTeX-artifact cleanup,
  the "this is a note in a later PR, not a closure" framing of ABANDONED-9) all held up under checking, and the local
  source-tree citations are exact. The defects are confined to (1) one mischaracterised row (OPEN-7), (2) one
  condensed table presented as verbatim (ABANDONED-5), (3) one HTML-only quote attributed to an abstract URL
  (CLOSED-17), and (4) unmarked cosmetic elisions/joins in a handful of long quotes. None of these changes a
  conclusion the file draws about the state of the art.
