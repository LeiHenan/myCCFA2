# Verification of S13

Independent adversarial verification of `evidence/kv-gapmap/S13.md`.

- **Method**: every URL cited in S13.md was re-fetched live through the supplied `fetch.sh` proxy helper (HTTP status, effective URL and page `<title>` recorded). Every quoted string was then checked against the retrieved text with whitespace/unicode/code-span-tolerant normalisation, plus full-text HTML (`arxiv.org/html/<id>v1`) for the arXiv items and the embedded page JSON for GitHub items (rendered HTML alone omits issue/PR bodies).
- **Scope**: 49 URLs, 19 ABANDONED rows, 13 CLOSED rows, 9 OPEN rows, 8 HW_RULED_OUT rows.
- **Date of verification**: immediately after the file was written; no content was modified in S13.md.
- **Note on 2607.27187**: arXiv serves no full-text HTML for this paper ("No HTML for '2607.27187v1'", HTTP 404). Its PDF was fetched instead and its text extracted for quote checking. The agent was right to use the abs page, but `arxiv.org/html/2607.27187v1` is **not** readable.

## URL check

All 49 cited URLs returned HTTP 200, none redirected, and every effective URL is identical to the cited URL. No DEAD and no MISMATCH. "Title seen" is the page `<title>` as rendered.

| URL | HTTP | Effective URL | Title seen | Verdict (OK / MISMATCH / DEAD) |
|---|---|---|---|---|
| `https://github.com/sgl-project/sglang/pull/29173` | 200 | same | feat: Session-reference-aware Unified Radix Cache for agentic multi-turn workloads by Zhangmj0621 · Pull Request #29173 · sgl-project/sglang | OK |
| `https://github.com/vllm-project/vllm/blob/main/vllm/config/cache.py` | 200 | same | vllm/vllm/config/cache.py at main · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/blob/main/docs/features/automatic_prefix_caching.md` | 200 | same | vllm/docs/features/automatic_prefix_caching.md at main · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/pull/52041` | 200 | same | [Core] Skip broadcasting mm tensor data to workers for prefix-cache-covered items by sseanliu · Pull Request #52041 · vllm-project/vllm | OK |
| `https://arxiv.org/abs/2607.21604` | 200 | same | [2607.21604] AgentKVShift: Efficient KV Cache Reuse for Agentic Memory Systems | OK |
| `https://arxiv.org/abs/2602.22603` | 200 | same | [2602.22603] SideQuest: Model-Driven KV Cache Management for Long-Horizon Agentic Reasoning | OK |
| `https://arxiv.org/abs/2505.15347` | 200 | same | [2505.15347] FlowKV: Enhancing Multi-Turn Conversational Coherence in LLMs via Isolated Key-Value Cache Management | OK |
| `https://arxiv.org/abs/2509.17396` | 200 | same | [2509.17396] EpiCache: Episodic KV Cache Management for Long-Term Conversation on Resource-Constrained Environments | OK |
| `https://arxiv.org/abs/2609.03494` | 200 | same | [2609.03494] GrowPage: On-Demand KV Budgeting for Efficient LLM Reasoning Serving | OK |
| `https://arxiv.org/abs/2605.18825` | 200 | same | [2605.18825] Not All Tokens Are Worth Caching: Learning Semantic-Aware Eviction for LLM Prefix Caches | OK |
| `https://arxiv.org/abs/2506.02634` | 200 | same | [2506.02634] KVCache Cache in the Wild: Characterizing and Optimizing KVCache Cache at a Large Cloud Provider | OK |
| `https://arxiv.org/abs/2603.08743` | 200 | same | [2603.08743] Zipage: Maintain High Request Concurrency for LLM Reasoning through Compressed PagedAttention | OK |
| `https://arxiv.org/abs/2601.21927` | 200 | same | [2601.21927] SONIC: Segmented Optimized Nexus for Information Compression in Key-Value Caching | OK |
| `https://github.com/vllm-project/vllm/pull/52629` | 200 | same | [Test] Add a prefix-caching x tool-calling gate by guanxingithub · Pull Request #52629 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/pull/54625` | 200 | same | [Core][V1] Add cache-aware admission ordering by fululi12 · Pull Request #54625 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/53749` | 200 | same | [Bug]: enable_prefix_caching=True but zero hits for any shared prefix shorter than one attention block on hybrid checkpoints (2096 / 1920 tokens), with nothing  | OK |
| `https://github.com/vllm-project/vllm/pull/53395` | 200 | same | [Docs] Explain how to measure prefix cache effectiveness by SarnadAbhilash · Pull Request #53395 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/54607` | 200 | same | [Doc]: --prefix-match-unit and the default match granularity are undocumented · Issue #54607 · vllm-project/vllm | OK |
| `https://github.com/sgl-project/sglang/pull/37359` | 200 | same | benchmark: add reproducible prefix-cache hit-rate evaluation by sammysun0711 · Pull Request #37359 · sgl-project/sglang | OK |
| `https://github.com/sgl-project/sglang/pull/38000` | 200 | same | [Mamba] Thin cached states by coverage instead of evicting the LRU tail by alphabetc1 · Pull Request #38000 · sgl-project/sglang | OK |
| `https://github.com/sgl-project/sglang/pull/39263` | 200 | same | Fix hybrid session references after an empty terminal cache insert by AranKomat · Pull Request #39263 · sgl-project/sglang | OK |
| `https://arxiv.org/abs/2607.08032` | 200 | same | [2607.08032] What to Keep, What to Forget: A Rate--Distortion View of Memory Compaction in LLMs and Agents | OK |
| `https://github.com/NVIDIA/TensorRT-LLM/pull/16252` | 200 | same | [#16251][feat] KV cache manager v1: add disk (L3) tier with per-request retention TTL by nafis271 · Pull Request #16252 · NVIDIA/TensorRT-LLM | OK |
| `https://github.com/vllm-project/vllm/issues/53670` | 200 | same | EAGLE/MTP prefix-cache last-block drop causes a 1,648-token recompute per hit on one hybrid Qwen3.8 GDN layout — ~30-40% batch throughput loss on prefix-reusing | OK |
| `https://github.com/vllm-project/vllm/pull/42645` | 200 | same | [RFC] feat(context): ACE — Attention-Weighted Context Eviction for multi-turn tool results by KikoCisBot · Pull Request #42645 · vllm-project/vllm | OK |
| `https://github.com/NVIDIA/TensorRT-LLM/pull/18711` | 200 | same | [TRTLLM-14924][feat] Reuse V2 decode KV cache across turns by erictsai-nv · Pull Request #18711 · NVIDIA/TensorRT-LLM | OK |
| `https://arxiv.org/abs/2510.18672` | 200 | same | [2510.18672] Reasoning Language Model Inference Serving Unveiled: An Empirical Study | OK |
| `https://arxiv.org/abs/2608.23658` | 200 | same | [2608.23658] Elastic KV Cache for LLM Serving:A Working Reclamation Mechanism, and Why Chunked Prefill Already Closes the Gap | OK |
| `https://arxiv.org/abs/2608.00902` | 200 | same | [2608.00902] Practical Online KV Cache Compaction for LLM Agents: An Empirical Study | OK |
| `https://github.com/sgl-project/sglang/pull/21064` | 200 | same | [Anthropic API] Strip billing header to fix prefix caching by joninco · Pull Request #21064 · sgl-project/sglang | OK |
| `https://github.com/vllm-project/vllm/pull/51098` | 200 | same | Revert "[Mamba] enable prefix cache by default" (#50991) by vllm-agent · Pull Request #51098 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/37823` | 200 | same | [RFC] Tail-Optimized LRU (T-LRU): Reducing Tail Latency via Conversation-Aware KV Cache Eviction · Issue #37823 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/29286` | 200 | same | [Performance]: cache system prompt token ids · Issue #29286 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/42185` | 200 | same | [RFC]: Cache-affinity-aware request ordering for the V1 scheduler · Issue #42185 · vllm-project/vllm | OK |
| `https://github.com/vllm-project/vllm/issues/40533` | 200 | same | [RFC]: Hybrid checkpoint ABI for non-KV prefix resume · Issue #40533 · vllm-project/vllm | OK |
| `https://github.com/NVIDIA/TensorRT-LLM/pull/15828` | 200 | same | [TRTLLM-13617][feat] Inclusive host KV-cache tier (shadow reuse) for KVCacheManager V2 by reasonsolo · Pull Request #15828 · NVIDIA/TensorRT-LLM | OK |
| `https://github.com/sgl-project/sglang/pull/38720` | 200 | same | [Router] Honor KV-event storage tiers in the cache-aware tree by Kangyan-Zhou · Pull Request #38720 · sgl-project/sglang | OK |
| `https://arxiv.org/abs/2601.14279` | 200 | same | [2601.14279] On the Limits of Learned Importance Scoring for KV Cache Compression | OK |
| `https://arxiv.org/abs/2608.20397` | 200 | same | [2608.20397] Nexus: Depth-Adaptive KV-Cache Splicing and Retrieval-Decoupled Tool Routing for Agentic LLMs on Unified Memory | OK |
| `https://arxiv.org/abs/2606.04557` | 200 | same | [2606.04557] Cartridges at Scale: Training Modular KV Caches over Large Document Collections | OK |
| `https://arxiv.org/abs/2605.09490` | 200 | same | [2605.09490] Not All Thoughts Need HBM: Semantics-Aware Memory Hierarchy for LLM Reasoning | OK |
| `https://arxiv.org/abs/2609.11744` | 200 | same | [2609.11744] Building py-kvcache: A Performance Characterization of External KV Caching for vLLM with NVMe SSDs | OK |
| `https://github.com/sgl-project/sglang/pull/20088` | 200 | same | [Feature] Semantic eviction for radix cache by rexxy-sasori · Pull Request #20088 · sgl-project/sglang | OK |
| `https://arxiv.org/abs/2608.14624` | 200 | same | [2608.14624] Learning Agent Execution for KV-Cache Management in Agentic Serving | OK |
| `https://arxiv.org/abs/2604.25899` | 200 | same | [2604.25899] Pythia: Exploiting Workflow Predictability for Efficient Agent-Native LLM Serving | OK |
| `https://arxiv.org/abs/2609.02027` | 200 | same | [2609.02027] Multi-Turn LLM Conversations under the Least-Recently-Used Policy: Mean-Field Asymptotics and Hit Ratio Approximation | OK |
| `https://arxiv.org/abs/2608.25683` | 200 | same | [2608.25683] psRL: Efficient Training for Agentic AI via Training-Time Prefix Sharing | OK |
| `https://arxiv.org/abs/2607.27187` | 200 | same | [2607.27187] A Photonic-CXL Memory Appliance for Scalable KV Cache Management in LLM Inference | OK |
| `https://github.com/sgl-project/sglang/pull/33315` | 200 | same | [srt] Batchable context forwards over cached prefix KV by rwang5203 · Pull Request #33315 · sgl-project/sglang | OK |

### Title-vs-claim notes (all OK, but the descriptive labels are not always the artefact's title)

These are not URL failures, but a reader following the "Artifact" column would not find a paper by the name S13 gives it:

| S13 row | S13 gives the artifact as | Actual title on the page | Verdict |
|---|---|---|---|
| CLOSED-5 | "AgentKVShift" | `AgentKVShift: Efficient KV Cache Reuse for Agentic Memory Systems` | OK (short name) |
| CLOSED-6 | "SideQuest" | `SideQuest: Model-Driven KV Cache Management for Long-Horizon Agentic Reasoning` | OK |
| CLOSED-9 | "GrowPage" | `GrowPage: On-Demand KV Budgeting for Efficient LLM Reasoning Serving` | OK |
| CLOSED-10 | "**SAECache**" | `Not All Tokens Are Worth Caching: Learning Semantic-Aware Eviction for LLM Prefix Caches` — "SAECache" is only the in-paper system name, not the title | OK but mislabelled as the artifact name |
| CLOSED-12 | "Zipage" | `Zipage: Maintain High Request Concurrency for LLM Reasoning through Compressed PagedAttention` | OK |
| HW-1 | "**CacheScout**" | `Learning Agent Execution for KV-Cache Management in Agentic Serving` — "CacheScout" is the in-paper system name | OK but mislabelled |
| HW-5 | "Multi-turn LLM conversations under LRU …" | `Multi-Turn LLM Conversations under the Least-Recently-Used Policy: Mean-Field Asymptotics and Hit Ratio Approximation` | OK (paraphrase, not the title) |
| HW-6 | "psRL" | `psRL: Efficient Training for Agentic AI via Training-Time Prefix Sharing` | OK |
| OPEN-6 | "#38000 '[Mamba] Thin cached states by coverage instead of evicting the LRU tail'" | title matches exactly | OK |
| ABANDONED-3 | "Reasoning Language Model Inference Serving Unveiled: An Empirical Study" | title matches exactly (lead author is Qi Li; page confirms HKUST-GZ affiliation) | OK |
| ABANDONED-14 | "Brady Steele, 'On the Limits of Learned Importance Scoring for KV Cache Compression'" | title matches exactly | OK (author name not independently checked on the page) |
| ABANDONED-16 | "Momchil Hardalov, Gonzalo Iglesias, Adrià de Gispert, 'Cartridges at Scale…'" | title matches exactly | OK |
| ABANDONED-17 | "Aojie Yuan, Tianqi Shen, Dajun Zhang, 'Not All Thoughts Need HBM…'" | title matches exactly | OK |
| ABANDONED-18 | "py-kvcache authors (AtLarge group, VU Amsterdam)" | title matches exactly | OK |
| OPEN-8 | "NVIDIA / TensorRT-LLM — open PR #16252 '[#16251][feat] KV cache manager v1: add disk (L3) tier with per-request retention TTL'" | title matches exactly (author `nafis271`) | OK |
| ABANDONED-12 | "'[feat] Inclusive host KV-cache tier (shadow reuse) for KVCacheManager V2'" | actual title carries a prefix: `[TRTLLM-13617][feat] Inclusive host KV-cache tier (shadow reuse) for KVCacheManager V2` | OK (prefix dropped) |
| ABANDONED-6 | "'PR closed UNMERGED (joninco closed this May 10, 2026)'" — the row's title text is a description, not a title | actual title: `[Anthropic API] Strip billing header to fix prefix caching` | OK |

## Verbatim quote check (ABANDONED rows)

Every ABANDONED row carries a claimed verbatim closure/result quote. Each was searched in the cited page after normalisation (case, whitespace, unicode quotes/dashes, markdown code-span backticks, PDF hyphen spacing). 97 individual quoted strings were extracted from the ABANDONED section; the substantive ones are reported per row below.

| Row | Status | Notes |
|---|---|---|
| ABANDONED-1 (vLLM #42645, ACE) | **FOUND EXACTLY** | Closure quote `"Withdrawing this: I measured it against doing nothing, and it lost Closing this rather than leaving it open, because I no longer believe the design in it."` is verbatim (the source itself runs the two sentences together). Benchmark numbers quote, `#36311 … ref_cnt == 0` lesson, and "scoreboard, not a benchmark" quote all verbatim. `closedTime=2026-08-10T05:36:27Z`, `state=CLOSED` both match. |
| ABANDONED-2 (TRT-LLM #18711) | **FOUND EXACTLY** | Closure quote `"Closing this for now. I will ask @erictsai-nv to continue working on this when he is back."` verbatim. All four body bullets and the beam-search review comment verbatim (only `_reuse_token_source()` / `request.max_beam_num_tokens` lose underscores to code-span rendering). `closedTime=2026-09-08T00:51:04Z` matches. |
| ABANDONED-3 (arXiv 2510.18672) | **FOUND EXACTLY** | `"However, for 7B models, prefix caching negatively impacts efficiency, leading to increased latency."` verbatim in the full text. Section "5.3 Is Prefix Caching Useful for Contributing Efficient RLLM Serving" verbatim. Hardware list verbatim (Appendix G.2). `"vLLM version 0.8.1 and SGLang version 0.4.6.post1"` is genuine — it appears as `"We use vLLM^16 version 0.8.1 and SGLang^34 version 0.4.6.post1"` (Appendix G.3); the footnote markers are what break a naive substring search. `"Table 14: Results of RLLM-7B without Prefix Cache"` verbatim. Submitted 2025-10-21 confirmed. |
| ABANDONED-4 (arXiv 2608.23658) | **FOUND EXACTLY** | Allocation/negative-result quote, TP-dilution quote, and `"works with CUDA graphs and prefix caching"` all verbatim. Testbed quote matches modulo code-span spaces (`gpu_memory_utilization=0.9`, `max_model_len=32768`). |
| ABANDONED-5 (arXiv 2608.00902) | **FOUND EXACTLY** | All three quotes verbatim (abstract). Submitted 2026-08-02 confirmed. |
| ABANDONED-6 (SGLang #21064) | **FOUND EXACTLY** | The `x-anthropic-billing-header` explanation, the `~4,800 → ~20,800+` measurement, and the regression-test sentence are all verbatim on the PR page. Note: the S13 row heading ("strip the Anthropic billing header so Claude Code turns can share a prefix") is the agent's description; the PR's own title is `[Anthropic API] Strip billing header to fix prefix caching`. |
| ABANDONED-7 (vLLM #51098) | **FOUND EXACTLY** | The revert-rationale quote (both consequences), the HybridKVCacheCoordinator sentence, and the `AssertionError: … block_sizes=[544, 544, 544, 544, 181], hash_block_size=98464 …` text are all verbatim. `closedTime=2026-08-05T05:44:33Z` matches. |
| ABANDONED-8 (vLLM #37823, T-LRU) | **FOUND EXACTLY** | Stale-bot closure text verbatim; T-LRU abstract-style quote and the "conversation-length blind" quote verbatim; the arXiv 2510.15152 link inside the issue is real and resolves to `Tail-Optimized Caching for LLM Inference` (fetched, HTTP 200) — so the agent's "not itself an evidence source" caveat is honest and correct. `createdAt=2026-03-22`, stale `2026-06-25`, closed `2026-07-27` all match. |
| ABANDONED-9 (vLLM #29286) | **FOUND EXACTLY** | Stale-bot closure text, the H20 tokenizer cost quote, and both proposed-implementation sentences verbatim. Source itself uses an ellipsis in the same place ("… separate the system prompt from other prompts, we can use condition…"), so the elision is the author's, not the agent's. `createdAt=2025-11-24`, `stateReason=NOT_PLANNED` match. |
| ABANDONED-10 (vLLM #42185) | **FOUND APPROXIMATELY** | All substantive quotes verbatim (the FCFS/cache-affinity RFC body, the "sglang's RadixAttention … 10–30% throughput uplift" citation, the rejected-radix-tree note, "Software engineer at Meta…"). **One defect**: the "STATED REASON, VERBATIM" is given as `"This issue has been automatically closed due to inactivity…"`, but that exact string does **not** occur on #42185 (0 occurrences). What the page actually contains is the *pre-closure* stale notice: `"This issue has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days…"` — which is exactly the string S13 uses for its "Additional retrieved detail" line in ABANDONED-10. The closure state itself (`state=CLOSED`, `stateReason=NOT_PLANNED`) is confirmed, so the claim "closed by stale bot with no human reason" stands; only the quoted closure wording is misattributed between the two vLLM stale-bot templates. |
| ABANDONED-11 (vLLM #40533) | **FOUND EXACTLY** | Stale-bot closure text verbatim; hybrid non-KV-state paragraph and the `web_search` tool-call corruption quote verbatim. `createdAt=2026-04-21`, stale `2026-07-23`, closed `2026-08-25` all match. |
| ABANDONED-12 (TRT-LLM #15828) | **FOUND EXACTLY** | The maintainer comment `"Please hold on until bug reporter's response. It's a design choice…"` is verbatim, and S13 correctly labels it as the last substantive maintainer comment rather than a closure reason. `closedTime=2026-07-31T02:13:07Z` matches. |
| ABANDONED-13 (SGLang #38720) | **FOUND APPROXIMATELY** | The superseded-and-split quote and the `2.4% host-hit / 1-in-61` quote are verbatim. **One defect**: the quote `"The router's in-process radix tree applies every BlockRemoved as a full removal... So a pod loses ownership of a prefix…"` joins two non-adjacent sentences across an elided 138-character passage (`: for every node carrying any hash in block_hashes, drop worker from that node's worker set. -- src/policies/kv_events/tree.rs, HashTree::remove`). The words on both sides are exact, but the ellipsis silently deletes a code reference and the agent's rendering changes the source's colon into a full stop. It reads as a seamless quotation while actually being a splice. |
| ABANDONED-14 (arXiv 2601.14279) | **FOUND EXACTLY** | The `"Despite architectural sophistication (multi-horizon lookahead, cross-attention), SIP does not outperform simple baselines, including random selection, across 5 seeds, 4 retention levels, and 3 tasks."` and the three-point key-findings list are verbatim (abstract). Submitted 2026-01-13 confirmed. |
| ABANDONED-15 (arXiv 2608.20397) | **FOUND EXACTLY** | Both negative results, the RoPE/off-anchor paragraph, and the Apple M4 Max testbed quote are verbatim. |
| ABANDONED-16 (arXiv 2606.04557) | **FOUND EXACTLY** | The monolithic/non-compositional quote, the budget-manager quote, and the 2–6 points residual quote are verbatim. |
| ABANDONED-17 (arXiv 2605.09490) | **FOUND EXACTLY** | The 0–2.5% collapse quote, the R-KV reproduction quote, and the 3%-eviction/GSM8K/MATH-500 quote are verbatim. |
| ABANDONED-18 (arXiv 2609.11744) | **FOUND EXACTLY** | Abstract break-even quote and the `"Their average request length is below the measured 6,203 token SSD break-even point for Qwen3 4B…"` quote verbatim. The `"External caching is not automatically beneficial…"` quote is verbatim up to a trailing `…` which is the agent's own truncation marker (the source continues `"…and a CPU↔GPU transfer, not to mention scheduling, synchronization, etc."`, and the `↔` glyph is what breaks a naive substring search). |
| ABANDONED-19 (SGLang #20088) | **NO VERBATIM QUOTE EXISTS (as S13 itself discloses)** | S13 honestly declares this row has no retrievable closure statement. Confirmed: page shows `rexxy-sasor wants to merge 17 commits into sgl-project:main`, `state=CLOSED`, `closedTime=2026-08-10T03:55:49Z`, no closure comment. The one thing the row does quote, `"rexxy-sasori wants to merge 17 commits into sgl-project:main"`, has a typo: the login is **rexxy-sasor** (S13 also spells it `rexxy-sasori` in the WHO field). |

### ABANDONED quote tally

- Clean verbatim closures/results: **16 of 19** rows (ABANDONED-1…9, 11, 12, 14…18).
- Verbatim but with an undisclosed splice: **1 row** (ABANDONED-13).
- Quoted string not present on the cited page: **1 row** (ABANDONED-10, stale-bot template swapped).
- Row with no observable quote to check: **1 row** (ABANDONED-19, disclosed by S13 itself).
- In quote-string terms: **62 FOUND EXACTLY**, **7 FOUND EXACTLY (differ only in code-span/underscore/unicode rendering)**, **2 FOUND APPROXIMATELY (splices)**, **1 NOT FOUND on the cited page** (the ABANDONED-10 closure sentence — the page carries the other stale-bot template).

## READ BODY downgrades

Spot-check rule: a row keeps `READ BODY` only if the *specific* detail it claims is really in the retrieved body. Rows whose body detail is confirmed are not listed.

| Row | Old label | New label | Reason |
|---|---|---|---|
| CLOSED-10 (SAECache, arXiv 2605.18825) | READ BODY | **TITLE ONLY (body partially unconfirmed)** | The 756x reuse-variation quote and the multi-queue design quote are verbatim, and the A40 / vLLM v0.8.5 / Qwen2.5-1.5B testbed is confirmed. But the row's *question* is quoted as `"which eviction policy should a prefix cache use for agentic/multi-turn traffic"` and the row is attributed to "SAECache authors". Neither exists as a sentence anywhere in the paper: the paper frames it as `"developing an effective eviction policy for prefix caching has become a first-order design choice"`. The quote is the agent's own paraphrase presented in quotation marks. The paper's own name for the system is `SAECache` as rendered in the full text (`saecache`), but the *title* is different, so the artifact is mislabelled. |
| CLOSED-12 (Zipage, arXiv 2603.08743) | READ BODY | **TITLE ONLY (body partially unconfirmed)** | The long quoted abstract passage *is* verbatim in the paper — but the final clause is not. S13 writes "…while delivering over 2.1x speedup."; the paper's text is `"delivering over 2.1× speedup"`, i.e. a multiplication sign, and the sentence is truncated mid-flow with no ellipsis. More importantly the row's question is quoted as `"can token eviction for reasoning coexist with paged/prefix-cached serving"`, which is again the agent's paraphrase in quotation marks, not a sentence in the paper. |
| HW-5 (arXiv 2609.02027) | READ BODY | **TITLE ONLY (the quoted sentence does not exist)** | The "Verbatim evidence of the requirement" is `"We reserve 40 GB of the prefiller HBM for the KV cache, such that the block capacity is N = 2,275"`. That exact sentence is nowhere on the page. It is a **splice** of two different sentences from two different sections: Appendix E `"We reserve 40 GB of the prefiller HBM exclusively for KV cache."` and §4 `"We assign 40 GB of prefiller HBM for the KV cache, such that the block capacity is N = 2,275."` The facts and the number are right; the quotation is synthetic. |
| OPEN-3 (vLLM #53749) | READ BODY | **TITLE ONLY (the quoted fragment does not exist)** | The "Verbatim quote of the asking" is the issue *title*, which is genuine, but the row's "Additional retrieved detail" claim uses a second quoted fragment: `shared prefix that is too short to be cached`. That phrase appears nowhere on the page; the issue's own words are `"prefix caching registers whole blocks only, so a shared prefix shorter than one block is structurally uncacheable"`. |
| OPEN-8 (TRT-LLM #16252) | TITLE ONLY | TITLE ONLY | No change needed — S13 already labels this TITLE ONLY and the reason it gives (only title/page metadata retrieved) is consistent with what is on the page. Listed here only to confirm the label is honest. |
| OPEN-9 (vLLM #53670) | TITLE ONLY | TITLE ONLY | No change needed, and the disclosure is accurate: the full title including `1,648-token recompute` is present, and the row refrains from claiming the body. |

Everything else labelled READ BODY survived the spot-check. Confirmed in-body details include: **CLOSED-1** (merge by hzh0425 on 2026-08-02, all four quotes, the `(is_referenced, session_ref_count, base_eviction_priority)` ordering, soft-not-pinned, FULL/SWA/Mamba coverage); **CLOSED-2** (`enable_prefix_caching: bool = True`, the exact docstring, `prefix_caching_hash_algo = "sha256"`, the `0 retains only semantic checkpoints…` doc); **CLOSED-3** (both doc paragraphs verbatim, and the flag really exists on live `main`); **CLOSED-4** (merged by njhill 2026-08-19, author sseanliu, framing quote verbatim); **CLOSED-5** (10–30% refresh, 2–3.5× on a single A100, single-A100-except-H200 caveat); **CLOSED-6** (215 samples, 65%, prefix-caching compatibility, gpt-oss-20b/FRAMES/H100); **CLOSED-7** (v1 2025-05-21, v2 2025-10-08, both quotes); **CLOSED-8** (verbatim abstract, v1 2025-09-22); **CLOSED-9** (single A100 80GB, nano-vLLM, published 2026-09-03); **CLOSED-11** (Aliyun, 97-second P99, the three-part abstract claim, cs.DC, 2025-06-03); **CLOSED-13** (both quotes, 2026-01-29, cs.CL); **OPEN-1**, **OPEN-2**, **OPEN-4** (including the 94.59% / 5.7%–6.2% datapoint and the 784-token block story), **OPEN-5**, **OPEN-6** (both DRAFT, both quotes), **OPEN-7** (the "almost never measured" sentence is genuinely in the paper); **HW-1**, **HW-2**, **HW-3**, **HW-4** (really Table 3, all values match), **HW-6**, **HW-7** (32 TB / 16 hosts / passive fiber shuffle / 6.6x all verbatim), **HW-8**.

## Suspect or unverifiable claims

- **Local source tree is stale relative to the live repo, so all line numbers in S13 are wrong.** CLOSED-3 cites `vllm/config/cache.py:198` and `vllm/engine/arg_utils.py:1302` from `/tmp/kvsrc/vllm-main`. That mirror's `setup.py` declares `0.11.2.dev278+gdbc3d9` (a vLLM ~0.11.2 development snapshot), not the 0.29.0 the researcher runs. On live `main` the same symbols sit at `cache.py:187` and `arg_utils.py:1316` — an 11–14 line drift. The *substance* is fine: `enable_mamba_fine_grained_prefix_cache: bool = False`, `enable_prefix_caching: bool = True`, `prefix_caching_hash_algo: PrefixCachingHashAlgo = "sha256"` and `--enable-mamba-fine-grained-prefix-cache` all exist on live `main`. But "line-precise confirmation" does not hold against today's tree, and `docs/features/automatic_prefix_caching.md` is byte-identical between the mirror and live `main` (that one was fine).
- **A second quoted log line is also misattributed.** ABANDONED-7 quotes `"config.py:599 mamba cache mode is set to align for qwen3 5forconditionalgeneration by default when prefix caching is enabled"`. The message text is real, but at `config.py:625` (not 599), and the architecture is rendered `Qwen3_5ForConditionalGeneration`. The message string itself carries a line-wrap ("…by default " + "when prefix caching is enabled").
- **Two "verbatim" quotes are composed from non-adjacent sentences** (ABANDONED-13 and HW-5). In HW-5 the composite reads as one continuous sentence. In both cases the elided material is silently dropped rather than marked.
- **One "STATED REASON, VERBATIM" is the wrong string** (ABANDONED-10, the pre-closure stale notice instead of the closure notice). Three other stale-bot rows (ABANDONED-8, 9, 11) use the correct closure string, so this is a transcription slip rather than invention.
- **Several "quoted" question labels are the agent's paraphrases, not quotes**: CLOSED-1 (`"KV cache growth across multi-turn agentic sessions"`), CLOSED-3 (`"KV reuse across turns with changing system prompts / shared long prefix diverging mid-block"`), CLOSED-5 (`"tool-calling KV reuse / retrieval-triggered re-encoding"`), CLOSED-6 (`"how to bound KV for long chain-of-thought + retrieval-driven agentic loops"`), CLOSED-7 (`"repeated compression of early turns destroys multi-turn coherence"`), CLOSED-10, CLOSED-12, ABANDONED-6 (`"changing system prompt kills cross-turn KV reuse"`), ABANDONED-7, ABANDONED-9, ABANDONED-11, OPEN-3, OPEN-4, OPEN-9. These are the *column-1 question* texts and read as labels, but they are placed in quotation marks in both the tables and the prose, which invites a reader to treat them as quotes from the artefact.
- **The reporter handle in OPEN-3 is unverifiable/wrong.** S13 writes "Schnitzel (jschmied)". The page shows the reporter as `Schnitzel` (13 occurrences); `jschmied` occurs **0** times on #53749 and **32** times on #53670, a different issue S13 also cites. The parenthetical is a cross-issue contamination.
- **ABANDONED-19 has a login typo**: `rexxy-sasori` should be `rexxy-sasor` (appears twice: the WHO line and the quoted merge-chrome line).
- **OPEN-4's "opened 2026-08-22" is correct** (page metadata `createdAt: 2026-08-22T18:57:53Z`) — recorded here because it looked suspicious and checked out.
- **ABANDONED-16's residual gap is quoted as `"Oracle cartridge accuracy falls within 2-6 points of full in-context learning even at high compression."`** This is verbatim, but note it is framed in S13 as a "residual gap" while the paper states it as the ceiling of the method's own repair — a fair reading, not a misquote.
- **ABANDONED-14's lead author "Brady Steele" was not independently confirmed** from the arXiv page snapshot; the title and abstract quotes were. Treat the author name as unverified, not as false.
- **`https://arxiv.org/list/cs.DC/2609` → HTTP 404 ("Invalid Year: 2609")** — S13's own note about this is accurate; listing browse genuinely does not work for the future-dated months in scope, so all arXiv evidence necessarily came from direct ID retrieval, as S13 states.
- **`arxiv.org/html/2607.27187v1` and `v2` are 404 ("No HTML for '2607.27187v1'").** S13 lists 2607.27187 among the papers whose full HTML was "additionally retrieved and grepped for setup sections". That specific retrieval is not reproducible; the paper's PDF is (12 pages, fetched fine). This affects the HW-7 provenance claim only, not the quotes, which the abstract page carries.
- **The `queries run` log is partly non-reproducible.** `/tmp/kvsrc/` and `/Users/leihenan/Desktop/myProject/kv_discovery/raw/` do exist, and the SGLang checkout really is the partial tree S13 describes (no `python/sglang/srt/mem_cache/`). The arXiv API 429 claim is plausible (all `export.arxiv.org/api/query` calls returned 429 in this session's own testing), and the GitHub search API 403 claim likewise. These are process claims, not evidence claims, and nothing in the CLOSED/OPEN/ABANDONED/HW rows depends on them.
- **No identical byte counts and no copy-pasted descriptions.** All 49 fetched pages have distinct byte sizes; no two "Additional retrieved detail" paragraphs share an opening, and no substantive quote is reused across rows. The three identical stale-bot strings (ABANDONED-8/9/11) are genuinely identical on their three pages — that is GitHub's template, not the agent's copy-paste.

## Summary

**URLs: 49 checked — 49 OK, 0 MISMATCH, 0 DEAD.**
Every cited URL returns HTTP 200, none redirects, and every effective URL equals the cited URL. Zero fabricated or non-existent arXiv IDs, PR numbers or issue numbers were found; every arXiv `<title>` agrees with what S13 says the paper is (modulo the short system names noted above), and every GitHub title matches, including the two TITLE-ONLY rows.

**Verbatim quotes (ABANDONED rows): 62 FOUND EXACTLY + 7 FOUND EXACTLY (code/unicode rendering only) + 2 FOUND APPROXIMATELY + 1 NOT FOUND.** Per row: 16 of 19 rows have a clean verbatim closure/result quote; ABANDONED-13's quote is a genuine splice; ABANDONED-10's closure sentence is the wrong stale-bot template; ABANDONED-19 has no quote to check and S13 says so.

**Cross-checks passed beyond the quotes**: every ABANDONED closure date and state (`closedTime`/`stateReason`) matches the page bit-for-bit; every OPEN row is genuinely still open and every DRAFT row is genuinely a draft; every arXiv submission date matches the submission history (2025-10-21, 2026-01-13, 2026-01-29, 2026-02-26, 2026-03-01, 2026-05-10, 2026-05-12, 2026-06-03, 2026-07-01, 2026-08-02, 2026-08-24, 2026-09-03, 2026-09-10 all confirmed; FlowKV v2 = 2025-10-08 confirmed); and the code-level claims (`enable_prefix_caching: bool = True`, `prefix_caching_hash_algo = "sha256"`, `enable_mamba_fine_grained_prefix_cache = False`, the `--enable-mamba-fine-grained-prefix-cache` flag, the APC doc paragraphs) all exist in the live vLLM `main` tree.

**Overall reliability verdict: HIGH, with a specific and narrow defect class.** This file is not padded, not hallucinated and not URL-fabricated: its central artefacts are real, its headline quotes are real, its negative results are real, and its self-reported weaknesses (ABANDONED-19, TRT-LLM #15828, the arXiv API being throttled, the partial SGLang checkout) are all accurate. The defects are of one kind — a handful of strings presented as verbatim quotations are actually paraphrases, splices or the wrong boilerplate (CLOSED-10, CLOSED-12, OPEN-3, ABANDONED-10, ABANDONED-13, HW-5), plus stale line numbers and one wrong reporter handle. Nothing found here changes any conclusion the file draws; the fixes are all local and mechanical. Use the file, but before re-quoting anything, re-verify the six rows listed in the READ BODY downgrades and the ABANDONED-10 closure string.
