# S8: KV for long context: 1M+ token inference, the long-context KV memory wall, chunked prefill, context parallelism for KV, ring attention KV, RAG with many documents, system-prompt cache bloat, long-context KV accuracy, long-context KV benchmarks, attention sinks for long context

*Research window prioritised: 2025-06 → 2026-09. Older work included only where it is the canonical reference for a still-open question. Local source trees inspected: `/tmp/kvsrc/vllm-main`, `/tmp/kvsrc/sglang-main` (the SGLang tree is a partial checkout — `python/` is absent, so only `benchmark/`, `docs/`, `3.14`-era scaffolding is present).*

---

## CLOSED

| Question it answers | Who closed it | Artifact (paper/PR/flag) | URL | Status (merged/shipped/published) | Evidence level |
|---|---|---|---|---|---|
| Can a long prompt be prefilled without one monolithic prefill pass (the long-context KV memory wall at prefill)? | vLLM project | `--enable-chunked-prefill`, default-on in V1 | https://docs.vllm.ai/en/latest/configuration/optimization/ | shipped (default-on) | READ BODY |
| Does chunked prefill have a knobs surface for long-prefill capping? | vLLM project | `--long-prefill-token-threshold` (registered `vllm/engine/arg_utils.py:1596`), `--max-num-batched-tokens` | https://github.com/vllm-project/vllm/blob/main/vllm/config/scheduler.py | shipped (flag in tree) | READ BODY |
| Can RAG / long-system-prompt prefix reuse be made to hit below one attention block (system-prompt cache bloat)? | vLLM project | `--prefix-match-unit` + `--enable-mamba-fine-grained-prefix-cache` | https://docs.vllm.ai/en/latest/features/automatic_prefix_caching/ | shipped (flags in tree) | READ BODY |
| Sharding the KV cache along T during decode (decode context parallelism) — is it a real feature? | vLLM project | `-dcp` / `--decode-context-parallel-size`, `--dcp-comm-backend`, `--dcp-q-replicate`, `--dcp-kv-cache-interleave-size` | https://github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md | shipped | READ BODY |
| Prefill-side context parallelism for long context — do the sharding layouts exist as selectable flags? | SGLang project | `--enable-prefill-cp`, `--cp-strategy {zigzag,interleave}` | https://docs.sglang.ai/advanced_features/server_arguments.html | shipped | READ BODY |
| Attention sinks for long context — is sink support actually wired into shipping inference backends? | vLLM project | `supports_sink` capability + backends matrix ("Sink … for StreamingLLM"); PR #54404 merged for ROCm AITER sparse MLA | https://github.com/vllm-project/vllm/pull/54404 | merged (Sep 7, 2026) / shipped | READ BODY |
| Does the long-context benchmark that governs KV-compression evaluation exist and is it usable? | NVIDIA (Hsieh et al.) | RULER (arXiv:2404.06654) | https://arxiv.org/abs/2404.06654 | published (canonical for still-open question) | READ BODY |
| Can a document collection be pre-encoded into reusable KV so RAG stops re-prefilling millions of tokens? | Eyuboglu et al. / CAS authors | Cartridges at Scale (arXiv:2606.04557) | https://arxiv.org/abs/2606.04557 | published | READ BODY |
| Is unequal per-chunk prefill latency under chunked-prefill pipeline parallelism fixed by anything that works? | Huawei / SJTU | VPP: Virtual Pipeline Parallelism (arXiv:2608.26523) — VPP is the positive half of a paper whose negative half is ABANDONED-2 | https://arxiv.org/abs/2608.26523 | published | READ BODY |
| Does an attention-sink + sliding-window KV budget actually pay off at 1M tokens on ONE GPU? | Windowed-MTP authors | Windowed-MTP (arXiv:2607.21535) — SGLang, 1M context, single B200, TP1 | https://arxiv.org/abs/2607.21535 | published | READ BODY |
| Ring attention as the long-context KV distribution answer — canonical reference | Liu et al. | Ring Attention with Blockwise Transformers (arXiv:2310.01889) | https://arxiv.org/abs/2310.01889 | published (canonical reference) | READ BODY |
| Attention sinks as a phenomenon — canonical reference for the still-open "why sinks" question | Xiao et al. | Efficient Streaming Language Models with Attention Sinks (arXiv:2309.17453) | https://arxiv.org/abs/2309.17453 | published (canonical reference) | READ BODY |

### CLOSED-1: Chunked prefill is the default V1 scheduler behaviour
- URL: https://docs.vllm.ai/en/latest/configuration/optimization/
- Evidence: READ BODY
- Detail: vLLM docs state the feature is shipped and on by default, and that a request exceeding the token budget is automatically split: "In V1, chunked prefill is enabled by default whenever possible . With chunked prefill enabled, the scheduling policy prioritizes decode requests." and "If a pending prefill request cannot fit into max_num_batched_tokens , it automatically chunks it." The same page gives the long-context tuning rule: "For optimal throughput, we recommend setting max_num_batched_tokens > 8192 especially for smaller models on large GPUs." Cross-checked in the local tree: `vllm/config/scheduler.py:116` declares `enable_chunked_prefill: bool = True` and `vllm/engine/arg_utils.py` `_set_default_chunked_prefill_and_prefix_caching_args` sets `self.enable_chunked_prefill = default_chunked_prefill` when the user passes nothing.

### CLOSED-2: `--long-prefill-token-threshold` caps how much of a long prompt one chunk may take
- URL: https://github.com/vllm-project/vllm/blob/main/vllm/config/scheduler.py
- Evidence: READ BODY
- Detail: The flag is shipped and is read from the local source tree. `vllm/config/scheduler.py:70-72` declares `long_prefill_token_threshold: int = Field(default=0, ge=0)` with the docstring "For chunked prefill, a request is considered long if the prompt is longer than this number of tokens. 0 disables the cap (default)." It is registered as `--long-prefill-token-threshold` at `vllm/engine/arg_utils.py:1596` and consumed in the scheduler at `vllm/v1/core/sched/scheduler.py:663` (`if 0 < self.scheduler_config.long_prefill_token_threshold < num_new_tokens:`), with `long_prefill_threshold` also read at `vllm/v1/core/sched/scheduler.py:476`. Startup validation rejects a value above the model window (`vllm/config/scheduler.py:324-329`): "long_prefill_token_threshold ({self.long_prefill_token_threshold}) cannot be greater than the max_model_len ({max_model_len})." The flag is exercised by in-tree tests, e.g. `tests/v1/core/test_scheduler.py:608` (`long_prefill_token_threshold=400`) and `tests/v1/core/prefix_cache/test_partial_prefix_cache_hits.py:271`. Caveat recorded separately as OPEN-6: this flag's interaction with long-context chunked prefill is still producing user-visible corruption reports.

### CLOSED-3: Sub-block prefix-cache matching for long shared system prompts
- URL: https://docs.vllm.ai/en/latest/features/automatic_prefix_caching/
- Evidence: READ BODY
- Detail: This is the direct answer to system-prompt cache bloat on hybrid checkpoints. The doc states the workload and the fix: "This helps when many requests share a long system prompt and then diverge. It is off by default, and takes effect only when all of the following hold" — followed by the four required conditions (`--mamba-cache-mode align`, EAGLE/MTP speculative decoding on the Mamba group, `--prefix-match-unit` smaller than the Mamba block size, no multi-module MTP). The matching-granularity knob is documented in-tree at `vllm/config/cache.py:100-109`: "The finest token boundary (in tokens) a prefix-cache hit can land on. … It can be set finer than the physical KV cache block sizes (e.g. 32 vs a 1024-token hybrid-model block) as long as every KV cache group's `block_size` is divisible by it, enabling cache hits at boundaries inside a physical block."

### CLOSED-4: Decode context parallelism (KV sharded along T) is shipped in vLLM
- URL: https://github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md
- Evidence: READ BODY
- Detail: The in-tree deployment doc defines the feature and its bound: "Since `H` is limited (determined by the model architecture), when we continue to increase the tensor parallel size, the KV cache for each GPU will be duplicated for `tp_size / H` times. … Then we need to add decode context parallel to further shard the KV cache along the `T` dimension. This is as simple as adding `-dcp <size>` to the command line." and "The dcp size should lie in the range of `[1, tp_size/H]`." The doc also states the status of the *prefill* half honestly: "Both approaches are under active development." Flags are registered in `vllm/engine/arg_utils.py:1090-1115` (`-dcp`, `--dcp-comm-backend`, `--dcp-q-replicate`, `--dcp-kv-cache-interleave-size`, `-pcp`). The interleaving sharding scheme is attributed to a named contributor and a paper: "This is proposed by [Chao Hong from Moonshot](https://github.com/youzhedian), and also explained in details in [this paper](http://arxiv.org/abs/2507.07120)." That linked paper resolves to "[2507.07120] Helix Parallelism: Rethinking Sharding Strategies for Interactive Multi-Million-Token LLM Decoding" (verified by fetching the abs page).

### CLOSED-5: Prefill context parallelism is shipped in SGLang with two selectable sharding layouts
- URL: https://docs.sglang.ai/advanced_features/server_arguments.html
- Evidence: READ BODY
- Detail: The server-arguments reference lists the live flags and their semantics verbatim: "`--enable-prefill-cp` — Enable context parallelism for the prefill phase. Select the layout with —cp-strategy ." (default False, "bool flag (set to enable)") and "`--cp-strategy` — Sharding strategy for prefill CP. zigzag assigns each rank one early and one late sequence block; interleave assigns token indices modulo the CP size." (choices `zigzag , interleave`, default None). A separate doc PR (#38413, closed) confirms by enumeration that these are the *only* live prefill-CP CLI spellings and that six other spellings (`--enable-dsa-prefill-context-parallel`, `--enable-nsa-prefill-context-parallel`, `--enable-prefill-context-parallel`, `--dsa-prefill-cp-mode`, `--nsa-prefill-cp-mode`, `--prefill-cp-mode`) never existed as argparse flags.

### CLOSED-6: Attention sinks for long context are wired into shipping attention backends
- URL: https://github.com/vllm-project/vllm/pull/54404
- Evidence: READ BODY
- Detail: The merge event is on the page: "AndreasKaratzas merged 7 commits into main vllm-project/vllm:main from akaratza_rocm_aiter_mla_sinks … Sep 7, 2026". The PR body describes what was actually made to work: "HY-V4 introduced learnable attention sinks in #54160 , but its ROCm initialization was unsupported in AMD build 12635 and build 12653 . This PR supplies reusable sparse-MLA sink support required by the model enablement in #54405 ." and "Add a graph-visible, nonpersistent AITER operation returning LSE and apply virtual zero-value sinks with logaddexp and output rescaling; empty rows contribute exactly zero." and "Preserve ordinary sparse-MLA paths, validate sink metadata, reject unsupported DCP combinations and advertise the supported contiguous cache layouts." The general capability is documented as a first-class backend property in-tree (`docs/design/attention_backends.md:113`): "| **Sink** | Attention sink support (for StreamingLLM) |", with a known hole on Blackwell head_size=256: "does not support logit soft capping, attention sinks, mm_prefix/R-SWA masking, DCP, or windowed encoder attention. Those configurations transparently fall back to FA2." A second sink PR exists for CPU and is still OPEN at retrieval, not merged: https://github.com/vllm-project/vllm/pull/56252 ("[CPU] Adds support for fp32 attention sinks", state OPEN).

### CLOSED-7: RULER — the long-context KV benchmark that supersedes plain NIAH
- URL: https://arxiv.org/abs/2404.06654
- Evidence: READ BODY
- Detail: Retrieved abstract states the motivation and the artifact: "The needle-in-a-haystack (NIAH) test, which examines the ability to retrieve a piece of information (the "needle") from long distractor texts (the "haystack"), has been widely adopted to evaluate long-context language models (LMs). However, this simp…" (title "[2404.06654] RULER: What's the Real Context Size of Your Long-Context Language Models?", submitted 9 Apr 2024, revised 6 Aug 2024). It is included because it is still the benchmark that the 2026 long-context KV papers in this subsection actually evaluate on — e.g. Windowed-MTP reports on "RULER [ Hsieh et al. , 2024 ] (single/multi-value/multi-query needle retrieval, variable tracking, common-/frequent-word aggregation) as a controlled easy → to hard difficulty axis", and arXiv:2608.01631 measures long-context retrieval on "RULER QA".

### CLOSED-8: Cartridges at Scale — pre-encoded KV for large document collections
- URL: https://arxiv.org/abs/2606.04557
- Evidence: READ BODY
- Detail: Retrieved abstract (title "[2606.04557] Cartridges at Scale: Training Modular KV Caches over Large Document Collections", submitted 3 Jun 2026): "Large Language Models can reason over long contexts, yet prefilling millions of tokens is wasteful as much of the content remains static across queries. Cartridges address this by distilling document collections into reusable key-value (KV) caches that eliminate prefilling while preserving accuracy." The paper also closes the specific failure it inherited: "A critical limitation of this approach is that cartridges are monolithic and non-compositional: encoding an entire collection into a single KV block does not scale, and naively mixing cartridges trained in isolation collapses performance to near chance." Its own limitations section keeps the scope honest (see ABANDONED-5 for the inverted-negative reading of the same text).

### CLOSED-9: VPP — a published fix for chunk-count-induced prefill fragmentation
- URL: https://arxiv.org/abs/2608.26523
- Evidence: READ BODY
- Detail: Retrieved abstract (title "[2608.26523] VPP: Virtual Pipeline Parallelism for Efficient Chunked Prefill in Long-Context LLM Inference", submitted 27 Aug 2026): "Existing approaches mitigate this imbalance through dynamic chunk resizing (Dynamic CPP, DCPP), but our measurements show that this trades scheduling overhead for load balancing, which becomes unfavorable on long sequences. In this study, we propose Virtual Pipeline Parallelism (VPP), which keeps chunk sizes fixed and optimizes the pipeline layout through virtual stages." and "VPP improves throughput by up to 13.1% over DCPP on long sequences and 6.7% on mixed workloads, while preserving performance on short sequences. On a 512K-token DeepSeek-V3.1 prefill workload, VPP reduces the pipeline bubble ratio from 6.4% to 0.1%, achieving a 98.0% reduction compared with DCPP." The negative half of the same paper is ABANDONED-2.

### CLOSED-10: Windowed-MTP — attention-sink + sliding-window KV budget validated at 1M tokens on ONE GPU
- URL: https://arxiv.org/abs/2607.21535
- Evidence: READ BODY
- Detail: Retrieved abstract (title "[2607.21535] Windowed-MTP: Removing the Full-Context Draft-KV Tax at Million-Token Context", submitted 23 Jul 2026): "At million-token context this breaks: an MTP draft head typically runs full attention over the entire KV cache at every draft step, so its read grows linearly with context and comes to dominate the draft cost". The paper's own method statement is the closest match in this whole subsection to a single-GPU, 1M-token, sink-based KV answer: "We apply a StreamingLLM-style sliding window plus attention sink to the draft's attention only ( Windowed-MTP ), leaving full-attention verification intact. It is training-free, drop-in, and lossless by construction : the full-attention target still decides every accepted token, so windowing changes only which tokens are proposed , never which are accepted . It bounds the draft's KV working set to a constant, dropping ∼ 99 % {\sim}99\% of KV entries from the draft's read path at 1M." Setup section: "Unless noted, we evaluate on a single NVIDIA B200 GPU (tensor-parallel degree 1) with bf16 KV in SGLang … Windowed-MTP uses W = 4032 W{=}4032 , n sink = 64 n_{\text{sink}}{=}64 ."

### CLOSED-11: Ring attention — canonical KV-distribution reference
- URL: https://arxiv.org/abs/2310.01889
- Evidence: READ BODY
- Detail: Retrieved title and dateline: "[2310.01889] Ring Attention with Blockwise Transformers for Near-Infinite Context", "[Submitted on 3 Oct 2023 ( v1 ), last revised 27 Nov 2023 (this version, v4)]". Abstract opening retrieved: "Transformers have emerged as the architecture of choice for many state-of-the-art AI models, showcasing exceptional performance across a wide range of AI applications. However, the memory demands imposed by Transformers limit their ability to handle long sequences, thereby posing challenge…". It is included as the canonical reference for the still-open ring-attention-KV question because vLLM's own in-tree CP design doc still points at it as the not-yet-implemented option: "If the request token length is too long, we cannot afford holding the full key/value tensors anymore, then we can only compute one chunk of query/key/value tensors for each GPU, and use techniques like [ring-attention](http://arxiv.org/abs/2310.01889) to send/recv key/value tensors chunk by chunk."

### CLOSED-12: Attention sinks — canonical phenomenon reference
- URL: https://arxiv.org/abs/2309.17453
- Evidence: READ BODY
- Detail: Retrieved title and dateline: "[2309.17453] Efficient Streaming Language Models with Attention Sinks", "[Submitted on 29 Sep 2023 ( v1 ), last revised 7 Apr 2024 (this version, v4)]". Abstract opening retrieved: "Deploying Large Language Models (LLMs) in streaming applications such as multi-round dialogue, where long interactions are expected, is urgently needed but poses two major challenges. Firstly, during the decoding stage, caching previous tokens' Key and Value states (KV) consumes extensive memory. Secondly, popular LLMs cannot generalize to longer texts than the training sequence length. Window attention, where only the most recent KVs are cached, is a natural approach -- but we show that it fails when the…". Still the canonical reference for the open question — 2026 papers and RFCs cite it directly, e.g. vLLM RFC #47351 lists it as "[3] Efficient Long-Context Generation with Sliding Window and Attention Sink arXiv:2309.17453".

---

## OPEN

### OPEN-1: How should a single long MLA prefill that carries existing context be scheduled chunk-by-chunk?
- WHO is still asking: LucasWilkinson (vLLM contributor), RFC #50497 "[RFC]: Per-request scheduling for MLA chunked context (prefill)"
- URL: https://github.com/vllm-project/vllm/issues/50497
- Verbatim quote of the asking: "MLA prefill with existing context gathers paged latent KV into a fixed workspace, up-projects it, attends, and merges the partial into the running output. The schedule is **batch-column shaped** — the workspace is divided evenly across all prefills, and every iteration processes the same context window of **every** prefill (`build_mla_chunked_context_metadata()`, `vllm/model_executor/layers/attention/[mla_attention.py](...))"
- What specifically is missing: the RFC was closed as **completed** (`stateReason: COMPLETED`, created 2026-07-31T00:49:21Z) but the unanswered core is the *scheduling policy* — whether the workspace should be divided per-request rather than per-batch-column, and what the fairness/TTFT contract is when one request has a 1M-token context and others have short ones. No landed flag or default encodes a per-request MLA chunked-context schedule.
- Evidence: READ BODY

### OPEN-2: Can the DSA indexer and sparse MLA use *different* context-parallel groups?
- WHO is still asking: vLLM contributor, RFC #53684 "[RFC]: Allow the DSA Indexer to Use Context Parallelism Independently"
- URL: https://github.com/vllm-project/vllm/issues/53684
- Verbatim quote of the asking: "vLLM currently derives both stages' parallel rank, group, and world size from the same Decode Context Parallelism (DCP) configuration. Enabling DCP for the Indexer therefore also enables it for Sparse MLA. The two stages do not benefit equally from this coupling." and "The Indexer obtains substantially higher CP2 speedup than Sparse MLA across most long-context Q>1 shapes, while both stages regress at Q=1 because communication and launch overheads dominate."
- What specifically is missing: entire architectural capability. The RFC says so itself: "This RFC requests architectural feedback on allowing the DSA Indexer and Sparse MLA to use different parallel contexts. It does not propose a complete scheduling policy or optimized cross-rank communication system." State at retrieval: OPEN, created 2026-08-25T06:11:50Z.
- Evidence: READ BODY

### OPEN-3: What owns KV memory for a request whose context never ends (streaming video, 1M-token sessions)?
- WHO is still asking: vLLM contributor, RFC #51948 "[RFC]: Bounded-memory video sessions — KV retention for long-running streaming-input requests"
- URL: https://github.com/vllm-project/vllm/issues/51948
- Verbatim quote of the asking: "Sessions exist; memory management for sessions does not." and, quoting vLLM's own streaming-input blog post back at the project: "additional care must be taken to avoid holding sessions open as they will be blocking the corresponding memory from being used by other requests… Currently, vLLM will not preempt "idle" streaming input sessions - this behaviour will be improved in a future update." and "One long-lived request that never ends. Two resources then grow without bound: GPU memory, since everything the model has seen stays resident for the life of the request (video is token-hungry - an hour at 2 fps is on the order of a million tokens of context); and sequence length, since the stream crosses the model's trained horizon within minutes to hours and output quality degrades past it."
- What specifically is missing: a KV retention/eviction policy for live sessions with unbounded context, and preemption of idle sessions. Streaming input exists (the cost-per-chunk problem is solved); bounded memory for it does not. State at retrieval: OPEN.
- Evidence: READ BODY

### OPEN-4: Can sparse-attention self-speculative decoding (StreamingLLM drafter) be upstreamed into vLLM?
- WHO is still asking: an author of the Vegas paper (vLLM RFC #47351), who has a runnable implementation and asks to upstream it in two PRs
- URL: https://github.com/vllm-project/vllm/issues/47351
- Verbatim quote of the asking: "vLLM's spec-decode framework currently covers ngram, Eagle, Medusa, and MTP. These proposers either require a draft model or rely on n-gram matching. None supports a same-model, sparse-KV draft. This RFC adds one." and "While developing Vegas, I implemented self-speculative decoding with sparse attention in vLLM, with two sparse attention algorithms for drafting: StreamingLLM (sliding window with attention sink) [3] and Vegas. Measured as accumulated decoding throughput on one H100 with Qwen3-8B, replaying AIME 2025 [4] prompts 32 times: vanilla vLLM reaches ~1,000 tokens/s; the StreamingLLM drafter ( num_speculative_tokens=4 ) reaches ~1,600 tokens/s; Vegas ( num_speculative_tokens=6 ) reaches ~1,850 tokens/s."
- What specifically is missing: the upstream `method="sparse_attn"` proposer + `SparseAttnProposer` + `sparse_attn_algorithm` knob. State at retrieval: OPEN, created 2026-07-01T22:41:22Z. The referenced paper (arXiv:2602.07223 Vegas) is itself published, so the research half is closed and the engine-integration half is open.
- Evidence: READ BODY

### OPEN-5: Per-token-range KV retention priority (protecting a shared system prompt's blocks from eviction) is proposed but not landed
- WHO is still asking: SGLang contributor, RFC #36208 "[RFC] Per-Token-Range KV-Cache Retention Priority"
- URL: https://github.com/sgl-project/sglang/issues/36208
- Verbatim quote of the asking: "Today an SGLang request carries a single scalar `priority` applied uniformly to every RadixCache node it touches. This RFC proposes **per-token-range** retention priority: a request can say "give tokens `[0, 4)` priority 100, leave the rest at default," so KV-cache blocks covering a high-value prefix (e.g. a shared system prompt) are evicted later than lower-priority blocks, independently of the tokens that follow." and "Cross-session unfairness. Nothing stops the engine from evicting Session A's important prefix to make room for Session B's less-important request, even when A matters more — because "importance" is not expressible at sub-request granularity."
- What specifically is missing: the feature itself, plus a listed set of unresolved design pieces. The RFC's own Non-Goals section says priority decay "Deferred — not because it is unwanted, but because it can't be added cleanly on top of the current tree without its own design pass", and specifically that decay "interacts with chunked prefill and node splitting. Chunked prefill re-enters `_insert_helper` per chunk, and `_split_node` raises a question with no default answer (does a split-created node inherit the parent's remaining duration, or start a fresh clock?)." State at retrieval: OPEN, created 2026-08-24T17:09:43Z.
- Evidence: READ BODY

### OPEN-6: Is `--enable-chunked-prefill` + `--enable-prefix-caching` + a 65536-token batch budget actually correct at long context?
- WHO is still asking: vLLM user, issue #46715
- URL: https://github.com/vllm-project/vllm/issues/46715
- Verbatim quote of the asking: "[Bug]: When the startup parameter "--max-model-len auto --enable-chunked-prefill -- enable-prefix-caching --max-num-batched-tokens 65536", garbled characters are displayed in the output of the GLM-5.1-NVFP4 model deployed using the VLLM Docker image."
- What specifically is missing: correctness, not capability. The three flags exist and chunked prefill is default-on (CLOSED-1/CLOSED-2), but the reported symptom is output corruption under exactly the long-context configuration this subsection is about. State at retrieval: **OPEN**, created 2026-06-25T09:31:58Z, no `stateReason`. Whether the fault is in long-prefill chunking, prefix-cache block reuse, or the NVFP4 path is not resolved on the retrieved page.
- Evidence: READ BODY

### OPEN-7: Semi-structured N:M sparse KV cache (hardware sparse tensor cores) is not in vLLM
- WHO is still asking: feature requester, vLLM issue #49630 "[Feature]: Integration of HieraSparse N:M Semi-Structured Sparse KV Cache Attention"
- URL: https://github.com/vllm-project/vllm/issues/49630
- Verbatim quote of the asking: "Currently, vLLM lacks native support for semi-structured N:M sparse KV cache acceleration using hardware sparse tensor cores." and, on why the existing long-context KV answers are insufficient: "Coarse Token Skipping (e.g., SnapKV, H2O): Skips entire tokens, which often leads to severe accuracy degradation in complex long-context tasks compared to fine-grained element-level $N:M$ sparsity."
- What specifically is missing: a backend. The proposal is explicit that it wants "native integration or an experimental backend feature based on the HieraSparse architecture" with "Hierarchical Block + Element Masking: Keeps critical blocks (attention sinks, local context windows) dense while applying $N:M$ semi-structured pruning (e.g., 2:4) to non-critical blocks." State at retrieval: OPEN, created 2026-07-23T18:12:44Z.
- Evidence: READ BODY

---

## ABANDONED

### ABANDONED-1: BFLA — a training-free sparse *prefill* attention backend for long-context workloads, offered for upstreaming to vLLM
- WHO: Alicewithrabbit (reference implementation at `github.com/Alicewithrabbit/BFLA`), RFC against vLLM 0.19.1
- URL: https://github.com/vllm-project/vllm/issues/42419
- Closure status: **closed as not planned (`stateReason: NOT_PLANNED`) — but by the automated stale bot, NOT by a maintainer.** The only closure text is bot text. This must not be read as a maintainer rejection.
- STATED REASON, VERBATIM: "This issue has been automatically marked as stale because it has not had any activity within 90 days. It will be automatically closed if no further activity occurs within 30 days. Leave a comment if you feel this issue should remain open. Thank you!" — followed by "This issue has been automatically closed due to inactivity. Please feel free to reopen if you feel it is still relevant. Thank you!" The issue also carries the label text "RFC stale Over 90 days of inactivity".
- Date: created 2026-05-12T12:27:26Z; closed ~2026-09 (stale-bot closure; page shows no human close event).
- Additional verbatim, what was tried: "We have been experimenting with a training-free sparse prefill attention mechanism called **BFLA (Block-Filtered Long-Context Attention)** for vLLM-style paged-attention workloads. BFLA is designed as an opt-in runtime attention backend: it does not modify model weights, does not require training or calibration, and falls back to dense attention for unsupported cases." and "I would like to ask whether the vLLM maintainers would be open to upstreaming this as an experimental V1 Triton attention feature, possibly in several small PRs." The motivation names exactly the S8 memory-wall problem: "Long-context prefill is often dominated by attention computation. As context lengths grow to 64K, 128K, and beyond, dense prefill attention becomes a major deployment bottleneck even when decoding is already optimized."
- Evidence: READ BODY

### ABANDONED-2: Dynamic chunk resizing for chunked-prefill pipeline parallelism (DCPP) — measured to LOSE on long sequences
- WHO: Yan Shi, Xiaochao Wang et al. (Huawei Technologies / Shanghai Jiao Tong University), the VPP paper's own negative result
- URL: https://arxiv.org/abs/2608.26523
- Closure status: paper concludes the technique does NOT pay off at long context (negative result inside a published paper — the paper's own contribution is the opposite design, VPP).
- STATED REASON, VERBATIM: "Existing approaches mitigate this imbalance through dynamic chunk resizing (Dynamic CPP, DCPP), but our measurements show that this trades scheduling overhead for load balancing, which becomes unfavorable on long sequences." and, with the numbers: "At 512K, where the optimal chunk-size budget is 24K, profiler traces show that CPP requires 22 invocations, whereas DCPP requires 97, or 4.4 × 4.4\times as many." and "However, these savings are outweighed by the cost of finer-grained execution. Computation time increases by 10.5% (+26.48 s) as smaller chunks reduce operator efficiency, while the increased number of chunk boundaries raises exposed communication by 36.1% (+6.01 s). Despite dynamic balancing, 20.47 s of bubble remains, and DCPP ultimately incurs a 14.32 s (4.6%) TTFT regression over CPP." and "DCPP outperforms CPP on 64K–256K sequences, delivering consistent 1.6–3.6% improvements in both throughput and TTFT. However, its advantage progressively diminishes with sequence length and reverses at 512K, resulting in a 4.4% throughput loss and a 4.6% TTFT increase." and the generalisation: "These observations yield a critical insight: DCPP trades scheduling overhead for load-balancing gains. As sequence length grows, however, DCPP requires progressively more invocations, and the resulting fragmentation overhead eventually outweighs the benefits of bubble reduction."
- Date: submitted 27 Aug 2026.
- Note: this is the *negative* half of the same paper whose positive half (VPP) is CLOSED-9.
- Evidence: READ BODY

### ABANDONED-3: DCP + FlashInfer MLA integration bundle — closed unmerged after a maintainer asked for evals and splitting
- WHO: xuanyu-mistral (Mistral AI) — vLLM PR #47181, "Integrate Decode Context Parallel with FlashInfer MLA and other features"
- URL: https://github.com/vllm-project/vllm/pull/47181
- Closure status: **PR closed unmerged by its own author** ("xuanyu-mistral closed this Jul 3, 2026"), after a maintainer request for model evals and a reviewer request to split it. The author's stated close reason is the split, not a rejection of the technique — this is a scope/process abandonment, and the sub-PRs were opened instead — https://github.com/vllm-project/vllm/pull/47541 ("SWA Hybrid attention integrated with FlashInfer MLA + DCP") and https://github.com/vllm-project/vllm/pull/47542 ("DCP + split-q for speculative decoding support"), both still OPEN at retrieval.
- STATED REASON, VERBATIM: "Breaking down to smaller PRs" — immediately followed on the page by "xuanyu-mistral closed this Jul 3, 2026". The maintainer ask that preceded it: "@xuanyu-mistral Could you share model evals with this PR against the different features you implemented?" and the reviewer ask: "@xuanyu-mistral Thanks for the great work. It seems this PR supports many features. It might make review easier if the PR can be broken into several smaller PRs?"
- Date: created 2026-06-30; closed 2026-07-03.
- Additional verbatim, what was tried (the DCP+KV surface that did not land in one piece): "DCP + Speculative Decoding (MTP): Adds a fused Triton dcp_split_q kernel that splits multi-token decode queries into per-token requests with pre-computed DCP-local seq_lens . This is necessary because the kernel's internal causal offset arithmetic is incorrect under DCP when tokens_per_req > 1 (local(G-k) != local(G) - k)." and "DCP + Sliding Window Attention (Hybrid Models): Enables DCP for models with mixed full-attention and sliding-window layers. SWA layers replicate KV cache on every DCP rank (effectively dcp=1) while full-attention layers use the full DCP group." and "DCP + TRTLLM GQA Decode: Enables the TRTLLM decode path for FlashInfer GQA attention with DCP (previously disabled entirely)." and "NaN × 0 guard in DCP output correction: when a rank has zero local KV tokens, the attention output is NaN and the LSE factor is 0. NaN * 0 = NaN, so we explicitly zero out."
- Evidence: READ BODY

### ABANDONED-4: Uneven decode context parallelism across heterogeneous GPUs — maintainer says out of scope in-tree
- WHO: efschu (contributor) — vLLM PR #50736, "[Feature] Uneven decode context parallelism for heterogeneous TP ranks"; closed by hmellor (vLLM member)
- URL: https://github.com/vllm-project/vllm/pull/50736
- Closure status: **closed unmerged by a human maintainer** — an explicit scope rejection, not a stale bot. The project board movement right after ("moved this to Done in NVIDIA") is done by the `github-project-automation` Bot, which is a bot action and not the human decision; the human decision is the comment below.
- STATED REASON, VERBATIM: "Uneven TP sharding is not something I think we are interested in supporting in-tree in vLLM as it is quite niche and appears to need changes to every supported model. In situations where you have different sized GPUs you likely don't have fast interconnect to run TP efficiently anyway. You should probably be using PP instead, which can already be unevenly split across GPUs using VLLM_PP_LAYER_PARTITION"
- Date: closed Aug 3, 2026 (close event timestamp 2026-08-03T14:03:55Z).
- Note: the author's rebuttal is also on the page and is a useful record of the measured tradeoff: "For the record, the per-model cost is a one-line unit declaration rather than per-model changes, and I measured PP against TP intra-rig on mismatched cards: PP=3 came out 30.5 % slower per token and 134 % worse on TTFT than TP=3 (same model, quant and rig), and it doesn't address the KV axis this PR targets."
- Evidence: READ BODY

### ABANDONED-5: Monolithic single-cartridge KV for a whole document collection — collapses to near-chance when composed
- WHO: Eyuboglu et al. (2025), the original Cartridges design — falsified by the CAS paper (arXiv:2606.04557), and by CAS's own limitations section
- URL: https://arxiv.org/abs/2606.04557
- Closure status: published paper concludes the earlier technique does NOT pay off at collection scale (and the same paper's own limitations undercut parts of its own replacement).
- STATED REASON, VERBATIM: "A critical limitation of this approach is that cartridges are monolithic and non-compositional: encoding an entire collection into a single KV block does not scale, and naively mixing cartridges trained in isolation collapses performance to near chance." and "However, their approach relies on a single monolithic cartridge per collection, which does not scale to real-world document collections: loading all documents into one cache bloats the prefix with irrelevant tokens, while information-dense content (e.g., financial tables, clinical notes, technical specifications) cannot be highly compressed without loss."
- Inverted-negative from the CAS paper's OWN limitations section (its technique does not cover mid-conversation cache insertion at all): "In multi-turn scenarios, however, a cartridge may need to be loaded mid-conversation—for example, when a user references a new document after several turns of dialogue. Prepending the cartridge at that point would invalidate the previously computed KV entries, requiring a full recomputation of the conversation history." And the compression-tolerance bound: "Compression tolerance varies with document complexity ( 100 × 100\times for TechQA, ≤ 2 × {\leq}2\times for FinQA), reaching within 5% of uncompressed baselines. Finally, we show that cartridges can be combined with retrieval, matching RAG performance while using 3–4 × \times fewer tokens, though for information-dense tasks such as FinQA, text RAG maintains an edge." And on retrieval quality: "This is a pragmatic choice, but it means retrieval quality is bounded by how well the query matches the raw document text rather than the compressed KV representation."
- Date: submitted 3 Jun 2026.
- Evidence: READ BODY

### ABANDONED-6: "Dropping a KV row and seeing no accuracy loss" as validation of an eviction policy — shown to be an unsound inference
- WHO: Zefeng Cai, Zerui Cai (Independent Researchers) — "Compute Globally, Materialize Locally: The Memory Contract of Sparse Event-KV"
- URL: https://arxiv.org/abs/2607.23693
- Closure status: published negative result that undercuts the standard validation methodology of long-context KV eviction/serving schemes (H2O-style, episodic-memory and agent-memory designs).
- STATED REASON, VERBATIM: "Long-horizon agents increasingly reuse their KV cache as memory: a serving system keeps a subset of cached entries and drops the rest. Eviction and episodic-memory schemes therefore rest on a premise rarely tested directly, that a retained event is still informative once the observations that produced it are gone." and the conclusion, verbatim: "One consequence follows for anyone who evicts. An ablation that drops a source event and observes no accuracy loss has not shown that the source was unnecessary; it may have retained a row that already carried the answer." and "For anyone who evicts the corollary is that dropping a source event and observing no accuracy loss does not show the source was unnecessary."
- Second, separate negative inside the same paper (position-extension makes the null WORSE, not better): "16 16 of 20 20 conversations exceed Qwen3's native window, where restoring range with YaRN widens the null rather than closing it (both arms re-run together on one stack: LoCoMo − .048 -.048 [ − .103 , + .009 ] [-.103,+.009] without scaling, − .121 -.121 [ − .181 , − .068 ] [-.181,-.068] with it; paired per-conversation change − .074 -.074 , p = .02 p{=}.02 )."
- Date: arXiv:2607.23693v1 [cs.AI] 26 Jul 2026.
- Evidence: READ BODY

### ABANDONED-7: Final-answer accuracy as the metric for KV compression at long context — shown to be an asymmetric (unsound) diagnostic
- WHO: Mengting Ai, Jingrui He, Yue Guo (University of Illinois Urbana-Champaign) — "Does Accuracy Equal Evidence? Reasoning Faithfulness under KV Cache Compression"
- URL: https://arxiv.org/abs/2608.01631
- Closure status: published negative result whose own framing undercuts accuracy-only evaluation of long-context KV compression — including token-eviction families (recency/attention-sink, H2O/SnapKV-class) that this subsection covers.
- STATED REASON, VERBATIM: "KV cache compression is commonly evaluated by final-answer accuracy, implicitly assuming that preserving the answer also preserves the reasoning that supports it. We test this assumption for large reasoning models and show that it can fail: under compression, correct answers and the validity of their visible supporting rationales can be preserved at different rates." and "Across tasks, token-eviction methods can preserve competitive final-answer accuracy while substantially degrading chain support or perturbation faithfulness. We call this the answer-evidence gap." and "A coverage-preserving quantization control is substantially less affected, suggesting that the failure is tied less to KV memory reduction itself than to losing access to parts of the reasoning trace." and "we show that the final–answer accuracy is an asymmetric diagnostic for compressed reasoning. While collapsed accuracy reveals compression damage, preserved accuracy can hide evidence loss, creating an illusion of competence where correct answer lacks supported evidence."
- Task-dependence, verbatim (which is what makes it usable as a scoping result): "Our results show that the answer–evidence gap is strongly task-dependent: on AIME, GPQA-Diamond, and MedCalc, compressed models can maintain competitive accuracy while reasoning validity or faithfulness substantially degrades. In contrast, on retrieval tasks such as RULER QA, where answers depend more directly on retaining exact supporting evidence, compression damage is more visible through accuracy collapse."
- Date: arXiv:2608.01631v1 [cs.CL] 03 Aug 2026.
- Evidence: READ BODY

### ABANDONED-8: TurboQuant KV cache for long-context chunked prefill — a locked workspace that cannot be tuned around
- WHO: vLLM user (issue author), issue #46767; closed by the reporter as a duplicate
- URL: https://github.com/vllm-project/vllm/issues/46767
- Closure status: **closed as not planned (`stateReason: NOT_PLANNED`) by the reporter themselves, as a duplicate** — not a maintainer rejection. The live issue remains OPEN-adjacent at #41565 and #43357 (both OPEN at retrieval).
- STATED REASON, VERBATIM: "Closing as a duplicate of #41565 (TurboQuant `_continuation_prefill` workspace under-sizing at long context). I posted the quantified ~1.43x scaling analysis (util-independent; need and cap both scale with --max-num-batched-tokens) as a comment there. Also see #43357."
- Date: created 2026-06-25T20:42:25Z.
- Additional verbatim, what was tried and why it is not tunable: "The continuation-prefill workspace **need** and the profiled workspace **cap** both scale linearly with `--max-num-batched-tokens`, with a **fixed ~1.43× ratio** — so the crash cannot be tuned away" and "Changing `--gpu-memory-utilization` (0.96 ↔ 0.94) changes **neither** number — the workspace buffer is sized independently of the KV-cache pool." and the fp8-vs-TurboQuant comparison: "On the same model/GPU, `--kv-cache-dtype fp8` passes a 128k-token needle-in-a-haystack retrieval test (returns the embedded needle correctly in ~16s) that crashes TurboQuant."
- Evidence: READ BODY

---

## HW_RULED_OUT

### HW-1: Decode context parallelism in vLLM (`-dcp`) — cannot be engaged at tp_size = 1
- Hardware requirement: >1 GPU in a tensor-parallel group, with `dcp_size ∈ [1, tp_size/H]`; the CP collective path can additionally require NVLS/NVLink-class symmetric memory multicast.
- URL: https://github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md
- Verbatim evidence of the requirement: "Then we need to add decode context parallel to further shard the KV cache along the `T` dimension. This is as simple as adding `-dcp <size>` to the command line. Note that `size` does not increase the number of GPUs we need to launch, but just reduces the KV cache duplication. The dcp size should lie in the range of `[1, tp_size/H]`." — with `tp_size = 1` the only admissible value is 1, i.e. DCP off. The doc's own case studies all assume 8–16 ranks: "The typical single-node deployment with `-tp 8` causes 8x KV cache duplication. We can consider adding `-dcp 8` to reduce the KV cache duplication." Second requirement, from the in-tree implementation: symmetric-memory multicast across the CP group. `vllm/v1/attention/ops/cp_common.py` probes `_SymmetricMemory.has_multicast_support(DeviceType.CUDA, device.index)` and gates the direct-CP path on `handle.multicast_ptr != 0`, logging "Direct CP symmetric memory across %d ranks: %s". Launch flags at `vllm/engine/arg_utils.py:1090-1115`.
- Evidence: READ BODY

### HW-2: Decode context parallelism in SGLang (`--dcp-size`) — requires an existing tensor-parallel group
- Hardware requirement: a TP group larger than the DCP size; the DCP group must nest inside one attention-TP group and one attention-DP replica (`attn_tp_size % dcp_size == 0`); communication backend resolves to `fi_a2a` only when the DCP group shares an MNNVL domain on Blackwell.
- URL: https://docs.sglang.ai/advanced_features/dcp.html
- Verbatim evidence of the requirement: "Decode context parallelism (DCP) stripes a request’s MLA KV cache across ranks in an existing tensor-parallel group. With DCP size c , rank r owns position p when p mod c = r , so each rank stores and reads roughly 1/c of the cache." and "A DCP group must fit inside one attention-TP group and one attention-DP replica: Topology attn_tp_size = tp_size / attn_dp_size attn_tp_size % dcp_size == 0 For example, TP=64, DP=4, DCP=16 creates four valid 16-rank attention replicas. DCP=32 would cross replica boundaries and is invalid." and, for the Blackwell path specifically: "Unset resolves to fi_a2a where the DCP group shares an MNNVL domain on Blackwell, otherwise a2a on CUDA/ROCm." The documented enablement examples are all multi-GPU: "MLA decode on DeepSeek-V3.1 with DCP 8 inside TP 8" (`--tp-size 8 --dcp-size 8`), and "Kimi K3 on 32 GPUs with DPA and DCP 8".
- Evidence: READ BODY

### HW-3: Ring attention / partial-query–partial-KV prefill CP in vLLM — the only fully-general long-context KV path is explicitly still under development
- Hardware requirement: multiple GPUs (`N` chunks for `N` GPUs); the full-KV variant additionally needs enough aggregate memory across ranks to hold gathered K/V.
- URL: https://github.com/vllm-project/vllm/blob/main/docs/serving/context_parallel_deployment.md
- Verbatim evidence of the requirement: "During prefill, for a long request with `T` new tokens, we need to compute query/key/value tensors for these new tokens. Say we have `N` GPUs, we can split the request into `N` chunks, and each GPU computes one chunk of the query/key/value tensors." and "2. Partial query, partial key/value: If the request token length is too long, we cannot afford holding the full key/value tensors anymore, then we can only compute one chunk of query/key/value tensors for each GPU, and use techniques like [ring-attention](http://arxiv.org/abs/2310.01889) to send/recv key/value tensors chunk by chunk." and the status line: "Both approaches are under active development."
- Evidence: READ BODY

### HW-4: vLLM RFC — independent Context Parallelism for the DSA indexer
- Hardware requirement: ≥2 GPUs in the CP group; the RFC's design evidence is measured on 2× NVIDIA H20.
- URL: https://github.com/vllm-project/vllm/issues/53684
- Verbatim evidence of the requirement: "Our preliminary operator benchmarks on 2x NVIDIA H20 report CP2 latency speedup as T(DCP=1) / T(DCP=2) . Each panel fixes batch size and query length; the x-axis is history length. The Indexer obtains substantially higher CP2 speedup than Sparse MLA across most long-context Q>1 shapes, while both stages regress at Q=1 because communication and launch overheads dominate." The RFC further states the coupling that makes it a CP-only feature: "vLLM currently derives both stages' parallel rank, group, and world size from the same Decode Context Parallelism (DCP) configuration."
- Evidence: READ BODY

### HW-5: [SM120] 1M-context DeepSeek-V4.1-Flash field report — 8× RTX PRO 6000
- Hardware requirement: 8× RTX PRO 6000 96GB with TP=8; Engram tables alone are ~189 GiB across the TP group, so a single 96GB card cannot hold the weights.
- URL: https://github.com/vllm-project/vllm/issues/56700
- Verbatim evidence of the requirement: "Cross-posting field data from a 3-day bring-up of **DeepSeek-V4.1-Flash** (769B total, MXFP4 experts, Engram memory) on **8x RTX PRO 6000 (SM120, consumer Blackwell)**, since SM120 users currently have to rediscover all of this from scratch." and "vLLM `deepseekv41-flash-0909` image, TP=8, … 1M context enabled: 950K-token double-needle retrieval **correct**, 4584 tok/s prefill throughput (pure-torch logits path, eager mode)" and "VRAM: ~91GB/96GB per GPU (Engram tables dominate: 2 x ~100GB shards = 189GiB across the TP group)" and "Env: vLLM 0.1.dev20904 (deepseekv41-flash-0909), torch 2.13.0+cu130, CUDA 13.0, 8x RTX PRO 6000 96GB."
- Evidence: READ BODY

### HW-6: VPP — virtual-stage chunked-prefill pipeline parallelism for up to 1M tokens
- Hardware requirement: 16 Ascend 910C NPUs, TP8 + PP2 (multi-accelerator, multi-stage).
- URL: https://arxiv.org/abs/2608.26523
- Verbatim evidence of the requirement: "We implement VPP in vLLM-Ascend and evaluate it on three MoE-based LLMs with sequences up to 1M tokens on 16 Ascend 910C NPUs." and, in the evaluation setup: "We compare three strategies on 16 NPUs, all using 8-way tensor parallelism with two pipeline ranks: TP8+CPP2 , chunked prefill pipeline parallelism; TP8+DCPP2 , CPP with dynamic chunk sizing, configured with vLLM-Ascend's recommended settings (minimum chunk size: 4096 tokens, smoothing factor: 1.0); and TP8+VPP2 , our proposed scheme."
- Evidence: READ BODY

### HW-7: Cartridges at Scale (CAS) — training modular KV caches over large document collections
- Hardware requirement: a multi-GPU cluster (NVIDIA H200 and B200 cloud GPUs), plus persistent storage for cartridge rotation; the training pool holds a bounded number of cartridges on GPU with the rest offloaded.
- URL: https://arxiv.org/abs/2606.04557
- Verbatim evidence of the requirement: "We train using a cluster with NVidia H200 and B200 cloud GPUs." and "In this work, we introduce CAS, a framework for training and deploying cartridges at scale over collections of hundreds of documents and millions of tokens. CAS proposes a training regime based on dynamic cartridge rotation and mixing that closes the performance gap of isolated training (Fig.  1 ) while remaining feasible within current GPU memory constraints." and "We propose CAS, a scalable training framework with GPU ↔ \leftrightarrow persistent storage swapping, optimizer offloading, and prioritized rotation, enabling joint training of hundreds of cartridges within a fixed GPU memory budget." Also: "with all 20 cartridges active simultaneously (no rotation needed for 20 cartridges on a H100 GPU)".
- Evidence: READ BODY

### HW-8: attention-sink inference for DeepSeek-style sparse MLA on consumer Blackwell — blocked by backend gaps
- Hardware requirement: the working configurations in the retrieved reports are 8× RTX PRO 6000 Blackwell Server Edition (the single-GPU sm_120 path is the one that fails).
- URL: https://github.com/vllm-project/vllm/issues/55757
- Verbatim evidence of the requirement: "Serving any DeepSeek-style sparse-MLA (DSA) model on consumer Blackwell (sm_120) with stock vLLM 0.28.0 hits two independent faults in the MHA prefill path . Fix the first and you immediately hit the second. Together they make GLM-5.3 / GLM-5.2 / DeepSeek-V4 unservable on RTX PRO 6000 Blackwell / RTX 5090 at realistic prompt lengths." and "GPU 8× NVIDIA RTX PRO 6000 Blackwell Server Edition (sm_120) driver 580.126.09" and "⚠️ Short prompts pass. A one-line smoke test succeeds and the server looks healthy; the failure only appears under a realistic prompt length. That is why this is easy to miss in bring-up." The portable fallback that would unblock it is still an open PR, not a shipped feature: https://github.com/vllm-project/vllm/pull/54929 ("[Attention] Portable Triton sparse-MLA fallback for SM12x (consumer Blackwell)", state OPEN at retrieval).
- Evidence: READ BODY

---

## EMPTY SUBSECTIONS

No class came back genuinely empty. Coverage notes and thin spots, stated honestly:

- **ABANDONED for *ring attention KV specifically***: no closed/unmerged PR or paper-level retraction of a ring-attention-for-KV proposal was found in vLLM, SGLang, or arXiv within the window. What exists instead is vLLM's design doc declaring the partial-Q/partial-KV ring path "under active development" (HW-3) — that is an OPEN status, not an abandonment, so no ABANDONED row was emitted for it. I did not find a maintainer rejection of ring attention to quote, and I will not invent one.
- **CLOSED for "RAG with many documents" at the *engine* level**: the only shipping artifacts I could verify are prefix caching (`--enable-prefix-caching`, `--prefix-match-unit`) and the research-side CAS/CoinRAG line. I found no merged vLLM/SGLang PR that specifically targets many-document RAG KV (e.g. per-document cache selection inside the engine), so that sub-question has no CLOSED row of its own; it is represented by CLOSED-3 (system-prompt bloat) and CLOSED-8 (CAS).
- **CLOSED for "attention sinks for long context" inside a *dense GQA long-context* backend on sm120**: sink support exists per backend (CLOSED-6) but the in-tree docs explicitly exclude sinks from the Blackwell head_size=256 FA4 kernel (`does not support logit soft capping, attention sinks, mm_prefix/R-SWA masking, DCP, or windowed encoder attention`), so on this target hardware the sink path is backend-conditional rather than universally available. Counted as a caveat on CLOSED-6, not as a separate row.
- **Thin: exact PR number for the `--enable-mamba-fine-grained-prefix-cache` merge.** CLOSED-3 is evidenced from the shipped docs page and the in-tree config source rather than from the merge PR, because I did not retrieve the merge PR page.
- **arXiv API sweeps returned no data**: `export.arxiv.org/api/query` answered HTTP 429 (rate-limited) throughout this session, and `arxiv.org/list/...` returned HTTP 404 for the 2606/2609 listing URLs I tried. All paper-level findings in this file therefore come from `arxiv.org/abs/<id>` and `arxiv.org/html/<id>v1`, which worked reliably.

---

## QUERIES RUN

**arXiv API (all returned HTTP 429 / empty; recorded as attempted):**
- `abs:"chunked prefill"` · `abs:"ring attention"` · `abs:"context parallelism"` · `abs:"attention sink"` · `abs:"KV cache" AND abs:"long-context"` · `abs:"retrieval-augmented generation" AND abs:"KV cache"` · `abs:"system prompt"` · `abs:"long-context benchmark"` · `abs:"prefill-decode" AND abs:"disaggregation"` · `abs:"million tokens"`

**arXiv abs/HTML pages actually retrieved (verification and abstract capture):**
- https://arxiv.org/abs/2309.17453 · 2310.01889 · 2306.14048 · 2406.10774 · 2404.06654 · 2402.02750 · 2404.14469 · 2404.11912 · 2402.17762 · 2307.03172 · 2501.12948 · 2504.19874 · 2402.17463 · 2606.04557 · 2608.07458 · 2607.21535 · 2608.04074 · 2608.26523 · 2602.07223 · 2607.23693 · 2608.01631
- https://arxiv.org/html/2607.23693v1 · 2608.01631v1 · 2606.04557v1 · 2608.07458v1 · 2607.21535v1 · 2608.26523v1
- (Non-matches retained as evidence of *not* guessing: 2407.12128 resolved to a TTA paper, 2402.09353 resolved to DoRA, 2409.03752/2406.02069 failed verification. Those candidate IDs were discarded rather than cited.)

**GitHub HTML issue/PR search queries:**
- `repo:vllm-project/vllm` — `is:issue is:closed reason:"not planned" long context` · `is:issue is:closed reason:"not planned" KV cache` · `is:issue is:closed reason:"not planned" RFC` · `is:issue is:closed reason:"not planned" prefill` · `is:issue is:closed reason:"not planned" 1M OR million` · `is:issue is:closed "stale" KV cache long context` · `is:issue "attention sink"` · `is:issue "system prompt" cache` · `is:issue prefix cache bloat OR "cache bloat"` · `is:issue is:open "long context"` · `is:issue is:open "sparse attention" long context` · `is:issue "RFC" KV cache` · `long prefill token threshold` · `long-prefill-token-threshold` · `is:issue is:open "sparse attention" long context`
- `repo:vllm-project/vllm` pulls — `is:pr is:closed is:unmerged KV` · `is:pr is:closed is:unmerged "context parallel"` · `is:pr is:closed is:unmerged ring` · `is:pr is:closed is:unmerged attention sink` · `is:pr is:closed is:unmerged chunked prefill` · `is:pr "attention sink"` · `is:pr "long-prefill-token-threshold"`
- `repo:sgl-project/sglang` — `context parallel OR ring attention` · `is:issue is:closed reason:"not planned" context parallel` · `is:issue is:open context parallel` · `is:issue RFC KV cache` · `is:issue is:open long context KV` · `is:pr is:closed is:unmerged KV cache` · `is:pr is:closed is:unmerged chunked prefill` · `is:pr is:closed is:unmerged "context parallel"` · `is:pr is:closed is:unmerged long context`
- `repo:NVIDIA/TensorRT-LLM` — `is:issue is:closed reason:"not planned" KvCacheRetentionConfig OR retention` (0 results) · `is:pr is:closed is:unmerged KV cache`

**GitHub API queries (rate-limited; recorded as attempted):**
- `https://api.github.com/search/issues?q=repo:vllm-project/vllm+"chunked prefill"+in:title&per_page=40&sort=created&order=desc` (HTTP 200, total_count 233)
- `…/search/issues?q=repo:vllm-project/vllm+label:wontfix` (HTTP 200, total_count **0** — vLLM does not use a `wontfix` label)
- `…/search/issues?q=repo:vllm-project/vllm+is:pr+is:closed+is:unmerged+"long context"` (HTTP 200, total_count 190, but the `is:unmerged` qualifier did not filter — results included merged PRs, so this result set was discarded as unusable)
- `…/search/issues?q=repo:vllm-project/vllm+"context parallel"+in:title` · `…/search/issues?q=repo:sgl-project/sglang+is:issue+"context parallel"` · `…/search/issues?q=repo:vllm-project/vllm+is:issue+is:closed+%22not+planned%22+%22long+context%22` → HTTP 403 rate limit

**GitHub pages retrieved in full and read (bodies + comments):**
- Issues: vllm-project/vllm #46767 · #41565 · #43357 · #50497 · #42419 · #42185 · #56700 · #53749 · #55394 · #53595 · #51948 · #47351 · #49630 · #55757 · #53684 · #54300 · #55733 · #46715 ; sgl-project/sglang #36522 · #36208
- Pulls: vllm-project/vllm #50736 · #47181 · #54404 · #54568 · #54929 · #52094 · #55789 ; sgl-project/sglang #38413

**Engine docs retrieved:**
- https://docs.vllm.ai/en/latest/configuration/optimization/ · https://docs.vllm.ai/en/latest/features/automatic_prefix_caching/ · https://docs.sglang.ai/advanced_features/server_arguments.html · https://docs.sglang.ai/advanced_features/dcp.html · https://docs.sglang.ai/advanced_features/hicache_design.html · https://docs.sglang.ai/advanced_features/pipeline_parallelism.html

**Local greps (source trees):**
- `/tmp/kvsrc/vllm-main`: `enable_chunked_prefill` in `vllm/config/scheduler.py`, `vllm/config/vllm.py`, `vllm/engine/arg_utils.py` · `long_prefill_token_threshold` across `vllm/` + `tests/` + `docs/` · `prefix_match_unit` / `enable_mamba_fine_grained_prefix_cache` across `vllm/` · `decode_context_parallel_size` / `dcp_*` / `prefill_context_parallel_size` / `-pcp` / `-dcp` in `vllm/engine/arg_utils.py` · `context_parallel` file list across `vllm/` · `ring` across `vllm/` · `attention_sink` / `sinks` across `vllm/` and `csrc/` · `TODO|FIXME|NOTE:` in `vllm/v1/attention/ops/dcp.py`, `pcp.py`, `vllm/v1/worker/gpu/pcp_manager.py` (nil) · `not supported|not implemented|unsupported|limitation` in the same files · reads of `vllm/v1/attention/ops/cp_common.py` (symmetric-memory multicast probe), `vllm/v1/attention/ops/dcp.py`, `vllm/config/scheduler.py`, `vllm/config/cache.py`, `docs/serving/context_parallel_deployment.md`, `docs/design/attention_backends.md`, `docs/features/context_extension.md`, `docs/features/automatic_prefix_caching.md`
- `/tmp/kvsrc/sglang-main`: file inventory (607 files; no `python/` subtree) · `context_parallel|ring_attn|ring attention` across `python/` (no such path — returned nothing, consistent with a partial checkout)

**Web searches:**
- "H2O heavy hitter oracle KV cache arxiv 2306" · "Quest query-aware sparsity KV cache arxiv 2406" · "arxiv 2026 long context KV cache survey 1M tokens" · "vLLM chunked prefill default enable v1 arxiv paper"
- "arxiv 2026 KV cache eviction does not help long context negative result" · "arxiv 2026 long context KV cache compression accuracy loss limitation" · "RULER benchmark KV cache compression fails at 128K arxiv 2025" · "attention sink long context 2026 arxiv follow-up"
- "arXiv 2602 Vegas self-speculative decoding verification-guided sparse attention" · "arxiv 2026 ring attention beyond single node bottleneck limitation negative" · "arxiv 2026 context parallelism inference long context KV cache sharding" · "arxiv 2026 chunked prefill 1M token inference serving"
- "arxiv 2026 attention sink does not help long context sliding window negative" · "arxiv 2026 \"attention sink\" long context KV cache paper" · "arxiv 2026 RAG many documents KV cache prefill cost" · "vLLM chunked prefill default v1 PR merged which version"
