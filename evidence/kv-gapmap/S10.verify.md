# Verification of S10

Verified: 2026-09-13 (UTC) by adversarial re-fetch of every cited URL through the local proxy helper
(`bash /tmp/fetch.sh <url> <out>`), re-extraction of page bodies, and exact-string search over the
retrieved payloads (HTML entities, JSON escapes and markdown artifacts normalised).
Target file: `/Users/leihenan/Desktop/myProject/evidence/kv-gapmap/S10.md` (321 lines).
Local source trees `/tmp/kvsrc/vllm-main` and `/tmp/kvsrc/sglang-main` were also inspected where the
file cites them.

## URL check

All 40 URLs cited in the deliverable were fetched. The table also includes 6 supporting URLs that
appear in the body text (rows 41-46); those are marked "(supporting)".

| URL | HTTP | Effective URL | Title seen | Verdict |
|---|---|---|---|---|
| https://github.com/vllm-project/vllm/pull/12523 | 200 | same | layerwise KV transfer in PD Disaggregation by chenqianfzh · PR #12523 | OK |
| https://github.com/vllm-project/vllm/issues/10818 | 200 | same | [RFC]: Disaggregated prefilling and KV cache transfer roadmap · Issue #10818 | OK |
| https://github.com/vllm-project/vllm/issues/32200 | 200 | same | [RFC]: Change `kv_load_failure_policy` Default from "recompute" to "fail" · Issue #32200 | OK |
| https://github.com/vllm-project/vllm/issues/42024 | 200 | same | [Bug]: NIXL connector silently disables HMA, halving KV cache capacity — flip default to HMA=on even with connectors · Issue #42024 | OK (content claim mismatched — see below) |
| https://github.com/vllm-project/vllm/issues/33170 | 200 | same | [Performance]: NIXL telemetry throughput does not consider transfer overlapping · Issue #33170 | OK |
| https://github.com/vllm-project/vllm/issues/25043 | 200 | same | [Bug]: disaggregated_serving_p2p_nccl_xpyd prefill worker failed with "EngineCore failed to start". · Issue #25043 | OK |
| https://github.com/ai-dynamo/dynamo/issues/5870 | 200 | same | Frontend panic with different TP sizes in SGLang disaggregated serving (non-MLA models) · Issue #5870 | OK |
| https://github.com/vllm-project/vllm/issues/31064 | 200 | same | [RFC]: Optional Hidden States Transfer in `KVConnectorBase_V1` to Eliminate Decoder Prefix-Prefill in P/D Disaggregation · Issue #31064 | OK |
| https://github.com/vllm-project/vllm/issues/55870 | 200 | same | [Bug]: Mooncake bootstrap and KV transfer failures can leave PD requests waiting without terminal error propagation · Issue #55870 | OK |
| https://github.com/vllm-project/vllm/pull/56089 | 200 | same | [Bugfix][KV Connector] Wait for KV transfers before freeing aborted requests by root1iu · PR #56089 | OK |
| https://github.com/vllm-project/vllm/pull/56166 | 200 | same | [Bugfix][KVConnector] Report Mooncake P2P pull failures to the scheduler by LH-and-FPGA · PR #56166 | OK |
| https://github.com/vllm-project/vllm/pull/41847 | 200 | same | [KV Transfer] Enable HMA by default for connectors that support it by chfeng-cs · PR #41847 | OK |
| https://github.com/vllm-project/vllm/issues/41830 | 200 | same | [Feature]: --kv-transfer-config unconditionally disables HMA, ignoring SupportsHMA on the connector · Issue #41830 | OK |
| https://github.com/vllm-project/vllm/pull/45019 | 200 | same | [NIXL][Mamba] Add Mamba1 support to NIXL P/D disaggregation by Josephasafg · PR #45019 | OK |
| https://github.com/vllm-project/vllm/pull/34388 | 200 | same | [WIP][Spec Decode] P/D disaggregation: transfer hidden states for EAGLE warm-up by ZhanqiuHu · PR #34388 | OK |
| https://github.com/LMCache/LMCache/pull/4221 | 200 | same | feat(sglang): add LMCachePDConnector for PD disaggregation over NIXL by skaulintel · PR #4221 | OK |
| https://github.com/LMCache/LMCache/issues/4921 | 200 | same | [Bug][GPU Connector] SGLang layerwise connector ignores the partial slot-mapping offset, writing retrieved KV to the wrong paged slots · Issue #4921 | OK |
| https://github.com/vllm-project/vllm/blob/main/vllm/distributed/kv_transfer/kv_connector/v1/nixl/base_worker.py | 200 | same | vllm/vllm/distributed/.../nixl/base_worker.py at main | OK |
| https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/distributed/kv_transfer/kv_connector/v1/nixl/base_worker.py | 200 | same | (source, 152,193 bytes) | OK |
| https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/distributed/kv_transfer/kv_connector/v1/nixl/connector.py | 200 | same | (source, 15,487 bytes) | OK |
| https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/distributed/kv_transfer/kv_connector/v1/mooncake/mooncake_connector.py | 200 | same | (source, 89,174 bytes) | OK |
| https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/config/kv_transfer.py | 200 | same | (source, 6,033 bytes) | OK |
| https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/config/vllm.py | 200 | same | (source, 149,794 bytes) | OK |
| https://docs.vllm.ai/en/latest/features/nixl_connector_usage/ | 200 | same | NixlConnector Usage Guide - vLLM | OK |
| https://docs.vllm.ai/en/latest/features/nixl_connector_compatibility/ | 200 | same | NixlConnector Compatibility Matrix - vLLM | OK |
| https://docs.vllm.ai/en/latest/features/disagg_prefill/ | 200 | same | Disaggregated Prefilling (experimental) - vLLM | OK |
| https://docs.sglang.ai/advanced_features/pd_disaggregation.html | 200 | https://docs.sglang.io/docs/advanced_features/pd_disaggregation | PD Disaggregation - SGLang Documentation | OK (redirect exactly as the file states) |
| https://docs.dynamo.nvidia.com/dynamo/v-0-8-1/user-guides/tuning-disaggregated-performance | 200 | same | Disaggregation and Performance Tuning \| NVIDIA Dynamo Documentation | OK |
| https://docs.nvidia.com/dynamo/ | 200 | same | Dynamo \| NVIDIA Dynamo Documentation | OK |
| https://arxiv.org/abs/2504.19867 | 200 | same | [2504.19867] semi-PD: Towards Efficient LLM Serving via Phase-Wise Disaggregated Computation and Unified Storage | OK (title matches "semi-PD"; quoted testbed text lives only on the /html/ page) |
| https://arxiv.org/html/2504.19867v1 | 200 | same | semi-PD: Towards Efficient LLM Serving via Phase-Wise Disaggregated Computation and Unified Storage | OK |
| https://arxiv.org/abs/2604.15039 | 200 | same | [2604.15039] Prefill-as-a-Service: KVCache of Next-Generation Models Could Go Cross-Datacenter | OK |
| https://arxiv.org/abs/2606.07684 | 200 | same | [2606.07684] Semantic Cache Distillation: Efficient State Transfer via Reuse and Selective Patching | OK |
| https://arxiv.org/abs/2606.29986 | 200 | same | [2606.29986] HBM Is Not All You Need: Efficient Disaggregated LLM Serving across Memory-heterogeneous Accelerators | OK (system is named HMA-Serve in the abstract, as the file says) |
| https://arxiv.org/html/2606.29986v1 | 200 | same | HBM Is Not All You Need: ... | OK |
| https://arxiv.org/abs/2607.01617 | 200 | same | [2607.01617] 3DLS: A 3D Logic-Stacked Architecture for Disaggregated LLM Serving | OK (Related DOI 10.1109/LCA.2026.3709108 confirmed on page) |
| https://arxiv.org/html/2607.01617v1 | 200 | same | 3DLS: A 3D Logic-Stacked Architecture for Disaggregated LLM Serving | OK |
| https://arxiv.org/abs/2606.29708 | 200 | same | [2606.29708] Demystifying the Design Space and Best Practices for Heterogeneous LLM Inference and Serving | OK (the "heterogeneous-serving survey") |
| https://arxiv.org/abs/2607.28150 | 200 | same | [2607.28150] SmartGen: Seamless Disaggregated LLM Inference with Selective KV Cache Transfer | OK |
| https://arxiv.org/a/qin_r_1 | 200 | https://arxiv.org/a/qin_r_1.html | Ruoyu Qin's articles on arXiv | OK (Ruoyu Qin is first author of 2604.15039) |
| https://docs.nvidia.com/dynamo/latest/user-guides/disaggregated-serving.html (supporting) | 404 | same | Documentation (error page) | DEAD — correctly declared 404 and not used by the file |
| https://docs.lmcache.ai/ (supporting) | 200 | same | Welcome to LMCache! \| LMCache | OK |
| https://docs.lmcache.ai/developer_guide/layerwise.html (supporting) | 404 | same | Page not found · GitHub Pages | DEAD — correctly declared 404 and not used by the file |
| https://docs.sglang.io/docs/advanced_features/pd_disaggregation (supporting) | 200 | same | PD Disaggregation - SGLang Documentation | OK (redirect target) |
| https://github.com/vllm-project/vllm/issues?q=is%3Aissue+disaggregation+reason%3Anot+planned (supporting) | 200 | same | Issues · vllm-project/vllm · GitHub | OK (page loads; contains 3 issue links, so the "zero parsable issue links" note is not reproducible) |
| https://api.github.com/rate_limit (supporting) | 200 | same | (JSON, 424 bytes) | OK at verification time |

No cited URL is dead, none redirects somewhere unrelated, and every effective URL equals the cited URL
except the SGLang docs redirect, which the file itself documents.

## Verbatim quote check (ABANDONED rows)

Each ABANDONED row was re-fetched and searched for the claimed `STATED REASON, VERBATIM` string.
Secondary "verbatim" quotes that failed or needed qualification are listed at the bottom of the table.

| Row | Status | Notes |
|---|---|---|
| ABANDONED-1 (PR #12523) | FOUND EXACTLY | "the layerwise method performs worse as it needs to retrieves layer_number tensors, so it requires more overhead. I will continue to work on the solution that the consumer starts to receive the KV cache once the producer completes a layer." appears verbatim in the PR thread (author chenqianfzh). All five corroborating benchmark strings also match exactly: both TTFT blocks (167.27/158.20/241.40 and 75.70/73.06/97.90), both throughput lines (1571.41 / 1570.67), the "in the same machine (an AWS machine with A10G GPU)" framing, and the `LayerwisePyNcclConnector` `kv_rank` 0/1 `kv_parallel_size` 2 commands. State CLOSED, `mergedTime: null`, `mergedBy: null`, created 2025-01-28T20:09:22Z, closed 2025-03-19T05:14:33Z — all confirmed. |
| ABANDONED-2 (issue #10818) | FOUND EXACTLY | "- [ ] [Perf] layer-by-layer pipelining (#12523)" is present verbatim in the roadmap body; the strikethrough lines "~~[ ] InfiniteStore (#9079 @chenqianfzh )~~ (no response from the developer)" and "~~[ ] Valkey (#8724 @zeroorhero @pizhenwei )~~ (no response from the developer)" are verbatim. `stateReason: "NOT_PLANNED"` and created 2024-12-02T03:01:11Z confirmed. Caveat: the quoted line is a roadmap checkbox, not a closure statement (see Suspect section). |
| ABANDONED-3 (issue #32200) | FOUND EXACTLY | Both stale-bot strings match character-for-character ("…marked as stale because it has not had any activity within 90 days…" and "…automatically closed due to inactivity…"). The two human endorsements quoted also match exactly, as does "Currently, the default is `"recompute"`, which recomputes failed blocks on the decode instance." Closed 2026-06-21T02:22:37Z by `github-actions` with `stateReason: NOT_PLANNED`; stale mark 2026-05-21 — the file's "stale-bot comments dated 2026-05-21 and 2026-06-21" is correct. |
| ABANDONED-4 (issue #42024) | FOUND EXACTLY (text) / closure reason NOT CONFIRMED | The quoted comment "This duplicates #41847 which addresses the same issue. Note that a class-level supports_hma() check is insufficient for MultiConnector — the wrapper class implements SupportsHMA but effective support depends on all configured children. #41847 handles this via KVConnectorFactory.supports_hma_config() which recurses into child connectors." is verbatim, and "- GPU: RTX 6000 Pro (reproduced); expected to affect all GPU types" is verbatim, and "vLLM version: 0.18.x" is confirmed. **But** the quote is an ordinary comment by `chfeng-cs` (the #41847 author, `authorAssociation: CONTRIBUTOR`, 2026-05-08T15:11:48Z), not a maintainer and not a closure reason. The actual `ClosedEvent` is 2026-09-12T01:40:06Z by `github-actions` (stale bot), `duplicateOf: null`. The date the file calls the "closure trace" (2026-08-08) is the stale-warning comment/label date (2026-08-08T02:12:17Z), not the closure. |
| ABANDONED-5 (issue #33170) | FOUND EXACTLY | "This issue has been automatically closed due to inactivity. Please feel free to reopen if you feel it is still relevant. Thank you!" verbatim; reporter's closing line "I encounter a magnificent NIXL throughput drop with the `batch_size` increasing and I think there should be some issues in the telemetry." verbatim; created 2026-01-27T18:07:23Z, stale 2026-05-06, closed 2026-06-05 by `github-actions` — all as stated. |
| ABANDONED-6 (issue #25043) | FOUND EXACTLY | Stale closure text verbatim; all five thread quotes verbatim ("It appears `bind_connector_metadata` is not called for the prefill worker", the `KVConnectorModelRunnerMixin`/`aefeea0fd` paragraph, "Also impacted, bisecting to locate the culprit…", "@QierLi have you identified a fix?", follow-up #26675, 1P3D on HGX B200). Date line is wrong — see Suspect section. |
| ABANDONED-7 (dynamo #5870) | FOUND EXACTLY | "This issue has been closed due to inactivity. If you believe this issue is still relevant, please feel free to reopen it with additional context or information." verbatim; the AIConfigurator topology description, Qwen3-32B-FP8, H200 SXM, Dynamo 0.8.0 and the `futures-util-0.3.31`/`Unfold must not be polled…` panic string are all present. Created 2026-02-01T23:38:19Z, stale 2026-03-04, closed 2026-03-09 by `github-actions` — as stated. |
| ABANDONED-8 (issue #31064) | FOUND EXACTLY | NickLucche's "I have an aversion to adding this complexity in order to avoid running a single forward pass for 1 token --- so just want to make sure I understand why the current impl is not working well" verbatim; the fallback hypothesis about incomplete blocks verbatim. Issue `stateReason: COMPLETED`, created 2025-12-20T05:56:32Z, closed 2025-12-20T21:43:09Z by robertgshaw2-redhat — as stated. |
| OPEN-6 / ABANDONED-5 secondary quote (issue #33170) | FOUND APPROXIMATELY | The file renders the second reporter's comment as "it treats each rank as **independent**, so if total bytes_transferred is 1000MB…". The issue text reads "it treats each rank **independently**, so if …". One word differs inside quotation marks; the rest of the sentence matches exactly. |
| OPEN-3 / ABANDONED-8 secondary claim (ExampleHiddenStatesConnector) | FOUND EXACTLY | The docstring quoted in OPEN-3 is verbatim in live `main` (`example_hidden_states_connector.py`, class at line 98): "Simple debug implementation of a HiddenStatesConnector. Simply extracts the hidden states from the kv cache and stores them to disk. Must be used in conjunction with the `extract_hidden_states` spec decoding method." It is the only hidden-states connector registered in `kv_connector/factory.py` (lines 159/161). |
| CLOSED-10 secondary quote (2606.07684) | FOUND APPROXIMATELY | "…while keeping generation quality within 5% F1 of the oracle" is in the abstract as `5\% F1` (arXiv LaTeX escape); "across heterogeneous models (e.g. base and fine-tuned variants)…" is in the abstract as "(e.g., base and fine-tuned variants)" with a comma. Wording otherwise identical. |
| HW-1 secondary quotes (2504.19867) | FOUND APPROXIMATELY (and not on the cited URL) | The four-platform testbed paragraph exists in the paper's HTML full text but as "Server A : one node with eight NVIDIA A100 80GB SXM4 GPUs connected with pairwise NVLink  [ 20 ] ." — the file normalised the spaces before colons and the citation markers to "Server A:" and "[20]". Content is faithful; the string is not findable on the cited `abs` page (only on `/html/2504.19867v1`). |
| HW-2 secondary quote (2606.29986) | FOUND EXACTLY (but not on the cited URL) | "We evaluate every system on real silicon: NVIDIA A100-80GB on the HBM side, a four-chip Tenstorrent Blackhole p150 mesh (TT × 4) on the GDDR side, over a 100 Gb RoCE fabric, all serving through vLLM 0.19.1" is verbatim on `/html/2606.29986v1`, not on the cited `abs` page. The abstract quote ("Pushed to its most economical form, MemHA serving is inherently cross-vendor…") is verbatim on the `abs` page. |
| HW-3 secondary quotes (2607.01617) | FOUND EXACTLY (two of three only on the /html/ page) | The architecture sentence is on the `abs` page verbatim; "We evaluate 3DLS against two planar baselines using a trace-driven in-house simulator that models TP, layer-wise compute and communication, and prefill-to-decode KVT." and "The experiment uses OPT-175B [8], TP=16, and 512 GB/s D2D bandwidth." are in the HTML full text (the latter with arXiv's "[ 8 ] ," citation spacing). Cited URL is the `abs` page. |

## READ BODY downgrades

| Row | Old label | New label | Reason |
|---|---|---|---|
| ABANDONED-4 (issue #42024) | READ BODY | TITLE ONLY | The row's distinguishing detail — "closed as not planned … **as a duplicate of** the merged PR #41847", with a **maintainer** response and a "closure trace dated 2026-08-08" — is not supported by the page. The issue was stale-closed by `github-actions` on 2026-09-12T01:40:06Z (`duplicateOf: null`); 2026-08-08 is the stale warning; the quoted comment is by contributor `chfeng-cs`, not a maintainer. The verbatim text is real, but the row's claimed semantics are not confirmable. |

No other READ BODY row needed downgrading: for every other CLOSED / OPEN / ABANDONED / HW row I was able
to locate the claimed detail in the retrieved body (see tables above and the spot-checks below).

Spot-checks that passed (body really contains the detail):
- CLOSED-1 — live `vllm/config/vllm.py` lines 2018-2024 match exactly (`# Auto-disable HMA only when the connector config does not support it.`, `if not KVConnectorFactory.supports_hma_config(self.kv_transfer_config):`, and the "hybrid SSM models (e.g. Jamba, Bamba)…" warning). PR #41847 merged 2026-05-26T10:28:51Z by NickLucche; the #41830 plan quote is verbatim; issue #41830 is `COMPLETED`.
- CLOSED-2 — live `kv_transfer.py` line 103 is exactly `kv_load_failure_policy: Literal["recompute", "fail"] = "fail"` with the stated docstring; all NixlConnector Usage Guide "KV Load Failure Policy" quotes are verbatim.
- CLOSED-3 — `kv_producer` / `kv_consumer` / `kv_both (deprecated)` text, "kv_role=\"kv_both\" is deprecated for NixlConnector.", `kv_lease_duration` default 30, heartbeat-extension sentence and `decoder_kv_blocks_ttl` default 480 are all verbatim in the usage guide.
- CLOSED-4 — "What must match between P and D" and "What can safely differ between P and D" lists, the compatibility-hash sentence, the push/pull incompatibility sentence, LBHNC/LBNHC and `enable_permute_local_kv`, and the dynamic-quantization refusal ("Per-block scales are not transferred alongside KV cache data.") are all verbatim.
- CLOSED-5 — PR #45019 merged 2026-06-25T12:50:42Z, created 2026-06-09; purpose text, the "8×H100 box" accuracy-parity line, the `ai21labs/AI21-Jamba2-Mini (Mamba1)` model line and ZhanqiuHu's review comment are verbatim.
- CLOSED-6 — every quoted Dynamo sentence ("At low load, disaggregation would not benefit much…", the "as few prefill engines as possible (even no prefill engine)" advice, the block-size-128 advice, "chunked prefill is currently not supported in Dynamo") is verbatim in the v-0-8-1 tuning guide.
- CLOSED-7 — "Now supports 9 types of connectors" plus all named connectors (ExampleConnector, LMCacheConnectorV1, LMCacheMPConnector, NixlConnector "which support fully async send/recv", MooncakeConnector, MoRIIOConnector (ROCm only), MultiConnector, OffloadingConnector, FlexKVConnectorV1), the `{"backends":["UCX", "GDS"]}` example, and the SGLang "Currently, we support Mooncake and NIXL as the transfer engine." are verbatim.
- CLOSED-8 — "Disaggregated prefill DOES NOT improve throughput." is verbatim in the vLLM page; the Dynamo low-load sentence is verbatim in the Dynamo guide.
- CLOSED-9/CLOSED-10 — HMA-Serve and SCD abstract sentences verified as noted above.
- OPEN-1 — issue #55870 body ("Ascend A3", MultiConnector + MooncakeConnector, `WAITING_FOR_REMOTE_KVS`) and PR #56166 body (all three dropping paths, "abandoning the pending `PullReqMeta` s", the NixlConnector-contrast sentence) are verbatim; both are open (created 2026-09-08 / 2026-09-09).
- OPEN-2 — live `nixl/base_worker.py` contains both `NotImplementedError` strings (lines 636-644), the comment "PP push slices regions per layer (uniform count); HMA breaks that." (line 635), `_report_failed_recv` with the `if not self._is_hma_required:` guard (line 2849), `_failed_inflight_recvs`, and the deferred-invalidation comment (lines 2836-2837). The correction note is accurate: the local tree's `# TODO (NickLucche) handle failed transfer for HMA.` (line 2776 of `/tmp/kvsrc/vllm-main/.../nixl/base_worker.py`) is absent from live `main`.
- OPEN-3 — RFC quotes, the NickLucche pushback, the "Per offline discussion with @xinyu-intel… Gaudi team will look into improving this." closer and the `ExampleHiddenStatesConnector` docstring are all confirmed.
- OPEN-4 — the whole "Reasoning models with stripped thinking traces" paragraph, "We currently assume the router is able to detect such mismatch across turns.", "See #43094" and host-buffer "planned for future work" are verbatim in the Limitations section.
- OPEN-5 — PR #56089 quotes are verbatim, it is open (opened 2026-09-09), and it references "Fixes #46240".
- OPEN-6 — first quote verbatim; "/cc @NickLucche" appears as a ping with no reply from him (comment authors are Spycsh, markmc, erezzarum).
- ABANDONED-1 downstream check — `nixl/connector.py` line 283 `"""NixlConnector does not do layerwise saving."""` and `mooncake/mooncake_connector.py` line 591 `"""MooncakeConnector does not do layerwise saving."""` confirmed on live `main`; `/tmp/kvsrc/vllm-main/.../mooncake/store/connector.py` carries `# No layerwise support - no-op` exactly twice (lines 333, 343).
- Local-tree claims — `/tmp/kvsrc/vllm-main/vllm/config/kv_transfer.py` line 69 does carry `kv_load_failure_policy: Literal["recompute", "fail"] = "fail"`; `/tmp/kvsrc/sglang-main` is indeed a partial checkout (benchmark/, docker/, docs/, 3rdparty/; no `python/` tree), matching the "SGLang is thin on purpose" note.
- QUERIES RUN numbers — spot-checked live against `api.github.com/search/issues`: `repo:vllm-project/vllm "layerwise"` = 526 (file says 526), `"hidden states" disaggregation` = 26 (file says 26), `repo:ai-dynamo/dynamo disaggregation state:closed reason:not_planned` = 25 (file says 25), `repo:vllm-project/vllm nixl state:closed reason:not_planned` = 85 (file says 85). Four exact reproductions.
- Referenced-but-not-cited issue numbers: LMCache #4132 and #4133 both exist, are open, and are layerwise-correctness bugs; sglang #16903 exists; PR #34388 really is titled "…transfer hidden states for EAGLE warm-up" and is CLOSED.

## Suspect or unverifiable claims

- **ABANDONED-4 is mislabelled and mis-dated.** "Closed as not planned … as a duplicate of the merged PR #41847" is contradicted by the page: `ClosedEvent` 2026-09-12T01:40:06Z, actor `github-actions`, `duplicateOf: null`. The "closure trace dated 2026-08-08" is the stale *warning* date. The quoted comment is by contributor `chfeng-cs`, not "a maintainer". The row should be re-described as a stale-closure whose duplicate comment came from the PR author.
- **ABANDONED-6's date line is internally impossible.** It says "created 2025-09-17T06:18:05Z; stale-bot activity 2025-04-15-era thread continued through 2025-10-15". No 2025-04-15 activity exists; the real timeline is: created 2025-09-17, human comments 2025-09-21 → 2025-10-15, stale label 2026-01-15, closed 2026-02-15T02:16:56Z. The actual closure date is omitted.
- **The "STATED REASON, VERBATIM" framing is stretched in two rows.** ABANDONED-2's "reason" is a roadmap checkbox line ("- [ ] [Perf] layer-by-layer pipelining (#12523)"), not a closure statement, and the issue's stale-closure date is not given. ABANDONED-4's "reason" is a comment, not the closure reason (see above). The quoted text itself is genuine in both cases.
- **One quote is a paraphrase inside quotation marks.** OPEN-6/ABANDONED-5 renders "it treats each rank as independent" where the issue says "it treats each rank independently". Everything else in that sentence is exact.
- **Minor "verbatim" normalisations** that a strict reader could object to: SCD "5% F1" (`5\% F1` in the source), SCD "(e.g. base…)" vs "(e.g., base…)", and HW-1's "Server A:" / "[20]" vs the HTML's "Server A :" / "[ 20 ]". Content-preserving, but not byte-exact.
- **Provenance mismatch on three HW rows.** HW-1, HW-2 and HW-3 each cite an `arxiv.org/abs/...` URL while the hardware requirement they quote is only present in the companion `/html/...v1` page (which the file does list separately under QUERIES RUN, and which it did retrieve). The rows would be cleaner citing the HTML URL.
- **CLOSED-table ↔ CLOSED-detail numbering inconsistency.** The table's ninth row is 3DLS/2607.01617, but detail section CLOSED-9 is HMA-Serve/2606.29986; the HMA-Serve paper has no CLOSED-table row (it only appears as HW-2) and 3DLS has no CLOSED detail section (it only appears as HW-3). Two papers are effectively double-classified, and the table row/detail mapping is off by one from row 9 onward.
- **Unverifiable process claims (historical, time-dependent).** The seven arXiv Atom API HTTP 429s and the "core bucket exhausted / per-issue API calls returned 403" claim cannot be reproduced after the fact; at verification time `api.github.com/rate_limit` and `api.github.com/search/issues` responded normally (the latter returned an exact match to the file's stated totals). Treat as plausible but unconfirmed.
- **Two soft process inaccuracies.** (a) The GitHub issue-search HTML page (`?q=is:issue disaggregation reason:not planned`) currently contains 3 parsable issue links, so "zero parsable issue links" is not reproducible. (b) PR #34388's body is not entirely unextractable — a TODO-list fragment ("…Change hidden states memory management to paged management…Related: #33118") plus empty `## Purpose / ## Test Plan / ## Test Result` headers are visible in the served payload. Neither affects an emitted row.
- **No identical-byte-count pattern exists to flag**: S10.md reports no `bytes=` numbers at all, so the classic duplicate-fetch tell is absent. Byte sizes in this verification are all distinct.
- **Disclosed duplication (not a defect):** OPEN-6 and ABANDONED-5 describe the same issue #33170, which the file states explicitly; CLOSED-8 reuses CLOSED-6's Dynamo low-load quote, which is a legitimate cross-reference.
- **Not independently verified:** the researcher's own environment profile (one RTX PRO 6000, vLLM 0.29.0, etc.) has no external source; the claim that the Prefill-as-a-Service paper surfaced via a third-party GitHub paper-notes aggregator is a process note I did not reconstruct.

## Summary

- **URLs checked: 46** (the 40 cited URLs plus 6 supporting URLs appearing in the body text).
  - **OK: 44** — all **40 cited URLs returned HTTP 200** with titles matching the file's descriptions, and none redirected somewhere unrelated (the one redirect, `docs.sglang.ai` → `docs.sglang.io`, is exactly what the file documents).
  - **MISMATCH: 0** at URL level. (One *content* mismatch: the ABANDONED-4 row's claimed closure semantics, reported above.)
  - **DEAD: 2** — `docs.nvidia.com/dynamo/latest/user-guides/disaggregated-serving.html` (404) and `docs.lmcache.ai/developer_guide/layerwise.html` (404). Both were declared 404 by the file itself and explicitly excluded from the evidence, so they are not agent errors.
- **Verbatim quotes (8 ABANDONED rows): FOUND EXACTLY 7, FOUND EXACTLY-but-mischaracterised 1 (ABANDONED-4), NOT FOUND 0, COULD NOT FETCH 0.** Across all checked quotes (including secondary ones in OPEN/CLOSED/HW rows): **FOUND EXACTLY 20+, FOUND APPROXIMATELY 4, NOT FOUND 0** — the only true wording deviation is "as independent" for "independently" in OPEN-6's second-reporter quote.
- **arXiv titles: all 8 IDs match the file's claimed papers** (semi-PD, PrfaaS, SCD, HMA-Serve, 3DLS, the heterogeneous-serving survey, SmartGen) and the 3DLS IEEE CAL DOI on the file's CLOSED row is confirmed by arXiv's "Related DOI" field. **PR/issue numbers: all resolve to the claimed artifacts**; no fabricated identifiers found.
- **READ BODY downgrades: 1** (ABANDONED-4 → TITLE ONLY). The remaining 28 READ BODY rows survived spot-checking.
- **Rows I could not fully verify:** ABANDONED-4 (closure reason and "maintainer" attribution — refuted, downgraded); the historical API rate-limit claims under QUERIES RUN (unreproducible, though the search totals reproduce exactly); the "zero parsable issue links" and "#34388 body not extractable" process notes (soft inaccuracies); and HW-1's exact string, which is only present in normalised form on the HTML page rather than the cited abstract URL.
- **Overall reliability verdict: HIGH.** The file's URLs, identifiers, dates, code citations and quoted evidence are overwhelmingly real and check out against live sources, including the self-correcting note that the local tree is stale on the HMA TODO line. The defects are concentrated in framing rather than fabrication: one row (ABANDONED-4) mislabels a stale closure as a duplicate closure and calls a contributor's comment a maintainer decision, one row (ABANDONED-6) carries a wrong date line, and there are a handful of cosmetic "verbatim" normalisations plus an off-by-one between the CLOSED table and its detail sections. Nothing in the file appears invented; the one thing a consumer should not trust without re-checking is ABANDONED-4's closure story.
