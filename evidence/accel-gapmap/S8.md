# S8 — Serving-layer / framework overhead: HTTP/API layer, request lifecycle, `/metrics`, and "we measure X but not Y" observability gaps

Rig context for relevance: ONE GPU (RTX PRO 6000 Blackwell 96 GB, sm120), vLLM 0.29.0, SGLang 0.5.19,
Qwen3-4B dense. Out of scope here: KV-cache management and speculative decoding.

Method note: where a primary source is a source file, the quote is taken verbatim from the retrieved
page (raw.githubusercontent for docs, `fetch.py gh` for issues/PRs, `fetch.py arxiv` for papers).
Every arXiv ID below was read back with `fetch.py arxiv <id>` and its title confirmed.

---

## A — CLOSED (shipped / merged / published)

### A1.1 | CLOSED
QUESTION: Does vLLM's own documentation treat the API-server / HTTP process as a performance-relevant cost, or does the project claim the HTTP layer is free?
WHO: vLLM maintainers (documentation)
ARTIFACT: `docs/configuration/optimization.md` § "CPU Resources for GPU Deployments" (v0.29.0)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/configuration/optimization.md
STATUS: shipped in v0.29.0
EVIDENCE: READ BODY
QUOTE: "Using fewer physical CPU cores than processes will cause contention and significantly degrade throughput and latency. The engine core process runs a busy loop and is particularly sensitive to CPU starvation."
QUOTE-2: "- **Output processing** -- detokenization, networking, and especially streaming token responses use CPU cycles"
QUOTE-3: "If you observe that GPU utilization is lower than expected, CPU contention may be the bottleneck. Increasing the number of available CPU cores and even the clock speed can significantly improve end-to-end performance."
NOTE: The same section names "tokenization, chat template rendering, and multi-modal data loading" as CPU-resident input-processing work. This is the project's own statement that the front-end is a first-class cost, i.e. it is evidence *against* an unqualified "the HTTP layer is not the bottleneck" claim.
S3B: SOLVED (shipped in v0.29.0 ; verified present at line 424+ of the v0.29.0 doc)

### A1.2 | CLOSED
QUESTION: Is there a supported way to scale the HTTP/API layer independently of the engine, and why does it exist?
WHO: Nick Hill (njhill), merged by vLLM maintainers
ARTIFACT: PR #17546 "[Perf] API-server scaleout with many-to-many server-engine comms" + `--api-server-count`
URL: https://github.com/vllm-project/vllm/pull/17546 ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/serving/data_parallel_deployment.md ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/arch_overview.md
STATUS: merged (shipped; documented in v0.29.0)
EVIDENCE: READ BODY
QUOTE: "A new --api-server-count arg to vllm serve can be used to specify an arbitrary number of API servers to run. When used in conjunction with --data-parallel-size there's all-to-all zmq-based communication between API servers and data parallel engines."
QUOTE-2: "When deploying large DP sizes using this method, the API server process can become a bottleneck. In this case, the orthogonal `--api-server-count` command line option can be used to scale this out (for example `--api-server-count=4`)."
S3B: SOLVED (merged #17546 ; flag `--api-server-count` present in vLLM 0.29.0 `vllm/entrypoints/launchers/cli_args.py`)

### A1.3 | CLOSED
QUESTION: How expensive is an "optional" FastAPI middleware in the vLLM HTTP layer, measured?
WHO: Yazan-Sharaya (author), merged by vLLM maintainers
ARTIFACT: PR #19946 "[Perf][Frontend]: eliminate api_key and x_request_id headers middleware overhead"
URL: https://github.com/vllm-project/vllm/pull/19946
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "The current implementation for the middleware that handles authentication (--api-key) and X-Request-Id headers (--enable-request-id-headers) uses a high level abstraction provided by FastAPI that allows middleware to be written as functions with access to the Request object. This causes an unacceptable performance hit when the middleware is installed."
QUOTE-2: "As can be seen, ~10% decrease in throughput is huge for the little computation that is actually done when enabling both middleware."
NUMBERS (verbatim tables in the PR body): before — without middleware 94.8 QPS, `--api-key` 90.6 QPS (-4.4%), `--api-key` + `--enable-request-id-headers` 85.5 QPS (-9.8%); after pure-ASGI rewrite — 94.0 / 93.8 (-0.2%) / 93.3 (-0.7%). The PR also removes the documentation warning that `--enable-request-id-headers` has a performance penalty at high QPS.
S3B: SOLVED (merged #19946)

### A1.4 | CLOSED
QUESTION: What does adding HTTP-level Prometheus instrumentation to the vLLM API server cost, and how was that measured?
WHO: daniel-salib (author), merged by vLLM maintainers
ARTIFACT: PR #15657 "implement prometheus fast-api-instrumentor for http service metrics"
URL: https://github.com/vllm-project/vllm/pull/15657
STATUS: merged (documented in `docs/design/metrics.md` § "Prometheus Client Library")
EVIDENCE: READ BODY
QUOTE: "track http level metrics such as requests counts by route/status code, request duration and request size using prometheus fast-api-instrumentor"
CAVEAT (from the same retrieved body): the benchmark block that is present in the retrieved page is the BEFORE run and is saturated — "Mean TTFT (ms): 391076.36" with "Request throughput (req/s): 50.29" over 32741 requests. An AFTER block was not present in the retrieved body, so this row does **not** support a numeric overhead delta; it is evidence only that HTTP-level metrics were added and benchmarked with `vllm bench serve`.
S3B: SOLVED (merged #15657 ; the instrumentator is wired in vLLM 0.29.0 under `vllm/entrypoints/serve/instrumentator/`)

### A1.5 | CLOSED
QUESTION: Has a non-Python front end actually shipped in vLLM, and what does the project say about its maturity?
WHO: Nick Hill (njhill) + BugenZhao, merged by vLLM maintainers
ARTIFACT: PR #40848 "[Frontend][RFC] Rust front-end integration" + `rust/` crate workspace + `VLLM_USE_RUST_FRONTEND=1`
URL: https://github.com/vllm-project/vllm/pull/40848 ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/rust/README.md
STATUS: merged (shipped as experimental/preview in v0.29.0)
EVIDENCE: READ BODY
QUOTE: "You can try it out by building this branch"
QUOTE-2: "It should still be considered experimental, and is not feature-complete. We are working to add more functionality from the python front-end."
S3B: SOLVED (merged #40848 ; Rust frontend present in vLLM 0.29.0, `VLLM_USE_RUST_FRONTEND` / `VLLM_RUST_FRONTEND_PATH` in `vllm/envs.py`)

### A1.6 | CLOSED
QUESTION: Does SGLang expose per-process scheduler CPU/idle utilisation — the measurement vLLM has no Prometheus metric for?
WHO: Jialin (author), merged by SGLang maintainers
ARTIFACT: PR #37461 "[Metrics] Add rolling scheduler utilization counters" → `sglang:scheduler_idle_seconds_total`, `sglang:scheduler_process_cpu_seconds_total`
URL: https://github.com/sgl-project/sglang/pull/37461
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "Existing forward-execution metrics cover GPU work but do not directly describe scheduler utilization."
S3B: SOLVED (merged #37461 ; both counters verified present in SGLang main at `python/sglang/srt/observability/metrics_collector.py:922` and `:929`)

### A1.7 | CLOSED
QUESTION: Where does token detokenization/stopping live in a Rust-router serving stack, and why was it moved there?
WHO: Nick Hill (njhill), merged by Hugging Face TGI maintainers
ARTIFACT: TGI PR #138 "proposal: Move token decoding and stopping evaluation to router"
URL: https://github.com/huggingface/text-generation-inference/pull/138
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "Centralizes this logic that's on the critical inference loop path and does it in rust instead of python"
QUOTE-2: "I've included implementation of IncrementalDecoder logic that addresses the various problems associated with incremental decoding when streaming for the various kinds of tokenizers."
S3B: SOLVED (merged #138)

### A1.8 | CLOSED
QUESTION: Do vLLM's own benchmark numbers include detokenization, and do other frameworks' numbers?
WHO: JArnoldAMD (author) + vLLM maintainer review (hmellor)
ARTIFACT: PR #11697 "[Benchmarks] Make detokenization optional in benchmark scripts"
URL: https://github.com/vllm-project/vllm/pull/11697
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "The benchmark scripts currently include detokenization as part of the performance measurement. In most cases the detokenization time is minimal, but for some models and script parameters it can be significant. Since detokenization isn't always needed (e.g. it may be handled by a separate server), it is helpful to have the option of whether or not to include detokenization in the measurement."
QUOTE-2: "Since benchmark scripts for some other frameworks (notably TensorRT-LLM) do not include detokenization time, I have opted to skip detokenization by default. This default could be changed if desired."
QUOTE-3 (maintainer, verbatim): "This is a reasonable idea. I do think we should maintain the current behavior by default, which is to include detokenization in these benchmarks"
NOTE: The merged state keeps detokenization IN by default, but the switch exists and is documented in `vllm/benchmarks/latency.py` and `vllm/benchmarks/throughput.py`: "Do not detokenize the response (i.e. do not include detokenization time in the measurement)". Cross-framework comparisons that mix the two conventions are not like-for-like.
S3B: SOLVED (merged #11697 ; `--disable-detokenize` present in vLLM 0.29.0 benchmark scripts)

### A1.9 | CLOSED
QUESTION: Are the Prometheus latency metrics from SGLang's Anthropic-compatible entrypoint trustworthy?
WHO: SGLang maintainers / issue reporter
ARTIFACT: Issue #22249 "[Bug] Anthropic API entrypoint produces negative TTFT and e2e latency in Prometheus metrics" (fixed by PR #22247)
URL: https://github.com/sgl-project/sglang/issues/22249
STATUS: fixed / closed
EVIDENCE: READ BODY
QUOTE: "The Anthropic API entrypoint (`python/sglang/srt/entrypoints/anthropic/serving.py`) uses `time.time()` (wall clock, epoch-based, approx 1.7e9) for `received_time`, while downstream metrics computation in `req_time_stats.py` calculates deltas against `time.perf_counter()` (monotonic). This clock mismatch produces large negative values in Prometheus histograms:"
S3B: SOLVED (fixed by #22247, referenced in the issue body; issue stateReason COMPLETED)

### A1.10 | CLOSED
QUESTION: What is the documented metrics cost model and the documented coverage holes in the multiprocess/API-server-scaled configuration?
WHO: vLLM maintainers (design doc)
ARTIFACT: `docs/design/metrics.md` (v0.29.0) — "Multi-process Mode", "Built in Python/Process Metrics", "Adding More Metrics"
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/metrics.md
STATUS: shipped in v0.29.0
EVIDENCE: READ BODY
QUOTE: "They can have a meaningful performance impact when enabled. And metrics are usually of very limited use unless they can be enabled by default and in production."
QUOTE-2: "Therefore, these metrics are unavailable when `--api-server-count > 1`."
QUOTE-3: "More recently, metrics are collected in the API server process and multiprocess mode is only used when `--api-server-count > 1`."
NOTE: The unavailable set is `process_cpu_seconds_total`, `process_resident_memory_bytes`, `python_gc_*`, `process_open_fds`, etc. — i.e. exactly the CPU-cost signals needed to see front-end overhead disappear as soon as you scale the front end out.
S3B: SOLVED (shipped in v0.29.0)

### A1.11 | CLOSED
QUESTION: Does vLLM ship a non-HTTP (gRPC) serving front end, and what is the stated reason for the keepalive tuning?
WHO: vLLM maintainers
ARTIFACT: `vllm serve --grpc` / `vllm/entrypoints/grpc_server.py` (requires `vllm[grpc]`, backed by `smg-grpc-servicer`)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/entrypoints/grpc_server.py
STATUS: shipped in v0.29.0
EVIDENCE: READ BODY
QUOTE: "Tolerate client keepalive pings every 10s (default 300s is too strict for non-streaming requests where no DATA frames flow during generation)"
NOTE: The same file sets `grpc.max_send_message_length` / `grpc.max_receive_message_length` to `-1` and enables server reflection; `--grpc` is registered in `vllm/entrypoints/launchers/cli_args.py:408`.
S3B: SOLVED (shipped in v0.29.0)

### A1.12 | CLOSED
QUESTION: Does SGLang gate its Prometheus endpoint behind a flag, and is the metric set documented per-version?
WHO: SGLang maintainers
ARTIFACT: `--enable-metrics` + `docs/docs/references/production_metrics.mdx` + `docs/docs/advanced_features/observability.mdx`
URL: https://raw.githubusercontent.com/sgl-project/sglang/main/docs/docs/references/production_metrics.mdx ; https://raw.githubusercontent.com/sgl-project/sglang/main/docs/docs/advanced_features/observability.mdx
STATUS: shipped
EVIDENCE: READ BODY
QUOTE: "SGLang exposes the following metrics via Prometheus. You can enable it by adding `--enable-metrics` when you launch the server."
QUOTE-2: "Enable --enable-metrics-for-all-schedulers when you want schedulers on all TP ranks (not just TP 0) to record request metrics separately. This is especially useful when dp_attention is enabled, as otherwise all metrics appear to come from TP 0."
NOTE: Unlike vLLM, SGLang's metric namespace includes scheduler-process signals: `sglang:scheduler_idle_seconds_total`, `sglang:scheduler_process_cpu_seconds_total`, `sglang:scheduler_stage_seconds_total`, `sglang:process_cpu_seconds_total`, `sglang:forward_execution_seconds_total`, `sglang:func_latency_seconds`, `sglang:http_requests_total`, `sglang:http_requests_active`. All verified by grep of SGLang main `python/sglang/`.
S3B: SOLVED (shipped)

---

## B — OPEN (capped at 3)

### B1.1 | OPEN
WHO IS STILL ASKING: RFC author + NVIDIA Dynamo, autoscaler/router authors, in vllm-project/vllm issue #38760 (labels: RFC; state OPEN)
URL: https://github.com/vllm-project/vllm/issues/38760
ASKING QUOTE: "Today, vLLM exposes Prometheus gauge/histogram metrics that are **scraped asynchronously** by an external collector. This has fundamental limitations for per-iteration telemetry: 1. **Lossy**: Prometheus scraping is pull-based at a configurable interval. With iteration times of 10-100ms, the scraper can miss 90%+ of iterations."
SECOND QUOTE (same body): "measuring wall time from the scheduler is inherently imprecise because the scheduler cannot observe the GPU forward pass boundary"
THIRD QUOTE (same body, on why the existing timestamps cannot substitute): "In async mode: `schedule(N+1)` runs concurrently with GPU batch N, so scheduler-side timestamps include overlap from adjacent batches"
WHAT IS MISSING: No per-iteration engine-level metric exists. vLLM 0.29.0 has no Prometheus metric for scheduler time, sampling time, detokenization time, or CPU time per engine step, and a grep of every `"vllm:<name>"` string in `vllm/` (0.29.0) returns no such family. The only scheduler-timing signal in the tree is the OpenTelemetry span attribute `gen_ai.latency.time_in_scheduler` (`vllm/tracing/utils.py:38`), which is unavailable unless tracing is on. The RFC's own scoped subset — accurate GPU forward-pass `wall_time`, per-iteration identity/history, queue and KV-depth fields, ZMQ transport — is explicitly declared out of scope of the one draft PR that exists.
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #53550 "[Metrics] Add phase-aware engine iteration histograms" — draft, and open PR #52061 "[Feature] Add native forward-pass metrics emission"; both found by an independent search of the vLLM open-PR list, not from the RFC's own links)

### B1.2 | OPEN
WHO IS STILL ASKING: yinjuncheng et al., vllm-project/vllm issue #47860 (labels: feature request, multi-modality) and sgl-project/sglang issue/PR track
URL: https://github.com/vllm-project/vllm/issues/47860
ASKING QUOTE: "Today this time is invisible. Per the [metrics docs](https://docs.vllm.ai/en/stable/design/metrics/), `arrival_time` starts when tokenization begins, so all of the above is absorbed into TTFT. Operators serving vision/audio/video models cannot answer basic questions: - Is high TTFT caused by slow media downloads, slow CPU decode, or the engine?"
SECOND QUOTE (same body): "However, there is **no metric** for: - media download latency - media decode (`load_bytes`) latency - total `resolve_items` time per request - total preprocessing (render + tokenize) time"
WHAT IS MISSING: The pre-engine slice of the request lifecycle — media download, media decode, item resolution, chat-template render, tokenize — has no Prometheus metric in vLLM 0.29.0. `arrival_time` is defined in `docs/design/metrics.md` as starting "when tokenization begins", so everything before that is folded into TTFT and cannot be attributed. `--limit-mm-per-prompt` bounds item counts but exposes no timing.
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #50875 "[Feature] Add Prometheus metrics for multimodal preprocessing pipeline" (same author, supersedes the closed-unmerged #50824), open PR #49682 "[Observability] Add multimodal media load metrics", open PR #36685 "Compute MM Pre-processing Timing Metrics in a Non-Blocking Way for Prod", and sgl-project/sglang open PR #38772 "[Feature] Add Prometheus metrics for multimodal preprocessing latency" — all found by independent open-PR-list searches)

### B1.3 | OPEN
WHO IS STILL ASKING: Kangyan-Zhou (SGLang), PR #29527 motivation, sgl-project/sglang
URL: https://github.com/sgl-project/sglang/pull/29527
ASKING QUOTE: "Part of that gap is delay that accrues before the engine starts its TTFT clock : the engine measures TTFT as received_time -> first token , but received_time is stamped inside the TokenizerManager process. If that process's asyncio event loop is starved (synchronous work, GIL contention, huge-prompt HF encoding), a request sits unstamped — invisible to engine-side TTFT but very visible to the client and the router."
WHAT IS MISSING: Neither engine's TTFT clock starts until after the front-end/tokenizer process has already consumed CPU time. The proposed metric (`sglang:event_loop_lag_seconds`) is not present in SGLang main (verified by grep of `python/sglang/`), and vLLM has no equivalent tokenizer/event-loop-lag metric at all — so on both stacks the client-visible pre-`received_time` delay is unmeasured.
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #29527 itself, plus open PR #35069 "feat(sgl-router): decompose the pre-first-token path into phase histograms" — both found by an independent search of the SGLang open-PR list)

---

## C — ABANDONED / REMOVED / MEASURED-NEGATIVE

### C1.1 | ABANDONED (scheduler-phase timing instrumentation)
WHO: Jialin (author), sgl-project/sglang
ARTIFACT: PR #37455 "[Metrics] Export scheduler stage wall time" — closed unmerged, author-closed 2026-09-01
URL: https://github.com/sgl-project/sglang/pull/37455
STATED REASON: "This PR is stacked on #37461 and will remain closed until that prerequisite lands."
SECOND QUOTE (the gap it was written to close): "Scheduler process CPU time and GPU forward occupancy show whether an engine is busy, but not which scheduler phase owns gaps between forwards."
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: the named prerequisite #37461 merged (independently confirmed), and `sglang:scheduler_stage_seconds_total{category=...}` — the metric this PR proposed — is present in SGLang main at `python/sglang/srt/observability/metrics_collector.py:936`. The blocker no longer exists; the stage breakdown shipped anyway.
S3B: SOLVED (shipped — `sglang:scheduler_stage_seconds_total` verified in SGLang main `metrics_collector.py:936`; independent search of SGLang open PRs for "scheduler stage" returns no competing open work)

### C1.2 | ABANDONED (removed request-logging flag)
WHO: hmellor (author), merged by vLLM maintainers — the removal itself is the shipped outcome
ARTIFACT: PR #21739 "Deprecate `--disable-log-requests` and replace with `--enable-log-requests`"
URL: https://github.com/vllm-project/vllm/pull/21739
STATED REASON: "This PR flips the default behaviour so that logging of requests is disabled by default for security/privacy reasons. It then replaces the --disable-log-requests with --enable-log-requests for a better UX for opting in to logging requests."
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The stated reason is a security/privacy default, not a performance or technical constraint, so it does not expire — but the *performance* framing does: `--disable-log-requests` no longer exists as a tuning knob in vLLM 0.29.0 (0 occurrences of the string in `vllm/`), and request logging is now OFF by default (`enable_log_requests: bool = False` in `vllm/engine/arg_utils.py:2968`). Any benchmark recipe that still says "measured with `--disable-log-requests`" is now a no-op on 0.28/0.29.
S3B: SOLVED (merged #21739 ; `--enable-log-requests` present, `--disable-log-requests` absent in vLLM 0.29.0 source)

### C1.3 | ABANDONED (removed front-end multiprocessing toggle)
WHO: sfeng33 (author), merged by vLLM maintainers as a V0-deprecation cleanup
ARTIFACT: PR #37612 "[V0 Deprecation] Deprecate --disable-frontend-multiprocessing"
URL: https://github.com/vllm-project/vllm/pull/37612
STATED REASON: "Remove the --disable-frontend-multiprocessing CLI flag from BaseFrontendArgs , API server, and benchmarks"
SECOND QUOTE (same body): "Clean up test parametrizations that varied over this flag, reducing redundant test variants"
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The removal reason is "this was a V0 flag", and V0 is gone; the flag has 0 occurrences in vLLM 0.29.0. The *behavioural* question it used to answer — "run the HTTP front end in-process with the engine or not" — is now answered only by `--api-server-count` (scale out) and, experimentally, `VLLM_USE_RUST_FRONTEND` (replace). There is no supported "no front-end process" configuration to A/B against.
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+%22frontend+multiprocessing%22` ; `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22disable-frontend-multiprocessing%22` ; plus a source-level grep of vLLM 0.29.0 showing 0 occurrences)

### C1.4 | ABANDONED (removed Prometheus metric, never implemented)
WHO: vLLM maintainers (design doc records the decision)
ARTIFACT: `docs/design/metrics.md` § "Unimplemented - `vllm:tokens_total`" (v0.29.0)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/metrics.md
STATED REASON: "Added by <https://github.com/vllm-project/vllm/pull/4464>, but apparently never implemented. This can just be removed."
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: No named external constraint; this is a dead-code cleanup, so the field is omitted.
S3B: NO-ACTIVE-WORK-FOUND (queries: independent grep of every `"vllm:<name>"` literal in the vLLM 0.29.0 tree returns no `vllm:tokens_total`; no open PR in the vLLM open-PR list matches "tokens_total")

### C1.5 | ABANDONED (duplicate queue-time metric left unreconciled)
WHO: vLLM maintainers (design doc records the decision)
ARTIFACT: `docs/design/metrics.md` § "Duplicated - Queue Time" (v0.29.0)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/metrics.md
STATED REASON: "This seems duplicative, and one of them should be removed. The latter is used by the Grafana dashboard, so we should deprecate or remove the former."
SECOND QUOTE (same section, the recorded plan): "Two weeks later, <https://github.com/vllm-project/vllm/pull/4464> added `vllm:request_queue_time_seconds` leaving us with:"
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The blocker is downstream dashboard compatibility ("used by the Grafana dashboard"), which is a live constraint, so it has not expired. Confirmed unresolved in 0.29.0.
S3B: NO-ACTIVE-WORK-FOUND (queries: vLLM open-PR list searched for "queue time metric" and "time_in_queue_requests"; independent grep of the 0.29.0 tree still shows both `vllm:request_queue_time_seconds` and the legacy queue-time path in `vllm/v1/metrics/loggers.py`)

### C1.6 | ABANDONED (removed preemption/swap metrics and their CLI flag)
WHO: vLLM maintainers (design doc records the decision)
ARTIFACT: `docs/design/metrics.md` § "KV Cache Offloading" (v0.29.0) — `vllm:num_requests_swapped`, `vllm:cpu_cache_usage_perc`, `--swap-space`
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/metrics.md
STATED REASON: "In this mode, when a request was preempted (e.g. to make room in KV cache to complete other requests), kv cache blocks were swapped out to CPU memory. The `--swap-space` flag has been removed as this feature is no longer used in V1."
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The reason explicitly names the architectural constraint — "no longer used in V1" — i.e. it is tied to the V1 engine, which is the only engine in 0.29.0. The observable consequence is that `vllm:num_requests_swapped` and `vllm:cpu_cache_usage_perc` are no longer produced, so a dashboard built on them shows nothing; `--swap-space` has 0 occurrences in the 0.29.0 source.
S3B: SOLVED (removal shipped; verified `--swap-space` absent and the two metric names absent from the 0.29.0 `vllm:` metric literal set)

### C1.7 | ABANDONED (Rust router-side tokenization metrics deleted for cost)
WHO: CatherineSue (author), merged by SGLang maintainers — the deletion is the shipped outcome
ARTIFACT: sgl-project/sglang PR #9474 "[router] remove all tokenizer metrics for performance"
URL: https://github.com/sgl-project/sglang/pull/9474
STATED REASON: "Tokenizer metrics add severe overhead"
SECOND QUOTE (same body, the other half of the fix): "use reference instead of cloning for token ids to reduce memory consumption"
NOTE: The PR's own summary records what was deleted: "All performance and usage metrics related to the tokenizer have been eliminated across various tokenizer modules (factory.rs, huggingface.rs, stop.rs, stream.rs)". This is a maintainer-accepted statement that the observability itself was the cost.
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The reason is a CPU/memory measurement, not an external constraint. It remains valid for the Rust router. Note that this is the *router/SMG* tokenizer, not the Python engine-side tokenizer; the metric coverage was never restored in that process.
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aopen+tokenizer+metrics+router` — 15 open rows, none re-adding router tokenizer metrics; the nearest is open PR #29527, which adds an event-loop-lag metric rather than tokenizer counters)

### C1.8 | ABANDONED (measured negative result: Ray Serve LLM streaming path, attribution to the HTTP layer)
WHO: Ray Serve / Ray LLM users and maintainers, ray-project/ray issues #52746 and #59681
ARTIFACT: Issue #52746 "[ray.serve.llm] serve.llm with streaming has overhead compared to vllm-v0 for a single replica when concurrency > 32" ; Issue #59681 "Additional Overhead in Ray Serve LLM than Bare vLLM images"
URL: https://github.com/ray-project/ray/issues/52746 ; https://github.com/ray-project/ray/issues/59681
STATED REASON: "**Isolating the overhead: it's the HTTP layer, not Ray Core**"
SECOND QUOTE (same comment, the measured table in prose): "The actor wrapper introduces effectively zero overhead. The entire ~100ms gap lives in the HTTP/routing path — `proxy.py` → `router.py` → `replica.py`."
THIRD QUOTE (same comment, the environment): "Tested with vLLM (Qwen3-0.6B) on a single replica, batch size 32, RTX 4090."
FOURTH QUOTE (#59681, the second measurement): "There is a ~200token/s total throughput decrease in Ray Serve LLM.(~30 tok/s difference in output throughput). 2. Continuing on the above, ~20ms increase in mean TTFT and ~50ms in P90 TTFT."
FIFTH QUOTE (#59681, maintainer response): "This overhead problem has completely been addressed using Direct streaming."
STATED CAVEATS (verbatim, #59681): "Caveats: small model on a consumer GPU makes fixed overheads proportionally more visible than on large models; single node, one replica."
ADDITIONAL CAVEAT (#52746, the baseline command in the repro): "VLLM_USE_V1=\"0\" vllm serve unsloth/llama-3-8b-Instruct --tensor-parallel-size 1  --disable-log-requests" — the vLLM baseline was run on the **V0** engine with request logging disabled, so the reported gap is not a v1-vs-Serve-LLM comparison and the baseline excludes request-logging cost.
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: Partly. The dominant fixed cost in #59681 was the response `Batcher` waiting out `MODEL_RESPONSE_BATCH_TIMEOUT_MS` (default 50 ms); the reporter measured that setting it to 0 drops TTFT p50 64.8 → 26.3 ms, and #64534 was opened to flush the first result immediately. The maintainer's remedy ("direct streaming") changes the recommended configuration, so the *default* numbers in these issues may no longer reproduce. The residual high-concurrency tail (TTFT 71 → 330 ms at c=32, 114 → 758 ms at c=64, throughput 5518 → 3634 tok/s) was explicitly flagged by the reporter as "architectural ... rather than a single fixable defect" and remained open at the time of the thread.
S3B: SOLVED for the fixed-cost half (reporter-opened fix #64534 plus the maintainer-recommended direct-streaming configuration) / NO-ACTIVE-WORK-FOUND for the concurrency-scaling half (queries: `https://github.com/ray-project/ray/issues?q=is%3Aissue+is%3Aopen+serve+llm+streaming+overhead`; `https://github.com/ray-project/ray/issues?q=is%3Aissue+is%3Aopen+%22direct+streaming%22`)

### C1.9 | ABANDONED (measured negative result: Triton Inference Server + vLLM backend, single node)
WHO: Triton user (reporter), closed by the reporter
ARTIFACT: triton-inference-server/server issue #8515 "vLLM backend multi-gpu one instance performance"
URL: https://github.com/triton-inference-server/server/issues/8515
STATED REASON: "After extensive testing, I've found that the problem may arise from excessive CPU usage by vllm when there are a large number of requests. Therefore, I'm closing this issue."
SECOND QUOTE (same body, the measurement): "when running dual-GPU single-instance on a single node, the triton_python_backend_shm_region threads consume extremely high CPU resources."
THIRD QUOTE (same body, the before/after): "When I launch one instance on a single GPU of a server, I can see from Triton metrics that the model achieves a maximum FPS of 210. However, when I launch one instance on each of the two GPUs in the same server, the FPS drops to 80-100 per GPU."
NOTE: The `top` output pasted in the issue shows two `triton_python_b` processes at 7812% and 6013% CPU. The reporter's own conclusion is that the serving-layer wrapper (Python backend + shm region) plus vLLM's CPU use, not the GPU, is the limiter — a negative result for using Triton as the serving layer for vLLM on a shared node.
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: The reason names CPU exhaustion under a Python-backend shm path; that is an architectural property of the Triton Python backend, so it does not obviously expire. The specific numbers are two-GPU, so they do not transfer to the target rig.
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/triton-inference-server/server/issues?q=is%3Aissue+is%3Aopen+vllm+backend+performance` ; `https://github.com/triton-inference-server/server/issues?q=is%3Aissue+is%3Aopen+shm_region+CPU` ; a web search for an open Triton vLLM-backend CPU-overhead item returned only unrelated feature requests)

### C1.10 | ABANDONED (whole serving stack retired: TGI is in maintenance mode)
WHO: Hugging Face (TGI maintainers), README notice
ARTIFACT: `huggingface/text-generation-inference` README top-of-file caution (the Rust-router + Python-sharded-server serving stack, i.e. the third mainstream serving layer next to vLLM and SGLang)
URL: https://raw.githubusercontent.com/huggingface/text-generation-inference/main/README.md
STATED REASON: "text-generation-inference is now in maintenance mode. Going forward, we will accept pull requests for minor bug fixes, documentation improvements and lightweight maintenance tasks."
SECOND QUOTE (same notice, the redirect): "TGI has initiated the movement for optimized inference engines to rely on a `transformers` model architectures. This approach is now adopted by downstream inference engines, which we contribute to and recommend using going forward: [vllm](https://github.com/vllm-project/vllm), [SGLang](https://github.com/sgl-project/sglang), as well as local engines with inter-compatibility such as llama.cpp or MLX."
EVIDENCE: READ BODY
REASON-MAY-HAVE-EXPIRED: No named constraint; this is an organisational decision, so the field is omitted. Practical consequence for this gap map: TGI's router-side detokenization design (row A1.7) is now a frozen reference implementation rather than an actively maintained serving layer to benchmark against.
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/huggingface/text-generation-inference/pulls?q=is%3Apr+is%3Aopen+router+metrics` ; a web search for TGI router/metrics work returns only the maintenance-mode notice and mirrored forks)

---

## D — HARDWARE-RULED-OUT

### D1.1 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: Nick Hill (njhill), vLLM maintainer; RFC #40846 "[RFC]: Rust front-end"
ARTIFACT: vllm-project/vllm issue #40846, § "Performance Benchmarks" (Benchmark 1 and Benchmark 2)
URL: https://github.com/vllm-project/vllm/issues/40846
HARDWARE QUOTE: "All benchmarks: Qwen3-0.6B, DP=4 on 4x GB200 GPUs, vllm 0.19.0, request_rate=inf. Results shown at concurrency=1024. asc = `--api-server-count`. These is obviously not a realistic real-world configuration  but chosen to emphasize the ceiling imposed by the current python implementation."
SECOND QUOTE (the front-end-bottleneck claim this row is about, verbatim): "With prefix cache fully warm, the frontend becomes the bottleneck. A single Rust frontend matches or exceeds 32 Python api-server processes. Default Python saturates at only 19% of Rust throughput with 10x worse P50 TTFT."
THIRD QUOTE (the qualitative claim, verbatim): "As GPU latency continues to fall, request concurrency grows, and large-scale deployments become the norm, the CPU parts of the system have again become a bottleneck in many situations. This is often seen in the front-end process where the asyncio event loop can’t keep up."
NOTE: The *qualitative* claim (a Python asyncio front end can saturate) is transferable; the *numbers* are not — they require 4 GB200 GPUs and 1024 concurrent requests to manifest, and the author states the configuration was chosen to exaggerate the effect.
EVIDENCE: READ BODY

### D2.1 | HARDWARE-RULED-OUT   (D.1 >1 GPU | D.3 multi-node/NVLink/IB)
WHO: Yuhang Yao, Han Jin, Alay Dilipbhai Shah, Shanshan Han, Zijian Hu, Yide Ran, Dimitris Stripelis, Zhaozhuo Xu, Salman Avestimehr, Chaoyang He — ScaleLLM
ARTIFACT: arXiv:2408.00008, "ScaleLLM: A Resource-Frugal LLM Serving Framework by Optimizing End-to-End Efficiency" (v2)
URL: https://arxiv.org/abs/2408.00008 ; https://arxiv.org/html/2408.00008v2
HARDWARE QUOTE: "We employ 8 NVIDIA DGX H100 GPUs, connected via 18 NVLink links, each providing a bandwidth of 26.562 GB/s."
SECOND HARDWARE QUOTE (the latency-breakdown experiment specifically): "We conducted a comprehensive latency breakdown evaluation for Mistral 8x7B running on two H100 GPUs, examining various levels of concurrent requests."
THIRD QUOTE (the finding this row is about, verbatim): "However, in commercial LLM applications, end-to-end latency, introduced from functionalities of the gateway, becomes the most significant bottleneck."
FOURTH QUOTE (verbatim, the crossover point): "Benchmark results in Figure 2 indicate that with 4 concurrent requests, engine latency is the primary bottleneck. However, at 256 concurrent requests, the gateway latency becomes the predominant bottleneck."
FIFTH QUOTE (verbatim, root cause): "For CPU bound jobs, the FastAPI gateway in the baseline implementation is constrained by the Global Interpreter Lock (GIL), which limits its ability to utilize multiple CPU cores effectively."
NOTE: The paper's Gateway is replaced by Axum (Rust) + gRPC with a Tonic connection pool, and it reports "with 64 concurrent requests, ScaleLLM achieves a 4.3x speed up over vLLM". The methodology's stated caveat is that the gateway-latency crossover is demonstrated on MoE models (Mistral 8x7B) at 64–256 concurrency on H100s.
EVIDENCE: READ BODY

### D2.2 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: Euijun Chung, Yuxiao Jia, Aaron Jezghani, Hyesoon Kim — "Characterizing CPU-Induced Slowdowns in Multi-GPU LLM Inference"
ARTIFACT: arXiv:2603.22774 (v3)
URL: https://arxiv.org/abs/2603.22774
HARDWARE QUOTE: "Large-scale machine learning workloads increasingly rely on multi-GPU systems, yet their performance is often limited by an overlooked component: the CPU."
SECOND QUOTE (the front-end cost claim, verbatim): "we find that multi-GPU performance often degrades not because GPUs are saturated, but because CPUs fail to keep them busy. Under limited CPU allocations, systems exhibit symptoms such as delayed kernel launch, stalled communication, and increased tokenization latency, leading to severe GPU underutilization even when ample GPU resources are available."
THIRD QUOTE (verbatim, the measured headroom): "Under moderate serving load, we observe that CPU-starved configurations frequently time out, while providing adequate CPU resources restores responsiveness and reduces time-to-first-token (TTFT) latency by 1.47-7.11x across configurations, all without requiring additional GPUs."
NOTE: Directly on-topic for this subtopic (tokenization latency + CPU starvation in the serving stack, persisting "even in serving stacks that employ process-level separation"), but the whole study is framed around multi-GPU nodes, so it is ruled out for a single-GPU rig.
EVIDENCE: READ BODY

---

## SEARCH LOG (queries actually issued)

GitHub issue/PR HTML pages fetched directly with `python3 fetch.py gh <url>`:
- https://github.com/vllm-project/vllm/issues/3616 (Even Better Observability, closed stale)
- https://github.com/vllm-project/vllm/issues/38760 , /40846 , /47860 , /55634 , /36189 , /28218
- https://github.com/vllm-project/vllm/pull/17546 , /19946 , /15657 , /40848 , /37612 , /21739 , /11697 , /51329 , /50824 , /50875 , /41765 , /55812 , /55837 , /32334
- https://github.com/sgl-project/sglang/pull/9474 , /37455 , /37461 , /36047 , /37605 , /39037 , /29527 , /14752
- https://github.com/sgl-project/sglang/issues/22249
- https://github.com/huggingface/text-generation-inference/pull/138
- https://github.com/triton-inference-server/server/issues/8515
- https://github.com/ray-project/ray/issues/52746 , /59681

Repo-scoped HTML list queries (these work when the `/search` endpoint is secondary-rate-limited):
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+frontend
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+%22api+server%22
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+metrics
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+HTTP
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+Revert
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+instrumentation
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+tokenizer+metrics
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+%22tokenization%22
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+%22chat+template%22
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+%22forward+pass%22+metrics
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+%22per-iteration%22
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+engine+step+counter
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+multimodal+preprocessing+metrics
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aopen+tokenizer+metrics+router
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+metrics
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+router
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+overhead
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aopen+multimodal+preprocessing+metrics
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aopen+tokenizer+metrics+router
- https://github.com/sgl-project/sglang/pulls?q=is%3Apr+is%3Aopen+scheduler+stage
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aclosed+label%3A%22not+planned%22+metrics
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aclosed+label%3Awontfix
- https://github.com/vllm-project/vllm/pulls?q=is%3Apr+is%3Aclosed+is%3Aunmerged+metrics+overhead
- https://github.com/triton-inference-server/server/issues?q=is%3Aissue+is%3Aopen+vllm+backend+performance
- https://github.com/triton-inference-server/server/issues?q=is%3Aissue+is%3Aopen+shm_region+CPU
- https://github.com/ray-project/ray/issues?q=is%3Aissue+is%3Aopen+serve+llm+streaming+overhead

GitHub `/search` endpoint attempts (blocked — secondary rate limit, "You have exceeded a secondary rate limit"):
- https://github.com/search?q=repo%3Avllm-project%2Fvllm+rust+frontend&type=issues
- https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22disable-frontend-multiprocessing%22
- https://github.com/search?q=repo%3Avllm-project%2Fvllm+is%3Aunmerged+is%3Apr+metrics
- https://github.com/search?q=repo%3Avllm-project%2Fvllm+is%3Aunmerged+is%3Apr+frontend

`web_search` strings issued:
- vllm github issue metrics scheduler time not measured instrumentation request
- vllm pull request remove --disable-frontend-multiprocessing
- vllm --disable-log-requests removed enable-log-requests default
- vllm github issue api server overhead bottleneck HTTP frontend
- text-generation-inference TGI router benchmark overhead sharded server github
- vllm Ray Serve vs vllm serve performance comparison overhead
- Triton Inference Server vLLM backend performance overhead issue
- sglang --enable-metrics prometheus overhead
- vllm closed unmerged pull request frontend rewrite rejected
- vllm RFC openai server rewrite closed not planned entrypoints refactor
- sglang closed unmerged pull request metrics overhead router rewrite
- text-generation-inference router rust rewrite closed unmerged pull request
- arxiv paper measuring LLM serving framework overhead HTTP frontend vLLM TGI comparison benchmark
- arxiv "serving framework" overhead tokenization detokenization LLM inference characterization 2025
- arxiv benchmark study vLLM TensorRT-LLM SGLang overhead request lifecycle latency breakdown
- arxiv 2025 "frontend" OR "gateway" CPU overhead LLM serving system characterization measurement study tokenization detokenization latency breakdown
- arxiv LLM inference server CPU overhead breakdown request lifecycle profiling FastAPI uvicorn
- "serving overhead" arxiv LLM inference framework measurement study 2026
- triton-inference-server github open issue vllm backend CPU overhead python backend shm 2026
- vllm github issue open request detokenization time metric not measured
- vllm issue ask metric "sampling time" OR "detokenization time" instrument

Local source/doc greps (vLLM 0.29.0 tarball at `/tmp/vllm-0.29.0`, SGLang main at `/tmp/sglang-main`):
- full enumeration of `"vllm:<name>"` metric literals; `"sglang:<name>"` metric literals
- flag presence/absence checks for `--disable-frontend-multiprocessing`, `--disable-log-requests`, `--swap-space`, `--api-server-count`, `--enable-log-requests`, `--disable-log-stats`, `--enable-mfu-metrics`, `--max-num-queued-reqs`, `--max-num-queued-tokens`, `--grpc`
- `vllm/entrypoints/launchers/`, `vllm/entrypoints/serve/`, `vllm/v1/metrics/`, `vllm/renderers/`, `docs/design/metrics.md`, `docs/configuration/optimization.md`, `docs/serving/`

arXiv read-backs (`python3 fetch.py arxiv <id>`, title confirmed for each):
- 2408.00008 → "ScaleLLM: A Resource-Frugal LLM Serving Framework by Optimizing End-to-End Efficiency" ✔
- 2510.02758 → "TokenFlow: Responsive LLM Text Streaming Serving under Request Burst via Preemptive Scheduling" ✔ (read back, not cited as a row: its subject is preemptive scheduling + KV transfer, which belongs to the sibling KV-cache map)
- 2602.00269 → "VoxServe: Streaming-Centric Serving System for Speech Language Models" ✔ (read back, not cited as a row: SpeechLM scope)
- 2603.22774 → "Characterizing CPU-Induced Slowdowns in Multi-GPU LLM Inference" ✔ (cited, D2.2)
- 2607.29678 → "TokTier: Exact Stateful CPU+GPU Tokenization for Agentic LLM Serving" ✔ (read back; cited in NEGATIVE FINDINGS as the strongest positive evidence that tokenization is a first-class front-end cost, but it is a tokenizer-service paper rather than a serving-layer-overhead measurement, so no row)

Items found and read but deliberately NOT given a row (with the reason):
- Issue #55634 "aborted requests are counted in prompt_tokens_total but absent from every per-request metric" — excellent "measure X but not Y" item with hard deltas (`vllm:prompt_tokens_total` +3010 vs `vllm:request_prompt_tokens_sum` 0 over five aborted streams), but it has an active fix (open PR #55940 "[Bugfix][Metrics] Record stats for aborted requests", plus open PR #55837) and the OPEN section is capped at 3, so it is recorded here instead of as a row.
- PR #55812 "[Metrics] Add admission rejection metrics" (OPEN) — verbatim gap: "Admission limits reject requests with HTTP 503, but operators cannot tell from metrics which queue cap fired (`max_num_queued_reqs` vs `max_num_queued_tokens`), and each rejection logged at INFO." Open, not abandoned; the admission-control flags themselves shipped (`--max-num-queued-reqs`, `--max-num-queued-tokens` in `vllm/engine/arg_utils.py:1603-1607`).
- PRs #51329 ("[Metrics] Add an engine step counter"), #50824 (MM preprocessing metrics, superseded), #41765 (token-native binary transport) — all closed unmerged, but the retrieved pages contain **no human comment stating a closure reason** (only the PR body plus bot/automated review comments; #41765 was closed by its own author with no comment in the timeline). Per the evidence rules these are dropped as ABANDONED rows.
- PRs #36047, #37605, #39037 (SGLang) — closed/author-closed with no human closure statement in the retrieved bodies. Dropped as ABANDONED rows; the *gap statements* in #39037 ("The tokenizer manager process is invisible in profiles") and in #36047's defect description are still valid evidence, but they are not closure reasons.
- Issue #36189 `--enable-request-stats-headers` — closed with `stateReason: NOT_PLANNED` and a `stale` label; no human comment stating a reason was retrieved, so it is dropped as a row (its "aggregate or backend-only observability" framing overlaps B1.1/B1.3).

## NEGATIVE FINDINGS

- **"The HTTP layer is not the bottleneck" as an explicit project claim** — nothing found. Queries tried: `vllm github issue api server overhead bottleneck HTTP frontend`; `arxiv LLM inference server CPU overhead breakdown request lifecycle profiling FastAPI uvicorn`; open-PR list searches for "http overhead", "frontend overhead", "api server overhead" in vllm-project/vllm. What exists instead is the opposite: vLLM's own docs name the API server process as a CPU consumer to be provisioned for (`docs/configuration/optimization.md`), name it as a scale-out target (`docs/serving/data_parallel_deployment.md`), and its maintainer's Rust-front-end RFC states the asyncio front end "can’t keep up". I found no document, issue or PR in which a maintainer asserts the HTTP layer is free.
- **A published, contiguous latency breakdown of the vLLM request lifecycle (tokenize → template → schedule → execute → detokenize) on one GPU** — nothing found. Queries tried: `arxiv paper measuring LLM serving framework overhead HTTP frontend vLLM TGI comparison benchmark`; `arxiv benchmark study vLLM TensorRT-LLM SGLang overhead request lifecycle latency breakdown`; `"serving overhead" arxiv LLM inference framework measurement study 2026`. The closest is ScaleLLM (arXiv:2408.00008), which is multi-GPU, and the vLLM issue threads, which are per-component rather than end-to-end. The engine-side segmentation vLLM does publish (`QUEUED`/`SCHEDULED`/`NEW_TOKENS`) is documented in `docs/design/metrics.md` but explicitly starts at tokenization.
- **Any metric, in either engine, for sampling time or detokenization time** — nothing found. Queries tried: local exhaustive grep of every `"vllm:<name>"` and `"sglang:<name>"` literal in vLLM 0.29.0 and SGLang main; `vllm issue ask metric "sampling time" OR "detokenization time" instrument`. vLLM's own design doc records the nearest thing — `vllm:model_forward_time_milliseconds` / `vllm:model_execute_time_milliseconds` — as OpenTelemetry-only and explicitly asks whether they are worth the cost: "We already have `inference_time` and `decode_time` metrics, so the question is whether there are sufficient common use cases for the higher-resolution timings to justify the overhead." Detokenization time appears in the tree only as a **benchmark opt-out** (`--disable-detokenize` in `vllm/benchmarks/latency.py` and `vllm/benchmarks/throughput.py`), never as a runtime metric.
- **`/metrics` scrape cost at high request rates, measured** — nothing found. Queries tried: open-PR list and issue-list searches in vllm-project/vllm for "metrics scrape", "metrics cardinality", "prometheus overhead"; `sglang --enable-metrics prometheus overhead`. What exists is a maintainer warning without a number ("They can have a meaningful performance impact when enabled", `docs/design/metrics.md`) and one concrete cardinality complaint in the same doc about `vllm:lora_requests_info` encoding per-adapter counts in a comma-separated label string ("Encoding a running/waiting counts for multiple adapters in a comma-separated string seems quite misguided"). No scrape-rate-vs-throughput experiment was retrieved from any of the three projects.
- **A closed-unmerged vLLM/SGLang front-end *rewrite* PR with a maintainer-stated rejection reason** — nothing found. Queries tried: `is%3Apr+is%3Aclosed+is%3Aunmerged+frontend` (25 rows, all bugfixes/backports/bumps), `+HTTP` (25 rows, mostly kernels/models), `+%22api+server%22` (25 rows), `vllm RFC openai server rewrite closed not planned entrypoints refactor`, `vllm closed unmerged pull request frontend rewrite rejected`. The Rust front end was *merged* as experimental (#40848) rather than rejected, and the architectural objections to it live as comments in the still-open RFC #40846 (verbatim objection from a maintainer in that thread: "There’s no way we can rewrite everything in Rust, and we don’t have enough contributors to maintain the Rust logic. The entrypoint is the fastest-changing and most complex part of the logic, let alone the multimodal stuff."), not as a closure reason.
- **TGI's router-side metric coverage / sharded-server overhead** — nothing found beyond the maintenance-mode notice. Queries tried: `text-generation-inference TGI router benchmark overhead sharded server github`; `text-generation-inference router rust rewrite closed unmerged pull request`; `https://github.com/huggingface/text-generation-inference/pulls?q=is%3Apr+is%3Aopen+router+metrics`. TGI's README lists "Prometheus metrics" as a feature and points at a third-party blog (Adyen) for internals; no primary measured overhead comparison was retrieved.
- **GitHub `/search` endpoint was unavailable for the whole session** — every attempt returned "You have exceeded a secondary rate limit." All GitHub discovery above was therefore done through repo-scoped `?q=` list pages and direct issue/PR URLs instead. Consequence: coverage of closed-unmerged PRs is good but not exhaustive, and the `?q=` issue-list pages returned 0 parsed rows for every `is%3Aissue` query, so closed *issues* were largely reached via web search rather than by enumeration.
