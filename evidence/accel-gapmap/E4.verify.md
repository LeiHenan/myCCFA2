# Verification report — E4

Adversarial verification of `E4.md` (SGLang, `main` / 0.5.19) — **17 NEVER-DISCUSSED rows**.
I did not write E4.md. Method: VERIFIER_BRIEF.md + the V4 assignment. Every GAP quote was re-fetched
from `raw.githubusercontent.com/sgl-project/sglang/main/...` and matched with a whitespace/comment-marker
insensitive matcher that also returns the line number; the local mirror `/tmp/sglang-main/python` was used
for corroboration only. Every absence claim was re-tested with phrasings the researcher did not use,
across `sgl-project/sglang` and the upstream repos the claim implicates
(`vllm-project/vllm`, `flashinfer-ai/flashinfer`, `Dao-AILab/flash-attention`, `NVIDIA/TensorRT-LLM`,
`pytorch/pytorch`), plus `web_search` and arXiv.

**Search-integrity note (applies to every file in this pass).** Mid-pass, `github.com/<repo>/issues?q=`
began returning **HTTP 429**. The original search helper (`/tmp/ghq.py`) silently reports `N=0` for a 429
page, so any "0 results" obtained during that window is a potential false negative. All E4 queries were
run through a BLOCKED-aware helper (`/tmp/ghq2.py`, which reports `BLOCKED` instead of `N=0` and retries
with backoff); **no E4 query was BLOCKED**, and every `N=0` that supports a CLEAR verdict was re-run and
returned a genuine `ok/N=0`.

---

## Corrections (rows I changed)

- ROW: **E4:E9** | FIELD: ABSENCE CLAIM + CONFIDENCE THE ABSENCE IS REAL | WAS: *"I did not find it. The
  identifier `CAPTURE_SAFE_WEIGHT_SENTINEL` returns nothing; the only 'startup weight overlap' hits are PR
  #32017/#31984 ... and #35259 ... none states the quantization / torch.compile / architecture exclusions
  above"* — CONFIDENCE MEDIUM-HIGH | NOW: **DROP (absence falsified)** | REASON: PR #35259 is not merely
  adjacent — its body raises the quantized-checkpoint exclusion verbatim: *"Other architectures,
  quantized/repacked-weight paths, speculative decoding, LoRA/offload, and backend/topology combinations
  outside these profiles remain rejected. In auto mode they use serial startup."*, and its follow-up list
  contains *"define a capture-stable contract for repacking quantization methods"*. The row's core
  narrower restriction (quantized checkpoints are excluded from capture-safe weight loading) is therefore
  raised by humans in the same feature's own thread. Only the "FP16/BF16-only" and "torch.compile"
  sub-clauses remain unreported. | EVIDENCE: https://github.com/sgl-project/sglang/pull/35259
  (state: OPEN) | `fetch.py gh`, HTTP 200; quote re-read from page text.
- ROW: **E4:E12** | FIELD: ABSENCE CLAIM | WAS: *"I did not find it. Adjacent-but-different: issue #36048
  (OPEN) ... but its mechanism is CUDA-graph hang + MTP request allocation, not the AWQMarlin layer check"*
  | NOW: **DROP (absence falsified)** | REASON: the per-layer AWQMarlin→AWQ demotion this row says is
  unreported is the exact gap raised upstream: vLLM PR #13119 "[Bugfix] Allow fallback to AWQ from
  AWQMarlin at per-layer granularity" (**MERGED**) body: *"Models like
  TechxGenus/DeepSeek-Coder-V2-Lite-Instruct-AWQ have some layers that can't satisfy the constraints of
  marlin even for TP=1."* followed by the `ValueError: Weight input_size_per_partition = 10944 is not
  divisible by min_thread_k = 128`. SGLang's AWQMarlin is a port of vLLM's, so the gap is discussed, just
  in the upstream repo. Cross-repo caveat recorded; the row's "no human anywhere" phrasing cannot stand.
  | EVIDENCE: https://github.com/vllm-project/vllm/pull/13119 (state: MERGED) | `fetch.py gh`, HTTP 200.
- ROW: **E4:E15** | FIELD: WHAT IS MISSING + CONFIDENCE | WAS: *"The Marlin fallback kernel set has no
  MXFP4 (E2M1 + E8M0 block-scale) MoE implementation, so an MXFP4 MoE checkpoint cannot be rescued onto
  Marlin ... there is no MXFP4-Marlin MoE kernel in the tree."* — CONFIDENCE MEDIUM-HIGH | NOW:
  **DROP (factually wrong)** | REASON: (a) merged PR #28231 "Use Marlin for SM120 MXFP4 MoE" — *"Implement
  MXFP4 Marlin MoE for GPT-OSS and make marlin the default GPT-OSS MXFP4 MoE backend on SM120. ... Remove
  that path and delete the DSV4 SM120 MXFP4 Triton fallback file."*; (b) `mxfp4_marlin_moe.py` **exists on
  main** (and in the local mirror) with `build_marlin_moe_quant_info()` / `Mxfp4MarlinMoEMethod` and
  `MarlinMoeQuantInfo`; (c) the cited raise at `mxfp4.py:635` sits inside a **per-layer shape gate** —
  `if not check_moe_marlin_supports_layer(layer, 32, allow_tile_padding=True): raise RuntimeError("Current
  MXFP4 MoE layer is not supported by Marlin.")` — with `prepare_moe_mxfp4_layer_for_marlin(layer)` on the
  very next lines. The raise is "this layer's shape is unsupported by Marlin", not "MXFP4 MoE cannot use
  Marlin". The quote is real; the row's reading of it is not. | EVIDENCE:
  https://github.com/sgl-project/sglang/pull/28231 (state: MERGED) |
  https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/mxfp4_marlin_moe.py
  (HTTP 200) | `fetch.py gh` + raw fetch + local `sed -n '620,650p'`.
- ROW: **E4:E2** | FIELD: CONFIDENCE THE ABSENCE IS REAL | WAS: HIGH | NOW: **MEDIUM (downgrade;
  counter-evidence disclosed)** | REASON: the coarse restriction is discussed by a human in PR #37952
  (OPEN, the SM120-FA4 policy PR): *"The new default applies only to SM120 FA4 with BF16 Q/K/V, causal
  global paged attention, and no cascade, context parallelism, local/sliding attention, sinks, score
  modification, relative bias, cross attention, softcap, or KV-cache bypass."* — i.e. relative bias being
  unsupported on the SM120 FA4 path is stated; only the specific `max(head_dim, head_dim_v) <= 128` and
  `tile_mn == (64, 128)` launch-plan limits are unstated. The quoted GAP sentence itself remains unique.
  | EVIDENCE: https://github.com/sgl-project/sglang/pull/37952 (state: OPEN) | `fetch.py gh`, HTTP 200.
- ROW: **E4:E4** | FIELD: CONFIDENCE THE ABSENCE IS REAL | WAS: MEDIUM | NOW: **LOW (downgrade;
  counter-evidence disclosed)** | REASON: the upstream kernel-side restriction is raised by a human in
  flashinfer RFC #3628: its "existing path / source? / runs on SM120?" table states *"`fmha_v2`
  `e4m3_fp32_*_sm120` | source | kernel compiles, **disabled in the Python API**"*. That is not SGLang's
  CLI rejection and does not mention skip-softmax or `fp8_e4m3` **KV cache**, so the row survives as an
  SGLang-specific residual — but "no human anywhere has raised this" is too strong for the fp8-FMHA-on-
  SM120 family. | EVIDENCE: https://github.com/flashinfer-ai/flashinfer/issues/3628 (state: OPEN) |
  `fetch.py gh`, HTTP 200.
- ROW: **E4:E5** | FIELD: CONFIDENCE THE ABSENCE IS REAL | WAS: HIGH | NOW: **MEDIUM (downgrade;
  cross-engine counter-evidence disclosed)** | REASON: vLLM PR #40394 (OPEN) "FlexAttention non-causal
  support": *"Currently only the FLASH_ATTN backend supports non-causal attention ... adds support for
  non-casual attention to the FLEX_ATTENTION backend for decoder models."* Same missing capability in the
  same PyTorch-FlexAttention integration, different engine. The exact SGLang string appears only inside
  PR #9947's introducing diff (rule ii: not a discussion of the gap), so the SGLang-specific absence
  holds. | EVIDENCE: https://github.com/vllm-project/vllm/pull/40394 | `fetch.py gh` (reported by the
  E4 search runner; the PR was not re-read line-by-line by me — marked as reported-not-reverified below).
- ROW: **E4:E16** | FIELD: WHAT IS MISSING + CONFIDENCE | WAS: *"... there is no in-tree MXFP8 dense
  GEMM."* — MEDIUM-HIGH | NOW: **MEDIUM (downgrade; claim partially contradicted)** | REASON: PR #33208
  (OPEN, "[MXFP8] Use FlashInfer CUTLASS for dense GEMM on SM120, delete Triton path") states the SM120
  auto path *did* fall through to a Triton MXFP8 dense kernel that the PR deletes, and #19637 tracks
  "Support MXFP8 dense GEMM through CUTLASS" as open work. So the *runtime-error* claim stands but "there
  is no in-tree MXFP8 dense GEMM" is contradicted for the tree the row was written against. |
  EVIDENCE: https://github.com/sgl-project/sglang/pull/33208 (state: OPEN) | reported by the E4 runner.
- ROW: **E4:E17** | FIELD: CONFIDENCE THE ABSENCE IS REAL | WAS: MEDIUM-HIGH | NOW: **MEDIUM
  (downgrade)** | REASON: the sm120 `auto -> cutlass` routing this row presents as an unremarked
  contradiction is explicitly discussed and intended: issue #18872 (COMPLETED, "Document & auto-enable FP8
  block-wise CUTLASS GEMM for SM120") has a maintainer stating *"a better SM120 compatible implementation
  of FP8 blockwise GEMM is implemented here (PR #9969), compared to Triton ... We should also enable it by
  default on SM120 (through auto-detection), with `--fp8-gemm-backend`."*; #19637 and PR #28189 continue it.
  What remains unstated is only the internal inconsistency with the "deprecated on this hardware" string.
  | EVIDENCE: https://github.com/sgl-project/sglang/issues/18872 | reported by the E4 runner.
- ROW: **E4:E1** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"I did not find it. ... The kernels are
  SGLang-owned (`fa4_sm120/`), so `sgl-project/sglang` is the exhaustive repo for this term."* —
  CONFIDENCE HIGH | NOW: **DROP (absence falsified)** | REASON: the premise that `sgl-project/sglang` is
  the exhaustive repo is false. `python/sglang/kernels/ops/attention/fa4_sm120/flash_fwd.py` carries the
  Dao-AILab CuTe FA authors' header — `# Copyright (c) 2025, Jay Shah, Ganesh Bikshandi, Ying Zhang, Vijay
  Thakkar, Pradeep Ramani, Tri Dao.` — i.e. it derives from the upstream CuTe SM120 forward kernel, and the
  SM120-forward block-sparsity limitation is raised there by humans: Dao-AILab PR #2406 "[CuTe, SM120]
  Forward kernel with optimized TMA path and full features support" states it covers everything *"except
  block sparsity"* and its comparison table marks block sparsity *"Explicitly unsupported (assert)"*; PR
  #2389 (OPEN) is titled "Add SM80/SM120 block-sparse forward attention support" and its body says *"SM120
  (`FlashAttentionForwardSm120`) inherits the SM80 base class and gets block sparsity for free; removes the
  `assert not use_block_sparsity` guard in `interface.py`"*; PR #2634 lists block sparsity under "What's
  supported" for FA4 sm_120. The exact SGLang assert string is still unquoted anywhere, but the gap "the
  SM120 FA4 forward refuses block sparsity" is squarely discussed upstream. The E4 researcher explicitly
  declined to search non-SGLang repos, which is what produced this false absence. | EVIDENCE:
  https://github.com/Dao-AILab/flash-attention/pull/2389 (state: OPEN) ;
  https://github.com/Dao-AILab/flash-attention/pull/2406 ;
  https://github.com/Dao-AILab/flash-attention/pull/2634 | `fetch.py gh`, HTTP 200; header read from
  `/tmp/sglang-main/python/sglang/kernels/ops/attention/fa4_sm120/flash_fwd.py:1`.
- ROW (cross-file): **E4:E1 and `E5.md` row E5.14 are the same quote at the same source line**
  (`fa4_sm120/flash_fwd.py:1091`). **Both are now dropped** (see above and `E5.verify.md`), so the
  duplication is resolved without leaving a surviving copy.

## Confirmations (rows I checked and could not falsify)

Each row's GAP quote was re-located verbatim on `main` at the cited line (local-mirror line agrees; source
files carry no state token — HTTP 200 raw fetch). Absence re-tested with ≥3 new phrasings each.

- E4:E3 — quote at `triton_backend.py:1797-1812` (4 guard strings, all present); #37983/#38142 confirmed to
  be the **vision** kernel path (`context_attention_fwd`), not `dense_prefill_attention_fwd`. MEDIUM fair.
- E4:E6 — rule list present at `cuda_graph_hook.py:182`; no thread states the
  `enable_torch_compile ⟂ tc_piecewise-prefill` rule (#23004 describes TCPiecewise, #31532 is the DCP
  trigger). MEDIUM-HIGH fair.
- E4:E7 — GGUF rule present at `cuda_graph_hook.py:217-224`; no thread connects GGUF to CUDA-graph capture.
- E4:E8 — quote at `kv_canary/api.py:44-48`; the exact string appears only in PR #26809 (the PR that added
  the canary API) whose body/comments never say "piecewise" → rule (ii), not a discussion.
- E4:E10 — `@torch._dynamo.disable()` + TODO at `flashattention_backend.py:3677-3680`; `merge_state_v2`
  corpus is the kernel (overflow #29602/#29603, #29720; chunked-KV merges), not the Dynamo exemption.
- E4:E11 — docstring at `hpc_ops_backend.py:14`, raise at `:135-140`; no thread asks for non-Hopper
  HPC-Ops. MEDIUM fair (third-party sm90a hard requirement).
- E4:E13 — asserts at `modelopt_quant.py:1973-1980`; #37378 touches the same helper for a different trigger
  (`swiglu_limit`), #32398 only rebinds `weights_padding_cols`. MEDIUM fair.
- E4:E14 — raise at `nvfp4_online.py:330-333`; requantization corpus is the inverse direction
  (NVFP4→MXFP4 ROCm #29328), grouped FP8 UE8M0 #39063, #38748. MEDIUM-HIGH fair.

## Could not verify

- **E4:E5** (vLLM PR #40394) and **E4:E16** (sglang PR #33208), **E4:E17** (sglang #18872, #19637,
  #28189): these counter-sources were retrieved and quoted by the E4 search runner; I confirmed the
  affected rows' own quotes and the dropped rows myself, but did not re-open these three threads
  line-by-line before writing this report. They are used only to **downgrade** confidence, never to drop a
  row, so the direction of risk is conservative.
- **E4:E1's upstream counter-sources**: I read PR #2389's title+body in full and #2406/#2634's titles and
  summary text; I did not enumerate #2406's full comparison table row-by-row. The load-bearing facts
  (SGLang kernel copyright header = Dao-AILab CuTe authors; #2406 says "full features support (except
  block sparsity)"; #2389 is titled "Add SM80/SM120 block-sparse forward attention support") are confirmed
  first-hand.
- arXiv: no paper-level venue exists for these kernel-dispatch/CLI-validation gaps; the runner's arXiv
  query ("SM120 block-sparse attention Blackwell") returned 0. No arXiv ID is cited in E4.md, so there was
  no ID to read back.

## Tally
checked=17 corrected=9 dropped=4 verbatim_relocated=17 unverifiable=0
absence_claims_falsified=4
