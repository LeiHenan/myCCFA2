# Verification report — S9 (model-side training-free acceleration)

Method: all 41 blocks of `S9.md` re-parsed; every QUOTE / SUPPORTING QUOTE / ASKING QUOTE / STATED REASON
re-fetched and re-located at its URL with `python3 verify.py quote` plus a raw-text containment pass;
every GitHub artifact's `"state":"…"` token re-read; every arXiv id re-read with `fetch.py arxiv`.
Supporting files `S9_sub_runtimes.md` and `S9_sub_structural.md` were read and their rows spot-checked
against the same URLs.

## Corrections (rows I changed)

- ROW: A9.4 | FIELD: STATUS | WAS: `merged (both); present at v0.29.0 as vllm/compilation/passes/fusion/qk_norm_rope_fusion.py` | NOW: `#27165 MERGED; #27018 is CLOSED unmerged` | REASON: PR #27018's page payload carries only `"state":"CLOSED"` (no MERGED token) and the visible banner reads "Closed — izhuhaoran wants to merge 5 commits into vllm-project:main". PR #27165 carries `"state":"MERGED"`. The row's own A9.4 QUOTE ("This PR fuses QNorm, KNorm, and RoPE…") is from #27018, so the quote is from an unmerged PR — the capability did land, but via #27165 only. | EVIDENCE: https://github.com/vllm-project/vllm/pull/27018 | raw curl via fetch.py, HTTP 200
- ROW: A9.11 | FIELD: SUPPORTING QUOTE 3 | WAS: runtime-gate quote attributed to the row's URL set (`CMakeLists.txt`; `.../mixed_precision/marlin.py`; PR #43731) | NOW: `add https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/layers/quantization/utils/marlin_utils.py` | REASON: `"Marlin W4A8-FP8 only support SM89 or SM12x device"` and `"(It is slower than Marlin W4A16 on other devices)."` are verbatim in `marlin_utils.py` (lines 676-677 of the local mirror) — that file is cited nowhere in the row, so `verify.py quote` MISMATCHes at all three of the row's URLs. | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/layers/quantization/utils/marlin_utils.py | fetch.py get, HTTP 200
- ROW: D9.3 | FIELD: SUPPORTING QUOTE 3 | WAS: `"CutlassW4A8 / Machete : require SM90 (Hopper)"` attributed to PR #20830 | NOW: `attribute to PR #43731` | REASON: the string is verbatim in PR #43731's body as one bullet of the list "On Ampere (SM80), all available CUDA W4A16 kernels reject these shapes: / CutlassW4A8 / Machete : require SM90 (Hopper) / Marlin : requires input_size_per_partition % 128 == 0 …". PR #20830 contains 0 hits for `CutlassW4A8` and only 1 for `SM90` (its title). | EVIDENCE: https://github.com/vllm-project/vllm/pull/43731 | fetch.py gh, HTTP 200
- ROW: C9.8 | FIELD: SUPPORTING QUOTE 3 | WAS: attributed to `docs/design/fusions.md` (the row's only URL) | NOW: `add https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/optimization_levels.md` | REASON: "These fusions are only enabled when either op is using a custom kernel, otherwise Inductor fusion is better." is verbatim in `optimization_levels.md` (the O1 footnote, line 61) and appears 0 times in `fusions.md`. | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/optimization_levels.md | fetch.py get, HTTP 200
- ROW: C9.13 | FIELD: S3B | WAS: `… the state token could not be extracted from its page payload, so this is recorded as an in-flight re-attempt rather than a merge` | NOW: `PR #56586 state token reads DRAFT (open draft, not merged)` | REASON: the payload does contain an extractable state token: `"state":"DRAFT"`. The row's caution ("not a merge") is correct; the claim that the token could not be extracted is not. | EVIDENCE: https://github.com/vllm-project/vllm/pull/56586 | raw curl via fetch.py, HTTP 200

## Confirmations (rows I checked and could not falsify)

- **A9.4 / C9.7 (the assigned high-stakes vLLM 0.29.0 claims) — CONFIRMED at the live tag.**
  `vllm/config/vllm.py` @ v0.29.0 reads `IS_QUANTIZED = False` / `IS_DENSE = False` with the comment
  "The optimizations that depend on these properties currently set to False in all cases", and
  `OPTIMIZATION_LEVEL_02/03` map `"fuse_attn_quant": IS_QUANTIZED`, `"enable_sp": IS_DENSE`,
  `"fuse_gemm_comms": IS_DENSE` (O0/O1 hardcode all three to `False`); `"enable_qk_norm_rope_fusion": False`
  appears at **every** level O0–O3. `docs/design/fusions.md` @ v0.29.0: "Not enabled by default at any
  optimization level due to perf issues on H100: [#34391]". Corroborated in `/tmp/vllm-0.29.0`
  (lines 127-128 and 239-321) — local mirror used for corroboration only.
- **C9.1 / A9.12 (the assigned TensorRT-LLM removal claim) — CONFIRMED.** The migration page's table reads
  "trtllm-build / trtllm-refit / trtllm-prune → No engine-build step — HuggingFace checkpoints load
  directly", "Per-model convert_checkpoint.py → Not needed", "--backend tensorrt (CLI) → Omit, or pass
  --backend pytorch", under the banner "Breaking change. The TensorRT engine backend has been removed.
  PyTorch is now the sole execution backend for TensorRT LLM".
- Merged/closed states re-read and matching the rows: A9.1 #2497 MERGED; A9.2 #7174 MERGED **and** #7701
  MERGED; A9.3 #15160 MERGED; A9.5 #10867 MERGED; A9.6 #27883 MERGED; A9.7 #16155 MERGED; A9.9 #30817
  MERGED; A9.11 #43731 MERGED; C9.10 #40171 MERGED and #47343 CLOSED unmerged; C9.11 #35424 MERGED with
  #34458 / #35426 CLOSED `COMPLETED` (the row's "SOLVED" claim holds); C9.14 #29483 MERGED and the reverted
  #28841 reads MERGED; C9.15 #27281 CLOSED unmerged and #22687 CLOSED; D9.2 #42230 CLOSED unmerged;
  D9.3 #20830 MERGED; B9.1 open issue #19662 still OPEN.
- Quotes re-located verbatim: all 41 primary quotes, plus 128 quoted fragments across every field of every
  block. A9.8's five supporting quotes, C9.7's H100-regression quotes, C9.12's four ROCm/AITER quotes,
  D9.1–D9.5's code and doc quotes all MATCH.
- arXiv readbacks: 2408.11743, 2403.17887, 2403.03853, 2404.16710 (plus the AWQ/GPTQ ids cited inside
  prose) — titles as claimed.

## Could not verify

- Nothing. Two supporting quotes could not be relocated **at the row's own URLs** but were found verbatim
  at the correct sources (A9.11 SQ3 → `marlin_utils.py`; D9.3 SQ3 → PR #43731; see Corrections). A further
  five supporting quotes (A9.11 SQ1/SQ2/SQ5, C9.8 SQ3) fail `verify.py quote` only because the checker runs
  `strip_html` over raw `.txt`/`.md`/CMake sources and a stray `<`…`>` span deletes whole regions of
  `CMakeLists.txt`; each was re-verified verbatim against the raw bytes.

## Notes

- The file header states "Verdicts were MATCH or MATCH-FRAGMENTS for every quote below". That is true for
  every **primary** quote, and true for the supporting quotes only via the raw-text path or a corrected
  attribution — the orchestrator should apply the four attribution fixes above before the header claim is
  literally reproducible with the shipped `verify.py`.
- C9.16 (ABANDONED) cites issue #25094 whose state token reads **OPEN** (`stateReason: NOT_PLANNED`
  appears in the payload but the issue itself is open). Its substance is a measured negative result and its
  owner-tracker #25689 is CLOSED, so I did not reclassify it — but it is the "ABANDONED row whose artifact
  is still open" pattern and is flagged for the orchestrator's judgement.

## Tally
checked=41 corrected=5 dropped=0 verbatim_relocated=41 unverifiable=0
