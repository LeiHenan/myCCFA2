# Verification report — E2

Adversarial verification of `E2.md` (vLLM 0.29.0) — **19 NEVER-DISCUSSED rows** (E1–E19).
I did not write E2.md. Every GAP quote was re-fetched from
`raw.githubusercontent.com/vllm-project/vllm/v0.29.0/<path>` and re-located verbatim by line number,
corroborated with `grep -n -F` against `/tmp/vllm-0.29.0`. Absence claims were re-tested with phrasings the
researcher did not use on `vllm-project/vllm`, `sgl-project/sglang`, `flashinfer-ai/flashinfer`,
`pytorch/pytorch`, plus `web_search` and arXiv.

**Search-integrity note.** The repo-scoped issues route hit **HTTP 429** mid-pass; the E2 run used a
status-checking searcher and verified all 34 zero-result pages as 200-OK pages containing the GitHub
"no results" blank slate. I re-ran independent probes for the rows below and none came back BLOCKED.
Standing caveat the researcher also recorded: the repo issues-list index covers **titles + bodies only**,
not code or review comments — so a gap raised only deep inside a review thread is invisible to this route
(this is precisely how the E2:E9 miss happened, and why the falsified rows below were found via reviewers'
prose, not via source strings).

---

## Corrections (rows I changed)

- ROW: **E2:E9** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"The only closely-ranked results are #12071
  Support torchrun and SPMD-style offline inference (the original SPMD PR) and #38701 ... — neither states
  the determinism blocker."* / *"no one has asked for deterministic v1 scheduling as a feature."* — HIGH |
  NOW: **DROP (absence falsified)** | REASON: #12071's review thread contains exactly that exchange —
  reviewer comaniac: *"How users guarantee the scheduling decisions are deterministic?"*; author: *"vLLM
  needs to guarantee it. right now, I added the check f1dfd2b to rule out known factors that can cause
  non-deterministic behavior."* — in a PR whose example uses
  `distributed_executor_backend = "external_launcher"`, i.e. the very path the TODO blocks on. In addition,
  #38701's review feedback explicitly flags *"the loss of a TODO comment regarding deterministic scheduling
  for the external launcher backend"* — a reviewer raising the TODO itself, which is outside the
  "author documents own code" exclusion. Both PRs are MERGED. | EVIDENCE:
  https://github.com/vllm-project/vllm/pull/12071 (MERGED) ; https://github.com/vllm-project/vllm/pull/38701
  | `fetch.py gh`, HTTP 200.
- ROW: **E2:E15** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"the xgrammar-gate PRs (#48416, #49996,
  #45599, #56616) all concern JSON-schema keywords"* (implying the NUL rejection is unreported) — HIGH |
  NOW: **DROP (absence falsified)** | REASON: PR #51796 "[Bugfix] Reject NUL byte in
  structured_outputs.regex" (**MERGED**) is that report and fix — body: *"A NUL byte is never meaningful in
  a regex pattern and is not handled by the regex-to-grammar conversion. Currently a
  `structured_outputs.regex` containing a NUL is [broken] ... This PR rejects a regex containing a NUL at
  request validation, before ... xgrammar's native code"*, with test `test_regex_with_nul_byte_rejected`.
  The miss is a query-vocabulary artefact: the thread is indexed under "NUL byte", not `\x00`/`\x00'`.
  | EVIDENCE: https://github.com/vllm-project/vllm/pull/51796 (state: MERGED) | `fetch.py gh`, HTTP 200.
- ROW: **E2:E1** | FIELD: CONFIDENCE | WAS: HIGH — *"the identifier `apply_scaled_mm` plus `b12x` is
  unique enough that any report would surface"* | NOW: **MEDIUM (downgrade)** | REASON: the same missing
  capability is reported **by symptom** in issue #49076 (OPEN) — "`--linear-backend=flashinfer_b12x` crashes
  on Qwen3.6-35B-A3B-NVFP4 — no kernel for FP8-quantized GDN QKVZ layer", the user asking for a fallback
  instead of a hard error — without naming `apply_scaled_mm`/`B12xTensorFP8ScaledMMLinearKernel`. The exact
  string is a genuine zero, but "any report would surface" is weaker than HIGH implies. | EVIDENCE:
  https://github.com/vllm-project/vllm/issues/49076 (OPEN) | reported by the E2 runner; consistent with my
  own probes (`"Install the B12X backend"` → 0, `b12x apply_weights` → #54959/#40969 unrelated).
- ROW: **E2:E3** | FIELD: CONFIDENCE | WAS: HIGH | NOW: **MEDIUM (downgrade; class disclosed)** | REASON:
  the E8M0-upcast limitation is a live public topic: PR #54435 "Upcast E8M0 block scales on every platform,
  not just ROCm/XPU" (kernels cannot bind E8M0 scale tensors; fp32 upcast is the workaround) and PR #47988
  "Handle E8M0 block scales in CUTLASS and Triton FP8 linear kernels" (SM120 sweep). Neither names **B12X**
  or 128x128 UE8M0, so the row's narrow claim survives, but "the class is unmentioned" does not. |
  EVIDENCE: https://github.com/vllm-project/vllm/pull/54435 ; https://github.com/vllm-project/vllm/pull/47988
  | reported by the E2 runner.
- ROW: **E2:E5** | FIELD: counter-evidence disclosure (confidence unchanged, MEDIUM) | WAS: no package-side
  counter-evidence cited | NOW: disclose FlashInfer #5175 / PR #5191 | REASON: #5175 "b12x mm_mxfp8 requires
  K % 128 == 0 ... silently takes a 3.4x slower path" (with fix PR #5191) raises the same *silent
  downgrade* of b12x MXFP8 dense with no user-visible reason, at package level; the vLLM-side
  `is_supported()` probe is still unnamed. | EVIDENCE:
  https://github.com/flashinfer-ai/flashinfer/issues/5175 ; https://github.com/flashinfer-ai/flashinfer/pull/5191
  | reported by the E2 runner.
- ROW: **E2:E6** | FIELD: CONFIDENCE | WAS: HIGH | NOW: **MEDIUM (downgrade; class disclosed)** | REASON:
  FlashInfer PR #4479 "feat(b12x): support MXFP4 dense GEMM on SM120" (MERGED) raises that b12x FP4 dense
  GEMM only supported NVFP4 (`use_nvfp4`), i.e. the very capability vLLM's MXFP4 gate probes — at package
  level, not in vLLM's `is_supported()`/private-intrinsics terms. The row's vLLM-side scoping (all FP4+B12X
  vLLM threads are MoE) still holds. | EVIDENCE:
  https://github.com/flashinfer-ai/flashinfer/pull/4479 | reported by the E2 runner.
- ROW: **E2:E10** | FIELD: CONFIDENCE | WAS: HIGH — *"nothing reports that CLS and MEAN pooling still
  reject partial prefill in 0.29.0"* | NOW: **MEDIUM (downgrade; design boundary is public)** | REASON:
  PR #20930's body lists the pooling types that *"do not support chunked prefill & prefix caching"*
  (including MEAN-pooling causal models) and PR #27145 scopes chunked-prefill enablement to "all (and last)
  pooling" only — i.e. CLS/MEAN being left out is a documented, reviewed boundary rather than an
  undiscovered hole. Neither quotes the two 0.29.0 raise strings, so the rows survive at reduced
  confidence. | EVIDENCE: https://github.com/vllm-project/vllm/pull/20930 ;
  https://github.com/vllm-project/vllm/pull/27145 | reported by the E2 runner; my own probe
  `"partial prefill is not supported with MEAN pooling"` → genuine `N=0`.
- ROW: **E2:E17** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"No issue, PR, blog or paper states the
  ≤4-token limit."* — MEDIUM | NOW: **LOW + reclassify** (cap is documented; only the >4-token failure mode
  is unreported) | REASON: the merged origin PR #34875 ("[Bugfix] Fix V1 logprobs empty strings for
  multi-byte UTF-8 tokens when logprobs > 0") states the cap in its own description: *"`_get_sampled_context_ids()`
  extracts the last ≤4 sampled token IDs"* and *"`_correct_decoded_token(...)` tries increasing context
  windows (1-4 tokens)"*. This is the introducing PR, so it is not a discussion OF the limitation (rule ii,
  as the runner correctly applied) — but the literal claim "no PR states the ≤4-token limit" is false, and
  the row must be rewritten as "the cap is documented in #34875; the >4-byte-sequence empty-string failure
  is unreported". | EVIDENCE: https://github.com/vllm-project/vllm/pull/34875 (state: MERGED) |
  `state.py` + `fetch.py gh`, HTTP 200.
- ROW: **E2:E19** | FIELD: CONFIDENCE | WAS: MEDIUM | NOW: **LOW (downgrade; class disclosed)** | REASON:
  legitimate HF tokenization kwargs being silently unhonored is a discussed class: PR #51157
  "[Bugfix][Frontend] Let pooling requests set padding" (pooling tok params *"not from
  `tokenization_kwargs`, and that list had no padding"*) and PR #41949 (truncation kwargs dropped for token
  prompts). The specific warning/allow-list at `renderers/params.py:292` is never named, and my own probe
  of `"tokenization arguments are not supported"` is a genuine `N=0`. | EVIDENCE:
  https://github.com/vllm-project/vllm/pull/51157 ; https://github.com/vllm-project/vllm/pull/41949 |
  reported by the E2 runner.

## Confirmations (rows I checked and could not falsify)

Each row's GAP quote was re-located verbatim at the cited line in the `v0.29.0` raw file (local mirror
agrees; source files carry no state token — HTTP 200). Absence re-tested with ≥3 new phrasings; the probes
marked "my own" were re-run independently by me.

- E2:E2 — `vllm/utils/b12x.py:38`; `"b12x._lib.intrinsics"` → my own `N=0`; runner's four phrasings also 0
  (vLLM "scale swizzle" hits are MoE oracle-migration work; FlashInfer hits are scale-factor conventions).
  HIGH stands.
- E2:E4 — `b12x.py:66`; exact string `"B12X regular block-FP8 GEMM is not supported"` → my own `N=0`;
  #54996 (test stabilisation) is unrelated. HIGH stands.
- E2:E7 — `prefix_prefill.py:887`; `"FP8 output not supported with alibi"` → my own `N=0`; runner's four
  phrasings 0 (ROCm/AITER fused-quant PRs never mention alibi). MEDIUM stands.
- E2:E8 — `fused_moe/utils.py:377-380, 400-404`; my own `moe_kernel_quantize_input` → 3 hits, none raising
  the int8/mxfp8 branches (#42093 is a GPT-OSS CI failure; #39022/#30621 stale); the runner read the only
  exact-phrase hit (#43983) in full and it is test-only. HIGH stands.
- E2:E11 — `modular_kernel.py:1252, 1414`; `"alternative schedules followup"` → my own `N=0`; the runner's
  `finalize_async` probes raise only the contract, never the unpack refactor. HIGH stands.
- E2:E12 — `v1/engine/core.py:501-503`; no thread proposes propagating abort-vs-stop into the scheduler
  (#46240, #52852 are different defects). MEDIUM stands.
- E2:E13 — `backend_lm_format_enforcer.py:191`; `"does not support grammar specifications"` → my own
  `N=0`; #51660 documents a different lm-format-enforcer rejection. HIGH stands.
- E2:E14 — `backend_outlines.py:210`; same exact-string probe `N=0`; SGLang and transformers sweeps 0.
  HIGH stands.
- E2:E16 — `parser/mistral.py:387`; `MistralParser structured_outputs` → my own `N=0`; nearest is #15551
  (a different layer/mechanism, xgrammar tokenizer rejection). HIGH stands.
- E2:E18 — `tokenizers/mistral.py:347-350`; `"text_pair" MistralTokenizer` → my own `N=0`; #30200/#22121
  unrelated. HIGH stands.

## Could not verify

- **E2:E1's** #49076, **E2:E3's** #54435/#47988, **E2:E5's** #5175/#5191, **E2:E6's** #4479,
  **E2:E10's** #20930/#27145, **E2:E19's** #51157/#41949: counter-sources quoted by the E2 runner. They are
  used only to **downgrade** confidence or add a disclosure, never to drop a row, so the direction of risk is
  conservative. The two dropped rows (E9, E15) rest on #12071/#38701 and #51796, which I re-read first-hand.
- Local B12X package tracker: the private `b12x._lib.intrinsics` dependency could in principle be discussed
  inside the B12X package's own (non-GitHub) tracker; no public thread was found.
- arXiv: E2.md cites no arXiv ID (verified: no `arxiv.org/abs/` and no `NNNN.NNNNN` pattern in the file);
  the researcher's three arXiv searches are logged and returned nothing.

## Tally
checked=19 corrected=9 dropped=2 verbatim_relocated=19 unverifiable=0
absence_claims_falsified=2
