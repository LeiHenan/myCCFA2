# Verification report — S7

Verifier: V2 (adversarial), method per `VERIFIER_BRIEF.md`. Same tooling as the S5/S6 reports:
`verify.py quote` + code-aware matching (`vcode.py`) + un-`strip_html`ed matching, and GitHub state
read from the page's embedded payload rather than `fetch.py`'s `STATE-TOKEN` regex. S7 is the only
one of the four files that carries an explicit composite-quoting convention, and its multi-source
quote fields do verify fragment-by-fragment (`MATCH` / `MATCH-FRAGMENTS`), so almost all of this file
survived.

## Corrections (rows I changed)

- ROW: C7.2 | FIELD: STATE and S3B (and REASON-MAY-HAVE-EXPIRED) | WAS: "STATE: PR #38952 closed, NOT merged (`STATE-TOKEN: closed`, no merged token); successor #39635 shows `STATE-TOKEN: merged`." … "S3B: SOLVED (merged #39635 '[SM120][MLA] Fix FlashInfer MLA DCP/MTP decode path')" … "REASON-MAY-HAVE-EXPIRED: PARTLY — the 'superseded' reason expired the moment #39635 merged" | NOW: "#39635 is **CLOSED, not merged** (page payload `"state":"CLOSED"`, `"mergedTime":null`; UI shows 'Closed / voipmonitor wants to merge 1 commit'). Its author closed it himself on 2026-08-20: 'Closing this pull request because the implementation is not maintained against the current vLLM main branch and has no active maintainer review. This closure does not imply that the reported behavior is invalid.'" → S3B: **NO-ACTIVE-WORK-FOUND** (the claimed successor never landed); REASON-MAY-HAVE-EXPIRED: PARTLY — the supersede claim in #38952's body did **not** take effect; the underlying SM120 sparse-indexer constraint is still what it was. | REASON: the `STATE-TOKEN: merged` line the block relied on is a false positive — the word comes from cross-referenced PRs in the timeline, and the same regex reports `merged` for other closed-unmerged PRs (verified on #44405/#41316 in S5). | EVIDENCE: https://github.com/vllm-project/vllm/pull/39635 | `fetch.py raw` (payload state CLOSED, 490,813 B) + `fetch.py gh` timeline ("voipmonitor closed this / Aug 20, 2026"), HTTP 200
- ROW: D7.6 | FIELD: HARDWARE QUOTE (fragment 1) and URL | WAS: "The **Context Parallelism** row offers DCP4, validated on 4x GB300 (TP4/EP4) with both KV pairings and adaptive MTP 5/1/6; it requires the current release image, which carries the TileLang LSE fix." | NOW: re-quote from the live file: "The **Context Parallelism** row offers DCP4, validated on 4x GB300 with both KV pairings on the earlier TP4/EP4 recipe and queued for re-measurement on the current command; it requires the current release image, which carries the TileLang LSE fix." | REASON: the file was edited upstream after the researcher's snapshot. The quoted wording matches the 2026-09-11 local mirror byte-for-byte but **is not present at the cited live URL** — the live sentence moved "(TP4/EP4)", deleted "on the earlier TP4/EP4 recipe and queued for re-measurement on the current command", and inserted "adaptive MTP 5/1/6" (which on the live page appears in a different sentence). Live 16,062 B vs mirror 15,846 B. | EVIDENCE: https://raw.githubusercontent.com/sgl-project/sglang/main/docs/cookbook/autoregressive/GLM/GLM-5.3-Flash.mdx | `fetch.py raw`, HTTP 200 + `difflib` mirror-vs-live diff (both fetched)
- ROW: D7.6 | FIELD: HARDWARE QUOTE (fragments 2 and 3) and URL | WAS: fragment 2 "--attn-cp-size 8 \ --enable-prefill-cp \ --cp-strategy interleave \" marked "[second, continuous, GLM-5.2 cookbook]" and fragment 3 "which splits the sequence equally across `--attn-cp-size` ranks during attention forward." | NOW: re-point both to their real sources: `docs/cookbook/autoregressive/GLM/GLM-5.3.mdx` (≈ll. 251-255) and/or `GLM-5.2.mdx` (≈ll. 263-267) | REASON: neither fragment exists in the cited file — `GLM-5.3-Flash.mdx` has **0** occurrences of `attn-cp-size`, `enable-prefill-cp` and `cp-strategy` (counts verified on the raw fetch). A quote whose URL field names one page and whose text comes from two other pages is not verifiable as written. | EVIDENCE: https://raw.githubusercontent.com/sgl-project/sglang/main/docs/cookbook/autoregressive/GLM/GLM-5.3.mdx ; https://raw.githubusercontent.com/sgl-project/sglang/main/docs/cookbook/autoregressive/GLM/GLM-5.2.mdx | `fetch.py raw`, HTTP 200 + `grep -rn` over the local mirror for all three fragments
- ROW: (search log, not a row) | FIELD: "Mechanical self-checks run before hand-off … State re-reads by raw-HTML extraction" | WAS: "`STATE-TOKEN: merged` for #21217, #39635, #5949" | NOW: "#21217 merged, #5949 merged, **#39635 closed-unmerged**" | REASON: same false positive as the C7.2 correction; the file's own stated verification method produced the wrong state token here. | EVIDENCE: https://github.com/vllm-project/vllm/pull/39635 | `fetch.py raw` payload, HTTP 200

## Confirmations (rows I checked and could not falsify)

- **C7.3 — the NSA claim is intact.** Issue #43235 re-read as **OPEN** (labels `RFC`, `stale`); both
  quoted sentences MATCH verbatim in the body, including "The empirical evidence is clear: after 7+
  days of testing every available NSA+MTP Docker image on 8×RTX PRO 6000, none are production-viable.
  NSA on SM120 is a dead end." The only non-author comment is the automated stale-bot notice, exactly
  as the row states; the row correctly refuses to treat it as a closure reason. (See Notes for the
  open-artifact-in-ABANDONED question.)
- A7.1 — the JSON fragment is verbatim in `config.json` (fetched; `"sliding_window": null`,
  `"use_sliding_window": false`, `max_window_layers` present). No SWA option for Qwen3-4B.
- A7.2 — the two doc fragments MATCH; the qwen2.py assert literals and the decoder-layer construction
  MATCH. (Fragment 1 differs only by stripped backticks around `config.json` — see Notes.)
- A7.3 — both python fragments MATCH (`gpt_oss.py` alternating layer, `gemma3.py` `layer_types`).
- A7.4/A7.5 — the `Sink` and `Sparse` legend lines and the full `TRITON_ATTN` and
  `FLASHINFER_MLA_SPARSE_SM120` rows MATCH on the versioned docs page.
- A7.6 — both source gates MATCH; `fa_utils.py` prefers FA4 only for `major == 10`, else FA2.
- A7.7 — the MINIMAX_M3_SPARSE description MATCHes the docs page word-for-word.
- A7.8 — the DSA doc sentence MATCHes once markdown link syntax is accounted for (the block keeps the
  `[DeepSeek V3.2](…)` link; the extractor drops the URL — same words); the SGLang `main`
  SM100/SM120 comment block MATCHes; and the absence claim is confirmed —
  `https://raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py`
  returns exactly `404: Not Found` (14 B) as the block states.
- A7.9 — both FlexAttention docstrings MATCH.
- A7.10 — the chunked-prefill doc paragraph and the `flash_attn_varlen_func` call fragment MATCH.
- A7.11 — the context-extension sentence MATCHes (backticks differ only).
- A7.12 — both fragments MATCH; issue #41236 re-read as CLOSED/`COMPLETED`; PR #37089 CLOSED.
- A7.13 — arXiv:2501.15383 title "Qwen2.5-1M Technical Report" and the quoted sentence MATCH.
- A7.14 — both fragments MATCH (`enable_prefix_caching: bool = True`; SGLang `chunked_prefill_size`).
- B7.1 — issue #2860 OPEN and PR #2862 OPEN, both re-read from payloads (no `mergedTime`); both
  quoted fragments MATCH. The row's own falsification check reproduces: vLLM `main`
  `_is_fa4_supported()` still accepts only capability families 90/100/110 ("FA4 is only supported on
  devices with compute capability 9.x, 10.x, or 11.x"), so FA4-on-sm120 is still gated off.
- B7.2 — issue #50705 OPEN and PR #54267 OPEN; both quotes MATCH.
- B7.3 — issue #48168 OPEN; the roadmap fragment MATCHes.
- C7.1 — PR #21217 MERGED (`2025-07-19T20:53:17Z`); the STATED REASON MATCHes; the registry line
  `"Phi3SmallForCausalLM": "0.9.2"` is present under `_PREVIOUSLY_SUPPORTED_MODELS`
  (`vllm/model_executor/models/registry.py:766`).
- C7.4/C7.5/C7.6/C7.7/C7.8/C7.9 — all quoted paper fragments MATCH; arXiv IDs 2410.13276, 2407.02490,
  2408.07092, 2502.11089, 2502.13189, 2406.10774 all read back with titles matching the block claims.
- C7.10 — PR #5949 re-read as MERGED (`2025-08-07T06:49:37Z`); the Todo quote MATCHes; the absence
  check reproduces (`v0.5.19` `attention_registry.py`: 0 hits for `block_sparse`/`blocksparse`, and
  `dsa`/`nsa` are the sparse registrations; `nsa` is a deprecated alias that warns).
- C7.11 — arXiv:2309.00071 YaRN, title + quoted sentence MATCH.
- C7.12 — arXiv:2402.13753 LongRoPE, title + quoted sentence MATCH.
- C7.13 — arXiv:2310.05209 "Scaling Laws of RoPE-based Extrapolation", title + quote MATCH.
- C7.14 — arXiv:2004.05150 Longformer, title + quote MATCH; the cross-referenced BigBird ID reads back
  as arXiv:2007.14062 "Big Bird: Transformers for Longer Sequences".
- D7.1 — arXiv:2310.01889 title + hardware sentence MATCH ("both single DGX A100 server with 8 GPUs and
  distributed 32 A100 GPUs" — no GPU-count words removed).
- D7.2 — arXiv:2405.07719 title + hardware sentence MATCH ("two 8xA800 nodes … sequence length 208K").
- D7.3 — arXiv:2406.18485 title + both hardware fragments MATCH ("8 physical nodes, each equipped with
  8 NVIDIA Ampere GPUs connected by NVLINK. Each node has four 200 HDR NICs"; "To scale training with
  1M sequence length to 2048 GPUs").
- D7.4 — arXiv:2411.17116 title + both fragments MATCH ("sharding attention across multiple hosts";
  "using 8 A100 GPUs").
- D7.5 — the three context-parallel doc fragments MATCH, including "Both approaches are under active
  development."

## Could not verify

- D7.6 — the *content* is now verified in three places (see corrections), but the row as written
  cannot be verified at its own URL; and because the upstream page changed, no stable quote can be
  pinned to `main` without a commit-pinned URL. Recommend pinning the D7.6 cookbook URL to a commit
  (or to the release tag the rig runs) before it is used.
- A7.4/A7.5 — I verified the strings on the versioned `docs.vllm.ai/en/v0.29.0/` page, which is
  mutable in principle; the extracted table rows are the renderer's text, not the upstream generator,
  so column semantics are as-flattened (the block says so).

## Notes

- Corrected rows in this file are C7.2 and D7.6 (D7.6 carries two corrections); the search-log entry
  fixed above is not a row, so it is not counted in `corrected`.
- **C7.3 bucket question (not applied as a correction).** The brief's ABANDONED check asks "is the
  artifact actually still open?" — for C7.3 the artifact (issue #43235) **is still OPEN**. The row
  discloses this explicitly and frames itself as an abandoned-*attempt* record (the author's NSA-on-
  consumer-GPU effort is what is dead, not the issue). Literally applied, the brief's rule would move
  it to OPEN; the OPEN section is capped at 3 rows (B7.1-B7.3) and none of those is weaker, so the
  row's placement is defensible **only** because the STATE field is honest about the artifact being
  open. Flagging for the orchestrator rather than silently re-bucketing.
- **Formatting-only deviations (words unchanged, not corrected):** A7.2 ("`config.json`" → "config.json"),
  A7.8 (markdown link syntax retained), A7.11 (backticks stripped). All three fail the stock checker
  only on punctuation/markup; verified word-for-word.
- **`fetch.py` STATE-TOKEN is unsafe for merged-vs-closed.** It returned `merged` for #39635 (closed,
  `mergedTime` null) from timeline cross-references. This is the second file in which that regex
  produced a wrong state claim; the fix is to read the payload object keyed by `"number": <n>`.
- The file's own "Mechanical self-checks" list is otherwise accurate: I reproduced the #2860/#2862/
  #50705/#48168/#43235 OPEN reads, the #21217/#5949 MERGED reads, the #54267 open read, the #38952
  closed read, and the vLLM-`main` FA4-gate falsification attempt — only the #39635 entry was wrong.
- arXiv check-by-readback for this file: 2501.15383, 2410.13276, 2407.02490, 2408.07092, 2502.11089,
  2502.13189, 2406.10774, 2309.00071, 2402.13753, 2310.05209, 2004.05150, 2007.14062, 2310.01889,
  2405.07719, 2406.18485, 2411.17116 — 16/16 titles match the claims; no fabricated IDs.

## Tally
checked=37 corrected=2 dropped=0 verbatim_relocated=36 unverifiable=0
