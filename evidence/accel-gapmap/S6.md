# S6 — CPU-side / host-bound decode and engine-loop overhead

Researcher: S6. Target rig: ONE RTX PRO 6000 Blackwell 96 GB (sm120), driver 580, 208 CPU cores,
single node, no NVLink. vLLM 0.29.0, SGLang 0.5.19, Qwen3-4B dense.
All items below are on the CPU/host axis only; KV-cache and speculative-decoding rows are excluded.

---

## A. CLOSED (answered / shipped)

### A1.1 | CLOSED
QUESTION: Does vLLM document a regime where decode/serving is limited by CPU rather than GPU, and what does under-provisioning do to the engine loop?
WHO: vLLM maintainers (docs)
ARTIFACT: `docs/configuration/optimization.md` § "CPU Resources for GPU Deployments" ; `docs/design/arch_overview.md` § "V1 Process Architecture"
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/configuration/optimization.md ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/arch_overview.md
STATUS: shipped in v0.29.0 (both docs pages present at the rig's release tag)
EVIDENCE: READ BODY
QUOTE: "Using fewer physical CPU cores than processes will cause contention and significantly degrade throughput and latency. The engine core process runs a busy loop and is particularly sensitive to CPU starvation."
NOTE (second continuous quote, from `docs/design/arch_overview.md` § "V1 Process Architecture", covering the frontend↔engine IPC hop): "It communicates with the engine core process(es) via ZMQ sockets."

### A1.2 | CLOSED
QUESTION: Was per-step Python input-preparation CPU cost actually eliminated by running the model runner ahead of the GPU, and what did that cost in feature compatibility?
WHO: benchislett (authored); merged by vLLM maintainers
ARTIFACT: PR #23569 "[Perf][V1] Fully overlap model execution"; follow-on PR #24219 "[Core] Support async scheduling with uniproc executor"
URL: https://github.com/vllm-project/vllm/pull/23569 ; https://github.com/vllm-project/vllm/pull/24219
STATUS: merged (both). #24219 records the delivered magnitude: "This provides most of the speedup of that PR, like +22% rather than +25%."
EVIDENCE: READ BODY
QUOTE: "This results in an elimination of the CPU overhead of input preparation and sampling."
NOTE (second continuous quote, the compatibility price paid for that elimination, same body): "Currently, this PR is not compatible with: Pipeline Parallelism / Speculative Decoding / Guided Decoding"

### A1.3 | CLOSED
QUESTION: Why do the vLLM authors consider per-step CPU tensor construction in Python a first-order problem, and what did they build to remove it?
WHO: vLLM maintainers (Model Runner V2 design doc)
ARTIFACT: `docs/design/model_runner_v2.md` § "1. Persistent Batch"
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/model_runner_v2.md
STATUS: shipped in v0.29.0
EVIDENCE: READ BODY
QUOTE: "V1 introduced persistent batches to minimize CPU overhead during input preparation. When requests are scheduled for a step, the model runner must construct contiguous input tensors (for example, block tables and per-request temperature values) to feed into the model. Building these tensors from scratch each step is often very slow in Python, especially for large tensors like block tables."

### A1.4 | CLOSED
QUESTION: Does an over-large PyTorch/OpenMP intra-op thread pool measurably slow the engine loop, and what is the shipped fix?
WHO: njhill (vLLM maintainer) — authored and merged
ARTIFACT: PR #49919 "[Core] Explicitly manage torch CPU threads in workers"
URL: https://github.com/vllm-project/vllm/pull/49919
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "torch defaults its intra-op thread pool to the host core count, ignoring process affinity, cgroup CPU quotas, and co-located worker processes. Any torch CPU op above the parallel_for grain size (32k elements) fans out across that pool, and the OpenMP workers then spin-wait after every parallel region - stealing cycles from the engine loop's serial code and, in containers, burning the CFS quota so that the entire process is throttled for the rest of the quota period."
NOTE (second continuous quote from same body, shipped behaviour): "After warmup completes in compile_or_warm_up_model() : set torch threads to 1. No steady-state CPU op benefits from intra-op parallelism; it only creates spin-wait contention and quota burn."

### A1.5 | CLOSED
QUESTION: Was there a measured per-step CPU regression from building/pinning a host tensor inside the decode step, and how big was it?
WHO: vLLM issue reporter; closed as completed
ARTIFACT: Issue #49013 "[Perf] ~2x decode throughput regression for structured outputs since #45424: apply_grammar_bitmask staging rewrite (bisected to commit, file, and hunk)"
URL: https://github.com/vllm-project/vllm/issues/49013
STATUS: closed (stateReason COMPLETED); root cause fixed by the same per-step-CPU-work axis as PR #49919
EVIDENCE: READ BODY
QUOTE: "For a 262k-vocab model at max_num_seqs=64 with k=5 ngram speculation this is a ~(384 x 8192) int32 = ~12.6 MB freshly allocated (normally pinned) host tensor per engine step, plus the reorder loop writing through the numpy view of that pinned buffer, freed again at the end of the step. This path runs every step for every batch containing structured-output requests."

### A1.6 | CLOSED
QUESTION: How much of a decode step was measured to be scheduler bookkeeping, and what was merged to cut it?
WHO: youngkent (authored); merged by vLLM maintainers
ARTIFACT: PR #12094 "[V1][Perf] Reduce scheduling overhead in model runner after cuda sync"
URL: https://github.com/vllm-project/vllm/pull/12094
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "The change should reduce scheduling overhead between decode iterations by ~20%. (See attached gpu trace)"

### A1.7 | CLOSED
QUESTION: Which specific CPU tensor operation in `compute_slot_mapping` was the measured bottleneck?
WHO: linzebing (authored); merged by vLLM maintainers
ARTIFACT: PR #22940 "[Core] direct indexing on self.block_table_np in compute_slot_mapping"
URL: https://github.com/vllm-project/vllm/pull/22940
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "yes tensor.flatten and np.ravel are similar. Looks like the main overhead is torch tensor's indexing is slow; the numpy conversion is relatively cheap"

### A1.8 | CLOSED
QUESTION: Was the frontend↔engine ZMQ serialization cost of tensors/ndarrays solved?
WHO: njhill (vLLM maintainer) — authored; merged
ARTIFACT: PR #13790 "[V1] Zero-copy tensor/ndarray serialization/transmission"
URL: https://github.com/vllm-project/vllm/pull/13790
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "Update the custom msgpack encoding/decoding to work with lists of buffers so that the backing data of tensors/numpy arrays contained in messages is sent directly by zmq without copying."

### A1.9 | CLOSED
QUESTION: Is `pin_memory()` in the per-step H2D path free, or does it cost host time under concurrency?
WHO: sbeurnier (authored); merged by vLLM maintainers
ARTIFACT: PR #37006 "[V1] Remove pin_memory() in async_copy_to_gpu to fix sporadic stalls"
URL: https://github.com/vllm-project/vllm/pull/37006
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "Under sustained high-concurrency workloads (e.g., TTS serving at 24+ concurrent requests), x.pin_memory() in async_copy_to_gpu causes sporadic stalls of up to ~12ms per call. This is because pin_memory() calls cudaHostAlloc() under the hood, which takes a global lock in the CUDA driver to pin pages in physical RAM. Under concurrency, threads serialize on this lock."

### A1.10 | CLOSED
QUESTION: What is the canonical published statement that CPU-side work (scheduling, allocation, prefix matching) can dominate an unoptimised engine, and what is the shipped overlap mechanism?
WHO: SGLang Team (LMSYS blog, primary vendor publication)
ARTIFACT: "SGLang v0.4: Zero-Overhead Batch Scheduler, Cache-Aware Load Balancer, Faster Structured Outputs" (blog, 2024-12-04)
URL: https://lmsys.org/blog/2024-12-04-sglang-v0-4/
STATUS: published 2024-12-04; scheduler on by default per the same page
EVIDENCE: READ BODY
QUOTE: "While LLM inference runs on GPUs, there is substantial work that also needs to be done by the CPU, such as batch scheduling, memory allocation, and prefix matching. An unoptimized inference engine can spend as much as half of its time on CPU overhead."

### A1.11 | CLOSED
QUESTION: Is the SGLang scheduler↔detokenizer↔tokenizer-manager ZMQ hop a measured CPU cost, and was it fixed?
WHO: sshleifer (authored); merged by SGLang maintainers
ARTIFACT: PR #32223 "[perf] Assemble flat prompt top logprobs scheduler-side as numpy arrays"
URL: https://github.com/sgl-project/sglang/pull/32223
STATUS: merged
EVIDENCE: READ BODY
QUOTE: "≈ 15 ms per hop × 2 hops ≈ 30 ms of event-loop CPU saved per 32k-position request , on the two busiest singleton loops in the server."

### A1.12 | CLOSED
QUESTION: What is async scheduling documented as *not* compatible with, in the rig's exact version?
WHO: vLLM maintainers (source of record, v0.29.0 tag)
ARTIFACT: `vllm/config/vllm.py` — `async_scheduling` validation block
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/config/vllm.py
STATUS: shipped in v0.29.0 (hard `ValueError` when `--async-scheduling` is set explicitly; silent `False` when left on `None`)
EVIDENCE: READ BODY
QUOTE: "Currently, async scheduling is only supported with EAGLE/MTP/Draft Model/NGram GPU/DSpark kind of speculative decoding"
NOTE (second continuous quote from same file, the other hard-fail branch): "Async scheduling is not compatible with disable_padded_drafter_batch=True."
NOTE (third continuous quote from same file, the default-disable branch): "The current implementation of asynchronous scheduling negatively impacts performance of pooling models, so we disable by default."

---

## B. OPEN (capped at 3)

### B1.1 | OPEN
QUESTION: Are the remaining unpinned H2D staging copies in SGLang's `ForwardBatch.init_new` still stalling the scheduler thread on every batch?
WHO IS STILL ASKING: zhangxin81, SGLang PR #38703 (open, unmerged, awaiting maintainer benchmark request)
URL: https://github.com/sgl-project/sglang/pull/38703
ASKING QUOTE: "torch.tensor(host_list).to(device, non_blocking=True) without a pinned staging buffer is effectively a host-synchronous copy: per CUDA semantics, cudaMemcpyAsync to/from pageable host memory blocks the caller until the copy completes, so non_blocking=True is a no-op there and the calling thread (the scheduler, in this case) stalls on every such copy in the per-batch critical path."
WHAT IS MISSING: Five H2D sites in `forward_batch_info.py` are still unpinned and the author states "Not benchmarked here (no GPU on the dev machine used for this change)"; no maintainer has run the A/B on an H100 as offered.
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #38703)

### B1.2 | OPEN
QUESTION: Does vLLM's automatic GPU→NUMA CPU binding still mis-assign worker CPU lists when the process has a restricted (cpuset/affinity) CPU set — i.e. on a 208-core box running several servers?
WHO IS STILL ASKING: sylvesterkaczmarek, vLLM PR #56353 (open) — addressing OPEN issue #43240
URL: https://github.com/vllm-project/vllm/pull/56353 ; https://github.com/vllm-project/vllm/issues/43240
ASKING QUOTE: "Noticed that when running VLLM on entire system performance is fine , but when running 2 VLLM instances inside pinned to specific cores the performance of each instances differs significantly."
WHAT IS MISSING: The two-server-on-one-host affinity interaction is still open; #56353 is a narrow fix ("Intersect visible memory nodes with process CPU affinity before assigning workers") and explicitly leaves the manual-binding CPU-reservation behaviour out of scope, with no maintainer review yet.
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #56353)

### B1.3 | OPEN
QUESTION: Are there remaining unpinned/reused input buffers that break the "CPU prepares step N+1 while GPU runs step N" contract under async scheduling on Model Runner V2?
WHO IS STILL ASKING: vLLM contributors, open PR #54269 "[BugFix] V2 model runner: guard reused pinned input buffers under async scheduling"; and draft PR #35938 "[Model Runner V2] Reuse constant tensors in input prep" (still `DRAFT`)
URL: https://github.com/vllm-project/vllm/pull/54269 ; https://github.com/vllm-project/vllm/pull/35938
ASKING QUOTE: "Split out reused constant-buffer changes from #35561"
WHAT IS MISSING: Input-prep CPU work on MRV2 is being trimmed piecemeal (constant-tensor reuse, pinned-buffer guards) and #35938 has sat in `DRAFT` rather than merging; no consolidated per-step CPU-budget accounting exists.
EVIDENCE: TITLE + STATE ONLY (both PRs read; the quoted line is the entirety of #35938's description body)
S3B: OCCUPIED (open PR #54269 / draft PR #35938)

---

## C. ABANDONED (attempted, then closed unmerged / reverted / measured negative)

### C1.1 | ABANDONED (scheduler post-step loop)
WHO: ji24077
ARTIFACT: PR #46112 "[Performance] Reduce update_from_output CPU overhead for decode batches"
URL: https://github.com/vllm-project/vllm/pull/46112
STATED REASON: "Where it does NOT help — batch <=16 (loop cost negligible)". [Source: the PR author's own description; the PR was closed unmerged with no maintainer comment on the page.]  The author also records the in-code admission of the bottleneck it targets: "The loop is already noted as a potential performance bottleneck in-code ( NOTE(woosuk) in scheduler.py )".
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `is:open+update_from_output`, `is:open+CPU+overhead` on https://github.com/vllm-project/vllm/issues — only hits are unrelated bugfixes #53799/#56236 and metrics fixes, none targeting `update_from_output` CPU cost)
REASON-MAY-HAVE-EXPIRED: yes — the change's own benefit window is "batch 128-256, decode-only"; the target rig runs Qwen3-4B at small batch where the author measured the loop as negligible, and vLLM has since moved the step loop to Model Runner V2 (shipped in 0.29.0), which is a different post-step path.

### C1.2 | ABANDONED (per-step input preparation: numpy construction)
WHO: Aayushgoyal00
ARTIFACT: PR #36868 "[Core] Skip np.repeat in _prepare_inputs when all requests are decodes"
URL: https://github.com/vllm-project/vllm/pull/36868
STATED REASON: "In this case, np.repeat(arange[:N], [1,1,...,1]) is mathematically equivalent to arange[:N] but performs an unnecessary heap allocation and memory copy." [Source: the PR author's description; closed unmerged, no maintainer closer comment on the page.] The premise it is built on is the author's verbatim claim: "This condition is true on every pure-decode step, which is the dominant steady-state of continuous batching."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `is:open+_prepare_inputs`, `is:open+prepare_inputs` on https://github.com/vllm-project/vllm/issues — open hits are #54269/#35938 on pinned-buffer reuse and #51212 RFC on MRv2 pluggability, none re-landing this `np.repeat` fast path)
REASON-MAY-HAVE-EXPIRED: yes — the patch targets V1's `_prepare_inputs`; vLLM 0.29.0 ships Model Runner V2, whose design doc states it "decouples persistent state tensors from per-step input tensors", so the `np.repeat(arange…)` construction this removed may no longer exist on the default path.

### C1.3 | ABANDONED (overlap of Python input prep with GPU execution)
WHO: Ronald1995
ARTIFACT: PR #23811 "[WIP] implement overlap of prepare_input during execute_model"
URL: https://github.com/vllm-project/vllm/pull/23811
STATED REASON: "The current implementation of execute_model_with_queue will lead to a deadlock." [Source: verbatim review comment on the PR; the PR is closed unmerged.] A second continuous quote from the same review thread: "The logic for when to process items from the queue versus returning early needs to be reconsidered to avoid these problems."
EVIDENCE: READ BODY
S3B: SOLVED (merged #23569 "Fully overlap model execution" + merged #24219 "Support async scheduling with uniproc executor" — see A1.2). No open PR re-attempts a threaded `prepare_inputs` overlap: queries `is:open+prepare_inputs`, `is:open+CPU+overhead`.
REASON-MAY-HAVE-EXPIRED: yes — the deadlock was in a bespoke `exe_queue` threading design for V1; #23569 later achieved the same "overlap of the cpu operations (including prepare_inputs) and the model forward pass" by returning a CUDA tensor reference instead of threads, so the threaded design this PR tried is no longer the live approach.

### C1.4 | ABANDONED (core-loop restructure for async input preparation)
WHO: WoosukKwon (vLLM maintainer) — authored and closed
ARTIFACT: PR #23391 "[Core] Restructure core loop for async input preparation"
URL: https://github.com/vllm-project/vllm/pull/23391 ; closes https://github.com/vllm-project/vllm/issues/23233
STATED REASON: "For async scheduling, this allows overlapping input preparation (and bitmask) with the model execution. Currently, only deserialization of scheduler outputs is overlapped. This could be expanded in future PRs." [Source: the maintainer's own PR description; the PR was closed unmerged.] The blocker recorded on the PR is a correctness one: "The RayDistributedExecutor in vllm/v1/executor/ray_distributed_executor.py has not been updated to implement the new abstract methods prepare_inputs , execute_model , and sample ."
EVIDENCE: READ BODY
S3B: SOLVED (merged #23569 "Fully overlap model execution" + merged #24219; the parent RFC #23233 is now `stateReason: COMPLETED`). No open PR restructures the core loop for input-prep overlap: queries `is:open+prepare_inputs`, `is:open+update_from_output`.
REASON-MAY-HAVE-EXPIRED: yes — the abstention was forced by the Ray executor not implementing the split interface; async scheduling shipped anyway through a different route and the RFC that motivated #23391 is closed as completed.

### C1.5 | ABANDONED (engine/worker busy-loop spin burning host cores)
WHO: nacyot
ARTIFACT: PR #51950 "[Core] Expose MessageQueue busy-loop duration as VLLM_MQ_SPIN_SECONDS"
URL: https://github.com/vllm-project/vllm/pull/51950
STATED REASON: "During active decoding, messages arrive every few milliseconds, so readers never reach the idle path — the hybrid wait degenerates into a permanent spin." [Source: the PR author's description; closed unmerged with no maintainer comment on the page.] The measured consequence, same body: "on machines where CPU and GPU share a package — e.g. NVIDIA DGX Spark (GB10) — it pins 3–4 performance cores at 100% doing no useful work, and becomes the dominant heat source."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `is:open+MessageQueue+busy`, `is:open+busy+loop+CPU` on https://github.com/vllm-project/vllm/issues — no open PR or issue exposes or bounds the shm_broadcast spin duration)
REASON-MAY-HAVE-EXPIRED: no

### C1.6 | ABANDONED (revert of a multimodal device-placement change that tripped the GPU/CPU sync guard)
WHO: khluu — authored the revert
ARTIFACT: PR #52870 "[Revert][MM] Restore multimodal metadata device placement" (reverts #52827)
URL: https://github.com/vllm-project/vllm/pull/52870
STATED REASON: "After #52827 merged, two H200 paths reproducibly tripped the strict GPU/CPU synchronization guard" [Source: the author's description; the PR is closed.] The same body gives the expiry: "Focused forward fix PR #52881 merged as main commit ee117307512f and is now fully exact-validated by Buildkite #84562."
EVIDENCE: READ BODY
S3B: SOLVED (merged #52881 — the forward fix that replaced this revert)
REASON-MAY-HAVE-EXPIRED: yes — the body itself states the replacement fix "#52881 merged as main commit ee117307512f", so the constraint this revert existed to satisfy no longer holds.

### C1.7 | ABANDONED (zero-overhead scheduling, pre-async-scheduling era)
WHO: lizhigong1988
ARTIFACT: PR #15801 "[Feature]Zero overhead schedule support" (state CLOSED, not merged)
URL: https://github.com/vllm-project/vllm/pull/15801
STATED REASON: "After removing these synchronization operations, the CPU cannot immediately obtain the decode result, so a 'placeholder token' needs to be introduced to return a fake result for the next scheduling" [Source: the author's description; the PR is CLOSED with no merger and no additional human comment on the page]. The dependent issue it references, #15781, was closed `stateReason: NOT_PLANNED` after the stale bot ran, so no human closure reason exists on either artifact.
EVIDENCE: READ BODY (PR description); for issue #15781: READ BODY but EXCLUDED as a closure reason per the brief's stale-bot rule
S3B: SOLVED (shipped async scheduling in vLLM; see A1.12). No open PR carries the placeholder-token design: queries `is:open+update_from_output`, `is:open+prepare_inputs`.
REASON-MAY-HAVE-EXPIRED: yes — this was a V0-era design (it edits `llm_engine.py`, `sample.py`, `stop_check.py`, `detokenizer.py`, none of which exist in the V1/V2 loop the rig runs).

### C1.8 | ABANDONED (SGLang decode-path allocation + tokenizer CPU work)
WHO: JIANGBOFENG
ARTIFACT: PR #23930 "[Perf] Optimize alloc_for_decode and stream_output functions" (state CLOSED, not merged)
URL: https://github.com/sgl-project/sglang/pull/23930
STATED REASON: "Goal: The write function can be optimized using Triton and removes an implicit synchronization operation." [Source: the author's description; the PR was closed unmerged, with only an automated code-review bot plus one PEP 8 nit — "The comparison self.input_token_logprobs_val != None should use is not None to follow PEP 8 best practices" — recorded on the page.]
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #38703 pins the remaining unpinned H2D copies in `ForwardBatch` hot paths; open PR #34876 "O(1) slot allocation in DecodeReqToTokenPool.alloc()" — surfaced by query `is:open+alloc_for_decode`)
REASON-MAY-HAVE-EXPIRED: no

### C1.9 | ABANDONED (SGLang scheduler per-pass CPU micro-cuts)
WHO: sshleifer
ARTIFACT: PR #36369 "scheduler: three per-pass CPU micro-cuts from a py-spy wall profile" (state CLOSED, not merged)
URL: https://github.com/sgl-project/sglang/pull/36369
STATED REASON: "Three small scheduler-thread cuts found in a py-spy wall profile at ~1,500-request batches (the scheduler thread was ~99% busy)" [Source: the author's description; the PR was closed unmerged.] The per-item measurements in the same body: "the scheduler thread was ~99% busy"; "_maybe_collect_customized_info : gate at the call sites on logits_output.customized_info is not None — the per-request call overhead alone is measurable (~1.4%)".
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `is:open+scheduler+CPU+overhead`, `is:open+scheduler+thread` on https://github.com/sgl-project/sglang/issues — open scheduler hits are TTFT batching, Kimi-K3 tokenizer encoding, and multimodal metrics, none re-landing these three cuts)
REASON-MAY-HAVE-EXPIRED: yes — all three cuts are calibrated to a "~1,500-request batch" regime ("the scheduler thread was ~99% busy"); at Qwen3-4B small-batch on one GPU the scheduler thread is not in that regime, and one of the three (`record_batch_in_overlap` field-name caching) is specific to the overlap-scheduler path as written then.

### C1.10 | ABANDONED (single-process threaded engine to eliminate ZMQ IPC)
WHO: liusy58
ARTIFACT: PR #25924 "Eliminating IPC Overhead with Free-Threaded Python" — declined at the public SGLang meeting
URL: https://github.com/sgl-project/sglang/pull/25924
STATED REASON: "I discussed this PR at the public meeting. Since we plan to migrate the entire frontend to Rust, this code path will likely be deprecated and removed in the future—even if the GIL ceases to be a performance bottleneck after Python 3.14. Consequently, we will not be moving forward with the PR to refactor the Python frontend at this time. Sorry about that."
EVIDENCE: READ BODY
STATE-NOTE: the PR page's embedded state is still `OPEN`; the maintainer statement above is an explicit refusal to move forward, and no merge occurred.
S3B: OCCUPIED (open issue #22889 "[Feature] Free-Threaded Python (3.14t / nogil) Support for SGLang" — SGLang is pursuing free-threading enablement, but as a separate RFC, not via this threaded-engine refactor; query `is:open+free-threaded`)
REASON-MAY-HAVE-EXPIRED: no — the stated reason is the Rust-frontend migration, which is still live (`[RFC] Sglang non-GPU process rust migration` #23206 remains the active plan and the reason cites it explicitly).

### C1.11 | ABANDONED (CPU/OpenMP thread-affinity init rewrite, redone and dropped)
WHO: louie-tsai (re-clone of #36487)
ARTIFACT: PR #38861 "Omp improvement from PR#36487" (state CLOSED, not merged)
URL: https://github.com/vllm-project/vllm/pull/38861 ; original: https://github.com/vllm-project/vllm/pull/36487
STATED REASON: "clone #36487 for fixing CI/CD issues" [Source: the author's description; closed unmerged.] The concrete defects recorded on the PR are affinity-correctness ones: "The logic in produce_cpu_list for limiting threads per core (SMT) is incorrect. It compares a CPU ID ( cpu from the mask list) directly with a Core ID ( value[0]["core"] ). Since CPU IDs and Core IDs belong to different namespaces, this comparison will fail to correctly identify SMT siblings on most systems".
EVIDENCE: READ BODY
S3B: SOLVED for the underlying capability (merged #36487 "[CPU] Replace OMP initialization"; merged #49919 manages torch threads explicitly, and vLLM 0.29.0 docs ship `--numa-bind` / `--numa-bind-cpus`). The specific unmerged clone was not re-landed: queries `is:open+OMP_NUM_THREADS`, `is:open+numa`.
REASON-MAY-HAVE-EXPIRED: yes — the rejection was "fixing CI/CD issues" on a fork clone plus SMT-sibling-parse defects in `ompmultiprocessing.py`; the shipped `--numa-bind*` path (A1.1) is a different implementation, and #56353 currently patches the same file's affinity handling.

### C1.12 | ABANDONED (measured negative result: overlap that costs more than it saves)
WHO: anonymous RFC author (self-withdrawn)
ARTIFACT: Issue #56126 "RFC: enable dual-batch overlap (DBO) for dense TP models at DP=1"
URL: https://github.com/vllm-project/vllm/issues/56126
STATED REASON: "Withdrawing this — filed in error and not intended for upstream at this time. Apologies for the noise." And, from the same thread, the measured negative result: "The overlap works. The batch split costs more than the overlap saves." with "| 64 | 3167.2 | 2018.0 | −36.3% |" and "| 128 | 4324.1 | 3176.5 | −26.5% |".
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (query: `is:open+overlap+schedule` on both repos; the related dense-DBO enablement is not carried by any open PR)
REASON-MAY-HAVE-EXPIRED: yes — the author's own conclusion names the changed constraint: "on this box, at b ≤ 128, it does not [pay]" because "at these shapes the per-rank GEMMs are weight-streaming bound, not compute bound", and predicts it "should pay where the per-rank GEMM is compute-bound — larger batch, larger hidden per rank, or fewer TP ranks". The target rig is *fewer* TP ranks (one GPU) but also much smaller batch.

---

## D. HARDWARE-RULED-OUT

### D1.1 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: anonymous RFC author (self-withdrawn from vLLM upstream)
ARTIFACT: Issue #56126 "RFC: enable dual-batch overlap (DBO) for dense TP models at DP=1"
URL: https://github.com/vllm-project/vllm/issues/56126
HARDWARE QUOTE: "**Scope caveat.** All measurements below come from a **single machine**: 4× RTX PRO 6000 Blackwell Server Edition (GB202, sm_120), PCIe peer-to-peer, **no NVLink**, driver 580.126.09, CUDA 13.0, vLLM `0.28.1rc1.dev500+g51da0ca66`, model Qwen3.6-27B-FP8 at TP=4."
EVIDENCE: READ BODY
NOTE (why it is ruled out for the rig): the mechanism it fixes is DP/TP-gated — the RFC's own root cause is that "`should_ubatch` is initialised `False` and only ever assigned inside the DP>1 branch" — and the measured benefit is negative at the batch sizes a single GPU would run (see C1.12).

---

## SEARCH LOG (queries actually issued)

GitHub HTML search (server-rendered `issues?q=` endpoint; `api.github.com` not used, `web_fetch` not used):

- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+CPU
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+async+scheduling
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+%22CPU+overhead%22
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+serialization
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+python+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+host
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+host+sync
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+zmq
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+pickle+msgpack
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+prepare_inputs
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+sampler+CPU
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+OMP_NUM_THREADS
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+launch+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+cpu+sync
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+numa
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+revert+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+revert+CPU
- https://github.com/vllm-project/vllm/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+slower
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aclosed+host+bound
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aclosed+CPU+bound+decode
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aclosed+%22CPU+overhead%22
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aopen+busy+loop+CPU
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aopen+spin
- https://github.com/vllm-project/vllm/issues?q=is%3Aissue+is%3Aopen+python+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+update_from_output
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+_prepare_inputs
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+prepare_inputs
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+CPU+overhead
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+MessageQueue+busy
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+OMP_NUM_THREADS
- https://github.com/vllm-project/vllm/issues?q=is%3Aopen+numa
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+overhead
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+CPU
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+schedule
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+%22CPU+overhead%22
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+host+sync
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+overlap
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+tokenizer
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+ScheduleBatch
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+process_batch
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+serialization
- https://github.com/sgl-project/sglang/issues?q=is%3Apr+is%3Aclosed+is%3Aunmerged+host
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+scheduler+CPU+overhead
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+overlap+schedule
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+alloc_for_decode
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+free-threaded
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+scheduler+thread
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+ZMQ
- https://github.com/sgl-project/sglang/issues?q=is%3Aopen+per-step+overhead

Primary pages fetched and read in full (via `python3 fetch.py gh` / `raw` / `get`):

- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/configuration/optimization.md
- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/arch_overview.md
- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/model_runner_v2.md
- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/optimization_levels.md
- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/multiprocessing.md
- https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/config/vllm.py
- https://github.com/vllm-project/vllm/pull/49919 , /issue 49013 , /pull/12094 , /pull/22940 , /pull/13790 , /pull/37006 , /pull/23569 , /pull/24219
- https://github.com/vllm-project/vllm/pull/46112 , /pull/36868 , /pull/23811 , /pull/23391 , /pull/51950 , /pull/52870 , /pull/15801
- https://github.com/vllm-project/vllm/issues/15781 , /issues/23233 , /issues/27222 , /issues/26114 , /issues/29760 , /issues/32140 , /issues/38079 , /issues/43240 , /issues/56126
- https://github.com/vllm-project/vllm/pull/38861 , /pull/36487 , /pull/56353 , /pull/54269 , /pull/35938 , /pull/34462 , /pull/16279 , /pull/31373 , /pull/39592 , /pull/56250 , /pull/49738 , /pull/33195
- https://github.com/sgl-project/sglang/pull/32223 , /pull/23930 , /pull/36369 , /pull/25924 , /pull/3711 , /pull/38703 , /pull/37551 , /pull/25516 , /pull/31468
- https://github.com/sgl-project/sglang/issues/23206 , /issues/22889
- https://lmsys.org/blog/2024-12-04-sglang-v0-4/
- Local source checkouts grepped for flags/config (not cited as evidence, used to locate): `/tmp/sglang-main` (`python/sglang/srt/server_args.py`, `environ.py`, `docs/docs/advanced_features/server_arguments.mdx`, `hyperparameter_tuning.mdx`), `/tmp/vllm-0.28.0`, `/tmp/vllm_src`, `/tmp/vllm_tar/vllm-main`.

arXiv read-back (per brief):
- `python3 fetch.py arxiv 2408.12757` → "NanoFlow: Towards Optimal Large Language Model Serving Throughput" (submitted 22 Aug 2024, v2 25 May 2025). Cited only as the origin of the overlap idea referenced by the SGLang v0.4 blog (A1.10); its own claim is about sequential use of compute/memory/networking within a device, not about CPU overhead.

---

## NEGATIVE FINDINGS

- **No maintainer statement found saying "per-step CPU overhead is a known unsolved problem"** — queries tried: `is:open+CPU+overhead`, `is:open+python+overhead`, `is:issue+is:open+busy+loop+CPU`, `is:issue+is:open+spin` on vllm-project/vllm; `is:open+scheduler+CPU+overhead`, `is:open+per-step+overhead`, `is:open+scheduler+thread` on sgl-project/sglang; plus seventeen `web_search` calls (1–4 queries each) aimed at "known but nobody has fixed it", "we haven't profiled that", "the scheduler is a bottleneck at low batch", "no one is working on this". The closest genuine artifacts are the in-code `NOTE(woosuk)` admission quoted in C1.1 and the shipped docs statement in A1.1; neither is an "unsolved / no ETA" statement, so no row was created from them beyond what is quoted.
- **`torch.cuda.set_sync_debug_mode` — nothing found as a vLLM/SGLang documented detection knob.** Queries tried: `is:open+cpu+sync`, `is%3Apr+is%3Aclosed+is%3Aunmerged+cpu+sync` on vllm-project/vllm; `is%3Apr+is%3Aclosed+is%3Aunmerged+host+sync` on sgl-project/sglang; web searches for the API name paired with vLLM/SGLang. The only sync-guard artifact retrieved is the pre-merge H200 sync-guard failure quoted in C1.6; no doc page, flag, or maintainer statement shows it being used to detect a host-bound decode regime. NOT MAPPED.
- **No measured "number of kernel launches per decode step" figure found for the rig's exact stack.** `--enforce-eager` is documented only as a startup-time trade ("Serve without CUDA graphs using `--enforce-eager`. Skips both compilation and CUDA-graph capture for the fastest possible startup, at the cost of steady-state decode performance.") with no per-step launch count. Queries tried: `is%3Apr+is%3Aclosed+is%3Aunmerged+launch+overhead`, `is%3Apr+is%3Aclosed+is%3Aunmerged+overhead` on vllm-project/vllm; web searches pairing "kernel launch overhead" / "number of kernels per decode step" with vLLM and SGLang. The one launch-count measurement retrieved is from an unrelated multi-GPU DBO trace (quoted inside D1.1's source, C1.12). NOT MAPPED.
- **`PYTORCH_CUDA_ALLOC_CONF` / `expandable_segments` effect on step time — nothing primary found.** No vLLM or SGLang doc page at v0.29.0 / 0.5.19 attributes a step-time change to the allocator config, and no issue/PR in the closed-unmerged sweeps above measured it. Queries tried: `is%3Apr+is%3Aclosed+is%3Aunmerged+CPU`, `is%3Apr+is%3Aclosed+is%3Aunmerged+overhead`, plus web searches for `PYTORCH_CUDA_ALLOC_CONF` + vLLM + step time. NOT MAPPED.
- **`cudaMalloc` in the hot path — no evidence found.** Searches as above plus `is:issue+is:open+spin`; the only allocator-lock hot-path artifact retrieved is `cudaHostAlloc`'s global lock in `pin_memory()` (A1.9), which is host-pinned allocation, not `cudaMalloc`. NOT MAPPED.
- **No SGLang maintainer statement characterizing `ScheduleBatch` / `ForwardBatch` / `Sampler` as the *named* per-step CPU hot spots.** The SGLang evidence that does exist is quantitative but from third-party/contributor measurements (C1.9's py-spy profile, B1.1's per-copy stall analysis). Queries tried: `is%3Apr+is%3Aclosed+is%3Aunmerged+ScheduleBatch` on sgl-project/sglang (returned #36369, #32532, #32463 — all contributor PRs, none maintainer analyses); `web_search` for "ScheduleBatch ForwardBatch overhead per step github issue". NOT MAPPED as a maintainer statement.
- **`--ipc-only` and `MQLLMEngine` serialization-cost threads — no primary artifact retrieved.** Query `is%3Apr+is%3Aclosed+is%3Aunmerged+zmq` on vllm-project/vllm returned routing/port-binding and `MessageQueue` items, not an `MQLLMEngine` serialization-cost thread; `--ipc-only` did not appear in the v0.29.0 optimization docs read in full. NOT MAPPED.
- **Only one genuinely hardware-ruled-out item on this axis.** The CPU/host axis is largely hardware-agnostic (it is the *absence* of GPU work that exposes it), so `is:pr is:closed is:unmerged` sweeps produced no further >1-GPU / >96 GB / multi-node items whose applicability is decided by the rig's hardware except D1.1. Queries tried: all 19 `is%3Apr+is%3Aclosed+is%3Aunmerged+*` vLLM sweeps listed above plus `is%3Aissue+is%3Aclosed+host+bound` / `+CPU+bound+decode`.
- **Excluded per the brief's stale-bot rule (recorded here rather than as rows):** vLLM issue #15781 "[Feature]: Implementation of Zero Overhead Scheduling" (human comments exist — "Looks promising. We are actively trying to reduce overhead while maintaining a hackable codebase. Could you share your PR and potentially join our slack for more in depth discussion?" and, later, "Why is there no follow-up to this?" — but the closure itself is the `github-actions` stale bot with `stateReason: NOT_PLANNED`, so no human closure reason exists) and vLLM issue #27222 "[Performance][Qwen3-next] Decrease huge CPU overhead" (same stale-bot closure; its useful human line is "I don't have specific way how we can speed up CPU time of GSN attn. It requires further investigation." — an investigation gap, not a closure reason).
