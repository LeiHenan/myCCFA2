# Verification report — S10 (measurement methodology)

Method: all 50 blocks of `S10.md` and all 26 blocks of `S10_gh_sub.md` re-parsed (multi-line field values
re-assembled), every QUOTE / STATED REASON / ASKING QUOTE re-fetched and re-located at its URL with
`python3 verify.py quote` and with a raw-text pass; every GitHub artifact's `"state":"…"` /
`"stateReason":"…"` token re-read; every arXiv id re-read with `fetch.py arxiv`; both withdrawal rows' arXiv
`Comments:` fields extracted from the abs-page HTML and diffed character-by-character.

## Corrections (rows I changed)

- ROW: A8 | FIELD: QUOTE | WAS: `"Coincidentally, I was using RandomDataset today and had found that random seed is not actually used in RandomDataset. Hence, …"` | NOW: `insert the source's inline Markdown link, or add a NOTE: the page reads "…had found that [random seed is not actually](https://github.com/vllm-project/vllm/blob/7108934142801dad2fd8ac42aec8b1699e37ff5d/benchmarks/benchmark_dataset.py#L292-L296) used in RandomDataset."` | REASON: `verify.py quote` → MISMATCH 0.70; the retrieved comment body embeds a markdown link whose *link text* is the phrase the row quotes, so the string as written is not contiguous in any retrieval variant. No word is altered or removed — this is a markdown-link rendering divergence, not a fabrication. | EVIDENCE: https://github.com/vllm-project/vllm/issues/20015 | fetch.py gh, HTTP 200
- ROW: B2 | FIELD: QUOTE | WAS: `… neither corresponds to \"this run\".` (literal backslashes in the file) | NOW: `… neither corresponds to "this run".` | REASON: the PR body reads `window — neither corresponds to "this run".` with plain quotes; the block-format escaping was left in the quoted text, so `verify.py quote` → NEAR 0.965 with `missing_tail` = `\"this run\".`. | EVIDENCE: https://github.com/vllm-project/vllm/pull/46938 | fetch.py gh, HTTP 200
- ROW: C3 | FIELD: STATED REASON | WAS: `My \"fixed\" harness counted …` | NOW: `My "fixed" harness counted …` | REASON: same escaping artefact; the issue comment reads `My "fixed" harness counted`. | EVIDENCE: https://github.com/vllm-project/vllm/issues/54677 | fetch.py gh, HTTP 200
- ROW: C16 | FIELD: STATED REASON | WAS: `"* Values from controlled progressive experiment. The 30-run benchmark (Table 2) reports 21.0 tok/s …"` | NOW: `"∗ Values from controlled progressive experiment. The 30-run benchmark (Table 2 ) reports 21.0 tok/s …"` | REASON: the arXiv HTML footnote marker is U+2217 `∗` (not ASCII `*`) and the citation renderer pads the bracket as `(Table 2 )` with a space before `)`. Two character-level divergences inside the quotation marks; the row carries no NOTE for them. | EVIDENCE: https://arxiv.org/abs/2604.02344 (html/2604.02344v1) | fetch.py get, HTTP 200
- ROW: C22 | FIELD: STATED REASON | WAS: `"We are also aware of higher numbers like [23]. However, we do not compare with them …"` | NOW: `"… higher numbers like [ 23 ] . However …"` | REASON: the source's citation renderer pads the reference as `[ 23 ]` and puts a space before the full stop. | EVIDENCE: https://arxiv.org/abs/2507.19427 (html v1) | fetch.py get, HTTP 200
- ROW: C23 | FIELD: STATED REASON | WAS: `"… AsyncTLS achieves 1.84× and 4.70× higher throughput than FA …"` | NOW: `"… AsyncTLS achieves 1.84 × \times and 4.70 × \times higher throughput than FA …"` | REASON: the arXiv HTML emits the rendered `×` *and* the raw LaTeX `\times`; the row silently normalised the duplication away. (Rows C14, C26 and D4 in the same file reproduce this exact class of artefact verbatim and NOTE it; this one neither reproduces nor notes it.) | EVIDENCE: https://arxiv.org/abs/2604.07815 (html v1) | fetch.py get, HTTP 200
- ROW: C25 | FIELD: STATED REASON | WAS: `"… at extremely high throughputs (BS > 256), the Python Global Interpreter Lock …"` | NOW: `"… at extremely high throughputs ( B S > 256 BS>256 ), the Python …"` | REASON: same unlabelled math-rendering duplication (the source doubles the inline math). | EVIDENCE: https://arxiv.org/abs/2604.09557 (html v1) | fetch.py get, HTTP 200
- ROW: C29 | FIELD: STATED REASON | WAS: `"… leading gpt-oss-puzzle-88B to achieve a 2.82× speedup on a single H100 GPU …"` | NOW: `"… to achieve a 2.82 × 2.82\times speedup on a single H100 GPU …"` | REASON: same unlabelled duplication. | EVIDENCE: https://arxiv.org/abs/2602.11937 (html v1) | fetch.py get, HTTP 200
- ROW: D1 | FIELD: HARDWARE QUOTE | WAS: `"… only uses 32 GPUs (§ 7.3). If the deployment scale expands significantly, network congestion becomes a critical issue [29], resulting in …"` | NOW: `"… only uses 32 GPUs (§ 7.3 ). … critical issue [ 29 ] , resulting in …"` | REASON: two divergences — a space before the closing parenthesis after the section reference, and the citation renderer's bracket padding. | EVIDENCE: https://arxiv.org/abs/2507.19427 (html v1) | fetch.py get, HTTP 200
- ROW: D2 | FIELD: HARDWARE QUOTE | WAS: `"… a 2.82× speedup on a single H100 GPU in a 64K/64K scenario, and a 2.44× speedup in a 4K/4K scenario."` | NOW: `"… a 2.82 × 2.82\times speedup … and a 2.44 × 2.44\times speedup in a 4K/4K scenario."` | REASON: same unlabelled duplication (both counts). | EVIDENCE: https://arxiv.org/abs/2602.11937 (html v1) | fetch.py get, HTTP 200

## Confirmations (rows I checked and could not falsify)

- **The two assigned high-stakes vLLM-0.29.0 harness claims are CONFIRMED.**
  `benchmarks/benchmark_serving.py` @ v0.29.0 is a 483-byte shim: it prints "DEPRECATED: This script has
  been moved to the vLLM CLI. / Please use the following command instead: / vllm bench serve" and calls
  `sys.exit(1)`. `vllm/benchmarks/serve.py` @ v0.29.0 contains the post-parse injection verbatim:
  `# when using random datasets, default to ignoring EOS` / `# so generation runs to the requested length`
  / `if (args.dataset_name in ("random", "random-mm") and args.backend in OPENAI_COMPATIBLE_BACKENDS):`
  / `args.ignore_eos = True`, and `datasets.py` defaults `--dataset-name` to `random` — so the default
  `vllm bench serve` run does suppress EOS.
- **The two WITHDRAWN papers are CONFIRMED verbatim from the arXiv `Comments:` field at the abs URL.**
  2406.18173 → "This article was not accepted, and its quality is not very good. Therefore, we have decided
  to withdraw the submission and will not resubmit it elsewhere"; page banner "This paper has been withdrawn
  by Wenhao Li", every version line "(withdrawn)", "No license for this version due to withdrawn".
  2505.17460 → "Due to fundamental errors in the methodology, I have to withdraw this paper"; banner
  "This paper has been withdrawn by Xinran Zheng". Both titles match the rows.
- All 25 arXiv ids in `S10.md` read back with matching titles (C12–C29, C31, C32, D1–D5, plus the AWQ id
  2306.00978 in C28). The S13↔S10 "EnergyLens" name collision is real but benign: 2605.10556 is
  "EnergyLens: Interpretable Closed-Form Energy Models for Multimodal LLM Inference Serving" (S10 D5) and
  2605.14249 is "EnergyLens: Predictive Energy-Aware Exploration for Multi-GPU LLM Inference Optimization"
  (S13 D.5.1) — two different papers, both read back.
- GitHub state tokens match every stated status in both files: A7 #52884 closed `COMPLETED` + #53920 merged;
  A9 #54101 closed `COMPLETED`; A12 #6294 closed `COMPLETED` + #3458 merged; C6 #1630, C7 #998, C8 #38398,
  C9 #24684, C10 #18712 all closed `COMPLETED`; B1 #56653, B2 #46938 open; B3 / B.gh5 sglang #38868 open;
  B.gh6 sglang #38917 open; A.gh14 #8391 closed `COMPLETED`. A8's closure attribution is right: the page
  shows "Closed as not planned" with label `stale` ("Over 90 days of inactivity"), i.e. the inactivity bot.
- All 26 `S10_gh_sub.md` rows' quotes relocate verbatim (0 corrections needed there); every `A.gh*` row's
  stateReason matches its "closed (stateReason COMPLETED)" claim.
- A1–A12 and D3–D5's source quotes (argparse blocks, doc warning blocks, docstrings) relocate verbatim once
  the multi-line field values are re-assembled; the apparent MISMATCHes produced by the shipped
  `verify.py` parser on A2/A3/A4/A5/A11 are continuation-line truncation, not row defects.

## Could not verify

- Nothing unverifiable. All ten divergences above are character-level rendering artefacts in which **no word
  and no number is added, removed or reordered**; the substance of every quote is on the page.

## Notes

- C1, C2 and C3 are ABANDONED rows whose artifact (issue #54677) is still **OPEN**. The ABANDONED
  classification rests on the author's own retraction (a human statement) rather than on closure, which is
  consistent with this file's section definition ("measured negative results, defective measurements, and
  papers undercut by their own stated limitations") — flagged, not changed.
- The `=>` takeaway lines that follow several QUOTE fields (A3, A4, A5, A10, A11) are researcher inference,
  not source text; `verify.py`'s parser correctly drops them into `_extra`, but a stricter block parser
  swallows them into the quote. A leading `=>` on its own line (as A3/A5 already do) would make the
  boundary unambiguous everywhere.

## Tally
checked=76 corrected=10 dropped=0 verbatim_relocated=66 unverifiable=0
