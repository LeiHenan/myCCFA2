# Verification report — E5

Adversarial verification of `E5.md` (SGLang, `main` / 0.5.19) — **16 NEVER-DISCUSSED rows**
(E5.1–E5.16). I did not write E5.md. Every GAP quote was re-fetched from
`raw.githubusercontent.com/sgl-project/sglang/main/...` and re-located verbatim by line number; the local
mirror `/tmp/sglang-main/python` was used for corroboration only. Absence claims were re-tested with
phrasings the researcher did not use across `sgl-project/sglang`, `vllm-project/vllm`,
`Dao-AILab/flash-attention`, `flashinfer-ai/flashinfer`, `pytorch/pytorch`, plus `web_search` and arXiv.

**Search-integrity note.** Mid-pass `github.com/<repo>/issues?q=` began returning **HTTP 429**; the
original helper silently reports `N=0` for a 429 page. All E5 queries were run through a BLOCKED-aware
paced client (`/tmp/ghq2.py` / the runner's `ghq3.py`); the runner additionally audited all 95 cache pages
that lacked result links (94 genuine "No results", 1 rate-limit page, used in no verdict) and re-ran every
load-bearing `N=0`. No E5 verdict rests on a blocked query. I re-verified the four dropped rows and the
two downgrades myself (raw page fetches, `fetch.py gh`, HTTP 200).

---

## Corrections (rows I changed)

- ROW: **E5.9** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"I did not find it — the literal phrase query
  returns **zero** results, and no thread raises FB-owned snapshots or the SB mutation-before-forward rule
  violation."* — CONFIDENCE "HIGH for the aliasing claim" | NOW: **DROP (absence falsified)** | REASON:
  merged PR #26022 "Group ScheduleBatch and ForwardBatch fields by data-flow role" (hnyls2002) is the PR
  that introduced the quoted "Borrowed from ScheduleBatch" annotation, and its thread raises exactly the
  ownership/aliasing question: body — *"ForwardBatch sections by provenance: required core; **borrowed from
  ScheduleBatch** (GPU tensors / config / host / compound); … **The borrowed GPU-tensor group marks the
  cross-stream clone targets.**"*; title history — *"Split ForwardBatch.init_new into snapshot +
  attach_runtime"* → *"Replace ForwardBatch.init_new with from_schedule_batch + attach_runtime"* (both
  strings re-read on the page). A human therefore proposes/lands a snapshot split and names the
  cross-stream clone targets, so "no thread raises FB-owned snapshots" is false. The narrower
  `init_new`-back-fills-`seq_lens_sum` sub-claim remains unraised, so if the orchestrator applies the
  strict "introducing PR ≠ discussion" rule this row becomes a downgrade rather than a drop; the
  absence claim **as written** cannot stand either way. | EVIDENCE:
  https://github.com/sgl-project/sglang/pull/26022 (state: MERGED) | `fetch.py gh` + raw page text, HTTP 200.
- ROW: **E5.12** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"I did not find it. Neither probe returns
  anything about API-key enforcement in multi-tokenizer mode …"* — MEDIUM | NOW: **DROP (absence
  falsified)** | REASON: open PR #29658 is titled *"Reject API keys in multi-tokenizer mode where auth is
  not enforced"* and its body states: *"The API-key auth middleware is only installed in single-tokenizer
  mode (`tokenizer_worker_num == 1`, see `launch_server`). When `--tokenizer-worker-num > 1`, a configured
  `--admin-api-key` (or `--api-key`) is never wired into the request path, so admin-tagged management routes
  become …"* — the row's exact gap, raised by a human. Falsified by title alone. | EVIDENCE:
  https://github.com/sgl-project/sglang/pull/29658 (state: OPEN) | `fetch.py gh`, HTTP 200.
- ROW: **E5.13** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"I did not find it. sm120 attention threads
  are about DSA/sparse-MLA … none about the FA4 rotary refusal."* — MEDIUM-HIGH | NOW: **DROP (absence
  falsified)** | REASON: merged PR #10940 "[1/2] Support FA4 for MHA Prefill in sgl-kernel" contains a
  reviewer statement raising exactly this gap (re-read verbatim on the page): *"However, I've identified a
  potential issue where rotary embeddings might be silently ignored in the new FA4 path, which could lead
  to incorrect attention outputs. I've suggested adding an assertion to prevent this until rotary embedding
  support is added for FA4."* PR #11182 continues the FA4/rotary-assertion discussion, and upstream
  `Dao-AILab/flash-attention`'s CuTe FA interface has no `rotary_*` parameters at all — i.e. FA4-without-
  rotary is a known, discussed non-feature. The literal current error string is unfiled, but the gap is
  discussed by humans. | EVIDENCE: https://github.com/sgl-project/sglang/pull/10940 (state: MERGED) ;
  https://github.com/sgl-project/sglang/pull/11182 | `fetch.py gh`, HTTP 200.
- ROW: **E5.14** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"I did not find it — with a caveat. … What no
  thread raises is the FA4/CUTLASS `blocksparse_tensors` parameter being hard-refused on SM120"* —
  LOW-MEDIUM | NOW: **DROP (absence falsified; also a duplicate of `E4.md` row E4:E1)** | REASON: the SM120
  FA4 forward's block-sparsity refusal is named by humans in the upstream repo that this kernel derives
  from (SGLang's `fa4_sm120/flash_fwd.py` carries the Dao-AILab CuTe FA authors' copyright header):
  Dao-AILab PR #2406 "[CuTe, SM120] Forward kernel with optimized TMA path and full features support" —
  *"full features support (except block sparsity)"*, comparison table *"Explicitly unsupported (assert)"*;
  PR #2389 (OPEN) "Add SM80/SM120 block-sparse forward attention support" — *"SM120
  (`FlashAttentionForwardSm120`) inherits the SM80 base class and gets block sparsity for free; removes the
  `assert not use_block_sparsity` guard in `interface.py`"*; PR #2634 lists block sparsity under "What's
  supported" for FA4 sm_120. Same source line as **E4:E1**, which falls for the same reason — so the
  cross-file duplicate is resolved with **no surviving copy**. | EVIDENCE:
  https://github.com/Dao-AILab/flash-attention/pull/2389 (state: OPEN) ;
  https://github.com/Dao-AILab/flash-attention/pull/2406 ;
  https://github.com/Dao-AILab/flash-attention/pull/2634 | `fetch.py gh`, HTTP 200.
- ROW: **E5.4** | FIELD: CONFIDENCE + ABSENCE CLAIM | WAS: *"**No issue asks for delayed sampling to
  become default.**"* — CONFIDENCE HIGH | NOW: **LOW (downgrade; row must be re-scoped)** | REASON: merged
  PR #32637 "Optimize delayed sample and mrope position computation" *enables the capability*: *"Enable
  delayed sampling for general overlap decode through `SGLANG_ENABLE_DELAY_SAMPLE`, allowing sampling
  overhead to overlap with model execution."* The row's factual framing ("sampling still runs after the
  forward step returns … the per-token sampler launch/read-back sits on the critical path of every decode
  step") is only true when the env flag is off; what remains genuinely unraised is *default-ness*. The
  HIGH label is not defensible for the capability as worded; re-scope the row to "delayed sampling is
  opt-in, not the default". | EVIDENCE: https://github.com/sgl-project/sglang/pull/32637 (state: MERGED) |
  reported by the E5 runner; consistent with the row's own `delay_sample_func is not None` quote.
- ROW: **E5.16** | FIELD: CONFIDENCE | WAS: MEDIUM | NOW: **LOW (downgrade; counter-evidence disclosed)**
  | REASON: merged PRs #7909 "[SWA] support swa mem pool page size > 1" and #16296 "Support swa allocator
  page size > 1" add and review the paged `alloc_extend`/`alloc_decode` path for the SWA allocator family
  (#7909's review thread flags unhandled allocation failures in those methods). They cover the hybrid SWA
  allocator, not `PureSWATokenToKVPoolAllocator`, so the row's specific exclusion survives — but the
  capability family is discussed and partly implemented. | EVIDENCE:
  https://github.com/sgl-project/sglang/pull/7909 ; https://github.com/sgl-project/sglang/pull/16296 |
  reported by the E5 runner.
- ROW: **E5.2** | FIELD: (no change; disclosure added) | REASON: PR #16807 "bugfix: prevent excessive token
  slot reservation" complains about over-reservation in the same function, but for *re-prefilled/preempted*
  requests rather than the page-slack admission reservation the TODO names. MEDIUM stands; the disclosure
  belongs in the row so a reader does not think the conservatism family is untouched. | EVIDENCE:
  https://github.com/sgl-project/sglang/pull/16807 | reported by the E5 runner.

## Confirmations (rows I checked and could not falsify)

Each row's GAP quote was re-located verbatim on `main` at the cited line (source files carry no state
token — HTTP 200 raw fetch). Absence re-tested with ≥3 new phrasings.

- E5.1 — quote at `scheduler.py:4012`; `is_mixed_chunk` + logprob remains a genuine zero-hit; #14096 is
  `copy_to_cpu(return_logprob=)` plumbing, not the `mix_with_running` skip; #34719/#35052 are a different
  wrong-request bug. MEDIUM fair.
- E5.2 — quote at `schedule_policy.py:907`; no thread claims the reservation is too conservative
  (#16807/#20018/#34554 are over- or under-shoot complaints). MEDIUM fair; disclosure added above.
- E5.3 — quote at `validation_hook.py:167`; no thread on the priority×policy exclusion (#28631 uses
  `--enable-priority-scheduling` only as a code anchor). MEDIUM-HIGH fair; "priority scheduling" is generic
  but the quoted assertion is distinctive.
- E5.4 — quote re-located at `scheduler.py:4523`; **downgraded** (above), not falsified.
- E5.5 — quote at `xgrammar_backend.py:369`; PR #10691 owns `dispatch_structural_tag` but no commenter
  raises the string→object→string round-trip. MEDIUM fair.
- E5.6 — quote at `model_runner.py:310`; only the additive observer family (#35747, #36461/#35956, #39164);
  none mentions the spec/dLLM exclusion.
- E5.7 — quote at `logits_processor.py:255`; no thread on payload pollution or a dedicated result type.
- E5.8 — quote at `tokenizer_manager.py:1898`; nobody filed against the `batch_size > 128` warning; #37202
  is the Rust server. MEDIUM-HIGH fair.
- E5.10 — quote at `observability/func_timer.py:53`; `time_func_latency` probes remain empty; #21108,
  #15964, #38711/#24197 are tests/correctness, not the await/coroutine cost. HIGH fair.
- E5.11 — quote at `serving_responses.py:340`; MCP hits are all router-side (#11173, #12097/#12290,
  #10978, #25881). MEDIUM fair.
- E5.15 — quote at `mem_cache/allocator/base.py:151/:154`; #16296/#7909 touch the SWA allocator, not the
  `TokenToKVPoolAllocator` base stubs; source check confirms `page_size == 1` takes
  `alloc_token_slots → allocator.alloc()`. MEDIUM fair.

## Could not verify

- **E5.13's** secondary source (sglang PR #11182) and **E5.16's** second PR (#16296) were quoted by the
  runner; I confirmed #10940's reviewer comment and #7909's existence/title only. Neither is load-bearing
  beyond the downgrade/drop already justified by the primary source.
- **E5.14's** #2634/#2406 comparison tables: I read the bodies/summary lines, not every table row. The
  decisive sentence — #2406's *"full features support (except block sparsity)"* — was re-read first-hand.
- arXiv: E5.md cites no arXiv ID (verified: no `arxiv.org/abs/...` and no `NNNN.NNNNN` pattern in the
  file), so there was no ID to read back.

## Tally
checked=16 corrected=7 dropped=4 verbatim_relocated=16 unverifiable=0
absence_claims_falsified=4
