# E3 — NEVER-DISCUSSED (negative evidence)

Researcher E3. Axes: `host_cpu_overhead`, `serve_api`, `longctx_attn`, `memory_alloc`.
Engine under test: vLLM 0.29.0. Local mirror `/tmp/vllm-0.29.0` was used for grepping and
corroboration only; every SOURCE below cites a GitHub URL.

Method: for each candidate the specific technical claim was extracted from the engine's own
source line, then searched against GitHub issue/PR search, `web_search`, and arXiv. Rows below
are the ones where **no human thread raising the specific claim was found**. Items where a
thread *was* found are demoted to `## DISCUSSED (therefore NOT Section E)` and are not rows.

---

## A. host_cpu_overhead

### E1 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(ngl): maybe replace with static launch grid to avoid overhead if
#   using cudagraphs"
SOURCE: `vllm/v1/attention/ops/triton_reshape_and_cache_flash.py:427-428` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/triton_reshape_and_cache_flash.py#L427-L428
  (second, identical site: `.../triton_reshape_and_cache_flash.py:583-584`
  https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/triton_reshape_and_cache_flash.py#L583-L584)
WHAT IS MISSING: Both `reshape_and_cache_kernel_flash` and `reshape_and_cache_kernel_flash_diffkv` are
launched with a *lambda* grid derived from the runtime `slot_mapping.shape[0]` instead of a fixed
static grid. Under CUDA-graph capture the grid must therefore be materialised from a Python lambda on
every replay/launch, which costs host time in exactly the decode-step region the graphs exist to
protect. No static-grid variant exists for either kernel.
QUERIES TRIED: `web_search "vllm slot_mapping static launch grid cudagraph overhead issue"` ;
`web_search "vllm reshape_and_cache_flash cudagraph static grid performance"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22static+launch+grid%22&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+slot_mapping+cuda+graph+overhead&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+reshape_and_cache+cudagraph+overhead&type=issues` ;
`web_search "vllm PR 26021 reshape_and_cache CUDA Kernel performance"`
ABSENCE CLAIM: I did not find it. The nearest hit is PR #26021 ("[Perf] Optimize reshape_and_cache
CUDA Kernel"), which is a kernel-body optimisation, not the launch-configuration/static-grid gap the
TODO describes; no issue or PR proposes a static launch grid for these two kernels.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the phrase "static launch grid" is a distinctive term of art
and returns no vLLM thread; the TODO is nicknamed (`ngl`) and unreferenced by any issue number.
TARGET-RIG RELEVANCE: yes — with `slot_mapping.shape[0]` changing every decode step at low batch,
this is a per-step host cost on the exact path the 96 GB Blackwell rig runs; it also interacts with
the rig's `--enforce-eager`-vs-full-CUDA-graph choice.
EVIDENCE: READ BODY

---

### E2 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO avoid gpu<->cpu sync here" — immediately followed by
"with gpu_sync_allowed(): # Prepare cuda kernel metadata tensors"
SOURCE: `vllm/lora/punica_wrapper/punica_gpu.py:87-91` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/lora/punica_wrapper/punica_gpu.py#L87-L91
WHAT IS MISSING: `PunicaWrapperGPU.update_metadata()` — which runs once per model step when LoRA is
enabled — must synchronise the GPU before it can build the CUDA-kernel metadata tensors
(`token_mapping_meta.prepare_tensors(...)` / `prompt_mapping_meta.prepare_tensors(...)`) that the
punica kernels consume. The engine has no way to build this metadata without the sync.
QUERIES TRIED: `web_search "vllm LoRA punica gpu cpu sync update_metadata performance issue"` ;
`web_search "vllm punica_wrapper prepare_tensors gpu_sync_allowed"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+punica+%22cpu+sync%22+LoRA&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+LoRA+metadata+gpu+sync+performance&type=issues` ;
`web_search "vllm PR 47725 punica metadata refresh LoRA slots"`
ABSENCE CLAIM: I did not find it. The obtainable hits are about *correctness* of punica metadata
(PR #47725, "Refresh punica metadata when LoRA slots are reassigned under an unchanged mapping"), not
about removing the per-step host sync the TODO names. No thread proposes a sync-free metadata path.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — "avoid gpu<->cpu sync" is a generic phrase that also appears
in other vLLM TODOs, so the search space is noisier than E1's; no LoRA-specific sync thread surfaced.
TARGET-RIG RELEVANCE: no for the Qwen3-4B dense baseline; yes if the rig attaches a LoRA adapter,
where this becomes a per-step stall. Included because the axis is critical and the engine admits it.
EVIDENCE: READ BODY

---

### E3 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(woosuk): This can be slow because we handle each request
# one by one. Optimize." — over `for i, generator in generators.items(): q[i].exponential_(generator=generator)`
SOURCE: `vllm/v1/sample/ops/topk_topp_sampler.py:468-471` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/sample/ops/topk_topp_sampler.py#L468-L471
WHAT IS MISSING: `random_sample()` draws the Gumbel/exponential noise for the whole batch in one
`q.exponential_()` — but any request that carries its own `torch.Generator` (i.e. any request with a
`seed`) is then re-drawn in a Python-level per-request loop, one CUDA launch per request. There is no
batched/vectorised path for per-request-seeded sampling. The per-request cost therefore scales with
the number of *seeded* requests in flight, on the serial sampling path of every decode step.
QUERIES TRIED: `web_search "vllm sampling per-request seed Generator loop slow random_sample optimize issue"` ;
`web_search "vllm \"q[i].exponential_\" generator per request optimize"` ;
`web_search "vllm sampling_params seed per-request torch.Generator slow issue"` ;
`web_search "vllm guided decoding seed sampling performance generator loop"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+sampling+seed+generator+slow&type=issues`
ABSENCE CLAIM: I did not find it. Retrieved hits are about seed *semantics and correctness* under
pipeline parallelism (PR #6698) and about attaching a generator when no seed is given (PR #17509) —
neither is the per-request Python loop cost. No thread asks for a batched per-generator draw.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the TODO is explicitly scoped to performance and names
the exact construct; searches on the construct plus "slow"/"optimize" surface no issue.
TARGET-RIG RELEVANCE: yes — Qwen3-4B is served with a 151k vocab and clients commonly pin `seed`
for reproducibility; each seeded request adds a per-step host-side kernel launch to a step that is
already host-bound at low batch.
EVIDENCE: READ BODY

---

### E4 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(rob): make into a coroutine and launch it in
# background thread once Prometheus overhead is non-trivial."
SOURCE: `vllm/v1/engine/async_llm.py:809-810` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/engine/async_llm.py#L809-L810
WHAT IS MISSING: The loggers' `record(...)` call — which folds scheduler stats and iteration stats
into the Prometheus registry — is executed **inline on the engine's single output-handler coroutine**,
in the same loop that drains `engine_core.get_output_async()` and pushes results to request streams.
There is no background thread or deferred task for metrics emission, so any Prometheus cardinality/label
work lands directly in the critical path between the engine core and the user-visible token stream.
QUERIES TRIED: `web_search "vllm output handler Prometheus logging overhead background thread issue"` ;
`web_search "vllm async_llm output_handler Prometheus logging overhead background thread issue"` ;
`web_search "vllm metrics logging per-step overhead reduce"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+prometheus+logging+overhead+background+thread&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+logger_manager+record+background+thread&type=issues`
ABSENCE CLAIM: I did not find it. The retrievable metrics threads are about *missing or wrong*
metric values (e.g. issue #7188 "No metrics exposed at /metrics", vllm-omni #4960 "time statistics
are incorrect"), not about moving emission off the output-handler coroutine.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the TODO is unreferenced by any issue number, and
"Prometheus overhead" + "background thread" surfaces no vLLM discussion.
TARGET-RIG RELEVANCE: yes — on a single GPU with one API server there is exactly one output-handler
loop; every `/metrics` scrape's synchronous registry work competes with token delivery, and the rig's
`--api-server-count 1` default leaves no other process to absorb it.
EVIDENCE: READ BODY

---

### E5 | NEVER-DISCUSSED
GAP (engine's own words): "NOTE: ``non_blocking=True`` only delivers true overlap when the
CPU target is pinned. The current fallback here allocates a
new pageable CPU tensor per call, which silently degrades to a
synchronous copy; acceptable because the sync happens on the
dedicated copy stream, not the default stream."
SOURCE: `vllm/v1/outputs.py:228-242` (`RoutedExpertsTensors.to_cpu_nonblocking`) @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/outputs.py#L228-L242
WHAT IS MISSING: Every call to this method allocates a **fresh pageable** CPU tensor for the D2H of
`routing_data` and `slot_mapping`, so the `non_blocking=True` is nominal — the copy is synchronous
and only escapes notice because it is queued on a side copy stream. The class docstring directly
above describes the intended design as "into a pinned CPU buffer"; the implementation does not
provide one. The engine also keeps a pinned allocator available (`LogprobsTensors.empty_cpu` uses
`pin_memory=PIN_MEMORY`), so the fallback is a local omission rather than a platform limit.
QUERIES TRIED: `web_search "vllm RoutedExpertsTensors pageable pinned CPU tensor D2H fallback"` ;
`web_search "vllm routed_experts pinned copy pageable fallback"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22routed_experts%22+pinned+copy&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+to_cpu_nonblocking+pageable&type=issues` ;
`web_search "vllm SamplerOutput to_cpu_nonblocking pinned memory issue"`
ABSENCE CLAIM: I did not find it. Sibling instances of the *same* pageable-staging bug in this file's
neighbourhood are heavily discussed (see DISCUSSED §D.1, PRs #51841 / #53412 / #54375), which makes
the silence specifically about `RoutedExpertsTensors.to_cpu_nonblocking` notable rather than a
search-coverage artefact.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the engine explicitly calls the fallback "acceptable", so
this may be a deliberate accepted trade-off rather than an unowned gap; no human has raised it either way.
TARGET-RIG RELEVANCE: no for dense Qwen3-4B (routed experts are a MoE artefact); yes only if the rig
serves a MoE model. Listed for completeness on the memory_alloc/host boundary.
EVIDENCE: READ BODY

---

### E6 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(woosuk): This buffer could be too large if max_model_len is big.
# Find a way to reduce the CPU memory usage.
# This buffer is not directly transferred to the GPU, so it does not
# need to be pinned."
SOURCE: `vllm/v1/worker/gpu_input_batch.py:130-139` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu_input_batch.py#L130-L139
  (second site: `vllm/v1/worker/tpu_input_batch.py:44`
  https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/tpu_input_batch.py#L44)
WHAT IS MISSING: `InputBatch` eagerly allocates `token_ids_cpu_tensor` as a dense
`(max_num_reqs, max_model_len)` int32 host tensor (`pin_memory=False`), plus a matching bool
`is_token_ids_tensor` of the same shape. Host RAM therefore scales as
`max_num_reqs × max_model_len × 5 bytes` regardless of how many tokens are actually in flight, and
the engine has no sparse/paged or lazily-grown representation for it. Note the buffer is deliberately
*unpinned* — so any future attempt to copy from it pays pageable-staging costs.
QUERIES TRIED: `web_search "vllm input_batch token_ids_cpu memory max_model_len large CPU RAM issue"` ;
`web_search "vllm \"max_num_reqs\" \"max_model_len\" CPU buffer memory usage issue"` ;
`web_search "vllm CPU memory usage grows with max_model_len host RAM"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22token_ids_cpu_tensor%22&type=issues` ;
`web_search "vllm PR 10730 Do not allocate beyond the max_model_len"`
ABSENCE CLAIM: I did not find it. The one adjacent merged PR I found (#10730, "Do not allocate beyond
the max_model_len") is about *KV-cache block* over-allocation and the block-table shape, not this host
token-id buffer. vllm-omni issue #4383 ("Per-request Python preprocess loop dominates decode step
time") is about CPU *time*, not this CPU *size*, and is a different repo.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the TODO is specific and unreferenced; searches on the
buffer attribute name and on the RAM-scaling phrasing both came back empty for vLLM core.
TARGET-RIG RELEVANCE: yes — the rig has 208 CPU cores and a large host, but Qwen3-4B's 40k native
context with a raised `--max-model-len` makes this buffer the dominant fixed host-RAM cost and a
hard cap on `max_num_reqs`; it is also the reason the buffer is unpinned.
EVIDENCE: READ BODY

---

### E7 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(woosuk): Avoid the copy. Optimize."
SOURCE: `vllm/v1/worker/gpu_model_runner.py:3641-3644` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu_model_runner.py#L3641-L3644
WHAT IS MISSING: On the prompt-embeds / multimodal path, the freshly produced
`inputs_embeds_scheduled` tensor is copied into the persistent `self.inputs_embeds.gpu` buffer with a
bare `.copy_(...)` immediately after `embed_input_ids(...)`. The copy is unconditional on this branch
and the engine names no way to write the embedding output directly into the buffer.
QUERIES TRIED: `web_search "vllm \"Avoid the copy\" inputs_embeds optimize issue gpu_model_runner"` ;
`web_search "vllm prompt_embeds embedding layer outside CUDA graph double compiling issue"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22Avoid+the+copy%22+embed&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+inputs_embeds+copy+optimize&type=issues`
ABSENCE CLAIM: I did not find it. Related-but-distinct threads exist and are listed in DISCUSSED
§D.2 (PR #25739 guards the copy behind `enable_prompt_embeds`; issue #35023 is about *lazy GPU
allocation* of the buffer). Neither addresses eliminating the copy itself, which is what this TODO says.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH.
TARGET-RIG RELEVANCE: no for text-only Qwen3-4B (the `else` branch is taken and `inputs_embeds` is
`None`); yes for any multimodal model on the rig. Recorded because it is a clean, unreferenced
engine-admitted per-step copy.
EVIDENCE: READ BODY

---

### E8 | NEVER-DISCUSSED
GAP (engine's own words): "self.sampled_token_ids_cpu.tolist()" preceded by
"# `tolist` would trigger a cuda wise stream sync, which
# would block other copy ops from other cuda streams." and the mitigation
"self.transfer_event.record(); self.transfer_event.synchronize(); return pinned.tolist()"
SOURCE: `vllm/v1/worker/gpu_model_runner.py:7671-7677` (and the consuming site at `:361`) @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu_model_runner.py#L7671-L7677
WHAT IS MISSING: The per-step path that turns sampled token ids into Python lists still does a
pinned D2H copy followed by a **blocking** `transfer_event.synchronize()` and then `pinned.tolist()`
on the host — i.e. one host-visible synchronisation point per model step, in the model runner's own
critical path. The engine's own comment calls this "a short term mitigation".
QUERIES TRIED: `https://github.com/vllm-project/vllm/issues/22754` (READ BODY) ;
`web_search "vllm sampled_token_ids tolist stream sync issue 22754"` ;
`web_search "vllm transfer_event synchronize tolist per step host sync"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22sampled_token_ids%22+tolist+sync&type=issues` ;
`web_search "vllm PR 41429 Eliminate various GPU CPU syncs"`
ABSENCE CLAIM: I did not find it. This is the rubric's **LOW-confidence-absence** case and is stated
plainly: the *class* of problem is discussed — issue #22754 is literally the thread that produced this
mitigation and PR #41429 is an njhill series called "Eliminate various GPU<->CPU syncs". What I could
not find is any thread targeting *this residual* per-step event-synchronise-plus-`tolist` pair as
still-open after the mitigation, nor any proposal to remove the host round-trip entirely.
CONFIDENCE THE ABSENCE IS REAL: LOW — the antecedent thread (#22754) exists and the general
sync-elimination effort is public; I am claiming only that the residual was never re-raised, not that
the area is undiscussed.
TARGET-RIG RELEVANCE: yes — one synchronise per step is exactly the low-batch host-bound cost the rig
is exposed to, and the mitigation's comment says it was motivated by a *disaggregated* setup, so the
single-node rig inherits the sync without the setup it was justified by.
EVIDENCE: READ BODY

---

## B. serve_api

### E9 | NEVER-DISCUSSED
GAP (engine's own words): "# Split outputs into chunks of at most
# VLLM_V1_OUTPUT_PROC_CHUNK_SIZE, so that we don't block the
# event loop for too long." and "# Allow other asyncio tasks to run between chunks" over
"if end < num_outputs: await asyncio.sleep(0)"
SOURCE: `vllm/v1/engine/async_llm.py:759`, `:772-798` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/engine/async_llm.py#L772-L798
  (default value: `VLLM_V1_OUTPUT_PROC_CHUNK_SIZE: int = 128` at
  https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/envs.py#L167)
WHAT IS MISSING: The single frontend coroutine that drains engine-core output must voluntarily yield
the asyncio event loop (`await asyncio.sleep(0)`) between fixed-size chunks of 128 outputs, because
processing the whole batch would otherwise monopolise the loop. There is no work-stealing, no
multi-consumer output path and no off-loop processing of engine-core outputs, so frontend
request-lifecycle throughput is explicitly rate-limited to protect the event loop rather than being
parallelised across it.
QUERIES TRIED: `web_search "vllm VLLM_V1_OUTPUT_PROC_CHUNK_SIZE output proc chunk"` ;
`web_search "vllm output processor chunk event loop block throughput"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+VLLM_V1_OUTPUT_PROC_CHUNK_SIZE&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+output_handler+event+loop+block&type=issues` ;
`web_search "vllm-omni issue 4383 per-request Python preprocess loop dominates decode step time"`
ABSENCE CLAIM: I did not find it. The closest retrieved artefact is vllm-omni issue #4383, which is
(a) a different repository and (b) about a per-request Python *preprocess* loop in vllm-omni, not
about `AsyncLLM.output_handler` chunking in vLLM core.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the env var name is unique and returns no issue thread.
TARGET-RIG RELEVANCE: yes — a single API server (`--api-server-count 1`, the rig default) means one
event loop and one output-handler task; the chunked drain plus `sleep(0)` sets the ceiling on how fast
finished tokens reach clients at high concurrency.
EVIDENCE: READ BODY

---

### E10 | NEVER-DISCUSSED
GAP (engine's own words): "# The Rust parser owns the incremental buffer, so this adapter feeds only
the newest text delta and lets the serving layer handle final empty chunks." followed by
"# TODO: Add a final-chunk hook if streaming needs to call Rust finish()."
SOURCE: `vllm/tool_parsers/rust_tool_parser.py:294-301` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/tool_parsers/rust_tool_parser.py#L294-L301
WHAT IS MISSING: The Rust-backed tool parser's streaming adapter has no mechanism to call the parser's
`finish()` on the final chunk of a stream — the hook does not exist. Anything the Rust parser can only
emit at end-of-stream therefore cannot be surfaced through the incremental streaming API.
QUERIES TRIED: `web_search "vllm rust tool parser streaming finish hook"` ;
`web_search "vllm rust_tool_parser final chunk streaming"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+rust_tool_parser&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+tool+parser+final+chunk+streaming&type=issues` ;
`https://github.com/vllm-project/vllm/issues/44280` (Rust frontend parity roadmap — READ BODY)
ABSENCE CLAIM: I did not find it. The Rust-frontend parity roadmap (issue #44280) is scoped to
endpoint/feature parity and does not mention a streaming final-chunk hook for the tool parser; no
other thread raises it.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH.
TARGET-RIG RELEVANCE: yes if the rig streams tool calls through the Rust frontend; neutral otherwise.
EVIDENCE: READ BODY

---

### E11 | NEVER-DISCUSSED
GAP (engine's own words): "assert name is not None, f\"Unknown metrics info type {type}\"" preceded by
"# Info type metrics are syntactic sugar for a gauge permanently set to 1
# Since prometheus multiprocessing mode does not support Info, emulate
# info here with a gauge." — where `name` is only ever assigned under
`if type == "cache_config":`
SOURCE: `vllm/v1/metrics/loggers.py:1076-1098` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/metrics/loggers.py#L1076-L1098
WHAT IS MISSING: `log_metrics_info()` has exactly one supported `type` — `"cache_config"` — producing
exactly one `vllm:cache_config_info` series. The mechanism for exposing build/engine configuration as
Info-style metrics exists but no other config object is wired into it, and the assert means any new
type raises rather than degrading.
QUERIES TRIED: `web_search "vllm log_metrics_info cache_config only metrics_info type"` ;
`web_search "vllm prometheus cache_config_info only info metric supported"` ;
`web_search "vllm /metrics endpoint coverage missing metrics request issue github"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+log_metrics_info&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22cache_config_info%22&type=issues`
ABSENCE CLAIM: I did not find it. The retrieved metrics threads are about values being wrong or the
endpoint being dead (vLLM issue #7188 "No metrics exposed at /metrics"), not about extending
`log_metrics_info` beyond `cache_config`.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — "metrics coverage" is a broad phrase; the specific assert
path is not discussed, but I cannot rule out a broader wishlist issue.
TARGET-RIG RELEVANCE: yes for observability of a single-GPU deployment — there is no per-engine or
per-config Info metric to distinguish builds/flag sets when comparing runs on the same host.
EVIDENCE: READ BODY

---

## C. longctx_attn

> Two `longctx_attn` candidates from `/tmp/ecand/E3.md` were run through the negative-evidence test
> and came back **DISCUSSED**, so they are *not* rows here. Verbatim quotes and URLs are in
> `DISCUSSED §D.3`:
> - `"Sliding window for some but all layers is not supported. "` (`vllm/model_executor/models/qwen2.py:350`,
>   also `seed_oss.py:282`, `iquest_loopcoder.py:387`) — issues #15705 and #17306 quote the string;
>   open PR #17772 attempts the fix.
> - `"Cascade attention does not support sliding window."` preceded by `# TODO: Support sliding window.`
>   (`vllm/v1/attention/backends/flash_attn.py:1770-1773`) — issue #15710 is a feature request for
>   exactly it; issue #14881 quotes the assert.

### E12 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(Isotr0py): sliding window pruning with image bidirectional mask"
SOURCE: `vllm/v1/attention/ops/triton_attention_helpers.py:197-202` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/triton_attention_helpers.py#L197-L202
WHAT IS MISSING: The Triton unified-attention helper prunes whole KV tiles for sliding-window requests
only when `SLIDING_WINDOW > 0 and not USE_MM_PREFIX`. When an image bidirectional prefix mask is
active (`USE_MM_PREFIX`), the sliding-window tile pruning is skipped entirely and the kernel falls
back to scanning the full sequence — so the compute saving the sliding window should buy is lost
precisely for multimodal long-context requests.
QUERIES TRIED: `web_search "vllm sliding window pruning image bidirectional mask"` ;
`web_search "vllm Isotr0py sliding window pruning mm prefix triton attention"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+sliding+window+pruning+bidirectional+mask&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22USE_MM_PREFIX%22&type=issues` ;
`web_search "vllm unified attention sliding window multimodal prefix pruning"`
ABSENCE CLAIM: I did not find it. No issue or PR mentions combining image bidirectional masking with
sliding-window tile pruning; the two features are discussed separately (issue #15710 for cascade +
sliding window, which is a different mechanism).
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the TODO is author-tagged and names a specific
kernel-level interaction that returns no thread.
TARGET-RIG RELEVANCE: partial — Qwen3-4B is text-only, so the `USE_MM_PREFIX` branch never activates;
this bites multimodal long-context models on the rig, not the dense baseline.
EVIDENCE: READ BODY

---

### E13 | NEVER-DISCUSSED
GAP (engine's own words): "if SLIDING_WINDOW > 0 and not USE_MM_PREFIX:" ... the whole
"# ---- Sliding-window tile pruning --------------------" block documented as
"# Default: keep previous global behavior"
SOURCE: `vllm/v1/attention/ops/triton_attention_helpers.py:195-221` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/triton_attention_helpers.py#L195-L221
  (the `CHUNK_LOOKBACK` / `CHUNK_SIZE` sub-branch, used to extend the pruned window when the model
  uses chunked local attention:
  https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/triton_attention_helpers.py#L215-L221)
WHAT IS MISSING: The pruning prefix is a union-of-allowed-keys approximation over a whole Q-block; the
`CHUNK_LOOKBACK > -1` branch differs from the plain `q_abs - SLIDING_WINDOW + 1` branch, meaning
chunked-local-attention models must pass their chunk geometry in explicitly to get correct pruning.
There is no general mechanism that derives the prunable key range from the model's attention spec, so
each chunked/sliding variant needs its own hand-tuned lookback plumbing.
QUERIES TRIED: `https://github.com/search?q=repo%3Avllm-project%2Fvllm+CHUNK_LOOKBACK+sliding+window&type=issues` ;
`web_search "vllm CHUNK_LOOKBACK CHUNK_SIZE sliding window attention triton"` ;
`web_search "vllm chunked local attention lookback tile pruning"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+sliding+window+tile+pruning&type=issues` ;
`web_search "vllm unified attention sliding window mask kernel"`
ABSENCE CLAIM: I did not find it. No thread discusses `CHUNK_LOOKBACK` or the union-approximation
pruning contract.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the identifiers are internal and unlikely to be discussed
even if the design gap were felt; the absence may reflect non-searchability as much as non-discussion.
TARGET-RIG RELEVANCE: partial — Qwen3-4B has no sliding window; relevant only if the rig serves a
sliding-window or chunked-local model.
EVIDENCE: READ BODY

---

### E14 | NEVER-DISCUSSED
GAP (engine's own words): "return \"sink not supported on compute capability < 9.0\""
SOURCE: `vllm/v1/attention/backends/flash_attn.py:215` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flash_attn.py#L215
WHAT IS MISSING: The FlashAttention backend's capability check refuses attention sinks on any GPU
below compute capability 9.0. Attention sinks are the mechanism GPT-OSS-class long-context models use
to keep a stable attention anchor; on such a device the backend reports the configuration invalid
rather than providing a fallback implementation.
QUERIES TRIED: `web_search "vllm attention sink not supported compute capability 9.0"` ;
`web_search "vllm attention sinks sm120 Blackwell support"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22sink+not+supported%22&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+attention+sink+compute+capability&type=issues` ;
`web_search "vllm gpt-oss attention sink flash attention support capability"`
ABSENCE CLAIM: I did not find it. GPT-OSS support discussions exist (e.g. PR #22342 "[gpt-oss]
Support chat completion api"), but none raises the sub-9.0 sink restriction as a gap to be removed.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the literal string is distinctive and returns no thread.
TARGET-RIG RELEVANCE: no — the rig's RTX PRO 6000 Blackwell is sm120, well above the 9.0 floor, so
this restriction never fires there. Recorded because it is a hard attention-feature cliff on the
GPU-generation boundary the rig sits just above.
EVIDENCE: READ BODY

---

## D. memory_alloc

### E15 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO: Actually only mi3xx has the sleep mode support now
# for ROCm, but currently we don't have a way to detect the
# exact GPU model statelessly here. So we return True for
# all ROCm platforms for now."
SOURCE: `vllm/platforms/interface.py:224-230` (`is_sleep_mode_available`) @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/platforms/interface.py#L224-L230
WHAT IS MISSING: There is no stateless capability query for sleep-mode/allocator support. The platform
layer returns `True` for the whole ROCm enum because it cannot detect the GPU model in a stateless
call, so the *check itself* is a known approximation rather than a probe. On the CUDA side the same
method simply trusts the enum, so `enable_sleep_mode` validation does not verify that the installed
allocator backend can actually carry the platform's tag/offload semantics.
QUERIES TRIED: `web_search "vllm sleep mode not supported on current platform cumem allocator"` ;
`web_search "vllm is_sleep_mode_available mi3xx detection"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+is_sleep_mode_available&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+sleep+mode+platform+detection&type=issues` ;
`https://github.com/vllm-project/vllm/issues/35463` ("[Bug]: Error on sleep mode when upgrade into
vllm 0.16.0") ; `https://github.com/vllm-project/vllm/pull/40812` ("Auto-disable expandable_segments
around cumem memory pool")
ABSENCE CLAIM: I did not find it. The retrieved sleep-mode threads are about (a) a version-upgrade
regression (#35463) and (b) the `expandable_segments` ↔ cumem-pool incompatibility (#40812) — a
different, already-fixed problem. No thread asks for a stateless per-GPU sleep-mode capability probe.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH.
TARGET-RIG RELEVANCE: yes in a narrow sense — the rig is CUDA/sm120, so it takes the `CUDA` branch and
never hits the ROCm approximation; but the absence of a real capability probe is what would matter if
the rig ever needed to verify that sleep/wake works on a consumer-Blackwell part before relying on it.
EVIDENCE: READ BODY

---

### E16 | NEVER-DISCUSSED
GAP (engine's own words): "def use_memory_pool(self, tag: str | None = None):" ... with
"# Expandable segments are incompatible with the memory pool used for
# sleep mode (see https://github.com/pytorch/pytorch/issues/147851).
# If the user has enabled expandable segments via
# PYTORCH_CUDA_ALLOC_CONF, temporarily disable them for the duration
# of the memory pool context and restore on exit."
SOURCE: `vllm/device_allocator/cumem.py:359-380` @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/device_allocator/cumem.py#L359-L380
WHAT IS MISSING: The engine cannot use its sleep-mode memory pool and PyTorch's `expandable_segments`
allocator setting at the same time. Rather than supporting the combination, it mutates
`PYTORCH_CUDA_ALLOC_CONF` for the duration of the pool context and restores it afterwards — so a user
who wants expandable segments (which is what many fragmentation-sensitive single-GPU deployments set)
silently loses that allocator behaviour for the lifetime of the pool, and any allocation made inside
the context is not segmented.
QUERIES TRIED: `https://github.com/vllm-project/vllm/pull/40812` (READ BODY — "Auto-disable
expandable_segments around cumem memory pool") ;
`web_search "vllm PYTORCH_CUDA_ALLOC_CONF expandable_segments incompatible memory pool sleep mode issue"` ;
`web_search "vllm sleep mode cumem expandable segments incompatibility pytorch 147851"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+expandable_segments+cumem&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+use_memory_pool+expandable&type=issues`
ABSENCE CLAIM: **DISCUSSED — this row is withdrawn as a Section E row.** PR #40812 is titled
"Auto-disable expandable_segments around cumem memory pool", i.e. it is the very change that produced
this code, so the incompatibility is publicly owned. Recorded in DISCUSSED §D.4 and must **not** be
counted as a NEVER-DISCUSSED row. What remains genuinely absent is the *converse* ask — supporting the
combination rather than papering over it — but that is a design position, not a distinct engine-admitted
gap with its own source line.
CONFIDENCE THE ABSENCE IS REAL: n/a (row withdrawn).
TARGET-RIG RELEVANCE: yes as context — a single 96 GB card is exactly where users reach for expandable
segments, and this is a live interaction with `--enable-sleep-mode`.
EVIDENCE: READ BODY

---

### E17 | NEVER-DISCUSSED
GAP (engine's own words): "assert allocator.get_current_usage() == 0, (
    \"CuMem allocator can only be used for one instance per process.\"
)"
SOURCE: `vllm/v1/worker/gpu_worker.py:315-322` (`_maybe_get_memory_pool_context`) @ v0.29.0
  URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu_worker.py#L315-L322
WHAT IS MISSING: The sleep-mode memory pool is a per-process singleton keyed on the `"weights"` tag:
once any allocation has been made through it, a second engine instance in the same process cannot use
it at all. There is no per-instance pool and no way to scope the pool to one model, so the API server
cannot host two sleep-mode engines (e.g. two models on the one 96 GB card) in one process.
QUERIES TRIED: `web_search "vllm CuMem allocator one instance per process weights assertion"` ;
`web_search "vllm sleep mode two models one process cuMem allocator"` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22CuMem+allocator%22+instance&type=issues` ;
`https://github.com/search?q=repo%3Avllm-project%2Fvllm+get_current_usage+weights+allocator&type=issues` ;
`web_search "vllm enable_sleep_mode multiple models single process limitation"`
ABSENCE CLAIM: I did not find it. No thread asks for multi-instance cumem pools or reports this assert
as a blocker; sleep-mode threads found are about upgrade regressions and the expandable-segments
interaction (see E17/DISCUSSED §D.4).
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the assert is a deliberate guard, so silence may mean nobody
has tried the two-models-one-process shape rather than that the gap is unowned.
TARGET-RIG RELEVANCE: yes — one 96 GB card is a plausible host for two sleep-mode Qwen3-4B-class
engines, and this assert forbids it in a single process.
EVIDENCE: READ BODY

---

## DISCUSSED (therefore NOT Section E)

### D.1 | DISCUSSED — strided-view pinned-copy pageable fallback
The `gpu_model_runner.py` comment that names "PR #51841" is one instance of a family that has been
raised, fixed and is still being extended.
- `OCCUPIED (open PR #54375)` — "[Perf][Spec Decode] Avoid strided-view D2H stall on dynamic-K draft
  token copy"; body states the mrope fix lineage explicitly: "same pattern as #53412 for XD-RoPE and
  njhill's original mrope fix in #51841". https://github.com/vllm-project/vllm/pull/54375
- `OCCUPIED (open PR #53412)` — "[Perf] Split xdrope_positions H2D copy into per-row transfers";
  body: "PR #51841 identified a subtle host-run-ahead killer: copy_(..., non_blocking=True) on a
  strided view of a pinned buffer silently falls back to a pageable intermediate". The source tree
  already contains the per-row workaround this PR generalises.
  https://github.com/vllm-project/vllm/pull/53412
- `SOLVED (merged PR #51841)` — "Avoid long-blocking H2D copies in ViT".
  https://github.com/vllm-project/vllm/pull/51841
- `SOLVED (merged PR #37006)` — "[V1] Remove pin_memory() in async_copy_to_gpu to fix sporadic
  stalls". https://github.com/vllm-project/vllm/pull/37006

### D.2 | DISCUSSED — prompt-embeds copy / embedding buffer
- `SOLVED (merged PR #25739)` — "perf: Avoid copying inputs_embeds tensors to GPU unless
  prompt_embeds is enabled"; body: "njhill pointed out that #24278 introduced a performance
  regression ... We can guard those copies with self.enable_prompt_embeds".
  https://github.com/vllm-project/vllm/pull/25739
- `OCCUPIED (open, stale / NOT_PLANNED) issue #35023` — "[Feature]: Remove embedding initialization in
  cases where embedding is not needed in gpu_model_runner init" (about lazy GPU allocation of the
  buffer, not the copy). https://github.com/vllm-project/vllm/issues/35023

### D.3 | DISCUSSED — sliding-window limitations in attention backends and models
- `OPEN issue #15710` — "[Feature]: Support Cascade Attention for Sliding Window Attention".
  https://github.com/vllm-project/vllm/issues/15710
- `OPEN issue #14881` — "[Bug]: Gemma3 Batch Offline Inference Error: Cascade attention does not
  support sliding window" (quotes the assert). https://github.com/vllm-project/vllm/issues/14881
- `OPEN issue #15705` — "[Bug]: Qwen2.5: Sliding window for some but all layers is not supported. ..."
  (quotes the exact error string). https://github.com/vllm-project/vllm/issues/15705
- `OPEN issue #17306` — "[Usage]: vllm infer QWQ32B can't enable sliding window".
  https://github.com/vllm-project/vllm/issues/17306
- `OCCUPIED (open PR #17772)` — "[Model] Allow the use of sliding window in Qwen2".
  https://github.com/vllm-project/vllm/pull/17772
- `SOLVED (merged PR #36318)` — "Disable cascade attention by default".
  https://github.com/vllm-project/vllm/pull/36318

### D.4 | DISCUSSED — sleep-mode memory pool vs expandable segments
- `SOLVED (merged PR #40812)` — "Auto-disable expandable_segments around cumem memory pool" — the
  change that introduced the `PYTORCH_CUDA_ALLOC_CONF` mutation quoted in E17.
  https://github.com/vllm-project/vllm/pull/40812
- `issue #35463` — "[Bug]: Error on sleep mode when upgrade into vllm 0.16.0" (unrelated regression,
  recorded to show the sleep-mode search space was swept).
  https://github.com/vllm-project/vllm/issues/35463

### D.5 | DISCUSSED — merge_attn_states CPU overhead (an explicitly-worked candidate from my file)
- `SOLVED (merged PR #16173)` — "[Kernel] support merge_attn_states CUDA kernel, 3x speedup";
  body: "Use CUDA kernel instead of Triton to minimize CPU overhead. Compared to the Triton kernel,
  the CUDA kernel implemented in this PR can achieve a maximum speedup of over 3x".
  https://github.com/vllm-project/vllm/pull/16173
- `merged PR #16060` — "[Kernel] Support merge attn cuda kernel"; body: "it will mitigate the CPU
  overhead of current triton kernel". https://github.com/vllm-project/vllm/pull/16060
Note: the v0.29.0 tree still runs the Triton kernel unconditionally at
`vllm/v1/attention/ops/triton_merge_attn_states.py:47-48` with the `TODO(woosuk)` comment in place, so
the source line survives even though the human thread exists. This is exactly the case the brief says
to classify DISCUSSED rather than Section E.

### D.6 | DISCUSSED — detokenization CPU cost
- `SOLVED (merged PR #32975)` — "[Perf] Optimize detokenizer python logic".
  https://github.com/vllm-project/vllm/pull/32975
- `SOLVED (merged PR #16963)` — "[BugFix] Fix incremental detokenization perf issue".
  https://github.com/vllm-project/vllm/pull/16963
- `OPEN issue #14173` — "[Performance] [V1]: Optimize batch token processing in
  `IncrementalDetokenizer.update()`". https://github.com/vllm-project/vllm/issues/14173

### D.7 | DISCUSSED — residual sampled-token-id D2H sync
- `OPEN, NOT_PLANNED issue #22754` — "[Bug]: [V1][P/D] Perf Gap D2H copy for sampled token ids
  (output) unintentionally block all other copy operations from other CUDA streams for KV transfers".
  This is the thread that produced the `_to_list()` mitigation currently in the tree.
  https://github.com/vllm-project/vllm/issues/22754
- `OCCUPIED (closed PR #41429)` — "[Perf][1/n] Eliminate various GPU<->CPU syncs" (njhill).
  https://github.com/vllm-project/vllm/pull/41429

### D.8 | DISCUSSED — sampling-mask CPU unpacking (same failure mode as E9's axis, already owned)
- `SOLVED in main (merged PR #54901)` — "[Perf][Model Runner V2] Compact sampling masks on GPU
  instead of unpacking the full-vocab bitmask on CPU"; body gives the exact host-bound mechanism:
  "SamplingMaskTensors.tolists() ran np.unpackbits + np.nonzero over a [num_reqs, vocab] bitmask on
  every step. That is an O(num_reqs × vocab) CPU pass ... With a 151k vocab it costs ~17 ms at 64
  requests and ~130 ms at 512 ... the engine is CPU-bound and throughput stops scaling with batch
  size." https://github.com/vllm-project/vllm/pull/54901
  NOTE for the aggregator: the `np.unpackbits` path is **still present** in the v0.29.0 tree at
  `vllm/v1/worker/gpu/sample/output.py:118`
  (https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu/sample/output.py#L118), so on
  the rig's pinned v0.29.0 plus `--return-sampling-mask` this cost is live even though the fix is
  merged upstream. It is not a Section E row because the thread exists and is explicit.
- `SOLVED (merged PR #35333)` — "[Perf] Optimize model runner v2 `prepare_inputs` copy logic, 6.1% E2E
  throughput improvement"; body: "Pre-allocate the gpu/cpu memory / Optimize copy logic (temple pin in
  async_copy_to_gpu is expensive)". https://github.com/vllm-project/vllm/pull/35333

### D.9 | DISCUSSED — Rust frontend feature parity
- `OPEN issue #44280` — "[Roadmap] Rust Frontend Feature Parity"; body: "The Rust frontend is still
  experimental and not yet feature-complete relative to the Python frontend. This issue tracks the
  roadmap for the remaining gaps in feature parity". Withdrawn as a row for that reason; note the
  in-tree restriction at `vllm/entrypoints/cli/serve.py:270-273`
  ("VLLM_RUST_FRONTEND_PATH does not support api_server_count > 1") is one instance of the tracked gap.
  https://github.com/vllm-project/vllm/issues/44280
- `issue #40846` — "[RFC]: Rust front-end". https://github.com/vllm-project/vllm/issues/40846
- `issue #49047` — "[RFC]: Standalone Rust renderer". https://github.com/vllm-project/vllm/issues/49047

### D.10 | DISCUSSED — Rust-frontend dispatch for `vllm bench serve` declares the Python frontend insufficient
- `SOLVED (merged PR #38011)` — "Add `/v1/chat/completions/batch` endpoint for batched chat
  completions". The endpoint exists in v0.29.0 and its docstring limitation block
  ("Streaming is not supported ... Tool use is not supported ... Beam search is not supported") is a
  documented restriction of a shipped endpoint, not an unraised gap.
  https://github.com/vllm-project/vllm/pull/38011

---

## SEARCH LOG (queries actually issued)

GitHub issue/PR searches (`type=issues`, HTML):
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22merge_attn_states%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+triton_merge_attn_states&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22static+launch+grid%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+slot_mapping+cudagraph+launch+overhead&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+reshape_and_cache+cudagraph+overhead&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22sliding+window%22+flash_attn+TODO&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+punica+%22cpu+sync%22+LoRA&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+LoRA+metadata+gpu+sync+performance&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+sampling+seed+generator+slow&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+prometheus+logging+overhead+background+thread&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+logger_manager+record+background+thread&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22routed_experts%22+pinned+copy&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+to_cpu_nonblocking+pageable&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22token_ids_cpu_tensor%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22Avoid+the+copy%22+embed&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+inputs_embeds+copy+optimize&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22sampled_token_ids%22+tolist+sync&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+VLLM_V1_OUTPUT_PROC_CHUNK_SIZE&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+output_handler+event+loop+block&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+rust_tool_parser&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+tool+parser+final+chunk+streaming&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+log_metrics_info&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22cache_config_info%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+sliding+window+pruning+bidirectional+mask&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22USE_MM_PREFIX%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+CHUNK_LOOKBACK+sliding+window&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+sliding+window+tile+pruning&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22sink+not+supported%22&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+attention+sink+compute+capability&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+is_sleep_mode_available&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+sleep+mode+platform+detection&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+expandable_segments+cumem&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+use_memory_pool+expandable&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+%22CuMem+allocator%22+instance&type=issues`
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+get_current_usage+weights+allocator&type=issues`

Primary artefacts fetched and read (`fetch.py gh`, comment bodies present):
- https://github.com/vllm-project/vllm/pull/16173 (merged)
- https://github.com/vllm-project/vllm/pull/16060
- https://github.com/vllm-project/vllm/issues/22754 (NOT_PLANNED)
- https://github.com/vllm-project/vllm/pull/31852 (merged)
- https://github.com/vllm-project/vllm/issues/27894
- https://github.com/vllm-project/vllm/pull/10730
- https://github.com/vllm-project/vllm/pull/25739 (merged)
- https://github.com/vllm-project/vllm/issues/35023 (NOT_PLANNED)
- https://github.com/vllm-project/vllm/pull/54375 (open)
- https://github.com/vllm-project/vllm/pull/53412 (open)
- https://github.com/vllm-project/vllm/pull/37006
- https://github.com/vllm-project/vllm/pull/41429 (closed)
- https://github.com/vllm-project/vllm/pull/38011 (merged)
- https://github.com/vllm-project/vllm/pull/40812
- https://github.com/vllm-project/vllm/issues/35463
- https://github.com/vllm-project/vllm/issues/44280
- https://github.com/vllm-project/vllm/issues/15705 ; /15710 ; /14881 ; /17306
- https://github.com/vllm-project/vllm/pull/17772 ; /36318 ; /54901 ; /35333 ; /26021 ; /47725
- https://github.com/vllm-project/vllm-omni/pull/2013 ; https://github.com/vllm-project/vllm-omni/issues/4383

`web_search` strings (each issued as a 1–3 query array):
- "vllm merge_attn_states CPU overhead CUDA kernel woosuk" ; "vllm triton_merge_attn_states issue CPU overhead" ; "vllm chunked prefill merge attention states performance CPU overhead github issue"
- "vllm slot_mapping static launch grid cudagraph overhead issue" ; "vllm reshape_and_cache_flash cudagraph static grid performance" ; "vllm LoRA punica gpu cpu sync update_metadata performance issue"
- "vllm prometheus metrics missing per-request latency histogram /metrics gap" ; "vllm output handler Prometheus logging overhead background thread issue" ; "vllm metrics_info cache_config_info only cache_config type"
- "vllm cascade attention sliding window not supported issue" ; "vllm memory pool CUDA graph workspace allocation limit issue" ; "vllm sleep mode not supported on current platform cumem allocator"
- "vllm GitHub issue sliding window some but not all layers not supported" ; "vllm Qwen2 sliding window some but all layers not supported issue" ; "vllm M-RoPE support is not implemented gpu_model_runner issue"
- "vllm async_llm output_handler Prometheus logging overhead background thread issue" ; "vllm SlowIncrementalDetokenizer slow python detokenization performance issue" ; "vllm sampling per-request seed Generator loop slow random_sample optimize issue"
- "vllm sampling_params seed per-request torch.Generator slow issue" ; "vllm \"q[i].exponential_\" generator per request optimize" ; "vllm guided decoding seed sampling performance generator loop"
- "vllm input_batch token_ids_cpu memory max_model_len large CPU RAM issue" ; "vllm \"max_num_reqs\" \"max_model_len\" CPU buffer memory usage issue" ; "vllm CPU memory usage grows with max_model_len host RAM"
- "vllm \"Avoid the copy\" inputs_embeds optimize issue gpu_model_runner" ; "vllm prompt_embeds embedding layer outside CUDA graph double compiling issue" ; "vllm RoutedExpertsTensors pageable pinned CPU tensor D2H fallback"
- "vllm strided view D2H stall pageable pinned copy mrope positions" ; "vllm PR 51841 pageable fallback sync copy_ non_blocking" ; "vllm mrope_positions per-row copy pinned buffer performance"
- "vllm async_copy_to_gpu pin_memory stalls issue" ; "vllm SamplerOutput to_cpu_nonblocking pinned memory issue" ; "vllm LogprobsTensors to_cpu_nonblocking pinned buffer discussion"
- "vllm PYTORCH_CUDA_ALLOC_CONF expandable_segments incompatible memory pool sleep mode issue" ; "vllm sleep mode cumem expandable segments incompatibility pytorch 147851" ; "vllm CuMem allocator one instance per process weights assertion"
- "vllm /v1/chat/completions/batch streaming tool use not supported limitation" ; "vllm batch chat completions endpoint limitations issue" ; "vllm chat completion batch endpoint n parameter must be 1"
- "vllm /metrics endpoint coverage missing metrics request issue github" ; "vllm log_metrics_info cache_config only metrics_info type" ; "vllm prometheus cache_config_info only info metric supported"
- "vllm rust frontend api_server_count not supported issue vllm-rs" ; "vllm VLLM_USE_RUST_FRONTEND vllm-rs frontend performance gap" ; "vllm rust frontend multiple api servers limitation"
- "vllm harmony gpt-oss browsing code interpreter not supported" ; "vllm reasoning_effort none harmony does not support issue" ; "vllm gpt-oss browsing support TODO"
- "vllm host CPU bound low batch size dense model decoding overhead issue" ; "vllm per-step CPU overhead python loop gpu_model_runner prepare_inputs performance" ; "vllm VLLM_CPU_OMP_THREADS_BIND 208 cores thread handling issue"

Local corroboration greps (source tree only, never cited as URL):
`grep -n -B6 -A6` around every cited line in `/tmp/vllm-0.29.0`; plus
`grep -rn "launch overhead|kernel launch overhead|CPU overhead|cpu overhead|CPU-bound|host-bound|host bound|CPU bottleneck" vllm/ --include=*.py`,
`grep -rniE "TODO|FIXME" --include=*.py vllm/v1/worker/ | grep -iE "cpu|host|numpy|sync|copy|overhead|slow|pin"`,
`grep -rniE "serializ|pickle|msgpack|msgspec" --include=*.py vllm/v1/ | grep -iE "TODO|FIXME|overhead|slow|expensive|cost"`.

---

## NEGATIVE FINDINGS

- **`host_cpu_overhead` — CPU-side serialization/IPC between frontend and engine.** I found no
  engine-admitted gap. `vllm/v1/engine/core_client.py` uses `msgspec.msgpack` encode/decode throughout
  and carries no TODO/FIXME about serialization cost;
  `grep -rniE "serializ|pickle|msgpack|msgspec" --include=*.py vllm/v1/ | grep -iE "TODO|FIXME|overhead|slow|expensive|cost"`
  returned nothing, and `grep -rniE "TODO|FIXME" vllm/v1/engine/ vllm/v1/executor/ vllm/entrypoints/ | grep -iE "cpu|host|serial|pickle|ipc|zmq|overhead|slow|latenc|thread"`
  also returned nothing. Queries tried: the two greps above plus web_search
  "vllm msgspec serialization overhead engine core frontend", "vllm engine core IPC overhead zmq performance".
  → No Section E row, because the brief requires a verbatim engine self-admission and there is none.
- **`host_cpu_overhead` — block-table / slot-mapping host construction.** The axis brief names this,
  but in v0.29.0 the work has moved to the GPU: `vllm/v1/worker/block_table.py` builds slot mappings
  via `_COMPUTE_SLOT_MAPPING_KERNEL` (`block_table.py:214-229`) and the numpy block-table path is
  narrowed to `map_to_kernel_blocks` / `get_numpy_array`. The only remaining numpy-to-torch friction I
  could find is the already-worked-around one at `vllm/v1/attention/backends/utils.py:585-592`
  (comment cites `pytorch/pytorch#160256`), which is a *documented workaround*, not a live admitted
  gap. Queries tried: `web_search "vllm block table construction CPU overhead numpy per step"`,
  `grep -n "np\.|numpy|copy_to_gpu|\.fill_|async_tensor_h2d" vllm/v1/worker/block_table.py`.
  → No Section E row.
- **`serve_api` — `/metrics` coverage holes beyond E11.** I looked for an admitted missing-metric list
  and found none in-source: `vllm/v1/metrics/loggers.py` exposes a broad set
  (`vllm:time_to_first_token_seconds`, `vllm:inter_token_latency_seconds`,
  `vllm:request_queue_time_seconds`, `vllm:request_prefill_time_seconds`,
  `vllm:request_decode_time_seconds`, `vllm:num_requests_waiting_by_reason`,
  `vllm:engine_sleep_state`, `vllm:corrupted_requests`, KV-block lifetime metrics, etc.). The only
  in-source metric-side admissions are the `Info`-emulation one (E11) and the
  `log_metrics_info` assert. Queries tried: the three `/metrics` web_search strings above plus
  `grep -rn "vllm:" vllm/v1/metrics/loggers.py | grep -oE '"vllm:[a-z_]+"' | sort -u`.
  → One row (E11), not a family.
- **`longctx_attn` — RoPE / rotary-scaling restrictions on the target model.** The in-tree RoPE-related
  markers are (a) `# TODO(zhuhaoran)` / `# TODO(Rohan138): support rope native forward match and remove
  this. Linked issue: https://github.com/vllm-project/vllm/issues/28042`
  (`vllm/config/compilation.py:965,973`) — a torch.compile fusion-pass gap, not an attention/numerics
  restriction, and both lines cite a public issue number so neither can be a Section E row; (b) an
  `# TODO: Consolidate under VLLM_ROCM_USE_AITER_ROPE` (`vllm/_aiter_ops.py:1757`, ROCm-only); and
  (c) `raise NotImplementedError("Qwen4Exp QSA does not support dual-chunk RoPE")`
  (`vllm/models/qwen4_exp/nvidia/qsa.py:227`) — a model that does not exist on the rig.
  Queries tried: `web_search "vllm rope scaling native forward match issue 28042"`,
  `web_search "vllm rotary embedding restriction long context max_model_len issue"`,
  `web_search "vllm rope scaling factor not supported"`. → No Section E row on this sub-topic.
- **`longctx_attn` — sparse-attention support holes for the target model.** The sparse-attention
  markers found are all MLA/DeepSeek-specific
  (`vllm/v1/attention/backends/mla/flashmla_sparse.py:609` "the bf16 sparse path is not supported
  under DCP"; `vllm/v1/attention/backends/mla/indexer.py:631` "DCP is not supported with sparse
  indexer KV compression"), and DCP is a decode-context-parallel KV mechanism — out of scope per the
  KV-cache exclusion. `vllm/v1/attention/backend.py:306-316` emits generic
  `"sparse not supported"` / `"non-sparse not supported"` / `"sliding window not supported"`
  invalid-reasons from capability negotiation, which is backend plumbing rather than a named missing
  capability. Queries tried: `web_search "vllm sparse attention support not implemented issue"`,
  `web_search "vllm native sparse attention qwen3 issue"`,
  `https://github.com/search?q=repo%3Avllm-project%2Fvllm+sparse+attention+not+supported&type=issues`.
  → No Section E row.
- **`memory_alloc` — CUDA-graph memory-pool and workspace-allocation limits.** The in-tree CUDA-graph
  pool surface is `current_platform.graph_pool_handle()` plus `BreakableCUDAGraphCapture(pool=...)`
  (`vllm/compilation/breakable_cudagraph.py:366-381`, `vllm/compilation/cuda_graph.py:306-315`), and
  neither carries an admitted limit. `_scoped_allocator_max_split`
  (`vllm/v1/worker/gpu_worker.py:324+`) *does* trade fragmentation against extra `cudaMalloc` calls
  but its own comment calls the cost "negligible in practice", so it is not an admitted gap. The one
  workspace-related TODO in the candidate file,
  `vllm/v1/attention/backends/rocm_aiter_fa.py:613` "Maybe reuse these 2 buffer from extend_workspace",
  is ROCm-only and therefore never fires on the rig. Queries tried:
  `web_search "vllm CUDA graph memory pool limit issue"`, `web_search "vllm workspace allocation
  cuda graph pool"`, `grep -rn "use_memory_pool" --include=*.py vllm/`, and the graph-pool grep above.
  → No Section E row.
- **`memory_alloc` — pinned-memory pool limits.** Beyond the `RoutedExpertsTensors` fallback (kept as
  row E5) and `vllm/utils/cache.py:75` ("# Todo: add warning to inform that del pinned item", which is a
  *warning-message* TODO about deleting a pinned LRU entry, not a missing capability), I found no
  admitted pinned-pool limitation. Queries tried:
  `grep -n "Todo\|TODO\|FIXME\|XXX" vllm/utils/*.py | grep -iE "pin|cpu|numpy|host|copy|sync|serial|pickle|thread"`
  (one hit, as quoted) and `web_search "vllm pin_memory pool limit host pinned memory issue"`.
  → No additional Section E row.
