# Verification report — S14 (negative results, undercut papers, sm120 gaps)

Method: all 61 blocks re-parsed; every QUOTE / STATED REASON / ASKING QUOTE / DETAIL re-fetched and
re-located at its URL with `python3 verify.py quote` **and** with a raw-text pass (the shipped checker's
`strip_html` corpus mangles quoted Python source from `github.com/…/blob/…` pages by inserting spaces at
token boundaries); every GitHub artifact's `"state":"…"` / `"stateReason":"…"` token re-read; every arXiv id
re-read with `fetch.py arxiv`; both assigned FA texts re-confirmed at the live `v0.29.0` tag.

## Corrections (rows I changed)

- ROW: B1.2 | FIELD: SECTION | WAS: `OPEN` ("WHO IS STILL ASKING: PyTorch users (pytorch/pytorch #179697) and the SGLang project's own tracking issue #1008") | NOW: `CLOSED — both artifacts are closed` | REASON: pytorch/pytorch #179697 payload reads `"state":"CLOSED"`, `"stateReason":"COMPLETED"`; sgl-project/sglang #1008 likewise reads CLOSED/COMPLETED. Neither is still open, so the row cannot stand as an OPEN ask. | EVIDENCE: https://github.com/pytorch/pytorch/issues/179697 ; https://github.com/sgl-project/sglang/issues/1008 | raw curl via fetch.py, HTTP 200
- ROW: C4.1 | FIELD: S3B | WAS: `OCCUPIED (this RFC is open; …)` | NOW: `issue #20098 is CLOSED (stateReason NOT_PLANNED)` | REASON: payload `"state":"CLOSED"`, `"stateReason":"NOT_PLANNED"`. The row's QUOTE is unaffected. | EVIDENCE: https://github.com/vllm-project/vllm/issues/20098 | raw curl, HTTP 200
- ROW: C4.2 | FIELD: S3B | WAS: `OCCUPIED (both issues open; a second SGLang thread #2278 …)` | NOW: `both cited issues are CLOSED (pytorch #179697 and sglang #1008, each stateReason COMPLETED)` | REASON: as above. | EVIDENCE: same URLs | raw curl, HTTP 200
- ROW: C4.3 | FIELD: S3B | WAS: `OCCUPIED (issue open; the general axis is also tracked in SGLang #1008)` | NOW: `sglang #21855 is CLOSED (stateReason COMPLETED)` | REASON: payload reads CLOSED/COMPLETED. | EVIDENCE: https://github.com/sgl-project/sglang/issues/21855 | raw curl, HTTP 200
- ROW: C4.4 | FIELD: S3B | WAS: `OCCUPIED (issue open; …)` | NOW: `vllm #28234 is CLOSED (stateReason NOT_PLANNED)` | REASON: payload reads CLOSED/NOT_PLANNED. | EVIDENCE: https://github.com/vllm-project/vllm/issues/28234 | raw curl, HTTP 200
- ROW: C4.5 | FIELD: S3B | WAS: `OCCUPIED (issue open; …)` | NOW: `vllm #37618 is CLOSED (stateReason COMPLETED)` | REASON: payload reads CLOSED/COMPLETED. | EVIDENCE: https://github.com/vllm-project/vllm/issues/37618 | raw curl, HTTP 200
- ROW: C1.2, C1.3, C1.5, C1.6, C1.7, C1.9, C1.10, C3.8, C3.9, C4.1, C4.2, C5.5 (12 rows) | FIELD: composite label | WAS: *(none)* | NOW: `add the block label (composite: quoted fragments interleaved with retrieved framing text)` — or split each into separate QUOTE fields | REASON: each STATED REASON interleaves two or more independently-quoted source fragments with the researcher's framing prose (e.g. C1.3: `"…marginally slower than the no-prefetch variant." The evaluation scope is also narrow by the authors' own account: "We report a single batch size of 4…"`). `verify.py quote` therefore returns MISMATCH on the joined value; every fragment individually relocates verbatim (fragment-level check: all MATCH). The brief requires the composite label in exactly this case. | EVIDENCE: per-row URLs, all HTTP 200
- ROW: D1.3 | FIELD: bucket | WAS: `D.1 >1 GPU, borderline` | NOW: `D.5 by the S13 bucket rule` | REASON: the row's own EVIDENCE line says the paper's RQ1 is "explicitly framed around 'single- and dual-GPU configurations'" — i.e. the artifact documents an evaluated single-GPU configuration, which is the D.5 definition used in the sibling hardware file, not D.1. | EVIDENCE: https://arxiv.org/abs/2601.09527 | fetch.py arxiv, HTTP 200
- ROW: C3.6 | FIELD: URL | WAS: `https://github.com/vllm-project/vllm/issues/56553` while ARTIFACT says "PR #56553" | NOW: `https://github.com/vllm-project/vllm/pull/56553` (the `/issues/` form redirects, so the fetch works, but the row should cite the same object it names) | REASON: artifact/URL naming inconsistency; the page is a pull request. | EVIDENCE: https://github.com/vllm-project/vllm/pull/56553 | raw curl, HTTP 200

## Confirmations (rows I checked and could not falsify)

- **The assigned FA claim is CONFIRMED in both halves.** `vllm/v1/attention/backends/fa_utils.py` @ v0.29.0:
  `if device_capability.major == 9 and is_fa_version_supported(3): … elif device_capability.major == 10 and
  is_fa_version_supported(4): # Blackwell (SM100+, restrict to SM100 for now): prefer FA4 … else:
  # Fallback to FA2 / fa_version = 2` — so major == 12 takes the FA2 branch. The shipped doc
  `docs/design/attention_backends.md` @ v0.29.0 says verbatim: "Default is FA4 on SM100+ (Blackwell), FA3
  on SM90 (Hopper), FA2 otherwise." A1.1's block quote and C3.1's `return (fa_version == 3 and
  current_platform.is_device_capability_family(90)) or (fa_version == 4 and
  current_platform.is_device_capability_family(100))` both re-locate verbatim in the raw file.
- All 21 arXiv ids cited with `/abs/` URLs read back with titles matching the rows (A1.6, C1.1–C1.10,
  C2.1–C2.4, C5.1, C5.3, D1.3, D5.1–D5.3), including the FlashAttention-3/4 papers and the withdrawal rows
  C2.1–C2.4 whose `Comments:`-field quotes match the abs pages character-for-character. The three rows whose
  URL is an `/html/` URL were read back at their abs pages too: 2509.14279 = "Towards Robust Agentic CUDA
  Kernel Benchmarking, Verification, and Optimization" (A1.7, C5.2) and 2507.11948 = "Kevin: Multi-Turn RL
  for Generating CUDA Kernels" (C5.4) — both titles as claimed.
- All 20 GitHub artifacts whose state the rows assert were re-read: A1.3 #52016 MERGED; A1.5 #55397,
  A1.9/B1.1 #55571, C3.9 #39302, C3.10 #54331, C3.11 #54225, C4.3-adjacent D-items, D1.2 #56700,
  D2.2 #52540, D3.1 #51920, D3.2 #39235, D5.5 #54125, C5.4b flash-attention #1572 — all OPEN as stated;
  C3.12 #18039 and C3.8/D1.1/D2.1 #56702 CLOSED (NOT_PLANNED for #56702, COMPLETED for #18039) exactly as
  the rows say, including the row's own CLOSURE CAVEAT that #56702 has no human closure reason.
- The sm120 code quotes that the shipped checker fails on (`fa_utils.py`, `flashmla.py`,
  `online/nvfp4.py`, sglang `fp8_utils.py`) were re-checked against the **raw** files and MATCH verbatim;
  the C4.2 second fragment and the C5.5 second fragment MATCH at their own (non-first) URLs —
  sglang #1008 and the GIGAZINE piece respectively.
- C5.2's `robust-kbench` quote, C5.3's TileLang quote, C5.4's Kevin quote, C5.4b's flash-attention quote,
  C2.1–C2.4's withdrawal quotes and all D-row hardware quotes relocate verbatim.

## Could not verify

- Nothing unverifiable. Every quoted fragment in all 61 rows was located in a primary source. Five rows
  (A1.1, C3.1, C3.2, C3.4, C3.7) fail `verify.py quote` only because the checker strips HTML from the
  `github.com/…/blob/…` rendering, which inserts spaces at syntax-highlight token boundaries
  (`is_device_capability_family ( 90 )`); all five are verbatim in the raw `.py` files. A1.2/A1.3 quote doc
  pages that are not in their URL fields — both verbatim at
  `docs/design/attention_backends.md` and `docs/features/quantization/b12x.md` respectively.

## Notes

- **Cross-file taxonomy conflict for the orchestrator.** This file uses `D.5` for "borderline —
  compute-capability-gated paper claim" (D5.1–D5.3) and "borderline — datacenter-Blackwell-only instruction
  / GEMM kernel" (D5.4, D5.5), while `S13.md` defines `D.5` as "published multi-GPU work that explicitly
  documents a single-GPU mode or a single-GPU configuration that was evaluated". If the assembled map merges
  on the bucket label, these two meanings collide; S14's D5.1–D5.3/D5.4–D5.5 are capability-gated items and
  do not carry a single-GPU-mode sentence.
- A1.8's DETAIL contains the phrase `"per-kernel, not per-architecture"` in quotation marks; that is the
  researcher's own characterisation, not a source string (the source comment is quoted verbatim in the same
  row's QUOTE). Cosmetic, no change applied.
- C3.8's fourth fragment, `"CC @hongxiayang @tjtanaa ..."`, is an ellipsis-truncated notification; the
  notification itself is present on the page. The row's CLOSURE CAVEAT (closed `NOT_PLANNED`, no human
  closure reason) is accurate and correctly prevents the quote being read as a maintainer statement.
- Section counts stated at the end of the file (CLOSED 9 · OPEN 3 · ABANDONED 36+1 held out · D 12) match
  the parsed block list; the B-row population is what the B1.2 correction changes.

## Tally
checked=61 corrected=19 dropped=0 verbatim_relocated=61 unverifiable=0
