# Verification report — S2 (compilation & CUDA graphs)

Verifier V1. Same method as S1: every URL re-fetched with `fetch.py gh|get|raw`, every claimed quote
re-located with `verify.py quote` first and against the un-stripped raw bytes where the checker
disagreed, every artifact's state re-read from the page (header token **and** `merged commit` marker
**and** `stateReason`), never from `STATE-TOKEN` alone.

## Corrections (rows I changed)

- ROW: C.2 | FIELD: STATED_REASON | WAS: `"Custom passes (fusion) rely on auto-functionalization V1 and don't work with V2. Addressing this will take extra engineering effort and it is not yet a priority."` | NOW: replace with the two sentences that are actually on the page — `"However, certain custom fusion passes rely on pattern matching and don't currently work with \`auto_functionalized_v2\`."` and `"This is not urgent yet, we can set a timeline when we establish this to be a priority."` — or DROP the row under evidence rule 1
  | REASON: **the quoted sentence does not exist on the page in any form.** It is a paraphrase placed
  inside quotation marks. `verify.py quote` → `MISMATCH`, ratio 0.155, `missing_tail` =
  `1 and don't work with V2. Addressing this will take extra engineering effort and it is not yet a priority.`
  I read the full `fetch.py gh` extract (issue body + all 7 comment bodies): the body's actual sentence
  is "However, certain custom fusion passes rely on pattern matching and don't currently work with
  `auto_functionalized_v2`." and the only "priority" sentence is "This is not urgent yet, we can set a
  timeline when we establish this to be a priority." Both of those re-locate as `MATCH` (ratio 1.0).
  No wording resembling "Addressing this will take extra engineering effort" appears anywhere.
  | EVIDENCE: https://github.com/vllm-project/vllm/issues/14703 | `fetch.py gh`, HTTP 200
- ROW: C.2 | FIELD: SECTION | WAS: `ABANDONED` | NOW: `OPEN`
  | REASON: the row's artifact, vLLM issue #14703, is **still open** — the fetched page text contains
  the state badge `Open` for `#14703`, and the issue carries the `stale`/`unstale` label pair with no
  closure. There is no human statement that the work is dead; the RFC's own text is a deferral
  ("This is not urgent yet"). An OPEN item needs a verbatim "someone is still asking" quote, which the
  corrected reason above supplies.
  | EVIDENCE: https://github.com/vllm-project/vllm/issues/14703 | `fetch.py gh` + raw HTML, HTTP 200
- ROW: A.4 | FIELD: QUOTE | WAS: the assembled log message, presented as a plain `QUOTE:` | NOW: append the label `(composite: quoted fragments interleaved with retrieved framing text — Python implicitly-concatenated adjacent string literals)`
  | REASON: `verify.py quote` → `MISMATCH` ratio 0.158. The retrieved source is
  `"CUDA graph memory profiling is enabled (default since " "v0.21.0). The current --gpu-memory-utilization=%.4f is " …`
  — every word of the row's quote is present, in order, with nothing added or removed, but the file
  text is split across adjacent string literals, so it is not one continuous run. The row's own URL is a
  `blob/main` page; the quote relocates from the equivalent raw URL.
  | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/v1/worker/gpu_worker.py | `fetch.py raw`, HTTP 200
- ROW: A.10 | FIELD: QUOTE | WAS: the warning string as a plain `QUOTE:` | NOW: same composite label
  | REASON: `verify.py quote` → `MISMATCH` ratio 0.29. Source reads
  `"fuse_attn_quant is incompatible with piecewise " "cudagraph when use_inductor_graph_partition is off. " "In this case, splitting_ops will be set to empty " "list, and cudagraph_mode will be set to FULL. " …`
  Words identical and in order; the literal boundaries are the only difference.
  | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/config/compilation.py | `fetch.py raw`, HTTP 200
- ROW: C.12 | FIELD: STATED_REASON | WAS: two fragments joined inside one pair of quotation marks, with the compositeness explained only in prose | NOW: carry the exact label required by the brief — `STATED REASON (composite: quoted fragments interleaved with retrieved framing text)`
  | REASON: the row is honest about the construction in words ("two separately-quoted continuous
  fragments from the same paragraph…") but does not use the mandated label, so the mechanical checker
  reads it as a single MISMATCH (ratio 0.52/0.39). I verified **both fragments independently** and each
  re-locates as `MATCH` at the cited PR: "…did not work together with `--enable-dp-attention`. Three
  separate blockers:" ✔ and "Dynamo cannot trace a `_FuncPtr` call and cannot break out of a context
  manager under `fullgraph`, so compilation fails outright." ✔
  | EVIDENCE: https://github.com/sgl-project/sglang/pull/37192 | `fetch.py gh`, HTTP 200

No S2 row needed a quote **content** change other than C.2.

## Confirmations (rows I checked and could not falsify)

- **A.4 / A.10 / A.11 / A.12 — I specifically tried to find an env var or explicit flag that still
  enables the "unavailable" mode and could not.** vLLM 0.29.0 `envs.py` has exactly three CUDA-graph
  switches — `VLLM_USE_BREAKABLE_CUDAGRAPH` (default `False`), `VLLM_ENABLE_CUDAGRAPH_GC`,
  `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS` (default `True`) — and none of them forces a CUDA-graph
  mode a backend cannot support. `CompilationConfig.resolve_cudagraph_mode_and_sizes` **raises
  `ValueError`** when `min_cg_support == AttentionCGSupport.NEVER` rather than silently proceeding, so
  the downgrade path the row describes is the shipped behaviour. The `VLLM_USE_BREAKABLE_CUDAGRAPH=1`
  spelling quoted in D.1 is real and present at `vllm/envs.py:177,757`.
- **Quote relocation — 32 of 33 rows.** A.1, A.2, A.3, A.5–A.9, B.1–B.3, C.1, C.3–C.11, C.13–C.15,
  D.1–D.3 all re-locate as continuous runs. Spot-checks of the more consequential ones:
  A.2's auto-downgrade paragraph and the FlashInfer `UNIFORM_SINGLE_TOKEN_DECODE` table row (`MATCH`,
  raw docs), A.5's `def profile_cudagraph_memory` stub (`MATCH`, gh-extract), B.1/B.2/B.3's asking
  quotes (`MATCH`), D.1/D.2/D.3's hardware quotes including both GPU-count strings
  ("Initial end-to-end results on 4 x B200, BF16, TP=4, EP=4…" and
  "GPU validation ran through Slurm on a GB200 node with TP=2…") — **no GPU-count words were trimmed**.
- **Status claims, each read from its own page:**
  - A.1 — PR #20059 header `Merged` ✔ (the row's `STATUS: merged; shipped in vLLM 0.29.0` is right)
  - A.5 — issue #49224 `Closed`, `stateReason: COMPLETED`; PR #36520 `Merged` + merged-commit ✔
  - A.9 — PR #29825 `Merged` + merged-commit ✔
  - C.1 — the row claims three PRs; I verified each: #25093 `Merged` + merged-commit, #29558 `Merged`,
    #30910 `Merged` + merged-commit ✔ (all three merged, so the C-section framing "revert of a revert"
    is accurate)
  - C.3 #52205 `Closed` (no merged commit) ✔ · C.4 #53640 `Closed` ✔ · C.5 #52094 `Closed` while
    #51768 `Merged` and #51430 `Merged` ✔ · C.6 #52128 `Closed` ✔ · C.7 #48806 `Closed` ✔ ·
    C.8 #53915 `Closed`, #37363 `stateReason: NOT_PLANNED` ✔ · C.9 #55833 `Closed` ✔ ·
    C.11 #34666 `Closed` ✔ · C.12 #37192 `Closed` ✔ · C.13 #38354 `Closed` ✔ ·
    C.14 #32782 `Closed`, #32655 `Closed`/`COMPLETED` ✔ · C.15 #32606 `Closed` ✔
  - D.1 #55194 `Closed` (WIP, unmerged, as its `WHY IT IS OUT OF REACH` implies) ✔ ·
    D.2 #51768 `Merged` ✔ · D.3 #29558 `Merged`, #30910 `Merged` ✔
- **A.3's capture-size default and docstring, A.6/A.7/A.8's doc quotes** re-locate verbatim at the
  pinned v0.29.0 raw docs; A.8's "`-O3` … Currently equal to `-O2`" is exact.
- C.7's and C.8's `REASON-MAY-HAVE-EXPIRED: no` claims are consistent with what I read; both rows'
  quotes are verbatim (C.7 through the composite of a code expression, C.8 as a continuous paragraph).

## Could not verify

- The A.5 issue thread's *code* claim (`return 0` in the dummy API) is quoted verbatim from the page,
  but I did not run vLLM to confirm the OOM behaviour; that is a runtime claim, not a text claim.
- C.10's retraction table (`−38 %` → `+0.4 % (neutral)`) is present on the page; I did not re-run the
  benchmark.
- The S2 file cites **no** arXiv ID, so no arXiv read-back was required (confirmed: zero `\d{4}\.\d{4,5}`
  matches in the file).

## Notes (anomalies I did not convert into corrections)

- **C.10 is `ABANDONED` while its artifact (issue #55722) is still `Open`.** I did not re-section it:
  the row's stated reason is a **human self-retraction** of the claim ("Retracting the performance
  claim in this issue."), which is a genuine withdrawal, and the row is filed as a *measured negative
  result*, consistent with how the sibling maps use section C.
- **C.10's `STATED REASON` strips the markdown bold:** the page reads
  `**Retracting the performance claim in this issue.** The −38 % does not hold.` The row renders it
  without the `**`. Rendering artefact, no words changed — noted, not corrected.
- **C.2's closure risk.** Under evidence rule 1 the fabricated primary reason is a DROP trigger. The
  row survives only because its `SECOND_REASON` (comment 5: "Update from me: Inductor's pattern matcher
  has separate issues matching in-place operators that I'm working through. I'm not sure the extension
  point is worth adding without fixing the pattern matcher.") is verbatim. If the orchestrator applies
  rule 1 strictly to the primary field, C.2 becomes a DROP rather than a correction.
- **A.11/A.12 cite `github.com/.../blob/...` URLs**, whose rendered page does not contain the file text
  contiguously. Both quotes re-locate verbatim from the equivalent raw URL
  (`raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/server_args.py`), so this is
  a citation-hygiene issue only: the checker cannot mechanically confirm a blob-URL quote.
- **C.15's `REASON-MAY-HAVE-EXPIRED: no`** rests on a gate conditioned on `spec_algorithm.is_none()`;
  that is inside the spec-decode map's territory, so I did not adjudicate it.

## Tally
checked=33 corrected=5 dropped=0 verbatim_relocated=32 unverifiable=0
