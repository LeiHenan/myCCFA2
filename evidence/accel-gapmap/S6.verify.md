# Verification report — S6

Verifier: V2 (adversarial), method per `VERIFIER_BRIEF.md`. Same tooling note as the S5 report:
quotes were re-located with `verify.py quote` plus a code-aware variant (`vcode.py`) and an
un-`strip_html`ed variant, because `fetch.py`'s `strip_html` deletes every span between a `<` and a
later `>` and therefore mangles Python/markdown sources. GitHub state was re-read from the page's
embedded payload (`"state"`, `"mergedTime"`, `"stateReason"`), not from `fetch.py`'s `STATE-TOKEN`
regex (which is a false-positive source — see S5.verify.md Notes).

## Corrections (rows I changed)

- ROW: C1.2 | FIELD: whole row | WAS: `### C1.2 | ABANDONED (per-step input preparation: numpy construction)` — PR #36868 with the author's rationale as STATED REASON | NOW: **DROP** | REASON: the closure is an automated stale-bot closure and there is no human closure statement. The page timeline reads: `github-actions Bot … "This pull request has been automatically marked as stale because it has not had any activity within 90 days."` (2026-06-21) then `github-actions Bot … "This pull request has been automatically closed due to inactivity."` and `github-actions Bot closed this` (2026-07-21). The quoted "reason" is the author's description of the change ("np.repeat(...) is mathematically equivalent to arange[:N] but performs an unnecessary heap allocation and memory copy."), not an abandonment reason. Per the brief's drop rule ("automated stale-bot closure with no human statement ⇒ DROP") the row goes. This was the only stale-bot closure among all ABANDONED rows in S5-S8 (scanned). | EVIDENCE: https://github.com/vllm-project/vllm/pull/36868 | `fetch.py raw` (page text), HTTP 200; state `CLOSED`, `mergedTime` null
- ROW: A1.2 | FIELD: NOTE | WAS: "NOTE (second continuous quote, the compatibility price paid for that elimination, same body): 'Currently, this PR is not compatible with: Pipeline Parallelism / Speculative Decoding / Guided Decoding'" | NOW: re-label as a composite: the body is `Currently, this PR is **not** compatible with:` followed by three separate bullets (`- Pipeline Parallelism`, `- Speculative Decoding`, `- Guided Decoding`), which the block joined with " / " | REASON: not one continuous run — the three build targets are list items, not a slash-separated phrase. All words are present and the substantive claim is correct. | EVIDENCE: https://github.com/vllm-project/vllm/pull/23569 | `fetch.py raw` (raw markdown body), HTTP 200; PR state MERGED (`2025-09-06T01:20:17Z`)
- ROW: C1.1 | FIELD: STATED REASON | WAS: "Where it does NOT help — batch <=16 (loop cost negligible)" | NOW: "'## Where it does NOT help' + bullet '- batch <=16 (loop cost negligible)'" (or drop the em-dash and mark the two-line splice) | REASON: the raw body has the first as a markdown **heading** and the second as a **bullet**; there is no dash between them. The em-dash was inserted by the researcher. Words unchanged; this is an edit to the quoted text, so it is reported. | EVIDENCE: https://github.com/vllm-project/vllm/pull/46112 | `fetch.py raw` (embedded `clipboard-copy` markdown body), HTTP 200; PR state CLOSED, `mergedTime` null, closed by the author `ji24077`
- ROW: A1.5 | FIELD: STATUS | WAS: "closed (stateReason COMPLETED); root cause fixed by the same per-step-CPU-work axis as PR #49919" | NOW: "closed (stateReason COMPLETED); the CPU-thread oversubscription fix #49919 merged, but the reporter's post-fix comment on the **same issue** says the regression was *not* fixed and attributes it to the per-step 12 MB allocation" | REASON: the issue's last substantive comment (comment 6, reporter `romancomplexio`) reads "I've finally tested the throughput on the newer version and, unfortunately, this hasn't fixed the performance regression. In my understanding the problem for us has been the added allocation of a 12MB tensor per every structured decode step. The thread oversubscription was a much smaller problem." "Root cause fixed" overstates what the cited page shows (and contradicts S5 A5.9's RESIDUAL). Bucket stays CLOSED/COMPLETED. | EVIDENCE: https://github.com/vllm-project/vllm/issues/49013 | `fetch.py gh`, HTTP 200 (MATCH on the residual quote)

## Confirmations (rows I checked and could not falsify)

- **A1.2 — both named PRs are MERGED** (#23569 `"state":"MERGED"`, `2025-09-06T01:20:17Z`; #24219
  `"state":"MERGED"`, `2025-09-12T23:34:29Z`) and the headline quote is verbatim: "This results in an
  elimination of the CPU overhead of input preparation and sampling." (raw body of #23569, MATCH, no
  markdown between). The magnitude quote is also verbatim on #24219: "This provides most of the
  speedup of that PR, like +22% rather than +25%." (MATCH).
- **C1.12 — self-withdrawal and numbers confirmed.** Issue #56126 re-read as CLOSED/`COMPLETED`;
  comment 2 verbatim: "Withdrawing this — filed in error and not intended for upstream at this time.
  Apologies for the noise."; comment 3 verbatim: "### The overlap works. The batch split costs more
  than the overlap saves." with the two quoted table rows present exactly
  (`| 64 | 3167.2 | 2018.0 | **−36.3%** |`, `| 128 | 4324.1 | 3176.5 | **−26.5%** |` — the only
  difference is the stripped markdown bold markers).
- A1.1 — both quotes MATCH (`docs/configuration/optimization.md`, `docs/design/arch_overview.md`),
  via code-normalised match for the wrapped docs lines; both pages present at the v0.29.0 tag.
- A1.3 — quote MATCH in `docs/design/model_runner_v2.md`.
- A1.4 — both quotes MATCH on PR #49919; state MERGED (`2026-08-04T17:06:38Z`).
- A1.6 — quote MATCH; PR #12094 MERGED (`2025-01-26T08:42:38Z`).
- A1.7 — quote MATCH; PR #22940 MERGED (`2025-08-15T19:12:13Z`).
- A1.8 — quote MATCH; PR #13790 MERGED (`2025-04-10T19:23:14Z`).
- A1.9 — quote MATCH; PR #37006 MERGED (`2026-03-14T01:37:33Z`).
- A1.10 — quote MATCH on the LMSYS SGLang-v0.4 blog (single trailing-period difference only).
- A1.11 — quote MATCH; SGLang PR #32223 MERGED (`2026-08-01T01:00:51Z`).
- A1.12 — all three quotes re-located in v0.29.0 `vllm/config/vllm.py` (code-normalised; each is a
  wrapped comment or adjacent string literals — lines 1249-1251, 1255-1256, 1269-1270). The
  behavioural claims match the source: hard `ValueError` on explicit `--async-scheduling` with an
  unsupported spec-decoding method or with `disable_padded_drafter_batch=True`; silent `False` when
  `async_scheduling is None` and the model is a pooling model.
- B1.1 — ASKING QUOTE MATCH; SGLang PR #38703 re-read as OPEN.
- B1.2 — ASKING QUOTE MATCH (issue #43240); issue OPEN, PR #56353 OPEN
  ("[Bugfix][CPU] Respect CPU affinity during automatic NUMA binding").
- B1.3 — ASKING QUOTE MATCH and it is the *entirety* of PR #35938's description body
  ("Split out reused constant-buffer changes from #35561"); #35938 re-read as `DRAFT` (page badge and
  payload agree — the block's "still DRAFT" is correct), #54269 OPEN. The "TITLE + STATE ONLY"
  evidence label is over-cautious rather than wrong: the quoted line is genuine body text.
- C1.1 — STATED REASON re-located (see correction); the second quoted line ("The loop is already noted
  as a potential performance bottleneck in-code (`NOTE(woosuk)` in `scheduler.py`)") MATCHes the body;
  PR CLOSED, closed by the author.
- C1.3 — both quotes MATCH; PR #23811 CLOSED, closed by the author `Ronald1995`. S3B claim that #23569
  achieved the same overlap by a different route: confirmed (both merged).
- C1.4 — both quotes MATCH; PR #23391 CLOSED, `closedBy` the author `WoosukKwon`; parent RFC #23233
  re-read as CLOSED/`COMPLETED`.
- C1.5 — both quotes MATCH; PR #51950 CLOSED, closed by the author `nacyot`.
- C1.6 — both quotes MATCH (only a trailing period differs); PR #52870 CLOSED; successor #52881
  re-read as MERGED (`2026-08-19T07:07:41Z`).
- C1.7 — quote MATCH apart from quote-mark style (`'placeholder token'` in the block vs
  `"placeholder token"` on the page — no word change); PR #15801 CLOSED, **closed by its author**
  `lizhigong1988`, not by the stale bot (checked explicitly). The referenced issue #15781 is
  CLOSED/`NOT_PLANNED` with a stale-bot closure and is correctly excluded as a closure reason.
- C1.8 — both quotes MATCH; SGLang PR #23930 CLOSED; cited successor #34876 re-read as OPEN.
- C1.9 — all three quoted fragments MATCH; SGLang PR #36369 CLOSED, closed by the author `sshleifer`.
- C1.10 — STATED REASON MATCH; PR #25924 re-read as **OPEN** — the block's own STATE-NOTE says so
  explicitly, and it is right. Cited issue #22889 OPEN (`stateReason: REOPENED`).
- C1.11 — quotes MATCH; PR #38861 CLOSED (closer `bigPYJ1151`, not the author named in WHO — WHO is
  not a status claim, so no correction); #36487 MERGED (`2026-04-03T10:42:44Z`); #49919 MERGED; the
  `--numa-bind` / `--numa-bind-cpus` claim is source-confirmed
  (`vllm/config/parallel.py:289, 305` — `numa_bind`, `numa_bind_cpus`).
- D1.1 — HARDWARE QUOTE MATCH, including the "4× RTX PRO 6000 Blackwell Server Edition (GB202,
  sm_120), PCIe peer-to-peer, no NVLink, driver 580.126.09, CUDA 13.0, vLLM
  `0.28.1rc1.dev500+g51da0ca66`, model Qwen3.6-27B-FP8 at TP=4" scope caveat (no GPU-count words
  removed).
- arXiv (read back per brief): arXiv:2408.12757 → "NanoFlow: Towards Optimal Large Language Model
  Serving Throughput" — title matches.

## Could not verify

- A1.10 / A1.11 — no independent measurement of the SGLang blog's "half of its time on CPU overhead"
  or of the perf claim in the PR bodies beyond the quoted text; the quotes themselves are verbatim,
  so the rows stand, but the numbers are the authors' own.
- C1.9 — the py-spy percentages are quoted from the PR body only; no raw profile is retrievable.
- A1.4 — the `32k elements` / `parallel_for` grain-size detail is quoted verbatim from the PR but is
  not independently checkable from the v0.29.0 tree (torch internals).

## Notes

- **Stale-bot scan (all four files):** I re-fetched every ABANDONED row's PR page in S5-S8 and
  searched for "automatically closed due to inactivity". Exactly one hit: S6 C1.2 (#36868), dropped
  above. The other automated closure referenced in this file (issue #15781) is already excluded by
  the researcher.
- **Minor quote-mark / markdown artefacts (not corrected):** C1.6 (trailing period), C1.7 (single vs
  double quotes), C1.12 (stripped `**` bold markers), A1.10 (trailing period). No words differ.
- **Cross-file inconsistency:** S6 A1.5 says the #49013 root cause was fixed; S5 A5.9 carries the
  reporter's post-fix comment saying it was not. Fixed on the S6 side (see corrections).
- Two evidence labels in this file are inconsistent with what was actually retrieved and could be
  tightened: B1.3 says "TITLE + STATE ONLY" while its quote is genuine body text (verified), and
  C1.12's row is fine but its `#56126` table quotes live in *comment 3*, not the issue body.
- Systematic source-reconstruction cases (word-exact, not continuous runs): A1.1, A1.4, A1.12. Same
  recommendation as S5: a composite/source-convention label.
- Authoritative states re-read for this file: #23569 MERGED, #24219 MERGED, #49919 MERGED, #12094
  MERGED, #22940 MERGED, #13790 MERGED, #37006 MERGED, #32223 MERGED, #46112 CLOSED, #36868
  CLOSED (stale bot), #23811 CLOSED, #23391 CLOSED, #23233 CLOSED/COMPLETED, #51950 CLOSED, #52870
  CLOSED, #52881 MERGED, #15801 CLOSED, #15781 CLOSED/NOT_PLANNED, #38861 CLOSED, #36487 MERGED,
  #38703 OPEN, #56353 OPEN, #43240 OPEN, #54269 OPEN, #35938 DRAFT, #23930 CLOSED, #36369 CLOSED,
  #25924 OPEN, #56126 CLOSED/COMPLETED, #34876 OPEN, #22889 OPEN, #49013 CLOSED/COMPLETED.

## Tally
checked=28 corrected=3 dropped=1 verbatim_relocated=28 unverifiable=0
