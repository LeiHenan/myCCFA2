# Verification report — E1

Adversarial verification of `E1.md` (vLLM 0.29.0) — **18 NEVER-DISCUSSED rows** (E1–E18).
I did not write E1.md. This is the highest-counter-evidence file in Section E, so the pass was run
first-hand rather than delegated. Every GAP quote was re-fetched from
`raw.githubusercontent.com/vllm-project/vllm/v0.29.0/<path>` and re-located verbatim (whitespace- and
comment-marker-tolerant matcher that returns the line number), corroborated by `grep -n -F` against
`/tmp/vllm-0.29.0`. Absence claims were re-tested with phrasings the researcher did not use on
`vllm-project/vllm`, `sgl-project/sglang`, `pytorch/pytorch`, `flashinfer-ai/flashinfer`,
`Dao-AILab/flash-attention`, plus `web_search` and `arxiv.org/search`.

**Search-integrity note.** Mid-pass the repo-scoped GitHub issues route began returning **HTTP 429**; the
batch helper used first (`/tmp/ghq.py`) is 429-blind and reports `N=0` for a throttled page. Every
load-bearing query was therefore re-run through `/tmp/ghq2.py`, which reports `BLOCKED` and retries with
backoff: **0 of the E1 queries used for a verdict were blocked**, and every exact-string `N=0` below is a
genuine "No results" page (`state=all`). All `is:issue` / `is:pr` queries below were run with `state=all`.

---

## Corrections (rows I changed)

- ROW: **E1:E1** | FIELD: ABSENCE CLAIM + SOURCE-STATE | WAS: *"Explicitly disclosed counter-evidence: the
  string itself does appear once on GitHub — as a quoted diff line inside the review thread of **open PR
  #43340** ... authored by the PR author documenting their own code. It is not indexed by issue/PR search
  ... and no thread anywhere raises the limitation"* — CONFIDENCE MEDIUM | NOW: **DROP (absence
  falsified)** | REASON: three concrete findings at the URL. (1) **#43340 is CLOSED, not open** —
  `"state":"CLOSED"` with no `merged` marker (a merged PR renders `"state":"MERGED"`, as verified on
  control PRs #35228 and #25093). (2) The **exact** GAP string does not appear in #43340 at all (0
  occurrences of `PIECEWISE cudagraphs are not supported for dflash.`); the PR's line reads
  `# PIECEWISE cudagraphs are not supported for dflash **draft forwards**.`, so the disclosure's "the
  string itself does appear" is inaccurate. (3) The review thread **does** contain a human raising the
  constraint — benchislett (PR author, Member) on the `speculator.py` `init_cudagraph_manager` hunk:
  *"@TheEpicDolphin I'm hoping that this constraint could be relieved but haven't checked it out"*, with a
  👍 from TheEpicDolphin. Per the assignment's rule (a commenter raising it counts, unlike a mere diff
  line), the claim "no thread anywhere raises the limitation" is false. Nuance for the orchestrator: this
  is a one-line author remark in a closed PR, not a dedicated report; if the strict "introducing PR ≠
  discussion" rule is applied it becomes a downgrade rather than a drop, but the row's ABSENCE CLAIM text
  cannot stand either way. | EVIDENCE: https://github.com/vllm-project/vllm/pull/43340 | `state.py` raw
  payload read + `fetch.py gh`, HTTP 200.
- ROW: **E1:E10** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"The mock-KV workaround is undocumented
  anywhere outside the source comment; no issue questions its correctness."* — CONFIDENCE HIGH | NOW:
  **DROP (absence falsified)** | REASON: (a) PR #22095 (**MERGED**, "[NVIDIA] Support Flashinfer TRT-LLM
  Prefill Attention Kernel" — the PR that added the prefill path) body states the limitation verbatim:
  *"Currently the TRTLLM prefill kernel only does not support Q-BF16 KV-FP8 O-BF16 config, will directly use
  Q-FP8 KV-FP8 O-FP8/O-NVFP4 after the attn+quant fusion is supported."*; (b) PR #48016 (OPEN, "[Bugfix] Do
  not re-apply q/k/v scales on FlashInfer TRTLLM BF16-Q paths") documents the mock-cache fallback and
  questions its correctness: *"The FlashInfer BF16-Q + FP8-KV TRTLLM prefill fallback dequantizes the KV
  cache into a BF16 mock cache with k/v scales already applied, but still passed bmm1_scale = sm_scale *
  q_scale * k_scale and bmm2_scale = v_scale ... The buggy branches only execute on SM100 with an FP8 KV
  cache and BF16 queries"*; (c) issue #47847 (OPEN) is a bug report on that exact combination
  ("BF16-Q paths also mis-apply q/k/v scales"). The quoted source comment is verbatim, but the gap is
  discussed in three separate threads. | EVIDENCE: https://github.com/vllm-project/vllm/pull/22095 (MERGED)
  ; https://github.com/vllm-project/vllm/pull/48016 (OPEN) ;
  https://github.com/vllm-project/vllm/issues/47847 | `fetch.py gh`, HTTP 200.
- ROW: **E1:E11** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"The TODO is explicit, author-written, and
  uniquely worded"* (no thread raises it) — CONFIDENCE HIGH | NOW: **DROP (absence falsified)** | REASON:
  the change that produced the quoted guard is PR #26130 "[Bugfix] Disable cascade attention with
  FlashInfer" (**MERGED**, mgoin), whose thread raises the limitation in prose: body — *"Using cascade
  attention with FlashInfer on Blackwell seems to break when prefix caching is hit. This eval hangs on the
  first batch of requests"*; review — *"This pull request correctly addresses a bug with cascade attention
  in the FlashInfer backend by disabling the feature. ... make it easier to re-enable the feature once the
  underlying bug is fixed."* The exact TODO string is not on the PR page, but "cascade attention does not
  work with FlashInfer" is raised by humans, including a reviewer. | EVIDENCE:
  https://github.com/vllm-project/vllm/pull/26130 (state: MERGED) | `state.py` + `fetch.py gh`, HTTP 200.
- ROW: **E1:E12** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"#49011 ... enumerates the SM120 editing
  sites and never mentions the non-causal path."* — CONFIDENCE HIGH | NOW: **DROP (absence falsified)** |
  REASON: merged PR #43081 "[SpecDecode] Support DFlash with FlashInfer" — the PR that wires non-causal
  DFlash through FlashInfer — states in its own change list: *"Keep non-causal FlashInfer away from
  unsupported DCP / TRTLLM / **NVFP4** paths."* A human therefore names the non-causal × NVFP4
  unsupportedness in the thread that lands the path (reviewed and merged). Adjacent: #50707 (non-causal
  draft attention asserting in FA2 on SM121), #48234 (NVFP4 + DFlash + torch.compile on SM120). The E1
  researcher never checked #43081. Nuance: #43081 is the likely introducing PR for these guards, so under
  the strict rule this is a downgrade rather than a drop — but the row's claim that no thread raises it is
  false. | EVIDENCE: https://github.com/vllm-project/vllm/pull/43081 (state: MERGED) | `fetch.py gh`, HTTP 200.
- ROW: **E1:E13** | FIELD: ABSENCE CLAIM + CONFIDENCE | WAS: *"verbatim sentence repeated twice in-source,
  zero external hits"* — CONFIDENCE HIGH | NOW: **DROP (absence falsified)** | REASON: the same merged
  PR #43081 sentence names DCP explicitly — *"Keep non-causal FlashInfer away from unsupported DCP / TRTLLM
  / NVFP4 paths."* Exact-string queries are genuinely 0 (`"FlashInfer non-causal prefill is not supported
  with DCP yet"` → 0; `"non-causal prefill" DCP` → 0), but the unsupported combination is raised by a human
  in the merged thread. Same introducing-PR nuance as E1:E12. | EVIDENCE:
  https://github.com/vllm-project/vllm/pull/43081 (state: MERGED) | `fetch.py gh` + `ghq2.py`, HTTP 200.
- ROW: **E1:E6** | FIELD: ABSENCE CLAIM counter-evidence (disclosure only; no drop) | WAS: counter-evidence
  checked = `pytorch/pytorch#157950` only | NOW: add `pytorch/pytorch#161127` | REASON: #161127
  "torch._scaled_mm with blockwise scaling doesn't work with torch.compile" is a nearer thread than the
  one the row checked — it is about `_scaled_mm` scale shapes under `torch.compile` (its error text:
  *"For tensorwise scaling, both scales should be scalar. For rowwise scaling, scale_a should be (16640, 1),
  scale_b should be (1, 5120)"*), stateReason COMPLETED. It does **not** state the 0-D-scale tracing
  limitation the row claims is unraised, so the row survives at MEDIUM — but the disclosure must name
  #161127. | EVIDENCE: https://github.com/pytorch/pytorch/issues/161127 | `fetch.py gh`, HTTP 200.
- ROW: **E1:E7** | FIELD: QUERIES TRIED / disclosure (no drop) | WAS: PR-side sweep limited to #53653,
  #56677 | NOW: add the fuller PCP PR set | REASON: `is:pr "MRV2 PCP"` (state=all) returns 7 PRs —
  #56677 (draft), #53653, #50475, #49564, #49517, #49246, #49109. I read the bodies/keywords of #49564
  (FlashAttention PCP for GQA: benchmark runs use `--enforce-eager`), #49517 (PCP KV update: "fail closed
  outside the validated ... eager ... configuration") and #49246 (DP with MRV2 PCP): **none raises the
  full-cudagraph prohibition**. Adjacent cross-repo work exists (vllm-ascend #14026 patches `pcp_manager`
  for "graph-safe PCP execution"), which does not state the prohibition either. HIGH stands with the wider
  sweep now recorded. | EVIDENCE: https://github.com/vllm-project/vllm/pull/49564 ;
  https://github.com/vllm-project/vllm/pull/49517 ; https://github.com/vllm-project/vllm/pull/49246 |
  `ghq2.py` + `fetch.py gh`, HTTP 200.
- ROW: **E1:E15** | FIELD: ABSENCE CLAIM counter-evidence (disclosure only; no drop) | WAS: *"The backend
  name appears only in source mirrors; there is no tracking issue, no PR, and no user report."* | NOW:
  qualify — a human document discusses the **sibling** class | REASON: a HuggingFace model card document
  ("MiMo-V2.5 int4 — vLLM A100 serving patches", `PATCHES.md`) states: *"MiMo uses SWA attention sinks +
  asymmetric head dims ... vLLM therefore hard-selects `FlashAttentionDiffKVBackend`, which asserts
  FlashAttention-3 → SM90+ only. On A100 (SM80) FA2 supports neither sinks ... nor asymmetric V."* That is
  `FlashAttentionDiffKVBackend`, **not** `TritonAttentionDiffKVBackend`, and it raises different holes
  (SM90 gate, sinks, asymmetric V) than the row's four (quantized KV, per-token-head quant, chunked
  lookback, fused output quant). HIGH stands; the disclosure prevents a reader thinking the DiffKV family
  is wholly untouched. | EVIDENCE:
  https://huggingface.co/spectator2026/MiMo-V2.5-AWQ-int4/raw/484809268e0e487122b4ffe71f951813be7ea043/vllm-patches/PATCHES.md
  | `fetch.py get`, HTTP 200.
- ROW: **E1:E18** | FIELD: ABSENCE CLAIM counter-evidence (disclosure only; no drop) | WAS: *"verbatim
  multi-clause string, zero hits on either route"* | NOW: qualify — the identical message shape is a filed
  bug for another backend | REASON: `is:issue "encoder/decoder cross-attention are not implemented"`
  (state=all) returns 4 threads, of which **#13053** is titled *"[Bug]: Encoder self-attention and
  encoder/decoder cross-attention are not implemented for HPUAttentionImpl when using Gaudi2"* — the same
  boilerplate raise, different backend. The FlashInferImpl variant remains 0-hit (`"are not implemented for
  FlashInferImpl"` → 0), so HIGH stands, but the row should disclose #13053 so the "never-discussed"
  reading is not overstated. | EVIDENCE: https://github.com/vllm-project/vllm/issues/13053 | `fetch.py gh`
  + `ghq2.py`, HTTP 200.
- DISCUSSED-LIST (not a row): **"OCCUPIED (open PR #39773)"** | FIELD: S3b label | WAS: OCCUPIED (open PR
  #39773) | NOW: **SOLVED (MERGED PR #39773)** | REASON: `"state":"MERGED"` on
  https://github.com/vllm-project/vllm/pull/39773 ("[Model Runner V2] Disable piecewise cudagraph mode
  fallback for eagle draft decodes"); its body quote used by the researcher is verbatim — *"The eagle draft
  decodes are currently able to run in PIECEWISE mode. This is problematic for at least one attention
  backend (FlashInfer) ..."*. The row was correctly **not** shipped as Section E (confirmed: no E1 row
  covers autoregressive draft-decode PIECEWISE), but the label is wrong. | EVIDENCE:
  https://github.com/vllm-project/vllm/pull/39773 | `state.py`, HTTP 200.
- CONFIRMED AS CLAIMED (not a correction): **DISCUSSED list — "ngram gpu kernel ... OCCUPIED (open PR
  #54905)"**, **"#49011"**, **"#45258"**, **"#49547"**, **"#53031"** | FIELD: state tokens | WAS: open |
  NOW: **confirmed** | REASON: #54905 `"state":"OPEN"` (title "[Bugfix][Spec Decode] Isolate ngram_gpu
  compile cache" — the compiler-cache gap is named by the PR title, so it was correctly excluded from
  Section E); #49011 OPEN ("[Feature]: nvfp4 KV cache on SM120 ...") with the quoted dtype contract present
  in the body; #45258 OPEN RFC; #49547 OPEN; #53031 OPEN. Recorded here because the assignment asked for it.
  | EVIDENCE: the four URLs above | `state.py`, HTTP 200.

## Confirmations (rows I checked and could not falsify)

Each row's GAP quote was re-located verbatim at the cited line in the `v0.29.0` raw file (local-mirror
`grep -n -F` agrees; source files carry no state token — HTTP 200). Absence re-tested with ≥3 new
phrasings, incl. exact-phrase `is:issue`/`is:pr` and `web_search`.

- E1:E2 — quote at `multi_module_mtp/speculator.py:99`; source confirms the `PIECEWISE→NONE` /
  `FULL_AND_PIECEWISE→FULL_DECODE_ONLY` downgrade. Exact `is:issue`/`is:pr` = 0 (genuine); `"multi-module
  MTP"` = #56701, #54369, #53488, #52688 — exactly the four the row lists, none about cudagraph mode.
  HIGH stands.
- E1:E3 — quote at `breakable_cudagraph.py:162` (thread-local re-entry guard confirmed in source). Exact
  strings 0/0; `BreakableCUDAGraphCapture` = #56506 (ROCm DeepSeek-V4.1 RFC) and #46253 (cross-node NCCL
  capture) — neither mentions nesting. HIGH stands.
- E1:E4 — quote at `piecewise_backend.py:169-172`; the `assert size == "cudagraph_capture_sizes"` +
  `NotImplementedError` confirmed in source. Exact `is:issue` and `is:pr` = 0; broad
  `compile_sizes cudagraph_capture_sizes` = 12 unrelated hits; the only `web_search` hit is
  `errors.standardbeagle.com/.../cudagraph-capture-sizes-not-supported-in-compile-s/`, which I fetched and
  confirmed is an auto-generated ErrLookup error-index page (4.6 KB, no author, no replies, no thread) —
  correctly rejected as a false positive. HIGH stands.
- E1:E5 — quote at `wrapper.py:162-168`, confirmed in source. Exact `is:issue`/`is:pr` = 0 (genuine via
  ghq2); the sole `web_search` hit is the analogous ErrLookup page
  `errors.standardbeagle.com/vllm-project/vllm/aot-compile-is-not-supported-by-the-current-config/`, also
  fetched and confirmed thread-less. HIGH stands.
- E1:E8 — quote at `compilation.py:1027-1036`; the `ValueError("Invalid backend for piecewise
  compilation")` for non-`""/eager/inductor` backends confirmed in source. Exact strings 0; broad
  `piecewise compilation custom backend` = 12 unrelated. MEDIUM stands.
- E1:E9 — docstring at `vit_attn_wrappers.py:4-6`, confirmed in source. Exact string 0;
  `vit_attn_wrappers` 0; `web_search` returns only docs/mirrors. MEDIUM stands.
- E1:E14 — quote at `merge_attn_states.py:119-123`, confirmed in source. Exact strings 0;
  `merge_attn_states fp8` = #48650 (Triton recompiles), #48480 (test RFC), #35792 (MLA fusions), #33123
  (stale ROCm), #33097 (FP8 **output** fusion, closed completed) — matching the row's characterisation;
  none states the FP8-**input** restriction. MEDIUM stands.
- E1:E16 — three quotes at `hpc_attn.py:346-351`, confirmed in source. Three exact strings 0;
  `is:pr "HPC attention"` = #50980 (merged), #56706, #46020 (merged — the PR that added the backend: its
  body contains **no** occurrence of alibi/soft_cap/decoder/encoder), #47443/#47441 (split-K selection),
  #39420 (SM90 backend); `sgl-project/sglang "HPC attention"` = 0. MEDIUM stands.
- E1:E17 — quote at `flash_attn.py:978-981`, confirmed in source. Exact `is:issue`/`is:pr` = 0;
  `output_block_scale fusion` = #51581, #34759 (unrelated); `"fused output quant"` on flashinfer and
  flash-attention = 0. HIGH stands.
- E1:E19-analogue check — no E1 row is a duplicate of another row in E1 (the E4:E1/E5.14 duplicate is a
  cross-file issue, reported in `E4.verify.md`/`E5.verify.md`).
- arXiv leg re-run independently: `piecewise CUDA graph speculative decoding`,
  `CUDA graph attention backend limitations`, `flashinfer attention backend unsupported combinations` →
  "Sorry, your query ... produced no results" for all three. E1.md cites no arXiv ID (verified: no
  `arxiv.org/abs/` and no `NNNN.NNNNN` pattern in the file), so there was no ID to read back.

## Could not verify

- **E1:E1's closing reason**: #43340's page exposes `"state":"CLOSED"` but no `stateReason` field, so I
  cannot say whether it was superseded, abandoned or replaced. The state itself (closed, unmerged) is
  established.
- **E1:E16's #47443/#47441**: titles and states read, bodies not read line-by-line; both concern split-K
  selection, not the ALiBi/soft-cap/encoder exclusions.
- **E1 DISCUSSED list**: I verified state tokens for the artifacts named in the assignment
  (#39773, #54905, #49011, #45258) plus #49547/#53031; the remaining DISCUSSED entries (e.g. #25584,
  #30919, #14881, #51828, #24358, flashinfer #2638/#4546) were not re-state-checked in this pass — they
  are not rows and do not gate any Section E claim.

## Tally
checked=18 corrected=9 dropped=5 verbatim_relocated=18 unverifiable=0
absence_claims_falsified=5
