# Section E — Researcher E4 (NEVER-DISCUSSED negative evidence)

Scope: **SGLang** (local mirror `/tmp/sglang-main/python`, `main`; rig version 0.5.19) across three axes —
`attn_backend`, `compile_cudagraph`, `weight_act_quant`. KV-cache management/quantization and speculative-decoding
*algorithms* are out of scope; attention-backend dtype/layout rejections and CUDA-graph-mode restrictions are in scope.

Method: every gap below is a verbatim source line from the installed tree. Each was then run through the
never-discussed test (repo-scoped GitHub issue+PR search on `sgl-project/sglang`, plus `web_search`).
Every cited line was re-fetched from `https://raw.githubusercontent.com/sgl-project/sglang/main/...` and the
line number below is the **remote** line number on `main` (the local mirror was used only for corroboration;
where local and remote line numbers differ, the remote one is used).

Target rig: one RTX PRO 6000 Blackwell 96 GB, sm120, driver 580, no NVLink; vLLM 0.29.0 / SGLang 0.5.19;
Qwen3-4B dense (full attention, no MoE, no MLA), single GPU.

Count: **17 NEVER-DISCUSSED rows**.

---

### E1 | NEVER-DISCUSSED
GAP (engine's own words): `assert blocksparse_tensors is None, "Block sparsity is not supported on SM120"`
SOURCE: `python/sglang/kernels/ops/attention/fa4_sm120/flash_fwd.py:1091` @ main (SGLang 0.5.19 line)
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/kernels/ops/attention/fa4_sm120/flash_fwd.py#L1091
WHAT IS MISSING: The SGLang-owned SM12x FlashAttention-4 forward kernel (the arch-owned FA4 path added for
sm120) has no block-sparse (blocksparse_tensors) support at all — any caller that asks for block-sparse attention
on sm120 is asserted out rather than falling back. The SM100 FA4 forward in the same tree does accept
`blocksparse_tensors`, so this is an sm120-only kernel-feature hole.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22Block+sparsity+is+not+supported+on+SM120%22` ;
`https://github.com/sgl-project/sglang/issues?q=%22Block+sparsity%22+SM120` ;
`https://github.com/sgl-project/sglang/issues?q=blocksparse+sm120+attention` ;
`https://github.com/sgl-project/sglang/issues?q=fa4+sm120+relative+bias+attention` ;
web_search `SGLang SM120 block sparsity FA4 flash attention not supported`
ABSENCE CLAIM: I did not find it. The three GitHub searches returned either zero results or, for the broadest one,
only unrelated SM90 Sage/SubBlock-sparse diffusion work (PR #37982) and a JIT arch-flag PR (#20103). The kernels
are SGLang-owned (`fa4_sm120/`), so `sgl-project/sglang` is the exhaustive repo for this term.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the exact assert string is unique and returns nothing anywhere, and the
FA4-on-sm120 topic that *is* discussed (issue #36531, PR #32991/#37952) never mentions block sparsity.
TARGET-RIG RELEVANCE: no for the target model (Qwen3-4B is dense full attention, no block sparsity), but the
row bounds what the sm120-native FA4 path can do on this exact GPU.
EVIDENCE: READ BODY

### E2 | NEVER-DISCUSSED
GAP (engine's own words): `"SM120 relative bias currently supports head_dim and " "head_dim_v up to 128"` and
`raise ValueError("SM120 relative bias requires tile_mn=(64, 128)")`
SOURCE: `python/sglang/kernels/ops/attention/fa4_sm120/runtime.py:628` and `:632` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/kernels/ops/attention/fa4_sm120/runtime.py#L626-L633
WHAT IS MISSING: On sm120 the FA4 forward supports attention bias / relative-position bias only for
`max(head_dim, head_dim_v) <= 128` and only at a single hard-coded tile (`tile_mn == (64, 128)`); every other
head-dim or tile request raises. There is no widened-bias or alternative-tile path on SM12x.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=SM120+relative+bias+head_dim+128` ;
`https://github.com/sgl-project/sglang/issues?q=fa4+sm120+relative+bias+attention` ;
`https://github.com/sgl-project/sglang/issues?q=%22relative+bias%22+sm120` ;
web_search `SGLang SM120 FA4 relative bias head_dim`
ABSENCE CLAIM: I did not find it. The two sm120-FA4 threads that exist are about target-verification auto-split
(PR #37952) and the pip-FA4 escape hatch (issue #36531); neither mentions relative/ALiBi bias limits.
CONFIDENCE THE ABSENCE IS REAL: HIGH — "SM120 relative bias" is a distinctive phrase with no hit; the constraint
is a launch-plan assertion that would surface verbatim in any bug report.
TARGET-RIG RELEVANCE: no (Qwen3-4B uses RoPE, not relative bias); it bites ALiBi/relative-bias architectures
trying to use the sm120 FA4 fast path on this GPU.
EVIDENCE: READ BODY

### E3 | NEVER-DISCUSSED
GAP (engine's own words): `"Triton dense one-shot prefill does not support sinks/score_mod"` /
`"Triton dense one-shot prefill does not support sliding windows"` /
`"Triton dense one-shot prefill does not support KV descales"` /
`"Triton dense one-shot prefill does not support xai temperature"`
SOURCE: `python/sglang/srt/layers/attention/triton_backend.py:1797-1812` (inside `_forward_extend_dense_one_shot`,
the `forward_batch.mha_one_shot` MHA path) @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/attention/triton_backend.py#L1797-L1812
WHAT IS MISSING: The Triton backend's dense one-shot prefill kernel (`dense_prefill_attention_fwd`) is a
deliberately guarded path — it raises instead of falling back for four features: attention sinks, score_mod,
sliding window, and KV descales (plus xai temperature). So a Triton-prefill run with `mha_one_shot` cannot serve
sink models, windowed models, or scaled KV at all.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22one-shot+prefill%22+sliding+window` ;
`https://github.com/sgl-project/sglang/issues?q=%22dense+one-shot%22` ;
`https://github.com/sgl-project/sglang/issues?q=%22mha_one_shot%22` ;
`https://github.com/sgl-project/sglang/issues?q=triton+sliding+window+prefill` ;
web_search `SGLang "dense one-shot prefill" sliding window NotImplementedError`
ABSENCE CLAIM: I did not find it. The live sliding-window/sink Triton work (issue #37983, OPEN PR #38142) targets
the **vision** kernel `context_attention_fwd`/`_fwd_kernel`, not the LLM `dense_prefill_attention_fwd` guarded
here; the `mha_one_shot` hits are FlashInfer DP-attention padding (#35876) and DSA/MLA dense-MHA work on AMD
(#30257/#30258/#29050). No thread names the dense-one-shot guard.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the guard strings are unique and unfound, but the neighbourhood
(Triton prefill + sliding window) is actively discussed, so a maintainer could fold this in without a dedicated thread.
TARGET-RIG RELEVANCE: conditional — Qwen3-4B bf16 has no sinks/SWA/xai, so the guards do not fire; they do fire
for windowed models or when KV scales are present on the one-shot path.
EVIDENCE: READ BODY

### E4 | NEVER-DISCUSSED
GAP (engine's own words): `"TRTLLM FMHAv2 prefill on SM120 does not support " "fp8_e4m3 KV cache or skip-softmax."`
SOURCE: `python/sglang/srt/arg_groups/attention_hook.py:149-152` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/arg_groups/attention_hook.py#L149-L152
WHAT IS MISSING: Having just admitted `trtllm_mha` prefill on SM120, the CLI validator rejects the combination
`--attention-backend trtllm_mha --kv-cache-dtype fp8_e4m3` on sm120 outright (and the same for the
`SGLANG_SKIP_SOFTMAX_PREFILL_THRESHOLD_SCALE_FACTOR` skip-softmax probe). The SM120 fmha_v2 prefill kernel
cannot read an fp8 KV cache, so on this GPU class the backend is unusable with the (otherwise fastest) fp8 KV.
This is an attention-backend × kv-dtype *rejection*, not a KV-cache management feature (the KV map owns the latter).
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=sm120+fp8_e4m3+kv+cache+prefill+fmha_v2` ;
`https://github.com/sgl-project/sglang/issues?q=%22FMHAv2%22` ;
`https://github.com/sgl-project/sglang/issues?q=skip-softmax+sm120` ;
`https://github.com/sgl-project/sglang/issues?q=trtllm_mha+sm120`
ABSENCE CLAIM: I did not find it. The `FMHAv2` hits are the feature-enablement PRs (#23112 merged,
#31959 OPEN "sm120 support for trtllm mha prefill", #32272 OPEN non-interleaved paged KV input) — none of them
mentions fp8_e4m3 KV or skip-softmax; #31959's body only claims "flashinfer exposes trtllm_fmha_v2_prefill which
supports SM120".
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the residual dtype restriction is unreported, but the parent
capability is an open PR, so this may be subsumed when that lands.
TARGET-RIG RELEVANCE: yes — sm120 + TRT-LLM MHA prefill + fp8 KV is exactly the rig's configuration space, and
the failure is a hard startup ValueError.
EVIDENCE: READ BODY

### E5 | NEVER-DISCUSSED
GAP (engine's own words): `raise NotImplementedError("TorchFlexAttnBackend does not support non-causal attention for now.")`
SOURCE: `python/sglang/srt/layers/attention/torch_flex_backend.py:268-270` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/attention/torch_flex_backend.py#L268-L270
WHAT IS MISSING: The `flex_attention` backend hard-codes `causal = True` and raises for cross-attention or
encoder-only attention. It therefore cannot serve any encoder/encoder-decoder or cross-attention layer, even
though flex_attention itself supports arbitrary masks. (Same backend also force-disables decode *and* prefill
CUDA graphs and rejects speculative decoding — see the `flex_attention` block in `arg_groups/attention_hook.py`.)
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=torch_flex+non-causal+attention` ;
`https://github.com/sgl-project/sglang/issues?q=flex+attention+non-causal` ;
`https://github.com/sgl-project/sglang/issues?q=flex+attention+cuda+graph+disabled` ;
PR body read: https://github.com/sgl-project/sglang/pull/9947 (the PR that added the backend)
ABSENCE CLAIM: I did not find it. The flex-attention issue/PR surface is tiny: PR #9947 (added the backend,
motivation/Modifications read in full — no mention of causal/non-causal), PR #26517 (unit tests), PR #21940
(unrelated EAGLE sync). No thread raises non-causal or cross-attention support.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the backend has almost no issue history and the `for now` marker is
still in-tree, i.e. no follow-up work exists.
TARGET-RIG RELEVANCE: no for Qwen3-4B (decoder-only causal, and flex is not the rig's default backend).
EVIDENCE: READ BODY

### E6 | NEVER-DISCUSSED
GAP (engine's own words): `("full torch.compile mode", lambda: cfg.enable_torch_compile),` — one of the rules in
`disable_tc_piecewise_cudagraph_if_incompatible`, whose docstring is
`"""TcPiecewise (torch.compile + piecewise) is incompatible with these configurations. Most are torch.compile / dynamo limitations."""`
SOURCE: `python/sglang/srt/arg_groups/cuda_graph_hook.py:182` (rule list) @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/arg_groups/cuda_graph_hook.py#L177-L183
WHAT IS MISSING: Asking for the *more* aggressive `--enable-torch-compile` mode silently turns the
tc_piecewise prefill CUDA graph **off** (`declare_resolution(... Phase.PREFILL, backend=Backend.DISABLED)`),
i.e. the two compilation-based accelerations are mutually exclusive and the operator loses the prefill graph
without any error. There is no supported "compile + piecewise prefill graph" configuration.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22full+torch.compile+mode%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=torch.compile+piecewise+prefill+mutually+exclusive` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22enable_torch_compile%22+piecewise` ;
`https://github.com/sgl-project/sglang/issues?q=piecewise+cuda+graph+torch.compile+incompatible` ;
web_search `SGLang enable-torch-compile piecewise cuda graph disabled`
ABSENCE CLAIM: I did not find it. Broader queries surface only unrelated PCG/compile items (LoRA-PCG #25783,
flashinfer_trtllm MoE capture IMA #26715, XPU graph #25853, MUSA #23633); no thread states the
enable-torch-compile ⟂ tc_piecewise-prefill rule.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the rule name string has zero hits and the interaction is
implemented as a silent disable (no error string to search), which is exactly why nobody has reported it.
TARGET-RIG RELEVANCE: yes — on a single sm120 GPU, `--enable-torch-compile` is a plausible operator flag and it
costs the prefill CUDA graph silently.
EVIDENCE: READ BODY

### E7 | NEVER-DISCUSSED
GAP (engine's own words): `( "GGUF quantization", lambda: ( cfg.load_format == "gguf" or resolved_view(server_args).quantization == "gguf" or check_gguf_file(cfg.model_path) ), ),`
SOURCE: `python/sglang/srt/arg_groups/cuda_graph_hook.py:217-224` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/arg_groups/cuda_graph_hook.py#L217-L224
WHAT IS MISSING: Any GGUF checkpoint (by `--load-format gguf`, by `--quantization gguf`, or by file sniffing)
is excluded from tc_piecewise prefill CUDA graph capture. There is no path that gives a GGUF-quantized model a
piecewise-compiled prefill graph, so GGUF loses prefill-graph acceleration by construction.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=GGUF+piecewise+cuda+graph` ;
`https://github.com/sgl-project/sglang/issues?q=%22GGUF%22+%22cuda+graph%22` ;
`https://github.com/sgl-project/sglang/issues?q=piecewise+cuda+graph+gguf+load_format` ;
`https://github.com/sgl-project/sglang/issues?q=%22GGUF%22+tc_piecewise`
ABSENCE CLAIM: I did not find it. GGUF threads are about MMQ kernel absence (issue #35019, PR #36122), sharded
loading (#28209) and a GGUF scheme split (roadmap #31783 item #26942) — none connects GGUF to CUDA-graph capture.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the incompatibility is encoded only as a rule-lambda with no
user-visible message, so absence from the issue corpus is expected and the search space (repo-scoped "GGUF" +
"cuda graph") is small and was covered.
TARGET-RIG RELEVANCE: partial — only if the operator serves a GGUF Qwen3-4B on this GPU (a realistic single-GPU
choice), in which case prefill graph capture is silently unavailable.
EVIDENCE: READ BODY

### E8 | NEVER-DISCUSSED
GAP (engine's own words): `"kv-canary: piecewise cuda graph is not supported by the current " "SingleForwardManager design; set --cuda-graph-backend-prefill=disabled " "(or =breakable) when canary is enabled"`
SOURCE: `python/sglang/srt/kv_canary/api.py:44-48` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/kv_canary/api.py#L44-L48
WHAT IS MISSING: The KV-canary install path asserts that tc_piecewise prefill CUDA graphs are impossible,
because the canary's SingleForwardManager design cannot express the piecewise split. Enabling canary therefore
forces the operator to downgrade to `=disabled`/`=breakable`; no piecewise-compatible canary design exists.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=kv-canary+piecewise` ;
`https://github.com/sgl-project/sglang/issues?q=%22kv-canary%22` ;
`https://github.com/sgl-project/sglang/issues?q=%22SingleForwardManager%22` ;
`https://github.com/sgl-project/sglang/issues?q=canary+piecewise+cuda+graph`
ABSENCE CLAIM: I did not find it. The canary's own PRs (#26808, #26809, #26813, #26815, #26820) and follow-ups
(#38586, #38596 workspace accounting after graph capture) never state this incompatibility; #38596 touches
"graph capture" only for workspace accounting.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — distinctive terms (`kv-canary`, `SingleForwardManager`) with no
matching discussion, though the feature is young so absence is partly age.
TARGET-RIG RELEVANCE: no for plain serving; yes only if the rig enables KV-canary (a debugging/verification path),
where it silently costs the prefill graph.
EVIDENCE: READ BODY

### E9 | NEVER-DISCUSSED
GAP (engine's own words): `(options.prefill_cuda_graph_backend == Backend.TC_PIECEWISE, "tc_piecewise prefill CUDA graphs are not supported"),` ;
`(options.enable_torch_compile, "torch.compile is not supported"),` ;
`(model_config.quantization is not None, "quantization is not supported"),` ;
`(bool(getattr(model_config, "modelopt_quant", False)), "ModelOpt is not supported"),` ;
`(model_config.dtype not in _SUPPORTED_DTYPES, "FP16 or BF16 only"),` with
`_SUPPORTED_ARCHITECTURES = frozenset({"LlamaForCausalLM", "Qwen2ForCausalLM", "Qwen3ForCausalLM",})`
SOURCE: `python/sglang/srt/model_executor/model_runner_components/startup_weight_load.py:38-45, 343-346, 400, 409-415` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/model_executor/model_runner_components/startup_weight_load.py#L38-L45 ; https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/model_executor/model_runner_components/startup_weight_load.py#L340-L415
WHAT IS MISSING: The capture-safe startup weight-load (loading checkpoint shards while CUDA graphs are being
captured) admits only three dense architectures, only fp16/bf16, only TP1/TP2, and rejects every quantized
checkpoint — including ModelOpt/NVFP4/FP8 — plus torch.compile, LoRA, spec decoding, offload and PP/DP/EP/CP>1.
So the whole "load weights during graph capture" overlap is unavailable for any quantized Qwen3-4B on this rig.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22CAPTURE_SAFE_WEIGHT_SENTINEL%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22capture-safe%22+weights` ;
`https://github.com/sgl-project/sglang/issues?q=startup+weight+load+overlap+cuda+graph+capture` ;
`https://github.com/sgl-project/sglang/issues?q=%22startup+weight+overlap%22`
ABSENCE CLAIM: I did not find it. The identifier `CAPTURE_SAFE_WEIGHT_SENTINEL` returns nothing; the only
"startup weight overlap" hits are PR #32017/#31984 (introduced overlap), #35259 ("Harden startup weight-load
overlap and expand model coverage", OPEN) and #37756 (RL disk sync) — none states the quantization /
torch.compile / architecture exclusions above, and none is an issue reporting them.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the gate reasons are compiled into a list of strings with no
runtime message until a user trips one, and the feature is new (in-file `NOTE(2026-08)`), which also explains
absence; a nearby open PR (#35259) could absorb it.
TARGET-RIG RELEVANCE: yes — `Qwen3ForCausalLM` is explicitly allow-listed, so bf16 Qwen3-4B on this single GPU
can use it, while any FP8/AWQ/NVFP4 Qwen3-4B cannot.
EVIDENCE: READ BODY

### E10 | NEVER-DISCUSSED
GAP (engine's own words): `# TODO(hebiao064): remove this once we have a better way to handle the merge_state_v2 torch.compile issue` followed by `@torch._dynamo.disable()` on `merge_state_v2_wrapper`
SOURCE: `python/sglang/srt/layers/attention/flashattention_backend.py:3677-3680` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/attention/flashattention_backend.py#L3677-L3680
WHAT IS MISSING: The FlashAttention backend's split/merge step is permanently exempted from Dynamo
(`torch._dynamo.disable()`), i.e. the FA merge-state path cannot be captured by torch.compile; there is no
graph-break-free implementation. Every full-attention model using this backend therefore has an uncompiled
segment in its compiled region, and the TODO records that the underlying issue is unresolved.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22merge_state_v2%22` ;
`https://github.com/sgl-project/sglang/issues?q=%22merge_state_v2%22+torch.compile` ;
`https://github.com/sgl-project/sglang/issues?q=merge_state_v2+dynamo` ;
web_search `SGLang merge_state_v2 torch.compile dynamo disable`
ABSENCE CLAIM: I did not find it. The `merge_state_v2` corpus is entirely about the *kernel* (integer overflow
#29602/#29603, PR #29720 64-bit indexing; AMD/XPU chunked-KV merges #34007/#33840/#35045). Disclosure: the
`"merge_state_v2" torch.compile` query returns the *old* Blackwell tracker #5338, where `merge_state_v2` appears
only inside an unrelated import error (`cannot import name 'merge_state_v2' from 'sgl_kernel'`) next to generic
"disable torch compile" advice — no Dynamo/compile problem with the merge wrapper is stated there or anywhere
else; the closest *analogue* is PR #12593 ("[HotFix] Disable torch dynamo for mrope_triton kernel"), a different op.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the search term is unique, the corpus is small and fully enumerated by the
queries above, and none of the hits is about compilation.
TARGET-RIG RELEVANCE: yes — Qwen3-4B full attention on this single GPU is exactly the split-KV/merge path, and
any `--enable-torch-compile` run inherits the uncompiled merge segment.
EVIDENCE: READ BODY

### E11 | NEVER-DISCUSSED
GAP (engine's own words): `- NVIDIA Hopper (SM90 only; the kernels ship sm90a)` (module docstring) and
`raise ValueError( "The hpc_ops attention backend requires an SM90 (Hopper) GPU " f"(the HPC-Ops kernels ship sm90a only), got sm{major}{minor}." )`
SOURCE: `python/sglang/srt/layers/attention/hpc_ops_backend.py:14` and `:136-140` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/attention/hpc_ops_backend.py#L14 ; https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/attention/hpc_ops_backend.py#L135-L140
WHAT IS MISSING: The Tencent HPC-Ops attention backend is hard-gated to `major == 9` — it refuses to construct on
any non-SM90 device (the shipped kernels are sm90a-only), so `--attention-backend hpc_ops` is unavailable on
Blackwell/sm120. There is no sm120 (or even sm100) HPC-Ops attention kernel and no announced plan in-tree.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=hpc_ops` ;
`https://github.com/sgl-project/sglang/issues?q=hpc_ops+attention+backend+hopper` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22HPC-Ops%22+Blackwell` (no matching result) ;
PR body read: https://github.com/sgl-project/sglang/pull/30540 (the PR that added the backend)
ABSENCE CLAIM: I did not find it. The `hpc_ops` corpus is only the integration PRs themselves (#30540 attention
backend, #31390/#32304 follow-ups, #30541/#17832/#17877 MoE/vision, #30247 router GEMM) plus #32846 (a2a error
message); none asks for or discusses non-Hopper support, and PR #30540's body only covers install method and FP8
KV-cache handling.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the constraint is a documented hard requirement of a third-party kernel
library (so nobody may see it as a fixable SGLang gap), not an unreported bug; absence of a *discussion* is
nonetheless verifiable from the small, fully enumerated corpus.
TARGET-RIG RELEVANCE: yes (negative sense) — on the sm120 RTX PRO 6000 this backend simply cannot be selected,
which removes one candidate fast path from the rig's option set.
EVIDENCE: READ BODY

### E12 | NEVER-DISCUSSED
GAP (engine's own words): `logger.warning_once( "Layer '%s' is not supported by AWQMarlin. Falling back to unoptimized AWQ kernels.", prefix, )`
SOURCE: `python/sglang/srt/layers/quantization/awq/awq.py:369-372` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/awq/awq.py#L369-L372
WHAT IS MISSING: For any AWQ layer whose shape/`group_size` fails `check_marlin_supports_layer`, the loader
silently demotes that layer from the Marlin kernel to the generic (slow) AWQ kernel — a per-layer performance
cliff with only a `warning_once`. There is no reported/queued work to widen the Marlin acceptance set for AWQ
layers that fall outside it.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22not+supported+by+AWQMarlin%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22unoptimized+AWQ+kernels%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=awq+marlin+fallback+unoptimized` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22AWQMarlin%22`
ABSENCE CLAIM: I did not find it. Adjacent-but-different: issue #36048 (OPEN) reports "Qwen3.8-27B AWQ has no
usable fast path on single Ada SM89 32GB" — but its mechanism is CUDA-graph hang + MTP request allocation, not the
AWQMarlin layer check; the roadmap's AWQ items are scheme/kernel splits (#21126) and XPU INT4 (#30236).
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the two exact strings are unfound, but a verifier may treat #36048 as
"AWQ fast-path absence already discussed" at a coarser granularity.
TARGET-RIG RELEVANCE: yes — AWQ Qwen3-4B is a realistic single-GPU configuration and the affected layers get the
slow kernel with no error.
EVIDENCE: READ BODY

### E13 | NEVER-DISCUSSED
GAP (engine's own words): `assert weights_padding_cols == 0, ( "_interleave_for_swiglu_fusion does not support K-padded weights; " f"got weights_padding_cols={weights_padding_cols}." )` and
`assert raw_scale_snapshot.shape[0] == w.shape[0], ( "_interleave_for_swiglu_fusion requires no N-padding; " ... )`
SOURCE: `python/sglang/srt/layers/quantization/modelopt_quant.py:1973-1980` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/modelopt_quant.py#L1973-L1980
WHAT IS MISSING: The dense NVFP4 SwiGLU-fusion interleave path requires un-padded weights: any N-padding (which
tensor-parallel sharding introduces) or K-padding aborts weight loading. The same restriction exists twice in the
diffusion runtime (`"Fused NVFP4 SwiGLU does not support K-padded weights"`, `"Fused NVFP4 SwiGLU does not support
N padding"` at `python/sglang/multimodal_gen/runtime/layers/quantization/modelopt_quant.py:110,120`). There is no
padded-weight-capable fused NVFP4 SwiGLU kernel.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22K-padded%22` ;
`https://github.com/sgl-project/sglang/issues?q=%22Fused+NVFP4+SwiGLU%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=%22interleave_for_swiglu_fusion%22` ;
`https://github.com/sgl-project/sglang/issues?q=swiglu+fusion+K-padded+nvfp4` ;
web_search `SGLang NVFP4 SwiGLU fusion K-padded weights`
ABSENCE CLAIM: I did not find it. Disclosure: PR #37378 ("fix(modelopt_fp4): skip NVFP4 swiglu-fusion interleave
for shared experts with swiglu_limit", CLOSED) touches the *same interleave helper* but for a different trigger
(`swiglu_limit` on shared experts), and does not mention K-padding or N-padding; the two diffusion strings have
zero hits.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the helper is mentioned in one merged PR (different cause), so the
*specific* padding restriction is unreported but the code region is not untouched territory.
TARGET-RIG RELEVANCE: yes-ish — an NVFP4 Qwen3-4B MLP is a Blackwell use case and the padding depends on the
TP/shape split, so this assert is reachable on the rig.
EVIDENCE: READ BODY

### E14 | NEVER-DISCUSSED
GAP (engine's own words): `raise ValueError( "Online NVFP4 weight conversion does not support " "requantization from MXFP8 expert checkpoints." )`
SOURCE: `python/sglang/srt/layers/quantization/nvfp4_online.py:330-333` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/nvfp4_online.py#L330-L333
WHAT IS MISSING: The online (runtime) NVFP4 weight-conversion path can dequantize FP8 weights but hard-rejects
MXFP8 (`use_mxfp8`) expert checkpoints, so an MXFP8-quantized MoE checkpoint cannot be converted online to NVFP4
on any hardware — the only route is offline requantization.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22requantization+from+MXFP8%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=Online+NVFP4+weight+conversion` ;
`https://github.com/sgl-project/sglang/issues?q=online+NVFP4+MXFP8+requantization` ;
`https://github.com/sgl-project/sglang/issues?q=%22NVFP4_AWQ%22+requantization`
ABSENCE CLAIM: I did not find it. The roadmap (#31783) lists the *inverse* direction as open work
("ROCm NVFP4-to-MXFP4 online requantization: #29328") and "Support ModelOpt MXFP8 checkpoints … #32538" (OPEN),
but no MXFP8→NVFP4 online path or its absence is discussed.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the error string is unique with zero hits; the online-quantization
roadmap section was read directly.
TARGET-RIG RELEVANCE: no for Qwen3-4B dense (this is an expert/MoE checkpoint path).
EVIDENCE: READ BODY

### E15 | NEVER-DISCUSSED
GAP (engine's own words): `"Current MXFP4 MoE layer is not supported by Marlin."`
SOURCE: `python/sglang/srt/layers/quantization/mxfp4.py:635` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/mxfp4.py#L635
WHAT IS MISSING: The Marlin fallback kernel set has no MXFP4 (E2M1 + E8M0 block-scale) MoE implementation, so an
MXFP4 MoE checkpoint cannot be rescued onto Marlin on hardware where the native MXFP4 kernels are unavailable —
there is no MXFP4-Marlin MoE kernel in the tree.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22not+supported+by+Marlin%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=MXFP4+MoE+Marlin` ;
`https://github.com/sgl-project/sglang/issues?q=%22MXFP4%22+%22Marlin%22+MoE`
ABSENCE CLAIM: I did not find it. The Marlin corpus is about INT4 (`moe_wna16`) and about the **NVFP4**-Marlin
fallback (#22513, #37759, #37946, #31898 "Reduce MXFP4 Marlin repack memory" — repack memory, not the MoE kernel
gap; open issues #38073/#38074 are NVFP4-Marlin memory accounting; #38884 adds a CUTLASS MXFP4 MoE backend for
SM90, i.e. not Marlin). No thread states that MXFP4 MoE cannot use Marlin.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the error string `"not supported by Marlin"` has zero hits and the
Marlin-adjacent threads are about NVFP4/INT4 or about memory accounting, not about an MXFP4 MoE kernel.
TARGET-RIG RELEVANCE: no for Qwen3-4B dense (MoE-only path), but it removes the universal fallback that would
otherwise make MXFP4 MoE checkpoints runnable on non-native hardware.
EVIDENCE: READ BODY

### E16 | NEVER-DISCUSSED
GAP (engine's own words): `raise RuntimeError( "No MXFP8 dense GEMM kernel is available on this device. MXFP8 dense linear " "requires Blackwell (SM100/SM103/SM110/SM120) with FlashInfer, Hopper (SM90) " "with DeepGEMM, or ROCm gfx95." )`
SOURCE: `python/sglang/srt/layers/quantization/fp8_utils.py:653-658` @ main (see also
`python/sglang/srt/layers/quantization/fp8.py:867-873`: `"device has no FlashInfer MXFP8 fallback kernel."`)
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/fp8_utils.py#L653-L658
WHAT IS MISSING: MXFP8 **dense** linear has no SGLang-owned kernel: `dispatch_w8a8_mxfp8_linear` returns
`_unsupported_mxfp8_linear` unless the build is Blackwell+FlashInfer, Hopper+DeepGEMM, or ROCm gfx95. On a
Blackwell device without a working FlashInfer MXFP8 backend (and for weight shapes DeepGEMM cannot serve), the
model fails at runtime rather than falling back to a Triton/bf16 path — there is no in-tree MXFP8 dense GEMM.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22No+MXFP8+dense+GEMM+kernel+is+available%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=MXFP8+dense+linear+unsupported+device` ;
`https://github.com/sgl-project/sglang/issues?q=MXFP8+dense+GEMM+no+kernel+available` ;
`https://github.com/sgl-project/sglang/issues?q=%22no+FlashInfer+MXFP8+fallback+kernel%22` ;
web_search `SGLang MXFP8 dense GEMM no kernel available device`
ABSENCE CLAIM: I did not find it. Adjacent-but-different: PR #34042 ("add flashinfer cute-dsl backend for mxfp8
gemm", CLOSED, WIP for #32950) and #28291/#18326/#18233 are about *selecting/speeding up* an MXFP8 backend on
devices that already have one; roadmap #31783 tracks MXFP8 online quantization (#17449/#31510) and checkpoint
support (#32538). None reports the no-kernel device case.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — the error string has zero hits, and the backend-selection
discussion (#32950, #34042) is explicitly about performance, not about the unsupported fallthrough.
TARGET-RIG RELEVANCE: yes — a Blackwell rig serving an MXFP8 Qwen3-4B hits the FlashInfer dependency directly
(`is_flashinfer_available()` is the only gate), and odd weight shapes can reach the same error via the DeepGEMM path.
EVIDENCE: READ BODY

### E17 | NEVER-DISCUSSED
GAP (engine's own words): `raise RuntimeError( "--fp8-gemm-backend=cutlass is deprecated on this hardware. " "Please switch to DeepGEMM or FlashInfer TRTLLM on SM90/SM100." )` together with
`backend = get_exec().kernel.fp8_gemm_runner_backend` / `if backend == "auto" and get_platform().is_sm120: backend = "cutlass"`
SOURCE: `python/sglang/srt/layers/quantization/fp8_utils.py:766-772` and `:831-833` @ main
URL: https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/fp8_utils.py#L766-L772 ; https://github.com/sgl-project/sglang/blob/main/python/sglang/srt/layers/quantization/fp8_utils.py#L828-L833
WHAT IS MISSING: For **block-wise** W8A8 FP8 GEMM on sm120 the `auto` default is rewritten to the CUTLASS
backend — the same backend the code declares deprecated for every other GPU ("--fp8-gemm-backend=cutlass is
deprecated on this hardware. Please switch to DeepGEMM or FlashInfer TRTLLM on SM90/SM100."). The sm120 auto
path therefore lands on the deprecated kernel and nothing in the tree routes sm120 auto-selection to a
non-deprecated block-FP8 GEMM.
QUERIES TRIED: `https://github.com/sgl-project/sglang/issues?q=%22cutlass+is+deprecated+on+this+hardware%22` (NO RESULTS) ;
`https://github.com/sgl-project/sglang/issues?q=FP8+GEMM+backend+cutlass+deprecated+sm120` ;
`https://github.com/sgl-project/sglang/issues?q=fp8_gemm_runner_backend+sm120+cutlass` ;
`https://github.com/sgl-project/sglang/issues?q=SM120+block+FP8+GEMM`
ABSENCE CLAIM: I did not find it. Adjacent-but-different: issue #23602 ("[Kernel] Add SM120 per-tensor FP8 GEMM
for small-M") and roadmap #31783's merged item "[x] Use cuBLAS for per-tensor FP8 GEMM on SM120: #31961" cover
**per-tensor** FP8; the deprecation string and the forced-cutlass rule are about the **block-wise** runner and
have zero hits. Disclosure: sm120 FP8 DeepGEMM work does exist for *grouped* FP8 (issue #39063 "[Bug] SM120
grouped FP8 DeepGEMM weight preparation skips UE8M0 requantization", PR #39065), but that is the MoE grouped path,
not the dense `--fp8-gemm-backend` selection this rule governs.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM-HIGH — exact-string search is empty; the SM120 FP8 discussion that exists
is explicitly per-tensor and does not address the block-scale runner.
TARGET-RIG RELEVANCE: yes — block-wise FP8 (W8A8 block) on an sm120 RTX PRO 6000 is a mainstream serving config,
and the auto-selected backend is the deprecated one.
EVIDENCE: READ BODY

---

## DISCUSSED (therefore NOT Section E)

* **`tc_piecewise` decode is not implemented** — engine line
  `- "tc_piecewise"     — not implemented for decode; logs a one-shot warning and falls back to "full".`
  (`python/sglang/srt/model_executor/runner/decode_cuda_graph_runner.py:22`) and
  `# Written to both phases so the value is in place when TC_PIECEWISE decode is implemented; today decode ignores it.`
  (`sglang/srt/arg_groups/cuda_graph_hook.py`). **OCCUPIED (open issue #36048)**: the report quotes the runtime
  message verbatim — `cuda_graph_config decode='tc_piecewise' is not yet implemented; falling back to 'full'.` —
  https://github.com/sgl-project/sglang/issues/36048
* **FlashInfer cannot do ragged verify inside a CUDA graph** —
  `"FlashInfer does not support ragged verify in cuda graph; " "disable SGLANG_RAGGED_VERIFY_MODE for this configuration."`
  (`python/sglang/srt/layers/attention/flashinfer_backend.py:716`). **OCCUPIED**: open issues #36481
  (compact ragged verify dies in decode CUDA-graph capture), #39173, and open PR #39257
  (derive compact ragged CUDA graph slots from request width).
* **`trtllm_mha` is not the sm120 default; the docstring says it "does not support SM120"** —
  `Note: trtllm_mha does not support SM120, which will fall back to flashinfer.`
  (`python/sglang/srt/arg_groups/model_override_base.py:302`). **OCCUPIED (open PR #31959, "sm120 support for
  trtllm mha prefill"; also #32272, #23112)**.
* **FlashInfer has no attention-sink support → triton default on sink models** —
  `# FlashInfer does not support attention sinks.` (`model_override_base.py:348-351`). **SOLVED (merged #23139,
  "fix: fallback to triton for attention-sink models (flashinfer unsupported)")**.
* **FA3 vision backend rejected on Blackwell** — `raise ValueError("The 'fa3' backend is not supported on Blackwell GPUs")`
  (`python/sglang/srt/layers/attention/vision.py:1319`). **DISCUSSED**: closed issue #12360
  ("[Bug] Qwen3-VL with FA3 not working on the B200"), PR #29253.
* **No MMQ kernel for I-Matrix GGUF quantization** —
  `# TODO(Isotr0py): Currently, we don't have MMQ kernel for I-Matrix quantization.`
  (`python/sglang/srt/layers/quantization/gguf.py:175`). **OCCUPIED (open issue #35019, "No MMQ kernel for
  I-quant GGUF: 4-6x slower prefill than llama.cpp on the same weights (measured)"; open PR #36122 adds them)**.
* **ModelOpt `int4_awq` / `w4a8_awq` have no native SGLang path** —
  `"int4_awq": "INT4_AWQ_CFG",  # TODO: add support for int4_awq` and
  `"w4a8_awq": "W4A8_AWQ_BETA_CFG",  # TODO: add support for w4a8_awq`
  (`python/sglang/srt/layers/modelopt_utils.py:9-10`). **DISCUSSED (closed issue #19444, "ModelOpt to
  CompressedTensors bridge")**, which states: *"A prototype is implemented for two FP8 schemes; INT4 AWQ and
  W4A8 AWQ are next on the roadmap"* and lists `INT4_AWQ_CFG`, `W4A8_AWQ_BETA_CFG` as "not first-class".
* **Compressed-Tensors W4A16 Sparse24 unsupported** —
  `"Other method (CompressedTensorsW4A16Sparse24) is not supported now"` → **OCCUPIED** (roadmap #31783 →
  issue #31248, PR #31555).
* **Block-quantized `lm_head` is rejected** —
  `raise NotImplementedError("Block-quantized lm_head is not supported; use channel or tensor weight scales for the head.")`
  (`python/sglang/srt/layers/quantization/compressed_tensors/compressed_tensors.py:1035-1037`).
  **SOLVED (merged PR #35228, "[Quant] Load compressed-tensors quantized lm_head instead of value-casting it")**,
  whose body states the limitation and the reason in the same words: *"Block-FP8 heads are rejected with a clear
  error (the vocab-parallel weight loader cannot shard vocab/block_n weight scales)."* The PR also adds
  `get_lm_head_scheme`, i.e. non-block quantized heads now load instead of being value-cast. Related open items:
  issues #35534 (DFlash2 selector + quantized target lm_head), #35797.
* **W4A8-FP8 MoE activation-scale lifecycle** —
  `# TODO: currently only support per tensor quant.` with `layer.a13_scale = None` / `layer.a2_scale = None`
  (`python/sglang/srt/layers/quantization/compressed_tensors/schemes/compressed_tensors_w4a8_fp8_moe.py:208-210`).
  **OCCUPIED (open PR #32173, "[Quant] Support static W4A8 FP8 activation quantization for MoE")**, which states:
  *"Currently W4A8 FP8 MoE only supports dynamic per-token activation quantization. This PR adds support for
  static per-tensor activation quantization"* and adds exactly the `a13_scale`/`a2_scale` lifecycle above.
  Same scheme also has open issue #38573 (literal `chunk_size=128`) and PR #37207 (mixed-precision W4A8-FP8 loading).
* **`--enable-unified-memory` rejects local-attention models** —
  `"--enable-unified-memory does not support local-attention models "` (`flashattention_backend.py:234`).
  **DISCUSSED**: the exact string appears in closed PR #30360 (MiniCPM-SALA support).
* **`moe_wna16 + awq` unsupported** —
  `"The quantization method moe_wna16 + awq is not supported "` (`moe_wna16.py:104`) —
  the exact combination string appears in closed PR #3450.
* **Full-prefill CUDA-graph compatibility rules** — the handler is literally empty
  (`"""Full prefill CG: empty rule list today; see the experimental warning."""`), but rules are being added:
  **OCCUPIED (open PR #32703, "[LoRA] Disable full prefill CUDA graph when LoRA is enabled")**.
* **Breakable CUDA graph needs `cuda-python`** —
  `raise ImportError("Breakable CUDA graph on NVIDIA requires the 'cuda-python' package. ...")` is *not* a real
  gap: `cuda-python>=13.0` is a declared CUDA dependency (`python/pyproject.toml:32`). Related import-failure
  threads exist for non-CUDA platforms (#31995, #32715).

## SEARCH LOG (queries actually issued)

GitHub repo-scoped search (both issues and PRs; `https://github.com/sgl-project/sglang/issues?q=...`, `type=issues` endpoint was unusable — it hangs through this proxy):

1. `tc_piecewise` ; 2. `piecewise cuda graph` ; 3. `over-capture-bs` ; 4. `"tc_piecewise" decode` ;
5. `"not implemented for decode"` (NO RESULTS) ; 6. `trtllm_mha sm120` ; 7. `hpc_ops` ;
8. `"Block sparsity is not supported on SM120"` (NO RESULTS) ; 9. `SM120 relative bias head_dim 128` (NO RESULTS) ;
10. `"one-shot prefill" sliding window` (NO RESULTS) ; 11. `torch_flex non-causal attention` (NO RESULTS) ;
12. `hpc_ops attention backend hopper` (NO RESULTS) ; 13. `sm120 fp8_e4m3 kv cache prefill fmha_v2` (NO RESULTS) ;
14. `flashinfer sm120` ; 15. `triton sliding window prefill` ; 16. `torch_native` ;
17. `"not supported by AWQMarlin"` (NO RESULTS) ; 18. `"add support for int4_awq"` (NO RESULTS) ;
19. `MXFP8 dense GEMM no kernel available` ; 20. `swiglu fusion K-padded nvfp4` (NO RESULTS) ;
21. `FP8 GEMM backend cutlass deprecated sm120` ; 22. `"ragged verify" cuda graph` ; 23. `"SGLANG_RAGGED_VERIFY_MODE"` ;
24. `flex attention non-causal` ; 25. `"not supported on Blackwell" fa3` ; 26. `skip-softmax sm120` ;
27. `"Block sparsity" SM120` ; 28. `"tc_piecewise" decode` ; 29. `piecewise cuda graph torch.compile incompatible` ;
30. `GGUF piecewise cuda graph` ; 31. `kv-canary piecewise` ; 32. `cuda graph decode steps only` ;
33. `breakable cuda graph cuda-python` ; 34. `"int4_awq"` ; 35. `"w4a8_awq"` ; 36. `"I-Matrix"` ;
37. `"Block-quantized lm_head"` (NO RESULTS) ; 38. `"only support per tensor quant"` (NO RESULTS) ; 39. `"K-padded"` ;
40. `"kv-canary"` ; 41. `"CAPTURE_SAFE_WEIGHT_SENTINEL"` (NO RESULTS) ; 42. `"GGUF" "cuda graph"` ;
43. `"enable_torch_compile" piecewise` ; 44. `"cuda-graph-backend-decode"` ; 45. `"moe_wna16"` ;
46. `"SingleForwardManager"` ; 47. `canary piecewise cuda graph` ; 48. `"interleave_for_swiglu_fusion"` ;
49. `awq marlin fallback unoptimized` (NO RESULTS) ; 50. `MXFP8 dense linear unsupported device` ;
51. `"NVFP4" "W4A16" cutedsl` ; 52. `"full torch.compile mode"` (NO RESULTS) ;
53. `"No MXFP8 dense GEMM kernel is available"` (NO RESULTS) ;
54. `"cutlass is deprecated on this hardware"` (NO RESULTS) ; 55. `"requantization from MXFP8"` (NO RESULTS) ;
56. `"not supported by Marlin"` (NO RESULTS) ; 57. `"moe_wna16 + awq"` ; 58. `flex attention cuda graph disabled` ;
59. `attention sinks flashinfer triton fallback` ; 60. `"HPC-Ops" Blackwell` ; 61. `MTP NEXTN full-attention draft worker` ;
62. `unified memory local attention` ; 63. `startup weight load overlap cuda graph capture` ;
64. `"does not support local-attention"` ; 65. `"dense one-shot"` ; 66. `"mha_one_shot"` ;
67. `"unoptimized AWQ kernels"` (NO RESULTS) ; 68. `"ModelOpt" "int4_awq"` ; 69. `"W4A8_AWQ"` ;
70. `"Full prefill CG"` ; 71. `"Fused NVFP4 SwiGLU"` (NO RESULTS) ; 72. `"cuda-python"` ;
73. `"capture-safe" weights` ; 74. `fa4 sm120 relative bias attention` ; 75. `blocksparse sm120 attention` (NO RESULTS) ;
76. `"merge_state_v2"` ; 77. `"FMHAv2"` ; 78. `piecewise cuda graph gguf load_format` ;
79. `torch.compile piecewise prefill mutually exclusive` (NO RESULTS) ; 80. `"cuda graph is disabled" torch native` ;
81. `"merge_state_v2" torch.compile` ; 82. `merge_state_v2 dynamo` ; 83. `block quantized lm_head` ;
84. `lm_head block weight_scale` ; 85. `W4A8 activation per channel per tensor` ; 86. `compressed tensors w4a8 scale` ;
87. `"AWQMarlin"` (NO RESULTS) ; 88. `Online NVFP4 weight conversion` ; 89. `NVFP4_AWQ requantization` ;
90. `MXFP4 MoE Marlin` ; 91. `"MXFP4" "Marlin" MoE` ; 92. `"no FlashInfer MXFP8 fallback kernel"` ;
93. `SM120 block FP8 GEMM` ; 94. `"startup weight overlap"`

Primary pages read in full (via `fetch.py gh`): #36048, #36481, #37983, #19444, #31783 (Quantization 2026 H2
roadmap), #36531, #5338, PR #30540, PR #31959, PR #38142, PR #9947, PR #34042, PR #35228, PR #32173.
Plus `raw.githubusercontent.com` re-fetch of all 19 cited source files to confirm every quote and its remote line
number on `main` (the local mirror `/tmp/sglang-main/python` was used only for corroboration).

web_search queries (two calls, 4 queries each): `SGLang SM120 block sparsity FA4 flash attention not supported` ;
`SGLang tc_piecewise decode not implemented falling back to full` ;
`SGLang "dense one-shot prefill" sliding window NotImplementedError` ;
`SGLang MXFP8 dense GEMM no kernel available device` ; `SGLang SM120 FA4 relative bias head_dim` ;
`SGLang enable-torch-compile piecewise cuda graph disabled` ;
`SGLang merge_state_v2 torch.compile dynamo disable` ; `SGLang NVFP4 SwiGLU fusion K-padded weights`.

## NEGATIVE FINDINGS

* **`/search?q=...&type=issues` endpoint is unusable** through this proxy (hangs indefinitely even with
  `--max-time`); repo-scoped `/<repo>/issues?q=` pages work and return both issues and PRs. Every query above was
  therefore run against `sgl-project/sglang/issues?q=`. Consequence: results are paginated top-10 best-match; a
  match beyond the first page would be missed. I mitigated this by running 2–4 differently-worded queries per gap.
* **Non-SGLang repos yielded nothing usable for these gaps**: `vllm-project/vllm`, `NVIDIA/TensorRT-LLM`,
  `flashinfer-ai/flashinfer`, `Dao-AILab/flash-attention` were not searched per-gap because every cited line is
  SGLang-owned code (including the `fa4_sm120/` kernels, whose adding PR #32991 is titled "feat(attention): add
  architecture-owned SM12x FA4 kernels" — TITLE ONLY) rather than vendored upstream code.
* **No results at all (exact-string queries, both repos searched where applicable)** — queries tried are listed
  in the SEARCH LOG: `"Block sparsity is not supported on SM120"`, `"full torch.compile mode"`,
  `"Block-quantized lm_head"`, `"only support per tensor quant"`, `"CAPTURE_SAFE_WEIGHT_SENTINEL"`,
  `"No MXFP8 dense GEMM kernel is available"`, `"cutlass is deprecated on this hardware"`,
  `"requantization from MXFP8"`, `"not supported by Marlin"`, `"unoptimized AWQ kernels"`,
  `"not supported by AWQMarlin"`, `"Fused NVFP4 SwiGLU"`, `"not implemented for decode"`,
  `"does not support local-attention"` (only PR #30360), `torch.compile piecewise prefill mutually exclusive`,
  `"one-shot prefill" sliding window`, `SM120 relative bias head_dim 128`.
* **arXiv / papers**: no academic venue was searched per-row; these are kernel-dispatch and CLI-validation gaps in
  a specific serving engine, which do not appear in paper literature. Searches issued instead were the GitHub and
  `web_search` queries above.
* **Dropped after the test (do not re-use as absences)**: tc_piecewise decode (#36048), ragged verify in CUDA graph
  (#36481/#39173/#39257), trtllm_mha-on-sm120 (#31959), flashinfer attention sinks (#23139),
  FA3-on-Blackwell vision (#12360), GGUF I-Matrix MMQ (#35019/#36122), ModelOpt int4_awq/w4a8_awq (#19444),
  Compressed-Tensors W4A16 Sparse24 (#31248/#31555), unified-memory local attention (#30360),
  SM120 per-tensor FP8 (#23602/#31961), `moe_wna16 + awq` (#3450), full-prefill-CG rules (#32703),
  breakable-graph `cuda-python` (declared dependency in `python/pyproject.toml:32`),
  block-quantized lm_head (#35228 merged — its body states the same limitation in words),
  W4A8-FP8 MoE activation scales (#32173 open — its body states the current dynamic-only limitation in words).
