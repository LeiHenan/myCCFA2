# Verification report — S8

Verifier: V2 (adversarial), method per `VERIFIER_BRIEF.md`. Quotes were re-located with
`python3 verify.py quote "<url>" "<fragment>"`, plus a code-aware matcher (`vcode.py`) and an
un-`strip_html`ed matcher for source files; GitHub state was read from each page's embedded payload
(`"state"`, `"stateReason"`, `"mergedTime"`), not from `fetch.py`'s `STATE-TOKEN` regex. The
highest-stakes claim in this file — that vLLM 0.29.0 has **no** Prometheus metric for scheduler /
sampling / detokenization time — was checked by exhaustive enumeration of every `"vllm:<name>"` string
in the whole local 0.29.0 tree, not by search.

## Corrections (rows I changed)

- ROW: A1.7 | FIELD: STATUS (+ S3B, + SECTION) | WAS: "STATUS: merged" ; "S3B: SOLVED (merged #138)" | NOW: "STATUS: **closed unmerged — superseded**; collaborator drbh closed #138 with 'closing in favor of more recent work that implements large parts of this PR #202'; the router-side token-decoding/stopping design shipped via merged PR #202, not via #138" ; "S3B: SOLVED-VIA-SUCCESSOR (merged #202 'feat(router): drop requests when client closes the channel', 2023-04-20)". **SECTION: bucket A (CLOSED = shipped / merged / published) is wrong as written** — the row's artifact is a closed-unmerged PR with a human supersede reason, i.e. an abandoned/superseded record (S6 C1.6 and S7 C7.2 are the in-file precedents for that shape); it can stay in A only if the row is re-pointed at #202. | REASON: TGI PR #138 page payload is `"state":"CLOSED"` with `"mergedTime":null`; the UI shows "Closed / njhill wants to merge 4 commits"; there is no `merged commit` marker anywhere on the page; the only "Merged" text on the page belongs to the cross-referenced PR **#202**. `fetch.py gh` prints `STATE-TOKEN: merged` here for exactly that reason — the same false positive that produced S7 C7.2's wrong state. #202 itself re-reads as MERGED (`"mergedTime":"2023-04-20T09:07:41Z"`). The two quoted fragments remain verbatim on #138's body ("Centralizes this logic that's on the critical inference loop path and does it in rust instead of python"; the IncrementalDecoder sentence), so only the state/section is wrong, not the quote. | EVIDENCE: https://github.com/huggingface/text-generation-inference/pull/138 ; https://github.com/huggingface/text-generation-inference/pull/202 | `fetch.py raw` + payload read (377,233 B / 390,267 B), HTTP 200; both fetched this session
- ROW: A1.8 | FIELD: NOTE | WAS: "…the switch exists and is documented in `vllm/benchmarks/latency.py` and `vllm/benchmarks/throughput.py`: 'Do not detokenize the response (i.e. do not include detokenization time in the measurement)'" | NOW: "…documented in both benchmark scripts, with different wording: `throughput.py` (ll. 903-906) reads 'Do not detokenize the response (i.e. do not include detokenization time in the measurement)'; `latency.py` (ll. 69-72) reads 'Do not detokenize responses (i.e. do not include detokenization time in the latency measurement)'." | REASON: the single quoted string is only exact for `throughput.py`. The `latency.py` help text says "responses" and inserts "latency" before "measurement", so the row's claim that this exact string is what both files document is wrong for one of the two. Both flags (`--disable-detokenize`) do ship at v0.29.0. | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/benchmarks/throughput.py ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/benchmarks/latency.py | `fetch.py raw`, HTTP 200 + local mirror `/tmp/vllm-0.29.0` (throughput.py:900-906, latency.py:66-72)

- ROW: C1.5 | FIELD: S3B and REASON-MAY-HAVE-EXPIRED | WAS: "REASON-MAY-HAVE-EXPIRED: The blocker is downstream dashboard compatibility ('used by the Grafana dashboard'), which is a live constraint, so it has not expired. Confirmed unresolved in 0.29.0." ; "S3B: NO-ACTIVE-WORK-FOUND (… independent grep of the 0.29.0 tree still shows both `vllm:request_queue_time_seconds` and the legacy queue-time path in `vllm/v1/metrics/loggers.py`)" | NOW: "REASON-MAY-HAVE-EXPIRED: the doc's 'duplicated queue time' record is now **historical for the V1 engine**: the legacy metric is not implemented in 0.29.0, so what remains is a doc-cleanup item rather than a live duplicate." ; "S3B: NO-ACTIVE-WORK-FOUND (only `vllm:request_queue_time_seconds` exists in the 0.29.0 tree; `vllm:time_in_queue_requests` has 0 occurrences anywhere under `vllm/` — it survives only in `docs/design/metrics.md` ll. 470-491, which quotes the V0 `seq_group.metrics.time_in_queue` code)" | REASON: the second half of the S3B grep claim is false. Full-tree enumeration returns `vllm:request_queue_time_seconds` (`vllm/v1/metrics/loggers.py:923`) but **no** `vllm:time_in_queue_requests` literal or dynamic construction; the only `time_in_queue` string in code is the OTel attribute `gen_ai.latency.time_in_queue` (`vllm/tracing/utils.py:35`). The QUOTE is genuine (raw-doc MATCH) and the row stays ABANDONED; only the "still duplicated" claim is wrong. | EVIDENCE: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/design/metrics.md ; `/tmp/vllm-0.29.0/vllm/v1/metrics/loggers.py` | `fetch.py raw`, HTTP 200 + exhaustive local grep (`grep -rn "time_in_queue" --include=*.py vllm/` → 1 hit, the tracing constant)

## Confirmations (rows I checked and could not falsify)

- **B1.1 — the metric-gap claim holds, verified exhaustively.** A full-tree scan of
  `/tmp/vllm-0.29.0` for every `"vllm:<name>"` literal returns 196 unique strings (Prometheus
  families plus `vllm::` Triton kernel names). A regex filter over that set for
  `tokeniz|sampl|detokeniz|grammar|schedul|logits` returns **zero** matches. There is no per-step
  scheduler, sampling, detokenization, grammar or logits metric family in vLLM 0.29.0. The one
  scheduler-timing signal the row names is also exact: `GEN_AI_LATENCY_TIME_IN_SCHEDULER =
  "gen_ai.latency.time_in_scheduler"` at `vllm/tracing/utils.py:38`. All three ASKING/body quotes on
  RFC #38760 MATCH; the issue re-reads as OPEN.
- **A1.1** — all three quotes MATCH `docs/configuration/optimization.md` (code-normalised where the
  doc wraps lines), including "- **Output processing** -- detokenization, networking, and especially
  streaming token responses use CPU cycles".
- **A1.2** — both quotes MATCH; PR #17546 re-read as MERGED (`2025-05-30T15:17:01Z`);
  `--api-server-count` present at `vllm/entrypoints/launchers/cli_args.py:394`.
- **A1.3** — both quotes and the QPS table MATCH; PR #19946 MERGED (`2025-06-27T04:44:14Z`).
- **A1.4** — quote MATCH; PR #15657 MERGED (`2025-03-29T00:12:02Z`). The row's self-imposed caveat is
  correct (only a saturated BEFORE block is quoted); the claim that the doc warning was removed is
  consistent with the v0.29.0 tree (no performance-penalty warning for
  `--enable-request-id-headers` remains; only a mention in `docs/serving/online_serving/
  openai_compatible_server.md:98`).
- **A1.5** — both quotes MATCH; PR #40848 MERGED (`2026-05-21T04:24:49Z`);
  `VLLM_USE_RUST_FRONTEND` / `VLLM_RUST_FRONTEND_PATH` present at `vllm/envs.py:163,165`.
- **A1.6** — quote MATCH; PR #37461 MERGED (`2026-09-04T15:06:18Z`); both counters present in SGLang
  `main` `observability/metrics_collector.py` (the block's `:922`/`:929` are the constructor-start
  lines — actual `name=` lines are `:923`/`:930`; see Notes).
- **A1.9** — quote MATCH; issue #22249 re-read as CLOSED/`COMPLETED`; fix PR #22247 MERGED.
- **A1.10** — all three quotes MATCH v0.29.0 `docs/design/metrics.md` (the `<https://…>` autolinks
  are stripped by the extractor, so these verify against the raw file).
- **A1.11** — quote MATCH (`grpc_server.py` wrapped comment); `--grpc` registered at
  `vllm/entrypoints/launchers/cli_args.py:408` exactly as claimed.
- **A1.12** — both quotes MATCH the SGLang docs; the SGLang metric namespace in the NOTE is
  source-confirmed (`sglang:scheduler_idle_seconds_total`,
  `sglang:scheduler_process_cpu_seconds_total`, `sglang:scheduler_stage_seconds_total` all in
  `metrics_collector.py`).
- **B1.2** — both body quotes MATCH; issue #47860 OPEN; the named open PRs #50875/#49682/#36685 and
  SGLang #38772 all re-read as OPEN.
- **B1.3** — ASKING QUOTE MATCH; PR #29527 re-reads as `DRAFT` (block says "open PR" — a draft PR is
  open, see Notes); companion PR #35069 OPEN.
- **C1.1** — both quotes MATCH; PR #37455 CLOSED (closed by its author `Jialin`), prerequisite #37461
  MERGED, and `sglang:scheduler_stage_seconds_total` is present in SGLang `main`
  (`metrics_collector.py:937`).
- **C1.2** — both quotes MATCH; PR #21739 MERGED (`2025-08-01T16:16:37Z`); the source-level claims
  reproduce exactly: `--disable-log-requests` has **0** occurrences in the 0.29.0 tree and
  `enable_log_requests: bool = False` is at `vllm/engine/arg_utils.py:2968`.
- **C1.3** — both quotes MATCH; PR #37612 MERGED (`2026-03-20T03:31:44Z`); the 0-occurrence claim for
  `--disable-frontend-multiprocessing` reproduces (0 hits tree-wide).
- **C1.4** — quote MATCH against the raw doc (the `<https://…/pull/4464>` autolink is stripped by the
  extractor, hence a stock-checker MISMATCH); no `vllm:tokens_total` literal exists in the tree.
- **C1.5** — both quotes MATCH against the raw doc (the `<https://…/pull/4464>` autolink is stripped
  by the extractor). Only the *first* half of the row's grep claim survives: the legacy
  `vllm:time_in_queue_requests` metric is **not** in the 0.29.0 tree (see corrections).
- **C1.6** — quote MATCH; `--swap-space`, `vllm:num_requests_swapped` and `vllm:cpu_cache_usage_perc`
  all have 0 occurrences in the 0.29.0 metric-literal set / source.
- **C1.7** — both quotes and the deletion list MATCH; SGLang PR #9474 MERGED (`2025-08-22T01:35:24Z`).
- **C1.8** — all five quoted fragments MATCH; Ray issues #52746 and #59681 both re-read as
  CLOSED/`COMPLETED`. The row's V0-baseline caveat (`VLLM_USE_V1="0"`, `--disable-log-requests`) is
  carried verbatim from the repro command.
- **C1.9** — all three quotes MATCH; Triton issue #8515 re-read as CLOSED/`COMPLETED`.
- **C1.10** — both README fragments MATCH the live `huggingface/text-generation-inference` README
  (fetched); no maintainer activity contradicts the maintenance-mode notice.
- **D1.1** — all three HARDWARE QUOTEs MATCH on RFC #40846 (issue OPEN), including the scope line
  "All benchmarks: Qwen3-0.6B, DP=4 on 4x GB200 GPUs, vllm 0.19.0, request_rate=inf … concurrency=1024
  … chosen to emphasize the ceiling imposed by the current python implementation." No GPU-count words
  removed; the "not a realistic real-world configuration" caveat is preserved in the row's NOTE.
- **D2.1** — all five fragments MATCH; arXiv:2408.00008 title re-verified as "ScaleLLM: A
  Resource-Frugal LLM Serving Framework by Optimizing End-to-End Efficiency".
- **D2.2** — all three fragments MATCH; arXiv:2603.22774 title re-verified as "Characterizing
  CPU-Induced Slowdowns in Multi-GPU LLM Inference".
- Other arXiv read-backs cited in this file: 2510.02758 → "TokenFlow: Responsive LLM Text Streaming
  Serving under Request Burst via Preemptive Scheduling"; 2602.00269 → "VoxServe: Streaming-Centric
  Serving System for Speech Language Models"; 2607.29678 → "TokTier: Exact Stateful CPU+GPU
  Tokenization for Agentic LLM Serving". All titles match; no fabricated IDs.

## Could not verify

- A1.4 — the PR-diff claim that #15657 "removes the documentation warning that
  `--enable-request-id-headers` has a performance penalty at high QPS" cannot be verified from the
  v0.29.0 tree alone (absence of the warning in 0.29.0 is consistent with the claim, but I cannot see
  the pre-PR text).
- C1.8 — the `MODEL_RESPONSE_BATCH_TIMEOUT_MS` 50 ms default and the "direct streaming" remedy are
  quoted from the Ray threads; I did not fetch the Ray source to confirm the default, so the
  expiry argument rests on the quoted reporter/maintainer statements (which MATCH).
- D1.1 — the benchmark numbers are quoted from the RFC but were not independently reproduced.

## Notes

- **Minor descriptor mismatches (not corrected):** B1.1's S3B calls PR #53550 a "draft" — its payload
  state is `OPEN`; B1.3 calls PR #29527 an "open PR" — its payload state is `DRAFT`. Both are open
  items, so no bucket is affected, but the descriptors are swapped.
- **Line numbers:** A1.6's `metrics_collector.py:922`/`:929` are one line off the `name=` lines
  (`:923`/`:930`); C1.1's `:936` is one off `:937`. Per the brief, line numbers drift and should not
  be presented as line-precise on live `main`; the strings themselves are present.
- **Highest-stakes S8 claim — result:** CONFIRMED. vLLM 0.29.0 ships no Prometheus metric for
  scheduler time, sampling time, detokenization time, tokenization time, or CPU time per engine step;
  the OTel-only `gen_ai.latency.*` attributes (including `time_in_scheduler`) are the only such
  signals, and they are gated behind tracing. S8's "we measure X but not Y" rows are well founded.
- **`fetch.py` STATE-TOKEN defect, third occurrence:** `fetch.py gh` reports `STATE-TOKEN: merged`
  for TGI #138, which is CLOSED with `mergedTime: null`; the word comes from the cross-referenced
  merged #202 in the timeline. Any row that used that regex as its state check should be re-read from
  the page payload.
- No verbatim-quote-free rows were found in this file (28/28 primary quotes re-located). Drop rules
  were applied: none of the five "found and read but deliberately NOT given a row" items in the file's
  own list needed re-adjudication (all are correctly excluded for absence of a human closure reason).

## Tally
checked=28 corrected=3 dropped=0 verbatim_relocated=28 unverifiable=0
