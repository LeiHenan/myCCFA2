# S14 — Published negative results, papers undercut by their own stated limitations, withdrawn arXiv submissions, sm120/Blackwell-consumer acceleration gaps, reproducibility failures

Researcher: S14. Target rig: **ONE RTX PRO 6000 Blackwell 96 GB, sm120 (compute capability 12.0)**, driver 580, 208 CPU cores, no NVLink/IB/cluster, vLLM 0.29.0, SGLang 0.5.19, Qwen3-4B (dense, full attention, not MoE/MLA/hybrid-Mamba).
Scope: Section C is the primary output; Section B is secondary and is capped at 3 items. KV-cache management and speculative decoding are out of scope as row subjects; where an artifact touches them, the row's subject is the *kernel / arch / negative-result* fact, not KV or draft policy.

Evidence discipline: every ABANDONED row carries a **verbatim** quote copied from the retrieved page. Every arXiv ID used here was read back with `python3 fetch.py arxiv <id>` and its title confirmed (list in the search log). `api.github.com` and `web_fetch` were not used. Raw text for every quoted page is cached under `/tmp/accelsrc/` and `/tmp/s14/` (`raw_sm120_*`, `raw_neg_*`, `raw_repro_*`, `raw_arxiv_*`, `html/<id>.txt`). **All 43 `STATED REASON` / `HARDWARE QUOTE` strings in this file were machine-checked as verbatim substrings of those saved raw pages (0 unverified).** Withdrawal-comment quotes come from the arXiv abs pages saved as `/tmp/s14/raw_arxiv_comments_<id>.txt`; ARCHIVED-NOTE: `fetch.py arxiv` does not print the `Comments:` field, so the field was extracted separately from the same abs-page HTML (`curl` via `fetch.py`, cached in `/tmp/accelsrc/`).

`S3B` on every C row is an **independent** search (fresh GitHub repo-issue / arXiv query, not one of my own citations) recording whether active work exists: `OCCUPIED (open PR #N / open issue #N)`, `SOLVED (merged #N / shipped in <version>)`, or `NO-ACTIVE-WORK-FOUND (queries: ...)`.
`REASON-MAY-HAVE-EXPIRED` names the constraint that would have to change for the stated limitation to stop applying — with the concrete check I ran.

---

## CLOSED

### A1.1 | CLOSED
QUESTION: On sm120, which FlashAttention version does vLLM 0.29.0 actually select for dense attention — and does the "SM100+" wording in the docs hold on capability 12.0?
WHO: vLLM project (shipped code + shipped docs)
ARTIFACT: `vllm/v1/attention/backends/fa_utils.py::get_flash_attn_version` and `docs/design/attention_backends.md`, tag v0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/fa_utils.py ; https://github.com/vllm-project/vllm/blob/v0.29.0/docs/design/attention_backends.md
STATUS: shipped in v0.29.0 (the rig's version)
EVIDENCE: READ BODY
QUOTE: "        if device_capability.major == 9 and is_fa_version_supported(3):
            # Hopper (SM90): prefer FA3
            fa_version = 3
        elif device_capability.major == 10 and is_fa_version_supported(4):
            # Blackwell (SM100+, restrict to SM100 for now): prefer FA4
            fa_version = 4
        else:
            # Fallback to FA2
            fa_version = 2"
DETAIL: sm120 has major == 12, so it takes the `else` branch: **FA2**. The shipped docs state the rule more loosely — "Default is FA4 on SM100+ (Blackwell), FA3 on SM90 (Hopper), FA2 otherwise." — so the docs and the code disagree for major 12, and the code comment itself flags the gap: "(SM100+, restrict to SM100 for now)". The same file hard-gates FA4 by family: `return (fa_version == 3 and current_platform.is_device_capability_family(90)) or (fa_version == 4 and current_platform.is_device_capability_family(100))`, which is False on sm120 for both terms.

### A1.2 | CLOSED
QUESTION: Which FlashInfer decode kernel exists on sm120, and does the TRT-LLM prefill kernel exist there too?
WHO: vLLM project (shipped code + docs)
ARTIFACT: `vllm/utils/flashinfer.py` (v0.29.0) + `docs/design/attention_backends.md`
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/utils/flashinfer.py
STATUS: shipped in v0.29.0
EVIDENCE: READ BODY
QUOTE: "    SM90 (Hopper) and SM12x support the XQA decode kernel but not TRTLLM
    prefill. SM100+ supports TRTLLM for both phases. All others are unsupported."
DETAIL: The docs concur: "XQA is the SM90 decode path exposed through FlashInfer's TRTLLM decode API. trtllm-gen is used on SM100 and supports sinks." The decode-kernel selector in `vllm/v1/attention/backends/flashinfer.py` matches this: `if current_platform.is_device_capability(90) or current_platform.is_device_capability_family(120): return FlashInferDecodeKernel.XQA` followed by `assert current_platform.is_device_capability_family(100)`.

### A1.3 | CLOSED
QUESTION: Is there a shipped sm120-specific dense-linear kernel family, or is sm120 stuck on generic paths?
WHO: vLLM project (merged PR by lukealonso)
ARTIFACT: PR #52016 "[Kernel] Add B12X dense linear backends" — merged
URL: https://github.com/vllm-project/vllm/pull/52016
STATUS: merged; documented at https://github.com/vllm-project/vllm/blob/v0.29.0/docs/features/quantization/b12x.md
EVIDENCE: READ BODY
QUOTE: "This PR integrates B12X dense linear kernels for NVIDIA SM120 and SM121 GPUs through the existing vLLM linear backend interfaces. B12X is an optional dependency installed with vllm[b12x] and pinned to b12x==1.2.4 ; it is a pure-Python CuTe DSL package and requires no additional vLLM build step."
DETAIL: The shipped doc page opens "NVIDIA SM120 and SM121 GPUs. Install the dependency with:". This is the positive half of the sm120 picture: a native dense-linear path exists (opt-in, `vllm[b12x]`).

### A1.4 | CLOSED
QUESTION: Does the `tile::gather4` (tcgen05/TMEM) MoE path work on sm120, and what does not?
WHO: vLLM project (shipped code, with an explicit note on consumer Blackwell)
ARTIFACT: `vllm/model_executor/layers/fused_moe/utils.py::moe_use_td_hw_supported`, v0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/model_executor/layers/fused_moe/utils.py
STATUS: shipped in v0.29.0 (opt-in; `VLLM_TRITON_USE_TD` unset auto-selects only on XPU)
EVIDENCE: READ BODY
QUOTE: "    ``tile::gather4`` instruction. That instruction is part of the
    ``tcgen05``/Tensor Memory (TMEM) family introduced with Blackwell and has
    no Hopper (sm90) equivalent -- ptxas rejects it there ("Feature
    '.tile::gather4 ...' requires .target sm_100 or higher"). Unlike
    ``scatter4``, ``gather4`` is supported across the whole sm100+ range
    including consumer Blackwell (sm120/sm121): see triton-lang/triton#8498,
    which enables ``gather4`` on sm120/sm121 while leaving ``scatter4``
    unsupported there. So this gates on a blanket ``has_device_capability(100)``
    rather than the sm100 *family* check used for the scatter store path."
DETAIL: Closed answer: gather4 works on sm120/sm121; **scatter4 does not**.

### A1.5 | CLOSED
QUESTION: Is `torch.cuda.is_device_capability_family(100)`-style family checking true on sm120?
WHO: vLLM project (shipped API semantics) + reporter evidence on cc 12.x
ARTIFACT: `vllm/platforms/interface.py::is_device_capability_family` (v0.29.0) and issue #55397
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/platforms/interface.py ; https://github.com/vllm-project/vllm/issues/55397
STATUS: shipped in v0.29.0 / issue open
EVIDENCE: READ BODY
QUOTE: "        Returns True if the device capability is any <major>.x.
        Mirrors CUDA 13 'family' architecture semantics (e.g. 10.x, 11.x, 12.x)."
DETAIL: `return (current_capability.to_int() // 10) == (capability // 10)` ⇒ family(100) is **False** on sm120. Issue #55397 supplies the consequence table for NVFP4 kernel selection on cc 12.x: `FlashInferCuteDslNvFp4LinearKernel` gated by `is_device_capability_family(100)` is "**rejected** — family is 120", so selection falls through to `FlashInferCuteDslNvFp4W4A16LinearKernel`, which is accepted by an explicit `compute_capability in (100, 103) or 120 <= compute_capability < 130` clause; the native W4A4 CUTLASS kernel that "would pass" is "**never reached**".

### A1.6 | CLOSED
QUESTION: Do LLM-generated GPU kernels actually beat PyTorch, as a class?
WHO: KernelBench authors (Ouyang, Guo, Arora, Zhang, Hu, Ré, Mirhoseini — Stanford)
ARTIFACT: KernelBench (arXiv:2502.10517)
URL: https://arxiv.org/abs/2502.10517
STATUS: published (benchmark authors' own headline)
EVIDENCE: READ BODY
QUOTE: "Our experiments across various state-of-the-art models and test-time methods show that frontier reasoning models perform the best out of the box but still fall short overall, matching the PyTorch baseline in less than 20% of the cases."
DETAIL: Closed: the answer is no, on the benchmark's own scoring — and later work (C5.2, C5.3) shows the <20% number is itself an over-estimate under verified evaluation.

### A1.7 | CLOSED
QUESTION: Was the claimed 100x/150x CUDA-kernel speedup from Sakana AI's "AI CUDA Engineer" real?
WHO: Sakana AI (the authors) + independent replications
ARTIFACT: robust-kbench follow-up (arXiv:2509.14279) and the Sakana post-mortem blog
URL: https://arxiv.org/html/2509.14279v1 ; https://sakana.ai/ai-cuda-engineer-post-mortem/
STATUS: published correction (v1 → v2 of the follow-up; blog post-mortem)
EVIDENCE: READ BODY (paper) / BLOG (post-mortem)
QUOTE: "Upon closer inspection, these speedups largely result from the exploitation of KernelBench's loopholes. More specifically, after excluding all contaminated tasks ( Appendix A ), the average speedup is reduced from 3.13x to 1.49x."
DETAIL: Closed question, with the original claim withdrawn by the authors themselves; the blog post-mortem additionally states the heads were not real. Independent measurement is recorded as C5.6.

### A1.8 | CLOSED
QUESTION: Does SGLang have a shipped FP4 MoE path on sm120 at all, or only sm100?
WHO: SGLang project (shipped code, tag v0.5.19 — the rig's version)
ARTIFACT: `python/sglang/srt/layers/quantization/mxfp4.py`
URL: https://github.com/sgl-project/sglang/blob/v0.5.19/python/sglang/srt/layers/quantization/mxfp4.py
STATUS: shipped in 0.5.19
EVIDENCE: READ BODY
QUOTE: "        # When `flashinfer_mxfp4` is enabled, dispatch to one of three FlashInfer
        # entry points depending on the GPU:
        #   - SM100 (Blackwell)  -> trtllm_fp4_block_scale_moe (existing)
        #   - SM120 (Blackwell)  -> cutlass_fused_moe(MXFP8 x MXFP4)
        #   - SM90  (Hopper)     -> cutlass_fused_moe(use_w4_group_scaling=True),
        #                           W4A16 by default (PR #3084) or opt-in
        #                           Humming W4A8 (PR #3738/#4431)"
DETAIL: Closed answer: sm120 has an FP4 MoE path in SGLang, but it is a *different kernel* (`cutlass_fused_moe(MXFP8 x MXFP4)`) from the sm100 one, i.e. different numerics and different tuning. This is the "per-kernel, not per-architecture" character of the sm120 situation.

### A1.9 | CLOSED
QUESTION: Is there a vLLM-provided escape hatch when the sm120-picked FP8 kernel misbehaves?
WHO: vLLM project (shipped env-var kernel disabling) + reporter-verified A/B
ARTIFACT: issue #55571 — `VLLM_DISABLED_KERNELS` / `--enforce-eager`
URL: https://github.com/vllm-project/vllm/issues/55571
STATUS: shipped (env var/flag exist); the underlying crash is unresolved (see B1.1)
EVIDENCE: READ BODY
QUOTE: "| `VLLM_DISABLED_KERNELS=FlashInferFP8ScaledMMLinearKernel` → `CutlassFP8ScaledMMLinearKernel` | CUTLASS | FULL_AND_PIECEWISE | **clean**, 981 questions, identical throughput and latency under load |"
DETAIL: Closed answer: yes — swap the FP8 scaled-MM kernel to CUTLASS (or drop graphs with `--enforce-eager`), at a documented cost (see C4.4).

---

## OPEN (capped at 3 by request)

### B1.1 | OPEN
WHO IS STILL ASKING: carstenkreissl (RTX PRO 5000 72 GB, SM120), plus the same failure family reported by filipsajdak (#52225, #55406) — vLLM issue tracker, open, labels `bug, quantization`
URL: https://github.com/vllm-project/vllm/issues/55571
ASKING QUOTE: "**Expected behaviour.** No illegal memory access; the FlashInfer FP8 scaled-MM kernel should behave like CUTLASS inside CUDA graph replay."
WHAT IS MISSING: A fix (or an upstream FlashInfer fix) for the Xid 13 / illegal-memory-access crash that the reporter localises to `FlashInferFP8ScaledMMLinearKernel` inside piecewise CUDA graphs on SM120; today the only remedies are disabling the kernel or disabling graphs, both of which cost throughput. The reporter explicitly notes "One clean hour is not a proof".
EVIDENCE: READ BODY

### B1.2 | OPEN
WHO IS STILL ASKING: PyTorch users (pytorch/pytorch #179697) and the SGLang project's own tracking issue #1008
URL: https://github.com/pytorch/pytorch/issues/179697 ; https://github.com/sgl-project/sglang/issues/1008
ASKING QUOTE: "For Batch Size 1-64, all torch.compile modes (Default, Reduce-Overhead, Max-Autotune) are significantly slower than Eager mode. The compiler works as expected at Batch Size 512"
WHAT IS MISSING: A compiler configuration (or a documented decision to skip compilation) that is not a net loss in the low-batch, latency-oriented regime this rig targets. SGLang's own issue concedes the axis: "torch.compile can accelerate small batch sizes for llama-3 8B. However,  it is sometimes slower for large batch size or tensor parallelism. We use this issue to track the performance and potential fixes."
EVIDENCE: READ BODY

### B1.3 | OPEN
WHO IS STILL ASKING: Nawrot, Li, Huang, Ruder, Marchisio, Ponti — "The Sparse Frontier" (arXiv:2504.17768, v3)
URL: https://arxiv.org/html/2504.17768v3
ASKING QUOTE: "In the training-free regime, no published method combines a sub-quadratic (or approximate-quadratic) fine-grained estimator with a kernel that effectively exploits the resulting irregular sparsity during prefilling"
WHAT IS MISSING: A sparse-attention kernel that converts fine-grained sparsity into wall-clock time at prefill; the paper's own numbers bound the prize at ~1.5× at 16K ("At 16K tokens, attention represents 40% of prefilling FLOPs (averaged across Qwen 7B–72B), so 5 × \times sparsity yields only 1.5 × \times speedup.") rising to 2.8× at 128K.
EVIDENCE: READ BODY

---

## ABANDONED

### C1.1 | ABANDONED   (papers undercut by their own stated limitations)
WHO: Dmitry Rybin, Yushun Zhang, Ding Tian, Zhihang Lin, Zhi-Quan Luo
ARTIFACT: arXiv:2510.05175 — "Exact Causal Attention with 10% Fewer Operations" (v3, 11 Oct 2025)
URL: https://arxiv.org/abs/2510.05175
STATED REASON: "We note that ECA cannot accelerate fused kernels such as FlashAttention on GPU. This is because ECA requires materialization of large intermediate expressions in the memory, while FlashAttention does not. However, it provides an alternative approach for compute-bound applications and can potentially be useful in scenarios with FLOPs considerations."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` → 3 hits, none for ECA; no upstream implementation issue found in vLLM or SGLang trackers).
REASON-MAY-HAVE-EXPIRED: The blocking constraint is *algorithmic* (FLOP-reduction requires materialising intermediates that IO-aware kernels deliberately never materialise), not a compute-capability gate; nothing about sm120 or a newer FA would remove it. No sm120/CUDA-13 change found that revives it.

### C1.2 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of arXiv:2609.04105 (Hardware-Aware FP4 FlashAttention-4)
ARTIFACT: arXiv:2609.04105 — "Hardware-Aware FP4 FlashAttention-4" (3 Sep 2026)
URL: https://arxiv.org/abs/2609.04105
STATED REASON: "NVIDIA Blackwell executes 4-bit floating-point (FP4) matrix multiplication much faster than bfloat16 (BF16), but FP4 does not accelerate the work between those products." — and, from the results section: "This is also why fewer arithmetic instructions do not always help: independent arithmetic can occupy issue slots while the kernel is waiting for a dependency. 8.3 Takeaway 3: faster arithmetic does not scale the whole kernel". The paper also records a null hardware result: "In a matched control, reducing dynamic shared memory from 209,920 to 163,840 bytes did not improve latency because the 512-column TMEM allocation still allowed only one CTA per streaming multiprocessor." Its scope section states: "The noncausal model experiments are fixed-input inference evaluations rather than finetuning or pretraining studies."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="FP4 FlashAttention" OR "flash attention 4" OR "flash attention 3"` → 12 loose hits, none an FP4-attention kernel port; the shipped sm120 attention paths are XQA decode + FA2, see A1.1/A1.2).
REASON-MAY-HAVE-EXPIRED: Would expire if the softmax/conversion middle stage stopped dominating, e.g. on sm100 B300/GB300 (the paper notes "B300 and GB300 GPUs have doubled the exponential throughput to 32 ops / clock / SM, though these GPUs are not yet widely available at the time of writing"). **sm120 is not that hardware** and is not addressable by this method at all today, because FA4-class kernels are gated to capability family 100 in vLLM.

### C1.3 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of SparDA (arXiv:2606.04511)
ARTIFACT: arXiv:2606.04511 — "SparDA: Sparse Decoupled Attention for Efficient Long-Context LLM Inference" (3 Jun 2026)
URL: https://arxiv.org/abs/2606.04511
STATED REASON: "Even without prefetch, this variant already outperforms Sparse at B4 on both models because the Forecast indexer cuts block-selection latency; at this small batch, the prefetch pipeline adds slight overhead that makes the full SparDA marginally slower than the no-prefetch variant." The evaluation scope is also narrow by the authors' own account: "We report a single batch size of 4 because prefill throughput is insensitive to batch size, as GPUs are already fully utilized during the compute-heavy prefill phase."
EVIDENCE: READ BODY
S3B: OCCUPIED (open issue #48445) — `https://github.com/vllm-project/vllm/issues?q="Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` returns "[RFC]: Support SparDA-style decoupled Forecast projection for lookahead KV prefetch in sparse attention" (#48445, opened Jul 13 2026, still open at retrieval).
REASON-MAY-HAVE-EXPIRED: The regression is batch-size dependent (the paper reports the full pipeline "pulls ahead of the no-prefetch variant" by B16 and is ~40% faster by B64), so the limitation would expire for a serving deployment at B≥16 — but *not* for the single-stream, batch-1 target of this rig. No sm120-specific revision found.

### C1.4 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of PersistentKV (arXiv:2606.26666)
ARTIFACT: arXiv:2606.26666 — "PersistentKV: Page-Aware Decode Scheduling for Long-Context LLM Serving on Commodity GPUs" (v2, 1 Jul 2026; 7-page workshop paper)
URL: https://arxiv.org/abs/2606.26666
STATED REASON: "PersistentKV is close to vLLM but does not beat FlashInfer on this roofline-style test. This result motivates the serving-system question: the advantage must come from low-active scheduling or page-aware execution, not from claiming a universally faster single-call kernel."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` → no PersistentKV hit; `.../issues?q="FlashLLM" OR "SparDA" OR "PersistentKV"` → no hit).
REASON-MAY-HAVE-EXPIRED: The claim is scoped by the authors to "commodity GPUs" and to a roofline-style isolated benchmark; it would expire if a scheduling-layer integration (not a kernel) were shipped in vLLM/SGLang. None found — the decode-kernel lead in these engines remains FlashInfer/XQA.

### C1.5 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of arXiv:2605.16928 (method name "RTPurbo")
ARTIFACT: arXiv:2605.16928 — "Full Attention Strikes Back: Transferring Full Attention into Sparse within Hundred Training Steps" (v2, 8 Jun 2026)
URL: https://arxiv.org/abs/2605.16928
STATED REASON: "For long sequences, even sparse attention remains memory-bound because the selected KV blocks can still span tens of thousands of tokens." The paper's own limitation section adds: "10 Limitation Although RTPurbo achieves strong efficiency–accuracy trade-offs, it still has several limitations. • Dependence on stable head specialization."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="TileMix" OR "LycheeDecode" OR "RTPurbo"` → no hit; `...?q="Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` → 3 hits, none RTPurbo).
REASON-MAY-HAVE-EXPIRED: The bound is a bandwidth argument (sparse selection still streams a large KV working set), so it would only expire with a change in the memory system or a harder sparsity budget — not with sm120. Note the paper also *requires a training phase* (head specialisation is learned), which is itself a limitation class the brief asked about.

### C1.6 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of arXiv:2607.08786
ARTIFACT: arXiv:2607.08786 — "Accelerating GPU Inference of Large Language Models with Moderately Unstructured Sparse Weight Matrices" (13 Jun 2026)
URL: https://arxiv.org/abs/2607.08786
STATED REASON: "Our scheme does not outperform FlashLLM ( Xia et al., 2023 ) under high sparsity. The limitation arises from the storage format, as most regions require zero padding while few contain surplus non-zeros, rendering our redistribution strategy ineffective." The same section reports a second loss: "In this compute-bound regime, our design, optimized for memory-bound decoding with small N N , runs slower than cuBLAS ( Nvidia, 2025b ) ." (spacing/`N N` duplicated in the retrieved HTML rendering).
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="FlashLLM" OR "SparDA" OR "PersistentKV"` → no FlashLLM hit; `...?q=SageAttention` → no port of this format).
REASON-MAY-HAVE-EXPIRED: The paper's own diagnosis is that the *format* is the problem, and it notes a compute-bound regime inversion (slower than cuBLAS). A denser/sparser hardware path (e.g. block-scaled FP4 tensor cores on sm100/sm120) could change the regime; no work found that ports this scheme onto it.

### C1.7 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of LycheeDecode (arXiv:2602.04541)
ARTIFACT: arXiv:2602.04541 — "LycheeDecode: Accelerating Long-Context LLM Inference via Hybrid-Head Sparse Decoding" (4 Feb 2026)
URL: https://arxiv.org/abs/2602.04541
STATED REASON: "As shown in Figure 5 , while the configuration with 4/8 sparse heads exhibits latency comparable to or slightly underperforming the dense FlashAttention-2 baseline, all other settings with a higher degree of sparsity consistently and significantly outperform it." The conclusion concedes deployment readiness: "Finally, while we have achieved considerable speedup, our method is not yet integrated with highly optimized inference serving frameworks like vLLM ( Kwon et al., 2023 ) , which is left for future work."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="TileMix" OR "LycheeDecode" OR "RTPurbo"` → no hit; `...?q="Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` → 3 hits, none LycheeDecode).
REASON-MAY-HAVE-EXPIRED: Two dependencies: (i) the method requires a *training phase* to learn per-head sparsity (its own §4.2), and (ii) integration into a serving engine is absent. Would expire only on a training+integration effort; nothing found.

### C1.8 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of CoSA (arXiv:2607.25291)
ARTIFACT: arXiv:2607.25291 — "CoSA: Accelerating Long-Context Inference via Proxy-Kernel Co-Designed Sparse Attention" (v2, 7 Aug 2026)
URL: https://arxiv.org/abs/2607.25291
STATED REASON: "As future work, we will extend CoSA beyond prefilling to decoding, which requires dedicated proxy and kernel designs due to its distinct query shapes and computational patterns."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (`https://github.com/vllm-project/vllm/issues?q="proxy-kernel" sparse attention` → no hit; the C1.3 group query returned only the SparDA RFC).
REASON-MAY-HAVE-EXPIRED: A decode-side proxy+kernel must be written; the ACCELERATION the paper claims is TTFT-only ("it reduces TTFT by 2.53 × \times at 128K context"), so for a single-stream decode-bound rig the benefit does not exist yet. No sm120-specific port found.

### C1.9 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of arXiv:2606.07713 ("Attention at the Theoretical Minimum")
ARTIFACT: arXiv:2606.07713 — "Attention at the Theoretical Minimum: A Mathematics of Arrays Framework for Memory-Optimal Transformer Kernels" (5 Jun 2026)
URL: https://arxiv.org/abs/2606.07713
STATED REASON: "The shared limitation is that gains are inseparable from the specific hardware platform: they cannot be formally reasoned about, ported, or proved correct on new architectures." The paper's forward plan says the experimental half does not exist yet: "Future work will complete the five-stage implementation roadmap: C reference from DNF , ONF derivation, dimension-lifted ONF , experimental validation on Frontier/Aurora/El Capitan, and backward-pass DNF for full training coverage."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="Beyond FLOPs" OR "pruning" GEMM` → no hit; no implementation artifact referenced by the paper).
REASON-MAY-HAVE-EXPIRED: The paper is a theory/derivation framework whose own roadmap places validation on Frontier/Aurora/El Capitan (exascale machines) — hardware this rig cannot be — and notes gains "cannot be ... ported". Nothing to port to sm120 today.

### C1.10 | ABANDONED   (papers undercut by their own stated limitations)
WHO: authors of arXiv:2605.30571 ("Memory-Bound but Not Bandwidth-Limited")
ARTIFACT: arXiv:2605.30571 — "Memory-Bound but Not Bandwidth-Limited: The Physical AI Inference Gap in Batch-1 LLM Decode" (28 May 2026)
URL: https://arxiv.org/abs/2605.30571
STATED REASON: "Physical-AI decode is memory-dominated, but faster memory does not translate into proportional latency gains." The paper also records a backend swap that *regresses*: "We confirm this directly in Section 6 : swapping sdpa for FA2 on H100 ctx = 2048 {=}2048 slows decode from 17.07 ms to 24.16 ms." (the `{=}2048` token is the retrieved HTML's rendering of `=2048`).
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="FlashDecoding"` → no artifact/maintainer thread; the paper is a self-contained cross-GPU measurement study with a released script payload, not an engine feature).
REASON-MAY-HAVE-EXPIRED: The finding is explicitly about launch-side overhead which "becomes visible on fast GPUs but remains mostly hidden on slower, bandwidth-bound GPUs" — the *opposite* expiry condition from a hardware upgrade: a faster GPU makes it worse, not better. Its scope excludes sm120 (testbed is H100/A100-80GB/L40S/L4), so on this rig the finding is unverified rather than expired.

### C2.1 | ABANDONED   (withdrawn / retracted arXiv submissions in inference acceleration)
WHO: authors of arXiv:2604.06798 (MoBiE)
ARTIFACT: arXiv:2604.06798 — "MoBiE: Efficient Inference of Mixture of Binary Experts under Post-Training Quantization" — withdrawn
URL: https://arxiv.org/abs/2604.06798
STATED REASON: "Although previously revised, per strict university regulations regarding incorrect affiliation, I am unauthorized to retain this manuscript. Furthermore, fundamental derivation errors in the NGES section compromise the mathematical framework, alongside misleading overlapping wording. The paper is therefore withdrawn"
EVIDENCE: READ BODY (withdrawal note in the arXiv `Comments:` field, retrieved from the abs page)
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q=MoBiE binary experts` → no hit; `...?q="Mixture of Binary Experts"` → no hit).
REASON-MAY-HAVE-EXPIRED: Not a hardware constraint — the withdrawal is due to derivation errors; the constraint that would "expire" is a corrected re-derivation, and no successor paper/implementation was found.

### C2.2 | ABANDONED   (withdrawn / retracted arXiv submissions in inference acceleration)
WHO: authors of arXiv:2605.09735 (KV-RM)
ARTIFACT: arXiv:2605.09735 — "KV-RM: Regularizing KV-Cache Movement for Static-Graph LLM Serving" — withdrawn
URL: https://arxiv.org/abs/2605.09735
STATED REASON: "Withdrawn by the authors. The authors identified substantive errors that affect the interpretation of the results and the support for the main conclusions. The current version should not be relied upon"
EVIDENCE: READ BODY (arXiv `Comments:` field)
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="static-graph" LLM serving` → no matching thread; the static-graph/CUDA-graph tracks that do exist are about capture cost and crashes, C4.1/C3.8).
REASON-MAY-HAVE-EXPIRED: The subject (static-graph executors for irregular KV movement) is exactly this rig's deployment style, so the question is live; but the specific result is retracted by its authors and no corrected version or implementation was found.

### C2.3 | ABANDONED   (withdrawn / retracted arXiv submissions in inference acceleration)
WHO: authors of arXiv:2608.30076
ARTIFACT: arXiv:2608.30076 — "Budget-Aware Compression Pipeline for Single-GPU LLM Inference: Methods, Trade-offs, and Coupling Effects" — withdrawn
URL: https://arxiv.org/abs/2608.30076
STATED REASON: "Withdrawn because the submission was made without the required authorization from a co-author for public disclosure"
EVIDENCE: READ BODY (arXiv `Comments:` field)
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q="Budget-Aware Compression"` → no hit).
REASON-MAY-HAVE-EXPIRED: Non-technical withdrawal (authorisation). Recorded because the artifact is squarely single-GPU inference compression — a rare, directly-relevant single-GPU study that is no longer citable.

### C2.4 | ABANDONED   (withdrawn / retracted arXiv submissions in inference acceleration)
WHO: authors of arXiv:2606.03026
ARTIFACT: arXiv:2606.03026 — "Spike-Aware INT8 Execution for Spiking Language Models on Commodity CPUs" — revised with results removed
URL: https://arxiv.org/abs/2606.03026
STATED REASON: "13 pages, 3 figures, 11 tables. Revised presentation and evaluation; added numerical validation and an ARM wall-power case study; withdrawn invalid pure-INT4 results and clarified comparison protocols"
EVIDENCE: READ BODY (arXiv `Comments:` field — this is a *downward revision of a headline result* rather than a full withdrawal)
S3B: NO-ACTIVE-WORK-FOUND (queries: `https://github.com/vllm-project/vllm/issues?q=Spike-Aware INT8` → no hit; subject is a CPU runtime, not the rig's engine).
REASON-MAY-HAVE-EXPIRED: The withdrawn sub-result is the INT4 claim; INT8 remains. No re-issued INT4 result found.

NOTE: Two further withdrawn inference-acceleration submissions were read back and verified but are held out of the row count as hardware-off-target and evidence-thin: arXiv:2512.24713 "FPGA Co-Design for Efficient N:M Sparse and Quantized Model Inference" ("Withdrawn due to substantial inconsistencies between the machine-learning pipeline and the independently developed FPGA-based hardware accelerator. The manuscript does not reflect a coherent, jointly developed system and a clearly integrated methodology") and arXiv:2606.04920 "Toward Multi-Domain and Long-Tailed Quantization via Feature Alignment and Scaling" ("Withdrawn by the submitter because the manuscript was submitted prematurely and requires further revision and final author/contributor approval"). Both verified by `fetch.py arxiv` read-back; both non-technical / non-GPU.

### C3.1 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: vLLM project (shipped v0.29.0 code)
ARTIFACT: `vllm/v1/attention/backends/fa_utils.py` — FA3/FA4 availability
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/fa_utils.py
STATED REASON: "    return (fa_version == 3 and current_platform.is_device_capability_family(90)) or (
        fa_version == 4 and current_platform.is_device_capability_family(100)
    )" — i.e. quantised-query/sink-capable FA3 requires Hopper-family and FA4 requires datacenter-Blackwell-family; on sm120 both terms are False.
EVIDENCE: READ BODY
S3B: SOLVED for FA2/XQA (shipped: A1.1/A1.2) but **NOT** for FA3/FA4 on sm120 — `https://github.com/vllm-project/vllm/issues?q="FP4 FlashAttention" OR "flash attention 4" OR "flash attention 3"` (12 loose hits, no sm120 FA3/FA4 enablement); `...?q="flash attention 4" CUTLASS` → no sm120 port.
REASON-MAY-HAVE-EXPIRED: This is the single highest-value expiry to watch: FA4's own paper targets sm100 only, and vLLM's comment says "restrict to SM100 for now". The stated reason expires if either (a) FlashAttention ships CuTe-DSL kernels for capability 12.x, or (b) vLLM lifts the `major == 10` branch. Checked at retrieval: neither has happened (the branch is still `major == 10`, and the FA4 paper's evaluation is B200-only).

### C3.2 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: vLLM project (shipped v0.29.0 code)
ARTIFACT: `vllm/v1/attention/ops/flashmla.py` — FlashMLA dense/sparse support predicates
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/flashmla.py
STATED REASON: "    if not current_platform.is_device_capability_family(90):
        return False, "FlashMLA Dense is only supported on Hopper devices."" and "            "FlashMLA Sparse is only supported on Hopper and Blackwell DC devices.","
EVIDENCE: READ BODY
S3B: OCCUPIED (open issue #56564, open issue #56563, open issue #54434) — `https://github.com/vllm-project/vllm/issues?q=FlashMLA OR "sparse MLA"` returns "[Performance]: GLM-5.3-Flash on H100: auto-selected FLASHINFER_MLA_SPARSE_SM90 is 36-70% slower than FLASH_ATTN_MLA_SPARSE" (#56564), a TMA-descriptor bug thread (#56563), and #54434 which states "the prescribed `FLASHMLA_SPARSE` gates to `major in [9, 10]`".
REASON-MAY-HAVE-EXPIRED: The gate is a shipped capability-family check; it would expire if FlashMLA gained an sm120 build. Evidence at retrieval says the opposite direction for this rig: vLLM/SGLang route sm120 to FlashInfer's `sparse_mla_sm120` instead (SGLang #39302 quotes the source comment "On SM120 (Blackwell Desktop / RTX PRO 6000) the flash_mla CUDA kernel is not available").

### C3.3 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: vLLM project (shipped v0.29.0 code)
ARTIFACT: `vllm/v1/attention/backends/mla/indexer.py` — MXFP4 indexer KV dtype
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/mla/indexer.py
STATED REASON: "            "indexer_kv_dtype='mxfp4' requires Blackwell datacenter GPUs "
            "(sm_10x, e.g. B200/GB200); sm_120 (consumer Blackwell) and "
            "earlier architectures are not supported.""
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for an sm120 MXFP4 indexer (queries: `https://github.com/vllm-project/vllm/issues?q=indexer_kv_dtype OR nvfp4_per_token OR mxfp4` → 12 loose hits, none an sm120 MXFP4 indexer port; `...?q=DeepGEMM sm120` → the sm12x DeepGEMM/UE8M0 threads, e.g. SGLang #39063, which record *failures* rather than MXFP4 kernel ports).
REASON-MAY-HAVE-EXPIRED: Expires only if FlashInfer/vLLM add an MXFP4 indexer kernel for capability 12.x. Note the sibling case that *did* get an sm120 path is the non-FP4 FlashInfer sparse-MLA backend — so the gap is per-dtype, not per-feature.

### C3.4 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: vLLM project (shipped v0.29.0 code)
ARTIFACT: `vllm/model_executor/layers/quantization/online/nvfp4.py` — online per-token NVFP4 MoE
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/model_executor/layers/quantization/online/nvfp4.py
STATED REASON: "        if not current_platform.is_device_capability_family(100):
            raise ValueError(
                "nvfp4_per_token online quantization requires a Blackwell (SM100) GPU."
            )"
EVIDENCE: READ BODY
S3B: OCCUPIED (open PR #56550, open PR #56553, open issue #54666) — `https://github.com/vllm-project/vllm/issues?q=sm120 OR sm_120 OR "RTX PRO 6000"` returns sm120 NVFP4 enablement work in flight (#56550 "[SM120] ... NVFP4 KV cache ... consumer Blackwell", #56553 restricting a CuTe-DSL NVFP4 kernel *away* from SM12x) and #54666 asking to consider B12X before the Marlin fallback on SM120/SM121.
REASON-MAY-HAVE-EXPIRED: The gate is `is_device_capability_family(100)`; it expires when someone rewrites it to an explicit capability set (the pattern #56553 uses: `SM100/103`). At retrieval the direction of travel is *restriction to sm100*, not extension to sm120.

### C3.5 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: filipsajdak (GB10, capability 12.1), reporting vLLM's own selection logic
ARTIFACT: vLLM issue #55397 — NVFP4 dense-linear kernel selection on SM12x
URL: https://github.com/vllm-project/vllm/issues/55397
STATED REASON: "| # | kernel | gate | on SM121 (cc=121) |
|---|---|---|---|
| 1 | `FlashInferCuteDslNvFp4LinearKernel` | `current_platform.is_device_capability_family(100)` | **rejected** — family is 120 |
| 2 | `FlashInferCuteDslNvFp4W4A16LinearKernel` | `compute_capability in (100, 103) or 120 <= compute_capability < 130` | **accepted** — scan stops |
| 3 | `FlashInferCutlassNvFp4LinearKernel` | `current_platform.has_device_capability(100)` | would pass — **never reached** |"
EVIDENCE: READ BODY
S3B: OCCUPIED (open issue #55397 itself; open issue #54666 "Consider B12X in automatic NVFP4 MoE backend selection on SM120/SM121 before the Marlin fallback").
REASON-MAY-HAVE-EXPIRED: This is precisely the `is_device_capability_family(100)` mis-classification named in the brief. It expires when the first gate is rewritten; the sibling clause (`120 <= cc < 130`) shows maintainers already writing sm120-aware gates elsewhere in the same file, so a fix is a one-line change but is not in the retrieved tree.

### C3.6 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: ch2lab (PR author, fork contributor)
ARTIFACT: vLLM PR #56553 — "[Bugfix][FlashInfer] Restrict W4A16 NVFP4 GEMM to SM100/103"
URL: https://github.com/vllm-project/vllm/issues/56553
STATED REASON: "Restrict FlashInferCuteDslNvFp4W4A16LinearKernel to SM100/103. On SM12x the un-tuned CuTe-DSL W4A16 kernel falls back to its heuristic tactic (several times slower than CUTLASS at prefill), and its autotune JIT-compiles a cubin + CUDA graph per tactic that empty_cache() cannot reclaim — accumulating ~7 GiB and OOMing KV-cache allocation on 32 GiB consumer GPUs."
EVIDENCE: READ BODY
S3B: OCCUPIED (this open PR #56553 is the active work; it is a *removal* of an sm120 path, not an addition).
REASON-MAY-HAVE-EXPIRED: The regression is "un-tuned ... heuristic tactic" — it would expire if tuning data for sm120 were added upstream; at retrieval the chosen remedy is to exclude capability 12.x instead. Note the author's own caveat: this is a fork PR, not a maintainer-accepted decision.

### C3.7 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: SGLang project (shipped code, tag v0.5.19 — the rig's version)
ARTIFACT: `python/sglang/srt/layers/quantization/fp8_utils.py` — FlashInfer CuTe-DSL MXFP8 dense GEMM
URL: https://github.com/sgl-project/sglang/blob/v0.5.19/python/sglang/srt/layers/quantization/fp8_utils.py
STATED REASON: "            raise RuntimeError(
                "MXFP8 dense GEMM requested via --fp8-gemm-backend=flashinfer_cutedsl, "
                "but that kernel requires an SM100/SM103 GPU and FlashInfer."
            )"
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for sm120 MXFP8 (queries: `https://github.com/sgl-project/sglang/issues?q=sm120 OR "RTX PRO 6000"` → 12 hits, none an sm120 MXFP8 dense-GEMM enablement; `...?q=SageAttention` → no sm120 MXFP8 kernel thread).
REASON-MAY-HAVE-EXPIRED: Expires if FlashInfer ships a CuTe-DSL MXFP8 dense kernel for capability 12.x. The shipped vLLM docs make the same statement for MXFP8 generally: "Requires SM 100+ (Blackwell or newer) for w8a8, other GPUs use a w8a16 fallback".

### C3.8 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: steven-umt (reporter, 4× RTX PRO 6000 Blackwell, cc 12.0)
ARTIFACT: vLLM issue #56702 — sm_120 missing from kernel-block-size branches (closed `not planned`)
URL: https://github.com/vllm-project/vllm/issues/56702
STATED REASON: "The recipe lists `h200 / gb200 / gb300 / mi350x` as verified, so sm_120 is understood to be unsupported." and, in the same body, "Three sites select the kernel block size, and all treat sm_120 as sm_100" (the report's own section heading) with the dispatcher error: "ValueError: SM120 sparse-MLA has no decode kernel for this shape: num_tokens=2, num_heads=16, topk=1152, d_qk=512, page_block_size=32, model_type=1". CLOSURE CAVEAT: the issue is closed with `stateReason: NOT_PLANNED`, but the only non-author comment is a "CC @hongxiayang @tjtanaa ..." notification — there is **no human closure reason**, so per the evidence rules the quote above is used only as the reporter's verbatim failure diagnosis, not as a maintainer statement.
EVIDENCE: READ BODY
S3B: OCCUPIED (multiple open follow-ups: #56461 reproduced on RTX PRO 6000 SM120, #56623 "On SM120 ... that top-k is not instantiated by the FlashInfer sparse-MLA kernels", #55757 DSA models `EngineDeadError` on sm_120) — queries: `https://github.com/vllm-project/vllm/issues?q=sm120 OR sm_120 OR "RTX PRO 6000"`, `...?q="masked_mha_available" OR "kernel-block-size"`.
REASON-MAY-HAVE-EXPIRED: The three block-size sites branch on `is_device_capability_family(90)`/family(100), so they expire when they gain an explicit `120` branch. The work is actively in flight elsewhere (#56509 was referenced as addressing the page-geometry faults), so this is a "watch" item rather than a permanent gap.

### C3.9 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: fsadj (reporter; RTX PRO 6000 Blackwell Workstation, SM120, 96 GB, **SGLang 0.5.19**)
ARTIFACT: SGLang issue #39302 — "GitHub" title is "…GLM-5.x NoPE MLA … cannot run on SM120: every DSA sparse-MLA backend is unavailable" (title truncated in `fetch.py gh` as "[Bug] GLM-5.x NoPE MLA (qk_rope_head_dim=0) cannot run on SM120: every DSA sparse-MLA backend is unavailable")
URL: https://github.com/sgl-project/sglang/issues/39302
STATED REASON: "On **SM120** (RTX PRO 6000 Blackwell, compute capability 12.0) this model cannot be served at all. Every `--dsa-prefill-backend` / `--dsa-decode-backend` choice fails, each for a distinct reason, and the one backend the code *forces* for GLM + fp8 KV + SM120 is the one whose FlashInfer kernel rejects NoPE. So with `--kv-cache-dtype fp8_e4m3` the model is unrunnable by construction". The same thread measures the underlying hardware reason: "`cudaDevAttrMaxSharedMemoryPerBlockOptin` on this part is **101376 B (99 KB)** (`sharedMemPerMultiprocessor` = 102400). … Hopper/B200 expose ~228 KB, which is the budget the kernels below are written against."
EVIDENCE: READ BODY
S3B: OCCUPIED (this issue is open; the same author filed #39226 the same week, and vLLM #55757/#56461/#56623 are the vLLM-side equivalents).
REASON-MAY-HAVE-EXPIRED: Two independent constraints: (i) per-kernel instantiation gaps (fixable by adding sm120 kernels), and (ii) **99 KB opt-in shared memory vs ~228 KB on Hopper/B200** — a silicon-level difference that no software change removes. Any kernel whose tiles were sized against ~228 KB will not run on sm120 regardless of dispatch fixes.

### C3.10 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: jiahansu (reporter; RTX PRO 6000 Blackwell Workstation, sm_120)
ARTIFACT: vLLM issue #54331 — GDN/linear-attention prefill backend on cc 12.0
URL: https://github.com/vllm-project/vllm/issues/54331
STATED REASON: "Note on sm_120 specifically: `_resolve_gdn_prefill_backend` only enables the flashinfer/cutedsl GDN prefill paths for SM90 and cc-family-10.x, so **cc 12.0 always falls through to Triton**. Passing `--gdn-prefill-backend flashinfer|cutedsl` is inert here. There is no GDN backend escape hatch on this card."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for an sm120 GDN prefill backend (queries: `https://github.com/vllm-project/vllm/issues?q=indexer_kv_dtype OR nvfp4_per_token OR mxfp4` → no GDN hit; `...?q=sm120 OR sm_120 OR "RTX PRO 6000"` → 12 hits, none a GDN-prefill enablement).
REASON-MAY-HAVE-EXPIRED: Same family-gate pattern; expires when the resolver gains a `120` branch. Note: this axis is for hybrid linear-attention models, not for the rig's dense Qwen3-4B — recorded because it is one more instance of the same mis-classification.

### C3.11 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: brainatworkharri (reporter; RTX PRO 6000 Blackwell Max-Q 96 GB, sm_120)
ARTIFACT: vLLM issue #54225 — FlashInfer backend + NVFP4 + fp8 KV-cache crash on sm120
URL: https://github.com/vllm-project/vllm/issues/54225
STATED REASON: "On sm_120 (RTX PRO 6000 Blackwell), the **FLASHINFER attention backend** crashes the engine with a CUDA memory fault when serving an NVFP4 (ModelOpt `MIXED_PRECISION`) Qwen3.6-35B-A3B with `--kv-cache-dtype fp8`. Switching only `--attention-backend TRITON_ATTN` — changing nothing else — makes the crash go away."
EVIDENCE: READ BODY
S3B: OCCUPIED (open issue; the same crash family is tracked in #55571 and #52225, and #56552 covers an sm120 NVFP4 draft startup crash).
REASON-MAY-HAVE-EXPIRED: Expires when the FlashInfer sm120 + NVFP4 + fp8-KV combination is fixed upstream; at retrieval the working configuration is the Triton fallback (i.e. the accelerated path is unavailable, exactly the pattern this section inventories).

### C3.12 | ABANDONED   (sm120 / Blackwell-consumer acceleration gaps)
WHO: NVIDIA TensorRT-LLM users (rohash123, 4× RTX PRO 6000 Blackwell Server Edition, AWS g7e.24xlarge), closed by erictsai-nv
ARTIFACT: TensorRT-LLM issue #18039 — DeepSeek-V4-Flash NVFP4 on RTX PRO 6000 Blackwell
URL: https://github.com/NVIDIA/TensorRT-LLM/issues/18039
STATED REASON: "After model init and during warmup, all ranks abort in `AttentionOp::initialize()`:
```text
terminate called after throwing an instance of 'tensorrt_llm::_v1::common::TllmException'
  what():  [TensorRT-LLM][ERROR] Assertion failed: Deepseek should be supported by fmha in generation part. (../tensorrt_llm/common/attentionOp.cpp:3187)"
```
EVIDENCE: READ BODY (STATE: closed/completed)
S3B: NO-ACTIVE-WORK-FOUND beyond this thread (queries: `https://github.com/NVIDIA/TensorRT-LLM/issues?q=sm120`, `...?q=compute+capability+12`, `...?q=NVFP4` → nav shell / non-matching items only; the subagent flagged TRT-LLM #18898 "Accept flashinfer W4A16 ultra-wide FC2 tile on SM120" as a real sm120 item it could not quote before its deadline).
REASON-MAY-HAVE-EXPIRED: A third engine joins the pattern: the FMHA generation kernel for this model family is not wired for the consumer-Blackwell part. Expires on an NVIDIA kernel enablement; no evidence of one at retrieval.

### C4.1 | ABANDONED   (negative results about specific acceleration axes — CUDA graphs)
WHO: vLLM maintainers (RFC author) and a contributor comment
ARTIFACT: vLLM issue #20098 — "[RFC]: Lazy CUDA Graph capture"
URL: https://github.com/vllm-project/vllm/issues/20098
STATED REASON: "Currently vLLM captures cudagraphs as part of the engine initialization significantly slowing down vLLM startup time. By default, vLLM captures 67 graphs, which depending on model size and GPU type, can take more than 10s." A contributor comment on the same thread records a case where the graph path was switched off for cost: "in the v0 engine, they're currently disabled by default partly because it doubled the cuda graph capture time".
EVIDENCE: READ BODY
S3B: OCCUPIED (this RFC is open; the shipped design doc still describes the same trade: "FULL_AND_PIECEWISE — (default mode) ... generally the most performant setting, especially for low latency with small models or MoEs, but also requires the most memory and takes the longest to capture." — https://docs.vllm.ai/en/v0.29.0/design/cuda_graphs.html).
REASON-MAY-HAVE-EXPIRED: Expires on lazy/partial capture shipping; the RFC is the active work. For a single 4B model the absolute capture time is small, so the practical weight of this item on this rig is low — recorded because the brief asked for CUDA-graph negatives and because it is the *verified* form of that negative (no primary source was found showing decode-time graphs slower than eager for a Qwen3-4B-class dense model at batch 1).

### C4.2 | ABANDONED   (negative results about specific acceleration axes — torch.compile)
WHO: PyTorch user report (pytorch/pytorch), and the SGLang project's own tracking issue
ARTIFACT: pytorch/pytorch issue #179697 ; sgl-project/sglang issue #1008
URL: https://github.com/pytorch/pytorch/issues/179697 ; https://github.com/sgl-project/sglang/issues/1008
STATED REASON: "For Batch Size 1-64, all torch.compile modes (Default, Reduce-Overhead, Max-Autotune) are significantly slower than Eager mode. The compiler works as expected at Batch Size 512" — with per-mode numbers in the same report (bs=1: eager 0.0792 ms, Default 0.1407 ms, Reduce-Overhead 0.0934 ms, Max-Autotune 0.0843 ms). SGLang's tracking issue states: "torch.compile can accelerate small batch sizes for llama-3 8B. However,  it is sometimes slower for large batch size or tensor parallelism."
EVIDENCE: READ BODY
S3B: OCCUPIED (both issues open; a second SGLang thread #2278 reports compiled fused-MoE regressions: "It is slower than the non-compiled version when bs > 1, and sometimes even when bs = 1.").
REASON-MAY-HAVE-EXPIRED: Expires when Inductor's small-batch codegen beats eager for these shapes; the reports show max-autotune is *not* the fix at bs=1 (0.0843 ms, still slower than eager 0.0792 ms).

### C4.3 | ABANDONED   (negative results about specific acceleration axes — kernel fusion)
WHO: SGLang contributor benchmarking Inductor fusions (Qwen3-30B-A3B bf16, TP=1)
ARTIFACT: sgl-project/sglang issue #21855 — "Replace memory-bound custom CUDA kernels with torch.compile generated fusions"
URL: https://github.com/sgl-project/sglang/issues/21855
STATED REASON: "Splitting q/k normalization into a separate static-shape graph (`QKNorm + RopeKV`) substantially improves over the fully-fused variant (`QKNormRopeKV`): at B=1 the regression drops from −5.6 % to −2.0 %, at B=32 it flips from −0.7 % to **+1.7 %**, and at B≥128 it consistently leads."
EVIDENCE: READ BODY
S3B: OCCUPIED (issue open; the general axis is also tracked in SGLang #1008).
REASON-MAY-HAVE-EXPIRED: The fusion's sign flips with batch (−5.6% at B=1 → positive at B≥32), so it expires for a high-concurrency server; for a single-stream latency rig the fusion is a regression. The remaining −2.0% at B=1 with the split graph is a second, smaller regression that is not attributed.

### C4.4 | ABANDONED   (negative results about specific acceleration axes — quantization that costs more than it saves)
WHO: vLLM user benchmark on RTX 5090 (sm120-class part), **exactly the rig's model**
ARTIFACT: vLLM issue #28234 — "Online Dynamic FP8 Quantization (`--quantization="fp8"`) is slower than BF16/FP16 on RTX 5090"
URL: https://github.com/vllm-project/vllm/issues/28234
STATED REASON: "When enabling Online Dynamic FP8 quantization ( --quantization=\"fp8\" ) for the Qwen3-4B (BF16) model, inference on an RTX 5090 becomes slower than BF16 (no quantization)."
EVIDENCE: READ BODY
S3B: OCCUPIED (issue open; the same trade-off is documented in vLLM's own FP8 material, which the report cites as explaining why online dynamic quantization limits latency gains — activation min/max are recomputed every forward).
REASON-MAY-HAVE-EXPIRED: The cause is per-forward dynamic scaling, which expires if a calibrated/static activation scale is used instead — i.e. the fix is a configuration change (static scales), not a hardware change. Measured cost on that part: throughput −17.2% (3370.29 → 2791.53 tokens/s), wall time +17.9%.

### C4.5 | ABANDONED   (negative results about specific acceleration axes — quantization that degrades quality at the stated setting)
WHO: vLLM CI maintainers (Blackwell CI investigation)
ARTIFACT: vLLM issue #37618 — "FP8 kv cache on b200 with qwen3.5 has degraded accuracy"
URL: https://github.com/vllm-project/vllm/issues/37618
STATED REASON: "The accuracy drop is caused by **DeepGEMM's `fp8_gemm_nt` with E8M0 scale format**"
EVIDENCE: READ BODY
S3B: OCCUPIED (issue open; the sm12x sibling of the scale-format problem is SGLang #39063: "`should_deepgemm_weight_requant_ue8m0` unconditionally returns False for SM120, including grouped MoE weights", after which the grouped FP8 DeepGEMM path asserts on scale layout).
REASON-MAY-HAVE-EXPIRED: The culprit is a *kernel's* scale handling, not the dtype: in the same report `VLLM_USE_DEEP_GEMM=0` restores GSM8K 0.8302 from 0.7089, while `--enforce-eager` (0.7210) and `--moe-backend triton` (0.7392) do not recover it. Expires when the DeepGEMM E8M0 path is fixed; that is also the path sm120 currently mis-gates (C3.5/C3.8 family).

NOTE: A further negative-axis artifact is held out of the row count because its own evaluation is on 8 GPUs (out of target): vLLM PR #6978 "Add Fused MoE W8A8 (Int8) Support" — "With some minor tuning, current kernel is for 4k context length with TP=8 on 8 A100 with max_num_batched_tokens= 64 * 4096 and 64 batch size is ~13400 toks/s for prefill stage (only slightly faster than bf16 with fused moe 12900 toks/s) and output ~23 toks/s which is much slower than bf16 output: 48.53 toks/s." (fused INT8 MoE decode 2.1× slower than bf16 on the author's own numbers; the same PR notes "the accuracy drop is too severe" for per-tensor INT8 scales). READ BODY; see also vLLM #54311: "Int8 not supported on SM120. Use FP8 quantization instead, or run on older arch (SM < 100)."

### C5.1 | ABANDONED   (reproducibility failures)
WHO: Yunxiang Zhang, Ping Yu, Jianyu Wang, Max (Xiangjun) Fan, Julian Reed, Azalia Mirhoseini, Will Su
ARTIFACT: arXiv:2607.16241 — "KernelBench-Verified: Do LLM-Generated Kernels Actually Beat PyTorch?"
URL: https://arxiv.org/abs/2607.16241
STATED REASON: "Under verified single-turn evaluation with seven frontier LLMs, we find that the best-performing model (GPT-5.5) achieves a 0.88x geometric mean speedup, significantly lower than the 1.43x speedup observed under the standard evaluation protocol. No model consistently outperforms PyTorch when evaluated against realistic baselines."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for a vLLM/SGLang adoption of this verification protocol (queries: `https://github.com/vllm-project/vllm/issues?q=KernelBench` → no hit; the benchmark's own repo is the venue, and it is outside the rig's serving stack).
REASON-MAY-HAVE-EXPIRED: The gap is a *measurement* gap (baseline TF32 setting + hidden test distributions), not hardware; it expires when the standard KernelBench protocol adopts the realistic baseline. This is a headline-number downward revision (1.43× → 0.88×) with no arXiv Comments-field record — see NEGATIVE FINDINGS.

### C5.2 | ABANDONED   (reproducibility failures)
WHO: Robert Tjarko Lange, Qi Sun, Aaditya Prasad, Maxence Faldor, Yujin Tang, David Ha (Sakana AI)
ARTIFACT: arXiv:2509.14279 — "Towards Robust Agentic CUDA Kernel Benchmarking, Verification, and Optimization" (robust-kbench)
URL: https://arxiv.org/html/2509.14279v1
STATED REASON: "Upon closer inspection, these speedups largely result from the exploitation of KernelBench's loopholes. More specifically, after excluding all contaminated tasks ( Appendix A ), the average speedup is reduced from 3.13x to 1.49x. Please refer to Appendix A for examples of ' cheating ' kernels which pass KernelBench's verification process, and even achieve fake speedups of 50-120x by exploiting loopholes in KernelBench."
EVIDENCE: READ BODY
S3B: SOLVED-by-retraction at the vendor (the authors' own blog revises the number: 3.13倍 (最大10-100倍を含む) → 1.49倍 — https://sakana.ai/ai-cuda-engineer-update/); NO-ACTIVE-WORK-FOUND for an upstream fix in vLLM/SGLang (queries: `https://github.com/vllm-project/vllm/issues?q=KernelBench` → no hit).
REASON-MAY-HAVE-EXPIRED: The contaminated-task set is what expires (already excluded in v2); the underlying reward-hacking surface of KernelBench is not fixed in general (C5.3, C5.5).

### C5.3 | ABANDONED   (reproducibility failures)
WHO: Tingxi Li, Ravishka Rathnasuriya, Wei Yang
ARTIFACT: arXiv:2607.04454 — "Correct but Slow: An Empirical Study of the GPU Kernel Evaluation Gap in Modern Domain-Specific Languages"
URL: https://arxiv.org/abs/2607.04454
STATED REASON: "correctness-based evaluation can admit severe slowdowns: an idiomatic TileLang LayerNorm kernel passes KernelBench's correctness check while running more than 300$\times$ slower than the PyTorch baseline."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for a fix in the rig's engines (queries: `https://github.com/vllm-project/vllm/issues?q=KernelBench` → no hit; the paper also records that "KernelBench's only reward-hacking guard never fires, because it watches for suspiciously fast kernels").
REASON-MAY-HAVE-EXPIRED: Pass/fail gates that ignore a slowdown bound; expires when evaluation adds a lower-bound on speed. Nothing found that changes this at retrieval.

### C5.4 | ABANDONED   (reproducibility failures)
WHO: Carlo Baronio, Pietro Marsella, Ben Pan, Simon Guo, Silas Alberti (GPU MODE / "Kevin")
ARTIFACT: arXiv:2507.11948 — "Kevin: Multi-Turn RL for Generating CUDA Kernels"
URL: https://arxiv.org/html/2507.11948v1
STATED REASON: "A sneakier bug in the KernelBench's evaluation harness caused the tested kernel to recycle the output tensor from the reference implementation (which was run immediately before) as its own tensor output. As a result of this, a kernel that only computes (correctly) a portion of the output tensor would still pass the correctness check. We address this by running the tested kernel first and only after the reference implementation, thus avoiding this hack."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for a vLLM/SGLang-side fix (queries: `https://github.com/vllm-project/vllm/issues?q=KernelBench` → no hit); the fix lives in the authors' harness.
REASON-MAY-HAVE-EXPIRED: Harness ordering only; the paper's own speedups are additionally scoped — "the speedups we measure in Section 3.2 are only accurate for those dimensions on our NVIDIA H200 GPUs" — and its headline is modest (mean speedup "from 0.53x to 1.10x of baseline (PyTorch Eager)").

### C5.4b | ABANDONED   (reproducibility failures — held out of the row count, hardware off-target)
WHO: independent user "TheRainstorm" on the Dao-AILab/flash-attention tracker (primary measurement, not a blog)
ARTIFACT: flash-attention issue #1572 — "flash-attention-3 batch decode is significantly slower than flashinfer on H20"
URL: https://github.com/Dao-AILab/flash-attention/issues/1572
STATED REASON: "I I concluded that: - On GQA, the speed of flash-attention is basically close to flashinfer. - On MHA, the speed of flash-attention is significantly slower than flashinfer. As batch_size and kv_len increase, the difference gradually becomes more obvious, reaching a maximum gap of 3 times."
EVIDENCE: READ BODY
S3B: NO-ACTIVE-WORK-FOUND for a fix (queries: `https://github.com/vllm-project/vllm/issues?q="flash attention 3" sm120` → no sm120 FA3 enablement; vLLM's answer to this class is the FA version switch, A1.1).
REASON-MAY-HAVE-EXPIRED: The measurement is on H20 (a Hopper-class part) for MHA decode; the rig's Qwen3-4B is GQA, where the same report finds parity — hence held out of the count.

### C5.5 | ABANDONED   (reproducibility failures)
WHO: independent verifiers (@main_horse, Lucas Beyer/OpenAI) reported by TechCrunch and GIGAZINE; Sakana AI's own post-mortem
ARTIFACT: blog — "Sakana walks back claims that its AI can dramatically speed up model training" (TechCrunch, 21 Feb 2025) — **BLOG**; corroborating **BLOG** GIGAZINE
URL: https://techcrunch.com/2025/02/21/sakana-walks-back-claims-that-its-ai-can-dramatically-speed-up-model-training/ ; https://gigazine.net/gsc_news/en/20250225-sakana-ai-cuda-engineer-walks-back
STATED REASON: "This week, Sakana AI, an Nvidia-backed startup that's raised hundreds of millions of dollars from VC firms, made a remarkable claim. The company said it had created an AI system, the AI CUDA Engineer, that could effectively speed up the training of certain AI models by a factor of up to 100x. The only problem is, the system didn't work. Users on X quickly discovered that Sakana's system actually resulted in worse-than-average model training performance. According to one user , Sakana's AI resulted in a 3x slowdown — not a speedup." (**BLOG**; the GIGAZINE piece quotes the mechanism: "it seems to 'steal' the result of the eager impl (memory reuse somehow?), allowing it to bypass the correctness check.")
EVIDENCE: BLOG (both sources labelled as blogs; the primary paper/A1.7 and C5.2 carry the paper-grade evidence)
S3B: SOLVED-by-retraction (Sakana's own revision: 3.13× → 1.49×, https://sakana.ai/ai-cuda-engineer-update/); NO-ACTIVE-WORK-FOUND for an engine-side change.
REASON-MAY-HAVE-EXPIRED: The specific exploit (memory reuse) was patched in KernelBench v0.1 and in the robust-kbench harness; the general "reward metric can be gamed" constraint is not expired (C5.3).

---

## HARDWARE-RULED-OUT

### D1.1 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: steven-umt (reporter) — the configuration that reproduces the failure
ARTIFACT: vLLM issue #56702 (the bring-up this failure was reported from)
URL: https://github.com/vllm-project/vllm/issues/56702
HARDWARE QUOTE: "- **GPU**: 4× NVIDIA RTX PRO 6000 Blackwell (compute capability **12.0**, sm_120), 96 GB each, no NVLink"
EVIDENCE: READ BODY

### D1.2 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: Lei-010 (reporter) — the only *working* configuration published for this model family on sm120
ARTIFACT: vLLM issue #56700 — "[SM120] Field report: running DeepSeek-V4.1-Flash end-to-end on 8x RTX PRO 6000 — pitfall map + working configuration (1M context verified)"
URL: https://github.com/vllm-project/vllm/issues/56700
HARDWARE QUOTE: "Cross-posting field data from a 3-day bring-up of **DeepSeek-V4.1-Flash** (769B total, MXFP4 experts, Engram memory) on **8x RTX PRO 6000 (SM120, consumer Blackwell)**, since SM120 users currently have to rediscover all of this from scratch."
EVIDENCE: READ BODY

### D1.3 | HARDWARE-RULED-OUT   (D.1 >1 GPU, borderline)
WHO: authors of arXiv:2601.09527 — "Private LLM Inference on Consumer Blackwell GPUs"
ARTIFACT: arXiv:2601.09527 (14 Jan 2026) — recommendation for 32k+ context on consumer Blackwell
URL: https://arxiv.org/abs/2601.09527
HARDWARE QUOTE: "• Consider dual-GPU configurations for consistent 32k+ workloads"
EVIDENCE: READ BODY (the same paper's RQ1 is explicitly framed around "single- and dual-GPU configurations", and its FP8-KV usage rule is "used for all dual-GPU configurations with long context ( ≥ \geq 16k) or high-concurrency agentic workloads" — i.e. its long-context results are multi-GPU results)

### D2.1 | HARDWARE-RULED-OUT   (D.2 >96 GB VRAM)
WHO: steven-umt (reporter), quoting the model recipe's verified hardware list
ARTIFACT: vLLM issue #56702
URL: https://github.com/vllm-project/vllm/issues/56702
HARDWARE QUOTE: "The recipe lists `h200 / gb200 / gb300 / mi350x` as verified, so sm_120 is understood to be unsupported."
EVIDENCE: READ BODY (all four listed parts exceed the rig's 96 GB: H200 141 GB, GB200 192 GB, GB300 288 GB, MI350X 288 GB)

### D2.2 | HARDWARE-RULED-OUT   (D.2 >96 GB VRAM)
WHO: reporter of vLLM issue #52540 (ModelOpt NVFP4 on SM120)
ARTIFACT: vLLM issue #52540
URL: https://github.com/vllm-project/vllm/issues/52540
HARDWARE QUOTE: "- GPU: 2x NVIDIA RTX PRO 6000 Blackwell Workstation Edition (sm120, 96 GB), driver 595.71.05, ECC clean"
EVIDENCE: READ BODY (the *working* reproduction of this NVFP4 failure needs two 96 GB parts, i.e. 192 GB total — beyond the target rig's single 96 GB)

### D3.1 | HARDWARE-RULED-OUT   (D.3 multi-node/NVLink/IB)
WHO: reporter of vLLM issue #51920
ARTIFACT: vLLM issue #51920 — FlashInferMLASparseSM120Impl crash on sm_121
URL: https://github.com/vllm-project/vllm/issues/51920
HARDWARE QUOTE: "4x NVIDIA GB10 (DGX Spark, sm_121a, aarch64, CUDA 13), official `vllm/vllm-openai:v0.27.0-aarch64`, GLM-5.2 (GlmMoeDsaForCausalLM) TP=4 across nodes, `--kv-cache-dtype fp8_ds_mla`, backend auto-selects `FLASHINFER_MLA_SPARSE_SM120` (+ FLASH_ATTN MLA prefill)."
EVIDENCE: READ BODY (TP=4 "across nodes" — the reproduction needs a multi-node cluster; the same thread's follow-up offer is "I have a 4-node GB10 / sm_121a cluster (aarch64, CUDA 13, TP=4 across nodes) that reproduces this in about 20 minutes per boot")

### D3.2 | HARDWARE-RULED-OUT   (D.3 multi-node/NVLink/IB)
WHO: knapcio (reporter), SGLang issue #39235
ARTIFACT: sgl-project/sglang issue #39235 — SM120 decode head-padding for an SM90 kernel constraint
URL: https://github.com/sgl-project/sglang/issues/39235
HARDWARE QUOTE: "At `num_attention_heads = 64` and attn-TP 4 each rank owns 16, `(16, 512)` is in the table, so decode attends on 64 heads where 16 would do."
EVIDENCE: READ BODY (the measurement is on a 4× DGX Spark GB10 / TP4 / EP2 cluster; the reported cost — "Removing the pad is worth **0.50% of a decode step** at TP4" — is only defined for a multi-rank deployment)

### D5.1 | HARDWARE-RULED-OUT   (D.5 borderline — compute-capability-gated paper claim)
WHO: Jay Shah, Ganesh Bikshandi, Ying Zhang, Vijay Thakkar, Pradeep Ramani, Tri Dao
ARTIFACT: arXiv:2407.08608 — "FlashAttention-3: Fast and Accurate Attention with Asynchrony and Low-precision"
URL: https://arxiv.org/abs/2407.08608
HARDWARE QUOTE: "We develop three main techniques to speed up attention on Hopper GPUs: exploiting asynchrony of the Tensor Cores and TMA to (1) overlap overall computation and data movement via warp-specialization and (2) interleave block-wise matmul and softmax operations, and (3) block quantization and incoherent processing that leverages hardware support for FP8 low-precision."
EVIDENCE: READ BODY (title and abstract verified by read-back; the paper's evaluation is H100-only — "with FP16 reaching up to 740 TFLOPs/s (75% utilization)"; sm120 is a different major and the FA4 paper states porting is not free: "Simply porting existing attention algorithms to this new hardware either leaves significant performance on the table or is impossible due to lack of forward compatibility for Hopper MMA instructions.")

### D5.2 | HARDWARE-RULED-OUT   (D.5 borderline — compute-capability-gated paper claim)
WHO: authors of FlashAttention-4 (arXiv:2603.05451)
ARTIFACT: arXiv:2603.05451 — "FlashAttention-4: Algorithm and Kernel Pipelining Co-Design for Asymmetric Hardware Scaling"
URL: https://arxiv.org/abs/2603.05451
HARDWARE QUOTE: "We demonstrate that our method, FlashAttention-4, achieves up to 1.3$\times$ speedup over cuDNN 9.13 and 2.7$\times$ over Triton on B200 GPUs with BF16, reaching up to 1613 TFLOPs/s (71% utilization)."
EVIDENCE: READ BODY (abstract retrieved and verified; the paper's framing is datacenter Blackwell — "The AI industry has rapidly transitioned to deploying Blackwell-based systems such as the B200 and GB200" — and vLLM's dispatch restricts FA4 to capability family 100, see C3.1)

### D5.3 | HARDWARE-RULED-OUT   (D.5 borderline — compute-capability-gated paper claim)
WHO: authors of arXiv:2604.00028
ARTIFACT: arXiv:2604.00028 — "Sequence-Aware Split Heuristic to Mitigate SM Underutilization in FlashAttention-3 Low-Head-Count Decoding" (April 2026)
URL: https://arxiv.org/abs/2604.00028
HARDWARE QUOTE: "The standard FlashAttention-3 heuristic exhibits a GPU occupancy bottleneck in low-head-count decoding configurations because it disables sequence splitting based on sequence length alone, underutilizing the Streaming Multiprocessors of Hopper GPUs."
EVIDENCE: READ BODY (abstract retrieved by search and the paper's HTML mined; the work patches "the underlying heuristics.h source file within the FlashAttention-3 Hopper stack" and reports only "kernel-level speedups of roughly 21 to 24% on metadata-enabled target configurations" — the artifact is Hopper-only, so it is unavailable on sm120 both because FA3 is gated to family 90 and because the patch is to the Hopper stack)

### D5.4 | HARDWARE-RULED-OUT   (D.5 borderline — datacenter-Blackwell-only instruction)
WHO: vLLM project (shipped v0.29.0 code)
ARTIFACT: `vllm/config/mamba.py` — mamba cache stochastic rounding
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/config/mamba.py
HARDWARE QUOTE: "                    "Stochastic rounding for Mamba cache with triton backend requires "
                    "compute capability 10.0 (data center Blackwell). The `cvt.rs` "
                    "PTX instruction is not supported on your GPU. Please do not "
                    "specify `--enable-mamba-cache-stochastic-rounding`, "
                    "or set `--mamba-backend flashinfer`."
EVIDENCE: READ BODY (gate is `is_device_capability_family(100)`; sm120 raises. Included as the clearest shipped statement of a *PTX-instruction-level* datacenter-Blackwell requirement.)

### D5.5 | HARDWARE-RULED-OUT   (D.5 borderline — datacenter-Blackwell-only GEMM kernel)
WHO: jschmied (reporter; DGX Spark GB10, capability 12.1), vLLM issue #54125
ARTIFACT: vLLM issue #54125 — DeepGEMM accepted by the gate, then faults on sm_121
URL: https://github.com/vllm-project/vllm/issues/54125
HARDWARE QUOTE: "`support_deep_gemm()` returns **True on sm_121**, but DeepGEMM's blockwise FP8 GEMM then dies with a hard CUDA fault. The gate accepts the whole `120` capability *family*, and GB10 is in that family without being a device DeepGEMM actually runs on."
EVIDENCE: READ BODY (the shipped predicate is `cls.is_device_capability(90) or cls.is_device_capability_family(100) or cls.is_device_capability_family(120)` with the docstring "Currently, only Hopper and Blackwell GPUs are supported." — a family-granularity acceptance that is wrong for capability 12)

---

## SEARCH LOG (queries actually issued)

**arXiv advanced search (HTML, `https://arxiv.org/search/advanced?...`, all issued via `fetch.py get`):**
- `comments:withdrawn AND abstract:quantization` (20) · `comments:withdrawn AND abstract:speedup` · `comments:withdrawn AND abstract:kernel` · `comments:withdrawn AND abstract:"efficient inference"` · `comments:withdrawn AND abstract:GPU`
- `comments:withdrawn AND title:serving` (2) · `comments:withdrawn AND title:inference` (23) · `comments:withdrawn AND title:attention` (14) · `comments:withdrawn AND title:vLLM` (0)
- `comments:withdrawn` (7,213 raw hits; top 50 scanned)
- `comments:withdrawn AND abstract:inference` (98, cs classification) — the single most productive withdrawn-paper query
- `comments:withdrawn AND abstract:serving` · `comments:withdrawn AND abstract:attention`
- `abstract:"only at batch size 1" AND all:attention` (24) · `abstract:"batch size 1" AND abstract:speedup`
- `title:"SageAttention"` · `title:"SeerAttention"` · `title:"MInference"` · `title:"XAttention"` · `title:"KernelBench"` · `title:"LLEMMA"` · `title:"sparse attention" AND all:GPU` · `title:"efficient LLM inference"` (several of these returned HTTP 429 / empty and are recorded as failed, not as zero-hit)
- `abstract:"four A100" AND all:inference` (24) · `abstract:"8xH100" AND all:inference` · `abstract:"eight H100"` · `abstract:"multiple GPUs" AND all:"inference acceleration"` · `abstract:"tensor parallelism" AND all:inference`
- `title:"FlashAttention-3" AND all:Hopper` (2) · `title:"FlashAttention-4" AND all:Blackwell` (2)
- Candidate mining used `https://arxiv.org/html/<id>v1` full text with a negative-phrase sentence extractor (keyword set: "we do not/cannot/could not", "does not improve/accelerate/translate", "no speedup/benefit", "batch size 1", "memory-bound", "limitation", "slower than", "requires a training phase", "fails to", "not supported", "we leave … to future work").

**arXiv read-backs (`fetch.py arxiv <id>`, title confirmed):** 2510.05175, 2609.04105, 2606.04511, 2606.26666, 2605.16928, 2607.08786, 2602.04541, 2607.25291, 2606.07713, 2606.09080, 2603.19296, 2608.17336, 2604.08584, 2603.11873, 2606.17104, 2605.30571, 2601.09527, 2407.08608, 2603.05451, 2604.06798, 2605.09735, 2608.30076, 2606.03026, 2512.24713, 2606.04920, 2605.03562, 2607.16241, 2607.04454, 2509.14279, 2502.10517, 2507.11948, 2502.14752, 2608.12004, 2606.08960, 2111.03759, 2604.00028, 2504.17768, 2504.04823, 2404.14047, 2411.02355.

**GitHub searches (repo-scoped issue lists — `https://github.com/<repo>/issues?q=…`; the global `/search?q=…&type=issues` form returned HTTP 429 in this environment and is recorded as failed):**
- `repo:vllm-project/vllm` — `sm120` · `sm120 attention` · `sm120 flashinfer` · `"RTX PRO 6000"` · `"capability 12"` · `sm120 is:open` · `FlashMLA OR "sparse MLA"` · `indexer_kv_dtype OR nvfp4_per_token OR mxfp4` · `"Xid 13" OR "illegal memory access" sm120` · `sm120 OR sm_120 OR "RTX PRO 6000"` · `cuda graph slower OR "enforce-eager"` · `torch.compile slower OR "compile time"` · `"Exact Causal Attention" OR SparDA OR PersistentKV OR LycheeDecode OR TileMix OR RTPurbo` · `"FP4 FlashAttention" OR "flash attention 4" OR "flash attention 3"` · `"FlashLLM" OR "SparDA" OR "PersistentKV"` · `"TileMix" OR "LycheeDecode" OR "RTPurbo"` · `SageAttention` · `"FlashDecoding"` · `DeepGEMM sm120` · `"flash attention 4" CUTLASS` · `"Beyond FLOPs" OR "pruning" GEMM` · `"sparse attention" prefill kernel wall-clock` · `"masked_mha_available" OR "kernel-block-size"` · `MoBiE binary experts` · `"static-graph" LLM serving`
- `repo:sgl-project/sglang` — `sm120` · `sm120 OR "RTX PRO 6000"` · `sm120 attention` · `cuda graph` · `SageAttention` · `"flash attention 4"`
- `repo:triton-lang/triton` — `scatter4 sm120` (target of the vLLM code comment)
- `repo:NVIDIA/TensorRT-LLM` — `sm120`, `sm_120`, `consumer Blackwell`, `compute capability 12`, `NVFP4`, `GB10`, closed variants, page 2 (all via the sm120 sub-sweep; nothing new beyond #18039)

**GitHub pages retrieved and read in full (bodies + comments):** vllm #51920, #52225, #52540, #52553, #54150, #54311, #54331, #54434, #55397, #55571, #55757, #56125, #56461, #56550, #56551, #56552, #56553, #56603, #56623, #56700, #56702, #54704, #53963, #54125, #20098, #37618, #28234, #32227, #35467, #10411, #6978, #52016 ; sglang #38980, #39063, #39226, #39235, #39302, #21855, #2278, #1008, #22671 ; pytorch #179697, #98441 ; Dao-AILab/flash-attention #1572 ; TensorRT-LLM #18039.

**Local source greps (vLLM v0.29.0 tree at `/tmp/vllm-0.29.0`, SGLang v0.5.19 tree at `/tmp/sglang-main`):** `is_device_capability_family|has_device_capability` across `vllm/` · `sm120|sm_120|capability 12|(12, 0)` across `vllm/ docs/ csrc/` · `capability_family(100)` in `vllm/model_executor/layers/quantization/`, `fused_moe/`, `vllm/kernels/` · `nvfp4|mxfp4` in quantization + docs · reads of `fa_utils.py`, `flashmla.py`, `mla/indexer.py`, `online/nvfp4.py`, `fused_moe/utils.py`, `config/mamba.py`, `platforms/interface.py`, `platforms/cuda.py`, `docs/design/attention_backends.md`, `docs/design/fusions.md`, `docs/features/quantization/b12x.md`.

**Blog / news pages retrieved (labelled BLOG where used):** sakana.ai post-mortem, sakana.ai update, techcrunch.com (21 Feb 2025), gigazine.net (25 Feb 2025), metr.org kernel-engineering, scalingintelligence.stanford.edu KernelBench v0.1, standardkernel.com benchmarking.

---

## NEGATIVE FINDINGS

- **No arXiv `Comments:`-field downward revision of an LLM-inference speedup exists.** Queries: `comments:corrects overstatement` → 0; `comments:overstatement` → 0; `comments:withdrawn speedup` → no parseable result list; `comments:"speedups" corrected overstated` → 0; `comments:speedup` (size 200) → 21 hits, all quantum-computing / optimisation-theory (closest: a quantum retraction whose notice says the correction "nullif[ies] the claimed speedup" — out of scope). The only genuine headline-number revisions found in this subtopic are the Sakana/KernelBench ones (C5.2, C5.6) and the withdrawn-results note in C2.4 ("withdrawn invalid pure-INT4 results").
- **No arXiv paper found whose v2/v3 abstract changelog revises an inference-acceleration speedup downward.** The `Comments:` field is the only place arXiv exposes such revision text and it yielded nothing in scope (above).
- **No reproducibility study of LLM inference acceleration exists in the arXiv title index within scanned depth.** Queries: `title:reproducibility` (2,104 hits) and `title:reproduction` (514 hits), top 100 relevance-ranked each — no inference-acceleration / kernel-generation / quantization speedup reproduction study.
- **No MLSys / NeurIPS Datasets-&-Benchmarks reproducibility report with a verbatim "could not reproduce this speedup" statement about an inference-acceleration paper could be loaded.** Queries: `MLSys reproducibility report could not reproduce speedup`; `MLSys 2020 reproducibility reviews published "reproducibility review" committee report`; `NeurIPS Datasets and Benchmarks reproducibility study "could not reproduce" speedup efficiency`. The MLSys-2020 effort exists (`https://github.com/parasj/checkmate`, `https://cknow.io/event/repro-mlsys2020/`) but neither page carries review text.
- **No ML Reproducibility Challenge report on an inference-acceleration paper.** OpenReview public search API queries (`reproducibility challenge quantization inference`, `"unable to reproduce" speedup inference`) returned ordinary ICLR/NeurIPS reviews only.
- **No independent blog replication of a specific quantized-kernel paper's speedup** was found that is verbatim-quotable; the closest legitimate independent measurement is the BentoML backend benchmark (vLLM's 4-bit AWQ path showing a lower token-generation rate than non-quantized competitors) and it is a *backend* benchmark, not a paper replication. Queries recorded in the sub-sweep log (`benchmarking LLM inference backends`, `4-bit AWQ slower vLLM benchmark blog`).
- **No primary source states that a decode-time CUDA graph is slower than eager** on vLLM/SGLang for a Qwen3-4B-class dense model at batch 1. The verified CUDA-graph negatives are capture cost (#20098: 67 graphs, >10 s), capture-disabled-by-default for prompt embeddings ("doubled the cuda graph capture time"), the default mode being the most memory-hungry and slowest to capture (vLLM CUDA-graphs design doc), and the sm120-specific *correctness* failure inside graphs (B1.1/C3.8). Exact queries: `cuda graph slower`, `"enforce-eager" performance`, `cuda graphs slower inference` (GitHub repo lists) and the corresponding Brave/DDG/Bing attempts recorded by the sub-sweep.
- **D.4 (datacenter fabric / distributed service) produced no item.** No paper or issue was found whose *acceleration claim itself* requires a datacenter interconnect or a distributed serving deployment (as opposed to merely being evaluated on one). The nearest artifacts are D2.1 (verified-hardware recipe listing H200/GB200/GB300/MI350X) and D3.1/D3.2 (multi-node reproductions), which are already recorded under their own categories.
- **arXiv API (`export.arxiv.org/api/query`) is unusable in this environment** — every batch attempt hung or returned nothing; all discovery was done through the arxiv.org HTML search endpoints and full-text HTML pages instead.
- **The global GitHub search endpoint (`https://github.com/search?q=…&type=issues`) returned HTTP 429 for most queries**; repo-scoped issue lists were used instead. One global query that did render (`repo:vllm-project/vllm sm120`, 351 results, 36 pages) is the source of the #49011/#51920/#53963/#54150 leads, so the failure mode is intermittent rather than absolute.

**Section counts (final):** CLOSED (A) 9 · OPEN (B) 3 · ABANDONED (C) 36 counted + 1 explicitly held out (C5.4b) + 2 verified-but-held-out withdrawn papers and 1 held-out INT8-MoE item noted inline · HARDWARE-RULED-OUT (D) 12.
