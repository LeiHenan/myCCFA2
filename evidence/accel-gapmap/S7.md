# S7 — Long-context acceleration that is NOT KV-cache policy (attention computation + architecture, single GPU)

Target rig: ONE RTX PRO 6000 Blackwell 96 GB, sm120 (CC 12.0, NOT sm100), no NVLink/IB, vLLM 0.29.0, SGLang 0.5.19,
target model **Qwen3-4B** (dense, 36 layers, GQA 32/8, full attention, `max_position_embeddings` 40960).
Sibling maps own KV-cache policy (paging / prefix caching / eviction / KV quant / offload / compression) and
speculative decoding — those appear here only as one-clause context. Context parallelism is multi-GPU and sits in Section D.

**Quoting convention used in this file.** Where a block has more than one retrieved source, the QUOTE / ASKING QUOTE /
STATED REASON / HARDWARE QUOTE field holds several *verbatim, individually continuous* fragments separated by ` ;; [n] `
markers, and each marker names the file or page the following fragment came from; such blocks carry the canonical
`(composite: quoted fragments interleaved with retrieved framing text)` label in EVIDENCE. Single-quote blocks are exactly
one continuous run from one page. Code fragments are reproduced with the source's own indentation; where a fragment is a
multi-line block, the line range is named in the marker. Every arXiv id in this file was re-read with `fetch.py arxiv <id>`
and its title matched (see NEGATIVE FINDINGS).

Local checkouts used for source quotes (primary, greppable): `/tmp/vllm-0.28.0` (vLLM release tree, used only where the
file is byte-identical in the v0.29.0 raw fetch, which is stated per row), `/tmp/sglang-main` (SGLang main snapshot,
tarball dated 2026-09-11), plus raw fetches pinned to tag `v0.5.19` / `v0.29.0` where version matters.

---

### A7.1 | CLOSED
QUESTION: Does Qwen3-4B itself have any sliding-window / local-attention / hybrid attention-pattern option?
WHO: Qwen team (shipped checkpoint config); vLLM consumes the same keys.
ARTIFACT: `Qwen/Qwen3-4B/config.json` keys `sliding_window`, `use_sliding_window`, `max_window_layers`
URL: https://huggingface.co/Qwen/Qwen3-4B/raw/main/config.json
STATUS: shipped (retrieved 2026-09-14)
EVIDENCE: READ BODY
QUOTE: "  "rope_scaling": null,
  "rope_theta": 1000000,
  "sliding_window": null,
  "tie_word_embeddings": true,
  "torch_dtype": "bfloat16",
  "transformers_version": "4.51.0",
  "use_cache": true,
  "use_sliding_window": false,"

### A7.2 | CLOSED
QUESTION: Can `--hf-overrides` alone give a full-attention model such as Qwen3-4B an interleaved SWA/full pattern (Gemma-3 / GPT-OSS style)?
WHO: vLLM maintainers (documented requirement + source guard).
ARTIFACT: `docs/contributing/model/basic.md` §"How to support models with interleaving sliding windows?"; `vllm/model_executor/models/qwen2.py` (parent class of `Qwen3Model`); `qwen3.py` decoder-layer construction
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/contributing/model/basic.md ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/models/qwen2.py
STATUS: shipped (vLLM 0.29.0 tree)
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "Make sure the model's config.json contains `layer_types`." ;; [second, continuous, same doc] "In the modeling code, parse the correct sliding window value for every layer, and pass it to the attention layer's `per_layer_sliding_window` argument." ;; [third, continuous source lines of qwen2.py, reproduced with the literal boundaries — at runtime these concatenate to: "Sliding window for some but all layers is not supported. This model uses sliding window but `max_window_layers` = {} is less than `num_hidden_layers` = {}. Please open an issue to discuss this feature."] "                "Sliding window for some but all layers is not supported. "
                "This model uses sliding window but `max_window_layers` = {} "
                "is less than `num_hidden_layers` = {}. Please open an issue "
                "to discuss this feature.".format(" ;; [fourth, continuous, qwen2.py — the decoder layers are built without any per-layer window] "            lambda prefix: decoder_layer_type(
                config=config,
                cache_config=cache_config,
                quant_config=quant_config,
                prefix=prefix,
            ),"

### A7.3 | CLOSED
QUESTION: Which models actually ship an interleaved SWA + full path in the rig's vLLM, and does the pattern alternate?
WHO: vLLM (`gpt_oss.py`, `gemma3.py`, `qwen3_next.py`).
ARTIFACT: `vllm/model_executor/models/gpt_oss.py` (alternating SWA + per-head sink logits); `gemma3.py` (`layer_types` + `rope_local_base_freq`)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/models/gpt_oss.py ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/models/gemma3.py
STATUS: shipped
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "        # Only apply sliding window to every other layer
        sliding_window = config.sliding_window if self.layer_idx % 2 == 0 else None" ;; [second, continuous, gemma3.py] "        layer_type = config.layer_types[layer_idx]
        self.is_sliding = layer_type == "sliding_attention"
        sliding_window = config.sliding_window if self.is_sliding else None"

### A7.4 | CLOSED
QUESTION: Attention sinks (StreamingLLM-style) exist as a model property in the rig's stack — which backend can actually execute them on sm120?
WHO: vLLM maintainers (auto-generated backend feature matrix).
ARTIFACT: `FLASHINFER trtllm-gen` (Sink ✅, Compute Cap. 10.x), `FLASH_ATTN FA3` (Sink ✅, 9.x), `FLASH_ATTN FA4` (Sink ✅, ≥10.0), `TRITON_ATTN` (Sink ✅, Compute Cap. Any); `FLASH_ATTN FA2` (Sink ❌)
URL: https://docs.vllm.ai/en/v0.29.0/design/attention_backends/
STATUS: shipped (vLLM 0.29.0 docs, page build 2026-09-08)
EVIDENCE: READ BODY (table rows flattened to text by HTML→text extraction)
QUOTE: "Sink Attention sink support (for StreamingLLM)" ;; [second, continuous] "TRITON_ATTN fp16, bf16, fp32 auto , float16 , bfloat16 , fp8 , fp8_e4m3 , fp8_e5m2 , int4_per_token_head , int8_per_token_head , fp8_per_token_head %16 Any ✅ ✅ ✅ ❌ All Any"

### A7.5 | CLOSED
QUESTION: Does vLLM have any sparse (subquadratic) attention path usable by a dense GQA model such as Qwen3-4B?
WHO: vLLM maintainers (backend feature matrix + backend priority lists).
ARTIFACT: Sparse column of the vLLM 0.29.0 attention-backend matrix; sparse MLA backends (`FLASHINFER_MLA_SPARSE` 10.x, `FLASHMLA_SPARSE` 9.x-10.x, `FLASH_ATTN_MLA_SPARSE` 9.x, `FLASHINFER_MLA_SPARSE_SM120` 12.x)
URL: https://docs.vllm.ai/en/v0.29.0/design/attention_backends/
STATUS: shipped
EVIDENCE: READ BODY (table rows flattened by extraction)
QUOTE: "Sparse Sparse attention support (MLA only)" ;; [second, continuous row] "FLASHINFER_MLA_SPARSE_SM120 bf16 auto , fp8 , fp8_e4m3 , fp8_ds_mla 64, 256 Any ❌ ❌ ❌ ❌ ❌ Decoder 12.x"

### A7.6 | CLOSED
QUESTION: On sm120, which FlashAttention version does vLLM actually resolve to, and is FA4 reachable?
WHO: vLLM (source gates, not docs).
ARTIFACT: `vllm/v1/attention/backends/fa_utils.py` version selection; `vllm/vllm_flash_attn/flash_attn_interface.py` `_is_fa4_supported()` / `_is_fa3_supported()`
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/v1/attention/backends/fa_utils.py ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/vllm_flash_attn/flash_attn_interface.py
STATUS: shipped (vLLM 0.29.0)
EVIDENCE: READ BODY
QUOTE: "        elif device_capability.major == 10 and is_fa_version_supported(4):
            # Blackwell (SM100+, restrict to SM100 for now): prefer FA4
            fa_version = 4
        else:
            # Fallback to FA2
            fa_version = 2" ;; [second, continuous, flash_attn_interface.py] "        or current_platform.is_device_capability_family(110)
    ):
        return (
            False,
            "FA4 is only supported on devices with compute capability 9.x, 10.x, or 11.x",
        )"

### A7.7 | CLOSED
QUESTION: Is there any *dense/GQA* block-sparse attention shipped in vLLM 0.29.0, and can a user select it?
WHO: vLLM maintainers (docs).
ARTIFACT: `MINIMAX_M3_SPARSE` ("lightning indexer" top-k KV-block selection)
URL: https://docs.vllm.ai/en/v0.29.0/design/attention_backends/
STATUS: shipped, model-wired only
EVIDENCE: READ BODY
QUOTE: "Block-sparse GQA backend used by MiniMax M3 sparse ("lightning indexer") layers. It is wired in directly by the model and is not part of the automatic priority lists above. A lightning indexer scores KV blocks, the top-k blocks (plus fixed init/local blocks) are selected, and attention attends only to those blocks; index keys live in a separate side cache."

### A7.8 | CLOSED
QUESTION: Does SGLang 0.5.19 (the rig's version) ship a single-GPU sparse attention that is validated on sm120?
WHO: SGLang maintainers (docs + source).
ARTIFACT: SGLang `dsa` (deprecated alias `nsa`) backend; `QwenSparseAttnBackend` / QSA (`layers/attention/qwen_sparse_attn_backend.py`, `layers/attention/qsa/`)
URL: https://docs.sglang.io/advanced_features/attention_backend.html ; https://raw.githubusercontent.com/sgl-project/sglang/main/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py
STATUS: DSA shipped in 0.5.19; QSA present on `main` only — `https://raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py` returns "404: Not Found" (fetched 2026-09-14)
EVIDENCE: READ BODY
QUOTE: "DSA (DeepSeek Sparse Attention) is a native sparse attention mechanism used by [DeepSeek V3.2](https://lmsys.org/blog/2025-09-29-deepseek-v32/). It is activated automatically when the model architecture requires it and is selected via `--attention-backend dsa` (deprecated alias: `nsa`)." ;; [second, continuous, SGLang main source] "    # This path is numerically validated on SM100 and SM120. Do not widen it
    # to every SM12x device: it silently corrupts long-context decode on
    # SM121/GB10.
    if not (is_sm100_supported() or is_sm120()):
        return None"

### A7.9 | CLOSED
QUESTION: Do the block-sparse *mask* mechanisms (FlexAttention `BlockMask`) ship in both engines, so a sparse pattern could be expressed without a custom CUDA kernel?
WHO: vLLM + SGLang + PyTorch.
ARTIFACT: `vllm/v1/attention/backends/flex_attention.py` (`BlockSparsityHint`, `BlockMask.from_kv_blocks`); SGLang `layers/attention/torch_flex_backend.py`
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/v1/attention/backends/flex_attention.py ; https://raw.githubusercontent.com/sgl-project/sglang/main/python/sglang/srt/layers/attention/torch_flex_backend.py
STATUS: shipped (both); vLLM lists `FLEX_ATTENTION` with Compute Cap. "Any"
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "    """This prunes KV blocks from the BlockMask before the flex_attention kernel
    is invoked, so that blocks that are fully masked never get loaded.
    Use this with custom mask_mods that are sparse to avoid
    the kernel iterating over all KV blocks unnecessarily.""" ;; [second, continuous, same file] "        """Direct block mask construction for paged KV cache attention.

        This method constructs the block mask directly using
        BlockMask.from_kv_blocks which is much more efficient than the
        generic create_block_mask approach."

### A7.10 | CLOSED
QUESTION: Prefill-side, non-KV: is chunked prefill the default, and is variable-length packing (`cu_seqlens`) how the attention kernel is fed?
WHO: vLLM maintainers (docs + source).
ARTIFACT: `docs/configuration/optimization.md` §Chunked Prefill; `vllm/v1/attention/backends/flash_attn.py` (`flash_attn_varlen_func`, `cu_seqlens_q=attn_metadata.query_start_loc`)
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/docs/configuration/optimization.md ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/v1/attention/backends/flash_attn.py
STATUS: shipped
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "In V1, **chunked prefill is enabled by default whenever possible**. With chunked prefill enabled, the scheduling policy prioritizes decode requests. It batches all pending decode requests before scheduling any prefill operations. When there are available tokens in the `max_num_batched_tokens` budget, it schedules pending prefills. If a pending prefill request cannot fit into `max_num_batched_tokens`, it automatically chunks it." ;; [second, continuous, flash_attn.py lines 1162-1167] "                flash_attn_varlen_func(
                    q=query[:num_actual_tokens],
                    k=key_cache,
                    v=value_cache,
                    out=output[:num_actual_tokens],
                    cu_seqlens_q=cu_seqlens_q,"

### A7.11 | CLOSED
QUESTION: RoPE side — is the old `--rope-scaling` flag still the way to configure context extension, and when is the RoPE cost paid?
WHO: vLLM maintainers (v0.29.0 docs + source).
ARTIFACT: `--rope-scaling` (removed) → `--hf-overrides '{"rope_parameters": {...}}'`; `RotaryEmbedding._compute_cos_sin_cache()` precomputes cos/sin at init
URL: https://docs.vllm.ai/en/v0.29.0/features/context_extension/ ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/layers/rotary_embedding/base.py
STATUS: shipped (v0.29.0 docs, page build 2026-09-08)
EVIDENCE: READ BODY
QUOTE: "The `--rope-scaling` parameter used in older versions of vLLM is no longer supported. Please use the `--hf-overrides` method with `rope_parameters` instead."

### A7.12 | CLOSED
QUESTION: Is "dynamic NTK" actually dynamic at runtime in vLLM, and are Qwen's own long-context RoPE flags honoured?
WHO: vLLM issue #41236 reporter (`zeel2104`); vLLM source.
ARTIFACT: `dynamic_ntk_scaling_rope.py::_compute_cos_sin_cache`; Qwen1 `use_logn_attn` / `use_dynamic_ntk`
URL: https://github.com/vllm-project/vllm/issues/41236 ; https://github.com/vllm-project/vllm/pull/37089
STATUS: issue closed COMPLETED (2026-04-29); PR #37089 closed unmerged
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "The original formula for NTK Dynamic scaling is: `(α * current sequence length / original model context length) - (α - 1)` ... However in `vllm/model_executor/layers/rotary_embedding/dynamic_ntk_scaling_rope.py` we see the following: ... If we substitute `max_len`, the formula becomes `(α * α) - (α - 1)`, which is a constant that is completely independent of the sequence length." ;; [second, continuous, PR #37089 body] "Qwen1 models (e.g. Qwen-7B) define two context-length extrapolation features — use_logn_attn and use_dynamic_ntk — that vLLM previously silently ignored."

### A7.13 | CLOSED
QUESTION: Methodology — do the headline "long-context speedup" numbers cover prefill, decode, or both?
WHO: Qwen team (Qwen2.5-1M Technical Report).
ARTIFACT: arXiv 2501.15383, "Qwen2.5-1M Technical Report" (title re-verified via `fetch.py arxiv 2501.15383`)
URL: https://arxiv.org/abs/2501.15383
STATUS: published (26 Jan 2025)
EVIDENCE: READ BODY
QUOTE: "By leveraging our inference framework, the Qwen2.5-1M models achieve a remarkable 3x to 7x prefill speedup in scenarios with 1 million tokens of context."

### A7.14 | CLOSED
QUESTION: Methodology — what is the default benchmark shape for long-context measurements in the rig's engines (single request? warm prefix?).
WHO: vLLM (config defaults + bench CLI docs); SGLang (`--chunked-prefill-size` default).
ARTIFACT: `vllm/config/cache.py::enable_prefix_caching: bool = True`; `vllm bench latency`; SGLang `--chunked-prefill-size`
URL: https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/config/cache.py ; https://raw.githubusercontent.com/sgl-project/sglang/main/python/sglang/srt/arg_groups/fields/schedule.py ; https://docs.vllm.ai/en/v0.29.0/benchmarking/cli/
STATUS: shipped
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)
QUOTE: "    enable_prefix_caching: bool = True
    """Whether to enable prefix caching.""" ;; [second, continuous, SGLang main `arg_groups/fields/schedule.py`] "    chunked_prefill_size: A[
        Optional[int],
        "The maximum number of tokens in a chunk for the chunked prefill. Setting this to -1 means disabling chunked prefill.",
    ] = None"

---

### B7.1 | OPEN
WHO IS STILL ASKING: `myshytf` on Dao-AILab/flash-attention (issue + fix PR), for FA4-on-sm120 correctness in varlen prefill.
URL: https://github.com/Dao-AILab/flash-attention/issues/2860 ; https://github.com/Dao-AILab/flash-attention/pull/2862
ASKING QUOTE: "2. **With (1) worked around, the SM120 kernel still hits an illegal memory access
   once the varlen batch has >= 4 segments under real-workload memory layouts.**" ;; [second, continuous, PR #2862] "We hit it in production on RTX PRO 6000 (sm_120) serving a MLA model whose prefill batches mix a prefix-cache-hit tail with a fresh request: a GPU core dump showed FlashAttentionForwardSm120 grid 330 (30 tiles × 11 heads) faulting in CTAs 319-329 — exactly the 11 over-provisioned tiles — at the K/V LDGSTS ."
WHAT IS MISSING: vLLM 0.29.0 gates FA4 out on sm120 entirely (`_is_fa4_supported()` accepts families 90/100/110 only) and falls back to FA2, so the upstream sm120 FA4 kernel is not reachable from the rig anyway; the fix PR is open and unmerged. Falsification check run 2026-09-14: `https://raw.githubusercontent.com/vllm-project/vllm/main/vllm/vllm_flash_attn/flash_attn_interface.py` (main, not 0.29.0) still returns the same 90/100/110-only gate, so nothing merged after 0.29.0 has fixed this.
STATE: issue #2860 OPEN, PR #2862 OPEN (both verified by raw-HTML `"state":"OPEN"` extraction on 2026-09-14; no `mergedAt` present on the PR).
S3B: OCCUPIED (open issue #2860 / open PR #2862) — fresh queries: `https://github.com/Dao-AILab/flash-attention/issues?q=sm120` (list page, React-rendered — inconclusive), web_search "flash-attn4 sm120 RTX PRO 6000 Blackwell support", direct fetch of both URLs above.
EVIDENCE: READ BODY

### B7.2 | OPEN
WHO IS STILL ASKING: `manunicholasjacob` (issue) and `Dmytro-Khvedchuk` (PR) on vllm-project/vllm, for sm120 FlashInfer JIT coverage below CUDA 12.9.
URL: https://github.com/vllm-project/vllm/issues/50705 ; https://github.com/vllm-project/vllm/pull/54267
ASKING QUOTE: "On an RTX PRO 6000 Blackwell Server Edition (sm_120) where the local CUDA
toolkit is 12.8 (below the 12.9 FlashInfer needs to compile SM 12.x)," ;; [second, continuous, same issue] "WARNING flashinfer.compilation_context: Failed to get device capability: SM 12.x requires CUDA >= 12.9."
WHAT IS MISSING: vLLM still auto-selects FLASHINFER on sm120 and then dies during warmup instead of falling back; no merged fix, and the attention-backend priority table for standard attention in the docs has no SM 12.x row at all.
S3B: OCCUPIED (open issue #50705 / open PR #54267) — fresh queries: web_search "flash-attn sm120 RTX 5090 not supported issue", "FlashAttention-4 support sm120 compute capability 12.0 only sm100"; direct fetch of both URLs.
STATE: issue #50705 `"state":"OPEN"` (raw-HTML extraction, 2026-09-14); PR #54267 `STATE-TOKEN: open`; the PR title itself is "[Bugfix] Disable FlashInfer on sm_120 when the CUDA toolkit is older than 12.9", i.e. still a proposal, not shipped.
EVIDENCE: READ BODY

### B7.3 | OPEN
WHO IS STILL ASKING: vLLM SIG Large Scale Serving (maintainer roadmap, issue tracked publicly).
URL: https://github.com/vllm-project/vllm/issues/48168
ASKING QUOTE: "* Long context:
  - [ ] Efficiently support various context parallelisms for SOTA models."
WHAT IS MISSING: vLLM's own Q3 2026 roadmap still lists long-context context parallelism as an unchecked item; the shipped surface today is `--decode-context-parallel-size` / `prefill_context_parallel_size`, both of which require more than one GPU rank (see D7.5).
S3B: OCCUPIED (open issue #48168, roadmap) — fresh queries: web_search "vllm github issue long context context parallel roadmap", plus fetch `fetch.py gh https://github.com/vllm-project/vllm/issues/48168`.
STATE: issue #48168 `"state":"OPEN"` (raw-HTML extraction, 2026-09-14); the unchecked checkbox is verbatim from the SIG Large Scale Serving section of that open roadmap.
EVIDENCE: READ BODY

---

### C7.1 | ABANDONED (sparse attention removed from vLLM: block-sparse / Phi-3-small)
WHO: WoosukKwon (vLLM maintainer), merged by the vLLM project.
ARTIFACT: PR #21217 "[V0 Deprecation] Deprecate BlockSparse Attention & Phi3-Small" (merged); registry entry `_PREVIOUSLY_SUPPORTED_MODELS = { ... "Phi3SmallForCausalLM": "0.9.2", ... }`
URL: https://github.com/vllm-project/vllm/pull/21217 ; https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/model_executor/models/registry.py
STATED REASON: "This PR removes the block sparse attention and the support for phi3-small which uses the attention."
STATE: PR #21217 merged (verified: `STATE-TOKEN: merged`); the registry line `"Phi3SmallForCausalLM": "0.9.2"` is under `_PREVIOUSLY_SUPPORTED_MODELS` in v0.29.0
S3B: SOLVED-BY-REMOVAL — the removal is the resolution; no open PR re-adds a general block-sparse attention kernel. Fresh queries: web_search "vllm pull request remove Phi-3-small blocksparse attention model support"; `grep -rn "BlockSparse" --include=*.py /tmp/vllm-0.28.0/vllm/` (0 attention hits; only MoE "block sparse" weight hits); `grep -rn -i "phi3small" vllm/model_executor/models/registry.py` → only the `_PREVIOUSLY_SUPPORTED_MODELS` line.
REASON-MAY-HAVE-EXPIRED: YES — the stated reason was the V0→V1 deprecation sweep, a constraint that no longer exists in vLLM 0.29.0; nothing about the removal is hardware-specific, so a block-sparse attention path could be re-added without that blocker.
EVIDENCE: READ BODY

### C7.2 | ABANDONED (vLLM sparse-indexer bring-up for SM120, superseded)
WHO: voipmonitor (author), closed by the project as superseded.
ARTIFACT: PR #38952 "[Superseded] Sparse indexer fallback warning for SM120 bring-up"
URL: https://github.com/vllm-project/vllm/pull/38952
STATED REASON: "Superseded by #39635 and tracked in #37113 . This standalone bring-up change is no longer the canonical path." ;; verbatim warning text the PR proposed: "DeepGEMM not available for Sparse Attention Indexer. Sparse attention will be disabled (dense MLA fallback)."
STATE: PR #38952 closed, NOT merged (`STATE-TOKEN: closed`, no merged token); successor #39635 shows `STATE-TOKEN: merged`.
S3B: SOLVED (merged #39635 "[SM120][MLA] Fix FlashInfer MLA DCP/MTP decode path"); fresh queries: web_search "vllm Sparse indexer fallback warning SM120 bring-up"; `fetch.py gh` on #38952 and #39635.
REASON-MAY-HAVE-EXPIRED: PARTLY — the "superseded" reason expired the moment #39635 merged; what has NOT expired is the underlying constraint quoted in the PR (no DeepGEMM path on the SM120 sparse indexer), which is why the fallback warning exists at all.
EVIDENCE: READ BODY

### C7.3 | ABANDONED (Native Sparse Attention on consumer/workstation GPUs)
WHO: voipmonitor, after "7+ days of testing every available NSA+MTP Docker image on 8×RTX PRO 6000".
ARTIFACT: vLLM issue #43235 "[RFC] NSA is architecturally incompatible with NVIDIA consumer/workstation GPUs — MLA is the only viable sparse attention path" (open; labeled `RFC`, `stale`)
URL: https://github.com/vllm-project/vllm/issues/43235
STATED REASON: "After extensive production testing on SM120 (RTX PRO 6000 Blackwell, 8×96GB, PCIe Gen5), NSA (Native Sparse Attention) cannot be made to work reliably for production serving on NVIDIA consumer/workstation GPUs. This is not a bug — it is an architectural incompatibility." ;; "The empirical evidence is clear: after 7+ days of testing every available NSA+MTP Docker image on 8×RTX PRO 6000, none are production-viable. NSA on SM120 is a dead end."
S3B: OCCUPIED (open issue #43235; related tracker #37113 "[SM120][GLM-5.1] NVFP4 DCP/MTP stack tracker", closed `NOT_PLANNED` with no human closure reason, therefore recorded only as a pointer, not as a closure reason) — fresh queries: web_search "vllm issue NSA architecturally incompatible consumer GPUs"; `fetch.py gh` on #43235, #37113, #39635.
STATE: **the issue itself is still OPEN** (`"state":"OPEN"`, no `stateReason`; labels `RFC`, `stale`; the only non-author comment is the automated stale-bot notice, which is explicitly NOT treated as a closure reason here). What is abandoned is the *effort the RFC describes* — the author's own NSA-on-consumer-GPU attempt — not the issue. This row is an abandoned-attempt record, not a closed-artifact record.
REASON-MAY-HAVE-EXPIRED: NONE — the stated blocker is architectural (SM120 lacks the FP8 block-scaled GEMM units the sparse indexer needs), which is not something a software version bump changes. Note the RFC is a community report, not a maintainer decision, and the stale-bot comment on it is not a closure reason.
EVIDENCE: READ BODY

### C7.4 | ABANDONED (SeerAttention — learned sparse attention)
WHO: Yizhao Gao et al. (Microsoft / Tsinghua / etc.)
ARTIFACT: arXiv 2410.13276 "SeerAttention: Learning Intrinsic Sparse Attention in Your LLMs" (title re-verified via `fetch.py arxiv 2410.13276`)
URL: https://arxiv.org/abs/2410.13276
STATED REASON: "When applied to pre-trained LLMs, SeerAttention only requires training the gate parameters in a lightweight self-distillation manner, allowing rapid convergence." ;; scale limit: "For instance, when applied to a Llama-3.1-8B-Instruct model, SeerAttention uses only 0.5B tokens for gate distillation, which is approximately 40 A100 GPU hours for training."
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rli "seerattention" --include=*.py /tmp/vllm-0.28.0/vllm/ /tmp/sglang-main/python/sglang/srt/` (0 hits); web_search "vllm github issue implement MInference Quest SeerAttention sparse attention support". No vLLM or SGLang PR/issue implementing SeerAttention was found.
REASON-MAY-HAVE-EXPIRED: PARTLY — "≈40 A100 GPU hours" is A100-era framing and would be cheaper on the rig, but the blocking property (a *trained* sparsity gate must exist per model before the kernel helps) does not expire.
EVIDENCE: READ BODY (abstract via `fetch.py arxiv`; scale quote from paper HTML)

### C7.5 | ABANDONED (MInference — dynamic sparse prefill)
WHO: Huiqiang Jiang et al. (Microsoft)
ARTIFACT: arXiv 2407.02490 "MInference 1.0: Accelerating Pre-filling for Long-Context LLMs via Dynamic Sparse Attention" (title re-verified)
URL: https://arxiv.org/abs/2407.02490
STATED REASON: "After obtaining the dynamic sparse mask, three optimized GPU kernels are used, which we developed for the above three sparse patterns." ;; hardware scope: "The latency experiments are conducted on a single Nvidia A100 GPU in the bfloat16 format."
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rli "minference" --include=*.py /tmp/vllm-0.28.0/vllm/ /tmp/sglang-main/python/sglang/srt/` (0 hits); web_search "vllm github issue implement MInference Quest SeerAttention sparse attention support". No engine-side implementation found.
REASON-MAY-HAVE-EXPIRED: PARTLY — the A100-only *measurement* is A100-era and expired as a fact, but the blocker is the three hand-written kernels, which have no Blackwell/sm120 build or CI that I could find (see NEGATIVE FINDINGS).
EVIDENCE: READ BODY (abstract via `fetch.py arxiv`; kernel/A100 quotes from paper HTML)

### C7.6 | ABANDONED (Double Sparsity — post-training sparse attention)
WHO: Shuo Yang, Ying Sheng, Joseph E. Gonzalez, Ion Stoica, Lianmin Zheng
ARTIFACT: arXiv 2408.07092 "Post-Training Sparse Attention with Double Sparsity" (title re-verified; note the id is right but the earlier working title in the brief was wrong)
URL: https://arxiv.org/abs/2408.07092
STATED REASON: "Our key insight is that the pattern of channel sparsity is relatively static, allowing us to use offline calibration to make it efficient at runtime, thereby enabling accurate and efficient identification of important tokens." ;; kernel/hardware: "The kernel for attention over top-k tokens was designed using OpenAI Triton." ; "Hardware. Our experiments were conducted on two types of GPUs: the A10G and the A100-SXM."
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rli "double_sparsity" --include=*.py /tmp/vllm-0.28.0/vllm/ /tmp/sglang-main/python/sglang/srt/` (0 hits); web_search query listed above.
REASON-MAY-HAVE-EXPIRED: PARTLY — the A10G half of the evaluation shows the idea is not A100-locked, but the offline-calibration requirement (a per-model calibration artifact) does not expire, and no sm120/Blackwell evaluation exists.
EVIDENCE: READ BODY

### C7.7 | ABANDONED (NSA paper — evaluation scale)
WHO: Jingyang Yuan et al. (DeepSeek-AI)
ARTIFACT: arXiv 2502.11089 "Native Sparse Attention: Hardware-Aligned and Natively Trainable Sparse Attention" (title re-verified)
URL: https://arxiv.org/abs/2502.11089
STATED REASON: "We evaluate the computational efficiency of NSA against Full Attention on an 8-GPU A100 system." ;; training cost: "Pretraining on a 27B-parameter transformer backbone with 260B tokens, we assess NSA's performance across general language evaluations, long-context evaluations, and chain-of-thought reasoning evaluation." ;; kernel: "To achieve FlashAttention-level speedup during the training and prefilling, we implement hardware-aligned sparse attention kernels upon Triton."
S3B: SOLVED-FOR-MODEL-NATIVE-CASES / NO-ACTIVE-WORK-FOUND for a dense-model drop-in — SGLang ships an `nsa`/`dsa` backend, but the docs state it "is activated automatically when the model architecture requires it" (see A7.8); `grep -rli "nsa" /tmp/vllm-0.28.0/vllm/` finds only MLA-sparse backends. Fresh queries: web_search "sglang NSA DeepSeek sparse attention pull request implementation"; `grep` above.
REASON-MAY-HAVE-EXPIRED: NONE for the paper's own numbers (8-GPU A100 evaluation is a scale constraint, not a hardware-era one), and none for the engine side — NSA remains model-native (MLA/DSA) rather than a switchable optimization.
EVIDENCE: READ BODY

### C7.8 | ABANDONED (MoBA — Mixture of Block Attention)
WHO: Enzhe Lu et al. (Moonshot AI / Tsinghua)
ARTIFACT: arXiv 2502.13189 "MoBA: Mixture of Block Attention for Long-Context LLMs" (title re-verified)
URL: https://arxiv.org/abs/2502.13189
STATED REASON: "To reach the 10M context length, we expanded tensor parallelism ( Shoeybi et al., 2019 ) toward the query head level, Specifically, we broadcast key and value tensors across distributed query heads, effectively addressing GPU memory limitations while preserving computational efficiency." (the paper names no GPU model anywhere; its only scale statement is tensor-parallel across distributed query heads)
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rli "moba" --include=*.py /tmp/vllm-0.28.0/vllm/ /tmp/sglang-main/python/sglang/srt/` (0 hits); web_search "sglang pull request MoBA mixture of block attention", "sglang pull request sparse attention closed unmerged MoBA MInference".
REASON-MAY-HAVE-EXPIRED: UNKNOWN — the paper does not state a hardware constraint precise enough to expire; MoBA-as-shipped lives in Kimi's stack, not in either engine on the rig.
EVIDENCE: READ BODY

### C7.9 | ABANDONED (Quest — query-aware sparsity; self-limited throughput)
WHO: Jiaming Tang et al. (MIT / NVIDIA / Univ. of Washington)
ARTIFACT: arXiv 2406.10774 "Quest: Query-Aware Sparsity for Efficient Long-Context LLM Inference" (title re-verified)
URL: https://arxiv.org/abs/2406.10774
STATED REASON: "Tested with FP16 FlashInfer implementation on an RTX4090 , limiting the overall throughput." (footnote; whitespace normalized by HTML→text extraction — original reads "RTX 4090") ;; kernel: "To demonstrate the feasibility of Quest, we implement the entire framework with dedicated CUDA kernels based on FlashInfer ( Ye et al., 2024 ) , a kernel library for LLM inference."
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rl "Quest" --include=*.py /tmp/vllm-0.28.0/vllm/` (hits are false positives: `iquest_loopcoder.py`, "request" substrings in `kv_offload/` and `prefix_prefill.py`); web_search "vllm github issue implement MInference Quest SeerAttention sparse attention support".
REASON-MAY-HAVE-EXPIRED: PARTLY — the RTX 4090 evaluation is the *opposite* of an A100-only constraint, so "consumer GPU" is not the blocker; what remains is that Quest's paged top-k selection has no upstream implementation in either engine.
EVIDENCE: READ BODY

### C7.10 | ABANDONED (SGLang's merged block-sparse attention backend for Qwen-1M)
WHO: FlamingoPg (author), merged by merrymercy into sgl-project/sglang.
ARTIFACT: PR #5949 "[Feat] QWen-1M context support[2/2]: Update block sparse attention backend" (state: merged)
URL: https://github.com/sgl-project/sglang/pull/5949
STATED REASON: no closure/removal reason was found. The PR's own stated outstanding gap is: "Todo: support cudagraphs for block sparse attention backend" — and the backend is **absent from the rig's version**: `https://raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/layers/attention/attention_registry.py` contains no `block_sparse`/`blocksparse` string and its sparse-looking backend choices are only `"dsa"` and `"nsa"` (raw fetch + grep, 2026-09-14).
S3B: NO-ACTIVE-WORK-FOUND in SGLang 0.5.19 (the merged backend is not in the rig's release); fresh queries: `grep -n -i "block_sparse\|blocksparse" /tmp/s7/sgl_reg119.py` (0 hits), web_search "sglang pull request sparse attention closed unmerged MoBA MInference", "Feat QWen-1M context support block sparse attention backend".
STATE: PR #5949 was **merged** (`STATE-TOKEN: merged`; page text "merrymercy merged 25 commits"), so this is not a rejected PR. The row records a shipped-then-absent backend, verified only by the v0.5.19 registry absence check above; no removal PR was located, so the removal *reason* is unknown and is not asserted anywhere in this row.
REASON-MAY-HAVE-EXPIRED: YES — the feature targeted Qwen-1M (Qwen2.5-1M class) long context and was written pre-Blackwell; SGLang has since replaced model-gated sparse attention with DSA/NSA/QSA backends, so the original constraint (no sparse block-sparse backend for Qwen long context) no longer describes the current tree.
EVIDENCE: TITLE + STATE ONLY (plus a verified absence check in v0.5.19's registry)

### C7.11 | ABANDONED (YaRN — RoPE extension still needs fine-tuning)
WHO: Bowen Peng, Jeffrey Quesnelle, Honglu Fan, Enrico Shippole
ARTIFACT: arXiv 2309.00071 "YaRN: Efficient Context Window Extension of Large Language Models" (title re-verified)
URL: https://arxiv.org/abs/2309.00071
STATED REASON: "We present YaRN (Yet another RoPE extensioN method), a compute-efficient method to extend the context window of such models, requiring 10x less tokens and 2.5x less training steps than previous methods." (i.e. it is a *training* recipe, not a training-free runtime knob)
S3B: SOLVED for the *mechanism* (vLLM ships `rope_type: "yarn"` via `--hf-overrides` — see A7.11) / NO-ACTIVE-WORK-FOUND for the fine-tuning claim. Fresh queries: `grep -rn "yarn" /tmp/vllm-0.28.0/vllm/model_executor/layers/rotary_embedding/__init__.py`, `fetch.py get https://docs.vllm.ai/en/v0.29.0/features/context_extension/`.
REASON-MAY-HAVE-EXPIRED: NO — the fine-tuning requirement is a property of the method, not of the hardware.
EVIDENCE: READ BODY

### C7.12 | ABANDONED (LongRoPE — requires per-model fine-tuning)
WHO: Yiran Ding et al. (Microsoft)
ARTIFACT: arXiv 2402.13753 "LongRoPE: Extending LLM Context Window Beyond 2 Million Tokens" (title re-verified)
URL: https://arxiv.org/abs/2402.13753
STATED REASON: "This paper introduces LongRoPE that, for the first time, extends the context window of pre-trained LLMs to an impressive 2048k tokens, with up to only 1k fine-tuning steps at within 256k training lengths, while maintaining performance at the original short context window."
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `grep -rli "longrope" --include=*.py /tmp/vllm-0.28.0/vllm/ /tmp/sglang-main/python/sglang/srt/` (no implementation); `fetch.py arxiv 2402.13753`.
REASON-MAY-HAVE-EXPIRED: NO — the 1k-step fine-tuning requirement per model is method-inherent.
EVIDENCE: READ BODY

### C7.13 | ABANDONED (Scaling Laws of RoPE-based Extrapolation — requires training)
WHO: Yikai Zhang, Jiayan Teng, et al.
ARTIFACT: arXiv 2310.05209 "Scaling Laws of RoPE-based Extrapolation" (title re-verified)
URL: https://arxiv.org/abs/2310.05209
STATED REASON: "Besides these observations and analyses, we achieve extrapolation up to 1 million context length within only 16K training length on LLaMA2 7B and 13B." (an extrapolation claim whose enabling step is training, not an inference-time setting)
S3B: NO-ACTIVE-WORK-FOUND — fresh queries: `fetch.py arxiv 2310.05209`; grep for the paper's method in both engines (no hits).
REASON-MAY-HAVE-EXPIRED: NO — training-length requirement is method-inherent.
EVIDENCE: READ BODY

### C7.14 | ABANDONED (Longformer local+global interleaved attention requires pretraining)
WHO: Iz Beltagy, Matthew E. Peters, Arman Cohan (Allen AI)
ARTIFACT: arXiv 2004.05150 "Longformer: The Long-Document Transformer" (title re-verified; BigBird 2007.14062 is the same family — sparse local+global pattern baked in at pretraining time)
URL: https://arxiv.org/abs/2004.05150
STATED REASON: "In contrast to most prior work, we also pretrain Longformer and finetune it on a variety of downstream tasks."
S3B: NO-ACTIVE-WORK-FOUND in the rig's engines as a switchable pattern; the *mechanism* is what shipped later as model-native interleaved `layer_types` (see A7.2/A7.3). Fresh queries: `grep -rli "longformer" --include=*.py /tmp/vllm-0.28.0/vllm/` (no attention implementation); web_search "vllm github issue sliding window attention interleaved layers support request".
REASON-MAY-HAVE-EXPIRED: YES, in a specific sense — the paper's constraint ("you must pretrain the pattern in") is exactly why the 2025-26 answer is *model-native* interleaved SWA (Gemma-3, GPT-OSS) rather than a runtime flag; the runtime-flag version is what `--hf-overrides` cannot deliver (A7.2).
EVIDENCE: READ BODY

---

### D7.1 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: Hao Liu, Matei Zaharia, Pieter Abbeel (UC Berkeley)
ARTIFACT: arXiv 2310.01889 "Ring Attention with Blockwise Transformers for Near-Infinite Context" (title re-verified)
URL: https://arxiv.org/abs/2310.01889
HARDWARE QUOTE: "For GPUs, we consider both single DGX A100 server with 8 GPUs and distributed 32 A100 GPUs."
EVIDENCE: READ BODY (abstract via `fetch.py arxiv`; hardware sentence from paper HTML)

### D7.2 | HARDWARE-RULED-OUT   (D.1 >1 GPU | D.3 multi-node)
WHO: Jiarui Fang, Shangchun Zhao (Tencent)
ARTIFACT: arXiv 2405.07719 "USP: A Unified Sequence Parallelism Approach for Long Context Generative AI" (title re-verified)
URL: https://arxiv.org/abs/2405.07719
HARDWARE QUOTE: "We achieved 47% MFU on two 8xA800 nodes using SP for the LLAMA3-8B model training using sequence length 208K."
EVIDENCE: READ BODY

### D7.3 | HARDWARE-RULED-OUT   (D.3 multi-node/NVLink/IB)
WHO: Diandian Gu et al. (Shanghai Jiao Tong University / Tencent)
ARTIFACT: arXiv 2406.18485 "LoongTrain: Efficient Training of Long-Sequence LLMs with Head-Context Parallelism" (title re-verified)
URL: https://arxiv.org/abs/2406.18485
HARDWARE QUOTE: "Figure 5 . Forward time evaluation of Ulysses-Attention and Ring-Attention on 8 physical nodes, each equipped with 8 NVIDIA Ampere GPUs connected by NVLINK. Each node has four 200 HDR NICs." ;; [second, continuous] "To scale training with 1M sequence length to 2048 GPUs, constrained by the global batch size of 4M tokens"
EVIDENCE: READ BODY

### D7.4 | HARDWARE-RULED-OUT   (D.1 >1 GPU | D.3 multi-host)
WHO: Shantanu Acharya, Fei Jia, Boris Ginsburg (NVIDIA)
ARTIFACT: arXiv 2411.17116 "Star Attention: Efficient LLM Inference over Long Sequences" (title re-verified)
URL: https://arxiv.org/abs/2411.17116
HARDWARE QUOTE: "We introduce Star Attention, a two-phase block-sparse approximation that improves computational efficiency by sharding attention across multiple hosts while minimizing communication overhead." ;; [second, continuous, paper HTML] "Table 6 : Time per sample (seconds) for Llama3.1-8B-Instruct model with dense, ring, and star attention, using 8 A100 GPUs."
EVIDENCE: READ BODY

### D7.5 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: vLLM maintainers (context-parallel deployment doc).
ARTIFACT: vLLM docs "Context Parallel Deployment": prefill context parallel + `-dcp` decode context parallel
URL: https://docs.vllm.ai/en/v0.29.0/serving/context_parallel_deployment/
HARDWARE QUOTE: "During prefill, for a long request with T new tokens, we need to compute query/key/value tensors for these new tokens. Say we have N GPUs, we can split the request into N chunks, and each GPU computes one chunk of the query/key/value tensors." ;; [second, continuous] "If the request token length is too long, we cannot afford holding the full key/value tensors anymore, then we can only compute one chunk of query/key/value tensors for each GPU, and use techniques like ring-attention to send/recv key/value tensors chunk by chunk." ;; [third, continuous, immediately following the second] "Both approaches are under active development."
EVIDENCE: READ BODY (composite: quoted fragments interleaved with retrieved framing text; each fragment boundary is marked inline by ' ;; [n] ', and each marker names its source file/page)

### D7.6 | HARDWARE-RULED-OUT   (D.1 >1 GPU)
WHO: SGLang maintainers (server args + cookbook).
ARTIFACT: `--enable-prefill-cp` + `--cp-strategy {zigzag,interleave}` + `--attn-cp-size` (prefill CP); `--dcp-size` (decode CP)
URL: https://github.com/sgl-project/sglang/blob/main/docs/docs/advanced_features/server_arguments.mdx ; https://github.com/sgl-project/sglang/blob/main/docs/cookbook/autoregressive/GLM/GLM-5.3-Flash.mdx
HARDWARE QUOTE: "The **Context Parallelism** row offers DCP4, validated on 4x GB300 (TP4/EP4) with both KV pairings and adaptive MTP 5/1/6; it requires the current release image, which carries the TileLang LSE fix." ;; [second, continuous, GLM-5.2 cookbook] "--attn-cp-size 8 \
--enable-prefill-cp \
--cp-strategy interleave \" ;; [third, continuous, same page] "which splits the sequence equally across `--attn-cp-size` ranks during attention forward."
EVIDENCE: READ BODY

---

## SEARCH LOG (queries actually issued)

GitHub search entry points attempted and their outcome (important for the next researcher):
- `https://github.com/search?q=repo%3Avllm-project%2Fvllm+...&type=issues` — **HUNG/timed out** on every attempt (60 s, 120 s, 300 s, and a bare `curl --max-time 30 --retry 2`). The brief's documented entry point is not usable in this environment right now.
- `<repo>/issues?q=<term>` and `<repo>/pulls?q=<term>` — pages load, but they are React-rendered: the embedded JSON (`<script type="application/json" data-target="react-app.embeddedData">`, parsed with a custom walker) contains **only pinned issues**, no search results. Verified by parsing `https://github.com/vllm-project/vllm/issues?q=mInference` (returned only #48168, #44280, #50001 — the pinned roadmap/tracking issues).
- What DID work: individual issue/PR pages via `python3 fetch.py gh <url>`, `web_search` for discovery of PR/issue numbers, and raw.githubusercontent.com file fetches pinned to tags.

web_search queries issued (discovery only; every resulting arXiv ID and GitHub artifact was then retrieved directly):
1. "vllm github issue sparse attention support for dense models not MLA"
2. "vllm pull request MInference dynamic sparse attention unmerged"
3. "sglang github pull request MoBA mixture of block attention"
4. "vllm issue sliding window max_model_len Gemma mistral attention"
5. "vllm github issue Qwen3 sliding window attention support hf-overrides"
6. "flash-attention pull request block sparse attention unmerged closed"
7. "vllm issue chunked prefill long context prefill TTFT"
8. "sglang issue ring attention context parallel single GPU"
9. "vllm pull request remove Phi-3-small blocksparse attention model support"
10. "\"BlockSparseFlashAttention\" vllm removed deprecate"
11. "flash-attention block sparse attention support removed unsupported GPUs"
12. "vllm issue dynamic NTK rope scaling not supported long context"
13. "vllm pull request sparse attention closed unmerged not planned dense model"
14. "sglang pull request sparse attention closed unmerged MoBA MInference"
15. "vllm github issue implement MInference Quest SeerAttention sparse attention support"
16. "sglang NSA DeepSeek sparse attention pull request implementation"
17. "vllm github issue sliding window attention interleaved layers support request"
18. "vllm flash attention varlen cu_seqlens packing prefill long context issue"
19. "vllm rope scaling removed hf-overrides issue"
20. "vllm long context benchmark prefill excluded prefix caching issue"
21. "flash-attn4 sm120 RTX PRO 6000 Blackwell support" (delegated thread, re-used)
22. "FlashAttention-4 support sm120 compute capability 12.0 only sm100" (delegated thread, re-used)
23. "flash-attn sm120 RTX 5090 not supported issue" (delegated thread, re-used)

Pages/artifacts retrieved with `fetch.py gh` (individual issue/PR bodies; comment extraction verified working):
- https://github.com/vllm-project/vllm/issues/43235 (NSA on consumer/workstation GPUs RFC — READ BODY)
- https://github.com/vllm-project/vllm/issues/37113 (SM120 GLM-5.1 NVFP4 DCP/MTP tracker)
- https://github.com/vllm-project/vllm/issues/48168 (Q3 2026 roadmap)
- https://github.com/vllm-project/vllm/issues/41236 (dynamic NTK RoPE bug)
- https://github.com/vllm-project/vllm/issues/50705 ; /pull/54267 (FlashInfer sm_120 + CUDA 12.9)
- https://github.com/vllm-project/vllm/pull/38952 (superseded SM120 sparse indexer)
- https://github.com/vllm-project/vllm/pull/39635 (merged SM120 MLA DCP/MTP decode)
- https://github.com/vllm-project/vllm/pull/21217 (BlockSparse + Phi3-Small removal)
- https://github.com/vllm-project/vllm/pull/12040 (BlockSparseFlashAttention naming bugfix, merged)
- https://github.com/vllm-project/vllm/pull/17772 ([Model] Allow the use of sliding window in Qwen2, merged)
- https://github.com/vllm-project/vllm/pull/37089 (Qwen1 use_logn_attn / use_dynamic_ntk, closed unmerged)
- https://github.com/sgl-project/sglang/pull/5949 (Qwen-1M block sparse attention backend, merged)
- https://github.com/sgl-project/sglang/issues/22223 (prefill CP with zigzag ring attention)
- https://github.com/Dao-AILab/flash-attention/issues/2860 ; /pull/2862 (FA4 SM120 varlen IMA)
- https://github.com/huggingface/transformers/issues/40126 (Qwen3 slide attention semantics)

Docs/config pages retrieved:
- `fetch.py get https://docs.vllm.ai/en/v0.29.0/design/attention_backends/` (full backend matrix, priority lists, legends, b12x note absent at v0.29.0)
- `fetch.py get https://docs.vllm.ai/en/latest/design/attention_backends/` (b12x SM120/SM121 attention backend present at `latest`, build 2026-09-13 — NOT present in the v0.29.0 page, build 2026-09-08: version drift, recorded as such)
- `fetch.py get https://docs.vllm.ai/en/v0.29.0/features/context_extension/`
- `fetch.py get https://docs.vllm.ai/en/v0.29.0/serving/context_parallel_deployment/`
- `fetch.py get https://docs.sglang.io/advanced_features/attention_backend.html` (delegated thread)
- `fetch.py raw https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/platforms/cuda.py`
- `fetch.py raw https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/v1/attention/backends/fa_utils.py`
- `fetch.py raw https://raw.githubusercontent.com/vllm-project/vllm/v0.29.0/vllm/vllm_flash_attn/flash_attn_interface.py`
- `fetch.py raw https://raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/layers/attention/attention_registry.py`
- `fetch.py raw https://raw.githubusercontent.com/sgl-project/sglang/main/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py`
- `fetch.py raw https://raw.githubusercontent.com/sgl-project/sglang/v0.5.19/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py` → "404: Not Found"
- `fetch.py raw https://huggingface.co/Qwen/Qwen3-4B/raw/main/config.json`

Local source greps (primary trees): `/tmp/vllm-0.28.0` and `/tmp/sglang-main`, covering `sliding_window`, `per_layer_sliding_window`, `layer_types`, `max_window_layers`, `sinks`, `sparse_attention`, `BlockSparse`, `dual_chunk`, `enable_prefix_caching`, `chunked_prefill_size`, `cu_seqlens`, `context_parallel`, `attn_cp_size`, `enable_prefill_cp`, `dcp-size`, `mInference`, `quest`, `seerattention`, `moba`, `double_sparsity`, `qsa`, `nsa`, `dsa`, `block_sparse`. A vLLM 0.29.0 mirror also exists at `/tmp/vllm-0.29.0`; it was used as corroboration only, and every 0.29.0 claim in this file is backed by a raw.githubusercontent.com URL pinned to the `v0.29.0` tag.

Mechanical self-checks run before hand-off with the project's own checker (`python3 verify.py quote <url> "<quote>"`), all returning `{"verdict": "MATCH", "ratio": 1.0}`:
- Qwen3-4B config block vs `https://huggingface.co/Qwen/Qwen3-4B/raw/main/config.json`
- gpt_oss alternating-SWA fragment vs `.../v0.29.0/vllm/model_executor/models/gpt_oss.py`
- qwen2.py assert string literals vs `.../v0.29.0/vllm/model_executor/models/qwen2.py`
- `Sink Attention sink support (for StreamingLLM)` and the full `TRITON_ATTN` row vs `https://docs.vllm.ai/en/v0.29.0/design/attention_backends/`
- issue #43235 NSA-incompatibility paragraph, PR #21217 body, PR #38952 supersede note vs their GitHub pages (variant `gh-extract`)
- SGLang QSA SM120 comment block vs `.../sglang/main/python/sglang/srt/layers/attention/qwen_sparse_attn_backend.py`
- arXiv: 2501.15383, 2309.00071, 2405.07719, 2410.13276, 2407.02490, 2310.01889 (variants `page-text` / `html:<id>`)
- State re-reads by raw-HTML extraction (`"state":"..."`): #2860 OPEN, #2862 OPEN (no mergedAt), #50705 OPEN, #48168 OPEN, #43235 OPEN; `STATE-TOKEN: merged` for #21217, #39635, #5949; `STATE-TOKEN: open` for #54267; `STATE-TOKEN: closed` for #38952. Falsification attempt on B7.1: vLLM `main`'s `_is_fa4_supported()` still accepts only families 90/100/110, i.e. FA4-on-sm120 is still gated off upstream of 0.29.0.

## NEGATIVE FINDINGS

- **No "Sequoia: Scalable, Robust and Hardware-aware Design for Long-context LLM Inference" paper exists that we could find.** `python3 fetch.py arxiv 2402.12374` returns "Sequoia: Scalable, Robust, and Hardware-aware Speculative Decoding" — speculative decoding, i.e. owned by a sibling map and out of scope here. Queries tried: web_search "Sequoia Scalable Robust and Hardware-aware Design for Long-context LLM Inference arXiv", "\"Sequoia\" long-context LLM inference hardware-aware paper arXiv 2024", plus an arXiv API title query (`http://export.arxiv.org/api/query?search_query=ti:%22Sequoia%22`) which timed out twice. Recommend the gap-map drop the "Sequoia (long-context)" row entirely rather than citing 2402.12374.
- **No NSA / MoBA / Quest / MInference / SeerAttention / Double Sparsity implementation exists in either engine on the rig.** `grep -rli` over `/tmp/vllm-0.28.0/vllm/` and `/tmp/sglang-main/python/sglang/srt/` returns zero hits for seerattention, minference, moba, double_sparsity (and only false-positive substring hits for "quest"). Queries tried: the greps above plus web_search items 14–16. Sparse attention in the rig's engines is MLA/DSA/QSA-class and model-gated only.
- **No sm120 (CC 12.0) evaluation exists in any of the ~2024-25 sparse-attention papers read back.** Across the retrieved full texts there are zero occurrences of "Blackwell", "sm120" or "consumer"; the only RTX mention in the whole corpus is Quest's RTX 4090 (and Double Sparsity's A10G). Bonus: `grep -n "SM120\|SM121" /tmp/s7_attn_backends.txt` on the v0.29.0 attention-backend doc matches exactly one backend row (`FLASHINFER_MLA_SPARSE_SM120`), i.e. on the rig's vLLM the *only* documented sm120-specific attention backend is a sparse-**MLA** decode backend that Qwen3-4B cannot use.
- **vLLM's documented backend-priority tables have no SM 12.x branch at all** (only "Blackwell (SM 10.x)" and "Ampere/Hopper (SM 8.x-9.x)"), and the `b12x` attention backend that does target SM120/SM121 appears at `docs.vllm.ai/en/latest` but not on the v0.29.0 page. Queries: `grep -n "SM120 and SM121\|b12x" /tmp/s7_attn_backends.txt` (0 hits) vs `/tmp/s7_attn_latest.txt` (hit). Version-drift finding, not a fabrication.
- **`fetch.py` caveats found while working:** (a) `fetch.py arxiv` prints only the FIRST paragraph of multi-paragraph abstracts (hit on SeerAttention, MoBA, SnapKV) — use `fetch.py get https://arxiv.org/abs/<id>` when the abstract has several paragraphs; (b) `fetch.py get` HTML-strips any span between `<` and `>` and therefore mangles code containing `>=` or `->`; (c) GitHub *search* pages are unusable (see SEARCH LOG).
- **No verbatim human closure reason exists for vLLM #56702 ("SM120 sparse-MLA has no decode kernel for this shape"):** it is closed `stateReason: NOT_PLANNED` with only a non-substantive CC comment, so per brief rule 2 it is not recorded as a closure reason anywhere in this file. Its user report is nonetheless a useful sm120 pitfall: "ValueError: SM120 sparse-MLA has no decode kernel for this shape: num_tokens=2, num_heads=16, topk=1152, d_qk=512, page_block_size=32, model_type=1".
- **No maintainer statement was found that resolves whether FA2 is officially supported on sm120**: FA2's README supported-GPU list stops at "Ampere, Ada, or Hopper" while `setup.py` compiles `arch=compute_120,code=sm_120` (CUDA ≥12.8). Queries: `https://github.com/Dao-AILab/flash-attention/issues?q=sm120` / `?q=5090` / `?q=RTX+PRO+6000` (list pages inconclusive), plus issue #1638 and #2535 bodies.
