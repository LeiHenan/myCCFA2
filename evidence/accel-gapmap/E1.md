# E1 — NEVER-DISCUSSED (negative evidence): attention kernels/backends + compilation & CUDA graphs

Researcher E1. Axes: `attn_backend`, `compile_cudagraph`. Candidate source: `/tmp/ecand/E1.md`
(engine-self-admitted gaps harvested from the installed **vLLM 0.29.0** tree).
Local mirror `/tmp/vllm-0.29.0` was used only for corroboration; every SOURCE URL below is a
GitHub `v0.29.0` blob URL and every quoted line was **re-read against
`raw.githubusercontent.com/vllm-project/vllm/v0.29.0/<path>`** to confirm the line number matches
the tag (spot-checked for `dflash/speculator.py:127`, `piecewise_backend.py:170`,
`flashinfer.py:1942`, `flash_attn.py:980` — all exact).

Search method note: `https://github.com/search?...&type=issues` started returning **HTTP 429** after
~4 queries in this environment. All GitHub negative evidence below was therefore taken from the
**repo-scoped issue-list route** (`https://github.com/{repo}/issues?q=<query>`), which is server-
rendered, un-throttled, and honours `is:issue` / `is:pr` / quoted phrases — a nonsense-token control
(`is:issue zzqqxxyy nonsense token`) correctly returned *"No results / Try adjusting your search
filters."*, and a known-hit control (`is:issue mnnvl`) returned 12 genuine hits. Quoted exact-phrase
queries behave reliably; bare multi-word queries on that route match loosely (OR-ish) and were only
used as secondary sweeps.

---

### E1 | NEVER-DISCUSSED
GAP (engine's own words): "# PIECEWISE cudagraphs are not supported for dflash."
SOURCE: `vllm/v1/worker/gpu/spec_decode/dflash/speculator.py:127` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu/spec_decode/dflash/speculator.py#L127
WHAT IS MISSING: `DFlashSpeculator.init_cudagraph_manager()` can only resolve the draft to
`FULL_DECODE_ONLY` or `NONE`; there is no PIECEWISE path for the dflash draft query forward, so
whenever the draft attention group's `AttentionCGSupport` is below `UNIFORM_BATCH` — or the runner's
decode mode is not FULL — the whole dflash draft runs eagerly, with no way to opt into a piecewise
capture of its compiled regions.
QUERIES TRIED: `is:issue "PIECEWISE cudagraphs are not supported for dflash"` (NO RESULTS) ;
`is:pr "PIECEWISE cudagraphs are not supported for dflash"` (NO RESULTS) ;
`is:issue "PIECEWISE cudagraphs are not supported"` (NO RESULTS) ;
`is:issue piecewise cudagraph dflash` (24 results, none raise the limitation) ;
`is:issue "dflash" "cudagraph_mode"` ; `is:issue "DFlashCudaGraphManager"` ;
`is:issue "skip_attn" dflash` ; `is:issue "PIECEWISE cudagraph" drafter` ;
`web_search` "vLLM dflash PIECEWISE cudagraphs not supported draft decodes" (no thread) ;
`https://github.com/vllm-project/vllm/issues?q=is%3Aissue+piecewise+cudagraph+dflash`
ABSENCE CLAIM: I did not find it. Explicitly disclosed counter-evidence: the *string itself* does
appear once on GitHub — as a **quoted diff line inside the review thread of open PR #43340**
("[MRV2][Spec Decode] DFlash for MRV2", https://github.com/vllm-project/vllm/pull/43340), authored by
the PR author documenting their own code. It is not indexed by issue/PR search (the `is:pr` quoted
search above returns NO RESULTS), and no thread anywhere raises the limitation, asks for PIECEWISE
dflash graphs, or reports the eager fallback as a problem. Adjacent-but-different threads found:
#53031 (dflash `capture()` logs unconditionally when mode resolves to NONE), #45258 (RFC asking for
the *opposite*: FULL graphs for drafters that are PIECEWISE-only), #55581 (draft CUDA graphs lost via
a head-count arithmetic bug), #49547 (target-side FlashInfer→PIECEWISE downgrade). So this is a
"found no thread that raises it", not a "does not exist".
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the token "dflash" is new and heavily discussed, so any
raised complaint would surface; but the code string is present in PR #43340's thread, so the claim is
"never *raised*", not "never *written*".
TARGET-RIG RELEVANCE: yes — the rig runs a dflash2 drafter under vLLM 0.29.0 with CUDA graphs; the
default `cudagraph_mode=FULL_AND_PIECEWISE` will silently collapse the dflash draft to eager (mode
`NONE`) whenever the draft group is not at `UNIFORM_BATCH`.
EVIDENCE: READ BODY

---

### E2 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO(TheEpicDolphin): Support piecewise cudagraph for multi-module MTP."
SOURCE: `vllm/v1/worker/gpu/spec_decode/multi_module_mtp/speculator.py:99` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu/spec_decode/multi_module_mtp/speculator.py#L99
WHAT IS MISSING: the multi-module MTP speculator's `init_cudagraph_manager()` throws away a requested
piecewise mode, downgrading `PIECEWISE`→`NONE` (or `FULL_AND_PIECEWISE`→`FULL_DECODE_ONLY`); there is
no piecewise capture support for a multi-module MTP draft at all.
QUERIES TRIED: `is:issue "Support piecewise cudagraph for multi-module MTP"` (NO RESULTS) ;
`is:pr "Support piecewise cudagraph for multi-module MTP"` (NO RESULTS) ;
`is:issue "multi-module MTP"` (4 results: #54369 ROCm sparse-MLA, #53488 prompt_logprobs, #52688
usage question, #56701 — none about cudagraph mode) ; `is:issue "MultiModuleMTP"` (NO RESULTS) ;
`is:issue "multi-module MTP" piecewise` (1 result, unrelated) ;
`is:issue piecewise cudagraph "draft decode"` (unrelated MTP crash reports)
ABSENCE CLAIM: I did not find it. The TODO is author-tagged and the phrase is unique; a PR
implementing it would match the quoted search.
CONFIDENCE THE ABSENCE IS REAL: HIGH — distinctive verbatim marker, zero hits on any variant tried.
TARGET-RIG RELEVANCE: yes — multi-module MTP is a drafter path on this rig's spec-decode menu; the
forced downgrade removes piecewise graphs from the draft.
EVIDENCE: READ BODY

---

### E3 | NEVER-DISCUSSED
GAP (engine's own words): "Nested BreakableCUDAGraphCapture is not supported."
SOURCE: `vllm/compilation/breakable_cudagraph.py:162` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/compilation/breakable_cudagraph.py#L162
WHAT IS MISSING: `BreakableCUDAGraphCapture.__enter__` hard-raises on re-entry via a thread-local
guard, so a model whose forward already runs under a breakable capture (e.g. a drafter captured inside
the target's capture scope, or a nested compiled region) cannot be captured; there is no
composition/re-entrancy story for breakable captures.
QUERIES TRIED: `is:issue "Nested BreakableCUDAGraphCapture"` (NO RESULTS) ;
`is:issue "Nested BreakableCUDAGraphCapture is not supported"` (NO RESULTS) ;
`is:pr "Nested BreakableCUDAGraphCapture"` (NO RESULTS) ;
`is:issue "Nested" "BreakableCUDAGraphCapture"` (NO RESULTS) ;
`is:issue "BreakableCUDAGraphCapture"` (2 results: #56506, #46253) ;
`web_search` "vllm \"Nested BreakableCUDAGraphCapture\"" (only source-file mirrors, no thread)
ABSENCE CLAIM: I did not find it. #46253 is about a *different* breakable-cudagraph defect
(cross-node host-staged NCCL all-reduce captured, no GPUDirect, `capture_end` IMA) and never
mentions nesting; it is the only substantive breakable-cudagraph issue.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the class name is unique and every quoted variant is empty.
TARGET-RIG RELEVANCE: yes — single-node RTX PRO 6000 runs `VLLM_USE_BREAKABLE_CUDAGRAPH` whenever
torch.compile tracing is unavailable, and any nested capture (target + drafter) hits this raise.
EVIDENCE: READ BODY

---

### E4 | NEVER-DISCUSSED
GAP (engine's own words): "cudagraph_capture_sizes not supported in compile_sizes.This should be
handled in `post_init_cudagraph_sizes`."
SOURCE: `vllm/compilation/piecewise_backend.py:169-172` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/compilation/piecewise_backend.py#L169-L172
WHAT IS MISSING: `compile_sizes` cannot reference the `"cudagraph_capture_sizes"` sentinel — the
documented/config-level alias is rejected with a `NotImplementedError` inside `PiecewiseBackend`, so a
user cannot make the piecewise compile grid follow the CUDA-graph capture grid; the two grids must be
kept in sync by hand.
QUERIES TRIED: `is:issue "cudagraph_capture_sizes not supported in compile_sizes"` (NO RESULTS) ;
`is:issue "cudagraph_capture_sizes not supported"` (NO RESULTS) ;
`is:pr "cudagraph_capture_sizes not supported"` (NO RESULTS) ;
`is:issue "cudagraph_capture_sizes" compile_sizes` (3 PRs: #55095, #53743, #52816 — none about the
sentinel rejection) ; `is:issue "compile_sizes"` (12 hits, none raise it) ;
`is:issue "cudagraph_capture_sizes"` on `sgl-project/sglang` (unrelated) ;
`web_search` "vLLM cudagraph_capture_sizes not supported in compile_sizes"
ABSENCE CLAIM: I did not find it. The string is a hard-coded assertion message; the only web hit is a
scraped source mirror and an auto-generated error-index page
(`errors.standardbeagle.com/vllm-project/vllm/aot-compile-is-not-supported-by-the-current-config/`
style SEO page), which is not a discussion thread.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim marker, empty on both `is:issue` and `is:pr`.
TARGET-RIG RELEVANCE: yes — the rig's `FULL_AND_PIECEWISE` default makes piecewise compile sizes and
cudagraph capture sizes the two grids this assertion is about.
EVIDENCE: READ BODY

---

### E5 | NEVER-DISCUSSED
GAP (engine's own words): "aot_compile is not supported by the current configuration. Please make sure
torch.compile is enabled with the latest version of PyTorch"
SOURCE: `vllm/compilation/wrapper.py:162-168` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/compilation/wrapper.py#L162-L168
WHAT IS MISSING: the AOT-compile entry point (`VLLM_USE_AOT_COMPILE` path) raises `RuntimeError`
whenever the wrapped callable exposes no `aot_compile`, so ahead-of-time artifact generation silently
depends on a private `torch.compile` API and has no supported configuration-level fallback other than
abandoning AOT compile.
QUERIES TRIED: `is:issue "aot_compile is not supported"` (NO RESULTS) ;
`is:issue "aot_compile is not supported by the current configuration"` (NO RESULTS) ;
`is:pr "aot_compile is not supported"` (NO RESULTS) ;
`is:issue "aot_compile"` on `sgl-project/sglang` (2 unrelated hits) ;
`web_search` "\"aot_compile is not supported by the current configuration\" vllm"
ABSENCE CLAIM: I did not find it. Sole web hit is an auto-generated error-string index page, which
does not raise the issue.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim error string, empty on all quoted variants.
TARGET-RIG RELEVANCE: yes — AOT compile is the mechanism that would remove first-token compile stalls
on a single-GPU deployment; the rig cannot use it when the callable has no `aot_compile`.
EVIDENCE: READ BODY

---

### E6 | NEVER-DISCUSSED
GAP (engine's own words): "# torch._scaled_mm under torch.compile does not support 0-D scales"
SOURCE: `vllm/model_executor/kernels/linear/scaled_mm/pytorch.py:102` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/model_executor/kernels/linear/scaled_mm/pytorch.py#L102
WHAT IS MISSING: FP8 `scaled_mm` must defensively `view(1)` its per-tensor scales before calling
`torch._scaled_mm`, because Inductor cannot trace a 0-D scale operand; there is no compiled path that
accepts scalar (0-D) activation/weight scales, so a shape-correct graph is impossible without the
manual reshape.
QUERIES TRIED: `is:issue "torch._scaled_mm under torch.compile does not support 0-D scales"`
(NO RESULTS) ; `is:pr "0-D scales"` (0 parsed rows on the specific phrase) ;
`is:issue "scaled_mm" "0-D"` (5 hits, none about 0-D scales under compile) ;
`is:issue "torch._scaled_mm" zero-dim scales` on `pytorch/pytorch` (2 hits: #157950, #146414) ;
`is:pr "0-D scales"` (NO RESULTS) ; `web_search` "torch._scaled_mm 0-D scales torch.compile"
ABSENCE CLAIM: I did not find it. Counter-evidence checked and rejected: `pytorch/pytorch#157950` is
an RFC for a *public low-precision GEMM API* ("[RFC]: PyTorch Low-Precision GEMMs Public API") that
mentions `_scaled_mm` only as a private API to replace — it does not state or raise the 0-D-scale
tracing limitation.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the claim names a private PyTorch op, so an upstream
discussion would be plausible; but the specific 0-D-scale-under-compile phrasing is absent from both
repos.
TARGET-RIG RELEVANCE: yes — FP8 weight/activation paths are the standard quantized-decode setup on
Blackwell sm120 and this is the code path they go through.
EVIDENCE: READ BODY

---

### E7 | NEVER-DISCUSSED
GAP (engine's own words): "MRV2 PCP supports PIECEWISE CUDA graphs only."
SOURCE: `vllm/v1/worker/gpu/pcp_manager.py:162` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/worker/gpu/pcp_manager.py#L162
WHAT IS MISSING: the Model-Runner-V2 prefill-context-parallel manager raises `NotImplementedError`
whenever `cudagraph_mode.has_full_cudagraphs()`; full CUDA graphs are simply unavailable for
PCP-sharded batches, with no capture path or documentation of a workaround beyond
`-cc.cudagraph_mode=NONE`/PIECEWISE.
QUERIES TRIED: `is:issue "MRV2 PCP supports PIECEWISE CUDA graphs only"` (NO RESULTS) ;
`is:issue "MRV2 PCP"` (2 results: #54732, #47846) ; `is:pr "MRV2 PCP"` (PRs #53653, #56677 —
neither raises the full-cudagraph prohibition) ;
`is:issue "MRV2 PCP currently supports MLA models only"`-family review of the same guard block ;
`web_search` "vLLM MRV2 PCP supports PIECEWISE CUDA graphs only"
ABSENCE CLAIM: I did not find it. #47846 is the feature request *for* MRV2 PCP support and does not
discuss cudagraph modes; PR #53653 ("[Spec Decode] Support PCP-sharded MTP prefill", draft) also does
not.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the guard string is unique and every quoted variant is empty.
TARGET-RIG RELEVANCE: no — the rig is single-GPU with no context parallelism, so the guard is
unreachable there; recorded for completeness of the compile-mode coverage hole.
EVIDENCE: READ BODY

---

### E8 | NEVER-DISCUSSED
GAP (engine's own words): "# Currently only eager and inductor backend are supported. # for piecewise
compilation. Custom backends are not supported for piecewise compilation. Update when more backends
are supported."
SOURCE: `vllm/config/compilation.py:1027-1036` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/config/compilation.py#L1027-L1036
WHAT IS MISSING: `CompilationConfig` raises `ValueError("Invalid backend for piecewise compilation")`
for any `backend` other than `""`, `eager`, `inductor`, so a user-supplied custom compile backend
cannot be combined with piecewise compilation at all.
QUERIES TRIED: `is:issue "Custom backends are not supported for piecewise compilation"` (NO RESULTS) ;
`is:issue "Invalid backend for piecewise compilation"` ; `is:pr "Custom backends are not supported"` ;
`is:issue "piecewise compilation" "not supported"` (4 hits: #55581, #40821, #27499, #25277 — none
about custom backends) ; `is:issue "custom backend" piecewise compilation`
ABSENCE CLAIM: I did not find it. The restriction is a bare `ValueError` with no test or manifest
entry pointing at a tracking issue.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — "custom backend" is a generic phrase that could co-occur in
unrelated text; but the quoted sentence has zero hits.
TARGET-RIG RELEVANCE: yes — plugging a custom Inductor backend (e.g. a Blackwell-specific pass
pipeline) is impossible together with the rig's default piecewise compilation.
EVIDENCE: READ BODY

---

### E9 | NEVER-DISCUSSED
GAP (engine's own words): "as there are operations here not supported by torch.compile (for instance,
`.item()` in flash attention)"
SOURCE: `vllm/v1/attention/ops/vit_attn_wrappers.py:4-6` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/vit_attn_wrappers.py#L4-L6
WHAT IS MISSING: the vision-tower attention wrappers exist only because the underlying flash-attention
calls contain graph-breaking operations (`.item()`), so ViT attention cannot be compiled directly and
must be re-expressed as custom ops; there is no compiled flash-attention path for vision blocks.
QUERIES TRIED: `is:issue "operations here not supported by torch.compile"` (NO RESULTS) ;
`is:issue "torch.compile" "ViT attention"` (12 loose hits, none on this) ;
`is:pr "operations here not supported by torch.compile"` (NO RESULTS) ;
`is:issue "vit_attn_wrappers"` ; `web_search` "vllm vit_attn_wrappers torch.compile .item()"
ABSENCE CLAIM: I did not find it. Counter-evidence checked: only the docstring itself and file mirrors
surface.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the wrapper file exists and is maintained, so the limitation
is accepted rather than discussed; no thread states it.
TARGET-RIG RELEVANCE: no — Qwen3-4B on this rig is a text-only dense model with no vision tower; the
gap only bites multimodal ViT compilation.
EVIDENCE: READ BODY

---

### E10 | NEVER-DISCUSSED
GAP (engine's own words): "# TRTLLM prefill attention does not support BF16 Q and fp8 kv cache. So to
enable prefill attention with fp8 kv cache, we can construct a mock block and mock kv cache with BF16
KV involved in the prefill."
SOURCE: `vllm/v1/attention/backends/flashinfer.py:2219-2226` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L2219-L2226
WHAT IS MISSING: the TRT-LLM prefill kernel cannot consume a BF16 query together with an FP8 KV cache;
vLLM works around it by synthesising a mock BF16 KV cache for the prefill call. There is no kernel
path that accepts BF16-Q with fp8-KV directly, so the prefill runs against a fabricated cache.
QUERIES TRIED: `is:issue "TRTLLM prefill attention does not support BF16 Q"` (NO RESULTS) ;
`is:pr "TRTLLM prefill attention does not support BF16 Q"` (NO RESULTS) ;
`is:issue "TRTLLM prefill" "BF16" fp8 kv cache` ; `is:issue "does not support BF16 Q"` ;
`web_search` "vLLM TRTLLM prefill BF16 Q fp8 kv cache mock"
ABSENCE CLAIM: I did not find it. The mock-KV workaround is undocumented anywhere outside the source
comment; no issue questions its correctness.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim multi-clause sentence, zero hits on either route.
TARGET-RIG RELEVANCE: yes — `--kv-cache-dtype fp8` on Blackwell is the common rig configuration and
this is the prefill path that then runs against a mock cache.
EVIDENCE: READ BODY

---

### E11 | NEVER-DISCUSSED
GAP (engine's own words): "# TODO: Cascade attention doesn't work, disable it for now"
SOURCE: `vllm/v1/attention/backends/flashinfer.py:1723-1730` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L1723-L1730
WHAT IS MISSING: `FlashInferImpl.use_cascade_attention()` unconditionally returns `False` — the
cascade path in the FlashInfer backend is dead code, so shared-prefix (cascade) attention is
unavailable on FlashInfer regardless of configuration; the wrapper is built but never selected.
QUERIES TRIED: `is:issue "Cascade attention doesn't work"` (NO RESULTS) ;
`is:issue "Cascade attention doesn't work, disable it for now"` (NO RESULTS) ;
`is:pr "Cascade attention doesn't work"` (NO RESULTS) ;
`is:issue "does not support attention sinks" flashinfer` (1 hit: #30919) ;
`is:issue cascade attention flashinfer disabled` ;
`web_search` "vLLM FlashInfer cascade attention disabled use_cascade_attention"
ABSENCE CLAIM: I did not find it. Related-looking hits are about *other* cascade limits, not this
one: vLLM #14881 ("Cascade attention does not support sliding window", closed) and the FA-side
cascade asserts; #30919 is about attention sinks on FlashInfer, a different unsupported feature.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the TODO is explicit, author-written, and uniquely worded.
TARGET-RIG RELEVANCE: yes — FlashInfer is one of the auto-selected backends on sm120, and prefixed
multi-request decoding loses the cascade path entirely.
EVIDENCE: READ BODY

---

### E12 | NEVER-DISCUSSED
GAP (engine's own words): "FlashInfer non-causal attention is not supported with NVFP4 KV cache."
SOURCE: `vllm/v1/attention/backends/flashinfer.py:1118-1122` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L1118-L1122
WHAT IS MISSING: the non-causal (bidirectional) FlashInfer prefill wrapper has no NVFP4-KV-cache
variant — the combination raises `NotImplementedError`, so a drafter that needs non-causal draft
attention cannot be paired with an NVFP4 KV cache on FlashInfer.
QUERIES TRIED: `is:issue "non-causal attention is not supported with NVFP4"` (NO RESULTS) ;
`is:issue "FlashInfer non-causal attention is not supported with NVFP4 KV cache"` (NO RESULTS) ;
`is:pr "non-causal attention is not supported with NVFP4"` (0 parsed rows on the phrase) ;
`is:issue "NVFP4" "non-causal"` ; `is:issue "using FlashInfer for draft model non-causal attention"`
(3 hits: #55581, #51581, #47934 — none about the NVFP4 restriction) ;
`web_search` "FlashInfer non-causal NVFP4 KV cache vLLM not supported"
ABSENCE CLAIM: I did not find it. #49011 (nvfp4 KV cache on SM120) enumerates the SM120 editing sites
and never mentions the non-causal path.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim sentence, empty on both routes.
TARGET-RIG RELEVANCE: yes — dflash-style drafters use non-causal draft attention, and NVFP4 KV is a
headline Blackwell feature; the intersection is exactly this raise.
EVIDENCE: READ BODY

---

### E13 | NEVER-DISCUSSED
GAP (engine's own words): "FlashInfer non-causal prefill is not supported with DCP yet."
SOURCE: `vllm/v1/attention/backends/flashinfer.py:1113-1117` and `:1341-1344` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L1113-L1117 ; https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L1341-L1344
WHAT IS MISSING: `_get_prefill_wrapper(causal=False)` and the forward-time guard both raise
`NotImplementedError` when `use_dcp` is set; the non-causal FlashInfer prefill wrapper has no
decode-context-parallel variant, so non-causal prefill and DCP are mutually exclusive on this backend.
QUERIES TRIED: `is:issue "FlashInfer non-causal prefill is not supported with DCP yet"` (NO RESULTS) ;
`is:pr "FlashInfer non-causal prefill is not supported with DCP yet"` (NO RESULTS) ;
`is:issue "non-causal prefill" DCP` ; `is:issue "non-causal" DCP flashinfer` ;
`web_search` "FlashInfer non-causal prefill DCP vLLM"
ABSENCE CLAIM: I did not find it. The DCP-related FlashInfer discussions found (#55581, the XQA/LSE
DCP note) concern decode LSE and head arithmetic, not the non-causal prefill wrapper.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim sentence repeated twice in-source, zero external hits.
TARGET-RIG RELEVANCE: no — DCP requires `decode_context_parallel_size > 1`, unreachable on a
single-GPU rig; recorded as a backend coverage hole.
EVIDENCE: READ BODY

---

### E14 | NEVER-DISCUSSED
GAP (engine's own words): "# NOTE(DefTruth): Currently, custom merge_attn_states CUDA kernel does not
support FP8 dtype for inputs, fallback to use Triton kernel."
SOURCE: `vllm/v1/attention/ops/merge_attn_states.py:119-123` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/ops/merge_attn_states.py#L119-L123
WHAT IS MISSING: the hand-written CUDA `merge_attn_states` kernel cannot take FP8 *inputs*, so any
prefix/suffix state merge with FP8 operands must divert to the Triton kernel; the CUDA path is only
usable when the inputs are FP32/FP16/BF16 and the FP8-ness lives purely in the output scale.
QUERIES TRIED: `is:issue "does not support FP8 dtype for inputs, fallback to use Triton kernel"`
(NO RESULTS) ; `is:issue "does not support FP8 dtype for inputs"` (NO RESULTS) ;
`is:issue "merge_attn_states"` (11 hits: #45500 integer overflow, #33097 FP8-output fusion feature,
#48650 Triton recompiles, #35792 MLA fusions, #15689, #27491, #33123, #18884, #48480, #44300, #50497 —
none states the FP8-input restriction) ;
`is:pr "merge_attn_states" fp8` (merged PRs #52796 LSE normalization, #52763 CI — unrelated) ;
`web_search` "vLLM merge_attn_states FP8 input fallback Triton"
ABSENCE CLAIM: I did not find it. Closest thread is #33097 ("Fuse FP8 output quantization into
merge_attn_states (DCP / cascade paths)", closed completed) — that is the FP8 *output* feature, which
the source comment says both kernels already support via `output_scale`, and it does not mention the
FP8 *input* limitation.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the module is discussed, so a maintainer may consider the
FP8-input limit known; but no thread states it.
TARGET-RIG RELEVANCE: yes — the merge path is exercised by chunked-prefill/cascade decoding with FP8
KV on the rig's flash-attention-backed setup.
EVIDENCE: READ BODY

---

### E15 | NEVER-DISCUSSED
GAP (engine's own words): "TritonAttentionDiffKVBackend does not yet support quantized KV cache (got
kv_cache_dtype=...)" / "TritonAttentionDiffKVBackend does not support per-token-head quantization." /
"TritonAttentionDiffKVBackend does not support chunked attention with lookback." /
"fused output quantization is not supported for TritonAttentionDiffKVImpl"
SOURCE: `vllm/v1/attention/backends/triton_attn_diffkv.py:113-127` and `:175-179` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/triton_attn_diffkv.py#L113-L127 ; https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/triton_attn_diffkv.py#L175-L179
WHAT IS MISSING: the DiffKV (asymmetric QK/V head-dim, packed cache) Triton backend implements only
decoder self-attention with unquantized KV and no chunk lookback, and cannot fuse output
quantization; four separate capability holes in one backend.
QUERIES TRIED: `is:issue "TritonAttentionDiffKVBackend does not support per-token-head"` (NO RESULTS) ;
`is:issue "does not yet support quantized KV cache"` (NO RESULTS) ;
`is:pr "TritonAttentionDiffKV"` (NO RESULTS) ; `is:issue "TritonAttentionDiffKV"` ;
`is:issue "does not support chunked attention with lookback"` (NO RESULTS) ;
`is:issue "fused output quantization is not supported for TritonAttentionDiffKVImpl"` (NO RESULTS) ;
`web_search` "TritonAttentionDiffKV vLLM"
ABSENCE CLAIM: I did not find it. The backend name appears only in source mirrors; there is no tracking
issue, no PR, and no user report.
CONFIDENCE THE ABSENCE IS REAL: HIGH — the class name is unique to this file and every quoted variant
returns nothing.
TARGET-RIG RELEVANCE: no — DiffKV targets models whose K and V head dims differ (e.g. MiMo-V2.5);
Qwen3-4B has symmetric head dims. Recorded because it is an attention-backend coverage hole on the
`attn_backend` axis.
EVIDENCE: READ BODY

---

### E16 | NEVER-DISCUSSED
GAP (engine's own words): "HPC attention only supports decoder attention" / "HPC attention does not
support ALiBi" / "HPC attention does not support logits_soft_cap"
SOURCE: `vllm/v1/attention/backends/hpc_attn.py:346-351` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/hpc_attn.py#L346-L351
WHAT IS MISSING: the Tencent `hpc-ops` attention backend accepts only decoder self-attention with
`head_dim == 128`, no ALiBi slopes and no logits soft-cap; encoder attention and ALiBi/soft-cap models
cannot use it at all.
QUERIES TRIED: `is:issue "HPC attention only supports decoder attention"` (NO RESULTS) ;
`is:issue "HPC attention does not support ALiBi"` (NO RESULTS) ;
`is:issue "HPC attention does not support logits_soft_cap"` (NO RESULTS) ;
`is:issue "HPC attention does not support"` (NO RESULTS) ;
`is:issue "hpc_attn" alibi` (NO RESULTS) ; `is:pr "HPC attention"` (PR #50980 merged — bf16 KV with
fp8 weights; PR #56706 — stale QK-norm weights; neither mentions the ALiBi/soft-cap/encoder exclusions)
; `web_search` "vLLM HPC attention does not support ALiBi logits_soft_cap"
ABSENCE CLAIM: I did not find it. The backend has active PRs, but they add dtype support, not the
excluded features, and no thread states the exclusions.
CONFIDENCE THE ABSENCE IS REAL: MEDIUM — the backend is niche and its PRs are recent; the specific
constraint sentences nevertheless have zero hits.
TARGET-RIG RELEVANCE: yes — `hpc-ops` is installable on Blackwell and is explicitly selectable via
`--attention-backend HPC_ATTN`; the rig's head_dim=128 dense decoder is exactly its supported case,
so the exclusions bound where it can be substituted.
EVIDENCE: READ BODY

---

### E17 | NEVER-DISCUSSED
GAP (engine's own words): "fused output quantization is not yet supported for FlashAttentionImpl"
SOURCE: `vllm/v1/attention/backends/flash_attn.py:978-981` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flash_attn.py#L978-L981
WHAT IS MISSING: the FlashAttention backend raises `NotImplementedError` whenever the attention layer
passes `output_scale`/`output_block_scale`, i.e. the FP8/block-FP8 output-quantization fusion is not
implemented for FA — the quantize step cannot be fused into the attention epilogue.
QUERIES TRIED: `is:issue "fused output quantization is not yet supported for FlashAttentionImpl"`
(NO RESULTS) ; `is:pr "fused output quantization is not yet supported for FlashAttentionImpl"`
(NO RESULTS) ; `is:issue "fused output quant" XQA` (NO RESULTS) ;
`is:issue "output_block_scale"` (hits #51581, #43498, #34759 — none about the FA fused-output-quant
raise) ; `web_search` "FlashAttentionImpl fused output quantization not supported vLLM"
ABSENCE CLAIM: I did not find it. #43498 ("Porting MLAAttnQuantFusionPass to manual fusion") is about
an MLA fusion pass, not this per-backend raise.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim error string, zero hits.
TARGET-RIG RELEVANCE: yes — with `--kv-cache-dtype fp8` the FA backend is a normal sm120 choice, and
the missing epilogue fusion forces a separate quantize kernel on every attention output.
EVIDENCE: READ BODY

---

### E18 | NEVER-DISCUSSED
GAP (engine's own words): "Encoder self-attention and encoder/decoder cross-attention are not
implemented for FlashInferImpl"
SOURCE: `vllm/v1/attention/backends/flashinfer.py:1773-1779` @ vLLM 0.29.0
URL: https://github.com/vllm-project/vllm/blob/v0.29.0/vllm/v1/attention/backends/flashinfer.py#L1773-L1779
WHAT IS MISSING: `FlashInferImpl.__init__` refuses any `attn_type` other than DECODER, so the
FlashInfer backend cannot serve encoder self-attention or encoder↔decoder cross-attention at all.
QUERIES TRIED: `is:issue "are not implemented for FlashInferImpl"` (NO RESULTS) ;
`is:issue "encoder/decoder cross-attention are not implemented"` ;
`is:issue "attention type" "not supported" backend` (1 slow-path hit #14062) ;
`web_search` "FlashInferImpl encoder self-attention not implemented vLLM"
ABSENCE CLAIM: I did not find it. The related-but-different threads are about *quantized* encoder
attention (#55048, "encoder attention rejects quantized KV cache only at request time") and about FA's
`quantization is not supported for encoder attention`; neither covers the FlashInfer attn_type refusal.
CONFIDENCE THE ABSENCE IS REAL: HIGH — verbatim multi-clause string, zero hits on either route.
TARGET-RIG RELEVANCE: no — Qwen3-4B is decoder-only, so this raise is unreachable on the rig; kept as
part of the attention-backend coverage inventory.
EVIDENCE: READ BODY

---

## DISCUSSED (therefore NOT Section E)

These were tested and **already have a thread**, so they are not Section E rows. Labels per S3b.

- `vllm/v1/worker/gpu/spec_decode/autoregressive/speculator.py:136` — "# PIECEWISE cudagraphs are not
  supported for draft decodes." → **OCCUPIED (open PR #39773)** "[Model Runner V2] Disable piecewise
  cudagraph mode fallback for eagle draft decodes"
  https://github.com/vllm-project/vllm/pull/39773 — the PR body states verbatim: "The eagle draft
  decodes are currently able to run in PIECEWISE mode. This is problematic for at least one attention
  backend (FlashInfer) ...". Also raised as an RFC in #45258 "[RFC]: FULL cudagraph support for
  spec-decode drafter chain steps (currently PIECEWISE-only ...)"
  https://github.com/vllm-project/vllm/issues/45258 (READ BODY).
- `vllm/v1/worker/gpu/pcp_manager.py` guard block context — dflash/eagle cudagraph-mode resolution is
  mentioned in #53031 "DFlash speculator `capture()` logs ... even when nothing is captured", which
  states "When `init_cudagraph_manager` resolves to `CUDAGraphMode.NONE` (draft attention does not
  support full cudagraphs, or the runner's decode mode is not FULL) ..."
  https://github.com/vllm-project/vllm/issues/53031 (READ BODY). Adjacent to E1; it describes the
  symptom, never the PIECEWISE prohibition.
- `vllm/config/compilation.py:1455` — "CUDAGraphMode.{...} is not supported with spec-decode for
  attention backend ..." → **OCCUPIED (open #49547)** "FlashInfer + spec-decode silently downgrades to
  PIECEWISE cudagraphs (−16% measured); warning understates the cost"
  https://github.com/vllm-project/vllm/issues/49547 (READ BODY, quotes the exact code branch).
- `vllm/compilation/backends.py:1084` — "# TODO(patchy): ngram gpu kernel will cause vllm torch compile
  cache errors." → **OCCUPIED (open PR #54905)** "[Bugfix][Spec Decode] Isolate ngram_gpu compile
  cache" https://github.com/vllm-project/vllm/pull/54905 (TITLE + STATE ONLY — found via
  `is:pr "ngram gpu kernel"`; the open PR is literally titled after this gap, so the gap is discussed).
- `vllm/compilation/passes/fusion/sequence_parallelism.py:519` — "Piecewise compilation is not
  supported because the residual tensor gets split across TP ranks" → **DISCUSSED (#25277, closed
  NOT_PLANNED)** "[Bug]: Sequence Parallelism and Async TP disabled by default"
  https://github.com/vllm-project/vllm/issues/25277 — body states verbatim: "sequence parallelism
  splits the residual tensor into smaller pieces, which breaks with piecewise compilation and dynamic
  shapes." (READ BODY).
- `vllm/v1/attention/backend.py:824-826` — "# TODO add support to more backends:
  https://github.com/vllm-project/vllm/issues/25584" → **DISCUSSED (code points at issue #25584)**
  https://github.com/vllm-project/vllm/issues/25584 ; see also #55757, #34759, #50682 matched by
  `is:issue "supports_quant_query_input"` (TITLE + STATE ONLY).
- `vllm/v1/attention/backends/flashinfer.py:1840` — "XQA does not support FP8/NVFP4 output, so require
  trtllm-gen (SM100+) here." and `flashinfer.py:960-967` — "Architectures with only fa2 (e.g. SM89,
  SM120) cannot consume FP8 queries" → **OCCUPIED (open #49011)** "[Feature]: nvfp4 KV cache on SM120
  — flashinfer ships the kernels, vLLM isn't wired to them"
  https://github.com/vllm-project/vllm/issues/49011 — body states the same dtype contract verbatim:
  "Q and O must be **model dtype** on this path (fa2 asserts no fp8-Q; XQA requires `output.dtype ==
  q.dtype`) — the current `get_q_data_type` returns fp8 for nvfp4 unconditionally, a trtllm-only
  assumption." (READ BODY). This is why the SM100-only FP8-Q/FP8-output gates are NOT claimed as
  never-discussed here.
- `vllm/v1/attention/backends/flash_attn.py:889-895` — "FA4's Blackwell head_size=256 kernel does not
  support local attention on encoder inputs" and `fa_utils.py:204-208` "FA4 on Blackwell does not
  support head_size=%d due to TMEM capacity limits" → **SOLVED/DISCUSSED** #51828 (closed), #47934
  (closed), #46367 (closed) matched by the quoted phrase (TITLE + STATE ONLY).
- `vllm/v1/attention/backends/flash_attn.py:1769-1773` — "Cascade attention does not support sliding
  window." → **SOLVED (#14881 closed completed)** "[Bug]: Gemma3 Batch Offline Inference Error:
  Cascade attention does not support sliding window"
  https://github.com/vllm-project/vllm/issues/14881 (READ BODY).
- `vllm/v1/attention/backends/flash_attn.py:1456` / `triton_attn.py:726` — "quantization is not
  supported for encoder attention" → **DISCUSSED (open #55048)** "[Bug]: encoder attention rejects
  quantized KV cache only at request time" https://github.com/vllm-project/vllm/issues/55048
  (READ BODY).
- `vllm/v1/attention/backends/flex_attention.py:1195-1222` — FlexAttention does not support
  alibi/softcap/kv-sharing/quantized-kv-cache → **DISCUSSED** #24358 "[Bug]: FlexAttention does not
  support sliding window yet." (closed), #25172 "[Feature]: FlexAttention + encoder_decoder support"
  (closed NOT_PLANNED), #25864 (closed) (TITLE + STATE ONLY).
- `vllm/v1/attention/backends/flashinfer.py:897-902` — "FlashInfer backend currently does not support
  attention sinks" → **DISCUSSED (#30919, closed NOT_PLANNED)** matched by
  `is:issue "does not support attention sinks" flashinfer` (TITLE + STATE ONLY).
- `vllm/v1/attention/backends/flashinfer.py:826` — "FlashInfer XQA decode does not support head_dim=%d"
  → **DISCUSSED upstream** flashinfer #2638 "XQA test failed when headdim=256, pagesize<64 on
  SM120(RTX PRO 6000 Blackwell/5090)" (closed) and flashinfer #4546 "XQA non-MLA head_dim=512 blocked
  by smem budget on SM120/121 (99KB cap)" (closed) — https://github.com/flashinfer-ai/flashinfer/issues/2638 ;
  https://github.com/flashinfer-ai/flashinfer/issues/4546 (TITLE + STATE ONLY).
- `vllm/v1/attention/backends/flashinfer.py:882-891` — SM90 sliding-window crash → the code itself
  cites https://github.com/flashinfer-ai/flashinfer/issues/3578 ; and vLLM #48495 "[Bug][Spec Decode]
  Drafter attention backend auto-selection picks FlashInfer on SM90 for a sliding-window drafter,
  hitting the guard crash" (TITLE + STATE ONLY).
- `vllm/env_override.py:462-467` — TorchInductor `get_raw_stream` workaround → the code cites
  https://github.com/vllm-project/vllm/issues/30905 (READ BODY of the source comment).
- `vllm/config/compilation.py:1245-1247` — "# TODO: Piecewise Cuda graph might be enabled if torch
  compile cache key issue fixed. See https://github.com/vllm-project/vllm/pull/25093" → the referenced
  PR exists and is **merged**: "[Bug] Fix torch Compilation Cache Hit Error"
  https://github.com/vllm-project/vllm/pull/25093 (TITLE + STATE ONLY).
- `vllm/compilation/breakable_cudagraph.py` general capture faults → **DISCUSSED (open #46253)**
  "[Bug]: Cross-node CUDA graph capture fails (illegal memory access at capture_end) — host-staged
  NCCL all-reduce captured by `breakable_cudagraph` on GB10 ..." (READ BODY); distinct from the nested
  capture raise in E3.

## SEARCH LOG (queries actually issued)

GitHub repo-scoped search route (server-rendered, un-throttled), `is:issue` unless noted.
`{repo}` ∈ {`vllm-project/vllm`, `sgl-project/sglang`, `pytorch/pytorch`, `flashinfer-ai/flashinfer`,
`Dao-AILab/flash-attention`}:

- `is:issue "PIECEWISE cudagraphs are not supported for dflash"`
- `is:pr "PIECEWISE cudagraphs are not supported for dflash"`
- `is:issue "PIECEWISE cudagraphs are not supported"`
- `is:issue "PIECEWISE cudagraphs are not supported for draft decodes"`
- `is:issue "Support piecewise cudagraph for multi-module MTP"`
- `is:pr "Support piecewise cudagraph for multi-module MTP"`
- `is:issue "multi-module MTP"` ; `is:issue "MultiModuleMTP"` ; `is:issue "multi-module MTP" piecewise`
- `is:issue "Nested BreakableCUDAGraphCapture"` ; `is:issue "Nested BreakableCUDAGraphCapture is not supported"`
- `is:pr "Nested BreakableCUDAGraphCapture"` ; `is:issue "Nested" "BreakableCUDAGraphCapture"`
- `is:issue "BreakableCUDAGraphCapture"`
- `is:issue "cudagraph_capture_sizes not supported in compile_sizes"`
- `is:issue "cudagraph_capture_sizes not supported"` ; `is:pr "cudagraph_capture_sizes not supported"`
- `is:issue "cudagraph_capture_sizes" compile_sizes` ; `is:issue "compile_sizes"`
- `is:issue "aot_compile is not supported"` ; `is:issue "aot_compile is not supported by the current configuration"`
- `is:pr "aot_compile is not supported"` ; `is:issue "aot_compile"` (sglang)
- `is:issue "torch._scaled_mm under torch.compile does not support 0-D scales"`
- `is:issue "scaled_mm" "0-D"` ; `is:issue "torch._scaled_mm" zero-dim scales` (pytorch) ; `is:pr "0-D scales"`
- `is:issue "MRV2 PCP supports PIECEWISE CUDA graphs only"` ; `is:issue "MRV2 PCP"` ; `is:pr "MRV2 PCP"`
- `is:issue "Custom backends are not supported for piecewise compilation"`
- `is:issue "piecewise compilation" "not supported"` ; `is:pr "Custom backends are not supported"`
- `is:issue "operations here not supported by torch.compile"`
- `is:issue "torch.compile" "ViT attention"` ; `is:pr "operations here not supported by torch.compile"`
- `is:issue "ngram gpu kernel will cause vllm torch compile cache errors"`
- `is:pr "ngram gpu kernel"` ; `is:issue "NGram" gpu kernel compile cache` ; `is:issue "ngram" "compile cache"`
- `is:issue "torch.compile cache" disabled`
- `is:issue "Piecewise Cuda graph might be enabled"`
- `is:issue "TRTLLM prefill attention does not support BF16 Q"`
- `is:pr "TRTLLM prefill attention does not support BF16 Q"`
- `is:issue "TRTLLM prefill" "BF16" fp8 kv cache` ; `is:issue "does not support BF16 Q"`
- `is:issue "Cascade attention doesn't work"` ; `is:issue "Cascade attention doesn't work, disable it for now"`
- `is:pr "Cascade attention doesn't work"` ; `is:issue "Cascade attention does not support sliding window"`
- `is:issue "non-causal attention is not supported with NVFP4"`
- `is:issue "FlashInfer non-causal attention is not supported with NVFP4 KV cache"`
- `is:pr "non-causal attention is not supported with NVFP4"`
- `is:issue "FlashInfer non-causal prefill is not supported with DCP yet"`
- `is:pr "FlashInfer non-causal prefill is not supported with DCP yet"`
- `is:issue "using FlashInfer for draft model non-causal attention"`
- `is:issue "does not support FP8 dtype for inputs, fallback to use Triton kernel"`
- `is:issue "does not support FP8 dtype for inputs"` ; `is:issue "merge_attn_states"` ; `is:pr "merge_attn_states" fp8`
- `is:issue "TritonAttentionDiffKVBackend does not support per-token-head"`
- `is:issue "does not yet support quantized KV cache"` ; `is:pr "TritonAttentionDiffKV"` ; `is:issue "TritonAttentionDiffKV"`
- `is:issue "does not support chunked attention with lookback"`
- `is:issue "fused output quantization is not supported for TritonAttentionDiffKVImpl"`
- `is:issue "HPC attention only supports decoder attention"` ; `is:issue "HPC attention does not support ALiBi"`
- `is:issue "HPC attention does not support logits_soft_cap"` ; `is:issue "HPC attention does not support"`
- `is:issue "hpc_attn" alibi` ; `is:pr "HPC attention"`
- `is:issue "fused output quantization is not yet supported for FlashAttentionImpl"`
- `is:pr "fused output quantization is not yet supported for FlashAttentionImpl"`
- `is:issue "output_block_scale is not supported when fusion has not happened"` ; `is:issue "output_block_scale"`
- `is:issue "are not implemented for FlashInferImpl"` ; `is:issue "encoder/decoder cross-attention are not implemented"`
- `is:issue "attention type" "not supported" backend`
- `is:issue "fused output quant" XQA` ; `is:issue "XQA does not support FP8/NVFP4 output"`
- `is:issue "Architectures with only fa2 (e.g. SM89, SM120) cannot consume FP8 queries"`
- `is:issue "cannot consume FP8 queries"` ; `is:issue "supports_quant_query_input"`
- `is:issue "FlashInfer XQA decode does not support head_dim"`
- `is:issue "FA4 on Blackwell does not support head_size"`
- `is:issue "quantization is not supported for encoder attention"`
- `is:issue "does not support attention sinks" flashinfer` ; `is:issue "FlashInfer backend on SM90 currently crashes with sliding-window attention"`
- `is:issue "FlexAttention does not support"` ; `is:issue "FlexAttention does not support quantized kv-cache"`
- `is:issue "per-token-head quantization"`
- `is:issue "supports_draft_decode_metadata_update"` ; `is:issue "Fused multi-step draft decode is not supported by attention"`
- `is:issue "Eagle currently only supports PIECEWISE cudagraphs"` ; `is:issue "Only supports PIECEWISE cudagraphs"`
- `is:issue "PIECEWISE cudagraph" drafter` ; `is:issue "draft decodes" cudagraph piecewise` ; `is:issue "dflash" "cudagraph_mode"`
- `is:issue "DFlashCudaGraphManager"` ; `is:issue "skip_attn" dflash`
- `is:issue "Per-sequence causal requires FA4"` ; `is:issue "Per-sequence causal"`
- `is:issue "set cudagraph_mode" attention backend` ; `is:issue "is not supported with spec-decode for attention backend"`
- `is:issue "CUDAGraphMode.FULL_AND_PIECEWISE is not supported"`
- `is:issue sm120 attention backend` ; `is:issue "SM120" "FP8" attention`
- `is:issue "vit attention" backend not supported`
- `is:issue piecewise cudagraph draft decode not supported` (sglang) ; `is:issue piecewise cudagraph draft` (sglang)
- `is:issue "cudagraph_capture_sizes"` (sglang) ; `is:issue "aot_compile"` (sglang)
- `is:issue XQA head_dim` (flashinfer) ; `is:issue "XQA" "fused output quant"` (flashinfer)
- `is:issue cascade sliding window` (flash-attention)
- `is:issue "FALLBACK_ALLOW_LIST"` (pytorch) ; `is:issue "_scaled_mm" "0-D" scales compile` (pytorch)
- `is:issue zzqqxxyy nonsense token` (negative control → "No results")
- `is:issue mnnvl` (positive control → 12 genuine hits)
- Issue/PR bodies read in full or in part:
  https://github.com/vllm-project/vllm/issues/45258 ; /issues/48234 ; /issues/49011 ; /issues/49547 ;
  /issues/53030 ; /issues/53031 ; /issues/53488 ; /issues/14881 ; /issues/55048 ; /issues/46253 ;
  /issues/55581 ; /pull/39773 ; /pull/43340 ; /pull/25093 ;
  https://github.com/pytorch/pytorch/issues/157950
- arXiv: `https://arxiv.org/search/?query=piecewise+CUDA+graph+speculative+decoding&searchtype=all&size=25`
  → "Sorry, your query for all: piecewise CUDA graph speculative decoding produced no results."
- arXiv: `https://arxiv.org/search/?query=CUDA+graph+capture+attention+backend+limitations&searchtype=all&size=25`
  → "Sorry, your query for all: CUDA graph capture attention backend limitations produced no results."
- arXiv: `https://arxiv.org/search/?query=CUDA+graph+capture+limitations+inference+serving&searchtype=all&size=25`
  → no result block returned (page contained no matching-results banner)
- web_search: "vLLM dflash PIECEWISE cudagraphs not supported draft decodes"
- web_search: "vLLM FlashInfer XQA FP8 NVFP4 fused output quant SM100 only"
- web_search: "vLLM MRV2 PCP supports PIECEWISE CUDA graphs only"
- web_search: "vLLM cudagraph_capture_sizes not supported in compile_sizes"
- web_search: "\"aot_compile is not supported by the current configuration\" vllm"
- web_search: "vllm \"Nested BreakableCUDAGraphCapture\""
- web_search: "vllm HPC attention does not support ALiBi logits_soft_cap"
- web_search: "vLLM merge_attn_states FP8 input fallback Triton"
- web_search: "FlashInfer non-causal NVFP4 KV cache vLLM not supported"
- web_search: "vLLM FlashInfer cascade attention disabled use_cascade_attention"

## NEGATIVE FINDINGS

- **GitHub `/search` endpoint is dead in this environment after ~4 queries.** `type=issues` returns
  `HTTP 429` (verified with `curl -o /dev/null -w "%{http_code}"`), which silently makes `fetch.py`
  retry for ~7 minutes per URL. All results above therefore come from the repo issue-list route
  `https://github.com/{repo}/issues?q=...`, which stayed at HTTP 200 throughout. The `.../pulls?q=...`
  route renders a client-side React shell that failed with "Uh oh! There was an error while loading
  this page" (0 Open / 0 Closed) — PR-side searches were therefore re-issued on the `issues?q=` route
  with an explicit `is:pr` term, which does render PRs server-side.
- **Auto-generated error-index pages are not discussions.** A `web_search` for
  `"aot_compile is not supported by the current configuration" vllm` returns
  `errors.standardbeagle.com/vllm-project/vllm/aot-compile-is-not-supported-by-the-current-config/`
  — a scraped error-string SEO page with no thread, author or replies. Treated as no evidence.
- **No arXiv paper discusses any of these engine-internal gaps.** Three arXiv full-text searches on
  piecewise CUDA graphs / CUDA-graph capture limits in serving returned "no results"; nothing on
  arXiv raises per-backend `not supported` combinations or piecewise-vs-full graph coverage.
- **No SGLang thread raises the analogue** of the E4/E5/E6 compile gaps: `is:issue "aot_compile"` and
  `is:issue "cudagraph_capture_sizes"` on `sgl-project/sglang` return only unrelated hits
  (#20372, #20269, #2470, #8179 — benchmarking/VLM profiling titles; no body read).
- **No flash-attention or TensorRT-LLM thread** was found for the cascade+sliding-window /
  fused-output-quant gaps: `is:issue cascade sliding window` on `Dao-AILab/flash-attention`
  → NO RESULTS; the FlashInfer-side XQA head_dim limits are discussed upstream (#2638, #4546) but the
  vLLM-side fallback messages (E-axis rows) are not.
- **FP8-input limitation of the custom `merge_attn_states` CUDA kernel**: no thread; the only nearby
  discussion (#33097) is about FP8 *output* fusion and is closed. Recorded as E14 with MEDIUM
  confidence for that reason.
- **PR-side phrase checks that came back empty on the vLLM repo** (via `is:pr` on the issues route):
  `"Nested BreakableCUDAGraphCapture"`, `"cudagraph_capture_sizes not supported"`,
  `"aot_compile is not supported"`, `"TritonAttentionDiffKV"`,
  `"fused output quantization is not yet supported for FlashAttentionImpl"`,
  `"Support piecewise cudagraph for multi-module MTP"`, `"Cascade attention doesn't work"`.
- **Rows deliberately DROPPED from this file** after the test (not enough separation from existing
  threads, so they are neither NEVER-DISCUSSED nor clean DISCUSSED): the ROCm encoder-decoder CUDA-graph
  gate (`vllm/config/model.py:1349-1358`, ROCm-only and out of rig scope); the `MambaAttention`
  decode-cudagraph buffer TODO (`vllm/v1/attention/backends/mamba_attn.py:127`, hybrid-Mamba subject);
  `vllm/v1/worker/gpu_model_runner.py:4024` ("Refactor how we capture cudagraphs", too vague to make a
  falsifiable absence claim); `flashinfer.py:1942` (`output_block_scale` assertion, adjacent to the
  open #43498 MLA-fusion feature request); `flash_attn.py:1141-1146` ("Per-sequence causal requires
  FA4", adjacent to #46914).
