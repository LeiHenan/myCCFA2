# S0 立项准备（Intake） — 跨引擎指标定义一致性（投机解码/服务度量）

**候选**：`engine-metric-conformance` ｜ **成本上限**：0 GPU·h
**目标**：把『想做什么』换成『在什么约束下做什么』；约束不量化就不许往下走。

## 目标与决策人

3–4 周内拿到**决策级 Go/No-Go**。决策人：用户（已选策略②：接受「有人在做」，我们写清差异轴做强）。

## 会议与截稿

本轮截止 = 决策级结论（21–28 工作日）；最晚必杀 = 第 10 个工作日。

## 算力预算

现役 **1×RTX PRO 6000 Blackwell 96 GB**（`connect.westd.seetacloud.com:11640`，driver 580.82.09）+ 可租 2–8 卡。**本方向单卡足够**。主体是**测量**：在 2 个引擎上跑同一负载 ⇒ ≤3 GPU·h。

## 时间预算

21–28 工作日；第 10 工作日为最晚必杀日。

## 可用资产

① **vLLM 0.29.0 已装可用**（`/root/ccfa_venv`）＋ Qwen3-4B + dflash2 drafter 已在盘；② **SGLang 源码快照**（`prior_art_fetch/sglang.tar.gz`）与 CUDA 13 兼容结论（0.5.19）；③ **本轮已造好的仪器**：`probes/p08-specbench/check_consistency.py`（五条恒等式自检器，含 2 次仪器 bug 修复经验）、`run_groundtruth.sh`（24 格对照，只花 0.3 GPU·h）；④ 已入库的 24 格 ground truth 数据。

## 已知不可做

① TensorRT-LLM / 闭源引擎（安装体积与许可风险）；② 多机集群；③ 需要生产 trace；④ 纯 CUDA 内核工作。

## 零算力降级路线

**允许**：即使不装第二个引擎，也可先从**文档与源码**两侧做定义对照（Dynamo `metrics-comparison.md`、AMD `forced_acceptance_length`、各引擎 metrics 源码），给出「定义差异表」作为零卡交付；但**判定必须靠实测**（见杀判据）。

## 完成清单

> 校验器逐条比对下面的文本；全部 `[x]` 才算该阶段完成。

- [x] 算力预算写成『GPU·小时 + 显存下限 + 卡型约束（驱动/CUDA/引擎版本）』，全部有数字
- [x] 时间预算写成工作日，并标注最晚『必须杀』的日期
- [x] 可用资产逐项列出（引擎与版本、模型族、数据集、已有代码/补丁、可用的真机）
- [x] 写明本轮**明确不做**什么（防止范围蔓延）
- [x] 写明零算力降级路线（测量论文 / 理论 / 复现性研究），或明确拒绝降级

> **杀出口**：算力 = 0 且不接受测量/理论降级路线 ⇒ 停止（选题阶段就结束）
