# S0 立项准备（Intake） — 投机解码/MTP 服务评测的方法学

**候选**：`specbench-methodology` ｜ **成本上限**：0 GPU·h
**目标**：把『想做什么』换成『在什么约束下做什么』；约束不量化就不许往下走。

## 目标与决策人

3–4 周内拿到**决策级 Go/No-Go**；若 Go，再按 MLSys 2027 时间线补论文级。决策人：用户。

## 会议与截稿

本轮截止 = 决策级结论（21–28 工作日）；**最晚必须杀 = 第 10 个工作日**（S4 之前不许开跑正式实验）。

## 算力预算

现役 **1×RTX PRO 6000 Blackwell 96 GB**（driver 580.82.09 / vLLM 0.29.0 / torch 2.13.0+cu130），实例 `connect.westd.seetacloud.com:11640`；**可租 2–8 卡**。闸门：零卡/量级取证 ≤1 GPU·h；复现实验 ≤3 GPU·h；超过 8 GPU·h 先问。**本方向单卡足够**。

## 时间预算

21–28 个工作日。第 10 个工作日为最晚必杀日。

## 可用资产

① vLLM 0.29.0 可读源码可打补丁（E1 已证明）；② **已有 `#6` 期间采的 120 格 A1 + A3 逐次原始数据**（四条伪影的直接证据，零成本复用）；③ `vllm bench serve` + 自研机制分解器（`analyze_mechanism.py`）；④ Qwen3-4B target 在盘上（8.5 G）；drafter 需重下（2.7 G，aria2c 走 hf-mirror）；⑤ 预登记/决策日志/multi-gpu 可选。

## 已知不可做

① 需要专有生产 trace 的结论；② 需要跨厂商 GPU 横向对比（只有 NVIDIA 一张卡）；③ 纯 CUDA 内核工作；④ 论文级全网格（本轮只到决策级）。

## 零算力降级路线

**允许**：四条伪影里有三条可直接从**已入库的 120 格逐次数据**复算（A2/A3/A4），无需任何 GPU ⇒ 即使零算力也能给出决策级结论。

## 完成清单

> 校验器逐条比对下面的文本；全部 `[x]` 才算该阶段完成。

- [x] 算力预算写成『GPU·小时 + 显存下限 + 卡型约束（驱动/CUDA/引擎版本）』，全部有数字
- [x] 时间预算写成工作日，并标注最晚『必须杀』的日期
- [x] 可用资产逐项列出（引擎与版本、模型族、数据集、已有代码/补丁、可用的真机）
- [x] 写明本轮**明确不做**什么（防止范围蔓延）
- [x] 写明零算力降级路线（测量论文 / 理论 / 复现性研究），或明确拒绝降级

> **杀出口**：算力 = 0 且不接受测量/理论降级路线 ⇒ 停止（选题阶段就结束）
