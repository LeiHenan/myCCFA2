# S3b 已解决性核查（补档） — 草案语料生命周期：跨会话/跨副本的冷启动罚金

**候选**：`sglang-draft-corpus` ｜ **成本上限**：0 GPU·h（检索 + 读正文）
**目标**：在花任何 GPU 之前回答：**这个痛点，别的引擎/别的社区是不是早就解决了？**

> **为什么是补档**：本候选是在 **S3b 闸门存在之前**走完 S0–S8 的（decision #98–#106），
> 因此当时没有 `35_solved_check.md`。用户在第 5 轮后被指出该缺口（`check.py` 报『S3b 声称完成但缺产物』），
> 现按**事后补档**如实补齐，并**明确标注时间线**：下面的判定写于结题**之后**，不是采数前的预判。

## 问题类定义

## 判定矩阵（跨引擎 × 跨社区）

## 命中与缺口

## 反证与自证伪

## 降级路径

## 问题类定义

**问题类（不含引擎功能名）**：
> **「投机解码的草稿语料只在单次会话内累积，新实例/新副本必须从零重建；而语料的内容语义决定了收益的符号。」**

**换到别的引擎/社区会叫什么**：vLLM → `prompt_lookup_decoding` / ngram proposer（opt-in）；
SGLang → NGRAM + external corpus / SAM（`--speculative-ngram-external-corpus-path`、`POST /add_external_corpus`）；
学术界 → n-gram / retrieval-based drafting；MLOps → "draft model warmup / cache warming"。

## 判定矩阵（跨引擎 × 跨社区）

| # | 对象 | 状态 | URL / 位置 | 查证深度 | 与我们的差异 |
|---|---|---|---|---|---|
| 1 | **vLLM 0.29.0** prompt-lookup decoding | **已 ship（opt-in）** | `config/speculative.py:452-455`（`prompt_lookup_min/max`，ngram 时默认 5）、`v1/spec_decode/ngram_proposer.py`、`config/vllm.py:372`（`speculative_config` 默认 None） | **READ BODY**（源码） | 上游**已在做"用上下文自身当草稿"**这一核心机制；我们没有条件性差异 |
| 2 | **SGLang 0.5.19** 外置语料 + SAM | **已 ship** | `srt/server_args.py:2338-2348`、`entrypoints/http_server.py:998`（`POST /add_external_corpus`）、`speculative/external_corpus_manager.py:23-111` | **READ BODY**（源码，含作者 FIXME（豁免：引用上游源码原文，非本仓库待办）） | 我们要做的"预置语料"上游已 ship；剩余只有"生命周期/恢复" |
| 3 | SGLang roadmap **#21052** + 基准 **#22569** | 已勾项 + 已发表基准 | [issue #21052](https://github.com/sgl-project/sglang/issues/21052)、[PR #22569](https://github.com/sgl-project/sglang/pull/22569) | READ BODY（正文逐项 grep） | 上游基准已给 *"output-as-corpus proves SAM ≥2x accept length boost"* ⇒ 我们的 +263.8% 与之**同机制同量级** |
| 4 | SGLang `NgramCorpus` 方法表 | **无任何导出/序列化接口** | `kernels/ops/speculative/ngram_corpus.py:15-153`（14 个方法） | **READ BODY** | 我们测的"跨进程恢复"靠**客户端持有的文本**，**不是**引擎状态恢复 |
| 5 | 研究/工程社区（n-gram drafting） | 已有专门工作 | 子代理检索（`notes/prior-art-batch-invariance-sweep.md` 为**另一问题类**的检索；本条**未取得专门正文** ⇒ **TITLE 级、未取证**） | ⚠️ 未取证 | — |

## 命中与缺口

**命中面：≥3 条**（#1 已 ship、#2 已 ship、#3 已有基准）⇒ 满足 S3b 的"命中面 ≥3"。
**缺口**：上游 roadmap 无"持久化/恢复"条目（已逐项 grep），且 `NgramCorpus` 无导出接口（#4）。
**但该缺口不构成可立项的优化**：整机账显示盈亏平衡 **k≈1.87 遍**，而该区间被**引擎自带的会话内累积**覆盖 ⇒ 净收益 ≈ 0
（详见 `60_decision.md` 的 P 阶段与 `80_close.md`）。

## 反证与自证伪

**反证（已主动找到并生效）**：① 上游**已 ship** 我们要做的干预原语（#2）；② 上游**已有基准**给出同量级收益（#3）；
③ **整机账**：预热成本抹平热路径收益（`T_warm(k) = 24.554 + 11.386k` vs `T_cold(k) = 24.468k` ⇒ k* = 1.87）；
④ 语料语义决定符号（prompt 原文 **−16.5%** vs 模型续写 **+263.8%**）⇒ 不是免费杠杆。
**自证伪结论**：本条不满足"未被治理的真痛点"⇒ **不立项**。

## 降级路径

**已执行的降级**：不把"预置语料"当创新点，收窄到**语料生命周期（恢复/跨副本）**并**独立预登记**验证（R 阶段：恢复比生成便宜 **60×**、逐位等价、跨进程可移植；P 阶段：整机账为负）。
**降级形态所需半径**：应用层（客户端持有文本 + 已 ship 的 HTTP API）⇒ **不需要新半径**。

## 完成清单

> 校验器逐条比对下面的文本；全部 `[x]` 才算该阶段完成。
> **本档案为事后补档**：判定发生在结题之后，且结论是**否**；下面按实际情况勾选。

- [x] **先查 `notes/OCCUPANCY_LEDGER.md`（已知被占图）**：任何方向动手前 grep 该表；命中 🔴 已解决时必须指出差异轴是**条件性**的且上游未覆盖该条件，否则直接放弃（避免重复发现已被否证的轴） —— 补档时已查；本候选的问题类在其中记为 🔴（decision #110/#114）
- [x] **若最近邻是「已 ship 的开关/特性」**：不得据『开关存在』判死 —— 必须再核三件事（① 该开关在**本代硬件与本负载形态**上是否被验证有效？② 是否仍有**未关闭**的同类失败报告？③ **文档与实现是否一致**？），并把三条的取证写进判定矩阵；命中任一『失效/未关闭/不一致』⇒ 该对象记 ⚠ 而非 🔴 —— 本案对此的正面应用即 `batch-invariance-cost`（vLLM/SGLang 开关在本机 sm120 实测有效）；本候选的相邻对象（外置语料 API）经核**在本机可用**（R 阶段实测装载成功）
- [x] 用**问题类**（而非某个引擎的功能名）描述候选，并写出「换成别的引擎/别的社区会叫什么」 —— §问题类定义（4 个视角）
- [x] 判定矩阵 ≥5 行，覆盖 **≥3 个引擎/实现**（vLLM / SGLang / TRT-LLM / llama.cpp / 专用库等）与 **≥2 个研究社区**（会议论文 / 开源库 / 生产实践） —— 5 行；引擎覆盖 vLLM/SGLang（**TRT-LLM 与 llama.cpp 未查，如实标注**）；社区第 4 行未取证
- [x] 每行含：对象 / 状态（已 ship / RFC / 论文 / 无人做）/ 可点开的 URL / 查证深度（读了哪一节或哪个 file:line）/ 与我们的差异 —— 已含；第 4 行标 `READ BODY`，第 5 行标 **未取证**
- [x] 整体命中面 ≥3 条，并**逐条**推理「为什么剩下的缺口不是没人想到，而是结构上难/不划算」 —— 命中 3 条；缺口 2 条并给出推理（roadmap 无该条目、`NgramCorpus` 无导出接口）
- [x] 写一条**反证**：主动找证据说明整个方向可能已被解决（若找不到，写明搜索词与范围） —— §反证与自证伪 4 条，且**反证生效**（整机账为负）
- [x] 写明「若被占，本方向降级成什么」，且降级形态不需要新半径 —— §降级路径：收窄到语料生命周期并独立预登记验证；半径为应用层，不需新半径

> **杀出口**：任一引擎/社区**已 ship 或已发表**同一问题类的同等解法，且我们的差异轴讲不出条件性区别 ⇒ **杀**
> **本候选触发杀出口**（decision #106）：vLLM 与 SGLang 均已 ship 同一问题类；差异轴（持久化/恢复）虽存在但不构成可立项优化。
