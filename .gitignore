# ==== 冻结证据库（~2.2 GB）：不入库 ====
# 含 vLLM/SGLang 源码快照、嵌套 git 仓库、160 MB trace 数据库、DuckDB 二进制等。
# 这些是审计痕迹（引用行号、默认值、时间戳都依赖它们）——只读，勿改。
archive/
subfield_scan/
gptverify/
priorart/
prior_art/
prior_art_fetch/

# ==== 引擎检出（可改的工作副本）与实验产物 ====
upstream/*
!upstream/README.md

# 实验产物：**大块原始数据**不入库，但结论与**小体积可复算产物**必须入库
#   （2026-09-12 教训：规则过窄导致 t1.csv / ridge.csv / T2-verdict.md / VERDICT.md 被静默忽略，
#     `git add -A` 不报错、`git status` 也看不出来 —— 必须用 `git ls-files` 读回；
#     且实例上的 54 个 bench.json 已因同步 rm -rf 删除 ⇒ 逐次重复数据永久丢失）
results/**
!results/README.md
!results/.gitkeep
!results/**/
!results/**/summary.md
# 结论文档与**逐格**小产物（CSV/JSON 每格 1–2 KB，含逐次重复，可用于算 p50/p95）
!results/**/*.md
!results/**/*.csv
!results/**/*.bench.json
# 仍然忽略大块原始流（E1 的逐事件 JSONL 每格 ~3 MB、metrics 转储、服务日志）
results/**/*.e1.jsonl
results/**/*.metrics
results/**/*.serve.log

# ==== 数据与二进制 ====
data/
*.tar.gz
*.duckdb

# ==== 系统 / 编辑器 ====
.DS_Store
*.log

# ==== Python 缓存 ====
__pycache__/
*.pyc
# ==== Python 虚拟环境（服务器上的 venv 数以 GB 计，绝不能入库）====
.venv/
venv/
env/
