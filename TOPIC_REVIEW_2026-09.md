# 选题独立审核与立项台账（2026-09-14）

本文件是一次**独立选题会话**的结论归档：在「大模型推理加速」方向内，
用一个**可复现的证伪流程**筛掉 9 个候选，并给出 1 个通过全部闸门的课题。

**本文件的定位**：不是新的 pipeline，而是对 `PIPELINE.md` 的一次**实证演练记录** +
一份**外部方案的独立审核**。所有结论都带可复跑脚本。

---

## 一、外部方案独立审核

**被审对象**：一份未署名的调研《LLM Serving「超越 KV cache 的计算复用」竞争格局调研》（100 行）
**审核报告**：[`docs/reviews/review-computation-reuse-landscape-2026-09-14.md`](docs/reviews/review-computation-reuse-landscape-2026-09-14.md)

**审核结论**：

| 项 | 判定 |
|---|---|
| 该文档**自我否定**原始 gap 表述（"KV cache 只复用 prefix 不复用计算"是错误二分） | ✅ **独立复核后同意**——命中即跳过全层 forward，机制正确 |
| 抽检其关键引用 `arXiv 2601.08343`（judge 场景 KV 复用破坏决策不变性） | ✅ **已读摘要原文，转述准确**，且该文自称 "a previously overlooked failure mode" |
| Tier 2「非前缀 PIC 是红海」 | ✅ 与本工作区独立侦察一致（2026 年 7 篇 + SGLang RFC #30928） |
| Tier 3「hidden-state 粒度复用刚开头」 | ✅ 与本工作区独立侦察一致 |
| **遗漏 A**：它称"没人量化 1→N 分支里 prefix caching 省了多少" | ❌ **本工作区已量过**：`n=8` 单请求 0.9374s vs 8 个独立请求 1.0074s = **1.075×** ⇒ 引擎**已共享 prefill**。但该测量是**逐字节相同 prompt**，文档关心的是 **TTS 分支** ⇒ 建议 3 须**收窄到 TTS 形状**才有增量 |
| **遗漏 B**：其复用量分解缺 **block 粒度**这一层 | ❌ 实测：hybrid 模型**最小可缓存前缀 = 528 / 784 token**（hybrid 页对齐决定，短于此命中恒为 0）；且 prefix cache × spec-decode 交互**按 drafter 类别分化**（dflash 系命中归零、重算 100% prompt = 256×） |
| **遗漏 C**：它把"决策不变性"框在 judge/multi-agent | ❌ 本工作区实测：**引擎默认配置（前缀缓存开启）下 T=0 的确定性本身就不成立**（冷/热 5/8–7/8 输出不同）⇒ 入口应扩到"缓存状态即改变决策" |

**对三个建议的判定**：
1. **TTS 跨阶段复用 + 决策不变性** ⇒ 🟢 **最强**（原文支持 + 本工作区两条可复现同类证据）
2. **分支感知 checkpoint/rollback** ⇒ 🟡 可用，但立论需修正（HCache 的 6×/1.93× 是**特定恢复路径**的收益，不是普适"更省"）
3. **workload 量化先行** ⇒ 🟡 须收窄（同 prompt 形态已被量过，且结论是"已治理"）

---

## 二、通过审核并立项的课题

### `candidates/quantile-speedup/` — 加速技术的**分位数效应**

> 推理加速文献报"平均快 N 倍"，生产 SLO 签的是分位数。**这两件事会系统性背离。**

**受控实测**（n=40，同进程两引擎，交错 + 交替顺序，输出长度钉死）：

| 指标 | 比值(投机/基线) | 噪声底 |
|---|---|---|
| mean | **0.718** | 0.942 |
| p95 | **0.997** | 1.002 |
| p99 | **0.994** | 1.025 |
| CV | **8.95×** | 3.72× |

`tail_vs_mean = 1.389` vs 噪声 `1.064` ⇒ **效应超出噪声 33 个百分点**。
逐请求加速比跨越 **0.395–1.089**，**17.5% 的请求被拖慢**。

**这一条同时满足你工作区的全部闸门**：

| 闸门 | 结果 |
|---|---|
| 机制格未被占 | ✅ 最近邻全文关键词扫描：`p95`/`p99`/`tail latency`/`jitter`/`SLO` 命中 **0**；`percentile` 仅 3 次且全部用于 **generation length** 箱线图 |
| ≤2 卡 / ≤2 周 | ✅ 单卡可做 |
| 量级来自实测 | ✅ 效应超噪声 33pp |

**文件**：`00_intake.md`（约束量化）/ `30_occupancy.md`（占位与新颖性核查）/
`40_measurability_pilot.md`（pilot 证据）/ `60_topic_proposal.md`（**完整方案**）
**结果**：`results/quantile-speedup/2026-09-14/`（summary + 原始 JSON）

---

## 三、被判死/归档的候选（负面资产，避免重复劳动）

| # | 候选 | 死因 | 证据 |
|---|---|---|---|
| 1 | 结构化输出 mask 填充病态 | **我自己用端到端实验杀掉**：微基准 15.6×，真实服务 conc≤8 吞吐 **0.98–1.00×** | `probes/e2e_grammar_concurrency.py` |
| 2 | 前缀缓存**预测型**驱逐 | **有上界证明**：LRU 达 Belady opt 99.4–100%，任何预测器收益锁死 **≤0.5pp** | 独立复现（68,266 真实请求） |
| 3 | 缓存键基础 / 身份别名 | **行为级效应 = 0**（三层验证：代码级碰撞真实、计数级可达、行为级无后果） | `probes/e2_substantive.py` |
| 4 | 隐式不变量审计 | 枚举器只找到 **1** 个键路径站点（即已证明良性的那个） | `probes/enumerate_aliasing.py` |
| 5 | 推理期并行 `n>1` | **已被引擎治理**：`n=8` vs 8 个独立请求 = **1.075×** | `probes/e3_parallel_sampling.py` |
| 6 | CUDA graph 显存核算 | vLLM 0.28 **已 ship**（`VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS` 默认 True） | 源码 |
| 7 | 自适应投机长度 | 维护者两次**结构性拒绝** | vLLM PR #44885 / #43522 |
| 8 | 位置无关 KV 复用 | **7 篇 2026 论文 + 1 RFC** 在占 | `KV_cache_pain_points_2026-09.md` |
| 9 | 投机解码 × 前缀缓存 | 上游在修（#53670 / #54360 / #52244 / #50897） | 同上 |

### 三条被自己抓住的**假阳性**（方法学教训）

| # | 我犯的错 | 后果 |
|---|---|---|
| 1 | 用 64-token prompt（仅 4 个 cache block） | 阴性是**空的**——缓存根本没机会复用 |
| 2 | 用 token id **0** 做碰撞测试 | 0 正是 vLLM 的掩码哨兵 ⇒ 两臂**本就等价** |
| 3 | 判定脚本在 `interpretable=false` 时打印 `*** FALSE HIT ***` | **脚本假阳性**；若没抓住，整篇论文会建立在假阳性上 |

⇒ 固化为四条硬规则：**前提检查必须内建 / 不可解释时拒绝报阳性 / 哨兵值要当数据值怀疑 / 对照要覆盖两个方向**。

---

## 四、尚未立项但已验证的现象（备选）

**跨引擎键空间分叉**：vLLM 用**无类型裸拼接**（`lora + mm + salt + embeds`，三种哈希算法全部跨语义域碰撞），
SGLang 用**类型化 `RadixKey` + 版本号 + 长度前缀 + 存在性编码**。
两引擎各解决了不同子集，**没有一个 sound**。
行为级后果实测为零（见上表 #3）⇒ **不足以单独立项**，但作为"键基础审计"的工具与对照可复用。
文件：`跨引擎键基础审计_2026-09-14.md`（随本台账入库）。
