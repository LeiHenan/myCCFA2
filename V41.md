# V41 — 候选课题合集

**日期**：2026-09-11
**来源**：一轮完整的选题流程（22 个方向被提名并击杀）→ 对全部击杀理由的对抗性复审（4 个复审簇、47 条可分离的击杀理由）→ **38 条被判定为越界（over-reach）**，本文件是幸存候选的合并结果。

**这份文档是什么**：不是"题目"，是**可在 1–4 周内判定生死的 E0 探针**。每项都带预注册的 go/no-go 判据。
**执行时必读**：§5 的引用纪律——本轮 47 条击杀里 38 条错在同一个模式上（§1.2），而那个模式在后续工作里会重复出现。

---

## 1. 复审总账与核心教训

### 1.1 四簇复审结果

| 复审簇 | 击杀理由数 | 越界 | **有效** | 其他 |
|---|---|---|---|---|
| REA-1 投机解码 | 8 | **8** | **0** | 0 |
| REA-2 约束解码 + KV 内存层级 | 13 | **10** | **3** | 1 |
| REA-3 dLLM + 多 LoRA + 长 CoT 服务 | 16 | **14** | **1** | 1 |
| REA-4 早期十项 | 10 | **6** | **2** | 2 |
| **合计** | **47** | **38 (81%)** | **6 (13%)** | **4** |

### 1.2 核心失效模式

> **把"一个测量格"提升为"整个空间的性质"。**

四个实例，全部是同一错误：

| 我用错的量 | 它实际是什么 |
|---|---|
| "约束解码只剩 ≤6% 差距" | XGrammar 2 在 **BFCL-v3 tool-call JSON、RTX 5090、0.6–8B、batch 1** 上的单格；论文**根本没有 batch 维度**。同引擎的 CFGzip Table 3 实测 C++ **+50.7%**、Bython **+802.9%** |
| "KV layout 天花板 ≤1.23×" | 那是 **vAttention 的容量/碎片化收益**（连续虚拟内存，2024），不是带宽测量 |
| "jump-forward 只有 0.04%" | 那算的是 **mask 成本**，不是**跳过的 decode 步数**——范畴错误，不是界 |
| "流式服务相对调好的静态 γ 只有 1.20×" | StreamServe Table 8 是**消融表**，无 γ 列；"1.20" 在 HTML 里**只是 LaTeX 行 id** |

**另有一条元结论**（REA-2 逐字）：
> **"两簇里没有任何一条击杀成立于 V2——没有一条被筛选者自己对部署基线的测量所支持。每一个天花板都是论文自报、厂商博客，或建立在这些之上的算术。"**

⇒ **凡要判"量级不足"，必须自测。**

### 1.3 三个取证管道缺陷（会系统性坑后续工作）

| 缺陷 | 后果 | 规则 |
|---|---|---|
| **剥离 HTML 标签会静默删除 payload 里的评论** | "我重读了线程、没有撤回"这类结论**全部不可靠**。实测：`vllm_49548_issues.raw` 含作者撤回，剥离后的 `.txt` 里**没有** | 撤回检查必须对 **`.raw`** 做 `grep -a` |
| **`state="DRAFT"` 不等于已关闭** | 只识别 OPEN/MERGED/CLOSED 的枚举器会**低估 tracker 拥挤度**（方向 B 会把"其实很拥挤"判成"清空"） | 状态枚举必须含 DRAFT |
| **phantom ID 能通过"返回 200"的检验** | `sgl-project/sglang#52477`/`#55931` **不存在**；它们真实的身份是 **vLLM** 编号 | **必须测试你正在断言的那个 repo 限定形式** |

**状态码/URL 骗人已有 5 种形态**：TechRxiv 403、arXiv 软 404、index-page 软 404、**ar5iv 软回退到摘要页**（返回与摘要页 MD5 完全相同的 43,040 B）、payload-only 评论。

**另**：`stateReason: "COMPLETED"` 也可能是 **stale bot** 关闭造成的（SGLang `#30263` 实例）——必须再看**是谁关的**。
**再另**：GitHub roadmap 的纯文本导出会**剥掉复选框**——SGLang `#14199` 状态 `CLOSED/COMPLETED` 但 **41 项里有 23 项未勾**。必须从原始 HTML 解析 `<li class="task-list-item">`。

---

## 2. 候选总表

| # | 候选 | 成本 | 关键判据 | 证据强度 |
|---|---|---|---|---|
| **A1** | 约束解码 **dynamic × complex × concurrent** | **1 周，不改代码** | class-4 @batch≥32 **≤0.85×** 无约束 ⇒ 杀 | 实测差距 +50.7%→+802.9% |
| **A2** | **hybrid Mamba/GDN 下的前缀保留 × 投机** | 1–2 卡，2 周 | 必须自测；**全库零论文**，机制在 hybrid 类上被显式禁用 | 最强（零论文 + 源码级禁用） |
| **A3** | **rank-imbalance-aware dispatch + padding-aware all-to-all**（**非投机论文**） | 2 周 | 20% 消息体积削减转化不成 >9% 时间 ⇒ 杀 | 最硬（大规模实测 trace） |
| **A4** | **drafter 内部深度/宽度自适应** | 1–2 卡，2 周 | 已核验无人占据；185k 格子有 **4.4×** 实测损失 | 强（核验过的空缺 + 实测损失） |
| B1 | 逐请求**生成前**准入 + 单步内 ragged K | 2 周 | 打不赢 2-D (bs,ctx) 表 >10% goodput ⇒ 杀 | 中（量级**未知**） |
| B2 | **elastic prefix-aware 投机 KV 预留** | 8×H200，2 周 | 命中率与 goodput 变动 <5% ⇒ 杀 | 中（SGLang 每请求每步预留 8 slot，实际只需 1） |
| B3 | 长 CoT **检查点/恢复**（单实例引擎） | 1 卡，2 周 | 抢占次数 ≈0 ⇒ 杀 | 中（1.3–2×，需抢占率真实） |
| B4 | dLLM **去同步/流水线化 denoise 循环** | 1 卡，2 周 | f<5% ⇒ 杀；f≥10% ⇒ 可发表 | 中（1.12–1.4×） |
| B5 | **per-adapter KV 准入 + 驱逐策略** | 2 周 | per-adapter 命中率离散度 <5% ⇒ 杀 | 中（1.2–2× goodput） |
| B6 | **branch group 可撤销 / KV 记账正确** | 2 周 | — | 中（1.5–3×） |
| B7 | **recompute-vs-load 作为显式调度决策** | 2 周 | >5% 请求落在已 ship 默认的错误一侧 | 中（5–20% TTFT） |
| B8 | 草稿**放置**（prefill / decode / 拆分） | 2 节点×8 卡，2 周 | 打不赢最佳同址配置 >10% ⇒ 杀 | 中（#42109 POC 1.4× TPOT） |
| B9 | **block-aware 多节点放置**（两个已发表半边的组合） | 4 节点×8 卡 | 打不赢 Epoch+EPLB >10% ⇒ 杀 | 中（1.1–1.3×） |
| B10 | **server 端多租户保留准入** | 1 节点，≤2 周 | — | 中（28.4% 会话 → 98.65% 可避免成本） |
| C1–C9 | 九个便宜的辅助探针 | 合计约 1 周 | 见 §4 | 各异 |

---

## 3. A 级候选详述

### A1. 约束解码：dynamic × complex × concurrent

**增量**：**动态（逐请求）且复杂（C++/Python 级）且并发（batch ≥32）**的语法——**这个交集在五篇 2026 工作里是空的**。

**在先工作为何没覆盖**：
- **PSC**（arXiv `2608.03065`，ISSTA 2026）：在 batch 1→256 上解决复杂 PL 语法，mask 快 700×——但价格是 **"half to one minute per schema"** 预处理 + 每 schema **3 GiB（Llama 3 / Qwen2.5）到 6 GiB（Gemma 3）**。**这笔账对逐请求动态语法不可支付**，而那正是 XGrammar 2 的动机场景。
- **CFGzip**（`2605.29986`）：只支持离线，自述 *"negates the advantages in dynamic contexts requiring small, single-use CFGs"*。
- **Gram2Token**（ICML 2026）：只支持确定性语法。
- **XGrammar 2**（`2601.04426`，**MLSys 2025**）：唯一的动态引擎，但**从未离开过简单 JSON**。

**实测差距**（CFGzip Table 3，**同一个引擎**）：

| 语法类 | XGrammar2 vs 无约束 | 差距 |
|---|---|---|
| JSON（Llama-3B） | 3.60 / 3.42 | **+5.3%** |
| C++（Llama-3B） | 2.14 / 1.42 | **+50.7%** |
| **Bython（Llama-3B）** | 12.55 / 1.39 | **+802.9%** |
| Bython（Qwen-4B） | — | **+733.3%** |
| Bython（GPT-20B） | — | **+631.1%** |

**决定性实验**（vLLM/SGLang，**不改代码**）：
batch **1 / 8 / 32 / 128 / 256** × 四个语法类 **{BFCL JSON, 200 行 schema, C++ 子集, 50-schema 动态并集}** × spec-decode **开/关**。
**Go 判据**：**class-4 在 batch ≥32 时吞吐 ≤0.85× 无约束 ⇒ 进入**；否则杀。
**成本**：**1 周**。

> ⚠️ **场地轴的更正**：`REAUDIT` 已证 "目标系统会议 0 篇" 是**假阴性**——**XGrammar 就是 MLSys 2025**（`proceedings.mlsys.org/paper_files/paper/2025`，Dong/Ruan/Cai/Xu/Zhao/Lai/Chen）。已有 MLSys 先例说明**场地契合度更好，不是更差**。

---

### A2. hybrid Mamba/GDN 下的前缀保留 × 投机（**全库零论文**）

**这是本合集里唯一"零论文 + 机制在最高代价的模型类上被显式禁用"的格子。**

**机制在两个引擎里的真实状态**：

**(a) 一般机制已在 SGLang ship**（所以"lookahead-aware hashing"本身不是增量）：SGLang ship 的是**bigram radix key**——`radix_cache.py:59-60`（`is_bigram=True: token_ids holds raw tokens (N+1 for N bigrams)`）、键迭代 `(t[i], t[i+1])`、由 `kv_cache_builder.py:305 is_eagle=spec_algorithm.is_eagle()` 启用，**即"键承诺到下一个 token"**。SGLang 还把被拒草稿 KV 挡在 radix 树外（`kv_committed_len` 只按被接受的 token 前进）。

**(b) 但那条已 ship 的路径对 hybrid Mamba 模型是显式关闭的**：
```python
unified_tree_core.py:402
self.is_eagle = params.is_eagle and ComponentType.MAMBA not in components
```
**而那恰恰是边界丢弃代价最高的模型类**（一个对齐单元 ≈ 1k token）。

**(c) vLLM 那侧离落地远得多，且被接管**：`#50438` 是 ISSUE、OPEN，维护者 `ZJY0516` 当天自己 assign 并把 PR `#50897` 连了上去，**原 RFC 作者公开抗议反馈期未满**（*"would have appreciated coordination"*）。`#50897` OPEN、**needs-rebase**、17 commits/50 files、**11 位 code owner 处于 "Awaiting requested review"**，且有未解决的 reviewer 报的 bug（冷启动 prefill 空 hash 列表；两个 MooncakeStoreScheduler 崩溃）。另有**第二个 RFC `#52817` 独立提出同一想法**。

**(d) flag 是真的，但只是 JSON-only 实验开关**：`config/speculative.py:440` 默认 False；由 **PR `#53388`（MERGED 2026-09-01）** 引入；**没有顶层 CLI flag**。交互逻辑**不在** `gpu_model_runner.py`，而在 scheduler + KV coordinator。

**(e) 那个著名数字站不住**：`#43559` 已 CLOSED/COMPLETED。读完 **68 条时间线项**：`shaharmor98` 实测 **−0.67% / −0.67% / −2%**，并写道 *"I couldn't reproduce anything remotely close to the 20% degradation observed here"*；`dribnet` 实测 ≈**−4.8%**；报告者本人从未跟进。
> **不得引用 "~20%"。**
> 现象本身另有佐证：*"MTP + APC on hybrid models is still not production-safe on 0.26"*；TensorRT-LLM `#11533`：*"not production quality for hybrid models due to prefix caching and spec decoding missing"*。
> 另：那条 *"Comment retracted: measurement was invalid"* 在 **PR `#50897`** 上，**不在 `#43559`**——避免交叉归因。

**(f) 全库零论文**：11 次 arXiv API 查询，**五次精确短语查询在全库范围内返回字面零**——`all:"EAGLE" AND all:"prefix cache"` → **0**；`all:"radix attention" AND all:"speculative"` → **0**；等。
**该机制只存在于工程产物中**——这是支持"研究缺口"的论据，也是"已被占"这一击杀前提的反证。

**收窄后的增量（三条）**：**(i) hybrid full-attention + Mamba/GDN 类；(ii) 复用-vs-重算的保留策略；(iii) MLA。**

**实验**：hybrid 模型（Qwen3.5/3.6-Next 级），1–2 卡。
测：对 hybrid 类显式关掉边界丢弃后，**前缀命中率**与 **goodput** 的变动；对照 `freeze` 与 `disable_eagle_block_drop`（JSON-only）两种干预。
**注意**：`#53670` 的三臂消融就是跑在 **dual RTX 5090** 上做的——**硬件门槛很低**。

---

### A3. rank-imbalance-aware dispatch + padding-aware all-to-all

> **这已不是一篇投机解码论文。** 投机的那一半被独立证伪，机制本身独立成立。

**投机部分：失败。**
- 消息数轴**独立确认死亡**：batch 级 rank 并集在 **γ=0 时就已经饱和（127.9 / 127）**，比值 **1.00×**。原筛出的"1.16×"是算术，其"<1%"并不从它自己的数字推出（`0.20 × 0.16` 被标为"dispatch+combine 的 3%"，**量纲错**）。
- 扇出那一列**等于一个已发表的闭式**：`2607.05876v2` §3.4，`r = n(1−(1−1/n)^k)`，*"expected number of distinct expert ranks touched by a token's top-k selection"*。**零新颖性。**
- 成本模型类别也已存在：**TerraceMoE `2608.27874`**（per-peer 消息大小 + per-token 扇出 + dedup 配额，Hockney 形式）。
- ⚠️ **"投机改变路由"这一前提无任何来源支持**——Cascade 的 "3.5× unique expert activations" 是在 *"Assuming uniform random selection of experts by the router"* 下的解析值，**不得当作 skew 证据引用**。

**幸存机制（独立成立）**：

| 证据 | 数值 |
|---|---|
| 真实 trace 的 per-rank skew（DeepSeek-V3 decode，100k 请求 / 300h / 8×H100） | **5–25×**；Llama-4-Maverick GSM8K **>80×**；Qwen3-235B 2–9× |
| 不均衡 → 延迟（`2604.23150` Fig. 1，IF=128） | **3.28× 归一化解码层延迟** |
| EPLB 状态 | **两个引擎都默认关闭**，且以 **1000 步**为窗口 |
| 厂商与 SGLang 自己的文档 | *"Even with EPLB, some imbalance is inevitable"* |
| 现成 kernel 的行为 | **padding 到最大 rank 的载荷** |
| 已测的转化率 | 消息体积降 **20%**，但 **all-to-all 时间只降 9%** |

**决定性实验（两臂，≤2 周）**：
- **(i) 证伪投机前提** —— instrument `|⋃ dest_ranks|`、消息数、**每 rank 收发 token 数**，扫 γ × batch × EP，用真实路由；**每 rank 占用率与 p99 不随 γ 移动即杀掉耦合**。
- **(ii) 机制独立测试** —— 在人为 skew 下 A/B **padding/rank-balance 感知的 all-to-all** vs 现成实现；**若把 20% 消息体积削减转化不成 >9% 时间，即杀**。
- **臂 (ii) 在 (i) 失败时依然成立**——这是它比其它候选安全的地方。

---

### A4. drafter 内部深度/宽度自适应

**增量**：**drafter 自身网络的深度/宽度**（在 drafter 内部跳层/早退），**不是它的长度**——per request，在一次批量 draft pass 内。
**经核验的空缺**：AdaEDL、SpecDec++、Pacer、SVIP **全部只调长度/停止，从未调过 drafter 的深度**。

**对应的大格子（实测）**：**DFlash DT=4 在 ~185k 上下文下 16.0 tok/s，而关掉投机是 71 tok/s（4.4× 损失）**（`#54691`）；其中 drafter 的全上下文重扫占 **~182 ms 的 FA**。
已部署的控制是**一个 batch 级数字**，而自适应表**默认关闭**。

**注意与 self-speculation 的区分**：self-speculation（DEL/SpecBound/DSSD/LayerSkip）退出的是**目标模型自己的浅层**；本候选是**独立 drafter 的内部深度**——前者的机制（复用 verifier 的计算前缀）**在结构上无法迁移**到后者。

---

## 4. B/C 级候选与辅助探针

### 4.1 B 级要点

| 候选 | 增量与关键事实 |
|---|---|
| **B1 逐请求生成前准入 + ragged K** | 准入在**请求到达时、从解码前信号**决定，逐请求（非每 batch、非每 step）。**量级未知**——既无"3–16%"的复合值支撑（那是我自己的拼接），也无 <5% 的反证。基线阶梯：静态 K / 1-D 表 / **2-D (bs,ctx) 表** / **逐请求 oracle**（`#48944` 实测 2-D 表值 **1.29–1.36×**，且 `#54801` 正在加它 ⇒ **必须打赢 2-D 表**）。同部署跨 ctx：**+129% @2k → −51% @30.7k**（`#47602`） |
| **B2 elastic prefix-aware 投机 KV 预留** | 在一个预算下权衡前缀保留 vs 投机深度。**SGLang 每请求每步预留 8 个 slot，而实际只需 1**。实验：8×H200 + EAGLE-3，高并发多轮轨迹；A/B 静态 2× / 实际长度 / γ-收缩；**命中率与 goodput 变动 <5% ⇒ 杀** |
| **B3 长 CoT 检查点/恢复** | **SGLang 并未默认 ship 部分抢占**——`schedule_batch.py::release_req` 把 host-KV 备份门控在 `if get_disagg().disaggregation_mode == "decode" and offload_kv:`，默认 `"null"` ⇒ **完整重算，与 vLLM 相同**。算术：60k token 重算 ~20s @3k tok/s vs 240MB PCIe 往返 ~10ms ⇒ **1.3–2×**。实验：32–64 条并发 100k–500k token 会话，KV 池封顶，记录 `num_preemptions` |
| **B4 dLLM 去同步 denoise 循环** | **关键背景**：dLLM 的 7 个已合并机制**互不组合**——`dllm_hook.py` + `overrides.py::_dllm_overlap_disable` 对 dLLM **强制关闭** overlap scheduler、HiCache、LMCache、FlexKV、流水并行、LoRA、分离式（→null）、mixed chunked prefill、AMD 上的 CUDA graph。增量：`low_confidence.py` 每步 `done.tolist()` 同步 + `base.py::_run_sync` 的 Python 循环。实验：1 卡，LLaDA-8B / LLaDA2.0-mini，batch 1–32，CUDA event 测 h 与 t_fwd，**f = h/(t_fwd+h)；<5% ⇒ 杀；≥10% ⇒ 可发表**（1.12–1.4×） |
| **B5 per-adapter KV 准入/驱逐** | SGLang roadmap `#2929` 的 **21 个打勾项全部是适配器权重驻留/算子/API——没有一项关于 KV 池**；per-adapter prefix keying 只覆盖 **KEY**；**两个引擎都没有配额/分区/准入策略**。要注意：用来"杀掉"该缝的四篇（ForkKV/aLoRA/ICaRus/LRAgent）**全是"共享"机制**，不是配额策略。实验：32–128 适配器，Zipf 偏斜 + 每适配器 SLO，KV 受限；全局 LRU vs per-adapter quota；**命中率离散度 <5% ⇒ 杀**（1.2–2× goodput） |
| **B6 branch group 可撤销/记账** | TAPER 管的是**准入**，不是 fork 出去那部分 KV 的**记账**；SGLang 至今**直接 abort** beam group——源码逐字：*"Beam groups cannot be retracted, so they are aborted instead of being requeued."* **1.5–3×** |
| **B7 recompute-vs-load 显式调度** | vLLM `#53485` 的实际提案是 load-vs-**store** 的延迟 EMA 背压——**"recompute" 在它里面根本没有出现**。**符号会随投机解码翻转**。实验：同一前缀在并发 32–256 下测交叉点，spec on/off；**>5% 请求落在已 ship 默认的错误一侧 ⇒ 进**（5–20% TTFT） |
| **B8 草稿放置** | 靶心是 **vLLM `#42109`（OPEN，未合并）**《[RFC]: Disaggregated Speculative Decoding with Standalone Parallel Draft Model》，逐字 *"moves the draft model to a dedicated GPU (or pool of GPUs) reached over a fast interconnect"*，**POC 实测 "1.4× TPOT improvement vs co-located spec decode"**。**转移成本的算术被更正了 30×**：草稿 KV = **112 KiB/token**（Qwen3-0.6B）vs 目标 128–320 KiB/token，真实比值 **~1.14:1**，不是 80:1。实验：2 节点×8 卡 + RDMA；草稿放 prefill / decode / 拆分；**打不赢最佳同址 >10% ⇒ 杀**。⚠️ **不得主张"没人在 PD fabric 上搬运草稿状态"**——SwiftSpec `2506.11309` 与 StarSD `2601.21622` 已占；可辩护框架是"**在实测转移项下的成本模型 + 放置决策**" |
| **B9 block-aware 多节点放置** | **两个已发表半边的组合**：Epoch 已把 dLLM MoE 跑过真实 EP=8 组并构建 block plan，然后**明确拒绝**喂给 placement（*"Epoch leaves placement unchanged … This avoids a new hot-path load-balancing problem."*——全文唯一一次 `load-balanc*`）；CAP 已做跨节点共激活放置。⚠️ **CAP 里 "block plan" 出现 0 次；CAP 是 AR 侧的占位者**（`diffusion`=0、`dLLM`=0、`denois`=0、`autoregressive`=0）。TIDE `2605.20179` 给出最干净的第一方陈述：*"our work is currently limited to resource-constrained settings, e.g., single GPU-CPU systems … Extension of our work to multi-GPU or even multi-node is an important future avenue of research."* **1.1–1.3×**；基线阶梯终点是 **Epoch + EPLB（需自建）** |
| **B10 server 端多租户保留准入** | Khailo 全文 `GPU: 0`、`cluster: 0`——它是**客户端计时器**，无引擎/调度器/租户；TraceLab 只做观测。"外部性被命名 ≠ 机制被建出来"。实验：回放 `data/syfi_coding_trace.duckdb`（665,453 轮），有限 KV 预算，2–4 租户，LRU vs TTL vs gap-aware。**28.4% 的会话 → 98.65% 的可避免成本** |

### 4.2 C 级辅助探针（合计约一周）

| 探针 | 成本 | 判据 |
|---|---|---|
| **prefill CUDA-graph padding** | **<1 周，1 卡** | 记录每次 replay 的 `padded/real`；`_MAX_PREFILL_CUDA_GRAPH_PADDING_FACTOR = 2` |
| **FlashInfer radix 探针** | **1 周，1 卡** | Blackwell 上 FlashInfer 是默认后端，而 `attention_hook.py:570` 对其**静默** `disable_radix_cache=True`；vLLM 在 `attention.py` L370 有同样的镜像 |
| **受迫区间测量 → jump-forward** | 1 周 | 5.8µs 是 **mask 成本**，不是跳步收益；**f·N ≥ 4–6 步/区间 ⇒ 1.3–1.4×** |
| **byte 记账的 KV 准入** | ≤2 周 | 每步 bytes-read vs blocks-held；**≥10% 步决策分歧 且 e2e ≥30%** 才采纳 |
| **RefShape 重开** | 待定 | 税 **+34.35%**；门控比例 p<14.6% 时 <5%。TML 的 `speculative:` 命中为 **0**；LLM-42 自述 *"does not support sharing prefix caches… does not currently integrate with speculative decoding"*，且 `margin/top-1/top-2` 出现 **0** 次 |
| dLLM radix refresh | 2 周 | 1.4×（引擎实测）；`#18724` 只是初始版本、block 粒度，且**禁用 HiCache/LMCache/FlexKV**；HERALD 已占 |
| 异构 rank 统一分页 **+ 策略** | 2 周 | ~87% 适配器内存；LoRAServe 做的是 placement，不是 paging |
| drafter 侧 LoRA（联合 target+drafter） | 未测量 | `#52038` 只覆盖 DFlash 一个家族，且吞吐基准标注为 **PLANNED** |
| G4 thinking-cache 生命周期 | 1 周 | 默认 **False**（opt-in），语义是"只保留 prompt 前缀"（丢掉整个输出），且只识别**一个**前导 span |

---

## 5. 执行时必须遵守的规则（本轮血的教训）

### 5.1 引用纪律

> **来源只有在"取到正文、看到标题、并看到被引用的那句话"时才算数。**
> **状态码不构成证据。** 404 不证明不存在（版本化 DOI / 重命名 / 私有）；200 不证明存在（软 404 / 索引页 / ar5iv 回退）。

- **判定"不存在"前必须跑 Crossref 标题查询**（`api.crossref.org/works?query.bibliographic=<title>&rows=3`）。
- **撤回检查必须对 `.raw` 做 `grep -a`**，不得在被剥离的文本上做。
- **引用 issue 必须带 repo 前缀**（vLLM `#38392` ≠ SGLang `#38392`）。
- **必须测试你正在断言的那个 repo 限定形式**。
- **状态枚举必须含 `DRAFT`**；`COMPLETED` 还要看**是谁关的**（stale bot 不算）。
- **roadmap 复选框必须从原始 HTML 解析** `<li class="task-list-item">`。
- **引用任何数字前先写清：它测的是哪个量、在哪个区间、对哪个基线。**

### 5.2 判据框架（三方向）

任何候选缝必须**三个方向都清**：

> **A（文献 → 引擎）**：文献前沿相对已 ship 的引擎是超前、持平、还是落后？
> **B（引擎 tracker → 文献）**：同一决策上是否已有 ≥2 个并行开放提案？——**文献沉默 ≠ 无人竞争**。
> **C（扩展点）**：该决策是否已是公开 plugin/policy hook？——若是，在此处创新等于写插件。
>
> ⚠️ **A 的反转**：在**引擎原生位置**，**低论文数是警告信号，不是空缺**——那里的工作以 PR 形式落地。
> ⚠️ **一个 open PR 证明的是"引擎项目已经在跑"，不是"这个领域还空着"**。

### 5.3 预注册决策规则

1. E0 **必须先测量级**（而非先做机制）；**相对"部署中实际在跑的配置" <8% ⇒ 终止**。
2. 任一候选只靠"我没搜到"支撑 ⇒ 直接判死。
3. 出现 ≥2 个通过 E0 的候选 ⇒ 取量级最高者深入。
4. 全部未通过 ⇒ **停止提名机制，改为要求放宽约束**（硬件 / 时间 / 领域 / 贡献类型）。

---

## 6. 未决与未验证（不得当作结论）

| 项 | 状态 |
|---|---|
| **Libra**（ICLR 2026，OpenReview `WhxNwgGkAS`） | **UNRESOLVED**（OpenReview 403 拦截）。二手来源称其做"下一层专家激活的投机预测 + 热专家复制"，8×H200 |
| **TransKV** 正文 | **不可读**（Cloudflare 403 × 8 种方法，Wayback 0 快照）。元数据已验证存在（Crossref `/v1` 200、OpenAlex W7128772398），**仅元数据级** |
| **Loquetier** `2511.00101` 正文 | ar5iv fatal error + SSL EOF，仅摘要与 NeurIPS'25 场地 |
| **CoLoRA** | 产物是 **18 页幻灯片（4,401 字符）**，不是论文——数字不可审计 |
| SIGCOMM 2026《Balancing and Beyond…》`10.1145/3789240.3829201` | 付费墙，未取全文 |
| Dynamo 的 router 源码 | 仅厂商文档，非自测 |
| Mooncake `admission_threshold` | 仅文档 |
| llm-d GDS rollout versioning；KVBM `frequency>=2` 是否在 `main` | 未验证 |

---

**本文件是这一轮选题流程的唯一保留产物。** 其余材料（各复审报告、原始取证、引擎源码快照）已归档。
